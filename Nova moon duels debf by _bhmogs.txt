repeat task.wait() until game:IsLoaded() task.spawn(function() local Players = game:GetService("Players") local RunService = game:GetService("RunService") local _LP = Players.LocalPlayer local speedLabels = {} local function createSpeedBillboard(char) if not char then return end task.spawn(function() local head = char:WaitForChild("Head", 5) local humanoid = char:WaitForChild("Humanoid", 5) local root = char:WaitForChild("HumanoidRootPart", 5) if not head or not humanoid or not root then return end local old = head:FindFirstChild("PlayerSpeedBB") if old then old:Destroy() end local player = Players:GetPlayerFromCharacter(char) local isLocal = (player == _LP) local bb = Instance.new("BillboardGui") bb.Name = "PlayerSpeedBB" if isLocal then bb.Size = UDim2.new(0, 200, 0, 62) bb.StudsOffset = Vector3.new(0, 3.5, 0) bb.AlwaysOnTop = true bb.LightInfluence = 0 bb.MaxDistance = 1500 bb.Parent = head local speedLbl = Instance.new("TextLabel", bb) speedLbl.Name = "SpeedText" speedLbl.Size = UDim2.new(1, 0, 0, 36) speedLbl.Position = UDim2.new(0, 0, 0, 0) speedLbl.BackgroundTransparency = 1 speedLbl.Text = "Speed: 0.0" speedLbl.TextColor3 = Color3.fromRGB(255, 255, 255) speedLbl.Font = Enum.Font.GothamBlack speedLbl.TextSize = 20 speedLbl.TextStrokeTransparency = 0.3 speedLbl.TextStrokeColor3 = Color3.fromRGB(0, 0, 0) speedLbl.TextXAlignment = Enum.TextXAlignment.Center speedLbl.TextYAlignment = Enum.TextYAlignment.Center local dcLbl = Instance.new("TextLabel", bb) dcLbl.Name = "DCLabel" dcLbl.Size = UDim2.new(1, 0, 0, 22) dcLbl.Position = UDim2.new(0, 0, 0, 36) dcLbl.BackgroundTransparency = 1 dcLbl.Text = "gg/Mw35sjGtnD" dcLbl.TextColor3 = Color3.fromRGB(200, 200, 210) dcLbl.Font = Enum.Font.GothamBold dcLbl.TextSize = 11 dcLbl.TextStrokeTransparency = 0.4 dcLbl.TextStrokeColor3 = Color3.fromRGB(0, 0, 0) dcLbl.TextXAlignment = Enum.TextXAlignment.Center dcLbl.TextYAlignment = Enum.TextYAlignment.Center speedLabels[char] = { Label = speedLbl, Root = root, Humanoid = humanoid } else bb.Size = UDim2.new(0, 200, 0, 52) bb.StudsOffset = Vector3.new(0, 3.5, 0) bb.AlwaysOnTop = true bb.LightInfluence = 0 bb.MaxDistance = 1500 bb.Parent = head local bg = Instance.new("Frame", bb) bg.Size = UDim2.new(1, 0, 1, 0) bg.BackgroundColor3 = Color3.fromRGB(15, 15, 25) bg.BackgroundTransparency = 0.1 bg.BorderSizePixel = 0 Instance.new("UICorner", bg).CornerRadius = UDim.new(0, 12) local stroke = Instance.new("UIStroke", bg) stroke.Color = Color3.fromRGB(60, 60, 90) stroke.Thickness = 1.5 local avatarFrame = Instance.new("Frame", bg) avatarFrame.Name = "AvatarFrame" avatarFrame.Size = UDim2.new(0, 36, 0, 36) avatarFrame.Position = UDim2.new(0, 8, 0.5, -18) avatarFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 45) avatarFrame.BorderSizePixel = 0 Instance.new("UICorner", avatarFrame).CornerRadius = UDim.new(1, 0) local avatarStroke = Instance.new("UIStroke", avatarFrame) avatarStroke.Color = Color3.fromRGB(80, 80, 100) avatarStroke.Thickness = 1.5 local avatarImg = Instance.new("ImageLabel", avatarFrame) avatarImg.Name = "AvatarImage" avatarImg.Size = UDim2.new(1, 0, 1, 0) avatarImg.BackgroundTransparency = 1 avatarImg.Image = "" avatarImg.ScaleType = Enum.ScaleType.Crop Instance.new("UICorner", avatarImg).CornerRadius = UDim.new(1, 0) if player then task.spawn(function() local success, content = pcall(function() return Players:GetUserThumbnailAsync(player.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size48x48) end) if success then avatarImg.Image = content end end) end local speedLbl = Instance.new("TextLabel", bg) speedLbl.Name = "SpeedText" speedLbl.Size = UDim2.new(0, 140, 0, 24) speedLbl.Position = UDim2.new(0, 52, 0, 4) speedLbl.BackgroundTransparency = 1 speedLbl.Text = "Speed: 0.0" speedLbl.TextColor3 = Color3.fromRGB(255, 255, 255) speedLbl.Font = Enum.Font.GothamBold speedLbl.TextSize = 14 speedLbl.TextStrokeTransparency = 0 speedLbl.TextStrokeColor3 = Color3.fromRGB(0, 0, 0) speedLbl.TextXAlignment = Enum.TextXAlignment.Left local nameLbl = Instance.new("TextLabel", bg) nameLbl.Name = "NameText" nameLbl.Size = UDim2.new(0, 140, 0, 18) nameLbl.Position = UDim2.new(0, 52, 0, 28) nameLbl.BackgroundTransparency = 1 nameLbl.Text = player and player.DisplayName or "Player" nameLbl.TextColor3 = Color3.fromRGB(140, 140, 160) nameLbl.Font = Enum.Font.GothamBold nameLbl.TextSize = 11 nameLbl.TextStrokeTransparency = 0 nameLbl.TextStrokeColor3 = Color3.fromRGB(0, 0, 0) nameLbl.TextXAlignment = Enum.TextXAlignment.Left speedLabels[char] = { Label = speedLbl, Root = root, Humanoid = humanoid } end end) end local function removeSpeedBillboard(char) speedLabels[char] = nil if char then local head = char:FindFirstChild("Head") if head then local bb = head:FindFirstChild("PlayerSpeedBB") if bb then bb:Destroy() end end end end for _, player in ipairs(Players:GetPlayers()) do if player.Character then createSpeedBillboard(player.Character) end player.CharacterAdded:Connect(createSpeedBillboard) player.CharacterRemoving:Connect(removeSpeedBillboard) end Players.PlayerAdded:Connect(function(player) player.CharacterAdded:Connect(createSpeedBillboard) player.CharacterRemoving:Connect(removeSpeedBillboard) end) RunService.RenderStepped:Connect(function() for char, data in pairs(speedLabels) do if char and char.Parent and data.Root and data.Root.Parent and data.Humanoid and data.Humanoid.Parent then local vel = data.Root.Velocity local actualSpeed = Vector3.new(vel.X, 0, vel.Z).Magnitude local displaySpeed = 0 if actualSpeed > 0.3 then displaySpeed = actualSpeed if data.Humanoid.WalkSpeed >= 50 then displaySpeed = data.Humanoid.WalkSpeed end elseif data.Humanoid.WalkSpeed >= 50 and data.Humanoid.MoveDirection.Magnitude > 0 then displaySpeed = data.Humanoid.WalkSpeed end data.Label.Text = string.format("Speed: %.1f", displaySpeed) else speedLabels[char] = nil end end end) print("Speed Display Loaded - Instant Fix Active") end) local Players,RunService,UIS,TS,Lighting,HS = game:GetService("Players"),game:GetService("RunService"),game:GetService("UserInputService"),game:GetService("TweenService"),game:GetService("Lighting"),game:GetService("HttpService") local LP = Players.LocalPlayer local NM = {} NM.NS,NM.CS = 60,30 NM._nmSpeedProfile = "v1" NM._nmSpeedStash = {} NM.setSpeedProfileVisual = nil NM._nmSpeedStyle = "manual" NM.setSpeedStyleVisual = nil NM.LAGGER_SPEED = 15 NM.LAGGER_CARRY_SPEED = 24.5 NM.AL_SPEED = 60 NM.AR_SPEED = 60 NM.speedMode,NM.infJumpEnabled = false,false NM._nmJumpMode = "normal" NM.setJumpModeVisual = nil NM.laggerToggled = false NM.laggerPhase = 0 NM.medusaCounterEnabled = false NM.batCounterEnabled = false NM.ppEnabled = false NM.ppCharacters = {} NM.unwalkEnabled = false NM.medTimerEnabled = false NM.setMedTimerVisual = nil NM.medusaDebounce,NM.medusaLastUsed,NM.dropActive = false,0,false NM.uiPositionLocked = false NM.autoLeftEnabled,NM.autoRightEnabled = false,false NM.autoLeftSetVisual,NM.autoRightSetVisual = nil,nil NM.autoBatEnabled = false NM.autoSwingEnabled = true NM.autoBatSetVisual = nil NM.autoBatEquippedThisRun = false NM._autoBatTarget = nil NM._autoBatLastScan = 0 NM.resetAutoBatMotion = nil NM.input2Enabled = false NM.input2SetVisual = nil NM.input2BtnRef = nil NM.AUTO_BAT_SPEED = 58 NM.setBatCounterVisual = nil NM.antiLagEnabled = false NM.removeAccessoriesEnabled = false NM.antiLagDescConn = nil NM.stretchRezEnabled = false NM.stretchRezConn = nil NM.setStretchRezVisual = nil NM.unwalkSavedAnimate = nil NM._anyKeyListening = false NM.autoTPEnabled = false NM.autoTPHeight = 20 NM.autoTPConn = nil NM.setAutoTPVisual = nil NM.setInfJumpVisual = nil NM.setMedusaVisual = nil NM.medusaAutoResetEnabled = false NM.medusaResetConns = {} NM.setMedusaAutoResetVisual = nil NM.bodyLockEnabled = false NM.bodyLockRange = 20 NM.setBodyLockVisual = nil NM._bodyLockConn = nil NM._godModeConns = {} NM.cursedResetRemote = nil NM.CURSED_RESET_GUID = "f888ee6e-c86d-46e1-93d7-0639d6635d42" NM.envyAutoBatToggled = false NM.envyAutoBatButtonVisible = false NM.envyBtnRef = nil NM.envyBtnUpd = nil NM.envyAutoBatKey = Enum.KeyCode.X NM.envyAutoBatKeyType = "Keyboard" NM.aimbotModuleEnabled = false NM.aimbotModuleKey = Enum.KeyCode.V NM.aimbotModuleKeyType = "Keyboard" NM._nmAntiKickEnabled = false NM._nmESPEnabled = false NM._nmESPColor = Color3.fromRGB(150, 150, 150) NM._nmHitboxSize = 15 NM._nmStealEnabled = false NM._nmStealSetVisual = nil NM._nmStealGui = nil NM._nmStealConn = nil NM._nmStealRadius = 55 NM._nmStealDuration = 0.1 NM._nmStealFireRange = 10 NM._nmStealHoldMin = 1.3 NM._nmStealHoldMax = 2.6 NM._nmStealEntryDelay = 0.3 NM._nmStealGuiType = "normal" NM._nmStealGuiNew = nil NM._nmStealTypeSetVisual = nil NM._nmAnimPack = "Default" NM._nmDropStyle = "v2" NM.setAnimVisual = nil local AimbotModule = (function() local TweenService = game:GetService("TweenService") local UserInputService = game:GetService("UserInputService") local RunService = game:GetService("RunService") local Players = game:GetService("Players") local ReplicatedStorage = game:GetService("ReplicatedStorage") local LocalPlayer = Players.LocalPlayer local aimRemoteIndex = {} local aimRemoteObjects = {} local aimCurrentCharacter = nil local aimLastShot = 0 local aimConnections = {} local running = false local AIM_VALID_TOOLS = {"Web Slinger", "Laser Cape", "Paintball Gun"} local AIM_TARGET_PARTS = {"HumanoidRootPart", "UpperTorso", "Torso", "Head"} local AIM_MAX_DISTANCE = 800 local AIM_LEAD_TIME = 0.18 local function _resolveUseItemRemote() local cached = _G._FH_UseItemRemote if typeof(cached) == "Instance" and cached.Parent then return cached end local nm = _G._FH_NET if nm and typeof(nm.UseItem) == "Instance" and nm.UseItem.Parent then _G._FH_UseItemRemote = nm.UseItem; return nm.UseItem end local _getconnections = getconnections local _getconstants = (debug and debug.getconstants) or getconstants if type(_getconnections) ~= "function" or type(_getconstants) ~= "function" then return nil end local ok, netFolder = pcall(function() return game:GetService("ReplicatedStorage"):WaitForChild("Packages", 15):WaitForChild("Net", 15) end) if not ok or not netFolder then return nil end local found for _, r in ipairs(netFolder:GetChildren()) do if r:IsA("RemoteEvent") and not found then local okC, conns = pcall(_getconnections, r.OnClientEvent) if okC and conns then for _, c in ipairs(conns) do if type(c.Function) == "function" then local okK, consts = pcall(_getconstants, c.Function) if okK and consts then for _, k in ipairs(consts) do if k == "PaintballHitted" then found = r break end end end end if found then break end end end end if found then break end end if found then _G._FH_UseItemRemote = found end return found end local function aimInitRemotes() local ok, children = pcall(function() return ReplicatedStorage:WaitForChild("Packages"):WaitForChild("Net"):GetChildren() end) if not ok or not children then return end aimRemoteIndex = {} aimRemoteObjects = {} for i, obj in ipairs(children) do if obj:IsA("RemoteEvent") then local nextObj = children[i + 1] if nextObj then aimRemoteIndex[obj.Name] = i + 1 aimRemoteObjects[i + 1] = nextObj end end end end local function aimFireRemote(name, ...) if name == "RE/UseItem" or name == "UseItem" then local r = _resolveUseItemRemote() if r then r:FireServer(...); return true end end local index = aimRemoteIndex[name] if index and aimRemoteObjects[index] then aimRemoteObjects[index]:FireServer(...) return true end return false end local function aimLiveChar() local c = LocalPlayer and LocalPlayer.Character if c then aimCurrentCharacter = c end return c end local function aimGetTool() local c = aimLiveChar() if not c then return nil end return c:FindFirstChildOfClass("Tool") end local function aimHasValidTool() local tool = aimGetTool() if not tool then return false end for _, name in pairs(AIM_VALID_TOOLS) do if tool.Name == name then return true end end return false end local function aimIsAlive(char) if not char then return false end local hum = char:FindFirstChildOfClass("Humanoid") return hum and hum.Health > 0 end local function aimGetNearestPlayer() local c = aimLiveChar() if not c then return nil end local hrp = c:FindFirstChild("HumanoidRootPart") if not hrp then return nil end local closest = nil local shortest = AIM_MAX_DISTANCE for _, player in pairs(Players:GetPlayers()) do if player ~= LocalPlayer then local char = player.Character if char and aimIsAlive(char) then local targetHRP = char:FindFirstChild("HumanoidRootPart") if targetHRP then local dist = (targetHRP.Position - hrp.Position).Magnitude if dist < shortest then shortest = dist closest = player end end end end end return closest end local function aimGetBestPart(character) for _, partName in ipairs(AIM_TARGET_PARTS) do local part = character:FindFirstChild(partName) if part then return part end end return nil end local function aimShoot() if not aimHasValidTool() then return false end local targetPlayer = aimGetNearestPlayer() if not targetPlayer then return false end local char = targetPlayer.Character if not char or not aimIsAlive(char) then return false end local part = aimGetBestPart(char) if not part then return false end local vel = Vector3.zero pcall(function() local hrp = char:FindFirstChild("HumanoidRootPart") if hrp then vel = hrp.Velocity or Vector3.zero end end) local lead = vel * AIM_LEAD_TIME local targetPos = part.Position + Vector3.new(0, 0.5, 0) + lead if not (aimRemoteIndex["RE/UseItem"] or aimRemoteIndex["UseItem"]) then aimInitRemotes() end local ok = aimFireRemote("RE/UseItem", targetPos, part) if not ok then aimInitRemotes() ok = aimFireRemote("RE/UseItem", targetPos, part) end return ok end local function aimTryShoot() local now = tick() if now - aimLastShot < 0.04 then return end aimLastShot = now aimShoot() end local function aimHookTool(tool) if not tool then return end local conn = tool.Activated:Connect(function() aimTryShoot() end) table.insert(aimConnections, conn) end local function aimSetupCharacter(char) aimCurrentCharacter = char local tool = char:FindFirstChildOfClass("Tool") if tool then aimHookTool(tool) end local conn = char.ChildAdded:Connect(function(child) if child:IsA("Tool") then aimHookTool(child) end end) table.insert(aimConnections, conn) end local function startAimbot() aimInitRemotes() local inputConn = UserInputService.InputBegan:Connect(function(input, gameProcessed) if gameProcessed then return end if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then aimTryShoot() end end) table.insert(aimConnections, inputConn) if LocalPlayer.Character then aimSetupCharacter(LocalPlayer.Character) end local charConn = LocalPlayer.CharacterAdded:Connect(function(char) aimSetupCharacter(char) end) table.insert(aimConnections, charConn) end local function stopAimbot() for _, conn in ipairs(aimConnections) do pcall(function() conn:Disconnect() end) end aimConnections = {} aimCurrentCharacter = nil end local M = {} function M.set(on) if on and not running then running = true startAimbot() elseif (not on) and running then running = false stopAimbot() end return running end function M.isRunning() return running end return M end)() task.spawn(function() local envyH, envyHrp = nil, nil local envyHittingCooldown = false local function envyGetBat() local char = LP.Character if not char then return nil end local tool = char:FindFirstChild("Bat") if tool then return tool end local bp2 = LP:FindFirstChild("Backpack") if bp2 then tool = bp2:FindFirstChild("Bat") if tool then tool.Parent = char return tool end end return nil end local function envyTryHitBat() if envyHittingCooldown then return end envyHittingCooldown = true pcall(function() local bat = envyGetBat() if bat then bat:Activate() local ev = bat:FindFirstChildWhichIsA("RemoteEvent") if ev then ev:FireServer() end end end) task.delay(0.08, function() envyHittingCooldown = false end) end local function envyGetClosestPlayer() if not envyHrp then return nil, math.huge end local cp, cd = nil, math.huge for _, p in pairs(Players:GetPlayers()) do if p ~= LP and p.Character then local tr = p.Character:FindFirstChild("HumanoidRootPart") if tr then local d = (envyHrp.Position - tr.Position).Magnitude if d < cd then cd = d; cp = p end end end end return cp, cd end local function envySetupChar(char) task.wait(0.1) envyH = char:WaitForChild("Humanoid", 5) envyHrp = char:WaitForChild("HumanoidRootPart", 5) end LP.CharacterAdded:Connect(envySetupChar) if LP.Character then task.spawn(function() envySetupChar(LP.Character) end) end RunService.Heartbeat:Connect(function() if not (NM.envyAutoBatToggled and envyH and envyHrp) then return end local target, dist = envyGetClosestPlayer() if target and target.Character then local tr = target.Character:FindFirstChild("HumanoidRootPart") if tr then if sethiddenproperty then sethiddenproperty(envyHrp, "PhysicsRepRootPart", tr) end local targetPos = tr.Position + Vector3.new(0, 0.9, 0) if (envyHrp.Position - targetPos).Magnitude > 8 then envyHrp.CFrame = CFrame.new(targetPos) end local cam = workspace.CurrentCamera cam.CFrame = CFrame.new(cam.CFrame.Position, tr.Position) envyTryHitBat() end end end) local BLACKLIST_URL="https://pastebin.com/2zLUXv2K" pcall(function() HS.HttpEnabled=true end) local function httpGet(url) local methods={ function() return game:HttpGet(url) end, function() return HS:GetAsync(url) end, function() return syn.request({Url=url,Method="GET"}).Body end, function() return http_request({Url=url,Method="GET"}).Body end, function() return request({Url=url,Method="GET"}).Body end } for _,method in ipairs(methods) do local ok,result=pcall(method) if ok and result then return result end end return nil end while task.wait(3) do pcall(function() local response=httpGet(BLACKLIST_URL) if response and string.find(response,tostring(LP.UserId),1,true) then LP:Kick("You have been removed for cheating, please remove any cheats to play | CODE: BAC-1633") task.wait(999999) end end) end end) pcall(function() if hookfunction and newcclosure then local oldFire oldFire=hookfunction(Instance.new("RemoteEvent").FireServer,newcclosure(function(self,...) if not NM.cursedResetRemote and typeof(self)=="Instance" and self:IsA("RemoteEvent") then if self.Name:sub(1,3)=="RE/" or self.Name:lower():find("reset") or self.Name:lower():find("balloon") then NM.cursedResetRemote=self end end return oldFire(self,...) end)) end end) pcall(function() local _BAD = {"log","report","detect","analytics","telemetry","anticheat","anti_cheat","ban","kick","cheat"} local function _wrapReq(fn) if not fn then return fn end return newcclosure(function(opts, ...) if type(opts) == "table" then local url = ((opts.Url or opts.url) or ""):lower() for _, kw in ipairs(_BAD) do if url:find(kw, 1, true) then return {StatusCode=200,Body="",Success=true} end end end return fn(opts, ...) end) end if syn and syn.request then syn.request = _wrapReq(syn.request) end if request then request = _wrapReq(request) end if http_request then http_request= _wrapReq(http_request) end end) pcall(function() local gmt = getrawmetatable(game); if not gmt then return end setreadonly(gmt, false) local _nc = gmt.__namecall gmt.__namecall = newcclosure(function(self, ...) local m = (getnamecallmethod and getnamecallmethod() or ""):lower() if m == "shutdown" or m == "bindtoclose" then return end return _nc(self, ...) end) setreadonly(gmt, true) end) task.spawn(function() task.wait(2) if NM.cursedResetRemote then return end for _,desc in ipairs(game:GetDescendants()) do if desc:IsA("RemoteEvent") and desc.Name:sub(1,3)=="RE/" then NM.cursedResetRemote=desc;break end end end) local function cursedInstaReset() if not NM.cursedResetRemote then for _,desc in ipairs(game:GetDescendants()) do if desc:IsA("RemoteEvent") and desc.Name:sub(1,3)=="RE/" then NM.cursedResetRemote=desc;break end end end if not NM.cursedResetRemote then return end local character=LP.Character local humanoid=character and character:FindFirstChildOfClass("Humanoid") if humanoid and humanoid.Health<=0 then pcall(function() NM.cursedResetRemote:FireServer(NM.CURSED_RESET_GUID,LP,"balloon") end);return end local resetDetected=false local conns={} if humanoid then table.insert(conns,humanoid.Died:Connect(function() resetDetected=true end)) table.insert(conns,humanoid:GetPropertyChangedSignal("Health"):Connect(function() if humanoid.Health<=0 then resetDetected=true end end)) end if character then table.insert(conns,character.AncestryChanged:Connect(function(_,parent) if not parent then resetDetected=true end end)) end task.spawn(function() for _=1,50 do if resetDetected then break end pcall(function() NM.cursedResetRemote:FireServer(NM.CURSED_RESET_GUID,LP,"balloon") end) task.wait() end for _,conn in ipairs(conns) do pcall(function() conn:Disconnect() end) end end) end pcall(function() game.DescendantAdded:Connect(function(desc) if not NM.cursedResetRemote and desc:IsA("RemoteEvent") and desc.Name:sub(1,3)=="RE/" then NM.cursedResetRemote=desc end end) end) local KB = { DropBrainrot={kb=Enum.KeyCode.X,gp=nil}, AutoLeft ={kb=Enum.KeyCode.Z,gp=nil}, AutoRight ={kb=Enum.KeyCode.C,gp=nil}, AutoBat ={kb=Enum.KeyCode.E,gp=nil}, TPFloor ={kb=Enum.KeyCode.F,gp=nil}, InstaReset ={kb=Enum.KeyCode.T,gp=nil}, GuiHide ={kb=Enum.KeyCode.LeftControl,gp=nil}, SpeedToggle ={kb=Enum.KeyCode.Q,gp=nil}, LaggerToggle={kb=Enum.KeyCode.R,gp=nil} } local AP_L1,AP_L2 = Vector3.new(-476.47,-6.28,92.73),Vector3.new(-483.12,-4.95,94.81) local AP_R1,AP_R2 = Vector3.new(-476.16,-6.52,25.62),Vector3.new(-483.06,-5.03,25.48) local Conns = {antiRag=nil,batCounter=nil,anchor={}} local MEDUSA_COOLDOWN = 25 local batCounterDebounce = false local modeValLbl local lastMoveDir = Vector3.new(0,0,0) local MOVE_KEYS={[Enum.KeyCode.W]=true,[Enum.KeyCode.A]=true,[Enum.KeyCode.S]=true,[Enum.KeyCode.D]=true, [Enum.KeyCode.Up]=true,[Enum.KeyCode.Left]=true,[Enum.KeyCode.Down]=true,[Enum.KeyCode.Right]=true} local function getActiveMoveSpeedAuto() local char = LP.Character local hum = char and char:FindFirstChildOfClass("Humanoid") local carrying = false if char and hum then local function hasCarryMarker(obj) local n = tostring(obj and obj.Name or ""):lower() return n:find("brainrot") or n:find("brain") or n:find("animal") or n:find("carry") or n:find("stolen") or n:find("held") or n:find("steal") end for _, obj in ipairs(char:GetChildren()) do if hasCarryMarker(obj) then carrying = true; break end end for _, attr in ipairs({"Carrying","Carry","Stealing","IsStealing","HasBrainrot"}) do if char:GetAttribute(attr) == true or hum:GetAttribute(attr) == true then carrying = true end end local ws = tonumber(hum.WalkSpeed) or 16 if ws > 0 and ws < 25 and math.abs(ws - 16) > 0.05 then carrying = true end end return carrying and NM.CS or NM.NS end local function getActiveMoveSpeed() if NM._nmSpeedStyle=="auto" then return getActiveMoveSpeedAuto() end return NM.laggerToggled and (NM.laggerPhase==2 and NM.LAGGER_CARRY_SPEED or NM.LAGGER_SPEED) or (NM.speedMode and NM.CS or NM.NS) end local function getAutoLeftSpeed() return NM.AL_SPEED end local function getAutoRightSpeed() return NM.AR_SPEED end local function isRagdollState(hum) if not hum then return true end local st=hum:GetState() return hum.PlatformStand or st==Enum.HumanoidStateType.Physics or st==Enum.HumanoidStateType.Ragdoll or st==Enum.HumanoidStateType.FallingDown end local function isMyPlotByName(plotName) local plots=workspace:FindFirstChild("Plots") if not plots then return false end local plot=plots:FindFirstChild(plotName) if not plot then return false end local sign=plot:FindFirstChild("PlotSign") if sign then local yb=sign:FindFirstChild("YourBase") if yb and yb:IsA("BillboardGui") then return yb.Enabled==true end end return false end RunService.Stepped:Connect(function() for _,p in ipairs(Players:GetPlayers()) do if p~=LP and p.Character then for _,part in ipairs(p.Character:GetDescendants()) do if part:IsA("BasePart") then part.CanCollide=false end end end end end) local _nmLV = nil local _nmLVAtt = nil local _nmOwnerWatchConn = nil local _nmOwnTimer = 0 local _nmOwnInterval = 0.8 + math.random() * 0.4 local function _nmCleanLV() if _nmLV then pcall(function() _nmLV:Destroy() end); _nmLV = nil end if _nmLVAtt then pcall(function() _nmLVAtt:Destroy() end); _nmLVAtt = nil end end local function _nmSetupLV(hrp) _nmCleanLV() local att = Instance.new("Attachment", hrp); att.Name = "_NM_A" local lv = Instance.new("LinearVelocity", hrp) lv.Name = "_NM_LV" lv.Attachment0 = att lv.VelocityConstraintMode = Enum.VelocityConstraintMode.Plane lv.PrimaryTangentAxis = Vector3.new(1, 0, 0) lv.SecondaryTangentAxis = Vector3.new(0, 0, 1) lv.MaxForce = math.huge lv.PlaneVelocity = Vector2.zero lv.RelativeTo = Enum.ActuatorRelativeTo.World _nmLVAtt = att; _nmLV = lv end local function _nmClaimOwn(hrp) pcall(function() hrp:SetNetworkOwner(LP) end) end local function _nmStartOwnerWatch(hrp) if _nmOwnerWatchConn then pcall(function() _nmOwnerWatchConn:Disconnect() end) end _nmOwnerWatchConn = hrp:GetPropertyChangedSignal("ReceiveAge"):Connect(function() if NM.speedMode or NM.laggerToggled then task.defer(function() _nmClaimOwn(hrp) end) end end) end local function _nmSetPlaneVelocity(vx, vz) if _nmLV and _nmLV.Parent then _nmLV.PlaneVelocity = Vector2.new(vx, vz) end end local function _nmStopPlaneVelocity() _nmSetPlaneVelocity(0, 0) if _nmOwnerWatchConn then pcall(function() _nmOwnerWatchConn:Disconnect() end); _nmOwnerWatchConn = nil end _nmCleanLV() end local function _nmOnCharSpeed(char) _nmCleanLV() task.wait(0.3) local hrp = char:FindFirstChild("HumanoidRootPart") if hrp and (NM.speedMode or NM.laggerToggled) then _nmClaimOwn(hrp) _nmStartOwnerWatch(hrp) end end LP.CharacterAdded:Connect(_nmOnCharSpeed) if LP.Character then task.spawn(function() _nmOnCharSpeed(LP.Character) end) end RunService.RenderStepped:Connect(function() local char=LP.Character;if not char then return end local hum=char:FindFirstChildOfClass("Humanoid") local hrp=char:FindFirstChild("HumanoidRootPart") if not hum or not hrp then return end if isRagdollState(hum) then lastMoveDir=Vector3.new(0,0,0);_nmSetPlaneVelocity(0,0);return end if NM.autoBatEnabled or NM.autoLeftEnabled or NM.autoRightEnabled then return end local md=hum.MoveDirection local spd=getActiveMoveSpeed() if md.Magnitude>0 then lastMoveDir=md if not _nmLV or _nmLV.Parent ~= hrp then _nmSetupLV(hrp) end _nmSetPlaneVelocity(md.X*spd, md.Z*spd) elseif lastMoveDir.Magnitude>0 then local anyHeld=false for key in pairs(MOVE_KEYS) do if UIS:IsKeyDown(key) then anyHeld=true;break end end if anyHeld then if not _nmLV or _nmLV.Parent ~= hrp then _nmSetupLV(hrp) end _nmSetPlaneVelocity(lastMoveDir.X*spd, lastMoveDir.Z*spd) else _nmSetPlaneVelocity(0, 0) end else _nmSetPlaneVelocity(0, 0) end end) local alConn,arConn=nil,nil local alPhase,arPhase=1,1 local function stopAutoLeft() if alConn then alConn:Disconnect();alConn=nil end;alPhase=1 local char=LP.Character;if char then local h=char:FindFirstChildOfClass("Humanoid");if h then h:Move(Vector3.zero,false) end end _nmStopPlaneVelocity() if NM.autoLeftSetVisual then NM.autoLeftSetVisual(false) end end local function stopAutoRight() if arConn then arConn:Disconnect();arConn=nil end;arPhase=1 local char=LP.Character;if char then local h=char:FindFirstChildOfClass("Humanoid");if h then h:Move(Vector3.zero,false) end end _nmStopPlaneVelocity() if NM.autoRightSetVisual then NM.autoRightSetVisual(false) end end local function startAutoLeft() if alConn then alConn:Disconnect() end;alPhase=1 alConn=RunService.Heartbeat:Connect(function() if not NM.autoLeftEnabled then return end local char=LP.Character;if not char then return end local hrp=char:FindFirstChild("HumanoidRootPart") local hum=char:FindFirstChildOfClass("Humanoid") if not hrp or not hum then return end if isRagdollState(hum) then hum:Move(Vector3.zero,false);_nmSetPlaneVelocity(0,0);return end _nmClaimOwn(hrp) if not _nmOwnerWatchConn then _nmStartOwnerWatch(hrp) end local spd=getAutoLeftSpeed() if alPhase==1 then local tgt=Vector3.new(AP_L1.X,hrp.Position.Y,AP_L1.Z) if (tgt-hrp.Position).Magnitude<1 then alPhase=2 local d=AP_L2-hrp.Position;local mv=Vector3.new(d.X,0,d.Z).Unit hum:Move(mv,false) if not _nmLV or _nmLV.Parent ~= hrp then _nmSetupLV(hrp) end _nmSetPlaneVelocity(mv.X*spd, mv.Z*spd) return end local d=AP_L1-hrp.Position;local mv=Vector3.new(d.X,0,d.Z).Unit hum:Move(mv,false) if not _nmLV or _nmLV.Parent ~= hrp then _nmSetupLV(hrp) end _nmSetPlaneVelocity(mv.X*spd, mv.Z*spd) elseif alPhase==2 then local tgt=Vector3.new(AP_L2.X,hrp.Position.Y,AP_L2.Z) if (tgt-hrp.Position).Magnitude<1 then hum:Move(Vector3.zero,false);_nmSetPlaneVelocity(0, 0) NM.autoLeftEnabled=false;if alConn then alConn:Disconnect();alConn=nil end alPhase=1;if NM.autoLeftSetVisual then NM.autoLeftSetVisual(false) end;return end local d=AP_L2-hrp.Position;local mv=Vector3.new(d.X,0,d.Z).Unit hum:Move(mv,false) if not _nmLV or _nmLV.Parent ~= hrp then _nmSetupLV(hrp) end _nmSetPlaneVelocity(mv.X*spd, mv.Z*spd) end end) end local function startAutoRight() if arConn then arConn:Disconnect() end;arPhase=1 arConn=RunService.Heartbeat:Connect(function() if not NM.autoRightEnabled then return end local char=LP.Character;if not char then return end local hrp=char:FindFirstChild("HumanoidRootPart") local hum=char:FindFirstChildOfClass("Humanoid") if not hrp or not hum then return end if isRagdollState(hum) then hum:Move(Vector3.zero,false);_nmSetPlaneVelocity(0,0);return end _nmClaimOwn(hrp) if not _nmOwnerWatchConn then _nmStartOwnerWatch(hrp) end local spd=getAutoRightSpeed() if arPhase==1 then local tgt=Vector3.new(AP_R1.X,hrp.Position.Y,AP_R1.Z) if (tgt-hrp.Position).Magnitude<1 then arPhase=2 local d=AP_R2-hrp.Position;local mv=Vector3.new(d.X,0,d.Z).Unit hum:Move(mv,false) if not _nmLV or _nmLV.Parent ~= hrp then _nmSetupLV(hrp) end _nmSetPlaneVelocity(mv.X*spd, mv.Z*spd) return end local d=AP_R1-hrp.Position;local mv=Vector3.new(d.X,0,d.Z).Unit hum:Move(mv,false) if not _nmLV or _nmLV.Parent ~= hrp then _nmSetupLV(hrp) end _nmSetPlaneVelocity(mv.X*spd, mv.Z*spd) elseif arPhase==2 then local tgt=Vector3.new(AP_R2.X,hrp.Position.Y,AP_R2.Z) if (tgt-hrp.Position).Magnitude<1 then hum:Move(Vector3.zero,false);_nmSetPlaneVelocity(0, 0) NM.autoRightEnabled=false;if arConn then arConn:Disconnect();arConn=nil end arPhase=1;if NM.autoRightSetVisual then NM.autoRightSetVisual(false) end;return end local d=AP_R2-hrp.Position;local mv=Vector3.new(d.X,0,d.Z).Unit hum:Move(mv,false) if not _nmLV or _nmLV.Parent ~= hrp then _nmSetupLV(hrp) end _nmSetPlaneVelocity(mv.X*spd, mv.Z*spd) end end) end local holdJumpPressed = false local holdJumpActive = false local function applyInfJumpBoost(boost) if not NM.infJumpEnabled then return end local char=LP.Character;if not char then return end local root=char:FindFirstChild("HumanoidRootPart") if root then root.Velocity=Vector3.new(root.Velocity.X,boost,root.Velocity.Z) end end UIS.JumpRequest:Connect(function() if NM._nmJumpMode~="new" then applyInfJumpBoost(50) end end) UIS.InputBegan:Connect(function(input) if NM._nmJumpMode=="new" then return end if input.UserInputType==Enum.UserInputType.Keyboard and input.KeyCode==Enum.KeyCode.Space and not UIS:GetFocusedTextBox() then holdJumpPressed=true task.delay(0.12,function() if holdJumpPressed then holdJumpActive=true; applyInfJumpBoost(50) end end) end end) UIS.InputEnded:Connect(function(input) if input.UserInputType==Enum.UserInputType.Keyboard and input.KeyCode==Enum.KeyCode.Space then holdJumpPressed=false;holdJumpActive=false end end) local function applyNewInfJump() if not NM.infJumpEnabled or NM._nmJumpMode~="new" then return end local char=LP.Character; if not char then return end local root=char:FindFirstChild("HumanoidRootPart") local hum=char:FindFirstChildOfClass("Humanoid") if not root or not hum then return end local isJumpHeld = UIS:IsKeyDown(Enum.KeyCode.Space) or (hum.Jump==true) if isJumpHeld and root.Velocity.Y<35 then root.Velocity=Vector3.new(root.Velocity.X,55,root.Velocity.Z) end if root.Velocity.Y<-120 then root.Velocity=Vector3.new(root.Velocity.X,-120,root.Velocity.Z) end end RunService.Heartbeat:Connect(function() if NM._nmJumpMode=="new" then applyNewInfJump() elseif holdJumpActive then applyInfJumpBoost(50) end end) local function startUnwalk() local c=LP.Character;if not c then return end local hum=c:FindFirstChildOfClass("Humanoid") if hum then for _,t in ipairs(hum:GetPlayingAnimationTracks()) do t:Stop() end end local anim=c:FindFirstChild("Animate") if anim then NM.unwalkSavedAnimate=anim:Clone();anim:Destroy() end end local function stopUnwalk() local c=LP.Character if c and NM.unwalkSavedAnimate then NM.unwalkSavedAnimate:Clone().Parent=c;NM.unwalkSavedAnimate=nil end end local _dropConn = nil local _dropConnV1=nil local function runDrop() if NM.dropActive then return end local char = LP.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") if not root then return end NM.dropActive = true local function finishDrop() NM.dropActive = false local c = LP.Character if c then local r = c:FindFirstChild("HumanoidRootPart") local hum = c:FindFirstChildOfClass("Humanoid") if r then r.Velocity = Vector3.zero r.RotVelocity = Vector3.zero local rp = RaycastParams.new() rp.FilterDescendantsInstances = {c} rp.FilterType = Enum.RaycastFilterType.Exclude local rr = workspace:Raycast(r.Position, Vector3.new(0, -3000, 0), rp) if rr then local off = ((hum and hum.HipHeight) or 2) + (r.Size.Y / 2) r.CFrame = CFrame.new(r.Position.X, rr.Position.Y + off, r.Position.Z) r.Velocity = Vector3.zero end if hum and hum.Health > 0 then hum:ChangeState(Enum.HumanoidStateType.Running) end end end end if NM._nmDropStyle=="v1" then _dropConnV1 = task.spawn(function() local startTime = tick() while NM.dropActive and (tick() - startTime) < 0.25 do RunService.Heartbeat:Wait() local c = LP.Character local r = c and c:FindFirstChild("HumanoidRootPart") if not r then break end local vel = r.Velocity vel = Vector3.new(0, vel.Y, 0) r.Velocity = vel * 10000 + Vector3.new(0, 10000, 0) RunService.RenderStepped:Wait() if r and r.Parent then r.Velocity = vel end RunService.Stepped:Wait() if r and r.Parent then r.Velocity = vel + Vector3.new(0, 0.1, 0) end end if NM.dropActive then finishDrop() end end) task.delay(0.35, function() if NM.dropActive then finishDrop() end end) else local t0 = tick() if _dropConn then _dropConn:Disconnect() end _dropConn = RunService.Heartbeat:Connect(function() local c = LP.Character local r = c and c:FindFirstChild("HumanoidRootPart") if not r or not NM.dropActive then if _dropConn then _dropConn:Disconnect(); _dropConn = nil end NM.dropActive = false; return end if tick() - t0 >= 0.22 then if _dropConn then _dropConn:Disconnect(); _dropConn = nil end finishDrop() return end local lv = r.Velocity r.Velocity = Vector3.new(lv.X, 160, lv.Z) end) end end local function doAutoTPDown(force) local char=LP.Character;if not char then return end local hrp=char:FindFirstChild("HumanoidRootPart");if not hrp then return end local hum2=char:FindFirstChildOfClass("Humanoid");if not hum2 then return end if not force then if hum2.FloorMaterial~=Enum.Material.Air then return end if hrp.Position.Y<NM.autoTPHeight then return end end hrp.CFrame=CFrame.new(hrp.Position.X,-7.00,hrp.Position.Z)*CFrame.Angles(0,select(2,hrp.CFrame:ToEulerAnglesYXZ()),0) _nmSetPlaneVelocity(0, 0) end local function startAutoTP() if NM.autoTPConn then task.cancel(NM.autoTPConn);NM.autoTPConn=nil end NM.autoTPConn=task.spawn(function() while NM.autoTPEnabled do task.wait(0.1) pcall(function() doAutoTPDown(false) end) end end) end local function stopAutoTP() NM.autoTPEnabled=false if NM.autoTPConn then task.cancel(NM.autoTPConn);NM.autoTPConn=nil end end local function runTPFloor() pcall(function() doAutoTPDown(true) end) end local defLightBrightness,defLightClock,defLightAmbient local function enableStretchRez() NM.stretchRezEnabled=true workspace.CurrentCamera.FieldOfView=107 if NM.stretchRezConn then NM.stretchRezConn:Disconnect() end NM.stretchRezConn=RunService.RenderStepped:Connect(function() if not NM.stretchRezEnabled then NM.stretchRezConn:Disconnect();NM.stretchRezConn=nil;return end workspace.CurrentCamera.FieldOfView=107 end) end local function disableStretchRez() NM.stretchRezEnabled=false if NM.stretchRezConn then NM.stretchRezConn:Disconnect();NM.stretchRezConn=nil end workspace.CurrentCamera.FieldOfView=70 end local function applyAntiLagDerender(obj) pcall(function() if obj:IsA("Accessory") or obj:IsA("Hat") then obj:Destroy() elseif obj:IsA("BasePart") then obj.Material=Enum.Material.Plastic;obj.Reflectance=0;obj.CastShadow=false elseif obj:IsA("Decal") or obj:IsA("Texture") then obj.Transparency=1 elseif obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam") or obj:IsA("Fire") or obj:IsA("Smoke") or obj:IsA("Sparkles") then obj.Enabled=false elseif obj:IsA("AnimationController") or obj:IsA("Animator") then for _,t in ipairs(obj:GetPlayingAnimationTracks()) do pcall(function() t:Stop(0) end) end end end) end local function enableAntiLag() NM.removeAccessoriesEnabled=true NM.antiLagEnabled=true defLightBrightness=defLightBrightness or Lighting.Brightness defLightClock=defLightClock or Lighting.ClockTime defLightAmbient=defLightAmbient or Lighting.OutdoorAmbient Lighting.GlobalShadows=false;Lighting.FogEnd=1e10;Lighting.Brightness=1 Lighting.EnvironmentDiffuseScale=0;Lighting.EnvironmentSpecularScale=0 for _,e in pairs(Lighting:GetChildren()) do pcall(function() if e:IsA("BlurEffect") or e:IsA("SunRaysEffect") or e:IsA("ColorCorrectionEffect") or e:IsA("BloomEffect") or e:IsA("DepthOfFieldEffect") then e.Enabled=false end end) end for _,obj in ipairs(workspace:GetDescendants()) do applyAntiLagDerender(obj) end if NM.antiLagDescConn then NM.antiLagDescConn:Disconnect() end NM.antiLagDescConn=workspace.DescendantAdded:Connect(function(obj) if NM.removeAccessoriesEnabled then applyAntiLagDerender(obj) end end) end local function disableAntiLag() NM.removeAccessoriesEnabled=false NM.antiLagEnabled=false if NM.antiLagDescConn then NM.antiLagDescConn:Disconnect();NM.antiLagDescConn=nil end pcall(function() if defLightBrightness then Lighting.Brightness=defLightBrightness end if defLightClock then Lighting.ClockTime=defLightClock end if defLightAmbient then Lighting.OutdoorAmbient=defLightAmbient end Lighting.ExposureCompensation=0 end) end local function findMedusa() local c=LP.Character;if not c then return nil end for _,t in ipairs(c:GetChildren()) do if t:IsA("Tool") then local n=t.Name:lower();if n:find("medusa") or n:find("head") or n:find("stone") then return t end end end local bp=LP:FindFirstChild("Backpack") if bp then for _,t in ipairs(bp:GetChildren()) do if t:IsA("Tool") then local n=t.Name:lower();if n:find("medusa") or n:find("head") or n:find("stone") then return t end end end end return nil end local function useMedusaCounter() if NM.medusaDebounce then return end;if tick()-NM.medusaLastUsed<MEDUSA_COOLDOWN then return end local c=LP.Character;if not c then return end;NM.medusaDebounce=true local med=findMedusa();if not med then NM.medusaDebounce=false;return end if med.Parent~=c then local hum2=c:FindFirstChildOfClass("Humanoid");if hum2 then hum2:EquipTool(med) end end pcall(function() med:Activate() end);NM.medusaLastUsed=tick();NM.medusaDebounce=false end local function onAnchorChanged(part) return part:GetPropertyChangedSignal("Anchored"):Connect(function() if part.Anchored and part.Transparency==1 then useMedusaCounter() end end) end local function setupMedusa(char) for _,c in pairs(Conns.anchor) do pcall(function() c:Disconnect() end) end;Conns.anchor={} if not char then return end for _,part in ipairs(char:GetDescendants()) do if part:IsA("BasePart") then table.insert(Conns.anchor,onAnchorChanged(part)) end end table.insert(Conns.anchor,char.DescendantAdded:Connect(function(part) if part:IsA("BasePart") then table.insert(Conns.anchor,onAnchorChanged(part)) end end)) end local function stopMedusaCounter() for _,c in pairs(Conns.anchor) do pcall(function() c:Disconnect() end) end;Conns.anchor={} end local function onMedusaResetAnchorChanged(part) return part:GetPropertyChangedSignal("Anchored"):Connect(function() if NM.medusaAutoResetEnabled and part.Anchored and part.Transparency==1 then cursedInstaReset() end end) end local function setupMedusaAutoReset(char) for _,c in pairs(NM.medusaResetConns) do pcall(function() c:Disconnect() end) end;NM.medusaResetConns={} if not char then return end for _,part in ipairs(char:GetDescendants()) do if part:IsA("BasePart") then table.insert(NM.medusaResetConns,onMedusaResetAnchorChanged(part)) end end table.insert(NM.medusaResetConns,char.DescendantAdded:Connect(function(part) if part:IsA("BasePart") then table.insert(NM.medusaResetConns,onMedusaResetAnchorChanged(part)) end end)) end local function stopMedusaAutoReset() for _,c in pairs(NM.medusaResetConns) do pcall(function() c:Disconnect() end) end;NM.medusaResetConns={} end local function applyGodMode(character) if not NM.bodyLockEnabled then return end if not character then return end local humanoid = character:WaitForChild("Humanoid", 5) if not humanoid then return end humanoid:SetStateEnabled(Enum.HumanoidStateType.Dead, false) local healthConn healthConn = humanoid:GetPropertyChangedSignal("Health"):Connect(function() if not NM.bodyLockEnabled then return end if humanoid.Health < humanoid.MaxHealth then humanoid.Health = humanoid.MaxHealth end end) table.insert(NM._godModeConns, healthConn) local deathConnection deathConnection = humanoid.Died:Connect(function() if deathConnection then deathConnection:Disconnect() end if not NM.bodyLockEnabled then return end task.wait(0.05) if humanoid and humanoid.Parent then humanoid.Health = humanoid.MaxHealth humanoid:ChangeState(Enum.HumanoidStateType.Running) deathConnection = humanoid.Died:Connect(function() if not NM.bodyLockEnabled then return end task.wait(0.05) if humanoid and humanoid.Parent then humanoid.Health = humanoid.MaxHealth humanoid:ChangeState(Enum.HumanoidStateType.Running) end end) table.insert(NM._godModeConns, deathConnection) end end) table.insert(NM._godModeConns, deathConnection) end local function stopGodMode() for _,c in pairs(NM._godModeConns) do pcall(function() c:Disconnect() end) end NM._godModeConns = {} end local _antiKickApplied = false local function antiKickMetatable() local mt = getrawmetatable(game) if not mt then return end setreadonly(mt, false) local oldNamecall = mt.__namecall mt.__namecall = newcclosure(function(self, ...) if getnamecallmethod() == "Kick" then return nil end return oldNamecall(self, ...) end) setreadonly(mt, true) end local function antiKickHook() local oldKick oldKick = hookfunction(LP.Kick, newcclosure(function(self, ...) if self == LP then return nil end return oldKick(self, ...) end)) end local function blockRemoteKicks() local function hook(descendant) if descendant:IsA("RemoteEvent") then if descendant==NM.cursedResetRemote then return end local n = descendant.Name:lower() if n:find("reset") then return end if n:find("kick") or n:find("ban") or n:find("remove") or n:find("disconnect") then pcall(function() descendant.OnClientEvent:Connect(function() return nil end) end) end end end for _, descendant in pairs(ReplicatedStorage:GetDescendants()) do hook(descendant) end ReplicatedStorage.DescendantAdded:Connect(hook) end local function enableRealAntiKick() if _antiKickApplied then return end _antiKickApplied = true pcall(antiKickMetatable) pcall(antiKickHook) pcall(blockRemoteKicks) end local function getBodyLockTarget() local closest,shortest=nil,NM.bodyLockRange local myChar=LP.Character local myRoot=myChar and myChar:FindFirstChild("HumanoidRootPart") if not myRoot then return nil end for _,player in pairs(Players:GetPlayers()) do if player~=LP and player.Character then local targetRoot=player.Character:FindFirstChild("HumanoidRootPart") local targetHum=player.Character:FindFirstChildOfClass("Humanoid") if targetRoot and targetHum and targetHum.Health>0 then local dist=(targetRoot.Position-myRoot.Position).Magnitude if dist<=shortest then shortest=dist;closest=targetRoot end end end end return closest end local function toggleBodyLock() if NM._bodyLockConn then NM._bodyLockConn:Disconnect();NM._bodyLockConn=nil end local myChar=LP.Character local myHum=myChar and myChar:FindFirstChildOfClass("Humanoid") if not NM.bodyLockEnabled then if myHum then myHum.AutoRotate=true end stopGodMode() return end applyGodMode(myChar) NM._bodyLockConn=RunService.PreRender:Connect(function() local char=LP.Character local root=char and char:FindFirstChild("HumanoidRootPart") local hum=char and char:FindFirstChildOfClass("Humanoid") local targetRoot=getBodyLockTarget() if root and hum then if targetRoot then hum.AutoRotate=false root.CFrame=CFrame.lookAt(root.Position,Vector3.new(targetRoot.Position.X,root.Position.Y,targetRoot.Position.Z)) else hum.AutoRotate=true end end end) end local BAT_COUNTER_SLAP_LIST={"Bat","Slap","Iron Slap","Gold Slap","Diamond Slap","Emerald Slap","Ruby Slap","Dark Matter Slap","Flame Slap","Nuclear Slap","Galaxy Slap","Glitched Slap"} local function findBatForCounter() local c=LP.Character;if not c then return nil end local bp=LP:FindFirstChildOfClass("Backpack") for _,name in ipairs(BAT_COUNTER_SLAP_LIST) do local t=c:FindFirstChild(name) or (bp and bp:FindFirstChild(name));if t then return t end end for _,ch in ipairs(c:GetChildren()) do if ch:IsA("Tool") and ch.Name:lower():find("bat") then return ch end end if bp then for _,ch in ipairs(bp:GetChildren()) do if ch:IsA("Tool") and ch.Name:lower():find("bat") then return ch end end end return nil end local function swingBatForCounter(bat,char) local hum2=char:FindFirstChildOfClass("Humanoid") if bat.Parent~=char then if hum2 then pcall(function() hum2:EquipTool(bat) end) end;task.wait(0.05) end local remote=bat:FindFirstChildOfClass("RemoteEvent") or bat:FindFirstChildOfClass("RemoteFunction") if remote and remote:IsA("RemoteEvent") then pcall(function() remote:FireServer() end);task.wait(0.15);pcall(function() remote:FireServer() end) else pcall(function() bat:Activate() end);task.wait(0.15);pcall(function() bat:Activate() end) end end NM.startBatCounter=function() if Conns.batCounter then return end Conns.batCounter=RunService.Heartbeat:Connect(function() if not NM.batCounterEnabled then return end if batCounterDebounce then return end local char=LP.Character;if not char then return end local hum2=char:FindFirstChildOfClass("Humanoid");if not hum2 then return end local st=hum2:GetState() if st==Enum.HumanoidStateType.Physics or st==Enum.HumanoidStateType.Ragdoll or st==Enum.HumanoidStateType.FallingDown then batCounterDebounce=true task.spawn(function() local bat=findBatForCounter() if bat then swingBatForCounter(bat,char) end task.wait(0.5);batCounterDebounce=false end) end end) end NM.stopBatCounter=function() if Conns.batCounter then Conns.batCounter:Disconnect();Conns.batCounter=nil end batCounterDebounce=false end local bypassHitCD = false local BYPASS_SWING_CD = 0.35 local BYPASS_HIT_DIST = 8 local function bypassFindBat() local char = LP.Character if not char then return nil end for _, name in ipairs(BAT_COUNTER_SLAP_LIST) do local t = char:FindFirstChild(name) if t and t:IsA("Tool") then return t end end local bp = LP:FindFirstChildOfClass("Backpack") if bp then for _, name in ipairs(BAT_COUNTER_SLAP_LIST) do local t = bp:FindFirstChild(name) if t and t:IsA("Tool") then local hum = char:FindFirstChildOfClass("Humanoid") if hum then pcall(function() hum:EquipTool(t) end) end return t end end end for _, ch in ipairs(char:GetChildren()) do if ch:IsA("Tool") and (ch.Name:lower():find("bat") or ch.Name:lower():find("slap")) then return ch end end return nil end local function bypassTrySwing() if bypassHitCD then return end bypassHitCD = true pcall(function() local char = LP.Character if char then local bat = bypassFindBat() if bat then if bat.Parent ~= char then local hum = char:FindFirstChildOfClass("Humanoid") if hum then pcall(function() hum:EquipTool(bat) end) end end pcall(function() bat:Activate() end) end end end) task.delay(BYPASS_SWING_CD, function() bypassHitCD = false end) end local function bypassGetClosestTarget() local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart") if not root then return nil, math.huge end local closest, minDist = nil, math.huge for _, plr in ipairs(Players:GetPlayers()) do if plr ~= LP and plr.Character then local tRoot = plr.Character:FindFirstChild("HumanoidRootPart") local hum = plr.Character:FindFirstChildOfClass("Humanoid") if tRoot and hum and hum.Health > 0 then local dist = (tRoot.Position - root.Position).Magnitude if dist < minDist then closest = tRoot; minDist = dist end end end end return closest, minDist end function resetAimbotTarget() local char = LP.Character local hrp = char and char:FindFirstChild("HumanoidRootPart") local hum = char and char:FindFirstChildOfClass("Humanoid") if hrp then hrp.Velocity = hrp.Velocity * 0.3; hrp.RotVelocity = Vector3.zero end if hum then hum.AutoRotate = true end end local function bypassRunTracking() local char = LP.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") local hum = char:FindFirstChildOfClass("Humanoid") if not root or not hum then return end hum.AutoRotate = false if not char:FindFirstChildOfClass("Tool") then local bat = bypassFindBat() if bat then pcall(function() hum:EquipTool(bat) end) end end local target, targetDist = bypassGetClosestTarget() if not target then root.Velocity = Vector3.new(root.Velocity.X * 0.3, root.Velocity.Y, root.Velocity.Z * 0.3) root.RotVelocity = Vector3.zero hum.AutoRotate = true return end local myPos = root.Position local targetPos = target.Position local direction = targetPos - myPos local flatDir = Vector3.new(direction.X, 0, direction.Z) if flatDir.Magnitude > 0 then flatDir = flatDir.Unit else flatDir = Vector3.zero end local chaseSpeed = 58 local desiredHeight = targetPos.Y + 3.7 local yVel = (desiredHeight - myPos.Y) * 19.5 if hum.FloorMaterial ~= Enum.Material.Air then yVel = math.max(yVel, 13) end yVel = math.clamp(yVel, -70, 110) local desiredVel = Vector3.new(flatDir.X * chaseSpeed, yVel, flatDir.Z * chaseSpeed) root.Velocity = root.Velocity:Lerp(desiredVel, 0.8) local toTarget = targetPos - myPos if toTarget.Magnitude > 0.1 then local goalCF = CFrame.lookAt(myPos, targetPos) local diffCF = root.CFrame:Inverse() * goalCF local rx, ry, rz = diffCF:ToEulerAnglesXYZ() rx = math.clamp(rx, -2.5, 2.5); ry = math.clamp(ry, -2.5, 2.5); rz = math.clamp(rz, -2.5, 2.5) root.RotVelocity = root.CFrame:VectorToWorldSpace(Vector3.new(rx * 42, ry * 42, rz * 42)) end if targetDist <= BYPASS_HIT_DIST then bypassTrySwing() end end local ENVY_CHASE_SPEED = 58 local ENVY_PREDICT_TIME = 0.14 local ENVY_Y_VEL_MULT = 19.5 local ENVY_MIN_Y_BOOST = 13 local ENVY_TILT_SPEED = 42 local ENVY_SWING_COOLDOWN = 0.08 local envyHittingCooldown = false local function envyFindBat() local char = LP.Character if not char then return nil end for _, tool in ipairs(char:GetChildren()) do if tool:IsA("Tool") and (tool.Name:lower():find("bat") or tool.Name:lower():find("slap")) then return tool end end local bp = LP:FindFirstChild("Backpack") if bp then for _, tool in ipairs(bp:GetChildren()) do if tool:IsA("Tool") and (tool.Name:lower():find("bat") or tool.Name:lower():find("slap")) then return tool end end end return nil end local function envyGetClosestTarget() local char = LP.Character if not char then return nil end local root = char:FindFirstChild("HumanoidRootPart") if not root then return nil end local closest, minDist = nil, math.huge for _, plr in ipairs(Players:GetPlayers()) do if plr ~= LP and plr.Character then local targetRoot = plr.Character:FindFirstChild("HumanoidRootPart") local hum = plr.Character:FindFirstChildOfClass("Humanoid") if targetRoot and hum and hum.Health > 0 then local dist = (targetRoot.Position - root.Position).Magnitude if dist < minDist then minDist = dist; closest = targetRoot end end end end return closest end local function envySwingBat() if envyHittingCooldown then return end envyHittingCooldown = true pcall(function() local char = LP.Character if not char then return end local hum = char:FindFirstChildOfClass("Humanoid") local tool = envyFindBat() if tool then if tool.Parent ~= char and hum then pcall(function() hum:EquipTool(tool) end) end local remote = tool:FindFirstChildOfClass("RemoteEvent") if remote then pcall(function() remote:FireServer() end) else pcall(function() tool:Activate() end) end end end) task.delay(ENVY_SWING_COOLDOWN, function() envyHittingCooldown = false end) end local function envyRunTracking() local char = LP.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") local hum = char:FindFirstChildOfClass("Humanoid") if not root or not hum then return end hum.AutoRotate = false if not char:FindFirstChildOfClass("Tool") then local bat = envyFindBat() if bat then pcall(function() hum:EquipTool(bat) end) end end local target = envyGetClosestTarget() if not target then root.Velocity = Vector3.new(root.Velocity.X * 0.3, root.Velocity.Y, root.Velocity.Z * 0.3) root.RotVelocity = Vector3.zero hum.AutoRotate = true return end local targetVel = target.Velocity local myPos = root.Position local targetPos = target.Position local predictPos = targetPos + targetVel * ENVY_PREDICT_TIME predictPos = predictPos + target.CFrame.LookVector * 0.3 local direction = predictPos - myPos local flatDir = Vector3.new(direction.X, 0, direction.Z).Unit local desiredHeight = targetPos.Y + 3.7 local yVel = (desiredHeight - myPos.Y) * ENVY_Y_VEL_MULT + targetVel.Y * 0.8 if hum.FloorMaterial ~= Enum.Material.Air then yVel = math.max(yVel, ENVY_MIN_Y_BOOST) end yVel = math.clamp(yVel, -70, 110) local desiredVel = Vector3.new(flatDir.X * ENVY_CHASE_SPEED, yVel, flatDir.Z * ENVY_CHASE_SPEED) root.Velocity = root.Velocity:Lerp(desiredVel, 0.8) local speed = targetVel.Magnitude local predictTime = math.clamp(speed / 150, 0.05, 0.2) local predictedPos = targetPos + targetVel * predictTime local toPredict = predictedPos - myPos if toPredict.Magnitude > 0.1 then local goalCF = CFrame.lookAt(myPos, predictedPos) local curCF = root.CFrame local diffCF = curCF:Inverse() * goalCF local rx, ry, rz = diffCF:ToEulerAnglesXYZ() rx = math.clamp(rx, -2.5, 2.5); ry = math.clamp(ry, -2.5, 2.5); rz = math.clamp(rz, -2.5, 2.5) root.RotVelocity = root.CFrame:VectorToWorldSpace(Vector3.new(rx * ENVY_TILT_SPEED, ry * ENVY_TILT_SPEED, rz * ENVY_TILT_SPEED)) end envySwingBat() end local function getAutoBatTarget() local root=LP.Character and LP.Character:FindFirstChild("HumanoidRootPart") if not root then return nil end local now=tick() if now-NM._autoBatLastScan<=0.1 and NM._autoBatTarget and NM._autoBatTarget.Parent then local hum=NM._autoBatTarget.Parent:FindFirstChildOfClass("Humanoid") if hum and hum.Health>0 then return NM._autoBatTarget end end NM._autoBatLastScan=now NM._autoBatTarget=nil local closest,minDist=nil,math.huge for _,plr in ipairs(Players:GetPlayers()) do if plr~=LP and plr.Character then local tRoot=plr.Character:FindFirstChild("HumanoidRootPart") local hum=plr.Character:FindFirstChildOfClass("Humanoid") if tRoot and hum and hum.Health>0 then local dist=(tRoot.Position-root.Position).Magnitude if dist<minDist then minDist=dist;closest=tRoot end end end end NM._autoBatTarget=closest return NM._autoBatTarget end NM.resetAutoBatMotion=function() local char=LP.Character local hrp=char and char:FindFirstChild("HumanoidRootPart") local hum=char and char:FindFirstChildOfClass("Humanoid") if hrp then hrp.Velocity=hrp.Velocity*0.3;hrp.RotVelocity=Vector3.zero end if hum then hum.AutoRotate=true end end local _autoTPWasEnabled=false _G.AimbotMode = "Input1" local _aimbotLastMode = "Input1" local function enableAutoBat() if NM.autoLeftEnabled then NM.autoLeftEnabled=false;if NM.autoLeftSetVisual then NM.autoLeftSetVisual(false) end;stopAutoLeft() end if NM.autoRightEnabled then NM.autoRightEnabled=false;if NM.autoRightSetVisual then NM.autoRightSetVisual(false) end;stopAutoRight() end if NM.autoTPEnabled then _autoTPWasEnabled=true;stopAutoTP();if NM.setAutoTPVisual then NM.setAutoTPVisual(false) end else _autoTPWasEnabled=false end NM.autoBatEquippedThisRun=false NM.autoBatEnabled=true end local function disableAutoBat() NM.autoBatEnabled=false NM.autoBatEquippedThisRun=false local char=LP.Character if char then local hum2=char:FindFirstChildOfClass("Humanoid") if hum2 then hum2.AutoRotate=true end end if NM.resetAutoBatMotion then NM.resetAutoBatMotion() end if _autoTPWasEnabled then _autoTPWasEnabled=false;NM.autoTPEnabled=true if NM.setAutoTPVisual then NM.setAutoTPVisual(true) end;startAutoTP() end end local function queueAutoLeftStart() NM.autoLeftEnabled=true if NM.autoRightEnabled then NM.autoRightEnabled=false;if NM.autoRightSetVisual then NM.autoRightSetVisual(false) end;stopAutoRight() end startAutoLeft() end local function queueAutoRightStart() NM.autoRightEnabled=true if NM.autoLeftEnabled then NM.autoLeftEnabled=false;if NM.autoLeftSetVisual then NM.autoLeftSetVisual(false) end;stopAutoLeft() end startAutoRight() end local function queueAutoBatStart() if NM.autoLeftEnabled then NM.autoLeftEnabled=false;if NM.autoLeftSetVisual then NM.autoLeftSetVisual(false) end;stopAutoLeft() end if NM.autoRightEnabled then NM.autoRightEnabled=false;if NM.autoRightSetVisual then NM.autoRightSetVisual(false) end;stopAutoRight() end _nmCleanLV() if _nmOwnerWatchConn then pcall(function() _nmOwnerWatchConn:Disconnect() end); _nmOwnerWatchConn = nil end enableAutoBat() end RunService.Heartbeat:Connect(function() if not NM.autoBatEnabled then return end if _aimbotLastMode ~= _G.AimbotMode then resetAimbotTarget() _aimbotLastMode = _G.AimbotMode end if _G.AimbotMode == "Input3" then envyRunTracking(); return end if _G.AimbotMode == "Input2" then bypassRunTracking(); return end local char=LP.Character local hum=char and char:FindFirstChildOfClass("Humanoid") local root=char and char:FindFirstChild("HumanoidRootPart") if not root or not hum then return end if not NM.autoBatEquippedThisRun then NM.autoBatEquippedThisRun=true if not char:FindFirstChildOfClass("Tool") then local bp=LP:FindFirstChildOfClass("Backpack") or LP:FindFirstChild("Backpack") local bpBat=bp and bp:FindFirstChild("Bat") if bpBat then pcall(function() hum:EquipTool(bpBat) end) end end end local target=getAutoBatTarget() if target then hum.AutoRotate=false local targetVel=target.Velocity local myPos=root.Position local targetPos=target.Position local predictPos=targetPos+targetVel*0.14+target.CFrame.LookVector*0.3 local direction=predictPos-myPos if direction.Magnitude>0.01 then local flatDir=Vector3.new(direction.X,0,direction.Z) if flatDir.Magnitude>0.01 then flatDir=flatDir.Unit local desiredHeight=targetPos.Y+3.7 local yVel=(desiredHeight-myPos.Y)*19.5+targetVel.Y*0.8 if hum.FloorMaterial~=Enum.Material.Air then yVel=math.max(yVel,13) end yVel=math.clamp(yVel,-70,110) local chaseSpeed=NM.AUTO_BAT_SPEED or 58 local desiredVel=Vector3.new(flatDir.X*chaseSpeed,yVel,flatDir.Z*chaseSpeed) root.Velocity=root.Velocity:Lerp(desiredVel,0.8) end local speed3=targetVel.Magnitude local predictTime=math.clamp(speed3/150,0.05,0.2) local predictedPos=targetPos+targetVel*predictTime local toPredict=predictedPos-myPos if toPredict.Magnitude>0.1 then local goalCF=CFrame.lookAt(myPos,predictedPos) local diffCF=root.CFrame:Inverse()*goalCF local rx,ry,rz=diffCF:ToEulerAnglesXYZ() rx=math.clamp(rx,-2.5,2.5);ry=math.clamp(ry,-2.5,2.5);rz=math.clamp(rz,-2.5,2.5) root.RotVelocity=root.CFrame:VectorToWorldSpace(Vector3.new(rx*42,ry*42,rz*42)) end end else hum.AutoRotate=true root.RotVelocity=Vector3.zero end if NM.autoSwingEnabled then local bat=char:FindFirstChild("Bat") if bat and bat:IsA("Tool") then bat:Activate() end end end) local AnimationPacks = { ["Zombie"] = { Name = "Zombie", Idle = {"616158929", "616160636", "885545458"}, Walk = {"616168032"}, Run = {"616163682"}, Jump = {"616161997"}, Fall = {"616157476"}, Climb = {"616156119"}, Sit = {"2506281703"}, Swim = {"616165109"}, SwimIdle = {"616166655"}, }, ["Superhero"] = { Name = "Superhero", Idle = {"616111295", "616113536", "885535855"}, Walk = {"616122287"}, Run = {"616117076"}, Jump = {"616115533"}, Fall = {"616108001"}, Climb = {"616104706"}, Sit = {"2506281703"}, Swim = {"616119360"}, SwimIdle = {"616120861"}, }, ["Ninja"] = { Name = "Ninja", Idle = {"656117400", "656118341", "886742569"}, Walk = {"656121766"}, Run = {"656118852"}, Jump = {"656117878"}, Fall = {"656115606"}, Climb = {"656114359"}, Sit = {"2506281703"}, Swim = {"656119721"}, SwimIdle = {"656121397"}, }, ["Toy"] = { Name = "Toy", Idle = {"782841498", "782845736", "980952228"}, Walk = {"782843345"}, Run = {"782842708"}, Jump = {"782847020"}, Fall = {"782846423"}, Climb = {"782843869"}, Sit = {"2506281703"}, Swim = {"782844582"}, SwimIdle = {"782845186"}, }, ["Stylish"] = { Name = "Stylish", Idle = {"616136790", "616138447", "886888594"}, Walk = {"616146177"}, Run = {"616140816"}, Jump = {"616139451"}, Fall = {"616134815"}, Climb = {"616133594"}, Sit = {"2506281703"}, Swim = {"616143378"}, SwimIdle = {"616144772"}, }, ["Vampire"] = { Name = "Vampire", Idle = {"1083445855", "1083450166", "1088037547"}, Walk = {"1083473930"}, Run = {"1083462077"}, Jump = {"1083455352"}, Fall = {"1083443587"}, Climb = {"1083439238"}, Sit = {"2506281703"}, Swim = {"1083464683"}, SwimIdle = {"1083467779"}, }, ["Werewolf"] = { Name = "Werewolf", Idle = {"1083195517", "1083214717", "1099492820"}, Walk = {"1083178339"}, Run = {"1083216690"}, Jump = {"1083218792"}, Fall = {"1083189019"}, Climb = {"1083182000"}, Sit = {"2506281703"}, Swim = {"1083222527"}, SwimIdle = {"1083225406"}, }, ["Knight"] = { Name = "Knight", Idle = {"657595757", "657568135", "885499184"}, Walk = {"657552124"}, Run = {"657564596"}, Jump = {"658409194"}, Fall = {"657600338"}, Climb = {"658360781"}, Sit = {"2506281703"}, Swim = {"657560551"}, SwimIdle = {"657557095"}, }, ["Mage"] = { Name = "Mage", Idle = {"707742142", "707855907", "885508740"}, Walk = {"707897309"}, Run = {"707861613"}, Jump = {"707853694"}, Fall = {"707829716"}, Climb = {"707826056"}, Sit = {"2506281703"}, Swim = {"707876443"}, SwimIdle = {"707894699"}, }, ["Astronaut"] = { Name = "Astronaut", Idle = {"891621366", "891633237", "1047759695"}, Walk = {"891636393"}, Run = {"891636393"}, Jump = {"891627522"}, Fall = {"891617961"}, Climb = {"891609353"}, Sit = {"2506281703"}, Swim = {"891639666"}, SwimIdle = {"891663592"}, }, ["Pirate"] = { Name = "Pirate", Idle = {"750781874", "750782770", "885515365"}, Walk = {"750785693"}, Run = {"750783738"}, Jump = {"750782230"}, Fall = {"750780242"}, Climb = {"750779899"}, Sit = {"2506281703"}, Swim = {"750784579"}, SwimIdle = {"750785176"}, }, ["Robot"] = { Name = "Robot", Idle = {"616088211", "616089559", "885531463"}, Walk = {"616095330"}, Run = {"616091570"}, Jump = {"616090535"}, Fall = {"616087089"}, Climb = {"616086039"}, Sit = {"2506281703"}, Swim = {"616092998"}, SwimIdle = {"616094091"}, }, ["Levitation"] = { Name = "Levitation", Idle = {"616006778", "616008087", "886862142"}, Walk = {"616013216"}, Run = {"616010382"}, Jump = {"616008936"}, Fall = {"616005863"}, Climb = {"616003713"}, Sit = {"2506281703"}, Swim = {"616011509"}, SwimIdle = {"616012453"}, }, ["Bubbly"] = { Name = "Bubbly", Idle = {"910004836", "910009958", "1018536639"}, Walk = {"910034870"}, Run = {"910025107"}, Jump = {"910016857"}, Fall = {"910001910"}, Climb = {"909997997"}, Sit = {"2506281703"}, Swim = {"910028158"}, SwimIdle = {"910030921"}, }, ["Cartoony"] = { Name = "Cartoony", Idle = {"742637544", "742638445", "885477856"}, Walk = {"742640026"}, Run = {"742638842"}, Jump = {"742637942"}, Fall = {"742637151"}, Climb = {"742636889"}, Sit = {"2506281703"}, Swim = {"742639220"}, SwimIdle = {"742639812"}, }, ["Elder"] = { Name = "Elder", Idle = {"845397899", "845400520", "901160519"}, Walk = {"845403856"}, Run = {"845386501"}, Jump = {"845398858"}, Fall = {"845396048"}, Climb = {"845392038"}, Sit = {"2506281703"}, Swim = {"845401742"}, SwimIdle = {"845403127"}, }, ["Popstar"] = { Name = "Popstar", Idle = {"1212900985", "1212954651"}, Walk = {"1212980338"}, Run = {"1212980348"}, Jump = {"1212954642"}, Fall = {"1212900995"}, Climb = {"1213044953"}, Sit = {"2506281703"}, Swim = {"1212852603"}, SwimIdle = {"1212998578"}, }, ["Princess"] = { Name = "Princess", Idle = {"941003647", "941013098"}, Walk = {"941028902"}, Run = {"941015281"}, Jump = {"941008832"}, Fall = {"941000007"}, Climb = {"940996062"}, Sit = {"2506281703"}, Swim = {"941018893"}, SwimIdle = {"941025398"}, }, ["Cowboy"] = { Name = "Cowboy", Idle = {"1014390418", "1014398616"}, Walk = {"1014421541"}, Run = {"1014401683"}, Jump = {"1014394726"}, Fall = {"1014384571"}, Climb = {"1014380606"}, Sit = {"2506281703"}, Swim = {"1014406523"}, SwimIdle = {"1014411816"}, }, ["Confident"] = { Name = "Confident", Idle = {"1069977950", "1069987858"}, Walk = {"1070017263"}, Run = {"1070001516"}, Jump = {"1069984524"}, Fall = {"1069973677"}, Climb = {"1069946257"}, Sit = {"2506281703"}, Swim = {"1070009914"}, SwimIdle = {"1070012133"}, }, ["Sneaky"] = { Name = "Sneaky", Idle = {"1132473842", "1132477671"}, Walk = {"1132510133"}, Run = {"1132494274"}, Jump = {"1132489853"}, Fall = {"1132469004"}, Climb = {"1132461372"}, Sit = {"2506281703"}, Swim = {"1132500520"}, SwimIdle = {"1132506407"}, }, ["Patrol"] = { Name = "Patrol", Idle = {"1149612882", "1150842221"}, Walk = {"1151231493"}, Run = {"1150967949"}, Jump = {"1150944216"}, Fall = {"1148863382"}, Climb = {"1148811837"}, Sit = {"2506281703"}, Swim = {"1151204998"}, SwimIdle = {"1151221899"}, }, ["Rthro"] = { Name = "Rthro", Idle = {"2510197257", "2510196951", "3711062489"}, Walk = {"2510202577"}, Run = {"2510198475"}, Jump = {"2510197830"}, Fall = {"2510195892"}, Climb = {"2510192778"}, Sit = {"2506281703"}, Swim = {"2510199791"}, SwimIdle = {"2510201162"}, }, } local function ApplyAnimationPack(packKey) local character = LP.Character if not character then return end NM._nmAnimPack = packKey if packKey == "Default" then local animate = character:FindFirstChild("Animate") if animate then animate:Destroy() end local ok,starterAnimate = pcall(function() return game:GetService("StarterPlayer").StarterCharacterScripts:FindFirstChild("Animate") end) if ok and starterAnimate then local newAnimate = starterAnimate:Clone() newAnimate.Parent = character end return end local pack = AnimationPacks[packKey] if not pack then return end local animate = character:FindFirstChild("Animate") if not animate then return end local function setAnim(path, ids) if not ids then return end local folder = animate:FindFirstChild(path) if folder then for i, animObj in ipairs(folder:GetChildren()) do if animObj:IsA("Animation") and ids[i] then animObj.AnimationId = "rbxassetid://" .. ids[i] end end end end setAnim("idle", pack.Idle);setAnim("walk", pack.Walk);setAnim("run", pack.Run) setAnim("jump", pack.Jump);setAnim("fall", pack.Fall);setAnim("climb", pack.Climb) setAnim("sit", pack.Sit);setAnim("swim", pack.Swim);setAnim("swimidle", pack.SwimIdle) end LP.CharacterAdded:Connect(function(char) NM.dropActive = false if _dropConn then _dropConn:Disconnect(); _dropConn = nil end task.wait(0.5) if NM.medusaCounterEnabled then setupMedusa(char) end if NM.medusaAutoResetEnabled then setupMedusaAutoReset(char) end if NM.bodyLockEnabled then task.delay(0.5, function() pcall(toggleBodyLock) end) end if NM.batCounterEnabled then NM.startBatCounter() end if NM.unwalkEnabled then task.wait(0.5);startUnwalk() end if NM.medTimerEnabled then task.delay(0.6, function() pcall(startMedTimer) end) end if NM._nmAnimPack and NM._nmAnimPack~="Default" then task.delay(1, function() pcall(ApplyAnimationPack, NM._nmAnimPack) end) end end) task.spawn(function() while task.wait(0.1) do if NM.autoBatEnabled and _aimbotLastMode ~= _G.AimbotMode then resetAimbotTarget() _aimbotLastMode = _G.AimbotMode end end end) local PP_HITBOX_SIZE = Vector3.new(NM._nmHitboxSize, NM._nmHitboxSize, NM._nmHitboxSize) local PP_HITBOX_TRANSPARENCY = 0.9 local ppConn = nil local function ppExpandHitbox(character) if not character or not character.Parent then return end if NM.ppCharacters[character] then return end local hrp = character:FindFirstChild("HumanoidRootPart") if not hrp then return end NM.ppCharacters[character] = { part = hrp, originalSize = hrp.Size, originalTransparency = hrp.Transparency } hrp.Size = Vector3.new(NM._nmHitboxSize, NM._nmHitboxSize, NM._nmHitboxSize) hrp.Transparency = PP_HITBOX_TRANSPARENCY hrp.CanCollide = false local highlight = Instance.new("Highlight") highlight.Name = "HitboxHighlight" highlight.FillColor = Color3.fromRGB(255, 0, 0) highlight.OutlineColor = Color3.fromRGB(255, 255, 255) highlight.FillTransparency = 0.95 highlight.OutlineTransparency = 0.8 highlight.Parent = character NM.ppCharacters[character].highlight = highlight end local function ppRestoreHitbox(character) local data = NM.ppCharacters[character] if not data then return end if data.part and data.part.Parent then data.part.Size = data.originalSize data.part.Transparency = data.originalTransparency end if data.highlight then data.highlight:Destroy() end NM.ppCharacters[character] = nil end local function ppScanAndExpand() for _, player in ipairs(Players:GetPlayers()) do if player ~= LP and player.Character then ppExpandHitbox(player.Character) end end end local function startPP() if ppConn then return end ppScanAndExpand() ppConn = RunService.Heartbeat:Connect(function() if not NM.ppEnabled then return end ppScanAndExpand() end) for _, player in ipairs(Players:GetPlayers()) do if player ~= LP then player.CharacterAdded:Connect(function(char) if not NM.ppEnabled then return end task.wait(0.1) ppExpandHitbox(char) end) end end Players.PlayerAdded:Connect(function(player) if player == LP then return end player.CharacterAdded:Connect(function(char) if not NM.ppEnabled then return end task.wait(0.1) ppExpandHitbox(char) end) end) end local function stopPP() if ppConn then ppConn:Disconnect(); ppConn = nil end for char, _ in pairs(NM.ppCharacters) do ppRestoreHitbox(char) end NM.ppCharacters = {} end local function saveConfig() local function ks(e) return {kb=e.kb and e.kb.Name or nil,gp=e.gp and e.gp.Name or nil} end local cfg={ normalSpeed=NM.NS,carrySpeed=NM.CS, speedProfile=NM._nmSpeedProfile,speedStashV1NS=NM._nmSpeedStash.v1 and NM._nmSpeedStash.v1.ns,speedStashV1CS=NM._nmSpeedStash.v1 and NM._nmSpeedStash.v1.cs,speedStashV2NS=NM._nmSpeedStash.v2 and NM._nmSpeedStash.v2.ns,speedStashV2CS=NM._nmSpeedStash.v2 and NM._nmSpeedStash.v2.cs, speedStyle=NM._nmSpeedStyle, dropBrainrotKey=ks(KB.DropBrainrot),autoLeftKey=ks(KB.AutoLeft),autoRightKey=ks(KB.AutoRight), autoBatKey=ks(KB.AutoBat),laggerToggleKey=ks(KB.LaggerToggle),tpFloorKey=ks(KB.TPFloor),instaResetKey=ks(KB.InstaReset),guiHideKey=ks(KB.GuiHide), speedToggleKey=ks(KB.SpeedToggle), infiniteJump=NM.infJumpEnabled,jumpMode=NM._nmJumpMode,medusaCounter=NM.medusaCounterEnabled,medusaAutoReset=NM.medusaAutoResetEnabled, bodyLock=NM.bodyLockEnabled,bodyLockRange=NM.bodyLockRange, dropStyle=NM._nmDropStyle, batCounter=NM.batCounterEnabled, carryMode=NM.speedMode,laggerMode=NM.laggerToggled,laggerCarryMode=NM.laggerPhase==2,laggerSpeed=NM.LAGGER_SPEED,laggerCarrySpeed=NM.LAGGER_CARRY_SPEED, autoBat=NM.autoBatEnabled,autoSwing=NM.autoSwingEnabled, aimbotMode=_G.AimbotMode, input2Enabled=NM.input2Enabled, autoBatSpeed=NM.AUTO_BAT_SPEED, alSpeed=NM.AL_SPEED,arSpeed=NM.AR_SPEED, unwalkEnabled=NM.unwalkEnabled, antiLag=NM.antiLagEnabled,stretchRez=NM.stretchRezEnabled, autoTPEnabled=NM.autoTPEnabled,autoTPHeight=NM.autoTPHeight, uiPositionLocked=NM.uiPositionLocked, medTimer=NM.medTimerEnabled, envyAutoBatToggled=NM.envyAutoBatToggled, envyAutoBatButtonVisible=NM.envyAutoBatButtonVisible, envyAutoBatKey=NM.envyAutoBatKey.Name, envyAutoBatKeyType=NM.envyAutoBatKeyType, ppEnabled=NM.ppEnabled, aimbotModuleEnabled=NM.aimbotModuleEnabled, antiRagdoll=_G._nmSharedAR and _G._nmSharedAR.state or false, aimbotModuleKey=NM.aimbotModuleKey.Name, nmAutoSteal=NM._nmStealEnabled, nmStealRadius=NM._nmStealRadius, nmStealDuration=NM._nmStealDuration, nmStealFireRange=NM._nmStealFireRange, nmStealHoldMin=NM._nmStealHoldMin, nmStealHoldMax=NM._nmStealHoldMax, nmStealEntryDelay=NM._nmStealEntryDelay, nmStealGuiType=NM._nmStealGuiType, aimbotModuleKeyType=NM.aimbotModuleKeyType, antiKick=NM._nmAntiKickEnabled, espEnabled=NM._nmESPEnabled, espColorR=NM._nmESPColor.R, espColorG=NM._nmESPColor.G, espColorB=NM._nmESPColor.B, hitboxSize=NM._nmHitboxSize, animPack=NM._nmAnimPack } if writefile then pcall(function() writefile("novaMoonPC.json",HS:JSONEncode(cfg)) end) end end task.spawn(function() while task.wait(5) do saveConfig() saveButtonPositions() end end) local BTN_POS_FILE = "novaMoonPC_btn_positions.json" local savedBtnPositions = {} local function saveButtonPositions() local posData = {} for name, data in pairs(savedBtnPositions) do if data.btn and data.btn.Parent then posData[name] = { X = {Scale = data.btn.Position.X.Scale, Offset = data.btn.Position.X.Offset}, Y = {Scale = data.btn.Position.Y.Scale, Offset = data.btn.Position.Y.Offset} } end end if writefile then pcall(function() writefile(BTN_POS_FILE, HS:JSONEncode(posData)) end) end end local function loadButtonPositions() if not (isfile and isfile(BTN_POS_FILE)) then return {} end local ok, data = pcall(function() return HS:JSONDecode(readfile(BTN_POS_FILE)) end) if ok and type(data) == "table" then return data end return {} end local function resetButtonPositions() if isfile and isfile(BTN_POS_FILE) then pcall(function() delfile(BTN_POS_FILE) end) end savedBtnPositions = {} end local setUnwalkVisual,setAntiLagVisual,setAutoSwingVisual local normalBox,carryBox,laggerBox,laggerCarryBox,autoTPHeightBox,bodyLockRangeBox local function refreshSpeedModeLabel() if modeValLbl then modeValLbl.Text=NM.laggerToggled and (NM.laggerPhase==2 and "Lagger Carry" or "Lagger Normal") or (NM.speedMode and "Carry" or "Normal") end end local function toggleCarryMode() if NM.laggerToggled then NM.laggerToggled=false; NM.laggerPhase=0; NM.speedMode=true else NM.speedMode=not NM.speedMode if NM.speedMode and NM.laggerToggled then NM.laggerToggled=false; NM.laggerPhase=0 end end refreshSpeedModeLabel() end local function toggleLaggerMode() if not NM.laggerToggled then NM.speedMode=false; NM.laggerToggled=true; NM.laggerPhase=2 elseif NM.laggerPhase==2 then NM.laggerPhase=1 else NM.laggerPhase=2 end refreshSpeedModeLabel() end local btnWidthBox, btnHeightBox local _arEnabled = false local _arResetCooldown = 0 local _arConnection = nil local function _arForceReset() local char = LP.Character if not char then return end local hum = char:FindFirstChildOfClass("Humanoid") local root = char:FindFirstChild("HumanoidRootPart") if not hum or not root or hum.Health <= 0 then return end pcall(function() hum:ChangeState(Enum.HumanoidStateType.GettingUp) root.Velocity = Vector3.zero root.RotVelocity = Vector3.zero root.Velocity = Vector3.zero root.RotVelocity = Vector3.zero for _, obj in ipairs(char:GetDescendants()) do if obj:IsA("Motor6D") then obj.Enabled = true end if obj:IsA("Constraint") then obj.Enabled = true end end workspace.CurrentCamera.CameraSubject = hum local PM = LP.PlayerScripts:FindFirstChild("PlayerModule") if PM then local CM = require(PM:FindFirstChild("ControlModule")) if CM then CM:Enable() end end hum.AutoRotate = true hum.PlatformStand = false hum.Sit = false end) end local function _arStart() if _arConnection then return end _arEnabled = true _arConnection = RunService.Heartbeat:Connect(function() if not _arEnabled then return end local char = LP.Character if not char then return end local hum = char:FindFirstChildOfClass("Humanoid") if not hum or hum.Health <= 0 then return end local state = hum:GetState() local isRagdolled = (state == Enum.HumanoidStateType.Physics or state == Enum.HumanoidStateType.Ragdoll or state == Enum.HumanoidStateType.FallingDown) if isRagdolled then local now = tick() if now - _arResetCooldown > 0.15 then _arResetCooldown = now _arForceReset() end end end) end local function _arStop() _arEnabled = false if _arConnection then _arConnection:Disconnect() _arConnection = nil end end local function _nmInitAutoSteal() local Players = game:GetService("Players") local RunService = game:GetService("RunService") local LP = Players.LocalPlayer local StealRadius = NM._nmStealRadius local Steal = { AutoStealEnabled = false, StealRadius = StealRadius, StealDuration = NM._nmStealDuration, Mode = "half", HalfFireRange = NM._nmStealFireRange, HalfHoldMin = NM._nmStealHoldMin, HalfHoldMax = NM._nmStealHoldMax, HalfEntryDelay = NM._nmStealEntryDelay, Data = {} } local isStealing = false local stealStartTime = nil local stealEndTime = nil local stealCompleted = false local autoConn = nil local progressFill, progressTitle local function isMyPlotByName(plotName) local plots = workspace:FindFirstChild("Plots") if not plots then return false end local plot = plots:FindFirstChild(plotName) if not plot then return false end local sign = plot:FindFirstChild("PlotSign") if sign then local yb = sign:FindFirstChild("YourBase") if yb and yb:IsA("BillboardGui") then return yb.Enabled == true end end return false end local function findNearestPrompt() local char = LP.Character if not char then return nil end local root = char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso") if not root then return nil end local plots = workspace:FindFirstChild("Plots") if not plots then return nil end local nearest, dist = nil, math.huge for _, plot in ipairs(plots:GetChildren()) do if plot:IsA("Model") and not isMyPlotByName(plot.Name) then local pods = plot:FindFirstChild("AnimalPodiums") if pods then for _, pod in ipairs(pods:GetChildren()) do local base = pod:FindFirstChild("Base") local sp = base and base:FindFirstChild("Spawn") if sp then local d = (sp.Position - root.Position).Magnitude if d <= NM._nmStealRadius and d < dist then local found = nil local att = sp:FindFirstChild("PromptAttachment") if att then for _, pr in ipairs(att:GetChildren()) do if pr:IsA("ProximityPrompt") and pr.ActionText and pr.ActionText:find("Steal") then found = pr end end end if not found then for _, pr in ipairs(sp:GetDescendants()) do if pr:IsA("ProximityPrompt") and pr.ActionText and pr.ActionText:find("Steal") then found = pr end end end if found then nearest, dist = found, d end end end end end end end return nearest end local function _promptDist(prompt) local char = LP.Character if not char then return math.huge end local root = char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso") if not root then return math.huge end local part = prompt.Parent if part and part:IsA("Attachment") then part = part.Parent end if part and part:IsA("BasePart") then return (part.Position - root.Position).Magnitude end local ok, cf = pcall(function() return prompt.Parent and prompt.Parent.WorldPosition end) if ok and cf then return (cf - root.Position).Magnitude end return math.huge end local function executeSteal(prompt) if isStealing then return end if not Steal.Data[prompt] then Steal.Data[prompt] = {hold = {}, trigger = {}, ready = true} if getconnections then for _, c in ipairs(getconnections(prompt.PromptButtonHoldBegan)) do if c.Function then table.insert(Steal.Data[prompt].hold, c.Function) end end for _, c in ipairs(getconnections(prompt.Triggered)) do if c.Function then table.insert(Steal.Data[prompt].trigger, c.Function) end end end end local data = Steal.Data[prompt] if not data.ready then return end data.ready = false isStealing = true stealCompleted = false stealStartTime = tick() stealEndTime = nil task.spawn(function() for _, fn in ipairs(data.hold) do task.spawn(fn) end task.wait(NM._nmStealHoldMin) local inRange = _promptDist(prompt) <= NM._nmStealFireRange while true do local el = tick() - stealStartTime if el > NM._nmStealHoldMax or not prompt.Parent then break end if _promptDist(prompt) <= NM._nmStealFireRange then if not inRange then task.wait(NM._nmStealEntryDelay) end for _, fn in ipairs(data.trigger) do task.spawn(fn) end break end task.wait() end stealCompleted = true stealEndTime = tick() task.wait(0.5) data.ready = true isStealing = false stealCompleted = false end) end local function startAutoSteal() if autoConn then return end autoConn = RunService.Heartbeat:Connect(function() if not Steal.AutoStealEnabled or isStealing then return end local p = findNearestPrompt() if p then executeSteal(p) end end) end local function stopAutoSteal() if autoConn then autoConn:Disconnect(); autoConn = nil end Steal.AutoStealEnabled = false isStealing = false stealCompleted = false end local AceDuelsAutoStealTopBar = Instance.new("ScreenGui") AceDuelsAutoStealTopBar.Name = "AceDuelsAutoStealTopBar" AceDuelsAutoStealTopBar.IgnoreGuiInset = true AceDuelsAutoStealTopBar.ResetOnSpawn = false AceDuelsAutoStealTopBar.DisplayOrder = 999 AceDuelsAutoStealTopBar.ZIndexBehavior = Enum.ZIndexBehavior.Sibling AceDuelsAutoStealTopBar.Parent = LP:WaitForChild("PlayerGui") AceDuelsAutoStealTopBar.Enabled = false NM._nmStealGui = AceDuelsAutoStealTopBar local Container = Instance.new("Frame") Container.Name = "Container" Container.Size = UDim2.new(0, 380, 0, 36) Container.Position = UDim2.new(0.5, -190, 1, -80) Container.BackgroundTransparency = 1 Container.BorderSizePixel = 0 Container.Parent = AceDuelsAutoStealTopBar local StealBarShadow = Instance.new("Frame") StealBarShadow.Name = "StealBarShadow" StealBarShadow.ZIndex = 1 StealBarShadow.Size = UDim2.new(1, 0, 1, 0) StealBarShadow.Position = UDim2.new(0, 0, 0, 3) StealBarShadow.BackgroundColor3 = Color3.fromRGB(0, 0, 0) StealBarShadow.BackgroundTransparency = 0.55 StealBarShadow.BorderSizePixel = 0 StealBarShadow.Parent = Container Instance.new("UICorner", StealBarShadow).CornerRadius = UDim.new(1, 0) local StealBar = Instance.new("Frame") StealBar.Name = "StealBar" StealBar.ZIndex = 2 StealBar.Size = UDim2.new(1, 0, 1, 0) StealBar.Position = UDim2.new(0, 0, 0, 0) StealBar.BackgroundColor3 = Color3.fromRGB(10, 10, 18) StealBar.BackgroundTransparency = 0.04 StealBar.BorderSizePixel = 0 StealBar.ClipsDescendants = true StealBar.Parent = Container Instance.new("UICorner", StealBar).CornerRadius = UDim.new(1, 0) local UIStroke = Instance.new("UIStroke") UIStroke.Color = Color3.fromRGB(60, 60, 75) UIStroke.Thickness = 1 UIStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border UIStroke.Transparency = 0.18 UIStroke.Parent = StealBar local LeftToRightFill = Instance.new("Frame") LeftToRightFill.Name = "LeftToRightFill" LeftToRightFill.ZIndex = 1 LeftToRightFill.Size = UDim2.new(0, 0, 1, 0) LeftToRightFill.BackgroundColor3 = Color3.fromRGB(255, 255, 255) LeftToRightFill.BackgroundTransparency = 0.82 LeftToRightFill.BorderSizePixel = 0 LeftToRightFill.Parent = StealBar Instance.new("UICorner", LeftToRightFill).CornerRadius = UDim.new(1, 0) local FillGradient = Instance.new("UIGradient") FillGradient.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, Color3.fromRGB(80, 80, 90)), ColorSequenceKeypoint.new(0.4, Color3.fromRGB(150, 150, 170)), ColorSequenceKeypoint.new(0.8, Color3.fromRGB(200, 200, 220)), ColorSequenceKeypoint.new(1, Color3.fromRGB(180, 180, 200)) }) FillGradient.Rotation = 0 FillGradient.Parent = LeftToRightFill local Title = Instance.new("TextLabel") Title.Name = "Title" Title.ZIndex = 3 Title.Position = UDim2.new(0, 0, 0, 0) Title.Size = UDim2.new(1, 0, 1, 0) Title.BackgroundTransparency = 1 Title.Text = "0%" Title.TextColor3 = Color3.fromRGB(255, 255, 255) Title.TextSize = 15 Title.Font = Enum.Font.GothamBlack Title.TextXAlignment = Enum.TextXAlignment.Center Title.TextStrokeTransparency = 0.6 Title.TextStrokeColor3 = Color3.fromRGB(0, 0, 0) Title.Parent = StealBar progressFill = LeftToRightFill progressTitle = Title local _lastPct = 0 local _visualSpeed = 0.35 local _decaySpeed = 1.5 RunService.RenderStepped:Connect(function(dt) local targetPct = 0 if isStealing and stealStartTime then if stealCompleted then local timeSinceComplete = tick() - (stealEndTime or tick()) if timeSinceComplete < 0.3 then targetPct = 1 else targetPct = math.max(0, 1 - (timeSinceComplete - 0.3) * _decaySpeed) end else local rawPct = math.clamp((tick() - stealStartTime) / math.max(NM._nmStealHoldMin, 0.01), 0, 1) targetPct = rawPct end else targetPct = 0 end _lastPct = _lastPct + (targetPct - _lastPct) * math.min(dt * _visualSpeed * 60, 1) local f = math.clamp(_lastPct, 0, 1) progressFill.Size = UDim2.new(f, 0, 1, 0) progressTitle.Text = math.floor(f * 100) .. "%" end) local dragging = false local dragOffset = Vector2.new(0, 0) local dragConn = nil local dragEndConn = nil StealBar.InputBegan:Connect(function(inp) if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then dragging = true local absPos = Container.AbsolutePosition dragOffset = Vector2.new(inp.Position.X - absPos.X, inp.Position.Y - absPos.Y) if dragConn then dragConn:Disconnect(); dragConn = nil end if dragEndConn then dragEndConn:Disconnect(); dragEndConn = nil end dragConn = UIS.InputChanged:Connect(function(changedInp) if not dragging then return end if changedInp.UserInputType == Enum.UserInputType.MouseMovement or changedInp.UserInputType == Enum.UserInputType.Touch then local cam = workspace.CurrentCamera local vp = cam and cam.ViewportSize or Vector2.new(1000, 1000) local sz = Container.AbsoluteSize local newAbsX = changedInp.Position.X - dragOffset.X local newAbsY = changedInp.Position.Y - dragOffset.Y newAbsX = math.clamp(newAbsX, 0, vp.X - sz.X) newAbsY = math.clamp(newAbsY, 0, vp.Y - sz.Y) Container.Position = UDim2.new(0, newAbsX, 0, newAbsY) end end) dragEndConn = UIS.InputEnded:Connect(function(endInp) if endInp.UserInputType == Enum.UserInputType.MouseButton1 or endInp.UserInputType == Enum.UserInputType.Touch then dragging = false if dragConn then dragConn:Disconnect(); dragConn = nil end if dragEndConn then dragEndConn:Disconnect(); dragEndConn = nil end end end) end end) local NewStealGui = Instance.new("ScreenGui") NewStealGui.Name = "StealProgressScreenGui" NewStealGui.ResetOnSpawn = false NewStealGui.DisplayOrder = 200 NewStealGui.IgnoreGuiInset = true NewStealGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling NewStealGui.Parent = LP:WaitForChild("PlayerGui") NewStealGui.Enabled = false NM._nmStealGuiNew = NewStealGui local spFrame = Instance.new("Frame", NewStealGui) spFrame.Name = "StealProgressGui" spFrame.Size = UDim2.new(0, 380, 0, 36) spFrame.Position = UDim2.new(0.5, 0, 1, -44) spFrame.AnchorPoint = Vector2.new(0.5, 1) spFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 18) spFrame.BackgroundTransparency = 0.04 spFrame.BorderSizePixel = 0; spFrame.Active = true; spFrame.Visible = true spFrame.ZIndex = 300; spFrame.ClipsDescendants = true Instance.new("UICorner", spFrame).CornerRadius = UDim.new(1, 0) local spBorder = Instance.new("UIStroke", spFrame) spBorder.Color = Color3.fromRGB(60, 60, 75); spBorder.Thickness = 1; spBorder.Transparency = 0.18 spBorder.ApplyStrokeMode = Enum.ApplyStrokeMode.Border local _spWasDragged = false local spToggleBtn = Instance.new("TextButton", spFrame) spToggleBtn.Name = "ToggleGUIBtn"; spToggleBtn.Size = UDim2.new(1, 0, 1, 0) spToggleBtn.BackgroundTransparency = 1; spToggleBtn.Text = ""; spToggleBtn.AutoButtonColor = false; spToggleBtn.ZIndex = 310 spToggleBtn.Activated:Connect(function() if _spWasDragged then return end local base = spFrame.Size TS:Create(spFrame, TweenInfo.new(0.08), {Size = UDim2.new(base.X.Scale, base.X.Offset * 0.95, base.Y.Scale, base.Y.Offset * 0.9)}):Play() task.delay(0.1, function() TS:Create(spFrame, TweenInfo.new(0.16, Enum.EasingStyle.Back), {Size = base}):Play() end) end) local spFill = Instance.new("Frame", spFrame) spFill.Name = "ProgressFill"; spFill.Size = UDim2.new(0, 0, 1, 0); spFill.Position = UDim2.new(0, 0, 0, 0) spFill.BackgroundColor3 = Color3.fromRGB(255, 255, 255); spFill.BackgroundTransparency = 0.82 spFill.BorderSizePixel = 0; spFill.ZIndex = 301 Instance.new("UICorner", spFill).CornerRadius = UDim.new(1, 0) local spFillGradient = Instance.new("UIGradient", spFill) spFillGradient.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, Color3.fromRGB(80, 80, 90)), ColorSequenceKeypoint.new(0.4, Color3.fromRGB(150, 150, 170)), ColorSequenceKeypoint.new(0.8, Color3.fromRGB(200, 200, 220)), ColorSequenceKeypoint.new(1, Color3.fromRGB(180, 180, 200)) }) spFillGradient.Rotation = 0 local spPct = Instance.new("TextLabel", spFrame) spPct.Size = UDim2.new(1, 0, 1, 0); spPct.Position = UDim2.new(0, 0, 0, 0) spPct.BackgroundTransparency = 1; spPct.Text = "UNREADY"; spPct.TextColor3 = Color3.fromRGB(255, 255, 255) spPct.Font = Enum.Font.GothamBlack; spPct.TextSize = 15; spPct.TextStrokeTransparency = 0.6; spPct.TextStrokeColor3 = Color3.fromRGB(0, 0, 0) spPct.TextXAlignment = Enum.TextXAlignment.Center; spPct.TextYAlignment = Enum.TextYAlignment.Center; spPct.ZIndex = 304 local _spLastPct = 0 RunService.RenderStepped:Connect(function() if not NewStealGui.Enabled then return end local pct = 0 if isStealing and stealStartTime then pct = math.clamp((tick() - stealStartTime) / math.max(NM._nmStealHoldMin, 0.01), 0, 1) elseif Steal.AutoStealEnabled then pct = (findNearestPrompt() and 1 or 0) end _spLastPct = _spLastPct + (pct - _spLastPct) * 0.2 local f = math.clamp(_spLastPct, 0, 1) spFill.Size = UDim2.new(f, 0, 1, 0) if _spLastPct >= 0.87 then spPct.Text = "READY"; spPct.TextColor3 = Color3.fromRGB(120, 255, 140) else spPct.Text = "UNREADY"; spPct.TextColor3 = Color3.fromRGB(255, 255, 255) end end) local spDragging, spDragStart, spStartPos = false, nil, nil spToggleBtn.InputBegan:Connect(function(inp) if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then spDragging = true; _spWasDragged = false; spDragStart = inp.Position; spStartPos = spFrame.Position inp.Changed:Connect(function() if inp.UserInputState == Enum.UserInputState.End then spDragging = false end end) end end) UIS.InputChanged:Connect(function(inp) if not spDragging then return end if inp.UserInputType == Enum.UserInputType.MouseMovement or inp.UserInputType == Enum.UserInputType.Touch then local dx = inp.Position.X - spDragStart.X local dy = inp.Position.Y - spDragStart.Y if math.abs(dx) > 5 or math.abs(dy) > 5 then _spWasDragged = true end local cam = workspace.CurrentCamera local vp = cam and cam.ViewportSize or Vector2.new(1000, 1000) local sz = spFrame.AbsoluteSize local newXScalePx = spStartPos.X.Scale * vp.X local newX = math.clamp(newXScalePx + spStartPos.X.Offset + dx, sz.X / 2, vp.X - sz.X / 2) local newY = math.clamp(spStartPos.Y.Scale * vp.Y + spStartPos.Y.Offset + dy, sz.Y, vp.Y) spFrame.Position = UDim2.new(spStartPos.X.Scale, newX - newXScalePx, spStartPos.Y.Scale, newY - spStartPos.Y.Scale * vp.Y) end end) function _G._nmSetStealGuiType(t) NM._nmStealGuiType = t if NM._nmStealEnabled then if AceDuelsAutoStealTopBar then AceDuelsAutoStealTopBar.Enabled = (t == "normal") end if NewStealGui then NewStealGui.Enabled = (t == "new") end end if NM._nmStealTypeSetVisual then NM._nmStealTypeSetVisual(t) end end function _G._nmToggleAutoSteal(on) NM._nmStealEnabled = on Steal.AutoStealEnabled = on if on then pcall(startAutoSteal) if NM._nmStealGuiType == "new" then if NewStealGui then NewStealGui.Enabled = true end if NM._nmStealGui then NM._nmStealGui.Enabled = false end else if NM._nmStealGui then NM._nmStealGui.Enabled = true end if NewStealGui then NewStealGui.Enabled = false end end else stopAutoSteal() if NM._nmStealGui then NM._nmStealGui.Enabled = false end if NewStealGui then NewStealGui.Enabled = false end end if NM._nmStealSetVisual then NM._nmStealSetVisual(on) end end end _nmInitAutoSteal() local function buildGui() local BG = Color3.fromRGB(15,15,25) local BG2 = Color3.fromRGB(25,25,40) local CARD = Color3.fromRGB(30,30,48) local HOV = Color3.fromRGB(45,45,70) local RED = Color3.fromRGB(235,235,245) local REDDIM= Color3.fromRGB(180,180,200) local STROKE= Color3.fromRGB(90,90,120) local W = Color3.fromRGB(240,240,255) local DIM = Color3.fromRGB(120,120,150) local INP = Color3.fromRGB(20,20,35) local OFF = Color3.fromRGB(40,40,60) local old=game:GetService("CoreGui"):FindFirstChild("NovaMoonHub");if old then old:Destroy() end local pg=LP:FindFirstChild("PlayerGui");if pg then local o=pg:FindFirstChild("NovaMoonHub");if o then o:Destroy() end end local gui=Instance.new("ScreenGui") gui.Name="NovaMoonHub";gui.ResetOnSpawn=false;gui.DisplayOrder=10;gui.IgnoreGuiInset=true pcall(function() if syn and syn.protect_gui then syn.protect_gui(gui) end end) if not pcall(function() gui.Parent=game:GetService("CoreGui") end) then gui.Parent=LP:WaitForChild("PlayerGui") end local main=Instance.new("Frame",gui) main.Name="MainPanel" main.Size=UDim2.new(0,380,0,560);main.Position=UDim2.new(0,20,0,20) main.BackgroundColor3=BG;main.BorderSizePixel=0;main.ClipsDescendants=false Instance.new("UICorner",main).CornerRadius=UDim.new(0,20) local mainStroke=Instance.new("UIStroke",main);mainStroke.Color=Color3.fromRGB(60,60,90);mainStroke.Thickness=1.5 local mainGlow=Instance.new("UIStroke",main) mainGlow.Name="EdgeGlow" mainGlow.Color=Color3.fromRGB(180,180,200) mainGlow.Thickness=2 mainGlow.Transparency=0.7 mainGlow.ApplyStrokeMode=Enum.ApplyStrokeMode.Border task.spawn(function() while main and main.Parent do TS:Create(mainGlow,TweenInfo.new(2,Enum.EasingStyle.Sine,Enum.EasingDirection.InOut),{Transparency=0.5}):Play() task.wait(2) if not main or main.Parent then break end TS:Create(mainGlow,TweenInfo.new(2,Enum.EasingStyle.Sine,Enum.EasingDirection.InOut),{Transparency=0.8}):Play() task.wait(2) end end) local function drag(f) local dn,ds,sp,di=false f.InputBegan:Connect(function(i) if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then dn=true;ds=i.Position;sp=f.Position i.Changed:Connect(function() if i.UserInputState==Enum.UserInputState.End then dn=false end end) end end) f.InputChanged:Connect(function(i) if i.UserInputType==Enum.UserInputType.MouseMovement or i.UserInputType==Enum.UserInputType.Touch then di=i end end) UIS.InputChanged:Connect(function(i) if i==di and dn then local nX=sp.X.Offset+(i.Position.X-ds.X) local nY=sp.Y.Offset+(i.Position.Y-ds.Y) f.Position=UDim2.new(sp.X.Scale,nX,sp.Y.Scale,nY) end end) end drag(main) local hdr=Instance.new("Frame",main) hdr.Size=UDim2.new(1,0,0,60);hdr.BackgroundColor3=BG2;hdr.BorderSizePixel=0 Instance.new("UICorner",hdr).CornerRadius=UDim.new(0,20) local hdrStroke=Instance.new("UIStroke",hdr);hdrStroke.Color=Color3.fromRGB(80,80,95);hdrStroke.Thickness=1 local brand=Instance.new("TextLabel",hdr) brand.Size=UDim2.new(1,-80,0,36);brand.Position=UDim2.new(0,66,0,6) brand.BackgroundTransparency=1;brand.Text="NOVA MOON" brand.TextColor3=Color3.fromRGB(255,255,255);brand.Font=Enum.Font.GothamBlack;brand.TextSize=22 brand.TextXAlignment=Enum.TextXAlignment.Left local brandStroke=Instance.new("UIStroke",brand);brandStroke.Color=Color3.fromRGB(60,60,80);brandStroke.Thickness=0.8 local avatarFrame=Instance.new("Frame",hdr) avatarFrame.Size=UDim2.new(0,48,0,48);avatarFrame.Position=UDim2.new(0,10,0,8) avatarFrame.BackgroundColor3=Color3.fromRGB(30,30,45);avatarFrame.BorderSizePixel=0 Instance.new("UICorner",avatarFrame).CornerRadius=UDim.new(1,0) local avatarStroke=Instance.new("UIStroke",avatarFrame);avatarStroke.Color=Color3.fromRGB(80,80,100);avatarStroke.Thickness=2 local avatarImg=Instance.new("ImageLabel",avatarFrame) avatarImg.Size=UDim2.new(1,0,1,0);avatarImg.BackgroundTransparency=1;avatarImg.Image="" avatarImg.ScaleType=Enum.ScaleType.Crop Instance.new("UICorner",avatarImg).CornerRadius=UDim.new(1,0) task.spawn(function() local success,content=pcall(function() return Players:GetUserThumbnailAsync(LP.UserId,Enum.ThumbnailType.HeadShot,Enum.ThumbnailSize.Size48x48) end) if success then avatarImg.Image=content end end) local nameLbl=Instance.new("TextLabel",hdr) nameLbl.Size=UDim2.new(0,160,0,18);nameLbl.Position=UDim2.new(0,66,0,42) nameLbl.BackgroundTransparency=1;nameLbl.Text=LP.Name nameLbl.TextColor3=Color3.fromRGB(200,200,220);nameLbl.Font=Enum.Font.GothamBlack;nameLbl.TextSize=14 nameLbl.TextXAlignment=Enum.TextXAlignment.Left local closeBtn=Instance.new("TextButton",hdr) closeBtn.Size=UDim2.new(0,32,0,32);closeBtn.Position=UDim2.new(1,-40,0,8) closeBtn.BackgroundColor3=Color3.fromRGB(45,45,65);closeBtn.BorderSizePixel=0 closeBtn.Text="—";closeBtn.TextColor3=Color3.fromRGB(220,220,235);closeBtn.Font=Enum.Font.GothamBold;closeBtn.TextSize=18 Instance.new("UICorner",closeBtn).CornerRadius=UDim.new(0,10) local closeStroke=Instance.new("UIStroke",closeBtn);closeStroke.Color=Color3.fromRGB(60,60,85);closeStroke.Thickness=1 closeBtn.MouseEnter:Connect(function() TS:Create(closeBtn,TweenInfo.new(0.15),{BackgroundColor3=Color3.fromRGB(130,130,145)}):Play();TS:Create(closeStroke,TweenInfo.new(0.15),{Color=Color3.fromRGB(220,220,230)}):Play() end) closeBtn.MouseLeave:Connect(function() TS:Create(closeBtn,TweenInfo.new(0.15),{BackgroundColor3=Color3.fromRGB(45,45,65)}):Play();TS:Create(closeStroke,TweenInfo.new(0.15),{Color=Color3.fromRGB(60,60,85)}):Play() end) local miniBtn=Instance.new("TextButton",gui) miniBtn.Name="NovaMoonMiniBtn" miniBtn.Size=UDim2.new(0,130,0,36);miniBtn.Position=UDim2.new(0,26,0,50) miniBtn.BackgroundColor3=BG2;miniBtn.BorderSizePixel=0 miniBtn.Text="NOVA MOON";miniBtn.TextColor3=Color3.fromRGB(240,240,245);miniBtn.Font=Enum.Font.GothamBlack;miniBtn.TextSize=13 miniBtn.ZIndex=20;miniBtn.Visible=false Instance.new("UICorner",miniBtn).CornerRadius=UDim.new(0,12) local miniStroke=Instance.new("UIStroke",miniBtn);miniStroke.Color=Color3.fromRGB(100,100,120);miniStroke.Thickness=1.5 drag(miniBtn) local miniGlow=Instance.new("UIStroke",miniBtn) miniGlow.Name="MiniGlow" miniGlow.Color=Color3.fromRGB(200,200,220) miniGlow.Thickness=1.5 miniGlow.Transparency=0.6 miniGlow.ApplyStrokeMode=Enum.ApplyStrokeMode.Border miniBtn.MouseEnter:Connect(function() TS:Create(miniBtn,TweenInfo.new(0.1),{BackgroundColor3=HOV}):Play() TS:Create(miniGlow,TweenInfo.new(0.1),{Transparency=0.3}):Play() end) miniBtn.MouseLeave:Connect(function() TS:Create(miniBtn,TweenInfo.new(0.1),{BackgroundColor3=BG2}):Play() TS:Create(miniGlow,TweenInfo.new(0.1),{Transparency=0.6}):Play() end) local function showGui() main.Visible=true;miniBtn.Visible=false end local function hideGui() main.Visible=false;miniBtn.Visible=true end closeBtn.MouseButton1Click:Connect(hideGui) miniBtn.MouseButton1Click:Connect(showGui) local lo=0 local function LO() lo=lo+1;return lo end local function mkSect(parent,txt) local f=Instance.new("Frame",parent);f.Size=UDim2.new(1,0,0,28);f.BackgroundTransparency=1;f.BorderSizePixel=0;f.LayoutOrder=LO() local l=Instance.new("TextLabel",f);l.Size=UDim2.new(1,-8,1,0);l.Position=UDim2.new(0,4,0,0) l.BackgroundTransparency=1;l.Text=txt:upper();l.TextColor3=Color3.fromRGB(240,240,245) l.Font=Enum.Font.GothamBlack;l.TextSize=11;l.TextXAlignment=Enum.TextXAlignment.Left local underline=Instance.new("Frame",f);underline.Size=UDim2.new(0.3,0,0,2);underline.Position=UDim2.new(0,0,1,-4) underline.BackgroundColor3=Color3.fromRGB(220,220,230);underline.BorderSizePixel=0 Instance.new("UICorner",underline).CornerRadius=UDim.new(1,0) end local function mkRow(parent,h) local f=Instance.new("Frame",parent);f.Size=UDim2.new(1,0,0,h or 42) f.BackgroundColor3=CARD;f.BorderSizePixel=0;f.LayoutOrder=LO() Instance.new("UICorner",f).CornerRadius=UDim.new(0,12) local rowStroke=Instance.new("UIStroke",f);rowStroke.Color=Color3.fromRGB(40,40,60);rowStroke.Thickness=1 local rowGlow=Instance.new("UIStroke",f) rowGlow.Name="RowGlow" rowGlow.Color=Color3.fromRGB(150,150,170) rowGlow.Thickness=1 rowGlow.Transparency=0.8 rowGlow.ApplyStrokeMode=Enum.ApplyStrokeMode.Border f.MouseEnter:Connect(function() TS:Create(f,TweenInfo.new(0.15),{BackgroundColor3=HOV}):Play() TS:Create(rowStroke,TweenInfo.new(0.15),{Color=Color3.fromRGB(130,130,145)}):Play() TS:Create(rowGlow,TweenInfo.new(0.15),{Transparency=0.4}):Play() end) f.MouseLeave:Connect(function() TS:Create(f,TweenInfo.new(0.15),{BackgroundColor3=CARD}):Play() TS:Create(rowStroke,TweenInfo.new(0.15),{Color=Color3.fromRGB(40,40,60)}):Play() TS:Create(rowGlow,TweenInfo.new(0.15),{Transparency=0.8}):Play() end) return f end local function mkLabel(row,txt) local l=Instance.new("TextLabel",row);l.Size=UDim2.new(0.55,0,1,0);l.Position=UDim2.new(0,14,0,0) l.BackgroundTransparency=1;l.Text=txt;l.TextColor3=W l.Font=Enum.Font.GothamBold;l.TextSize=12;l.TextXAlignment=Enum.TextXAlignment.Left end local function mkPill(row,offset) local pill=Instance.new("Frame",row);pill.Size=UDim2.new(0,44,0,24) pill.Position=UDim2.new(1,-(offset or 50),0.5,-12) pill.BackgroundColor3=OFF;pill.BorderSizePixel=0;pill.ZIndex=3 Instance.new("UICorner",pill).CornerRadius=UDim.new(1,0) local pillStroke=Instance.new("UIStroke",pill);pillStroke.Color=Color3.fromRGB(50,50,70);pillStroke.Thickness=1 local dot=Instance.new("Frame",pill);dot.Size=UDim2.new(0,18,0,18);dot.Position=UDim2.new(0,3,0.5,-9) dot.BackgroundColor3=DIM;dot.BorderSizePixel=0;dot.ZIndex=4 Instance.new("UICorner",dot).CornerRadius=UDim.new(1,0) return pill,dot end local function animPill(pill,dot,on) TS:Create(pill,TweenInfo.new(0.25,Enum.EasingStyle.Quad),{BackgroundColor3=on and Color3.fromRGB(220,220,230) or OFF}):Play() TS:Create(dot,TweenInfo.new(0.25,Enum.EasingStyle.Back),{ Position=on and UDim2.new(1,-21,0.5,-9) or UDim2.new(0,3,0.5,-9), BackgroundColor3=on and Color3.fromRGB(255,255,255) or DIM }):Play() end local function mkToggle(parent,txt,cb) local row=mkRow(parent,38);mkLabel(row,txt) local pill,dot=mkPill(row,46) local on=false local function sv(s) on=s;animPill(pill,dot,s) end local clk=Instance.new("TextButton",pill);clk.Size=UDim2.new(1,0,1,0);clk.BackgroundTransparency=1;clk.Text="";clk.ZIndex=5 clk.Activated:Connect(function() on=not on;sv(on);cb(on) end) pill.ZIndex=3;dot.ZIndex=4 return sv end local function mkBox(parent,default,w,xOff,cb) local tb=Instance.new("TextBox",parent) tb.Size=UDim2.new(0,w or 60,0,28);tb.Position=UDim2.new(1,-(xOff or 66),0.5,-14) tb.BackgroundColor3=INP;tb.BorderSizePixel=0;tb.Text=tostring(default);tb.TextColor3=W tb.Font=Enum.Font.GothamBold;tb.TextSize=12;tb.ClearTextOnFocus=false;tb.ZIndex=5 Instance.new("UICorner",tb).CornerRadius=UDim.new(0,8) local bs=Instance.new("UIStroke",tb);bs.Color=Color3.fromRGB(45,45,65);bs.Thickness=1 tb.Focused:Connect(function() TS:Create(bs,TweenInfo.new(0.15),{Color=Color3.fromRGB(220,220,230)}):Play() end) tb.FocusLost:Connect(function() TS:Create(bs,TweenInfo.new(0.15),{Color=Color3.fromRGB(45,45,65)}):Play() if cb then local n=tonumber(tb.Text);if n then cb(n) else tb.Text=tostring(default) end end end) return tb end local GAMEPAD_KEYS={ [Enum.KeyCode.ButtonA]=true,[Enum.KeyCode.ButtonB]=true,[Enum.KeyCode.ButtonX]=true,[Enum.KeyCode.ButtonY]=true, [Enum.KeyCode.ButtonL1]=true,[Enum.KeyCode.ButtonR1]=true,[Enum.KeyCode.ButtonL2]=true,[Enum.KeyCode.ButtonR2]=true, [Enum.KeyCode.ButtonL3]=true,[Enum.KeyCode.ButtonR3]=true,[Enum.KeyCode.ButtonStart]=true,[Enum.KeyCode.ButtonSelect]=true, [Enum.KeyCode.DPadUp]=true,[Enum.KeyCode.DPadDown]=true,[Enum.KeyCode.DPadLeft]=true,[Enum.KeyCode.DPadRight]=true } local function isGamepadInput(inp) return inp and inp.UserInputType and inp.UserInputType.Name:match("^Gamepad")~=nil end local function isBindableInput(inp) if not inp or inp.KeyCode==Enum.KeyCode.Unknown then return false end if inp.UserInputType==Enum.UserInputType.Keyboard then return true end return isGamepadInput(inp) and GAMEPAD_KEYS[inp.KeyCode]==true end local function kbMatch(entry,kc) return kc and (kc==entry.kb or (entry.gp and kc==entry.gp)) end local function mkKB(parent,kbEntry,cb) local btn=Instance.new("TextButton",parent) btn.Size=UDim2.new(0,54,0,28);btn.Position=UDim2.new(1,-58,0.5,-14) btn.BackgroundColor3=INP;btn.BorderSizePixel=0 local function getLabel() return (kbEntry.gp and kbEntry.gp.Name) or (kbEntry.kb and kbEntry.kb.Name) or "None" end btn.Text=getLabel();btn.TextColor3=W btn.Font=Enum.Font.GothamBold;btn.TextSize=10;btn.ZIndex=5 Instance.new("UICorner",btn).CornerRadius=UDim.new(0,8) local kbStroke=Instance.new("UIStroke",btn);kbStroke.Color=Color3.fromRGB(45,45,65);kbStroke.Thickness=1 btn.MouseEnter:Connect(function() TS:Create(kbStroke,TweenInfo.new(0.15),{Color=Color3.fromRGB(220,220,230)}):Play() end) btn.MouseLeave:Connect(function() TS:Create(kbStroke,TweenInfo.new(0.15),{Color=Color3.fromRGB(45,45,65)}):Play() end) local li=false;local lc;local pv=btn.Text;local listenStart=0 btn.Activated:Connect(function() if li then li=false;NM._anyKeyListening=false;if lc then lc:Disconnect();lc=nil end;btn.Text=pv;btn.TextColor3=W;return end pv=btn.Text;li=true;NM._anyKeyListening=true;listenStart=tick();btn.Text="...";btn.TextColor3=W lc=UIS.InputBegan:Connect(function(inp) if not li then return end if inp.KeyCode==Enum.KeyCode.Escape then li=false;NM._anyKeyListening=false;if lc then lc:Disconnect();lc=nil end;btn.Text=pv;btn.TextColor3=W;return end local isGp=isGamepadInput(inp) if isGp and tick()-listenStart<0.15 then return end if not isBindableInput(inp) then return end btn.Text=inp.KeyCode.Name;pv=inp.KeyCode.Name;btn.TextColor3=W li=false;NM._anyKeyListening=false;if lc then lc:Disconnect();lc=nil end if cb then cb(inp.KeyCode,isGp) end end) end) return btn end local function mkToggleKB(parent,txt,kbEntry,onToggle,onKB) local row=mkRow(parent,38);mkLabel(row,txt) if kbEntry then mkKB(row,kbEntry,function(k,isGp) if isGp then kbEntry.gp=k;kbEntry.kb=nil else kbEntry.kb=k;kbEntry.gp=nil end if onKB then onKB(k,isGp) end end) end local pill,dot=mkPill(row,kbEntry and 110 or 46) local on=false local function sv(s) on=s;animPill(pill,dot,s) end local clk=Instance.new("TextButton",pill);clk.Size=UDim2.new(1,0,1,0);clk.BackgroundTransparency=1;clk.Text="";clk.ZIndex=5 clk.Activated:Connect(function() if NM._anyKeyListening then return end;on=not on;sv(on);if onToggle then onToggle(on) end end) pill.ZIndex=3;dot.ZIndex=4 return sv end local BTN_WH_FILE = "novaMoonPC_btn_wh.json" local btnWidth = 80 local btnHeight = 80 local btnSpacing = 88 local UI_MODE_FILE = "novaMoonPC_ui_mode.json" local UI_MODE = "UI1" local ALT_BOX_SIZE = UDim2.new(0, 170, 0, 58) local ALT_CORNER_RADIUS = UDim.new(0, 14) local DEFAULT_CORNER_RADIUS = UDim.new(0, 18) local function loadBtnSize() if isfile and isfile(BTN_WH_FILE) then local ok, data = pcall(function() return HS:JSONDecode(readfile(BTN_WH_FILE)) end) if ok and type(data) == "table" then if data.width then btnWidth = data.width end if data.height then btnHeight = data.height end btnSpacing = btnHeight + 8 end end end local function saveBtnSize() if writefile then pcall(function() writefile(BTN_WH_FILE, HS:JSONEncode({width = btnWidth, height = btnHeight})) end) end end local function loadUIMode() if isfile and isfile(UI_MODE_FILE) then local ok, data = pcall(function() return HS:JSONDecode(readfile(UI_MODE_FILE)) end) if ok and type(data) == "table" and data.mode then UI_MODE = (data.mode == "UI2") and "UI2" or "UI1" end end end local function saveUIMode() if writefile then pcall(function() writefile(UI_MODE_FILE, HS:JSONEncode({mode = UI_MODE})) end) end end loadBtnSize() loadUIMode() local separateButtons = {} local separateUpdaters = {} local TOP_Y = 10 local function getCurrentBtnSize() if UI_MODE == "UI2" then return ALT_BOX_SIZE else return UDim2.new(0, btnWidth, 0, btnHeight) end end local function getCurrentCornerRadius() if UI_MODE == "UI2" then return ALT_CORNER_RADIUS else return DEFAULT_CORNER_RADIUS end end local function getCurrentTextSize() if UI_MODE == "UI2" then return math.clamp(math.floor(58 / 6.2), 8, 22) else return math.clamp(math.floor(math.min(btnWidth, btnHeight) / 6.2), 8, 22) end end local function applyButtonShape(btn) if not btn or not btn.Parent then return end local newSize = getCurrentBtnSize() local newRadius = getCurrentCornerRadius() btn.Size = newSize btn.TextSize = getCurrentTextSize() for _, child in ipairs(btn:GetChildren()) do if child:IsA("UICorner") then child.CornerRadius = newRadius elseif child:IsA("Frame") and child.Name == "" then for _, gChild in ipairs(child:GetChildren()) do if gChild:IsA("UICorner") then gChild.CornerRadius = newRadius end end end end end local function createSeparateButton(name, displayText, defaultPos, toggleFn, getStateFn) local btn = Instance.new("TextButton", gui) btn.Name = name local currentSize = getCurrentBtnSize() btn.Size = currentSize local loadedPos = loadButtonPositions() if loadedPos[name] then btn.Position = UDim2.new(loadedPos[name].X.Scale, loadedPos[name].X.Offset, loadedPos[name].Y.Scale, loadedPos[name].Y.Offset) else btn.Position = defaultPos end btn.BackgroundColor3 = Color3.fromRGB(25, 25, 35) btn.Text = displayText btn.TextColor3 = Color3.fromRGB(200, 200, 210) btn.Font = Enum.Font.GothamBlack btn.TextSize = getCurrentTextSize() btn.TextWrapped = true btn.AutoButtonColor = false btn.ZIndex = 10 local corner = Instance.new("UICorner", btn) corner.CornerRadius = getCurrentCornerRadius() local stroke = Instance.new("UIStroke", btn) stroke.Color = Color3.fromRGB(80, 80, 100) stroke.Thickness = 1.5 local glow = Instance.new("Frame", btn) glow.Size = UDim2.new(1, 0, 1, 0) glow.BackgroundColor3 = Color3.fromRGB(220, 220, 230) glow.BackgroundTransparency = 1 glow.BorderSizePixel = 0 glow.ZIndex = 9 local glowCorner = Instance.new("UICorner", glow) glowCorner.CornerRadius = getCurrentCornerRadius() local btnEdgeGlow = Instance.new("UIStroke", btn) btnEdgeGlow.Name = "EdgeGlow" btnEdgeGlow.Color = Color3.fromRGB(200, 200, 220) btnEdgeGlow.Thickness = 1.5 btnEdgeGlow.Transparency = 0.6 btnEdgeGlow.ApplyStrokeMode = Enum.ApplyStrokeMode.Border task.spawn(function() while btn and btn.Parent do TS:Create(btnEdgeGlow, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Transparency = 0.3}):Play() task.wait(1.5) if not btn or not btn.Parent then break end TS:Create(btnEdgeGlow, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Transparency = 0.7}):Play() task.wait(1.5) end end) local function updateVisual() local isOn = getStateFn and getStateFn() or false if isOn then btn.BackgroundColor3 = Color3.fromRGB(200, 200, 210) btn.TextColor3 = Color3.fromRGB(20, 20, 30) stroke.Color = Color3.fromRGB(160, 160, 180) glow.BackgroundTransparency = 0.92 else btn.BackgroundColor3 = Color3.fromRGB(25, 25, 35) btn.TextColor3 = Color3.fromRGB(200, 200, 210) stroke.Color = Color3.fromRGB(80, 80, 100) glow.BackgroundTransparency = 1 end end btn.MouseEnter:Connect(function() local isOn = getStateFn and getStateFn() or false if not isOn then TS:Create(btn, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(40, 40, 55)}):Play() end TS:Create(stroke, TweenInfo.new(0.2), {Color = Color3.fromRGB(120, 120, 140)}):Play() end) btn.MouseLeave:Connect(function() updateVisual() end) btn.MouseButton1Down:Connect(function() if UI_MODE == "UI2" then TS:Create(btn, TweenInfo.new(0.1), {Size = UDim2.new(0, 167, 0, 55)}):Play() else TS:Create(btn, TweenInfo.new(0.1), {Size = UDim2.new(0, btnWidth - 3, 0, btnHeight - 3)}):Play() end end) btn.MouseButton1Up:Connect(function() TS:Create(btn, TweenInfo.new(0.15), {Size = getCurrentBtnSize()}):Play() end) btn.Activated:Connect(function() if toggleFn then toggleFn() end task.delay(0.05, updateVisual) end) local dragging = false local dragStart = nil local startPos = nil local dragConnBegan, dragConnChanged, dragConnEnded local dragThreshold = 5 local dragStartPos = nil dragConnBegan = btn.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = true; dragStart = input.Position; dragStartPos = input.Position; startPos = btn.Position end end) dragConnChanged = btn.InputChanged:Connect(function(input) if not dragging then return end if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then if NM.uiPositionLocked then local moved = (input.Position - dragStartPos).Magnitude if moved > dragThreshold then dragging = false end return end local delta = input.Position - dragStart btn.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y) end end) dragConnEnded = UIS.InputEnded:Connect(function(input) if (input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch) and dragging then dragging = false if not NM.uiPositionLocked then saveButtonPositions() end end end) savedBtnPositions[name] = {btn = btn, connections = {dragConnBegan, dragConnChanged, dragConnEnded}} updateVisual() return btn, updateVisual end local envyBtn, envyUpd = createSeparateButton("BtnEnvyBat", "AUTO\nBAT TP", UDim2.new(1, -(btnWidth*2 + 110), 0, TOP_Y), function() NM.envyAutoBatToggled = not NM.envyAutoBatToggled if not NM.envyAutoBatToggled then if sethiddenproperty and envyHrp then pcall(function() sethiddenproperty(envyHrp, "PhysicsRepRootPart", nil) end) end end end, function() return NM.envyAutoBatToggled end) separateUpdaters.EnvyBat = envyUpd envyBtn.Visible = NM.envyAutoBatButtonVisible NM.envyBtnRef = envyBtn local input2Btn, input2Upd = createSeparateButton("BtnInput2", "BYPASS\nAIM", UDim2.new(1, -(btnWidth*2 + 110), 0, TOP_Y + btnSpacing), function() NM.input2Enabled = not NM.input2Enabled if NM.input2Enabled then _G.AimbotMode = "Input2" if not NM.autoBatEnabled then queueAutoBatStart() if NM.autoBatSetVisual then NM.autoBatSetVisual(true) end end else _G.AimbotMode = "Input1" NM.autoBatEnabled = false disableAutoBat() if NM.autoBatSetVisual then NM.autoBatSetVisual(false) end end resetAimbotTarget() _aimbotLastMode = _G.AimbotMode saveConfig() end, function() return NM.input2Enabled end) separateUpdaters.Input2 = input2Upd input2Btn.Visible = NM.input2Enabled NM.input2BtnRef = input2Btn local alBtn, alUpd = createSeparateButton("BtnAL", "AUTO\nLEFT", UDim2.new(1, -(btnWidth+100), 0, TOP_Y), function() NM.autoLeftEnabled = not NM.autoLeftEnabled if NM.autoLeftEnabled then queueAutoLeftStart() else stopAutoLeft() end if NM.autoLeftSetVisual then NM.autoLeftSetVisual(NM.autoLeftEnabled) end end, function() return NM.autoLeftEnabled end) separateUpdaters.AutoLeft = alUpd local arBtn, arUpd = createSeparateButton("BtnAR", "AUTO\nRIGHT", UDim2.new(1, -(btnWidth+10), 0, TOP_Y), function() NM.autoRightEnabled = not NM.autoRightEnabled if NM.autoRightEnabled then queueAutoRightStart() else stopAutoRight() end if NM.autoRightSetVisual then NM.autoRightSetVisual(NM.autoRightEnabled) end end, function() return NM.autoRightEnabled end) separateUpdaters.AutoRight = arUpd local tpBtn, tpUpd = createSeparateButton("BtnTP", "TP\nDOWN", UDim2.new(1, -(btnWidth+100), 0, TOP_Y + btnSpacing), function() runTPFloor() end, function() return false end) separateUpdaters.TP = tpUpd local dropBtn, dropUpd = createSeparateButton("BtnDrop", "JUMP\nDROP", UDim2.new(1, -(btnWidth+10), 0, TOP_Y + btnSpacing), function() runDrop() end, function() return false end) separateUpdaters.Drop = dropUpd local spdBtn, spdUpd = createSeparateButton("BtnSpd", "CARRY\nSPD", UDim2.new(1, -(btnWidth+100), 0, TOP_Y + btnSpacing * 2), function() toggleCarryMode(); saveConfig() end, function() return NM.speedMode end) separateUpdaters.Speed = spdUpd local batBtn, batUpd = createSeparateButton("BtnBat", "AUTO\nBAT", UDim2.new(1, -(btnWidth+100), 0, TOP_Y + btnSpacing * 3), function() if not NM.autoBatEnabled then queueAutoBatStart(); if NM.autoBatSetVisual then NM.autoBatSetVisual(true) end else NM.autoBatEnabled = false; disableAutoBat(); _nmCleanLV(); if NM.autoBatSetVisual then NM.autoBatSetVisual(false) end end end, function() return NM.autoBatEnabled end) separateUpdaters.AutoBat = batUpd local resetBtn, resetUpd = createSeparateButton("BtnReset", "INSTANT\nRESET", UDim2.new(1, -(btnWidth+10), 0, TOP_Y + btnSpacing * 3), function() cursedInstaReset() end, function() return false end) separateUpdaters.Reset = resetUpd local lagBtn, lagUpd = createSeparateButton("BtnLag", "LAGGER\nMODE", UDim2.new(1, -(btnWidth+10), 0, TOP_Y + btnSpacing * 2), function() toggleLaggerMode(); saveConfig() end, function() return NM.laggerToggled end) separateUpdaters.Lagger = lagUpd local function syncSeparateButtons() for _, upd in pairs(separateUpdaters) do pcall(upd) end end RunService.Heartbeat:Connect(syncSeparateButtons) local singleSF = Instance.new("ScrollingFrame", main) singleSF.Name = "SinglePage" singleSF.Size = UDim2.new(1, 0, 1, -106) singleSF.Position = UDim2.new(0, 0, 0, 66) singleSF.BackgroundTransparency = 1 singleSF.BorderSizePixel = 0 singleSF.ClipsDescendants = true singleSF.ScrollBarThickness = 3 singleSF.ScrollBarImageColor3 = Color3.fromRGB(60, 60, 80) singleSF.CanvasSize = UDim2.new(0, 0, 0, 0) singleSF.AutomaticCanvasSize = Enum.AutomaticSize.Y local singleLayout = Instance.new("UIListLayout", singleSF) singleLayout.SortOrder = Enum.SortOrder.LayoutOrder singleLayout.Padding = UDim.new(0, 8) local singlePad = Instance.new("UIPadding", singleSF) singlePad.PaddingLeft = UDim.new(0, 10) singlePad.PaddingRight = UDim.new(0, 10) singlePad.PaddingTop = UDim.new(0, 8) singlePad.PaddingBottom = UDim.new(0, 20) mkSect(singleSF, "⚙️ SPEED SETTINGS") mkSect(singleSF,"Speed Values") do local row=mkRow(singleSF,38);mkLabel(row,"Normal Speed");normalBox=mkBox(row,NM.NS,56,54,function(v) if v>0 and v<=500 then NM.NS=v end;saveConfig() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Carry Speed");carryBox=mkBox(row,NM.CS,56,54,function(v) if v>0 and v<=500 then NM.CS=v end;saveConfig() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Speed Profile") local profBtn=Instance.new("TextButton",row) profBtn.Size=UDim2.new(0,90,0,26) profBtn.Position=UDim2.new(1,-98,0.5,-13) profBtn.BackgroundColor3=Color3.fromRGB(45,45,60) profBtn.BorderSizePixel=0 profBtn.Font=Enum.Font.GothamBold profBtn.TextSize=12 profBtn.TextColor3=W profBtn.ZIndex=3 Instance.new("UICorner",profBtn).CornerRadius=UDim.new(0,8) profBtn.AutoButtonColor=false local profBtnBase=profBtn.BackgroundColor3 profBtn.MouseButton1Down:Connect(function() TS:Create(profBtn,TweenInfo.new(0.08),{BackgroundColor3=Color3.fromRGB(255,255,255)}):Play() end) profBtn.MouseButton1Up:Connect(function() TS:Create(profBtn,TweenInfo.new(0.15),{BackgroundColor3=profBtnBase}):Play() end) profBtn.MouseLeave:Connect(function() TS:Create(profBtn,TweenInfo.new(0.15),{BackgroundColor3=profBtnBase}):Play() end) local function profLabelFor(t) return t=="v2" and "2v" or "1v" end profBtn.Text=profLabelFor(NM._nmSpeedProfile) NM.setSpeedProfileVisual=function(t) profBtn.Text=profLabelFor(t) end profBtn.Activated:Connect(function() local newProfile = NM._nmSpeedProfile=="v2" and "v1" or "v2" NM._nmSpeedStash[NM._nmSpeedProfile]={ns=NM.NS,cs=NM.CS} local saved=NM._nmSpeedStash[newProfile] if saved then NM.NS,NM.CS=saved.ns,saved.cs end NM._nmSpeedProfile=newProfile if normalBox then normalBox.Text=tostring(NM.NS) end if carryBox then carryBox.Text=tostring(NM.CS) end profBtn.Text=profLabelFor(newProfile) saveConfig() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Speed Style") local styBtn=Instance.new("TextButton",row) styBtn.Size=UDim2.new(0,90,0,26) styBtn.Position=UDim2.new(1,-98,0.5,-13) styBtn.BackgroundColor3=Color3.fromRGB(45,45,60) styBtn.BorderSizePixel=0 styBtn.Font=Enum.Font.GothamBold styBtn.TextSize=12 styBtn.TextColor3=W styBtn.ZIndex=3 Instance.new("UICorner",styBtn).CornerRadius=UDim.new(0,8) styBtn.AutoButtonColor=false local styBtnBase=styBtn.BackgroundColor3 styBtn.MouseButton1Down:Connect(function() TS:Create(styBtn,TweenInfo.new(0.08),{BackgroundColor3=Color3.fromRGB(255,255,255)}):Play() end) styBtn.MouseButton1Up:Connect(function() TS:Create(styBtn,TweenInfo.new(0.15),{BackgroundColor3=styBtnBase}):Play() end) styBtn.MouseLeave:Connect(function() TS:Create(styBtn,TweenInfo.new(0.15),{BackgroundColor3=styBtnBase}):Play() end) local function styLabelFor(t) return t=="auto" and "Auto" or "Manual" end styBtn.Text=styLabelFor(NM._nmSpeedStyle) NM.setSpeedStyleVisual=function(t) styBtn.Text=styLabelFor(t) end styBtn.Activated:Connect(function() NM._nmSpeedStyle = NM._nmSpeedStyle=="auto" and "manual" or "auto" styBtn.Text=styLabelFor(NM._nmSpeedStyle) saveConfig() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Lagger Normal");laggerBox=mkBox(row,NM.LAGGER_SPEED,56,54,function(v) if v>0 and v<=500 then NM.LAGGER_SPEED=v end;saveConfig() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Lagger Carry");laggerCarryBox=mkBox(row,NM.LAGGER_CARRY_SPEED,56,54,function(v) if v>0 and v<=500 then NM.LAGGER_CARRY_SPEED=v end;saveConfig() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Mode") modeValLbl=Instance.new("TextLabel",row) modeValLbl.Size=UDim2.new(0,100,1,0);modeValLbl.Position=UDim2.new(1,-104,0,0) modeValLbl.BackgroundTransparency=1;modeValLbl.Text="Normal";modeValLbl.TextColor3=Color3.fromRGB(200,200,220) modeValLbl.Font=Enum.Font.GothamBlack;modeValLbl.TextSize=11;modeValLbl.TextXAlignment=Enum.TextXAlignment.Right local clk=Instance.new("TextButton",row);clk.Size=UDim2.new(1,0,1,0);clk.BackgroundTransparency=1;clk.Text="";clk.ZIndex=2 clk.Activated:Connect(function() if NM._anyKeyListening then return end toggleCarryMode();saveConfig() end) end mkSect(singleSF,"Auto Path Speeds") do local row=mkRow(singleSF,38);mkLabel(row,"Auto Left Speed");alSpeedBox=mkBox(row,NM.AL_SPEED,56,62,function(v) if v>=1 and v<=500 then NM.AL_SPEED=v end;saveConfig() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Auto Right Speed");arSpeedBox=mkBox(row,NM.AR_SPEED,56,62,function(v) if v>=1 and v<=500 then NM.AR_SPEED=v end;saveConfig() end) end mkSect(singleSF, "⚔️ COMBAT & MOVEMENT") mkSect(singleSF,"Auto Combat") do local row=mkRow(singleSF,38);mkLabel(row,"Body Lock") local pill,dot=mkPill(row,46) local blOn=NM.bodyLockEnabled local function setBLVisual(s) blOn=s;animPill(pill,dot,s) end NM.setBodyLockVisual=setBLVisual local blClk=Instance.new("TextButton",pill);blClk.Size=UDim2.new(1,0,1,0);blClk.BackgroundTransparency=1;blClk.Text="";blClk.ZIndex=5 blClk.Activated:Connect(function() blOn=not blOn;setBLVisual(blOn) NM.bodyLockEnabled=blOn toggleBodyLock() saveConfig() end) pill.ZIndex=3;dot.ZIndex=4 end do local row=mkRow(singleSF,38);mkLabel(row,"Body Lock Range");bodyLockRangeBox=mkBox(row,NM.bodyLockRange,56,62,function(v) if v>0 and v<=500 then NM.bodyLockRange=v end;saveConfig() end) end do local abRow=mkRow(singleSF,38);mkLabel(abRow,"Bat Bot") mkKB(abRow,KB.AutoBat,function(k,isGp) if isGp then KB.AutoBat.gp=k;KB.AutoBat.kb=nil else KB.AutoBat.kb=k;KB.AutoBat.gp=nil end saveConfig() end) local abPill,abDot=mkPill(abRow,108) abPill.ZIndex=3;abDot.ZIndex=4 local abOn=false local function svAutoBat(s) abOn=s;animPill(abPill,abDot,s) end NM.autoBatSetVisual=svAutoBat local abClk=Instance.new("TextButton",abPill);abClk.Size=UDim2.new(1,0,1,0);abClk.BackgroundTransparency=1;abClk.Text="";abClk.ZIndex=5 abClk.Activated:Connect(function() if NM._anyKeyListening then return end abOn=not abOn;svAutoBat(abOn) if abOn then queueAutoBatStart() else NM.autoBatEnabled=false;disableAutoBat();_nmCleanLV() end saveConfig() end) end setAutoSwingVisual=mkToggle(singleSF,"Auto Swing",function(on) NM.autoSwingEnabled=on; saveConfig() end) if setAutoSwingVisual then setAutoSwingVisual(NM.autoSwingEnabled) end do NM.setBatCounterVisual=mkToggle(singleSF,"Bat Counter",function(on) NM.batCounterEnabled=on if on then NM.startBatCounter() else NM.stopBatCounter() end saveConfig() end) end NM.setMedusaVisual=mkToggle(singleSF,"Medusa Counter",function(on) NM.medusaCounterEnabled=on if on then if NM.medusaAutoResetEnabled then NM.medusaAutoResetEnabled=false if NM.setMedusaAutoResetVisual then NM.setMedusaAutoResetVisual(false) end stopMedusaAutoReset() end setupMedusa(LP.Character) else stopMedusaCounter() end saveConfig() end) NM.setMedusaAutoResetVisual=mkToggle(singleSF,"Medusa Auto Reset",function(on) NM.medusaAutoResetEnabled=on if on then if NM.medusaCounterEnabled then NM.medusaCounterEnabled=false if NM.setMedusaVisual then NM.setMedusaVisual(false) end stopMedusaCounter() end setupMedusaAutoReset(LP.Character) else stopMedusaAutoReset() end saveConfig() end) do local row=mkRow(singleSF,38);mkLabel(row,"Auto Bat TP Button") local pill,dot=mkPill(row,46) local envyOn=NM.envyAutoBatButtonVisible local function setEnvyVisual(s) envyOn=s;animPill(pill,dot,s) end local envyClk=Instance.new("TextButton",pill);envyClk.Size=UDim2.new(1,0,1,0);envyClk.BackgroundTransparency=1;envyClk.Text="";envyClk.ZIndex=5 envyClk.Activated:Connect(function() envyOn=not envyOn;setEnvyVisual(envyOn) NM.envyAutoBatButtonVisible=envyOn if NM.envyBtnRef then NM.envyBtnRef.Visible=envyOn end if not envyOn and NM.envyAutoBatToggled then NM.envyAutoBatToggled=false if sethiddenproperty and envyHrp then pcall(function() sethiddenproperty(envyHrp, "PhysicsRepRootPart", nil) end) end end saveConfig() end) pill.ZIndex=3;dot.ZIndex=4 if envyOn then animPill(pill,dot,true) end end do local row=mkRow(singleSF,38);mkLabel(row,"Bypass Aim Button") local pill,dot=mkPill(row,46) local bypassOn=NM.input2Enabled local function setBypassVisual(s) bypassOn=s;animPill(pill,dot,s) end NM.input2SetVisual=setBypassVisual local bypassClk=Instance.new("TextButton",pill);bypassClk.Size=UDim2.new(1,0,1,0);bypassClk.BackgroundTransparency=1;bypassClk.Text="";bypassClk.ZIndex=5 bypassClk.Activated:Connect(function() bypassOn=not bypassOn;setBypassVisual(bypassOn) NM.input2Enabled=bypassOn if NM.input2BtnRef then NM.input2BtnRef.Visible=bypassOn end if bypassOn then _G.AimbotMode = "Input2" if not NM.autoBatEnabled then queueAutoBatStart() if NM.autoBatSetVisual then NM.autoBatSetVisual(true) end end else _G.AimbotMode = "Input1" NM.autoBatEnabled = false disableAutoBat() if NM.autoBatSetVisual then NM.autoBatSetVisual(false) end end resetAimbotTarget() _aimbotLastMode = _G.AimbotMode saveConfig() end) pill.ZIndex=3;dot.ZIndex=4 if bypassOn then animPill(pill,dot,true) end end do local row=mkRow(singleSF,38);mkLabel(row,"Aimbot Module") local pill,dot=mkPill(row,46) local aimbotOn=NM.aimbotModuleEnabled local function setAimbotVisual(s) aimbotOn=s;animPill(pill,dot,s) end local aimbotClk=Instance.new("TextButton",pill);aimbotClk.Size=UDim2.new(1,0,1,0);aimbotClk.BackgroundTransparency=1;aimbotClk.Text="";aimbotClk.ZIndex=5 aimbotClk.Activated:Connect(function() aimbotOn=not aimbotOn;setAimbotVisual(aimbotOn) NM.aimbotModuleEnabled=aimbotOn AimbotModule.set(NM.aimbotModuleEnabled) saveConfig() end) pill.ZIndex=3;dot.ZIndex=4 if aimbotOn then animPill(pill,dot,true) end end do local row=mkRow(singleSF,38);mkLabel(row,"Med Timer") local pill,dot=mkPill(row,46) local mtOn=NM.medTimerEnabled local function setMTVisual(s) mtOn=s;animPill(pill,dot,s) end NM.setMedTimerVisual=setMTVisual local mtClk=Instance.new("TextButton",pill);mtClk.Size=UDim2.new(1,0,1,0);mtClk.BackgroundTransparency=1;mtClk.Text="";mtClk.ZIndex=5 mtClk.Activated:Connect(function() mtOn=not mtOn;setMTVisual(mtOn) NM.medTimerEnabled=mtOn saveConfig() end) pill.ZIndex=3;dot.ZIndex=4 if mtOn then animPill(pill,dot,true) end end do local row=mkRow(singleSF,38);mkLabel(row,"ESP ▼") local pill,dot=mkPill(row,46) local gggOn=false local gggTracers = {} local gggConn = nil local function setGGGVisual(s) gggOn=s;animPill(pill,dot,s) end local function gggCreateESP(character, player) if not character or player == LP then return end local hrp = character:WaitForChild("HumanoidRootPart", 5) local head = character:WaitForChild("Head", 5) if not hrp or not head then return end if character:FindFirstChild("NovaMoonESP") then return end local highlight = Instance.new("Highlight") highlight.Name = "NovaMoonESP" highlight.Adornee = character highlight.FillColor = NM._nmESPColor highlight.OutlineColor = Color3.new( math.min(NM._nmESPColor.R + 0.1, 1), math.min(NM._nmESPColor.G + 0.1, 1), math.min(NM._nmESPColor.B + 0.1, 1) ) highlight.FillTransparency = 0.6 highlight.OutlineTransparency = 0.3 highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop highlight.Parent = character if not _G._nmESPHighlights then _G._nmESPHighlights = {} end table.insert(_G._nmESPHighlights, highlight) local tracer = Drawing.new("Line") tracer.Visible = false tracer.Color = NM._nmESPColor tracer.Thickness = 2 tracer.Transparency = 1 gggTracers[character] = {Tracer = tracer, Part = head} if not _G._nmESPTracers then _G._nmESPTracers = {} end table.insert(_G._nmESPTracers, tracer) character.AncestryChanged:Connect(function(_, parent) if not parent then tracer:Remove() gggTracers[character] = nil end end) end local function gggSetupPlayer(player) if player == LP then return end if player.Character then task.spawn(function() gggCreateESP(player.Character, player) end) end player.CharacterAdded:Connect(function(char) task.spawn(function() gggCreateESP(char, player) end) end) end local function gggStart() for _, plr in ipairs(Players:GetPlayers()) do gggSetupPlayer(plr) end gggConn = RunService.RenderStepped:Connect(function() local cam = workspace.CurrentCamera local myChar = LP.Character local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart") for char, data in pairs(gggTracers) do if char and char.Parent and data.Part and data.Part.Parent then local vector, onScreen = cam:WorldToViewportPoint(data.Part.Position) if onScreen and vector.Z > 0 and math.abs(vector.X) < 10000 and math.abs(vector.Y) < 10000 then if myHRP then local myVector = cam:WorldToViewportPoint(myHRP.Position) data.Tracer.From = Vector2.new(myVector.X, myVector.Y) else data.Tracer.From = Vector2.new(cam.ViewportSize.X / 2, cam.ViewportSize.Y) end data.Tracer.To = Vector2.new(vector.X, vector.Y) data.Tracer.Visible = true else data.Tracer.Visible = false end else if data.Tracer then data.Tracer.Visible = false end gggTracers[char] = nil end end end) Players.PlayerAdded:Connect(gggSetupPlayer) end local function gggStop() if gggConn then gggConn:Disconnect(); gggConn = nil end for char, data in pairs(gggTracers) do if data.Tracer then data.Tracer:Remove() end if char then local hl = char:FindFirstChild("NovaMoonESP") if hl then hl:Destroy() end end end gggTracers = {} end _nmSetESPVisual = setGGGVisual _nmESPStart = gggStart _nmESPStop = gggStop local gggClk=Instance.new("TextButton",pill);gggClk.Size=UDim2.new(1,0,1,0);gggClk.BackgroundTransparency=1;gggClk.Text="";gggClk.ZIndex=5 gggClk.Activated:Connect(function() gggOn=not gggOn;setGGGVisual(gggOn) NM._nmESPEnabled = gggOn if gggOn then gggStart() else gggStop() end saveConfig() end) pill.ZIndex=3;dot.ZIndex=4 if NM._nmESPEnabled then animPill(pill,dot,true); gggOn=true task.spawn(function() task.wait(0.3) if NM._nmESPEnabled and _nmESPStart then _nmESPStart() end end) end do local colorWheelFrame=Instance.new("Frame",singleSF) colorWheelFrame.Name="ESPColorWheel" colorWheelFrame.Size=UDim2.new(1,0,0,180) colorWheelFrame.BackgroundColor3=Color3.fromRGB(20,20,30) colorWheelFrame.BorderSizePixel=0 colorWheelFrame.Visible=false colorWheelFrame.LayoutOrder=LO() Instance.new("UICorner",colorWheelFrame).CornerRadius=UDim.new(0,14) local cwStroke=Instance.new("UIStroke",colorWheelFrame);cwStroke.Color=Color3.fromRGB(50,50,70);cwStroke.Thickness=1 local colorWheel=Instance.new("ImageButton",colorWheelFrame) colorWheel.Name="ColorWheel" colorWheel.Size=UDim2.new(0,120,0,120) colorWheel.Position=UDim2.new(0,15,0,15) colorWheel.BackgroundColor3=Color3.fromRGB(255,255,255) colorWheel.BorderSizePixel=0 colorWheel.ZIndex=4 colorWheel.AutoButtonColor=false Instance.new("UICorner",colorWheel).CornerRadius=UDim.new(1,0) local hueGradient=Instance.new("UIGradient",colorWheel) hueGradient.Color=ColorSequence.new({ ColorSequenceKeypoint.new(0,Color3.fromRGB(255,0,0)), ColorSequenceKeypoint.new(0.17,Color3.fromRGB(255,255,0)), ColorSequenceKeypoint.new(0.33,Color3.fromRGB(0,255,0)), ColorSequenceKeypoint.new(0.5,Color3.fromRGB(0,255,255)), ColorSequenceKeypoint.new(0.67,Color3.fromRGB(0,0,255)), ColorSequenceKeypoint.new(0.83,Color3.fromRGB(255,0,255)), ColorSequenceKeypoint.new(1,Color3.fromRGB(255,0,0)) }) local satOverlay=Instance.new("Frame",colorWheel) satOverlay.Name="SatOverlay" satOverlay.Size=UDim2.new(1,0,1,0) satOverlay.BackgroundColor3=Color3.fromRGB(255,255,255) satOverlay.BackgroundTransparency=0 satOverlay.BorderSizePixel=0 satOverlay.ZIndex=5 Instance.new("UICorner",satOverlay).CornerRadius=UDim.new(1,0) local satGradient=Instance.new("UIGradient",satOverlay) satGradient.Color=ColorSequence.new({ ColorSequenceKeypoint.new(0,Color3.fromRGB(255,255,255)), ColorSequenceKeypoint.new(1,Color3.fromRGB(255,255,255)) }) satGradient.Transparency=NumberSequence.new({ NumberSequenceKeypoint.new(0,0), NumberSequenceKeypoint.new(1,1) }) do local WHEEL_PX = 128 local realWheelOk = pcall(function() local AssetService = game:GetService("AssetService") local editable = AssetService:CreateEditableImage({Size=Vector2.new(WHEEL_PX,WHEEL_PX)}) if not editable then error("no editable image") end local buf = buffer.create(WHEEL_PX*WHEEL_PX*4) local center = WHEEL_PX/2 local maxR = center for py=0,WHEEL_PX-1 do local dy=(py+0.5)-center for px=0,WHEEL_PX-1 do local dx=(px+0.5)-center local dist=math.sqrt(dx*dx+dy*dy) local idx=(py*WHEEL_PX+px)*4 if dist>maxR then buffer.writeu8(buf,idx,0);buffer.writeu8(buf,idx+1,0) buffer.writeu8(buf,idx+2,0);buffer.writeu8(buf,idx+3,0) else local angle=math.atan2(dy,dx) local hh=((angle/(math.pi*2))%1+1)%1 local ss=math.clamp(dist/maxR,0,1) local col=Color3.fromHSV(hh,ss,1) buffer.writeu8(buf,idx,math.floor(col.R*255+0.5)) buffer.writeu8(buf,idx+1,math.floor(col.G*255+0.5)) buffer.writeu8(buf,idx+2,math.floor(col.B*255+0.5)) buffer.writeu8(buf,idx+3,255) end end end editable:WritePixelsBuffer(Vector2.zero,Vector2.new(WHEEL_PX,WHEEL_PX),buf) colorWheel.ImageContent=Content.fromObject(editable) end) if realWheelOk then hueGradient.Enabled=false satOverlay.Visible=false end end local picker=Instance.new("Frame",colorWheel) picker.Name="Picker" picker.Size=UDim2.new(0,10,0,10) picker.Position=UDim2.new(0.5,-5,0.5,-5) picker.BackgroundColor3=Color3.fromRGB(255,255,255) picker.BorderSizePixel=0 picker.ZIndex=8 Instance.new("UICorner",picker).CornerRadius=UDim.new(1,0) local pickerStroke=Instance.new("UIStroke",picker);pickerStroke.Color=Color3.fromRGB(0,0,0);pickerStroke.Thickness=2 local brightnessFrame=Instance.new("Frame",colorWheelFrame) brightnessFrame.Name="BrightnessFrame" brightnessFrame.Size=UDim2.new(0,24,0,120) brightnessFrame.Position=UDim2.new(0,150,0,15) brightnessFrame.BackgroundColor3=Color3.fromRGB(30,30,40) brightnessFrame.BorderSizePixel=0 brightnessFrame.ZIndex=5 Instance.new("UICorner",brightnessFrame).CornerRadius=UDim.new(0,8) local brightnessGradient=Instance.new("UIGradient",brightnessFrame) brightnessGradient.Color=ColorSequence.new({ ColorSequenceKeypoint.new(0,Color3.fromRGB(255,255,255)), ColorSequenceKeypoint.new(1,Color3.fromRGB(0,0,0)) }) brightnessGradient.Rotation=90 local brightnessSlider=Instance.new("Frame",brightnessFrame) brightnessSlider.Name="Slider" brightnessSlider.Size=UDim2.new(1,4,0,4) brightnessSlider.Position=UDim2.new(0,-2,0,-2) brightnessSlider.BackgroundColor3=Color3.fromRGB(255,255,255) brightnessSlider.BorderSizePixel=0 brightnessSlider.ZIndex=6 Instance.new("UICorner",brightnessSlider).CornerRadius=UDim.new(1,0) local previewFrame=Instance.new("Frame",colorWheelFrame) previewFrame.Name="Preview" previewFrame.Size=UDim2.new(0,50,0,50) previewFrame.Position=UDim2.new(0,190,0,15) previewFrame.BackgroundColor3=NM._nmESPColor previewFrame.BorderSizePixel=0 previewFrame.ZIndex=5 Instance.new("UICorner",previewFrame).CornerRadius=UDim.new(0,10) local previewStroke=Instance.new("UIStroke",previewFrame);previewStroke.Color=Color3.fromRGB(80,80,100);previewStroke.Thickness=1.5 local colorLabel=Instance.new("TextLabel",colorWheelFrame) colorLabel.Name="ColorLabel" colorLabel.Size=UDim2.new(0,50,0,20) colorLabel.Position=UDim2.new(0,190,0,70) colorLabel.BackgroundTransparency=1 colorLabel.Text=string.format("%d,%d,%d",math.floor(NM._nmESPColor.R*255),math.floor(NM._nmESPColor.G*255),math.floor(NM._nmESPColor.B*255)) colorLabel.TextColor3=Color3.fromRGB(200,200,220) colorLabel.Font=Enum.Font.GothamBold colorLabel.TextSize=9 colorLabel.TextXAlignment=Enum.TextXAlignment.Center colorLabel.ZIndex=5 local hue,sat,val=0,0,1 local wheelDown,brightDown=false,false local function updateColor() local newColor=Color3.fromHSV(hue,sat,val) NM._nmESPColor=newColor previewFrame.BackgroundColor3=newColor colorLabel.Text=string.format("%d,%d,%d",math.floor(newColor.R*255),math.floor(newColor.G*255),math.floor(newColor.B*255)) brightnessGradient.Color=ColorSequence.new({ ColorSequenceKeypoint.new(0,Color3.fromHSV(hue,sat,1)), ColorSequenceKeypoint.new(1,Color3.new(0,0,0)) }) if _G._nmESPHighlights then for _,hl in ipairs(_G._nmESPHighlights) do if hl and hl.Parent then hl.FillColor=newColor hl.OutlineColor=Color3.new( math.min(newColor.R+0.1,1), math.min(newColor.G+0.1,1), math.min(newColor.B+0.1,1) ) end end end if _G._nmESPTracers then for _,tr in ipairs(_G._nmESPTracers) do if tr then tr.Color=newColor end end end saveConfig() end local function updatePickerFromColor() local h,s,v=NM._nmESPColor:ToHSV() hue=h;sat=s;val=v local angle=h*math.pi*2 local radius=s*60 local px=math.cos(angle)*radius local py=-math.sin(angle)*radius picker.Position=UDim2.new(0.5,px-5,0.5,py-5) brightnessSlider.Position=UDim2.new(0,-2,1-v,-2) brightnessGradient.Color=ColorSequence.new({ ColorSequenceKeypoint.new(0,Color3.fromHSV(hue,sat,1)), ColorSequenceKeypoint.new(1,Color3.new(0,0,0)) }) previewFrame.BackgroundColor3=NM._nmESPColor colorLabel.Text=string.format("%d,%d,%d",math.floor(NM._nmESPColor.R*255),math.floor(NM._nmESPColor.G*255),math.floor(NM._nmESPColor.B*255)) end updatePickerFromColor() colorWheel.InputBegan:Connect(function(inp) if inp.UserInputType==Enum.UserInputType.MouseButton1 or inp.UserInputType==Enum.UserInputType.Touch then wheelDown=true local centerX=colorWheel.AbsolutePosition.X+colorWheel.AbsoluteSize.X/2 local centerY=colorWheel.AbsolutePosition.Y+colorWheel.AbsoluteSize.Y/2 local dx=inp.Position.X-centerX local dy=inp.Position.Y-centerY local dist=math.clamp(math.sqrt(dx*dx+dy*dy),0,60) local angle=math.atan2(dy,dx) hue=((angle/(math.pi*2))%1+1)%1 sat=dist/60 picker.Position=UDim2.new(0.5,dx-5,0.5,dy-5) updateColor() end end) brightnessFrame.InputBegan:Connect(function(inp) if inp.UserInputType==Enum.UserInputType.MouseButton1 or inp.UserInputType==Enum.UserInputType.Touch then brightDown=true local relY=math.clamp(inp.Position.Y-brightnessFrame.AbsolutePosition.Y,0,brightnessFrame.AbsoluteSize.Y) val=1-(relY/brightnessFrame.AbsoluteSize.Y) brightnessSlider.Position=UDim2.new(0,-2,1-val,-2) updateColor() end end) UIS.InputChanged:Connect(function(inp) if inp.UserInputType==Enum.UserInputType.MouseMovement or inp.UserInputType==Enum.UserInputType.Touch then if wheelDown then local centerX=colorWheel.AbsolutePosition.X+colorWheel.AbsoluteSize.X/2 local centerY=colorWheel.AbsolutePosition.Y+colorWheel.AbsoluteSize.Y/2 local dx=inp.Position.X-centerX local dy=inp.Position.Y-centerY local dist=math.clamp(math.sqrt(dx*dx+dy*dy),0,60) local angle=math.atan2(dy,dx) hue=((angle/(math.pi*2))%1+1)%1 sat=dist/60 picker.Position=UDim2.new(0.5,dx-5,0.5,dy-5) updateColor() elseif brightDown then local relY=math.clamp(inp.Position.Y-brightnessFrame.AbsolutePosition.Y,0,brightnessFrame.AbsoluteSize.Y) val=1-(relY/brightnessFrame.AbsoluteSize.Y) brightnessSlider.Position=UDim2.new(0,-2,1-val,-2) updateColor() end end end) UIS.InputEnded:Connect(function(inp) if inp.UserInputType==Enum.UserInputType.MouseButton1 or inp.UserInputType==Enum.UserInputType.Touch then wheelDown=false;brightDown=false end end) local arrowClk=Instance.new("TextButton",row);arrowClk.Size=UDim2.new(1,0,1,0);arrowClk.BackgroundTransparency=1;arrowClk.Text="";arrowClk.ZIndex=2 arrowClk.Activated:Connect(function() if NM._anyKeyListening then return end arrowOn=not arrowOn colorWheelFrame.Visible=arrowOn if arrowOn then row:FindFirstChildOfClass("TextLabel").Text="ESP ▲" else row:FindFirstChildOfClass("TextLabel").Text="ESP ▼" end end) end end do local row=mkRow(singleSF,38);mkLabel(row,"Animation Pack") local animBtn=Instance.new("TextButton",row) animBtn.Size=UDim2.new(0,110,0,26) animBtn.Position=UDim2.new(1,-118,0.5,-13) animBtn.BackgroundColor3=Color3.fromRGB(45,45,60) animBtn.BorderSizePixel=0 animBtn.Font=Enum.Font.GothamBold animBtn.TextSize=12 animBtn.TextColor3=W animBtn.ZIndex=3 Instance.new("UICorner",animBtn).CornerRadius=UDim.new(0,8) animBtn.AutoButtonColor=false local animBtnBase=animBtn.BackgroundColor3 animBtn.MouseButton1Down:Connect(function() TS:Create(animBtn,TweenInfo.new(0.08),{BackgroundColor3=Color3.fromRGB(255,255,255)}):Play() end) animBtn.MouseButton1Up:Connect(function() TS:Create(animBtn,TweenInfo.new(0.15),{BackgroundColor3=animBtnBase}):Play() end) animBtn.MouseLeave:Connect(function() TS:Create(animBtn,TweenInfo.new(0.15),{BackgroundColor3=animBtnBase}):Play() end) local animPackList={"Default","Astronaut","Bubbly","Cartoony","Confident","Cowboy","Elder","Knight","Levitation","Mage","Ninja","Patrol","Pirate","Popstar","Princess","Robot","Rthro","Sneaky","Stylish","Superhero","Toy","Vampire","Werewolf","Zombie"} local animPackIndex=1 for i,v in ipairs(animPackList) do if v==(NM._nmAnimPack or "Default") then animPackIndex=i;break end end local function animLabelFor(t) if t=="Default" then return "Default" end return AnimationPacks[t] and AnimationPacks[t].Name or t end animBtn.Text=animLabelFor(NM._nmAnimPack or "Default") NM.setAnimVisual=function(t) animBtn.Text=animLabelFor(t) end animBtn.Activated:Connect(function() animPackIndex=animPackIndex+1 if animPackIndex>#animPackList then animPackIndex=1 end local selected=animPackList[animPackIndex] NM._nmAnimPack=selected ApplyAnimationPack(selected) animBtn.Text=animLabelFor(selected) saveConfig() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Hitbox ▼") local pill,dot=mkPill(row,46) local ppOn=NM.ppEnabled local function setPPVisual(s) ppOn=s;animPill(pill,dot,s) end local ppClk=Instance.new("TextButton",pill);ppClk.Size=UDim2.new(1,0,1,0);ppClk.BackgroundTransparency=1;ppClk.Text="";ppClk.ZIndex=5 ppClk.Activated:Connect(function() ppOn=not ppOn;setPPVisual(ppOn) NM.ppEnabled=ppOn if ppOn then startPP() else stopPP() end saveConfig() end) pill.ZIndex=3;dot.ZIndex=4 if ppOn then animPill(pill,dot,true) end local arrowOn=false local hbFrame=Instance.new("Frame",singleSF) hbFrame.Name="HitboxSettings" hbFrame.Size=UDim2.new(1,0,0,140) hbFrame.BackgroundColor3=Color3.fromRGB(20,20,30) hbFrame.BorderSizePixel=0 hbFrame.Visible=false hbFrame.ClipsDescendants=true hbFrame.LayoutOrder=LO() Instance.new("UICorner",hbFrame).CornerRadius=UDim.new(0,14) local hbStroke=Instance.new("UIStroke",hbFrame);hbStroke.Color=Color3.fromRGB(50,50,70);hbStroke.Thickness=1 do local sizeRow=Instance.new("Frame",hbFrame) sizeRow.Size=UDim2.new(1,-20,0,34) sizeRow.Position=UDim2.new(0,10,0,8) sizeRow.BackgroundTransparency=1 local sizeLbl=Instance.new("TextLabel",sizeRow) sizeLbl.Size=UDim2.new(0.5,0,1,0) sizeLbl.BackgroundTransparency=1 sizeLbl.Text="Size" sizeLbl.TextColor3=Color3.fromRGB(220,220,235) sizeLbl.Font=Enum.Font.GothamBold sizeLbl.TextSize=12 sizeLbl.TextXAlignment=Enum.TextXAlignment.Left local sizeBox=mkBox(sizeRow,NM._nmHitboxSize,56,62,function(v) if v>=5 and v<=50 then NM._nmHitboxSize=v PP_HITBOX_SIZE=Vector3.new(v,v,v) for char,data in pairs(NM.ppCharacters) do if data.part and data.part.Parent then data.part.Size=Vector3.new(v,v,v) end end saveConfig() end end) sizeBox.Parent=sizeRow end do local transRow=Instance.new("Frame",hbFrame) transRow.Size=UDim2.new(1,-20,0,34) transRow.Position=UDim2.new(0,10,0,46) transRow.BackgroundTransparency=1 local transLbl=Instance.new("TextLabel",transRow) transLbl.Size=UDim2.new(0.5,0,1,0) transLbl.BackgroundTransparency=1 transLbl.Text="Transparency" transLbl.TextColor3=Color3.fromRGB(220,220,235) transLbl.Font=Enum.Font.GothamBold transLbl.TextSize=12 transLbl.TextXAlignment=Enum.TextXAlignment.Left local transBox=mkBox(transRow,PP_HITBOX_TRANSPARENCY*100,56,62,function(v) v=math.clamp(v,0,100)/100 PP_HITBOX_TRANSPARENCY=v for char,data in pairs(NM.ppCharacters) do if data.part and data.part.Parent then data.part.Transparency=v end end saveConfig() end) transBox.Parent=transRow end do local distRow=Instance.new("Frame",hbFrame) distRow.Size=UDim2.new(1,-20,0,34) distRow.Position=UDim2.new(0,10,0,84) distRow.BackgroundTransparency=1 local distLbl=Instance.new("TextLabel",distRow) distLbl.Size=UDim2.new(0.5,0,1,0) distLbl.BackgroundTransparency=1 distLbl.Text="Max Distance" distLbl.TextColor3=Color3.fromRGB(220,220,235) distLbl.Font=Enum.Font.GothamBold distLbl.TextSize=12 distLbl.TextXAlignment=Enum.TextXAlignment.Left local distBox=mkBox(distRow,1500,56,62,function(v) if v>=100 and v<=5000 then end end) distBox.Parent=distRow end local arrowClk=Instance.new("TextButton",row);arrowClk.Size=UDim2.new(1,0,1,0);arrowClk.BackgroundTransparency=1;arrowClk.Text="";arrowClk.ZIndex=2 arrowClk.Activated:Connect(function() if NM._anyKeyListening then return end arrowOn=not arrowOn hbFrame.Visible=arrowOn if arrowOn then row:FindFirstChildOfClass("TextLabel").Text="Hitbox ▲" else row:FindFirstChildOfClass("TextLabel").Text="Hitbox ▼" end end) end do local row=mkRow(singleSF,38);mkLabel(row,"Anti Kick") local pill,dot=mkPill(row,46) local qqOn=false local function setQQVisual(s) qqOn=s;animPill(pill,dot,s) end _nmSetAntiKickVisual = setQQVisual local qqClk=Instance.new("TextButton",pill);qqClk.Size=UDim2.new(1,0,1,0);qqClk.BackgroundTransparency=1;qqClk.Text="";qqClk.ZIndex=5 qqClk.Activated:Connect(function() qqOn=not qqOn;setQQVisual(qqOn) NM._nmAntiKickEnabled = qqOn if qqOn then enableRealAntiKick() end saveConfig() end) pill.ZIndex=3;dot.ZIndex=4 if NM._nmAntiKickEnabled then animPill(pill,dot,true); qqOn=true enableRealAntiKick() end end mkSect(singleSF,"Auto Movement") do local sv=mkToggleKB(singleSF,"Auto Left",KB.AutoLeft, function(on) NM.autoLeftEnabled=on if on then queueAutoLeftStart() else stopAutoLeft() end end, function(k,isGp) if isGp then KB.AutoLeft.gp=k;KB.AutoLeft.kb=nil else KB.AutoLeft.kb=k;KB.AutoLeft.gp=nil end;saveConfig() end) NM.autoLeftSetVisual=sv end do local sv=mkToggleKB(singleSF,"Auto Right",KB.AutoRight, function(on) NM.autoRightEnabled=on if on then queueAutoRightStart() else stopAutoRight() end end, function(k,isGp) if isGp then KB.AutoRight.gp=k;KB.AutoRight.kb=nil else KB.AutoRight.kb=k;KB.AutoRight.gp=nil end;saveConfig() end) NM.autoRightSetVisual=sv end do local row=mkRow(singleSF,38);mkLabel(row,"Jump Drop") local kbBtn=mkKB(row,KB.DropBrainrot,function(k,isGp) if isGp then KB.DropBrainrot.gp=k;KB.DropBrainrot.kb=nil else KB.DropBrainrot.kb=k;KB.DropBrainrot.gp=nil end;saveConfig() end) kbBtn.ZIndex=6 local clk=Instance.new("TextButton",row);clk.Size=UDim2.new(0.55,0,1,0);clk.BackgroundTransparency=1;clk.Text="";clk.ZIndex=2 clk.Activated:Connect(function() runDrop() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Drop Style") local styleBtn=Instance.new("TextButton",row) styleBtn.Size=UDim2.new(0,110,0,26) styleBtn.Position=UDim2.new(1,-118,0.5,-13) styleBtn.BackgroundColor3=Color3.fromRGB(45,45,60) styleBtn.BorderSizePixel=0 styleBtn.Font=Enum.Font.GothamBold styleBtn.TextSize=12 styleBtn.TextColor3=W styleBtn.ZIndex=3 Instance.new("UICorner",styleBtn).CornerRadius=UDim.new(0,8) styleBtn.AutoButtonColor=false local styleBtnBase=styleBtn.BackgroundColor3 styleBtn.MouseButton1Down:Connect(function() TS:Create(styleBtn,TweenInfo.new(0.08),{BackgroundColor3=Color3.fromRGB(255,255,255)}):Play() end) styleBtn.MouseButton1Up:Connect(function() TS:Create(styleBtn,TweenInfo.new(0.15),{BackgroundColor3=styleBtnBase}):Play() end) styleBtn.MouseLeave:Connect(function() TS:Create(styleBtn,TweenInfo.new(0.15),{BackgroundColor3=styleBtnBase}):Play() end) local function dropLabelFor(t) return t=="v1" and "V1 (Fling)" or "V2 (Float)" end styleBtn.Text=dropLabelFor(NM._nmDropStyle) NM.setDropStyleVisual=function(t) styleBtn.Text=dropLabelFor(t) end styleBtn.Activated:Connect(function() NM._nmDropStyle = NM._nmDropStyle=="v1" and "v2" or "v1" styleBtn.Text=dropLabelFor(NM._nmDropStyle) saveConfig() end) end do local row=mkRow(singleSF,38);mkLabel(row,"TP Down") local kbBtn=mkKB(row,KB.TPFloor,function(k,isGp) if isGp then KB.TPFloor.gp=k;KB.TPFloor.kb=nil else KB.TPFloor.kb=k;KB.TPFloor.gp=nil end;saveConfig() end) kbBtn.ZIndex=6 local clk=Instance.new("TextButton",row);clk.Size=UDim2.new(0.55,0,1,0);clk.BackgroundTransparency=1;clk.Text="";clk.ZIndex=2 clk.Activated:Connect(function() runTPFloor() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Instant Reset") local kbBtn=mkKB(row,KB.InstaReset,function(k,isGp) if isGp then KB.InstaReset.gp=k;KB.InstaReset.kb=nil else KB.InstaReset.kb=k;KB.InstaReset.gp=nil end;saveConfig() end) kbBtn.ZIndex=6 local clk=Instance.new("TextButton",row);clk.Size=UDim2.new(0.55,0,1,0);clk.BackgroundTransparency=1;clk.Text="";clk.ZIndex=2 clk.Activated:Connect(function() cursedInstaReset() end) end do NM.setAutoTPVisual=mkToggle(singleSF,"Auto TP Down",function(on) NM.autoTPEnabled=on if on then startAutoTP() else stopAutoTP() end saveConfig() end) end mkSect(singleSF,"Auto Steal v2") do local row=mkRow(singleSF,38);mkLabel(row,"Enable Auto Steal") local pill,dot=mkPill(row,46) local stealOn=false local function setStealVisual(s) stealOn=s;animPill(pill,dot,s) end NM._nmStealSetVisual=setStealVisual local stealClk=Instance.new("TextButton",pill);stealClk.Size=UDim2.new(1,0,1,0);stealClk.BackgroundTransparency=1;stealClk.Text="";stealClk.ZIndex=5 stealClk.Activated:Connect(function() stealOn=not stealOn;setStealVisual(stealOn) if _G._nmToggleAutoSteal then _G._nmToggleAutoSteal(stealOn) end saveConfig() end) pill.ZIndex=3;dot.ZIndex=4 end do local row=mkRow(singleSF,38);mkLabel(row,"Steal Style") local typeBtn=Instance.new("TextButton",row) typeBtn.Size=UDim2.new(0,110,0,26) typeBtn.Position=UDim2.new(1,-118,0.5,-13) typeBtn.BackgroundColor3=Color3.fromRGB(45,45,60) typeBtn.BorderSizePixel=0 typeBtn.Font=Enum.Font.GothamBold typeBtn.TextSize=12 typeBtn.TextColor3=W typeBtn.ZIndex=3 Instance.new("UICorner",typeBtn).CornerRadius=UDim.new(0,8) typeBtn.AutoButtonColor=false local typeBtnBase=typeBtn.BackgroundColor3 typeBtn.MouseButton1Down:Connect(function() TS:Create(typeBtn,TweenInfo.new(0.08),{BackgroundColor3=Color3.fromRGB(255,255,255)}):Play() end) typeBtn.MouseButton1Up:Connect(function() TS:Create(typeBtn,TweenInfo.new(0.15),{BackgroundColor3=typeBtnBase}):Play() end) typeBtn.MouseLeave:Connect(function() TS:Create(typeBtn,TweenInfo.new(0.15),{BackgroundColor3=typeBtnBase}):Play() end) local function labelFor(t) return t=="new" and "New Pill" or "Normal" end typeBtn.Text=labelFor(NM._nmStealGuiType) NM._nmStealTypeSetVisual=function(t) typeBtn.Text=labelFor(t) end typeBtn.Activated:Connect(function() local newType = NM._nmStealGuiType=="new" and "normal" or "new" if _G._nmSetStealGuiType then _G._nmSetStealGuiType(newType) else NM._nmStealGuiType=newType end typeBtn.Text=labelFor(newType) saveConfig() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Steal Radius");mkBox(row,NM._nmStealRadius,56,62,function(v) if v>=1 and v<=300 then NM._nmStealRadius=v end;saveConfig() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Steal Duration");mkBox(row,NM._nmStealDuration,56,62,function(v) if v>=0.01 and v<=10 then NM._nmStealDuration=v end;saveConfig() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Fire Range");mkBox(row,NM._nmStealFireRange,56,62,function(v) if v>=1 and v<=50 then NM._nmStealFireRange=v end;saveConfig() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Hold Min");mkBox(row,NM._nmStealHoldMin,56,62,function(v) if v>=0.1 and v<=10 then NM._nmStealHoldMin=v end;saveConfig() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Hold Max");mkBox(row,NM._nmStealHoldMax,56,62,function(v) if v>=0.1 and v<=10 then NM._nmStealHoldMax=v end;saveConfig() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Entry Delay");mkBox(row,NM._nmStealEntryDelay,56,62,function(v) if v>=0 and v<=5 then NM._nmStealEntryDelay=v end;saveConfig() end) end mkSect(singleSF, "⚙️ ADVANCED SETTINGS") mkSect(singleSF,"Aimbot Settings") do local row=mkRow(singleSF,38);mkLabel(row,"Aimbot Mode") local modeVal=Instance.new("TextLabel",row) modeVal.Size=UDim2.new(0,150,1,0);modeVal.Position=UDim2.new(1,-154,0,0) modeVal.BackgroundTransparency=1;modeVal.Text=_G.AimbotMode modeVal.TextColor3=Color3.fromRGB(200,200,220);modeVal.Font=Enum.Font.GothamBlack modeVal.TextSize=11;modeVal.TextXAlignment=Enum.TextXAlignment.Right local clk=Instance.new("TextButton",row);clk.Size=UDim2.new(1,0,1,0);clk.BackgroundTransparency=1;clk.Text="";clk.ZIndex=2 clk.Activated:Connect(function() if NM._anyKeyListening then return end if NM.input2Enabled then NM.input2Enabled = false if NM.input2SetVisual then NM.input2SetVisual(false) end if NM.input2BtnRef then NM.input2BtnRef.Visible = false end _G.AimbotMode = "Input1" elseif _G.AimbotMode == "Input1" then _G.AimbotMode = "Input3" else _G.AimbotMode = "Input1" end modeVal.Text = _G.AimbotMode resetAimbotTarget() _aimbotLastMode = _G.AimbotMode saveConfig() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Bat Speed (H)");autoBatSpeedBox=mkBox(row,NM.AUTO_BAT_SPEED,56,62,function(v) if v>=1 and v<=500 then NM.AUTO_BAT_SPEED=v end;saveConfig() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Auto TP Height") autoTPHeightBox=mkBox(row,NM.autoTPHeight,56,62,function(v) if v>=0 and v<=500 then NM.autoTPHeight=v else autoTPHeightBox.Text=tostring(NM.autoTPHeight) end saveConfig() end) end mkSect(singleSF,"Visual & Misc") do local row=mkRow(singleSF,38);mkLabel(row,"Anti Ragdoll") local pill,dot=mkPill(row,46) local arOn=false local function setARVisual(s) arOn=s;animPill(pill,dot,s) end _G._nmSharedAR = {visual=setARVisual, state=false} local arClk=Instance.new("TextButton",pill);arClk.Size=UDim2.new(1,0,1,0);arClk.BackgroundTransparency=1;arClk.Text="";arClk.ZIndex=5 arClk.Activated:Connect(function() arOn=not arOn;setARVisual(arOn) _G._nmSharedAR.state=arOn if arOn then _arStart() else _arStop() end end) pill.ZIndex=3;dot.ZIndex=4 end NM.setInfJumpVisual=mkToggle(singleSF,"Infinite Jump",function(on) NM.infJumpEnabled=on end) do local row=mkRow(singleSF,38);mkLabel(row,"Jump Mode") local modeBtn=Instance.new("TextButton",row) modeBtn.Size=UDim2.new(0,110,0,26) modeBtn.Position=UDim2.new(1,-118,0.5,-13) modeBtn.BackgroundColor3=Color3.fromRGB(45,45,60) modeBtn.BorderSizePixel=0 modeBtn.Font=Enum.Font.GothamBold modeBtn.TextSize=12 modeBtn.TextColor3=W modeBtn.ZIndex=3 Instance.new("UICorner",modeBtn).CornerRadius=UDim.new(0,8) modeBtn.AutoButtonColor=false local modeBtnBase=modeBtn.BackgroundColor3 modeBtn.MouseButton1Down:Connect(function() TS:Create(modeBtn,TweenInfo.new(0.08),{BackgroundColor3=Color3.fromRGB(255,255,255)}):Play() end) modeBtn.MouseButton1Up:Connect(function() TS:Create(modeBtn,TweenInfo.new(0.15),{BackgroundColor3=modeBtnBase}):Play() end) modeBtn.MouseLeave:Connect(function() TS:Create(modeBtn,TweenInfo.new(0.15),{BackgroundColor3=modeBtnBase}):Play() end) local function jumpLabelFor(t) return t=="new" and "New" or "Normal" end modeBtn.Text=jumpLabelFor(NM._nmJumpMode) NM.setJumpModeVisual=function(t) modeBtn.Text=jumpLabelFor(t) end modeBtn.Activated:Connect(function() NM._nmJumpMode = NM._nmJumpMode=="new" and "normal" or "new" modeBtn.Text=jumpLabelFor(NM._nmJumpMode) saveConfig() end) end setUnwalkVisual=mkToggle(singleSF,"Unwalk",function(on) NM.unwalkEnabled=on;if on then startUnwalk() else stopUnwalk() end end) setAntiLagVisual=mkToggle(singleSF,"Anti Lag",function(on) if on then enableAntiLag() else disableAntiLag() end saveConfig() end) NM.setStretchRezVisual=mkToggle(singleSF,"Stretch Rez",function(on) if on then enableStretchRez() else disableStretchRez() end saveConfig() end) mkSect(singleSF,"Interface Settings") do local row=mkRow(singleSF,38);mkLabel(row,"Button Width") local wBox=mkBox(row,btnWidth,56,62,function(v) if v>=1 then btnWidth=v;saveBtnSize() if UI_MODE == "UI1" then for name, data in pairs(savedBtnPositions) do if data.btn and data.btn.Parent then local btn=data.btn btn.Size=UDim2.new(0,btnWidth,0,btnHeight) btn.TextSize=math.clamp(math.floor(math.min(btnWidth,btnHeight)/6.2),8,22) end end end end end) btnWidthBox=wBox end do local row=mkRow(singleSF,38);mkLabel(row,"Button Height") local hBox=mkBox(row,btnHeight,56,62,function(v) if v>=1 then btnHeight=v;btnSpacing=btnHeight+8;saveBtnSize() if UI_MODE == "UI1" then for name, data in pairs(savedBtnPositions) do if data.btn and data.btn.Parent then local btn=data.btn btn.Size=UDim2.new(0,btnWidth,0,btnHeight) btn.TextSize=math.clamp(math.floor(math.min(btnWidth,btnHeight)/6.2),8,22) end end end end end) btnHeightBox=hBox end do local row=mkRow(singleSF,38);mkLabel(row,"Reset Default") local resetBtn=Instance.new("TextButton",row) resetBtn.Size=UDim2.new(0,86,0,24);resetBtn.Position=UDim2.new(1,-92,0.5,-12) resetBtn.BackgroundColor3=Color3.fromRGB(200,200,210);resetBtn.BorderSizePixel=0 resetBtn.Text="RESET";resetBtn.TextColor3=Color3.fromRGB(20,20,25) resetBtn.Font=Enum.Font.GothamBlack;resetBtn.TextSize=10;resetBtn.ZIndex=5 Instance.new("UICorner",resetBtn).CornerRadius=UDim.new(0,6) local rstStroke=Instance.new("UIStroke",resetBtn);rstStroke.Color=Color3.fromRGB(180,180,200);rstStroke.Thickness=1 resetBtn.MouseEnter:Connect(function() TS:Create(resetBtn,TweenInfo.new(0.1),{BackgroundColor3=Color3.fromRGB(220,220,230)}):Play() end) resetBtn.MouseLeave:Connect(function() TS:Create(resetBtn,TweenInfo.new(0.1),{BackgroundColor3=Color3.fromRGB(200,200,210)}):Play() end) resetBtn.Activated:Connect(function() btnWidth=80;btnHeight=80;btnSpacing=88;saveBtnSize() if btnWidthBox then btnWidthBox.Text="80" end if btnHeightBox then btnHeightBox.Text="80" end if UI_MODE == "UI1" then for name, data in pairs(savedBtnPositions) do if data.btn and data.btn.Parent then local btn=data.btn btn.Size=UDim2.new(0,btnWidth,0,btnHeight) btn.TextSize=math.clamp(math.floor(math.min(btnWidth,btnHeight)/6.2),8,22) end end end end) end do local row=mkRow(singleSF,38);mkLabel(row,"UI Mode (UI2)") local pill,dot=mkPill(row,46) local ui2On = (UI_MODE == "UI2") local function setUIVisual(s) ui2On = s;animPill(pill,dot,s) UI_MODE = s and "UI2" or "UI1";saveUIMode() for name, data in pairs(savedBtnPositions) do if data.btn and data.btn.Parent then applyButtonShape(data.btn) end end end local clk=Instance.new("TextButton",pill);clk.Size=UDim2.new(1,0,1,0);clk.BackgroundTransparency=1;clk.Text="";clk.ZIndex=5 clk.Activated:Connect(function() setUIVisual(not ui2On) end) pill.ZIndex=3;dot.ZIndex=4 if ui2On then animPill(pill,dot,true) end end do local row=mkRow(singleSF,38);mkLabel(row,"Reset Btn Pos") local resetBtnPos=Instance.new("TextButton",row) resetBtnPos.Size=UDim2.new(0,86,0,24);resetBtnPos.Position=UDim2.new(1,-92,0.5,-12) resetBtnPos.BackgroundColor3=Color3.fromRGB(200,200,210);resetBtnPos.BorderSizePixel=0 resetBtnPos.Text="RESET";resetBtnPos.TextColor3=Color3.fromRGB(20,20,25) resetBtnPos.Font=Enum.Font.GothamBlack;resetBtnPos.TextSize=10;resetBtnPos.ZIndex=5 Instance.new("UICorner",resetBtnPos).CornerRadius=UDim.new(0,6) local rstStroke=Instance.new("UIStroke",resetBtnPos);rstStroke.Color=Color3.fromRGB(180,180,200);rstStroke.Thickness=1 resetBtnPos.MouseEnter:Connect(function() TS:Create(resetBtnPos,TweenInfo.new(0.1),{BackgroundColor3=Color3.fromRGB(220,220,230)}):Play() end) resetBtnPos.MouseLeave:Connect(function() TS:Create(resetBtnPos,TweenInfo.new(0.1),{BackgroundColor3=Color3.fromRGB(200,200,210)}):Play() end) resetBtnPos.Activated:Connect(function() resetButtonPositions() UI_MODE = "UI1";saveUIMode() local defaults = { BtnEnvyBat = UDim2.new(1, -(btnWidth*2 + 110), 0, TOP_Y), BtnInput2 = UDim2.new(1, -(btnWidth*2 + 110), 0, TOP_Y + btnSpacing), BtnAL = UDim2.new(1, -(btnWidth+100), 0, TOP_Y), BtnAR = UDim2.new(1, -(btnWidth+10), 0, TOP_Y), BtnTP = UDim2.new(1, -(btnWidth+100), 0, TOP_Y + btnSpacing), BtnDrop = UDim2.new(1, -(btnWidth+10), 0, TOP_Y + btnSpacing), BtnSpd = UDim2.new(1, -(btnWidth+100), 0, TOP_Y + btnSpacing * 2), BtnLag = UDim2.new(1, -(btnWidth+10), 0, TOP_Y + btnSpacing * 2), BtnBat = UDim2.new(1, -(btnWidth+100), 0, TOP_Y + btnSpacing * 3), BtnReset = UDim2.new(1, -(btnWidth+10), 0, TOP_Y + btnSpacing * 3), } local function resetButtonByName(name, defaultPos) local btn = nil if savedBtnPositions[name] and savedBtnPositions[name].btn and savedBtnPositions[name].btn.Parent then btn = savedBtnPositions[name].btn else btn = gui:FindFirstChild(name) end if btn then applyButtonShape(btn) TS:Create(btn, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {Position = defaultPos}):Play() end end for name, pos in pairs(defaults) do resetButtonByName(name, pos) end if btnWidthBox then btnWidthBox.Text = tostring(btnWidth) end if btnHeightBox then btnHeightBox.Text = tostring(btnHeight) end task.delay(0.35, saveButtonPositions) end) end do local row=mkRow(singleSF,38);mkLabel(row,"Lock UI Pos") local lockPill,lockDot=mkPill(row,46) local lockOn=false local function setLockVisual(s) lockOn=s;animPill(lockPill,lockDot,s) end local lockClk=Instance.new("TextButton",lockPill);lockClk.Size=UDim2.new(1,0,1,0);lockClk.BackgroundTransparency=1;lockClk.Text="";lockClk.ZIndex=5 lockClk.Activated:Connect(function() lockOn=not lockOn;setLockVisual(lockOn) NM.uiPositionLocked=lockOn;saveConfig() end) lockPill.ZIndex=3;lockDot.ZIndex=4 _setLockVisualExternal = function(state) lockOn=state;setLockVisual(state);NM.uiPositionLocked=state end end mkSect(singleSF, "⌨️ KEYBINDS") mkSect(singleSF,"Movement Keys") do local row=mkRow(singleSF,38);mkLabel(row,"Speed Key");mkKB(row,KB.SpeedToggle,function(k,isGp) if isGp then KB.SpeedToggle.gp=k;KB.SpeedToggle.kb=nil else KB.SpeedToggle.kb=k;KB.SpeedToggle.gp=nil end;saveConfig() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Lagger Key");mkKB(row,KB.LaggerToggle,function(k,isGp) if isGp then KB.LaggerToggle.gp=k;KB.LaggerToggle.kb=nil else KB.LaggerToggle.kb=k;KB.LaggerToggle.gp=nil end;saveConfig() end) end mkSect(singleSF,"Combat Keys") do local row=mkRow(singleSF,38);mkLabel(row,"Envy Bat TP Key") local kbBtn=Instance.new("TextButton",row) kbBtn.Size=UDim2.new(0,50,0,24);kbBtn.Position=UDim2.new(1,-54,0.5,-12) kbBtn.BackgroundColor3=Color3.fromRGB(12,12,18);kbBtn.BorderSizePixel=0 kbBtn.Text=NM.envyAutoBatKey.Name;kbBtn.TextColor3=Color3.fromRGB(230,230,240) kbBtn.Font=Enum.Font.GothamBold;kbBtn.TextSize=9;kbBtn.ZIndex=5 Instance.new("UICorner",kbBtn).CornerRadius=UDim.new(0,6) local bs=Instance.new("UIStroke",kbBtn);bs.Color=Color3.fromRGB(30,30,40);bs.Thickness=1 local li=false;local lc;local pv=kbBtn.Text;local listenStart=0 kbBtn.Activated:Connect(function() if li then li=false;if lc then lc:Disconnect();lc=nil end;kbBtn.Text=pv;kbBtn.TextColor3=Color3.fromRGB(230,230,240);return end pv=kbBtn.Text;li=true;listenStart=tick();kbBtn.Text="...";kbBtn.TextColor3=Color3.fromRGB(230,230,240) lc=UIS.InputBegan:Connect(function(inp) if not li then return end if inp.KeyCode==Enum.KeyCode.Escape then li=false;if lc then lc:Disconnect();lc=nil end;kbBtn.Text=pv;kbBtn.TextColor3=Color3.fromRGB(230,230,240);return end if inp.UserInputType==Enum.UserInputType.Keyboard then NM.envyAutoBatKeyType="Keyboard";NM.envyAutoBatKey=inp.KeyCode elseif inp.UserInputType==Enum.UserInputType.Gamepad1 then NM.envyAutoBatKeyType="Gamepad";NM.envyAutoBatKey=inp.KeyCode else return end kbBtn.Text=inp.KeyCode.Name;pv=inp.KeyCode.Name;kbBtn.TextColor3=Color3.fromRGB(230,230,240) li=false;if lc then lc:Disconnect();lc=nil end;saveConfig() end) end) end do local row=mkRow(singleSF,38);mkLabel(row,"Aimbot Key") local kbBtn=Instance.new("TextButton",row) kbBtn.Size=UDim2.new(0,50,0,24);kbBtn.Position=UDim2.new(1,-54,0.5,-12) kbBtn.BackgroundColor3=Color3.fromRGB(12,12,18);kbBtn.BorderSizePixel=0 kbBtn.Text=NM.aimbotModuleKey.Name;kbBtn.TextColor3=Color3.fromRGB(230,230,240) kbBtn.Font=Enum.Font.GothamBold;kbBtn.TextSize=9;kbBtn.ZIndex=5 Instance.new("UICorner",kbBtn).CornerRadius=UDim.new(0,6) local bs=Instance.new("UIStroke",kbBtn);bs.Color=Color3.fromRGB(30,30,40);bs.Thickness=1 local li=false;local lc;local pv=kbBtn.Text;local listenStart=0 kbBtn.Activated:Connect(function() if li then li=false;if lc then lc:Disconnect();lc=nil end;kbBtn.Text=pv;kbBtn.TextColor3=Color3.fromRGB(230,230,240);return end pv=kbBtn.Text;li=true;listenStart=tick();kbBtn.Text="...";kbBtn.TextColor3=Color3.fromRGB(230,230,240) lc=UIS.InputBegan:Connect(function(inp) if not li then return end if inp.KeyCode==Enum.KeyCode.Escape then li=false;if lc then lc:Disconnect();lc=nil end;kbBtn.Text=pv;kbBtn.TextColor3=Color3.fromRGB(230,230,240);return end if inp.UserInputType==Enum.UserInputType.Keyboard then NM.aimbotModuleKeyType="Keyboard";NM.aimbotModuleKey=inp.KeyCode elseif inp.UserInputType==Enum.UserInputType.Gamepad1 then NM.aimbotModuleKeyType="Gamepad";NM.aimbotModuleKey=inp.KeyCode else return end kbBtn.Text=inp.KeyCode.Name;pv=inp.KeyCode.Name;kbBtn.TextColor3=Color3.fromRGB(230,230,240) li=false;if lc then lc:Disconnect();lc=nil end;saveConfig() end) end) end mkSect(singleSF,"System Keys") do local row=mkRow(singleSF,38);mkLabel(row,"Hide UI Key");mkKB(row,KB.GuiHide,function(k,isGp) if isGp then KB.GuiHide.gp=k;KB.GuiHide.kb=nil else KB.GuiHide.kb=k;KB.GuiHide.gp=nil end;saveConfig() end) end do local row=mkRow(singleSF,38);mkLabel(row,"Reset All Keys") local resetBtn=Instance.new("TextButton",row) resetBtn.Size=UDim2.new(0,86,0,24);resetBtn.Position=UDim2.new(1,-92,0.5,-12) resetBtn.BackgroundColor3=Color3.fromRGB(200,200,210);resetBtn.BorderSizePixel=0 resetBtn.Text="RESET";resetBtn.TextColor3=Color3.fromRGB(20,20,25) resetBtn.Font=Enum.Font.GothamBlack;resetBtn.TextSize=10;resetBtn.ZIndex=5 Instance.new("UICorner",resetBtn).CornerRadius=UDim.new(0,6) local rstStroke=Instance.new("UIStroke",resetBtn);rstStroke.Color=Color3.fromRGB(180,180,200);rstStroke.Thickness=1 resetBtn.MouseEnter:Connect(function() TS:Create(resetBtn,TweenInfo.new(0.1),{BackgroundColor3=Color3.fromRGB(220,220,230)}):Play() end) resetBtn.MouseLeave:Connect(function() TS:Create(resetBtn,TweenInfo.new(0.1),{BackgroundColor3=Color3.fromRGB(200,200,210)}):Play() end) resetBtn.Activated:Connect(function() KB.DropBrainrot={kb=Enum.KeyCode.X,gp=nil} KB.AutoLeft={kb=Enum.KeyCode.Z,gp=nil} KB.AutoRight={kb=Enum.KeyCode.C,gp=nil} KB.AutoBat={kb=Enum.KeyCode.E,gp=nil} KB.LaggerToggle={kb=Enum.KeyCode.R,gp=nil} KB.TPFloor={kb=Enum.KeyCode.F,gp=nil} KB.InstaReset={kb=Enum.KeyCode.T,gp=nil} KB.GuiHide={kb=Enum.KeyCode.LeftControl,gp=nil} KB.SpeedToggle={kb=Enum.KeyCode.Q,gp=nil} NM.envyAutoBatKey=Enum.KeyCode.X NM.aimbotModuleKey=Enum.KeyCode.V saveConfig() pcall(function() gui:Destroy() end) task.wait(0.1) buildGui() loadConfigState() end) end UIS.InputBegan:Connect(function(input,gpe) if NM._anyKeyListening then return end if input.UserInputType==Enum.UserInputType.Keyboard then if gpe or UIS:GetFocusedTextBox() then return end elseif not isGamepadInput(input) then return end if not isBindableInput(input) then return end local kc=input.KeyCode if kbMatch(KB.LaggerToggle,kc) then toggleLaggerMode();saveConfig() elseif kbMatch(KB.SpeedToggle,kc) then toggleCarryMode();saveConfig() elseif kbMatch(KB.DropBrainrot,kc) then runDrop() elseif kbMatch(KB.TPFloor,kc) then runTPFloor() elseif kbMatch(KB.InstaReset,kc) then cursedInstaReset() elseif kbMatch(KB.AutoLeft,kc) then NM.autoLeftEnabled=not NM.autoLeftEnabled if NM.autoLeftEnabled then queueAutoLeftStart() else stopAutoLeft() end if NM.autoLeftSetVisual then NM.autoLeftSetVisual(NM.autoLeftEnabled) end elseif kbMatch(KB.AutoRight,kc) then NM.autoRightEnabled=not NM.autoRightEnabled if NM.autoRightEnabled then queueAutoRightStart() else stopAutoRight() end if NM.autoRightSetVisual then NM.autoRightSetVisual(NM.autoRightEnabled) end elseif kbMatch(KB.AutoBat,kc) then if not NM.autoBatEnabled then queueAutoBatStart();if NM.autoBatSetVisual then NM.autoBatSetVisual(true) end else NM.autoBatEnabled=false;disableAutoBat();_nmCleanLV();if NM.autoBatSetVisual then NM.autoBatSetVisual(false) end end elseif NM.envyAutoBatKey and kc == NM.envyAutoBatKey then NM.envyAutoBatToggled = not NM.envyAutoBatToggled if not NM.envyAutoBatToggled then if sethiddenproperty and envyHrp then pcall(function() sethiddenproperty(envyHrp, "PhysicsRepRootPart", nil) end) end end if NM.envyBtnUpd then pcall(NM.envyBtnUpd) end elseif NM.aimbotModuleKey and kc == NM.aimbotModuleKey then NM.aimbotModuleEnabled = not NM.aimbotModuleEnabled AimbotModule.set(NM.aimbotModuleEnabled) saveConfig() elseif kbMatch(KB.GuiHide,kc) then if main.Visible then hideGui() else showGui() end end end) end local _savedCfg = nil local function loadConfigKeys() if not(isfile and isfile("novaMoonPC.json")) then return end local ok,cfg=pcall(function() return HS:JSONDecode(readfile("novaMoonPC.json")) end) if not ok or not cfg then return end _savedCfg=cfg local function lk(e,d) if type(d)~="table" then return end;if d.kb and Enum.KeyCode[d.kb] then e.kb=Enum.KeyCode[d.kb] end;if d.gp and Enum.KeyCode[d.gp] then e.gp=Enum.KeyCode[d.gp] end end lk(KB.DropBrainrot,cfg.dropBrainrotKey);lk(KB.AutoLeft,cfg.autoLeftKey);lk(KB.AutoRight,cfg.autoRightKey) lk(KB.AutoBat,cfg.autoBatKey);lk(KB.LaggerToggle,cfg.laggerToggleKey) lk(KB.TPFloor,cfg.tpFloorKey);lk(KB.InstaReset,cfg.instaResetKey);lk(KB.GuiHide,cfg.guiHideKey);lk(KB.SpeedToggle,cfg.speedToggleKey) if cfg.normalSpeed then NM.NS=cfg.normalSpeed end if cfg.carrySpeed then NM.CS=cfg.carrySpeed end if cfg.speedProfile=="v1" or cfg.speedProfile=="v2" then NM._nmSpeedProfile=cfg.speedProfile end if cfg.speedStyle=="manual" or cfg.speedStyle=="auto" then NM._nmSpeedStyle=cfg.speedStyle end if cfg.speedStashV1NS and cfg.speedStashV1CS then NM._nmSpeedStash.v1={ns=cfg.speedStashV1NS,cs=cfg.speedStashV1CS} end if cfg.speedStashV2NS and cfg.speedStashV2CS then NM._nmSpeedStash.v2={ns=cfg.speedStashV2NS,cs=cfg.speedStashV2CS} end if cfg.laggerSpeed and type(cfg.laggerSpeed)=="number" then NM.LAGGER_SPEED=cfg.laggerSpeed end if cfg.laggerCarrySpeed and type(cfg.laggerCarrySpeed)=="number" then NM.LAGGER_CARRY_SPEED=cfg.laggerCarrySpeed end if cfg.autoTPHeight and type(cfg.autoTPHeight)=="number" then NM.autoTPHeight=cfg.autoTPHeight end if cfg.autoSwing~=nil then NM.autoSwingEnabled=cfg.autoSwing==true end if cfg.aimbotMode then _G.AimbotMode=cfg.aimbotMode end if cfg.input2Enabled~=nil then NM.input2Enabled=cfg.input2Enabled==true end if cfg.autoBatSpeed and type(cfg.autoBatSpeed)=="number" then NM.AUTO_BAT_SPEED=cfg.autoBatSpeed end if cfg.alSpeed and type(cfg.alSpeed)=="number" then NM.AL_SPEED=cfg.alSpeed end if cfg.arSpeed and type(cfg.arSpeed)=="number" then NM.AR_SPEED=cfg.arSpeed end if cfg.medTimer~=nil then NM.medTimerEnabled=cfg.medTimer==true end if cfg.envyAutoBatToggled~=nil then NM.envyAutoBatToggled=cfg.envyAutoBatToggled==true end if cfg.envyAutoBatButtonVisible~=nil then NM.envyAutoBatButtonVisible=cfg.envyAutoBatButtonVisible==true end if cfg.envyAutoBatKey and Enum.KeyCode[cfg.envyAutoBatKey] then NM.envyAutoBatKey=Enum.KeyCode[cfg.envyAutoBatKey] end if cfg.envyAutoBatKeyType then NM.envyAutoBatKeyType=cfg.envyAutoBatKeyType end if cfg.ppEnabled~=nil then NM.ppEnabled=cfg.ppEnabled==true end if cfg.aimbotModuleEnabled~=nil then NM.aimbotModuleEnabled=cfg.aimbotModuleEnabled==true end if cfg.aimbotModuleKey and Enum.KeyCode[cfg.aimbotModuleKey] then NM.aimbotModuleKey=Enum.KeyCode[cfg.aimbotModuleKey] end if cfg.nmAutoSteal~=nil then NM._nmStealEnabled=cfg.nmAutoSteal==true end if cfg.nmStealRadius and type(cfg.nmStealRadius)=="number" then NM._nmStealRadius=cfg.nmStealRadius end if cfg.nmStealDuration and type(cfg.nmStealDuration)=="number" then NM._nmStealDuration=cfg.nmStealDuration end if cfg.nmStealFireRange and type(cfg.nmStealFireRange)=="number" then NM._nmStealFireRange=cfg.nmStealFireRange end if cfg.nmStealHoldMin and type(cfg.nmStealHoldMin)=="number" then NM._nmStealHoldMin=cfg.nmStealHoldMin end if cfg.nmStealHoldMax and type(cfg.nmStealHoldMax)=="number" then NM._nmStealHoldMax=cfg.nmStealHoldMax end if cfg.nmStealEntryDelay and type(cfg.nmStealEntryDelay)=="number" then NM._nmStealEntryDelay=cfg.nmStealEntryDelay end if cfg.nmStealGuiType=="new" or cfg.nmStealGuiType=="normal" then NM._nmStealGuiType=cfg.nmStealGuiType end if cfg.jumpMode=="new" or cfg.jumpMode=="normal" then NM._nmJumpMode=cfg.jumpMode end if cfg.bodyLockRange and type(cfg.bodyLockRange)=="number" then NM.bodyLockRange=cfg.bodyLockRange end if cfg.dropStyle=="v1" or cfg.dropStyle=="v2" then NM._nmDropStyle=cfg.dropStyle end if cfg.aimbotModuleKeyType then NM.aimbotModuleKeyType=cfg.aimbotModuleKeyType end if cfg.antiKick~=nil then NM._nmAntiKickEnabled=cfg.antiKick==true end if cfg.espEnabled~=nil then NM._nmESPEnabled=cfg.espEnabled==true end if cfg.espColorR and cfg.espColorG and cfg.espColorB then NM._nmESPColor = Color3.new(cfg.espColorR, cfg.espColorG, cfg.espColorB) end if cfg.hitboxSize and type(cfg.hitboxSize)=="number" then NM._nmHitboxSize = math.clamp(cfg.hitboxSize, 5, 50) end if cfg.animPack and (cfg.animPack=="Default" or AnimationPacks[cfg.animPack]) then NM._nmAnimPack = cfg.animPack end end local function loadConfigState() local cfg=_savedCfg;if not cfg then return end if cfg.uiPositionLocked and _setLockVisualExternal then _setLockVisualExternal(true) end if normalBox then normalBox.Text=tostring(NM.NS) end if carryBox then carryBox.Text=tostring(NM.CS) end if NM.setSpeedProfileVisual then NM.setSpeedProfileVisual(NM._nmSpeedProfile) end if laggerBox then laggerBox.Text=tostring(NM.LAGGER_SPEED) end if laggerCarryBox then laggerCarryBox.Text=tostring(NM.LAGGER_CARRY_SPEED) end if autoTPHeightBox then autoTPHeightBox.Text=tostring(NM.autoTPHeight) end if bodyLockRangeBox then bodyLockRangeBox.Text=tostring(NM.bodyLockRange) end if autoBatSpeedBox then autoBatSpeedBox.Text=tostring(NM.AUTO_BAT_SPEED) end if alSpeedBox then alSpeedBox.Text=tostring(NM.AL_SPEED) end if arSpeedBox then arSpeedBox.Text=tostring(NM.AR_SPEED) end task.spawn(function() task.wait(0.15) if cfg.antiRagdoll and _G._nmSharedAR then _G._nmSharedAR.state=true if _G._nmSharedAR.visual then _G._nmSharedAR.visual(true) end _arStart() end if cfg.infiniteJump then NM.infJumpEnabled=true;if NM.setInfJumpVisual then NM.setInfJumpVisual(true) end end if cfg.medusaCounter then NM.medusaCounterEnabled=true;if NM.setMedusaVisual then NM.setMedusaVisual(true) end;setupMedusa(LP.Character) end if cfg.medusaAutoReset then NM.medusaAutoResetEnabled=true;if NM.setMedusaAutoResetVisual then NM.setMedusaAutoResetVisual(true) end;setupMedusaAutoReset(LP.Character) end if cfg.bodyLock then NM.bodyLockEnabled=true;if NM.setBodyLockVisual then NM.setBodyLockVisual(true) end;toggleBodyLock() end if cfg.batCounter then NM.batCounterEnabled=true;if NM.setBatCounterVisual then NM.setBatCounterVisual(true) end;NM.startBatCounter() end if cfg.laggerMode then NM.laggerToggled=true;NM.speedMode=false;NM.laggerPhase=cfg.laggerCarryMode and 2 or 1;refreshSpeedModeLabel() elseif cfg.carryMode then NM.speedMode=false;toggleCarryMode() end if cfg.autoTPEnabled then NM.autoTPEnabled=true;if NM.setAutoTPVisual then NM.setAutoTPVisual(true) end;startAutoTP() end if setAutoSwingVisual then setAutoSwingVisual(NM.autoSwingEnabled) end if cfg.autoBat then NM.autoBatEnabled=true;if NM.autoBatSetVisual then NM.autoBatSetVisual(true) end;queueAutoBatStart() end if cfg.input2Enabled then NM.input2Enabled=true if NM.input2SetVisual then NM.input2SetVisual(true) end if NM.input2BtnRef then NM.input2BtnRef.Visible=true end _G.AimbotMode="Input2";_aimbotLastMode="Input2" if not NM.autoBatEnabled then queueAutoBatStart() if NM.autoBatSetVisual then NM.autoBatSetVisual(true) end end end if cfg.unwalkEnabled then NM.unwalkEnabled=true;if setUnwalkVisual then setUnwalkVisual(true) end;task.spawn(function() task.wait(0.5);startUnwalk() end) end if cfg.antiLag then enableAntiLag();if setAntiLagVisual then setAntiLagVisual(true) end end if cfg.stretchRez then enableStretchRez();if NM.setStretchRezVisual then NM.setStretchRezVisual(true) end end if cfg.envyAutoBatButtonVisible and NM.envyBtnRef then NM.envyBtnRef.Visible=true;NM.envyAutoBatButtonVisible=true end if cfg.envyAutoBatToggled then NM.envyAutoBatToggled=true end if cfg.medTimer then NM.medTimerEnabled=true;if NM.setMedTimerVisual then NM.setMedTimerVisual(true) end end if cfg.ppEnabled then NM.ppEnabled=true;startPP() end if cfg.aimbotModuleEnabled then NM.aimbotModuleEnabled=true AimbotModule.set(true) end if cfg.nmAutoSteal then NM._nmStealEnabled=true if _G._nmToggleAutoSteal then _G._nmToggleAutoSteal(true) end if NM._nmStealSetVisual then NM._nmStealSetVisual(true) end end if cfg.antiKick then NM._nmAntiKickEnabled=true end if cfg.espEnabled then NM._nmESPEnabled=true end if cfg.hitboxSize then NM._nmHitboxSize=cfg.hitboxSize end if cfg.animPack and (cfg.animPack=="Default" or AnimationPacks[cfg.animPack]) then NM._nmAnimPack=cfg.animPack if NM._nmAnimPack~="Default" then pcall(ApplyAnimationPack,NM._nmAnimPack) end if NM.setAnimVisual then NM.setAnimVisual(NM._nmAnimPack) end end end) end loadConfigKeys() buildGui() loadConfigState() task.spawn(function() task.wait(0.5) local gui = game:GetService("CoreGui"):FindFirstChild("NovaMoonHub") or LP:WaitForChild("PlayerGui"):FindFirstChild("NovaMoonHub") if not gui then return end local miniBtn = gui:FindFirstChild("NovaMoonMiniBtn") if not miniBtn then return end local miniStatsFrame=Instance.new("Frame",gui) miniStatsFrame.Name="MiniStatsFrame" miniStatsFrame.Size=UDim2.new(0,90,0,36) miniStatsFrame.BackgroundTransparency=1 miniStatsFrame.BorderSizePixel=0 miniStatsFrame.ZIndex=20 miniStatsFrame.Visible=false miniStatsFrame.ClipsDescendants=false local miniFpsLabel=Instance.new("TextLabel",miniStatsFrame) miniFpsLabel.Name="MiniFps" miniFpsLabel.Size=UDim2.new(1,0,0.5,0) miniFpsLabel.Position=UDim2.new(0,0,0,0) miniFpsLabel.BackgroundTransparency=1 miniFpsLabel.Text="FPS: --" miniFpsLabel.TextColor3=Color3.fromRGB(255,255,255) miniFpsLabel.Font=Enum.Font.GothamBold;miniFpsLabel.TextSize=13 miniFpsLabel.TextXAlignment=Enum.TextXAlignment.Left miniFpsLabel.TextYAlignment=Enum.TextYAlignment.Bottom miniFpsLabel.ZIndex=21 local miniPingLabel=Instance.new("TextLabel",miniStatsFrame) miniPingLabel.Name="MiniPing" miniPingLabel.Size=UDim2.new(1,0,0.5,0) miniPingLabel.Position=UDim2.new(0,0,0.5,0) miniPingLabel.BackgroundTransparency=1 miniPingLabel.Text="PING: --ms" miniPingLabel.TextColor3=Color3.fromRGB(255,255,255) miniPingLabel.Font=Enum.Font.GothamBold;miniPingLabel.TextSize=13 miniPingLabel.TextXAlignment=Enum.TextXAlignment.Left miniPingLabel.TextYAlignment=Enum.TextYAlignment.Top miniPingLabel.ZIndex=21 RunService.RenderStepped:Connect(function() if miniBtn.Visible and miniStatsFrame.Visible then miniStatsFrame.Position=UDim2.new( miniBtn.Position.X.Scale, miniBtn.Position.X.Offset+miniBtn.AbsoluteSize.X+10, miniBtn.Position.Y.Scale, miniBtn.Position.Y.Offset ) end end) local mainPanel = gui:FindFirstChild("MainPanel") if mainPanel then mainPanel:GetPropertyChangedSignal("Visible"):Connect(function() miniStatsFrame.Visible = not mainPanel.Visible and miniBtn.Visible end) end task.spawn(function() while task.wait(0.5) do local fps=0 pcall(function() fps=math.floor(workspace:GetRealPhysicsFPS()) end) if fps>0 then miniFpsLabel.Text="FPS: "..tostring(fps) end end end) task.spawn(function() while task.wait(0.5) do local ping=0 pcall(function() local stats=game:GetService("Stats") local network=stats:FindFirstChild("Network") if network then local serverStats=network:FindFirstChild("ServerStatsItem") if serverStats then local dataPing=serverStats:FindFirstChild("Data Ping") if dataPing then local val=dataPing:GetValue() if val and val>0 then ping=math.floor(val) end end end end end) if ping==0 then pcall(function() local np=LP:GetNetworkPing() if np and np>0 then ping=math.floor(np*1000) end end) end if ping==0 then pcall(function() local perf=game:GetService("Stats").PerformanceStats if perf then local pingStat=perf:FindFirstChild("Ping") if pingStat then ping=math.floor(pingStat:GetValue()) end end end) end if ping>0 then miniPingLabel.Text="PING: "..ping.."ms" end end end) end) local _medTimerConn = nil local _medTimerActive = false local _medTimerThread = nil local _medTimerDebounce = false local function startMedTimer() local char = LP.Character if not char then return end local head = char:FindFirstChild("Head") if not head then return end pcall(function() local old = head:FindFirstChild("NovaMoonMedTimerBB");if old then old:Destroy() end end) local bb = Instance.new("BillboardGui") bb.Name = "NovaMoonMedTimerBB" bb.Size = UDim2.new(0, 200, 0, 52) bb.StudsOffset = Vector3.new(0, 3.5, 0) bb.AlwaysOnTop = true bb.LightInfluence = 0 bb.MaxDistance = 1500 pcall(function() bb.Parent = head end) bb.Enabled = false local bgFrame = Instance.new("Frame", bb) bgFrame.Name = "Bg" bgFrame.Size = UDim2.new(1, 0, 1, 0) bgFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 25) bgFrame.BackgroundTransparency = 0.1 bgFrame.BorderSizePixel = 0 Instance.new("UICorner", bgFrame).CornerRadius = UDim.new(0, 12) local stroke = Instance.new("UIStroke", bgFrame) stroke.Color = Color3.fromRGB(60, 60, 90) stroke.Thickness = 1.5 local avatarFrame = Instance.new("Frame", bgFrame) avatarFrame.Name = "AvatarFrame" avatarFrame.Size = UDim2.new(0, 36, 0, 36) avatarFrame.Position = UDim2.new(0, 8, 0.5, -18) avatarFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 45) avatarFrame.BorderSizePixel = 0 Instance.new("UICorner", avatarFrame).CornerRadius = UDim.new(1, 0) local avatarStroke = Instance.new("UIStroke", avatarFrame) avatarStroke.Color = Color3.fromRGB(80, 80, 100) avatarStroke.Thickness = 1.5 local avatarImg = Instance.new("ImageLabel", avatarFrame) avatarImg.Name = "AvatarImage" avatarImg.Size = UDim2.new(1, 0, 1, 0) avatarImg.BackgroundTransparency = 1 avatarImg.Image = "" avatarImg.ScaleType = Enum.ScaleType.Crop Instance.new("UICorner", avatarImg).CornerRadius = UDim.new(1, 0) task.spawn(function() local success, content = pcall(function() return Players:GetUserThumbnailAsync(LP.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size48x48) end) if success then avatarImg.Image = content end end) local timerLbl = Instance.new("TextLabel", bgFrame) timerLbl.Name = "TimerLabel" timerLbl.Size = UDim2.new(0, 140, 0, 36) timerLbl.Position = UDim2.new(0, 52, 0.5, -18) timerLbl.BackgroundTransparency = 1 timerLbl.Text = "" timerLbl.Font = Enum.Font.GothamBlack timerLbl.TextSize = 22 timerLbl.TextColor3 = Color3.fromRGB(230, 230, 240) timerLbl.TextStrokeTransparency = 0.1 timerLbl.TextStrokeColor3 = Color3.new(0, 0, 0) timerLbl.TextXAlignment = Enum.TextXAlignment.Left timerLbl.TextYAlignment = Enum.TextYAlignment.Center timerLbl.Visible = false local statusLbl = Instance.new("TextLabel", bgFrame) statusLbl.Name = "StatusLabel" statusLbl.Size = UDim2.new(0, 140, 0, 14) statusLbl.Position = UDim2.new(0, 52, 0.5, 10) statusLbl.BackgroundTransparency = 1 statusLbl.Text = "MEDUSA TIMER" statusLbl.Font = Enum.Font.GothamBold statusLbl.TextSize = 9 statusLbl.TextColor3 = Color3.fromRGB(140, 140, 160) statusLbl.TextStrokeTransparency = 0.3 statusLbl.TextStrokeColor3 = Color3.new(0, 0, 0) statusLbl.TextXAlignment = Enum.TextXAlignment.Left statusLbl.TextYAlignment = Enum.TextYAlignment.Top statusLbl.Visible = false local function runCountdown() if _medTimerDebounce then return end _medTimerDebounce = true if _medTimerThread then pcall(function() task.cancel(_medTimerThread) end);_medTimerThread = nil end _medTimerThread = task.spawn(function() pcall(function() bb.Enabled = true;timerLbl.Visible = true;statusLbl.Visible = true end) local steps = { {text = "3", col = Color3.fromRGB(255, 60, 60)}, {text = "2", col = Color3.fromRGB(255, 140, 40)}, {text = "1", col = Color3.fromRGB(255, 220, 40)} } for _, step in ipairs(steps) do pcall(function() timerLbl.Text = step.text;timerLbl.TextColor3 = step.col end) task.wait(1) if not NM.medTimerEnabled then break end end pcall(function() timerLbl.Text = "READY";timerLbl.TextColor3 = Color3.fromRGB(80, 255, 120) end) task.wait(1.5) pcall(function() bb.Enabled = false;timerLbl.Visible = false;statusLbl.Visible = false;timerLbl.Text = "" end) _medTimerThread = nil _medTimerDebounce = false end) end if _medTimerConn then pcall(function() _medTimerConn:Disconnect() end);_medTimerConn = nil end _medTimerConn = RunService.Heartbeat:Connect(function() if not NM.medTimerEnabled then return end local hum2 = nil pcall(function() local c = LP.Character;if c then hum2 = c:FindFirstChildOfClass("Humanoid") end end) if not hum2 then return end local st = nil pcall(function() st = hum2:GetState() end) if st == Enum.HumanoidStateType.Physics or st == Enum.HumanoidStateType.Ragdoll or st == Enum.HumanoidStateType.FallingDown then runCountdown() end end) end local function stopMedTimer() if _medTimerConn then pcall(function() _medTimerConn:Disconnect() end);_medTimerConn = nil end if _medTimerThread then pcall(function() task.cancel(_medTimerThread) end);_medTimerThread = nil end _medTimerDebounce = false pcall(function() local char = LP.Character if char then local head = char:FindFirstChild("Head") if head then local bb = head:FindFirstChild("NovaMoonMedTimerBB");if bb then bb:Destroy() end end end end) end task.spawn(function() task.wait(1) if NM.medTimerEnabled then pcall(startMedTimer) end end) LP.CharacterAdded:Connect(function() task.wait(0.6) if NM.medTimerEnabled then pcall(startMedTimer) end end)
