
local SAB_LIVE = function() return true end
local _SABCAP = {}
if not LPH_OBFUSCATED then
    local ok, env = pcall(getfenv)
    local _envs = {}
    if ok and type(env) == "table" then _envs[#_envs + 1] = env end
    if getgenv then local gok, g = pcall(getgenv); if gok and type(g) == "table" then _envs[#_envs + 1] = g end end
    if type(_G) == "table" then _envs[#_envs + 1] = _G end
    for _, e in ipairs(_envs) do
        if not e["LPH_ATTRIBUTES"] then e["LPH_ATTRIBUTES"] = function() end end
        if not e["VM"]     then e["VM"]     = function() end end
        if not e["PRESET"] then e["PRESET"] = function() end end
        if not e["ERROR_HANDLING"] then e["ERROR_HANDLING"] = function() end end
        if not e["LPH_ENCSTR"] then e["LPH_ENCSTR"] = function(s) return s end end
        if not e["LPH_ENCNUM"] then e["LPH_ENCNUM"] = function(n) return n end end
    end end
_G.JAF_M2 = function(ax, az, bx, bz) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local dx, dz = ax - bx, az - bz
    return math.sqrt(dx * dx + dz * dz)
end
local _P; _P = function(o, ...) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            for i = 1, select("#", ...), 2 do o[select(i, ...)] = select(i + 1, ...) end return o end
do
    local _clsMemo = {}
    local _isACN; _isACN = (function(cn, inst, cls) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _G.JAF_ClassMemo == false then return inst:IsA(cls) end
        local m = _clsMemo[cls]
        if not m then m = {}; _clsMemo[cls] = m end
        local v = m[cn]
        if v == nil then v = inst:IsA(cls); m[cn] = v end
        return v
    end)
    _G.JAF_IsACN = _isACN
    _G.JAF_IsA = (function(inst, cls) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _G.JAF_ClassMemo == false then return inst:IsA(cls) end
        return _isACN(inst.ClassName, inst, cls)
    end) end
_G.JAF_BootReady = false
if _G.JAF_BootYield == false then _G.JAF_BootReady = true end
if _G.JAF_BootYield ~= false then
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local t0 = os.clock()
        local cap = tonumber(_G.JAF_BootYieldMax) or 20
        if not game:IsLoaded() then game.Loaded:Wait() end
        local RS = game:GetService("ReplicatedStorage")
        local pkgs = RS:WaitForChild("Packages", 10)
        local netF = pkgs and pkgs:WaitForChild("Net", 10)
        if netF then
            local last, stable = -1, 0
            while os.clock() - t0 < cap do
                local n = #netF:GetChildren()
                if n == last and n > 0 then
                    stable = stable + 1
                    if stable >= 2 then break end
                else
                    stable, last = 0, n
                end
                task.wait(0.2)
            end end
        _G.JAF_BootYieldTook = os.clock() - t0
    end)
    _G.JAF_BootReady = true
    end) end
if not game:IsLoaded() then game.Loaded:Wait() end
do
    local _efOn = true
    pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if type(isfile) == "function" and type(readfile) == "function" and isfile("SammydawgHub.json") then
            local d = game:GetService("HttpService"):JSONDecode(readfile("SammydawgHub.json"))
            if type(d) == "table" and d.FpsOptimizer == false then _efOn = false end
        end end)
    if _efOn then
        local _efPlayers = game:GetService("Players")
        local _efCls = {"Shirt","Pants","ShirtGraphic","Accessory","Hat","HairAccessory",
            "FaceAccessory","NeckAccessory","ShoulderAccessory","FrontAccessory",
            "BackAccessory","WaistAccessory"}
        local function _efStrip(obj, isOther) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if _G.JAF_FpsOptimizer == false then return end
            pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if obj.Name == "Overhead" then return end
                for _, c in ipairs(_efCls) do if obj:IsA(c) then obj:Destroy(); return end end
                if isOther and obj:IsA("BasePart") and obj.Name ~= "HumanoidRootPart" then
                    local s = obj.Size
                    if s.X > 10 or s.Y > 10 or s.Z > 10 then
                        _P(obj, "LocalTransparencyModifier", 1, "Transparency", 1, "CanCollide", false, "CanQuery", false, "CanTouch", false, "CastShadow", false)
                    end end end) end
        local function _efChar(char) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if not char then return end
            local _lp = _efPlayers.LocalPlayer
            local isOther = (_lp ~= nil) and (_efPlayers:GetPlayerFromCharacter(char) ~= _lp)
            task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                for _, d in ipairs(char:GetDescendants()) do _efStrip(d, isOther) end
            end)
            char.DescendantAdded:Connect(function(d) LPH_ATTRIBUTES(ERROR_HANDLING(false))
task.defer(_efStrip, d, isOther) end)
        end
        local function _efHook(plr) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            if plr.Character then _efChar(plr.Character) end
            plr.CharacterAdded:Connect(_efChar)
        end
        for _, plr in ipairs(_efPlayers:GetPlayers()) do pcall(_efHook, plr) end
        _efPlayers.PlayerAdded:Connect(_efHook)
    end end
_G.SAB_T_GameLoaded = tick()
if not _G.JAF_WaitPackages then
    local _pkgCache
    _G.JAF_WaitPackages = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _pkgCache and _pkgCache.Parent then return _pkgCache end
        local RS = game:GetService("ReplicatedStorage")
        local p = RS:FindFirstChild("Packages")
        while not p do p = RS:WaitForChild("Packages", 5) end
        _pkgCache = p
        return p
    end end
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local _SyncReal = nil
local _SyncProxy
local function _syncMod() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if _SyncReal ~= nil then return _SyncReal end
    local ok, m = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return require(ReplicatedStorage:WaitForChild("Packages"):WaitForChild("Synchronizer")) end)
    if ok and type(m) == "table" then _SyncReal = m end
    return _SyncReal
end
_G.JAF_SyncMod = _syncMod
_G.JAF_SyncModLoaded = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _SyncReal ~= nil end
_SyncProxy = setmetatable({}, {
    __index = function(_, k) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local m = _syncMod(); if not m then return nil end
        local v = m[k]
        if type(v) ~= "function" then return v end
        return function(a, ...) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if rawequal(a, _SyncProxy) then return v(m, ...) end return v(a, ...) end
    end,
    __newindex = function(_, k, v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
local m = _syncMod(); if m then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
m[k] = v end) end end,
})
local Synchronizer = _SyncProxy
task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
local t0 = os.clock() _syncMod() _G.JAF_SyncModWarmMs = (os.clock() - t0) * 1000 end)
_G.JAF_Mod = (function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local cache = {}
    return function(folder, name) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local key = folder .. "." .. name
        local hit = cache[key]
        if hit ~= nil then return hit or nil end
        local ok, mod = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local RS2 = game:GetService("ReplicatedStorage")
            return require(RS2:WaitForChild(folder, 10):WaitForChild(name, 10))
        end)
        cache[key] = (ok and mod) or false
        return cache[key] or nil
    end end)()
do
    local _RS = game:GetService("ReplicatedStorage")
    local _D  = _RS:WaitForChild("Datas")
    local okA, _A = pcall(require, _D:WaitForChild("Animals"))
    local okG, _G_ = pcall(require, _D:WaitForChild("Game"))
    local okM, _M = pcall(require, _D:WaitForChild("Mutations"))
    local okT, _T = pcall(require, _D:WaitForChild("Traits"))
    _G.JAF_SafeGen = function(index, mutation, traits, _player) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
        if _G.JAF_UseGameGen == true then
            local okX, vX = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return require(_RS.Shared.Animals):GetGeneration(index, mutation, traits, nil) end)
            if okX then return vX end
        end
        if not (okA and _A) then return 0 end
        local e = _A[index]
        if not e then return 0 end
        local base = e.Generation
        if not base then local mod = (okG and _G_ and _G_.Game and _G_.Game.AnimalGanerationModifier) or 0 base = (e.Price or 0) * mod end
        local mult, sleepy = 1, false
        local _mu = mutation and okM and _M and _M[mutation]
        if _mu then mult = mult + (_mu.Modifier or 0) end
        if traits and okT and _T then
            for _, t in pairs(traits) do
                local tr = _T[t]
                if tr then if t == "Sleepy" then sleepy = true else mult = mult + (tr.MultiplierModifier or 0) end end
            end end
        local v = base * mult
        if sleepy then v = v * 0.5 end
        return math.round(v)
    end end
_G.Synchronizer = Synchronizer
if _G.JAF_NoSynchronizer == nil then _G.JAF_NoSynchronizer = false end
if _G.JAF_NoCarpetScan   == nil then _G.JAF_NoCarpetScan   = false end
if _G.JAF_NoUpvalues == nil then _G.JAF_NoUpvalues = true end
if _G.JAF_NoFSHook   == nil then _G.JAF_NoFSHook   = true end
_G.JAF_UseGameGen = false
do
    local realT = getfenv(0)
    local BAD = {
        writefile=1, readfile=1, appendfile=1, isfile=1, delfile=1, listfiles=1, makefolder=1,
        isfolder=1, delfolder=1, loadstring=1, hookfunction=1, hookmetamethod=1, newcclosure=1,
        getgenv=1, getrenv=1, getreg=1, getgc=1, getrawmetatable=1, setrawmetatable=1, setreadonly=1,
        getconnections=1, firesignal=1, gethui=1, get_hidden_gui=1, getcustomasset=1, setclipboard=1,
        queue_on_teleport=1, getscriptclosure=1, getscriptbytecode=1, decompile=1, identifyexecutor=1,
        getexecutorname=1, request=1, http_request=1, syn=1, protect_gui=1, checkcaller=1,
        islclosure=1, iscclosure=1, clonefunction=1, getcallingscript=1, getnilinstances=1,
        setthreadidentity=1, getthreadidentity=1, set_thread_identity=1, get_thread_identity=1,
    }
    local cleanT = setmetatable({}, {
        __index    = function(_, k) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if BAD[k] then return nil end return realT[k] end,
        __newindex = function(_, k, v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
realT[k] = v end,
    })
    local function safeCall(f, ...) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if _G.JAF_NoSafeCall == true then return pcall(f, ...) end
        local args = table.pack(...)
        local body = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
setfenv(0, cleanT) return table.pack(pcall(f, table.unpack(args, 1, args.n))) end
        setfenv(body, cleanT)
        local co = coroutine.create(body)
        local ok, packed = coroutine.resume(co)
        if not ok then return false, packed end
        if type(packed) ~= "table" then return false, "safeCall: target yielded" end
        return table.unpack(packed, 1, packed.n)
    end
    _G.JAF_SafeCall = safeCall
    local chanCache = setmetatable({}, {__mode = "k"})
    local function wrapChannel(ch) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if ch == nil or (type(ch) ~= "table" and type(ch) ~= "userdata") then return ch end
        local hit = chanCache[ch]; if hit then return hit end
        local w = setmetatable({}, {
            __index = function(_, k) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                local v = ch[k]
                if type(v) ~= "function" then return v end
                return function(_, ...) LPH_ATTRIBUTES(ERROR_HANDLING(false))
local ok, r = safeCall(v, ch, ...) if not ok then return nil end return r end
            end,
            __newindex = function(_, k, v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
ch[k] = v end,
        })
        chanCache[ch] = w
        return w
    end
    _G.JAF_WrapChannel = wrapChannel
    local _chanTable, _chanAt = nil, 0
    local HEAP_MIN = 24
    local heapScanAsync
    local _heapBusy, _heapDone, _missAt = false, false, 0
    local RETRY_COOLDOWN = 3
    local _heapMissAt = 0
    local HEAP_RETRY  = 5
    local GUIDPAT_C = "^%x%x%x%x%x%x%x%x%-%x%x%x%x%-%x%x%x%x%-%x%x%x%x%-%x%x%x%x%x%x%x%x%x%x%x%x$"
    local function _looksLikeRegistry(t) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if type(t) ~= "table" then return false end
        local hits, n = 0, 0
        for k, chan in pairs(t) do
            n = n + 1
            if n > 64 then break end
            if type(k) == "string" and #k == 36 and k:match(GUIDPAT_C) and type(chan) == "table"
               and type(rawget(chan, "CacheTable")) == "table" then
                hits = hits + 1
                if hits >= 3 then return true end
            end end
        return false
    end
    _G.JAF_ChanValid = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _looksLikeRegistry(_chanTable) end
    local _needCache, _needAt, _haveCache = 0, 0, 0
    local _pulled = {}
    local _pullT0 = tick()      -- ⭐own clock: JAF_HubStart is defined much later in the file
    _G.JAF_Pulled = _pulled
    local function _requestData(name) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local pk = ReplicatedStorage:FindFirstChild("Packages")
        local sy = pk and pk:FindFirstChild("Synchronizer")
        local rf = sy and sy:FindFirstChild("RequestData")
        if not rf or not rf:IsA("RemoteFunction") then return nil end
        local ok, res = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return rf:InvokeServer(name) end)
        if ok and type(res) == "table" then return res end
        return nil
    end
    _G.JAF_PullPlot = function(name) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local ct = _requestData(name)
        if ct then
            _pulled[name] = ct
            _G.JAF_PullOk = (tonumber(_G.JAF_PullOk) or 0) + 1
        else
            _G.JAF_PullFail = (tonumber(_G.JAF_PullFail) or 0) + 1
        end
        return ct
    end
    local function _instanceChans() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if not (getconnections and debug and debug.getupvalues) then return nil end
        local pk = ReplicatedStorage:FindFirstChild("Packages")
        local sy = pk and pk:FindFirstChild("Synchronizer")
        local cf = sy and sy:FindFirstChild("Channel")
        if not cf then return nil end
        local map, n = {}, 0
        for _, re in ipairs(cf:GetChildren()) do
            if re:IsA("RemoteEvent") then
                local okC, conns = pcall(getconnections, re.OnClientEvent)
                if okC and type(conns) == "table" then
                    for _, conn in ipairs(conns) do
                        local okF, f = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return conn.Function end)
                        if okF and type(f) == "function" then
                            local okU, uvs = pcall(debug.getupvalues, f)
                            if okU and type(uvs) == "table" then
                                for _, uv in ipairs(uvs) do
                                    if type(uv) == "table" and type(rawget(uv, "CacheTable")) == "table" then
                                        map[re.Name] = uv
                                        n = n + 1
                                        break
                                    end end end end
                        if map[re.Name] then break end
                    end end end end
        local function _realCount(c) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            if type(c) ~= "table" then return -1 end
            local al = rawget(c, "AnimalList") or rawget(c, "Animals")
            if type(al) ~= "table" then return -1 end
            local n2 = 0
            for _, a in pairs(al) do if type(a) == "table" then n2 = n2 + 1 end end
            return n2
        end
        local _inWindow = (tick() - _pullT0) < (tonumber(_G.JAF_PullTrustWindow) or 15)
        if not _inWindow and next(_pulled) ~= nil then
            local anyLive = false
            for kk in pairs(_pulled) do
                local c2 = map[kk]
                if c2 and _realCount(rawget(c2, "CacheTable")) >= 0 then anyLive = true break end
            end
            if anyLive then
                table.clear(_pulled)
                _G.JAF_PullDropped = (tonumber(_G.JAF_PullDropped) or 0) + 1
            end
        end
        for k, ct in pairs(_pulled) do
            local cur   = map[k]
            local curCT = cur and rawget(cur, "CacheTable")
            if cur == nil then
                map[k] = { CacheTable = ct }
                n = n + 1
            elseif _inWindow and _realCount(ct) > _realCount(curCT) then
                map[k] = { CacheTable = ct }
                _G.JAF_PullPreferred = (tonumber(_G.JAF_PullPreferred) or 0) + 1
            end end
        if n == 0 then return nil end
        _G.JAF_ChanInstCount = n
        if not _G.JAF_ChansFirstAt then _G.JAF_ChansFirstAt = tick() end
        return map
    end
    _G.JAF_InstanceChans = _instanceChans
    if _G.JAF_PullOnJoin ~= false then
        task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local t0 = os.clock()
            local plotsF = workspace:FindFirstChild("Plots") or workspace:WaitForChild("Plots", 10)
            if not plotsF then return end
            local kids = plotsF:GetChildren()
            local left = #kids
            for _, pl in ipairs(kids) do task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G.JAF_PullPlot(pl.Name) left = left - 1 end) end
            local t1 = os.clock()
            while left > 0 and (os.clock() - t1) < 8 do task.wait(0.02) end
            _G.JAF_PullMs = (os.clock() - t0) * 1000
            local got = 0 ; for _ in pairs(_pulled) do got = got + 1 end
            local function allSettled() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                for _, ct in pairs(_pulled) do
                    if type(ct) == "table" and rawget(ct, "Owner") ~= nil then
                        local al = rawget(ct, "AnimalList") or rawget(ct, "Animals")
                        local real = 0
                        if type(al) == "table" then
                            for _, a in pairs(al) do if type(a) == "table" then real = real + 1 end end
                        end
                        if real == 0 then return false end   -- owned base, still empty -> ask again
                    end
                end
                return true
            end
            local rounds = math.clamp(tonumber(_G.JAF_PullRetries) or 4, 0, 8)
            for r = 1, rounds do
                if allSettled() then break end
                task.wait(tonumber(_G.JAF_PullRetryGap) or 0.4)
                local n2 = #kids
                for _, pl in ipairs(kids) do task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G.JAF_PullPlot(pl.Name) ; n2 = n2 - 1 end) end
                local t2 = os.clock()
                while n2 > 0 and (os.clock() - t2) < 5 do task.wait(0.02) end
                _G.JAF_PullRounds = r
                if allSettled() then
                    if _G.JAF_JoinMark then _G.JAF_JoinMark("pullSettled", "round " .. r) end
                    break
                end
            end end) end
    local ANIMAL_KEYS = { "Animals", "AnimalList" }
    local function animalsOf(cacheTable) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if type(cacheTable) ~= "table" then return nil end
        for _, k in ipairs(ANIMAL_KEYS) do local v = rawget(cacheTable, k) if type(v) == "table" then return v end end
        return nil
    end
    _G.JAF_AnimalsOf = animalsOf
    _G.JAF_OwnerName = function(o) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if o == nil then return nil end
        if typeof(o) == "Instance" then return o.Name end
        if type(o) == "table"  then return o.Name end
        if type(o) == "string" then if o == "" or o == "..." then return nil end return o end
        return nil
    end
    _G.JAF_OwnerPlayer = function(o) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if typeof(o) == "Instance" and o:IsA("Player") then return o end
        if type(o) == "table" and o.UserId then return game:GetService("Players"):GetPlayerByUserId(o.UserId) end
        local n = _G.JAF_OwnerName(o)
        return n and game:GetService("Players"):FindFirstChild(n) or nil
    end
    local function _findChans() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if _G.JAF_ChanInstOff ~= true then
            local _now = tick()
            if (_now - _needAt) > 2 then
                local _pl = workspace:FindFirstChild("Plots")
                _needCache, _needAt = (_pl and #_pl:GetChildren() or 0), _now
            end
            local _need     = _needCache
            local _have     = _haveCache
            local _complete = (_need > 0 and _have >= _need)
            if _complete and _G.JAF_ChanSource == "instances" and _chanTable and next(_chanTable) ~= nil
               and (tick() - _chanAt) < (tonumber(_G.JAF_ChanInstTTL) or 1) then
                return _chanTable
            end
            local m = _instanceChans()
            if m then
                if _chanTable then for k, v in pairs(_chanTable) do if m[k] == nil then m[k] = v end end end
                local mn = 0 ; for _ in pairs(m) do mn = mn + 1 end
                _chanTable, _chanAt = m, tick()
                _haveCache          = mn
                _G.JAF_ChanSource   = "instances"
                _G.JAF_ChanHave, _G.JAF_ChanNeed = mn, _need
                if _need > 0 and mn < _need then
                    _G.JAF_ChanPartial = (tonumber(_G.JAF_ChanPartial) or 0) + 1
                    if heapScanAsync then pcall(heapScanAsync) end
                end
                return m
            end end
        if _chanTable and not _looksLikeRegistry(_chanTable) then
            _chanTable = nil
            _haveCache = 0
            _heapDone  = false
            _G.JAF_ChanEvictions = (tonumber(_G.JAF_ChanEvictions) or 0) + 1
        end
        if _chanTable and next(_chanTable) ~= nil then return _chanTable end
        if _chanTable and (tick() - _chanAt) < 5 then return _chanTable end
        if (tick() - _missAt) < RETRY_COOLDOWN then return _chanTable end
        local best, bestScore = nil, 0
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local BUDGET, seen = 30000, {}
            local function score(t) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                local withCache, withAL, n = 0, 0, 0
                for _, v in next, t do
                    n = n + 1; if n > 400 then break end
                    if type(v) == "table" then
                        local c = rawget(v, "CacheTable")
                        if type(c) == "table" then withCache = withCache + 1 if animalsOf(c) then withAL = withAL + 1 end end
                    end end
                return withCache + withAL * 10
            end
            heapScanAsync = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                if not getgc then return end
                if _heapBusy or _heapDone then return end
                if (tick() - _heapMissAt) < HEAP_RETRY then return end
                _heapBusy = true
                task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    local ok, gc = pcall(getgc, true)
                    if not ok or type(gc) ~= "table" then _heapBusy, _heapMissAt = false, tick() return end
                    local SLICE = math.clamp(tonumber(_G.JAF_HeapSliceMs) or 4, 1, 16) / 1000
                    local _lastYield = os.clock()
                    local GUIDPAT = "^%x%x%x%x%x%x%x%x%-%x%x%x%x%-%x%x%x%x%-%x%x%x%x%-%x%x%x%x%x%x%x%x%x%x%x%x$"
                    local best, bestGuid, bestAnimals, cands, since = nil, 0, -1, 0, 0
                    for i = 1, #gc do
                        local v = gc[i]
                        if type(v) == "table" then
                            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                                local guidHits, animals, n = 0, 0, 0
                                for k, chan in pairs(v) do
                                    n = n + 1
                                    if n > 64 then return end
                                    if type(k) == "string" and k:match(GUIDPAT) and type(chan) == "table" then
                                        local ct = rawget(chan, "CacheTable")
                                        if type(ct) == "table" then
                                            guidHits = guidHits + 1
                                            local al = rawget(ct, "AnimalList")
                                            if type(al) == "table" then
                                                for _, a in pairs(al) do if type(a) == "table" then animals = animals + 1 end end
                                            end end end end
                                if guidHits >= 3 then
                                    cands = cands + 1
                                    if guidHits > bestGuid or (guidHits == bestGuid and animals > bestAnimals) then
                                        best, bestGuid, bestAnimals = v, guidHits, animals
                                    end end end) end
                        if (os.clock() - _lastYield) >= SLICE then task.wait() _lastYield = os.clock() since = since + 1 end
                    end
                    _G.JAF_HeapYields = since
                    if best then
                        _chanTable, _chanAt = best, tick()
                        do local _hc = 0 for _ in pairs(best) do _hc = _hc + 1 end _haveCache = _hc end
                        _G.JAF_ChanGuids   = bestGuid
                        _G.JAF_ChanCands   = cands
                        _G.JAF_ChanSource  = "heap-validated"
                    end
                    _heapBusy = false
                    if best then
                        _heapDone = true
                    else
                        _heapMissAt = tick()
                        _G.JAF_HeapMisses = (tonumber(_G.JAF_HeapMisses) or 0) + 1
                    end end) end
            _G.JAF_HeapRescan = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_heapDone, _heapBusy, _chanTable = false, false, nil heapScanAsync() end
            _G.JAF_HeapScan = heapScanAsync
            if _G.JAF_HeapWarm ~= false then
                task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
local t0 = os.clock() pcall(heapScanAsync) _G.JAF_HeapWarmMs = (os.clock() - t0) * 1000 end)
            end
            local function scan(v, depth) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                BUDGET = BUDGET - 1
                if BUDGET <= 0 or depth > 7 then return end
                local tv = type(v)
                if tv == "table" then
                    if seen[v] then return end; seen[v] = true
                    local n = 0
                    for _ in next, v do n = n + 1; if n > 600 then return end end
                    local s = score(v)
                    if s > bestScore then best, bestScore = v, s end
                    for _, val in next, v do scan(val, depth + 1) end
                elseif tv == "function" then
                    if seen[v] then return end; seen[v] = true
                    local oku, ups = pcall(debug.getupvalues, v)
                    if oku and type(ups) == "table" then for _, u in next, ups do scan(u, depth + 1) end end
                end end
            for _, fn in next, (_syncMod() or {}) do if type(fn) == "function" then pcall(scan, fn, 0) end end
        end)
        if best and bestScore >= HEAP_MIN then
            _chanTable, _chanAt = best, tick()
            _G.JAF_ChanSource = "upvalues"
        else
            _missAt = tick()
            if not _chanTable then heapScanAsync() end
        end
        _G.JAF_ChanScore = bestScore
        return _chanTable
    end
    _G.JAF_FindChans = _findChans
    local dcCache = setmetatable({}, {__mode = "k"})
    local function directChannel(cd) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if type(cd) ~= "table" then return cd end
        local hit = dcCache[cd]; if hit then return hit end
        local w = {
            Raw = cd,
            Get = function(_, key) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                local ct = rawget(cd, "CacheTable")
                if type(ct) ~= "table" then return nil end
                if key == "AnimalList" or key == "Animals" then return _G.JAF_AnimalsOf(ct) end
                return rawget(ct, key)
            end,
            GetTable = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return rawget(cd, "CacheTable") end,
        }
        dcCache[cd] = w
        return w
    end
    _G.JAF_DirectChannel = directChannel
    if _G.JAF_SnitchPatchOn == true and _G.JAF_NoSnitchPatch ~= true then
        task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local t
            for _ = 1, 120 do
                t = _findChans()
                if type(t) == "table" and (_G.JAF_ChanScore or 0) >= 10 then break end
                t = nil
                task.wait(0.25)
            end
            if type(t) ~= "table" then _G.JAF_SnitchPatched = 0 return end
            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            _G.JAF_SyncOrig = {
                GetAllChannels      = Synchronizer.GetAllChannels,
                Get                 = Synchronizer.Get,
                GetTableFromChannel = Synchronizer.GetTableFromChannel,
            }
            local function reg() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _findChans() or t end
            local patched = 0
            local function put(name, fn) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
Synchronizer[name] = fn end) then patched = patched + 1 end end
            put("GetAllChannels", function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return reg() end)
            put("Get", function(_, key) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                local r = reg()
                local direct = rawget(r, key)
                if direct ~= nil then return direct end
                return rawget(r, tostring(key))
            end)
            put("GetTableFromChannel", function(_, key) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                local r = reg()
                local ch = rawget(r, key) or rawget(r, tostring(key))
                if type(ch) ~= "table" then return nil end
                return rawget(ch, "CacheTable")
            end)
            _G.JAF_SnitchPatched = patched
            end) end) end
    local _getCache = {}
    _G.JAF_SyncGetCache = _getCache
    local SyncSafe = setmetatable({}, {
        __index = function(_, k) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            if k == "GetAllChannels" then
                return function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    local t = _findChans(); if not t then return {} end
                    local out = {}
                    for n, cd in next, t do out[n] = directChannel(cd) end
                    return out
                end end
            if k == "Get" and _G.JAF_SyncPlain ~= true then
                return function(_, name) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    local t = _findChans(); if not t then return nil end
                    return directChannel(rawget(t, tostring(name)))
                end end
            if k == "Get" and _G.JAF_SyncPlain == true then
                return function(_, name) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    local key = tostring(name)
                    local hit = _getCache[key]
                    if hit ~= nil then return hit end
                    local ok, ch = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Synchronizer:Get(name) end)
                    if not ok or ch == nil then return nil end
                    local w = directChannel(ch)
                    _getCache[key] = w
                    return w
                end end
            local v = Synchronizer[k]
            if type(v) ~= "function" then return v end
            return function(_, ...) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                if k == "Get" then local ok, r = safeCall(v, _syncMod(), ...) if not ok then return nil end return wrapChannel(r) end
                local a = table.pack(...)
                local ok, r = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return v(_syncMod(), table.unpack(a, 1, a.n)) end)
                if not ok then return nil end
                return r
            end end, })
    _G.JAF_SyncSafe = SyncSafe
    if _G.JAF_SyncStub == true and debug and debug.getupvalues then
        task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local function isGuid(s) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                return type(s) == "string" and #s == 36 and s:upper() == s
                   and s:match("^%x%x%x%x%x%x%x%x%-%x%x%x%x%-%x%x%x%x%-%x%x%x%x%-%x%x%x%x%x%x%x%x%x%x%x%x$") ~= nil
            end
            local patched, BUDGET, seen = 0, 40000, {}
            local function scan(v, depth) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                BUDGET = BUDGET - 1
                if BUDGET <= 0 or depth > 8 then return end
                local tv = type(v)
                if tv == "table" then
                    if seen[v] then return end; seen[v] = true
                    local n = 0
                    for _ in next, v do n = n + 1; if n > 600 then return end end
                    if n <= 12 then
                        local guid, fnKeys = nil, {}
                        for k, val in next, v do
                            if isGuid(val) then guid = val
                            elseif type(val) == "function" then fnKeys[#fnKeys + 1] = k end
                        end
                        if guid and #fnKeys > 0 then
                            for _, fk in ipairs(fnKeys) do
                                local g = guid
                                if pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
v[fk] = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return g end end) then patched = patched + 1 end
                            end end end
                    for _, val in next, v do scan(val, depth + 1) end
                elseif tv == "function" then
                    if seen[v] then return end; seen[v] = true
                    local oku, ups = pcall(debug.getupvalues, v)
                    if oku and type(ups) == "table" then for _, u in next, ups do scan(u, depth + 1) end end
                end end
            for _, fn in next, (_syncMod() or {}) do if type(fn) == "function" then pcall(scan, fn, 0) end end
            _G.JAF_SyncStubbed = patched
        end) end end
_G.JAF_GetSynchronizer = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_NoSafeCall == true then return Synchronizer end return _G.JAF_SyncSafe end
do
    local _PS = game:GetService("Players")
    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_PS.RespawnTime = 0 end)
    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_PS.CharacterAutoLoads = true end)
    pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        _PS:GetPropertyChangedSignal("CharacterAutoLoads"):Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            if _PS.CharacterAutoLoads ~= true then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_PS.CharacterAutoLoads = true end) end
        end) end)
    pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        _PS:GetPropertyChangedSignal("RespawnTime"):Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            if _PS.RespawnTime ~= 0 then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_PS.RespawnTime = 0 end) end
        end) end)
    local function _hookLP(lp) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not lp then return end
        lp.CharacterAdded:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false)) pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_PS.RespawnTime = 0 end) end)
    end
    _hookLP(_PS.LocalPlayer)
    _PS.PlayerAdded:Connect(function(plr) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if plr == _PS.LocalPlayer then _hookLP(plr) end
    end) end
pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if setfpscap then setfpscap(_G.JAF_FpsCap or 9999) end end)
local SharedState = {
    ConveyorAnimals = {},
    AllAnimalsCache = nil,
    DisableStealSpeed = nil,
    AdminButtonCache = {},
    StealSpeedToggleFunc = nil,
    BalloonedPlayers = {},
}
local Services = {
    Players = game:GetService("Players"),
    RunService = game:GetService("RunService"),
    UserInputService = game:GetService("UserInputService"),
    ReplicatedStorage = game:GetService("ReplicatedStorage"),
    TweenService = game:GetService("TweenService"),
    HttpService = game:GetService("HttpService"),
    Workspace = game:GetService("Workspace"),
    Lighting = game:GetService("Lighting"),
    VirtualInputManager = (cloneref and cloneref(game:GetService("VirtualInputManager"))) or game:GetService("VirtualInputManager"),
    GuiService = game:GetService("GuiService"),
    TeleportService = game:GetService("TeleportService"),
}
local Players = Services.Players
local RunService = Services.RunService
local UserInputService = Services.UserInputService
local ReplicatedStorage = Services.ReplicatedStorage
local TweenService = Services.TweenService
local HttpService = Services.HttpService
local Workspace = Services.Workspace
local Lighting = Services.Lighting
local VirtualInputManager = Services.VirtualInputManager
local GuiService = Services.GuiService
local TeleportService = Services.TeleportService
local LocalPlayer = Players.LocalPlayer
if _G.JAF_PingAdapt == nil then _G.JAF_PingAdapt = false end
_G.JAF_Ping = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if _G.JAF_PingAdapt ~= true then return 0 end
    local p = 0; pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
p = LocalPlayer:GetNetworkPing() or 0 end)
    return p
end
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local HubGui = PlayerGui
pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local h = (gethui and gethui()) or (get_hidden_gui and get_hidden_gui())
    if typeof(h) == "Instance" then HubGui = h end
end)
local hubFind; hubFind = function(name) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if HubGui ~= PlayerGui then local g = HubGui:FindFirstChild(name); if g then return g end end
    return PlayerGui:FindFirstChild(name)
end
local function safeKeyCode(name, fallback) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if type(name) ~= "string" then return fallback end
    local cleaned = name:gsub("^%s+", ""):gsub("%s+$", "")
    if cleaned == "" or cleaned == "--" or cleaned == "NONE" then return nil end
    local ok, kc = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Enum.KeyCode[cleaned] end)
    if ok and kc then return kc end
    return fallback
end
local FileName = "SammydawgHub.json"
do
    if type(isfile) == "function" and type(readfile) == "function" and type(writefile) == "function" then
        if not isfile(FileName) then
            for _, old in ipairs({ "SammydawgHub_v1.json", "JustAFanHub_v1.json" }) do
                if isfile(old) then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
writefile(FileName, readfile(old)) end); break end
            end end end end
local DefaultConfig = {
    Positions = {
        AdminPanel = {X = 0.1859375, Y = 0.5767123526556385},
        WalkingBestBar = {X = 0.5, Y = 0.96},
    },
    TpSettings = {
        Tool           = "Flying Carpet",
        TpKey          = "T",
        CloneKey       = "V",
        CarpetSpeedKey = "Q",
        InfiniteJump   = false,
        CacheWait      = 0.00,
        TpSpeedF1      = 130,
        TpSpeedF2      = 130,
        TpSpeedF1Front      = 130, TpSpeedF1Side      = 130,
        TpSpeedF2Front      = 130, TpSpeedF2Side      = 130,
        GrappleSpeedF1Front = 400, GrappleSpeedF1Side = 400,
        GrappleSpeedF2Front = 400, GrappleSpeedF2Side = 400,
        F1SpotMode      = "auto",
        F2SpotMode      = "auto",
        F2StealBelow    = true,
        TpMode          = "tween",
        GrappleFireMode = "firesignal",
        GrappleSpeed    = 400,
        BrainrotSpeed   = 175,
        InstaTpBrainrot = true,
        CloneDelay      = 0.15,
        F1FrontCloneDelay = 0.15,
        WaitStableFps   = false,
        FpsStableThresh = 60,
    },
    HitRecovery    = true,
    RetargetOnSelect      = true,
    SkipStolenTarget      = true,
    AutoTPPriority = true,
    StealSpeed   = 20,
    MenuKey      = "LeftControl",
    AntiRagdollV2 = true,
    PlayerESP    = true,
    StealerESP   = false,
    BaseOwnerESP = false,
    FpsOptimizer = true,
    RemoveAnimations = true,
    OptimizeFloorSmooth = true,
    OptimizeStripMeshTextures = true,
    DarkMode     = false,
    DarkBrightness = 0.4,
    BrainrotESP = true,
    LineToBase = false,
    StealNearest = false,
    ConveyorESP = false,
    PriorityList = {},
    MutationPriorityList = {},
    MutationPriorityEnabled = true,
    PrioritySkipPicker   = false,
    PriorityLastTab      = "brainrot",
    UILocked     = false,
    HideAdminPanel = false,
    HideAutoSteal = false,
    HideWalkingBest = false,
    AutoKickOnSteal = false,
    KickToPrivateOnSteal = false,
    PrivateServerCode = "",
    InvisStealAngle = 233,
    SinkSliderValue = 2.5,
    AutoRecoverLagback = true,
    AutoInvisDuringSteal = false,
    InvisDelayEnabled = false,
    InvisDelayMs = 0,
    InvisToggleKey = "I",
    ClickToAP = false,
    ClickToAPKeybind = "L",
    ProximityAP = false,
    ProximityAPKeybind = "P",
    ProximityRange = 15,
    StealSpeedKey = "C",
    ResetKey = "X",
    AntiBeeDisco = false,
    FOV = 70,
    AutoUnlockOnSteal = false,
    UIScale = 1,
    PanelScales = {},
    MobileMode      = "auto",
    HubButtonMode   = "auto",
    MobileHubButton = true,
    MobileSheet     = true,
    MobileBtnPos    = nil,
    KickKey = "",
    CleanErrorGUIs = false,
    ClickToAPSingleCommand = false,
    RagdollSelfKey = "",
    AutoBuyKey = "",
    DropBrainrotKey = "",
    FloatKey   = "" ,
    FloatSpeed = 40,
    AutoBuyEnabled = false,
    AutoBuyMode = "normal",
    AutoBuyFastRate = 12,
    CarpetAutoBuy = false,
    CarpetMinGen = "",
    CarpetBuySpeed = 70,
    AutoGrief = false,
    KickAfterBuy = false,
    KickAfterBuyToPS = false,
    AutoResetOnBalloon = false,
    AutoStealSpeed = false,
    HideAdminCooldowns = false,
    CurrentTheme = "white",
    CustomThemeHex = "FFC800",
    ThemeImageUrl = "",
    ShowMiniActions = true,
    MiniUIPos = {X = 0.01, Y = 0.35},
    Blacklist = {},
    AntiApAtlasUsers = true,
    AntiApKawaifuUsers = true,
    AntiApBraintopiaUsers = true,
    AntiApFmlyUsers = true,
    AntiApSonUsers = true,
    GriefDetectorEnabled = true,
    AdminRandomCmd = false,
    ShowProxCircle = false,
    AdminAllowedCmds = {},
    AdminCmdOrder = {"ragdoll", "balloon", "rocket", "jail", "inverse", "jumpscare", "morph", "nightvision", "tiny"},
    AdminCmdPinned = {},
    KnownSecrets = {},
    KnownSecretRarities = {},
    NewSecretsBatch = {},
}
local function _deepCopy(t) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local o = {}
    for k, v in pairs(t) do o[k] = (type(v) == "table") and _deepCopy(v) or v end
    return o
end
_G.JAF_ConfigDefaults = _deepCopy(DefaultConfig)
local Config = DefaultConfig
if isfile and isfile(FileName) then
    pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local ok, decoded = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return HttpService:JSONDecode(readfile(FileName)) end)
        if not ok then return end
        for k, v in pairs(DefaultConfig) do if decoded[k] == nil then decoded[k] = v end end
        for _, sub in ipairs({"TpSettings", "Positions"}) do
            if decoded[sub] then for k, v in pairs(DefaultConfig[sub]) do if decoded[sub][k] == nil then decoded[sub][k] = v end end end
        end
        for _, k in ipairs({"Blacklist", "AdminAllowedCmds", "AdminCmdPinned", "KnownSecrets"}) do
            if type(decoded[k]) ~= "table" then decoded[k] = {} end
        end
        if type(decoded.AdminCmdOrder) ~= "table" or #decoded.AdminCmdOrder == 0 then
            local _o = {}
            for _, c in ipairs(DefaultConfig.AdminCmdOrder) do _o[#_o + 1] = c end
            decoded.AdminCmdOrder = _o
        end
        Config = decoded
    end) end
Config.ProximityAP = false
if not Config._CloneDelayMigrated then
    Config._CloneDelayMigrated = true
    if Config.TpSettings and tonumber(Config.TpSettings.CloneHold) then
        Config.TpSettings.CloneDelay = tonumber(Config.TpSettings.CloneHold)
        Config.TpSettings.CloneHold  = nil
    end end
Config.StealRadius = nil
if not Config._F2StealBelowDefaultOn then
    Config._F2StealBelowDefaultOn = true
    if Config.TpSettings then Config.TpSettings.F2StealBelow = true end
end
Config.OptimizeFloorSmooth = true
Config.OptimizeStripMeshTextures = true
if _G.JAF_NoCarpetScan == true then Config.CarpetAutoBuy = false end
if _G.JAF_FastBuyPublic == nil then _G.JAF_FastBuyPublic = true end
_G.JAF_FastBuyAllowed = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return true end
if not (isfile and isfile(FileName)) then
    local vpH = 1080
    pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local cam = Workspace.CurrentCamera
        local h = cam and cam.ViewportSize and cam.ViewportSize.Y
        if not h or h < 200 then local res = GuiService.GetScreenResolution and GuiService:GetScreenResolution() h = res and res.Y end
        if h and h >= 200 then vpH = h end
    end)
    local autoScale = 0.9 * (vpH / 1080)
    Config.UIScale = math.floor(math.clamp(autoScale, 0.9, 1.5) * 20 + 0.5) / 20
end
_G.JAF_FFlagList = {
    {"DFFlagBrowserTrackerIdTelemetryEnabled","False"},{"DFFlagPreloadAsyncSupportTexturePack","True"},{"DFFlagTextureQualityOverrideEnabled","True"},
    {"DFFlagVideoCaptureServiceEnabled","False"},{"DFFlagSampleAndRefreshRakPing","True"},{"DFFlagRakNetUseSlidingWindow4","True"},
    {"DFFlagCoreScriptTelemetry2","False"},{"DFFlagEnableSoundPreloading","True"},{"DFFlagOptimizePartsInPart","True"},
    {"DFFlagDisableDPIScale","True"},{"DFFlagDebugPerfMode","True"},{"DFIntRaknetBandwidthInfluxHundredthsPercentageV2","10000"},
    {"DFIntRakNetClockDriftAdjustmentPerPingMillisecond","100"},{"DFIntPerformanceControlTextureQualityBestUtility","-1"},{"DFIntSignalRHubConnectionHeartbeatTimerRateMs","1000"},
    {"DFIntMaxReceiveToDeserializeLatencyMilliseconds","15"},{"DFIntMegaReplicatorNetworkQualityProcessorUnit","10"},{"DFIntNetworkInDeserializeLimitGameplayMsClient","6"},
    {"DFIntSignalRHubConnectionBaseRetryTimeMs","100"},{"DFIntClientPacketHealthyAllocationPercent","20"},{"DFIntTelemetryProfilerHundredthsPercentage","0"},
    {"DFIntMaxWaitTimeBeforeForcePacketProcessMS","1"},{"DFIntNetworkInProcessLimitGameplayMsClient","6"},{"DFIntAnimationLodFacsVisibilityDenominator","0"},
    {"DFIntRaknetBandwidthPingSendEveryXSeconds","1"},{"DFIntSignalRCoreKeepAlivePingPeriodMs","250"},{"DFIntClientPacketMaxFrameMicroseconds","200"},
    {"DFIntSoundServiceCacheCleanupMaxAgeDays","2"},{"DFIntMaxProcessPacketsStepsPerCyclic","5000"},{"DFIntClientPacketExcessMicroseconds","1000"},
    {"DFIntMaxProcessPacketsStepsAccumulated","0"},{"DFIntWaitOnUpdateNetworkLoopEndedMS","100"},{"DFIntLargePacketQueueSizeCutoffMB","1000"},
    {"DFIntMaxProcessPacketsJobScaling","10000"},{"DFIntRakNetNakResendDelayRttPercent","50"},{"DFIntSignalRCoreServerTimeoutMs","11100"},
    {"DFIntDebugFRMQualityLevelOverride","1"},{"DFIntClientPacketMinMicroseconds","1"},{"DFIntAnimationLodFacsDistanceMin","0"},
    {"DFIntAnimationLodFacsDistanceMax","0"},{"DFIntWaitOnRecvFromLoopEndedMS","100"},{"DFIntRakNetNakResendDelayMsMax","100"},
    {"DFIntCodecMaxOutgoingFrames","10000"},{"DFIntSignalRCoreRpcQueueSize","256"},{"DFIntRakNetMinAckGrowthPercent","0"},
    {"DFIntTaskSchedulerTargetFps","9999"},{"DFIntCodecMaxIncomingPackets","100"},{"DFIntRakNetMtuValue3InBytes","1200"},
    {"DFIntRakNetMtuValue1InBytes","1280"},{"DFIntRakNetMtuValue2InBytes","1240"},{"DFIntRakNetNakResendDelayMs","10"},
    {"DFIntRakNetResendRttMultiple","1"},{"DFIntMemCacheMaxCapacityMB","48"},{"DFIntClientPacketMaxDelayMs","11"},
    {"DFIntTextureQualityOverride","0"},{"DFIntRakNetSelectTimeoutMs","1"},{"DFIntSignalRCoreTimerMs","750"},
    {"DFIntConnectionMTUSize","1260"},{"DFIntMaxFrameBufferSize","4"},{"DFIntRakNetLoopMs","1"},
    {"DFIntDebugRestrictGCDistance","1"},{"FIntTaskSchedulerAsyncTasksMinimumThreadCount","2"},{"FIntRenderMaxShadowAtlasUsageBeforeDownscale","80"},
    {"FIntPerformanceTelemetryQueueProcessLimit","0"},{"FIntRenderShadowMapDepthCacheMemLimit","192"},{"FIntUITextureMaxRenderTextureSize","1024"},
    {"FIntRakNetResendBufferArrayLength","128"},{"FIntTaskSchedulerAutoThreadLimit","6"},{"FIntTerrainOTAMaxTextureSize","1024"},
    {"FIntOcclusionWorkerThreadCount","5"},{"FIntTaskSchedulerMaxNumOfJobs","86"},{"FIntTelemetryProfilerFrequency","0"},
    {"FIntRenderLocalLightFadeInMs","0"},{"FIntDefaultMeshCacheSizeMB","256"},{"FIntReportDeviceInfoRollout","0"},
    {"FIntTaskSchedulerThreadMin","1"},{"FIntRobloxGuiBlurIntensity","0"},{"FIntTerrainArraySliceSize","0"},
    {"FIntDebugForceMSAASamples","1"},{"FIntRenderShadowmapBias","0"},{"FIntFRMMaxGrassDistance","0"},
    {"FIntFRMMinGrassDistance","0"},{"FIntGrassMovementReducedMotionFactor","0"},{"FIntDebugTextureManagerSkipMips","7"},
    {"RenderUseTextureManager224","False"},{"TextureQualityOverride","1"},{"TextureQualityOverrideEnabled","True"},
    {"PerformanceControlTextureQualityBestUtility","1"},{"FFlagRenderAllocateShadowMapResourcesOnDemand","True"},{"FFlagSpecifyNetworkReplicatorScopeForItems","True"},
    {"FFlagTaskSchedulerLimitTargetFpsTo2402","False"},{"FFlagHandleAltEnterFullscreenManually","False"},{"FFlagGameBasicSettingsFramerateCap5","False"},
    {"FFlagSpecifyNetworkReplicatorScope","True"},{"FFlagSendRenderFidelityTelemetry2","False"},{"FFlagRenderGpuTextureCompressor","True"},
    {"FFlagBaseThreadPoolUseRuntime2","True"},{"FFlagCacheTextBoundsInGuiText","True"},{"FFlagEnableTelemetryService1","False"},
    {"FFlagDebugGraphicsPreferD3D11","True"},{"FFlagPerfDataOnTelemetryV2","False"},{"FFlagOpenTelemetryEnabled2","False"},
    {"FFlagRbxStorageUseMemCache","True"},{"FFlagDebugForceGenerateHSR","True"},{"FFlagRenderInitShadowmaps","True"},
    {"FFlagFastGPULightCulling3","True"},{"FFlagDebugSkyGray","True"},{"FFlagDebugRenderingSetDeterministic","True"},
    {"FLogNetwork","7"},
}
_G.JAF_ApplyFFlags = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local fn = rawget(getfenv and getfenv(0) or _G, "setfflag") or rawget(_G, "setfflag") or setfflag
    if type(fn) ~= "function" then return false end
    local n = 0
    for _, f in ipairs(_G.JAF_FFlagList) do if pcall(fn, f[1], f[2]) then n = n + 1 end end
    return n > 0
end
if Config.FFlagsEnabled then pcall(_G.JAF_ApplyFFlags) end
Config.AutoTPPriority = true
Config.UsePriority = true
if type(Config.MutationPriorityList) ~= "table" then Config.MutationPriorityList = {} end
if Config.PriorityLastTab    ~= "mutation" then Config.PriorityLastTab    = "brainrot" end
if Config.PrioritySkipPicker ~= true       then Config.PrioritySkipPicker = false end
if Config.MutationPriorityEnabled ~= false then Config.MutationPriorityEnabled = true end
Config.PriorityMode, Config.MutationFilterStrict = nil, nil
BlacklistedPlayers    = BlacklistedPlayers    or Config.Blacklist or {}
addToBlacklist        = addToBlacklist        or function() end
removeFromBlacklist   = removeFromBlacklist   or function() end
isBlacklisted         = isBlacklisted         or function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return false end
canUseAdminAction     = canUseAdminAction     or function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return true end
refreshBlacklistUI    = refreshBlacklistUI    or function() end
isAtlasUser           = isAtlasUser           or function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return false end
local function SaveConfig() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if writefile then
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local toSave = {}
            for k, v in pairs(Config) do toSave[k] = v end
            toSave.ProximityAP = false
            writefile(FileName, HttpService:JSONEncode(toSave))
        end) end end
_G.JAF_SaveConfig = SaveConfig
do
    local savePending = false
    function _G.JAF_SaveConfigSoon() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if savePending then return end
        savePending = true
        task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
savePending = false; SaveConfig() end)
    end end
_G.JAF_PositionDefaults = {
    AdminPanel     = {X = 0.1859375, Y = 0.5767123526556385},
    WalkingBestBar = {X = 0.5,  Y = 0.96},
    GriefDetector  = {X = 0.01, Y = 0.55},
    GriefHistory   = {X = 0.30, Y = 0.18},
}
function _G.JAF_ResetGuiPositions() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local moved = 0
    local targets = _G.JAF_DragTargets or {}
    Config.Positions = Config.Positions or {}
    for key, def in pairs(_G.JAF_PositionDefaults) do
        Config.Positions[key] = {X = def.X, Y = def.Y}
        local f = targets[key]
        if f and f.Parent then f.Position = UDim2.new(def.X, 0, def.Y, 0) moved = moved + 1 end
    end
    Config.JustAFanSettingsPos = nil
    local sg = hubFind("JustAFanSettings")
    local mp = sg and sg:FindFirstChild("MainPanel")
    if mp then mp.Position = UDim2.new(0, 60, 0.5, -280); moved = moved + 1 end
    Config.MiniUIPos = {X = 0.01, Y = 0.35}
    local mg = hubFind("JustAFanMiniActions")
    local mn = mg and mg:FindFirstChild("MiniPanel")
    if mn then mn.Position = UDim2.new(0.01, 0, 0.35, 0); moved = moved + 1 end
    Config.MobileBtnPos = nil
    local bg = hubFind("JustAFanMobileBar")
    local lb = bg and bg:FindFirstChild("Launcher")
    if lb then
        local inset = 10
        if type(_G.JAF_TopInset) == "function" then
            local ok, v = pcall(_G.JAF_TopInset); if ok and tonumber(v) then inset = tonumber(v) + 10 end
        end
        lb.Position = UDim2.new(0, 14, 0, inset); moved = moved + 1
    end
    SaveConfig()
    return moved
end
function _G.JAF_ResetSettingsToDefault() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local defs = _G.JAF_ConfigDefaults
    if type(defs) ~= "table" then return false end
    for k in pairs(Config) do Config[k] = nil end
    for k, v in pairs(defs) do Config[k] = (type(v) == "table") and _deepCopy(v) or v end
    _P(Config, "ProximityAP", false, "StealRadius", nil, "AutoTPPriority", true, "UsePriority", true, "OptimizeFloorSmooth", true, "OptimizeStripMeshTextures", true)
    Config._CloneDelayMigrated = true
    Config._F2StealBelowDefaultOn = true
    SaveConfig()
    pcall(_G.JAF_ResetGuiPositions)
    return true
end
_G.InvisStealAngle = Config.InvisStealAngle
_G.SinkSliderValue = Config.SinkSliderValue
_G.AutoRecoverLagback = Config.AutoRecoverLagback
_G.AutoInvisDuringSteal = Config.AutoInvisDuringSteal
_G.JAF_FpsOptimizer = Config.FpsOptimizer ~= false
_G.JAF_RemoveAnims = Config.RemoveAnimations ~= false
_G.JAF_OptimizeFloorSmooth       = _G.JAF_FpsOptimizer and Config.OptimizeFloorSmooth ~= false
_G.JAF_OptimizeStripMeshTextures = _G.JAF_FpsOptimizer and Config.OptimizeStripMeshTextures == true
_G.JAF_InvisDelayEnabled = Config.InvisDelayEnabled == true
_G.JAF_InvisDelayMs = tonumber(Config.InvisDelayMs) or 0
_G.JAF_F1Mode = (Config.TpSettings and Config.TpSettings.F1SpotMode) or "auto"
_G.JAF_F2Mode = (Config.TpSettings and Config.TpSettings.F2SpotMode) or "auto"
if Config.TpSettings and Config.TpSettings._WaitStableFpsDefaultOn == nil then
    Config.TpSettings.WaitStableFps = true
    Config.TpSettings._WaitStableFpsDefaultOn = true
end
_G.JAF_WaitStableFps   = (Config.TpSettings and Config.TpSettings.WaitStableFps) == true
_G.JAF_FpsStableThresh = (Config.TpSettings and Config.TpSettings.FpsStableThresh) or 60
if _G.JAF_LoadTpFpsWait == nil then _G.JAF_LoadTpFpsWait = 1.5 end
do
    local invisKey = nil
    if type(Config.InvisToggleKey) == "string" and Config.InvisToggleKey ~= "" then
        local ok, kc = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Enum.KeyCode[Config.InvisToggleKey] end)
        if ok and kc then invisKey = kc end
    end
    _G.INVISIBLE_STEAL_KEY = invisKey
end
_G.invisibleStealEnabled = false
_G.RecoveryInProgress = false
local function kickPlayer() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local ok = pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if game.Shutdown then game:Shutdown() else LocalPlayer:Kick("\nJUSTAFAN HUB") end
    end)
    if not ok then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
LocalPlayer:Kick("\nJUSTAFAN HUB") end) end
end
_G.JAF_KickPlayer = kickPlayer
local function _jafParsePSInput(input) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if type(input) ~= "string" then return nil end
    input = input:gsub("%s+", "")
    if input == "" then return nil end
    local placeId = tonumber(input:match("/games/(%d+)"))
    local direct = input:match("[?&]privateServerLinkCode=([%w%-_]+)") or input:match("[?&]linkCode=([%w%-_]+)")
    if direct then return direct, placeId, false end
    if input:match("[?&]code=([%w%-_]+)") then return nil, placeId, true end
    if input:match("^[%w%-_]+$") then
        local looksShare = (#input == 32) and (input:match("^[0-9a-f]+$") ~= nil)
        if looksShare then return nil, placeId, true end
        return input, placeId, false
    end
    return nil
end
_G.JAF_ParsePSInput = _jafParsePSInput
_G.JAF_TeleportToPrivateServer = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local linkCode, placeId, shareWarn = _jafParsePSInput(Config.PrivateServerCode)
    if shareWarn then return false, "that's a SHARE code, not a linkCode" end
    if not linkCode then return false, "no valid private-server link code saved" end
    placeId = placeId or game.PlaceId
    local ok = pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        game:GetService("ExperienceService"):LaunchExperience({ placeId = placeId, linkCode = linkCode })
    end)
    if ok then return true end
    local ok2, err = pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        TeleportService:TeleportToPrivateServer(placeId, linkCode, { LocalPlayer })
    end)
    if ok2 then return true end
    return false, tostring(err)
end
_G.JAF_KickToPrivateServer = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false)) return _G.JAF_TeleportToPrivateServer() end
local function triggerClosestUnlock(yLevel, maxY) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not SAB_LIVE() then return end
    local character = LocalPlayer.Character
    local hrp = character and character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local playerY = yLevel or hrp.Position.Y
    local Y_THRESHOLD = 5
    local bestPromptSameLevel = nil
    local shortestDistSameLevel = math.huge
    local bestPromptFallback = nil
    local shortestDistFallback = math.huge
    local plots = Workspace:FindFirstChild("Plots")
    if not plots then return end
    local hrpPos = hrp.Position
    for _, obj in ipairs(plots:GetDescendants()) do
        if obj:IsA("ProximityPrompt") and obj.Enabled then
            local part = obj.Parent
            if part and part:IsA("BasePart") then
                local partPos = part.Position
                if maxY and partPos.Y > maxY then
                else
                    local distance = (hrpPos - partPos).Magnitude
                    local yDifference = math.abs(playerY - partPos.Y)
                    if distance < shortestDistFallback then shortestDistFallback = distance bestPromptFallback = obj end
                    if yDifference <= Y_THRESHOLD then
                        if distance < shortestDistSameLevel then shortestDistSameLevel = distance bestPromptSameLevel = obj end
                    end end end end end
    local targetPrompt = bestPromptSameLevel or bestPromptFallback
    if targetPrompt then
        if fireproximityprompt then
            fireproximityprompt(targetPrompt)
        else
            targetPrompt:InputBegan(Enum.UserInputType.MouseButton1)
            task.wait(0.05)
            targetPrompt:InputEnded(Enum.UserInputType.MouseButton1)
        end end end
local Theme = {
    Background      = Color3.fromRGB(18, 18, 22),
    Surface         = Color3.fromRGB(28, 28, 34),
    SurfaceHighlight= Color3.fromRGB(48, 48, 56),
    Accent1         = Color3.fromRGB(235, 235, 245),
    Accent2         = Color3.fromRGB(170, 170, 185),
    TextPrimary     = Color3.fromRGB(245, 245, 248),
    TextSecondary   = Color3.fromRGB(185, 185, 195),
    Success         = Color3.fromRGB(235, 235, 245),
    Error           = Color3.fromRGB(210, 90, 90),
}
function _G.JAF_OnAccent(c) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    c = c or Theme.Accent1
    local lum = c.R * 0.299 + c.G * 0.587 + c.B * 0.114
    return lum > 0.55 and Color3.fromRGB(15, 15, 18) or Color3.fromRGB(245, 245, 250)
end
function _G.JAF_Rgb(c) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    c = c or Theme.TextPrimary
    return ("rgb(%d,%d,%d)"):format( math.floor(c.R * 255 + 0.5), math.floor(c.G * 255 + 0.5), math.floor(c.B * 255 + 0.5))
end
local _THEME_FIELDS = {"Background","Surface","SurfaceHighlight","Accent1","Accent2","TextPrimary","TextSecondary","Success","Error"}
THEMES = {}
do
    local _PAL = {
        cyan     = {8,18,28, 10,25,40, 15,45,65, 50,200,255, 0,150,210, 240,240,240, 170,210,230, 50,200,255, 210,90,90},
        midnight = {10,12,24, 15,18,36, 26,30,60, 125,135,255, 85,95,220, 238,240,250, 180,188,225, 125,135,255, 210,90,90},
        ice      = {10,18,26, 14,26,38, 22,44,62, 150,220,255, 95,180,225, 240,248,252, 180,210,228, 150,220,255, 210,90,90},
        rose     = {26,10,16, 38,14,24, 64,22,40, 255,120,160, 215,75,120, 250,240,244, 230,180,200, 255,120,160, 255,95,95},
        ocean    = {6,14,26, 8,22,42, 14,40,72, 60,130,255, 30,90,210, 238,242,250, 175,195,230, 60,130,255, 210,90,90},
        white    = {18,18,22, 28,28,34, 48,48,56, 235,235,245, 170,170,185, 245,245,248, 185,185,195, 235,235,245, 210,90,90},
        dawg     = {20,13,11, 34,19,17, 60,26,24, 196,42,38, 142,24,22, 246,236,232, 226,184,174, 196,42,38, 255,95,95},
        custom   = {18,8,38, 25,10,55, 45,15,85, 180,50,255, 130,0,220, 240,240,240, 200,170,230, 180,50,255, 210,90,90},
    }
    for name, rgb in pairs(_PAL) do
        local t = {}
        for i = 1, #_THEME_FIELDS do local b = (i - 1) * 3 t[_THEME_FIELDS[i]] = Color3.fromRGB(rgb[b + 1], rgb[b + 2], rgb[b + 3]) end
        THEMES[name] = t
    end end
DAWG_IMAGE_URL   = "https://raw.githubusercontent.com/Lebonbon3-5/StealABrainrot/refs/heads/main/dawg.png"
DAWG_IMAGE_ASSET = "rbxassetid://85709371910705"
_G.JAF_DawgResolved = _G.JAF_DawgResolved or nil
_G.JAF_DawgActive   = _G.JAF_DawgActive or false
function resolveDawgImage() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if _G.JAF_DawgResolved then return _G.JAF_DawgResolved end
    if type(DAWG_IMAGE_URL) == "string" and DAWG_IMAGE_URL ~= "" and jafDownloadWebImage then
        local asset = jafDownloadWebImage(DAWG_IMAGE_URL)
        if asset then _G.JAF_DawgResolved = asset; return asset end
    end
    if type(DAWG_IMAGE_ASSET) == "string" and DAWG_IMAGE_ASSET ~= "" then _G.JAF_DawgResolved = DAWG_IMAGE_ASSET return DAWG_IMAGE_ASSET end
    return nil
end
function buildCustomThemeFromColor(accent) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if typeof(accent) ~= "Color3" then return false end
    local r, g, b = accent.R * 255, accent.G * 255, accent.B * 255
    local function toward(c, t, amt) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return c + (t - c) * amt end
    local hex = tostring(Config.CustomThemeHex or "FFC800"):gsub("#", ""):gsub("%s", "")
    local cr = tonumber(hex:sub(1, 2), 16) or 255
    local cg = tonumber(hex:sub(3, 4), 16) or 200
    local cb = tonumber(hex:sub(5, 6), 16) or 0
    local clum = 0.299 * cr + 0.587 * cg + 0.114 * cb
    if clum < 120 then
        local lift = (120 - clum) / 120 * 0.7
        cr, cg, cb = toward(cr, 150, lift), toward(cg, 150, lift), toward(cb, 160, lift)
    end
    cr, cg, cb = math.clamp(cr, 0, 255), math.clamp(cg, 0, 255), math.clamp(cb, 0, 255)
    local accentBright = Color3.fromRGB(cr, cg, cb)
    local accent2 = Color3.fromRGB(math.floor(cr * 0.72), math.floor(cg * 0.72), math.floor(cb * 0.72))
    local function tint(f) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Color3.fromRGB(
        math.clamp(math.floor(r * f) + 6, 0, 255),
        math.clamp(math.floor(g * f) + 4, 0, 255),
        math.clamp(math.floor(b * f) + 10, 0, 255)) end
    THEMES.custom = {
        Background       = tint(0.06),
        Surface          = tint(0.10),
        SurfaceHighlight = tint(0.20),
        Accent1          = accentBright,
        Accent2          = accent2,
        TextPrimary      = Color3.fromRGB(240, 240, 240),
        TextSecondary    = Color3.fromRGB(
            math.clamp(math.floor(170 + r * 0.28), 0, 255),
            math.clamp(math.floor(170 + g * 0.28), 0, 255),
            math.clamp(math.floor(180 + b * 0.24), 0, 255)),
        Success          = accentBright,
        Error            = Color3.fromRGB(210, 90, 90),
    }
    return true
end
function buildCustomThemeFromHex(hex) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if type(hex) ~= "string" then return false end
    hex = hex:gsub("#", ""):gsub("%s", "")
    if #hex ~= 6 then return false end
    local r = tonumber(hex:sub(1, 2), 16)
    local g = tonumber(hex:sub(3, 4), 16)
    local b = tonumber(hex:sub(5, 6), 16)
    if not (r and g and b) then return false end
    return buildCustomThemeFromColor(Color3.fromRGB(r, g, b))
end
pcall(buildCustomThemeFromHex, Config.CustomThemeHex)
local AssetService = game:GetService("AssetService")
_G.JAF_ThemeImageAsset = _G.JAF_ThemeImageAsset or nil
local function jafHttpRequest(url) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local req = (syn and syn.request) or (http and http.request) or http_request or request or fluxus and fluxus.request
    if not req then return nil end
    local ok, res = pcall(req, { Url = url, Method = "GET" })
    if ok and res and (res.Body or res.body) then return res.Body or res.body end
    return nil
end
local function jafGetCustomAsset(fileName) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local getter = getcustomasset or getsynasset or (syn and syn.getcustomasset)
    if not getter then return nil end
    local ok, asset = pcall(getter, fileName)
    if ok and asset then return asset end
    return nil
end
local JAF_MEDIA_DIR = "SammyDawg_theme"
local function jafEnsureMediaDir() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if makefolder and (not isfolder or not isfolder(JAF_MEDIA_DIR)) then makefolder(JAF_MEDIA_DIR) end
    end)
    return JAF_MEDIA_DIR
end
jafJpegAverageColor = function(data) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if type(data) ~= "string" or #data < 4 then return nil, "not image data" end
    local byte = string.byte
    if byte(data, 1) ~= 0xFF or byte(data, 2) ~= 0xD8 then return nil, "not a JPEG" end
    local _dlen = #data
    local function u16(p) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return byte(data, p) * 256 + byte(data, p + 1) end
    local _jrsh, _jband = bit32.rshift, bit32.band
    local quant = {}
    local huffDC, huffAC = {}, {}
    local comps = {}
    local restartInterval = 0
    local W, H = 0, 0
    local pos = 3
    local progressive = false
    local function buildHuff(counts, vals) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local t, code, k = {}, 0, 1
        for L = 1, 16 do t[L] = {} for _ = 1, counts[L] do t[L][code] = vals[k]; k = k + 1; code = code + 1 end code = code * 2 end
        return t
    end
    while pos < _dlen do
        if byte(data, pos) ~= 0xFF then pos = pos + 1
        else
            while byte(data, pos) == 0xFF do pos = pos + 1 end
            local marker = byte(data, pos); pos = pos + 1
            if marker == 0xD9 then break
            elseif marker == 0xC0 or marker == 0xC1 or marker == 0xC2 then
                progressive = (marker == 0xC2)
                local len = u16(pos)
                H = u16(pos + 3); W = u16(pos + 5)
                local nf = byte(data, pos + 7)
                local p = pos + 8
                for i = 1, nf do
                    local samp = byte(data, p + 1)
                    comps[i] = { id = byte(data, p), h = _jrsh(samp, 4), v = _jband(samp, 15), tq = byte(data, p + 2) }
                    p = p + 3
                end
                pos = pos + len
            elseif marker == 0xC3 or (marker >= 0xC5 and marker <= 0xCF and marker ~= 0xC8) then
                return nil, "unsupported JPEG (marker " .. string.format("%02X", marker) .. ")"
            elseif marker == 0xC4 then
                local endp = pos + u16(pos); local p = pos + 2
                while p < endp do
                    local tcth = byte(data, p); p = p + 1
                    local counts, total = {}, 0
                    for i = 1, 16 do counts[i] = byte(data, p); total = total + counts[i]; p = p + 1 end
                    local vals = {}
                    for i = 1, total do vals[i] = byte(data, p); p = p + 1 end
                    local built = buildHuff(counts, vals)
                    if _jrsh(tcth, 4) == 0 then huffDC[_jband(tcth, 15)] = built else huffAC[_jband(tcth, 15)] = built end
                end
                pos = endp
            elseif marker == 0xDB then
                local endp = pos + u16(pos); local p = pos + 2
                while p < endp do
                    local pqtq = byte(data, p); p = p + 1
                    local pq, tbl = _jrsh(pqtq, 4), {}
                    for i = 1, 64 do if pq == 0 then tbl[i] = byte(data, p); p = p + 1 else tbl[i] = u16(p); p = p + 2 end end
                    quant[_jband(pqtq, 15)] = tbl
                end
                pos = endp
            elseif marker == 0xDD then
                local len = u16(pos); restartInterval = u16(pos + 2); pos = pos + len
            elseif marker == 0xDA then
                local ns = byte(data, pos + 2)
                local p = pos + 3
                local scan = {}
                for i = 1, ns do
                    local cs = byte(data, p); local tdta = byte(data, p + 1); p = p + 2
                    local td, ta = _jrsh(tdta, 4), _jband(tdta, 15)
                    for _, c in ipairs(comps) do
                        if c.id == cs then
                            c.dc = huffDC[td]; c.ac = huffAC[ta]
                            c.pred, c.sum, c.cnt = 0, 0, 0
                            scan[i] = c
                        end end end
                local Ss = byte(data, p); local Ah_Al = byte(data, p + 2)
                local Ah, Al = _jrsh(Ah_Al, 4), _jband(Ah_Al, 15)
                pos = p + 3
                if progressive and not (Ss == 0 and Ah == 0) then return nil, "progressive JPEG (DC scan not first)" end
                local alScale = 2 ^ Al
                local bitBuf, bitCnt = 0, 0
                local function nextBit() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    if bitCnt == 0 then
                        if pos > _dlen then return 0 end
                        local b = byte(data, pos); pos = pos + 1
                        if b == 0xFF then local nb = byte(data, pos) if nb == 0x00 then pos = pos + 1 else pos = pos - 1; return 0 end end
                        bitBuf = b; bitCnt = 8
                    end
                    bitCnt = bitCnt - 1
                    return _jband(_jrsh(bitBuf, bitCnt), 1)
                end
                local function receive(s) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    local v = 0
                    for _ = 1, s do v = v * 2 + nextBit() end
                    return v
                end
                local function extend(v, s) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    if s == 0 then return 0 end
                    if v < 2 ^ (s - 1) then return v - 2 ^ s + 1 end
                    return v
                end
                local function huffDecode(tbl) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    local code = 0
                    for L = 1, 16 do
                        code = code * 2 + nextBit()
                        local row = tbl[L]
                        if row then local val = row[code]; if val ~= nil then return val end end
                    end
                    return nil
                end
                local function decodeBlock(c) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    local t = huffDecode(c.dc); if t == nil then return false end
                    c.pred = c.pred + extend(receive(t), t)
                    c.sum = c.sum + c.pred * alScale; c.cnt = c.cnt + 1
                    if progressive then return true end
                    local k = 1
                    while k < 64 do
                        local rs = huffDecode(c.ac); if rs == nil then break end
                        local r, s = _jrsh(rs, 4), _jband(rs, 15)
                        if s == 0 then
                            if r == 15 then k = k + 16 else break end
                        else
                            k = k + r; if k > 63 then break end
                            receive(s); k = k + 1
                        end end
                    return true
                end
                local hmax, vmax = 1, 1
                for _, c in ipairs(comps) do hmax = math.max(hmax, c.h); vmax = math.max(vmax, c.v) end
                local total = math.ceil(W / (8 * hmax)) * math.ceil(H / (8 * vmax))
                local since = 0
                for m = 1, total do
                    for _, c in ipairs(scan) do for _ = 1, c.v do for _ = 1, c.h do decodeBlock(c) end end end
                    since = since + 1
                    if restartInterval > 0 and since == restartInterval and m < total then
                        since = 0; bitCnt = 0
                        while pos <= _dlen and byte(data, pos) ~= 0xFF do pos = pos + 1 end
                        while pos <= _dlen and byte(data, pos) == 0xFF do pos = pos + 1 end
                        pos = pos + 1
                        for _, c in ipairs(scan) do c.pred = 0 end
                    end end
                break
            else
                local len = u16(pos); pos = pos + len
            end end end
    local function avgVal(c) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not c or c.cnt == 0 then return 128 end
        local qdc = (quant[c.tq] and quant[c.tq][1]) or 1
        return (c.sum / c.cnt) * qdc / 8 + 128
    end
    local function clamp(x) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return math.max(0, math.min(255, math.floor(x + 0.5))) end
    if #comps == 0 then return nil, "no frame" end
    local Y = avgVal(comps[1])
    if #comps == 1 then local gg = clamp(Y); return gg, gg, gg end
    local Cb, Cr = avgVal(comps[2]), avgVal(comps[3])
    return clamp(Y + 1.402 * (Cr - 128)), clamp(Y - 0.344136 * (Cb - 128) - 0.714136 * (Cr - 128)), clamp(Y + 1.772 * (Cb - 128))
end
jafDownloadWebImage = function(url) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if type(url) ~= "string" then return nil, "no image input" end
    _G.JAF_LastImageBytes = nil
    _G.JAF_GifUrl = nil
    local idNum = url:match("^%s*(%d+)%s*$") or url:match("rbxassetid://(%d+)") or url:match("rbxasset://(%d+)")
    if idNum then return "rbxassetid://" .. idNum end
    if isfile and isfile(url) then
        local asset = jafGetCustomAsset(url)
        if asset then if readfile then local okR, b = pcall(readfile, url); if okR then _G.JAF_LastImageBytes = b end end return asset end
        return nil, "found the file but executor has no getcustomasset"
    end
    if not writefile then return nil, "executor has no writefile" end
    local b64 = url:match("^data:image/[%w%+%.%-]*;base64,(.+)$")
    if b64 then
        b64 = b64:gsub("%s", "")
        local dec = (crypt and crypt.base64decode)
                 or (crypt and crypt.base64 and crypt.base64.decode)
                 or (syn and syn.crypt and syn.crypt.base64decode)
        if not dec then return nil, "executor has no base64 decoder" end
        local okD, bin = pcall(dec, b64)
        if not okD or type(bin) ~= "string" or #bin < 64 then return nil, "base64 decode failed/empty" end
        _G.JAF_LastImageBytes = bin
        local ext = (url:match("^data:image/([%w]+)") or "png"):lower()
        if ext == "jpeg" then ext = "jpg" end
        if ext ~= "png" and ext ~= "jpg" and ext ~= "tga" and ext ~= "bmp" then ext = "png" end
        local fname = jafEnsureMediaDir() .. "/JustAFanThemeBG." .. ext
        if not pcall(writefile, fname, bin) then return nil, "writefile failed" end
        local asset = jafGetCustomAsset(fname)
        if asset then return asset end
        return nil, "executor has no getcustomasset"
    end
    if not url:match("^https?://") then return nil, "paste a direct http(s) image link, a data:image base64 string, or an asset id" end
    local mediaUrl = url
    local host = url:match("^https?://([^/]+)") or ""
    if host:find("tenor%.com") then
        local page = jafHttpRequest(url)
        if page then
            local og = page:match('property="og:image"[^>]-content="([^"]+)"') or page:match('content="([^"]+)"[^>]-property="og:image"')
            if og then mediaUrl = (og:gsub("\\u002[fF]", "/")) end
        end end
    local hostPath = mediaUrl:gsub("^https?://", "")
    if mediaUrl:lower():find("%.gif") then _G.JAF_GifUrl = mediaUrl end
    local function urlEnc(s) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return (s:gsub("[^%w%-%._~/]", function(ch) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return string.format("%%%02X", string.byte(ch)) end)) end
    local function imgExt(b) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if type(b) ~= "string" or #b < 64 then return nil end
        local a, c = b:byte(1), b:byte(2)
        if a == 0x89 and c == 0x50 then return "png" end
        if a == 0xFF and c == 0xD8 then return "jpg" end
        return nil
    end
    local candidates = {
        "https://images.weserv.nl/?url=" .. urlEnc(hostPath) .. "&w=512&output=png",
        "https://i0.wp.com/" .. hostPath .. "?w=512",
        mediaUrl,
    }
    local lastReason = "couldn't fetch/convert that image link"
    for _, u in ipairs(candidates) do
        local body = jafHttpRequest(u)
        local ext = imgExt(body)
        if ext then
            _G.JAF_LastImageBytes = body
            local fname = jafEnsureMediaDir() .. "/JustAFanThemeBG." .. ext
            if not pcall(writefile, fname, body) then return nil, "writefile failed" end
            local asset = jafGetCustomAsset(fname)
            if asset then return asset end
            return nil, "executor has no getcustomasset"
        elseif body and #body > 0 then
            lastReason = "image host/proxy didn't return a usable image (it may be blocked)"
        end end
    return nil, lastReason
end
jafImageAverageColor = function(assetUri) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local ok, col = pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local img
        local okA = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
img = AssetService:CreateEditableImageAsync(Content.fromUri(assetUri)) end)
        if not okA or not img then img = AssetService:CreateEditableImageAsync(assetUri) end
        if not img then return nil end
        local size = img.Size
        local getPix
        local pixels = 0
        local okBuf, buf = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return img:ReadPixelsBuffer(Vector2.new(0, 0), size) end)
        if okBuf and buf and buffer and buffer.len and buffer.len(buf) >= 4 then
            pixels = math.floor(buffer.len(buf) / 4)
            getPix = function(idx0) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local o = idx0 * 4
                return buffer.readu8(buf, o) / 255, buffer.readu8(buf, o + 1) / 255,
                       buffer.readu8(buf, o + 2) / 255, buffer.readu8(buf, o + 3) / 255
            end
        else
            local okPx, px = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return img:ReadPixels(Vector2.new(0, 0), size) end)
            if not okPx or type(px) ~= "table" or #px < 4 then return nil end
            pixels = math.floor(#px / 4)
            getPix = function(idx0) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local i = idx0 * 4 + 1
                return px[i], px[i + 1], px[i + 2], px[i + 3]
            end end
        if pixels <= 0 then return nil end
        local stride = math.max(1, math.floor(pixels / 1500))
        local r, g, b, n = 0, 0, 0, 0
        local rf, gf, bf, nf = 0, 0, 0, 0
        for p = 0, pixels - 1, stride do
            local pr, pg, pb, pa = getPix(p)
            pr = pr or 0; pg = pg or 0; pb = pb or 0
            rf = rf + pr; gf = gf + pg; bf = bf + pb; nf = nf + 1
            local sum = pr + pg + pb
            if (pa == nil or pa > 0.5) and sum > 0.15 and sum < 2.85 then
                r = r + pr; g = g + pg; b = b + pb; n = n + 1
            end end
        if n >= 8 then return Color3.new(r / n, g / n, b / n) end
        if nf > 0 then return Color3.new(rf / nf, gf / nf, bf / nf) end
        return nil
    end)
    if ok and col then return col end
    return nil
end
local function jafSetPanelImage(frame, assetUri) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not frame or not frame:IsA("GuiObject") then return end
    local existing = frame:FindFirstChild("JAFThemeBG")
    if assetUri == nil then if existing then existing:Destroy() end return end
    local img = existing
    if not img then
        img = Instance.new("ImageLabel")
        _P(img, "Name", "JAFThemeBG", "BackgroundTransparency", 1, "BorderSizePixel", 0, "ZIndex", 0, "ScaleType", Enum.ScaleType.Crop)
        local cor = frame:FindFirstChildOfClass("UICorner")
        local c = Instance.new("UICorner", img)
        c.CornerRadius = cor and cor.CornerRadius or UDim.new(0, 10)
        img.Parent = frame
    end
    _P(img, "Size", UDim2.new(1, 0, 1, 0), "Position", UDim2.new(0, 0, 0, 0), "Image", assetUri, "ImageTransparency", 0.05)
    img.ImageColor3 = Color3.fromRGB(200, 200, 200)
    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
frame.ClipsDescendants = true end)
end
local JAF_IMAGE_PANELS = {
    "JustAFanSettings", "AutoStealUI", "JustAFanMiniActions",
    "XiAdminPanel", "JustAFanStatusHUD", "XiStealingHUD",
    "JustAFanWalkingBestBar", }
local JAF_GIF_ANIM_PANELS = JAF_IMAGE_PANELS
local function jafSetPanelTextStroke(frame, active) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local BLACK = Color3.fromRGB(0, 0, 0)
    for _, d in ipairs(frame:GetDescendants()) do
        if d:IsA("TextLabel") or d:IsA("TextButton") or d:IsA("TextBox") then
            if active and d.BackgroundTransparency >= 1 then
                if d.TextStrokeTransparency ~= 0.4 then d.TextStrokeTransparency = 0.4 end
                d.TextStrokeColor3 = BLACK
            else
                d.TextStrokeTransparency = 1
            end end end end
function jafApplyThemeImageToPanels(assetUri) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not assetUri and jafStopGifAnimation then jafStopGifAnimation() end
    local _yield = _G.JAF_ThemePaintYield ~= false
    for _, gn in ipairs(JAF_IMAGE_PANELS) do
        local sg = hubFind(gn)
        if sg then
            for _, frame in ipairs(sg:GetChildren()) do
                if frame:IsA("Frame") then
                    if assetUri then frame.BackgroundTransparency = math.max(frame.BackgroundTransparency, 0.55) end
                    pcall(jafSetPanelImage, frame, assetUri)
                    pcall(jafSetPanelTextStroke, frame, assetUri ~= nil)
                end end
            if _yield then RunService.Heartbeat:Wait() end
        end end end
_G.JAF_HubStart = _G.JAF_HubStart or tick()
if _G.JAF_HeavyReady == nil then _G.JAF_HeavyReady = false end
function _G.JAF_LoadHeavyNow() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G.JAF_HeavyReady = true end
if not _G.JAF_CacheFillWatch then
    _G.JAF_CacheFillWatch = true
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local _emptyScans = 0
        local _lastScanned, _scanStallAt = -1, tick()
        while not _G.JAF_CacheFilledAt do
            local _scanned = (_G.JAF_PlotsTotal or 0) > 0 and (_G.JAF_PlotsScanned or 0) >= (_G.JAF_PlotsTotal or 0)
            local _hasCache = SharedState.AllAnimalsCache and #SharedState.AllAnimalsCache > 0
            local _ps = _G.JAF_PlotsScanned or 0
            if _hasCache and not _G.JAF_FirstBrainrotAt then _G.JAF_FirstBrainrotAt = tick() end
            if _ps ~= _lastScanned then _lastScanned = _ps; _scanStallAt = tick() end
            if _scanned and _hasCache then
                _G.JAF_CacheFilledAt = tick()
                break
            elseif _scanned then
                _emptyScans = _emptyScans + 1
                local _pullDone = (_G.JAF_PullOnJoin == false) or (_G.JAF_PullMs ~= nil)
                    or ((tick() - (_G.JAF_HubStart or 0)) > (tonumber(_G.JAF_PullGrace) or 4))
                if _emptyScans >= 3 and _pullDone then _G.JAF_CacheFilledAt = tick() break end
            elseif _hasCache and (tick() - _scanStallAt) > (_G.JAF_ScanStallTime or 3) then
                _G.JAF_CacheFilledAt = tick()
                break
            else
                _emptyScans = 0
            end
            task.wait(0.1)
        end end) end
function _G.JAF_WaitHeavy() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local _wasReady = _G.JAF_HeavyReady
    local hub = _G.JAF_HubStart or tick()
    while not _G.JAF_BootReady and (tick() - hub) < (_G.JAF_HeavyAbsCap or 12) do task.wait(0.05) end
    while not _G.JAF_HeavyReady and not _G.JAF_CacheFilledAt and (tick() - hub) < (_G.JAF_HeavyAbsCap or 12) do task.wait(0.1) end
    local s = _G.JAF_CacheFilledAt or hub
    while not _G.JAF_HeavyReady and (tick() - s) < (_G.JAF_HeavyMaxWait or 3) do task.wait(0.1) end
    do
        local _grace = tonumber(_G.JAF_HeavyMinGrace) or 1.5
        local _good = 0
        while (tick() - s) < _grace do
            local dt = task.wait(0.05)
            if type(dt) == "number" and dt < 0.025 then
                _good = _good + 1
                if _good >= 2 and _G.JAF_HeavyReady then break end
            else
                _good = 0
            end end end
    _G.JAF_HeavyReady = true
    if not _G.JAF_HeavyNoStagger and not _wasReady then
        local _n = (_G.JAF_HeavyReleaseSeq or 0) + 1
        _G.JAF_HeavyReleaseSeq = _n
        local _spacing = tonumber(_G.JAF_HeavyStaggerFrames) or 3
        local _frames  = math.min((_n - 1) * _spacing, tonumber(_G.JAF_HeavyStaggerCap) or 60)
        for _ = 1, _frames do task.wait() end
    end end
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local w = tick()
    local FALLBACK = _G.JAF_HeavyFallback or 3
    local ENDCAP   = _G.JAF_HeavyTpEndCap or 14
    while not _G.JAF_TpActive and not _G.JAF_HeavyReady and not _G.JAF_CacheFilledAt and (tick() - w) < (_G.JAF_HeavyAbsCap or 12) do task.wait(0.05) end
    local base = _G.JAF_CacheFilledAt or w
    while not _G.JAF_TpActive and not _G.JAF_HeavyReady and (tick() - base) < FALLBACK do task.wait(0.05) end
    if _G.JAF_TpActive and not _G.JAF_HeavyReady then
        local doneFor = 0
        while not _G.JAF_HeavyReady and (tick() - base) < ENDCAP do
            if _G.JAF_TpActive then doneFor = 0 else doneFor = doneFor + 1 end
            if doneFor >= 6 then break end
            task.wait(0.05)
        end
        if not _G.JAF_HeavyReady then task.wait(_G.JAF_HeavyPostTp or 0.25) end
    end
    _G.JAF_HeavyReady = true
end)
function jafStopGifAnimation() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    _G.JAF_GifToken = (_G.JAF_GifToken or 0) + 1
    _G.JAF_GifFrames = nil
    _G.JAF_GifDelays = nil
end
jafParseGifFrames = function(data) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if type(data) ~= "string" or data:sub(1, 3) ~= "GIF" then return 0, {} end
    local byte = string.byte
    local p = 7
    local packed = byte(data, p + 4)
    p = p + 7
    if packed and packed >= 128 then p = p + 3 * (2 ^ ((packed % 8) + 1)) end
    local frames, delays, lastDelay = 0, {}, 0
    while p <= #data do
        local b = byte(data, p)
        if not b or b == 0x3B then break
        elseif b == 0x21 then
            local label = byte(data, p + 1); p = p + 2
            if label == 0xF9 then lastDelay = (byte(data, p + 2) or 0) + (byte(data, p + 3) or 0) * 256 end
            while true do local bs = byte(data, p); if not bs then break end; p = p + 1 + bs; if bs == 0 then break end end
        elseif b == 0x2C then
            frames = frames + 1
            delays[frames] = (lastDelay >= 2 and lastDelay or 10) / 100
            lastDelay = 0
            local lp = byte(data, p + 9); p = p + 10
            if lp and lp >= 128 then p = p + 3 * (2 ^ ((lp % 8) + 1)) end
            p = p + 1
            while true do local bs = byte(data, p); if not bs then break end; p = p + 1 + bs; if bs == 0 then break end end
        else break end
    end
    return frames, delays
end
local function jafStartGifLoop() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    _G.JAF_GifToken = (_G.JAF_GifToken or 0) + 1
    local myToken = _G.JAF_GifToken
    local i, acc = 1, 0
    local conn
    conn = game:GetService("RunService").Heartbeat:Connect(function(dt) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _G.JAF_GifToken ~= myToken then conn:Disconnect(); return end
        if _G.JAF_TpActive then return end
        local frames = _G.JAF_GifFrames
        local delays = _G.JAF_GifDelays
        if not frames or #frames < 1 then return end
        acc = acc + dt
        local need = (delays and delays[i]) or 0.08
        if acc < need then return end
        acc = acc - need
        if acc > 0.4 then acc = 0 end
        i = (i % #frames) + 1
        local asset = frames[i]
        _G.JAF_ThemeImageAsset = asset
        for _, gn in ipairs(JAF_GIF_ANIM_PANELS) do
            local sg = hubFind(gn)
            if sg then
                for _, fr in ipairs(sg:GetChildren()) do
                    if fr:IsA("Frame") then local bg = fr:FindFirstChild("JAFThemeBG") if bg then bg.Image = asset end end
                end end end end) end
jafSetupGifAnimation = function(gifUrl) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if type(gifUrl) ~= "string" then return false end
    do local g = tick() while tick() - g < 16 do if game:IsLoaded() and _G.JAF_FirstLoadTpDone then break end task.wait(0.25) end end
    local req = (syn and syn.request) or (http and http.request) or http_request or request
    if not req then return false end
    local function get(u) LPH_ATTRIBUTES(ERROR_HANDLING(false))
local ok, r = pcall(req, { Url = u, Method = "GET" }); if ok and r then return r.Body or r.body end end
    local gif = get(gifUrl)
    if not gif or gif:sub(1, 3) ~= "GIF" then return false end
    local fcount, delays = jafParseGifFrames(gif)
    if fcount < 2 then return false end
    local CAP = 90
    local idxs = {}
    if fcount <= CAP then for i = 0, fcount - 1 do idxs[#idxs + 1] = i end
    else local step = fcount / CAP; for i = 0, CAP - 1 do idxs[#idxs + 1] = math.floor(i * step) end end
    local frameDelays = {}
    for j = 1, #idxs do
        local upper = idxs[j + 1] or fcount
        local d = 0
        for di = idxs[j] + 1, upper do d = d + (delays[di] or 0.1) end
        frameDelays[j] = math.max(d, 0.03)
    end
    local host = gifUrl:gsub("^https?://", "")
    local enc = (host:gsub("[^%w%-%._~/]", function(ch) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return string.format("%%%02X", string.byte(ch)) end))
    jafEnsureMediaDir()
    local frames, done, nextK, active = {}, 0, 0, 0
    local CONC = 8
    local function pump() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        while active < CONC and nextK < #idxs do
            nextK = nextK + 1; active = active + 1
            local k = nextK
            local pageIdx = idxs[k]
            task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                local body = get("https://images.weserv.nl/?url=" .. enc .. "&w=130&output=png&page=" .. pageIdx)
                if body and body:byte(1) == 0x89 then
                    local fn = JAF_MEDIA_DIR .. "/JAFgiff_" .. k .. ".png"
                    if pcall(writefile, fn, body) then frames[k] = jafGetCustomAsset(fn) end
                end
                active = active - 1; done = done + 1
                pump()
            end) end end
    pump()
    local function rebuild() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local s, sd = {}, {}
        for k = 1, #idxs do if frames[k] then s[#s + 1] = frames[k]; sd[#sd + 1] = frameDelays[k] end end
        return s, sd
    end
    local t0 = tick()
    local timeout = math.max(20, #idxs * 0.7)
    while done < #idxs and tick() - t0 < timeout do task.wait(0.1) end
    local seq, seqD = rebuild()
    if #seq < 2 then return false end
    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
game:GetService("ContentProvider"):PreloadAsync(seq) end)
    _G.JAF_GifFrames = seq; _G.JAF_GifDelays = seqD
    jafStartGifLoop()
    return true
end
function jafApplyImageTheme(url) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if jafStopGifAnimation then jafStopGifAnimation() end
    local asset, reason = jafDownloadWebImage(url)
    if not asset then return false, reason or "download failed" end
    _G.JAF_ThemeImageAsset = asset
    local avg = jafImageAverageColor(asset)
    local displayable = (avg ~= nil)
    if not avg and _G.JAF_LastImageBytes and jafJpegAverageColor then
        local r, g, b = jafJpegAverageColor(_G.JAF_LastImageBytes)
        if r then avg = Color3.fromRGB(r, g, b) end
    end
    if displayable then
        task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(jafApplyThemeImageToPanels, asset) end)
    else
        _G.JAF_ThemeImageAsset = nil
        task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(jafApplyThemeImageToPanels, nil) end)
    end
    if not avg then return false, "loaded the image but couldn't read its colour (try a smaller / non-corrupt image)" end
    buildCustomThemeFromColor(avg)
    if applyTheme then applyTheme("custom") end
    if displayable and _G.JAF_GifUrl and jafSetupGifAnimation and Config.GifAnimate ~= false then
        task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(jafSetupGifAnimation, _G.JAF_GifUrl) end)
    end
    return true
end
if not Config.DefaultWhiteMigrated then
    Config.DefaultWhiteMigrated = true
    if Config.CurrentTheme == nil or Config.CurrentTheme == "preto" then Config.CurrentTheme = "white" end
    pcall(SaveConfig)
end
do
    local removedRemap = { red = "rose", orange = "dawg", green = "ocean", gold = "white", preto = "midnight", pink = "rose" }
    local remapped = removedRemap[Config.CurrentTheme]
    if remapped then Config.CurrentTheme = remapped pcall(SaveConfig) end
end
if Config.CurrentTheme and THEMES[Config.CurrentTheme] then for k, v in pairs(THEMES[Config.CurrentTheme]) do Theme[k] = v end end
JAF_STYLE = { PanelCorner        = 10, PanelBgTransparency = 0, StrokeThickness    = 1, StrokeTransparency = 0.4, }
local JAF_STYLE_TARGETS = {
    {"XiAdminPanel"},
    {"AutoStealUI"},
    {"JustAFanSettings", "MainPanel"},
    {"JustAFanStatusHUD", "Main"},
    {"JustAFanMiniActions", "MiniPanel"},
    {"PriorityListGUI"},
}
local function jafStyleOnePanel(frame) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not frame or not frame:IsA("GuiObject") then return end
    local cor = frame:FindFirstChildOfClass("UICorner")
    if not cor then cor = Instance.new("UICorner", frame) end
    cor.CornerRadius = UDim.new(0, JAF_STYLE.PanelCorner)
    if frame.BackgroundTransparency < 1 and not (_G.JAF_ThemeImageAsset and frame:FindFirstChild("JAFThemeBG")) then
        frame.BackgroundColor3 = Theme.Background
        frame.BackgroundTransparency = JAF_STYLE.PanelBgTransparency
    end
    local stroke = frame:FindFirstChildOfClass("UIStroke")
    if not stroke then stroke = Instance.new("UIStroke", frame) end
    local g = stroke:FindFirstChildOfClass("UIGradient")
    if g then g:Destroy() end
    _P(stroke, "Color", Theme.Accent1, "Thickness", JAF_STYLE.StrokeThickness, "Transparency", JAF_STYLE.StrokeTransparency)
end
function jafUnifyPanelStyle() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    for _, t in ipairs(JAF_STYLE_TARGETS) do
        local sg = hubFind(t[1])
        if sg then
            local frame = t[2] and sg:FindFirstChild(t[2]) or nil
            if not frame then frame = sg:FindFirstChildWhichIsA("Frame") end
            if frame then pcall(jafStyleOnePanel, frame) end
        end end end
applyTheme = function(themeName) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local t = THEMES[themeName]
    if not t then return end
    local colorMap = {}
    for k, oldColor in pairs(Theme) do if t[k] then colorMap[oldColor] = t[k] end end
    for k, v in pairs(t) do Theme[k] = v end
    Config.CurrentTheme = themeName
    SaveConfig()
    local function matchColor(c1, c2) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not c1 or not c2 then return false end
        local dr = math.abs(c1.R - c2.R)
        local dg = math.abs(c1.G - c2.G)
        local db = math.abs(c1.B - c2.B)
        return (dr + dg + db) < 0.04
    end
    local function remapColor(c) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not c then return c end
        for oldC, newC in pairs(colorMap) do if matchColor(c, oldC) then return newC end end
        return c
    end
    local function remapSeq(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local changed = false
        local newKps = {}
        for _, kp in ipairs(obj.Color.Keypoints) do
            local nc = remapColor(kp.Value)
            if nc ~= kp.Value then changed = true end
            table.insert(newKps, ColorSequenceKeypoint.new(kp.Time, nc))
        end
        if changed then obj.Color = ColorSequence.new(newKps) end
    end
    local function remapObj(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local isBtn, isBox, isScroll = obj:IsA("TextButton"), obj:IsA("TextBox"), obj:IsA("ScrollingFrame")
        if isBtn or isBox or isScroll or obj:IsA("Frame") or obj:IsA("ImageLabel") then
            if obj.BackgroundTransparency < 1 then obj.BackgroundColor3 = remapColor(obj.BackgroundColor3) end
        end
        if isBtn or isBox or obj:IsA("TextLabel") then
            obj.TextColor3 = remapColor(obj.TextColor3)
            if obj.BackgroundTransparency < 1 and matchColor(obj.BackgroundColor3, Theme.Accent1) then
                obj.TextColor3 = _G.JAF_OnAccent(Theme.Accent1)
            end end
        if isScroll then obj.ScrollBarImageColor3 = remapColor(obj.ScrollBarImageColor3) end
        if obj:IsA("UIStroke") then obj.Color = remapColor(obj.Color) end
        if obj:IsA("UIGradient") or obj:IsA("Beam") then remapSeq(obj) end
    end
    local guiNames = {
        "AutoStealUI", "XiAdminPanel", "JustAFanStatusHUD", "PriorityListGUI",
        "JustAFanSettings", "JustAFanGrabKick", "XiStealingHUD", "JustAFanWalkingBestBar", "XiGriefDetector", "XiGriefHistory", "JustAFanMiniActions"
    }
    for _, guiName in ipairs(guiNames) do
        local sg = hubFind(guiName)
        if sg then
            for _, obj in ipairs(sg:GetDescendants()) do pcall(remapObj, obj) end
            pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local root = sg:FindFirstChildWhichIsA("Frame")
                if root and root.BackgroundTransparency < 1 then root.BackgroundColor3 = remapColor(root.BackgroundColor3) end
            end) end end
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local savedTab  = (_G.JustAFanSettingsUI and _G.JustAFanSettingsUI.currentTab) or "cfg"
        local wasVis    = _G.JustAFanSettingsUI and _G.JustAFanSettingsUI.panel and _G.JustAFanSettingsUI.panel.Visible
        if buildJustAFanSettingsUI then buildJustAFanSettingsUI() end
        task.wait()
        if _G.JustAFanSettingsUI then
            if _G.JustAFanSettingsUI.switchTab then _G.JustAFanSettingsUI.switchTab(savedTab) end
            if wasVis and _G.JustAFanSettingsUI.panel then _G.JustAFanSettingsUI.panel.Visible = true end
        end
        if _G.jafRebuildStatusHUD then _G.jafRebuildStatusHUD() end
        if jafUnifyPanelStyle then task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(jafUnifyPanelStyle) end) end
        if themeName == "dawg" then
            _G.JAF_DawgActive = true
            if jafApplyThemeImageToPanels then
                task.defer(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    local asset = resolveDawgImage()
                    if asset and asset ~= "" then _G.JAF_ThemeImageAsset = asset pcall(jafApplyThemeImageToPanels, asset) end
                end) end
        elseif _G.JAF_DawgActive and not (Config.ThemeImageUrl and Config.ThemeImageUrl ~= "") then
            _G.JAF_DawgActive = false
            _G.JAF_ThemeImageAsset = nil
            if jafApplyThemeImageToPanels then task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(jafApplyThemeImageToPanels, nil) end) end
        end
        if buildJustAFanMiniActionsUI and (_G.JAF_HeavyReady or (_G.JustAFanMiniActionsUI and _G.JustAFanMiniActionsUI.panel)) then
            local miniWasVis = _G.JustAFanMiniActionsUI and _G.JustAFanMiniActionsUI.panel and _G.JustAFanMiniActionsUI.panel.Visible
            buildJustAFanMiniActionsUI()
            task.wait()
            if miniWasVis and _G.JustAFanMiniActionsUI and _G.JustAFanMiniActionsUI.panel then _G.JustAFanMiniActionsUI.panel.Visible = true end
        end
        if _G.JAF_ThemeImageAsset and jafApplyThemeImageToPanels then
            task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(jafApplyThemeImageToPanels, _G.JAF_ThemeImageAsset) end)
        end end) end
local PRIORITY_LIST = {"Headless Horseman","Signore Carapace","Elefanto Frigo","Arcadragon","Strawberry Elephant","Antonio","John Pork","Meowl","Love Love Bear","Skibidi Toilet","Ginger Gerat","Griffin","Dragon Gingerini","Fishino Clownino","La Supreme Combinasion","Digi Narwhal","Dragon Cannelloni","Hydra Dragon Cannelloni","Ketupat Bros","La Casa Boo","Hydra Bunny","Duggy Bros","Bunny and Eggy","Cerberus","Celestial Pegasus","Rosey and Teddy","Reinito Sleighito","Capitano Moby","Los Sekolahs","Fragrama and Chocrama","Spooky and Pumpky","Cooki and Milki","La Food Combinasion","Los Amigos","Burguro And Fryuro","Popcuru and Fizzuru","Garama and Madundung","La Secret Combinasion","La Romantic Grande","La Taco Combinasion","Los Spaghettis","Swaggy Bros","Sammyni Fattini","Festive 67","Ketchuru and Musturu","Tang Tang Keletang","Ketupat Kepat","Tictac Sahur","Tralaledon","W or L","Eviledon","Lavadorito Spinito","Spaghetti Tualetti","Foxini Lanternini"}
do local saved = Config and Config.PriorityList if saved and type(saved) == "table" and #saved > 0 then PRIORITY_LIST = saved end end
local MUTATION_LIST = {}
local saveMutationPriorityToConfig
do
    local saved = Config and Config.MutationPriorityList
    if type(saved) == "table" then
        for _, v in ipairs(saved) do if type(v) == "string" and v ~= "" then MUTATION_LIST[#MUTATION_LIST + 1] = v end end
    end
    local MUT_ROSTER = {}
    do
        local okM, MD = pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            return require(game:GetService("ReplicatedStorage"):WaitForChild("Datas"):WaitForChild("Mutations"))
        end)
        if okM and type(MD) == "table" then
            for mName, d in pairs(MD) do
                if type(mName) == "string" and mName ~= "" then
                    MUT_ROSTER[#MUT_ROSTER + 1] = { name = mName, mod = (type(d) == "table" and tonumber(d.Modifier)) or 0 }
                end end end
        local hasNone = false
        for _, m in ipairs(MUT_ROSTER) do if m.name == "None" then hasNone = true break end end
        if not hasNone then MUT_ROSTER[#MUT_ROSTER + 1] = { name = "None", mod = 0 } end
        table.sort(MUT_ROSTER, function(a, b) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if a.mod ~= b.mod then return a.mod > b.mod end
            return a.name < b.name
        end)
        _G.JAF_MutRoster = MUT_ROSTER
        if #MUT_ROSTER > 0 then
            local inRoster, seen, merged = {}, {}, {}
            for _, m in ipairs(MUT_ROSTER) do inRoster[m.name] = true end
            for _, v in ipairs(MUTATION_LIST) do if inRoster[v] and not seen[v] then seen[v] = true; merged[#merged + 1] = v end end
            for _, m in ipairs(MUT_ROSTER) do if not seen[m.name] then seen[m.name] = true; merged[#merged + 1] = m.name end end
            table.clear(MUTATION_LIST)
            for i, v in ipairs(merged) do MUTATION_LIST[i] = v end
        end end
    _G.JAF_NormMut = (function(m) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if m == nil then return nil end
        m = tostring(m)
        if m == "" then return nil end
        return (m:gsub("%s+", "")):lower()
    end)
    local UNRANKED, RANK_MUL = 999, 1000
    local _nameRank, _mutRank = {}, {}
    local _dirty, _haveMut = true, false
    local _lastN, _lastM = -1, -1
    local _rebuild; _rebuild = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        table.clear(_nameRank); table.clear(_mutRank)
        for i, v in ipairs(PRIORITY_LIST) do
            if i >= UNRANKED then break end
            local k = tostring(v):lower()
            if _nameRank[k] == nil then _nameRank[k] = i end
        end
        for i, v in ipairs(MUTATION_LIST) do
            if i >= UNRANKED then break end
            local k = _G.JAF_NormMut(v)
            if k and _mutRank[k] == nil then _mutRank[k] = i end
        end
        _haveMut = (next(_mutRank) ~= nil) and (Config.MutationPriorityEnabled ~= false)
        _dirty = false
        _lastN, _lastM = #PRIORITY_LIST, #MUTATION_LIST
    end
    _G.JAF_PrioDirty = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_dirty = true end
    _G.JAF_PrioScore = (function(entryOrName, mutation) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _dirty or _lastN ~= #PRIORITY_LIST or _lastM ~= #MUTATION_LIST then _rebuild() end
        local nm, mu
        if type(entryOrName) == "table" then
            nm = entryOrName.nameLower or (entryOrName.name and tostring(entryOrName.name):lower())
            if not entryOrName.isWalking then mu = entryOrName.mutLower or entryOrName.mutation end
        else
            nm = entryOrName and tostring(entryOrName):lower()
            mu = mutation
        end
        local nr = (nm and _nameRank[nm]) or UNRANKED
        if nr == UNRANKED then return UNRANKED * RANK_MUL + UNRANKED end
        local mr = UNRANKED
        if _haveMut and mu ~= nil then
            mr = _mutRank[mu]
            if mr == nil then local k = _G.JAF_NormMut(mu) mr = (k and _mutRank[k]) or UNRANKED end
        end
        return nr * RANK_MUL + mr
    end)
    saveMutationPriorityToConfig = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        Config.MutationPriorityList = {}
        for i, v in ipairs(MUTATION_LIST) do Config.MutationPriorityList[i] = v end
        _G.JAF_PrioDirty()
        SaveConfig()
    end
    _G.JAF_MutList = MUTATION_LIST
end
local function savePriorityToConfig() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    Config.PriorityList = {}
    for i, v in ipairs(PRIORITY_LIST) do Config.PriorityList[i] = v end
    _G.JAF_PrioDirty()
    SaveConfig()
end
local CreateGradient; CreateGradient = function(parent) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local g = Instance.new("UIGradient", parent)
    g.Color = ColorSequence.new{ ColorSequenceKeypoint.new(0, Theme.Accent2), ColorSequenceKeypoint.new(1, Theme.Accent2) }
    g.Rotation = 45
    return g
end
LOCK_EXEMPT_GUIS = { JustAFanSettings        = true, PriorityListGUI         = true, }
function _isLockExempt(inst) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not inst then return false end
    local sg = inst:FindFirstAncestorWhichIsA("ScreenGui")
    return sg ~= nil and LOCK_EXEMPT_GUIS[sg.Name] == true
end
function _dragLocked(target) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false)) return Config.UILocked and not _isLockExempt(target) end
function _G.JAF_OnInputEnd(io, fn) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not io then return end
    local conn, done
    conn = io.Changed:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if done or io.UserInputState ~= Enum.UserInputState.End then return end
        done = true
        if conn then conn:Disconnect(); conn = nil end
        fn()
    end)
    return conn
end
local MakeDraggable; MakeDraggable = function(handle, target, saveKey) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if saveKey then _G.JAF_DragTargets = _G.JAF_DragTargets or {} _G.JAF_DragTargets[saveKey] = target end
    local dragging, dragInput, dragStart, startPos
    handle.InputBegan:Connect(function(input) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _dragLocked(target) then return end
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = target.Position
            _G.JAF_OnInputEnd(input, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                dragging = false
                if saveKey then
                    if not target or not target.Parent then return end
                    local parentSize = target.Parent.AbsoluteSize
                    Config.Positions[saveKey] = {
                        X = (target.AbsolutePosition.X + target.AbsoluteSize.X * target.AnchorPoint.X) / parentSize.X,
                        Y = (target.AbsolutePosition.Y + target.AbsoluteSize.Y * target.AnchorPoint.Y) / parentSize.Y,
                    }
                    _G.JAF_SaveConfigSoon()
                end end) end end)
    handle.InputChanged:Connect((function(input) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end end))
    local _uisDragConn
    _uisDragConn = UserInputService.InputChanged:Connect((function(input) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not target or not target.Parent then if _uisDragConn then _uisDragConn:Disconnect(); _uisDragConn = nil end return end
        if input == dragInput and dragging then
            _G.JAF_DragBusy = tick()
            local delta = input.Position - dragStart
            target.Position = UDim2.new( startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y )
        end end)) end
local attachResizeGrip; attachResizeGrip = function(panelFrame, guiName, minFrac) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not panelFrame or not guiName then return end
    if panelFrame:FindFirstChild("JAFResizeGrip") then return end
    local GRIP = 18
    local grip = Instance.new("Frame")
    grip.Name = "JAFResizeGrip"
    grip.AnchorPoint = Vector2.new(1, 1)
    grip.Position = UDim2.new(1, -2, 1, -2)
    grip.Size = UDim2.new(0, GRIP, 0, GRIP)
    grip.BackgroundColor3 = Theme.Accent1
    grip.BackgroundTransparency = 0.7
    _P(grip, "BorderSizePixel", 0, "ZIndex", 50, "Active", true, "Parent", panelFrame)
    Instance.new("UICorner", grip).CornerRadius = UDim.new(0, 5)
    local gStroke = Instance.new("UIStroke", grip)
    _P(gStroke, "Color", Theme.Accent1, "Thickness", 1.5, "Transparency", 0.2)
    for i = 1, 3 do
        local tick = Instance.new("Frame", grip)
        _P(tick, "BackgroundColor3", _G.JAF_OnAccent(), "BackgroundTransparency", 0.05, "BorderSizePixel", 0, "ZIndex", 52, "AnchorPoint", Vector2.new(0.5, 0.5), "Size", UDim2.new(0, 11 - (i-1)*3, 0, 2), "Rotation", -45, "Position", UDim2.new(1, -5 - (i-1)*4, 1, -5 - (i-1)*4))
    end
    local dragging, startPos, startW, startH, dragInput = false, nil, nil, nil, nil
    local baseW = panelFrame.Size.X.Offset
    local baseH = panelFrame.Size.Y.Offset
    local resizable = (baseW > 40 and baseH > 40)
    local MINF = math.clamp(tonumber(minFrac) or 0.55, 0.3, 1)
    local function clampW(w) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return math.clamp(math.floor(w + 0.5), math.floor(baseW * MINF), math.floor(baseW * 2.2)) end
    local function clampH(h) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return math.clamp(math.floor(h + 0.5), math.floor(baseH * MINF), math.floor(baseH * 2.2)) end
    grip.MouseEnter:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        grip.BackgroundTransparency = 0.35; gStroke.Transparency = 0
        grip.Size = UDim2.new(0, GRIP + 4, 0, GRIP + 4)
    end)
    grip.MouseLeave:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not dragging then grip.BackgroundTransparency = 0.7; gStroke.Transparency = 0.2 grip.Size = UDim2.new(0, GRIP, 0, GRIP) end
    end)
    grip.InputBegan:Connect(function(input) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if Config.UILocked and not LOCK_EXEMPT_GUIS[guiName] then return end
        if not resizable then return end
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            startPos = input.Position
            startW = panelFrame.Size.X.Offset
            startH = panelFrame.Size.Y.Offset
            grip.BackgroundTransparency = 0.2; gStroke.Transparency = 0
            _G.JAF_OnInputEnd(input, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                dragging = false
                grip.BackgroundTransparency = 0.7; gStroke.Transparency = 0.2
                grip.Size = UDim2.new(0, GRIP, 0, GRIP)
                _G.JAF_SaveConfigSoon()
            end) end end)
    grip.InputChanged:Connect(function(input) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end end)
    local _gripUISConn
    _gripUISConn = UserInputService.InputChanged:Connect((function(input) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not grip or not grip.Parent then if _gripUISConn then _gripUISConn:Disconnect(); _gripUISConn = nil end return end
        if input == dragInput and dragging then
            _G.JAF_DragBusy = tick()
            local delta = input.Position - startPos
            local f = panelFrame.AbsoluteSize.X / math.max(1, panelFrame.Size.X.Offset)
            if f <= 0 then f = 1 end
            local w = clampW(startW + delta.X / f)
            local h = clampH(startH + delta.Y / f)
            panelFrame.Size = UDim2.new(panelFrame.Size.X.Scale, w, panelFrame.Size.Y.Scale, h)
            Config.PanelSizes = Config.PanelSizes or {}
            Config.PanelSizes[guiName] = { w = w, h = h }
        end end))
    do
        local sv = Config.PanelSizes and Config.PanelSizes[guiName]
        if resizable and type(sv) == "table" and tonumber(sv.w) and tonumber(sv.h) then
            panelFrame.Size = UDim2.new(panelFrame.Size.X.Scale, clampW(tonumber(sv.w)), panelFrame.Size.Y.Scale, clampH(tonumber(sv.h)))
        end end
    if _G.JAF_ApplyUIScale and ((Config.UIScale or 1) ~= 1 or next(Config.PanelScales or {}) ~= nil) then
        task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(_G.JAF_ApplyUIScale, Config.UIScale or 1) end)
    end end
local JUI = {}
function JUI.new(class, parent, ...) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local o = Instance.new(class)
    if select("#", ...) > 0 then _P(o, ...) end
    if parent ~= nil then o.Parent = parent end
    return o
end
function JUI.corner(inst, r) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0, r or 8); c.Parent = inst
    return c
end
function JUI.round(inst) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1, 0); c.Parent = inst
    return c
end
function JUI.stroke(inst, color, thickness, transparency) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    return JUI.new("UIStroke", inst, "Color", color or Theme.Accent1, "Thickness", thickness or 1, "Transparency", transparency or 0)
end
function JUI.section(parent, lbl, order) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local row = JUI.new("Frame", parent, "Size", UDim2.new(1,0,0,28), "BackgroundTransparency", 1, "LayoutOrder", order)
    JUI.new("Frame", row, "Size", UDim2.new(1,0,0,1), "Position", UDim2.new(0,0,1,-1), "BackgroundColor3", Theme.SurfaceHighlight, "BorderSizePixel", 0)
    JUI.new("TextLabel", row, "Size", UDim2.new(1,0,1,0), "BackgroundTransparency", 1, "Text", lbl, "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", Theme.TextSecondary, "TextXAlignment", Enum.TextXAlignment.Left)
    return row
end
function JUI.note(parent, txt, order) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local row = JUI.new("Frame", parent, "Size", UDim2.new(1,0,0,20), "BackgroundTransparency", 1, "LayoutOrder", order)
    JUI.new("TextLabel", row, "Size", UDim2.new(1,-16,1,0), "Position", UDim2.new(0,14,0,0), "BackgroundTransparency", 1, "Text", txt, "Font", Enum.Font.Gotham, "TextSize", 12, "TextColor3", Theme.TextSecondary, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
    return row
end
local SW_OFF = Color3.fromRGB(140, 140, 155)
function JUI.toggle(parent, lbl, get, set, order, full) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local row = JUI.new("Frame", parent, "Size", full and UDim2.new(1,0,0,44) or UDim2.new(0.5,-4,0,40), "BackgroundColor3", Theme.Surface, "BackgroundTransparency", 0, "BorderSizePixel", 0, "LayoutOrder", order)
    JUI.new("TextLabel", row, "Size", UDim2.new(1,-54,1,0), "Position", UDim2.new(0,12,0,0), "BackgroundTransparency", 1, "Text", lbl, "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", Theme.TextPrimary, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
    local on = get()
    local sw = JUI.new("Frame", row, "Size", UDim2.new(0,40,0,22), "Position", UDim2.new(1,-48,0.5,-11), "BackgroundColor3", on and Theme.Success or SW_OFF)
    local dot = JUI.new("Frame", sw, "Size", UDim2.new(0,15,0,15), "Position", on and UDim2.new(1,-17,0.5,-7.5) or UDim2.new(0,2,0.5,-7.5), "BackgroundColor3", _G.JAF_OnAccent())
    local swb = JUI.new("TextButton", sw, "Size", UDim2.new(1,0,1,0), "BackgroundTransparency", 1, "Text", "", "ZIndex", 4, "AutoButtonColor", false)
    if _G.JAF_MobileOn() then _P(swb, "Parent", row, "Size", UDim2.new(1,0,1,0), "Position", UDim2.new(0,0,0,0), "ZIndex", 6) end
    _G.JAF_OnTap(swb, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if type(set) ~= "function" then return end
        local ns = not get(); set(ns)
        TweenService:Create(dot, TweenInfo.new(0.15), {Position = ns and UDim2.new(1,-17,0.5,-7.5) or UDim2.new(0,2,0.5,-7.5)}):Play()
        TweenService:Create(sw,  TweenInfo.new(0.15), {BackgroundColor3 = ns and Theme.Success or SW_OFF}):Play()
    end)
    return row
end
function JUI.keybind(parent, lbl, get, set, order) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local row = JUI.new("Frame", parent, "Size", UDim2.new(0.5,-4,0,40), "BackgroundColor3", Theme.Surface, "BackgroundTransparency", 0, "BorderSizePixel", 0, "LayoutOrder", order)
    JUI.corner(row, 8)
    local tl = JUI.new("TextLabel", row, "Size", UDim2.new(1,-88,1,0), "Position", UDim2.new(0,12,0,0), "BackgroundTransparency", 1, "Text", lbl, "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", Theme.TextPrimary, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
    local kb = JUI.new("TextButton", row, "Size", UDim2.new(0,72,0,26), "Position", UDim2.new(1,-80,0.5,-13), "BackgroundColor3", Theme.SurfaceHighlight, "Text", get() or "NONE", "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", Theme.Accent1, "AutoButtonColor", false)
    JUI.corner(kb, 6)
    kb.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        kb.Text = "..."; kb.TextColor3 = Theme.TextPrimary
        local con; con = UserInputService.InputBegan:Connect(function(inp) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if inp.UserInputType == Enum.UserInputType.Keyboard then
                set(inp.KeyCode.Name)
                kb.Text = inp.KeyCode.Name
                kb.TextColor3 = Theme.Accent1
                SaveConfig(); con:Disconnect()
            end end) end)
    return row, kb, tl
end
function JUI.keybindX(parent, lbl, get, set, order) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local row, kb, tl = JUI.keybind(parent, lbl, get, set, order)
    if tl then tl.Size = UDim2.new(1,-116,1,0) end
    local xb = JUI.new("TextButton", row, "Size", UDim2.new(0,22,0,26), "Position", UDim2.new(1,-108,0.5,-13), "BackgroundColor3", Theme.SurfaceHighlight, "Text", "X", "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", Theme.Error, "AutoButtonColor", false)
    JUI.corner(xb, 6)
    xb.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        pcall(set, "")
        if kb then local gv = get() kb.Text = (gv == nil or gv == "") and "NONE" or gv kb.TextColor3 = Theme.Accent1 end
        SaveConfig()
    end)
    return row
end
local DANGER_TOOLS = {
    ["Boogie Bomb"] = true,
    ["Medusa's Head"] = true,
    ["Body Swap Potion"] = true,
    ["Laser Cape"] = true,
    ["Rainbowrath Sword"] = true,
    ["Gummy Bear"] = true,
}
_G.JAF_MobileAuto = nil
local function _jafPhoneClass(p) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    return p == Enum.Platform.IOS or p == Enum.Platform.Android
end
local function _jafAutoMobile() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local ok, v = pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if GuiService and GuiService.IsTenFootInterface and GuiService:IsTenFootInterface() then return false end
        if UserInputService.VREnabled then return false end
        if not UserInputService.TouchEnabled then return false end
        local kb, mouse = UserInputService.KeyboardEnabled, UserInputService.MouseEnabled
        if not kb and not mouse then return true end
        local plat = UserInputService.GetPlatform and UserInputService:GetPlatform() or nil
        return _jafPhoneClass(plat) and not (kb and mouse)
    end)
    return (ok and v) and true or false
end
function _G.JAF_MobileOn() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local m = Config.MobileMode
    if m == "on"  then return true  end
    if m == "off" then return false end
    if _G.JAF_MobileAuto == nil then _G.JAF_MobileAuto = _jafAutoMobile() end
    return _G.JAF_MobileAuto
end
_G.JAF_MobileHooks = {}
function _G.JAF_OnMobileChange(fn) LPH_ATTRIBUTES(ERROR_HANDLING(false))
table.insert(_G.JAF_MobileHooks, fn) end
function _G.JAF_MobileRefresh() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false)) for _, fn in ipairs(_G.JAF_MobileHooks) do pcall(fn) end end
if _G.JAF_MobileLastInputConn then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G.JAF_MobileLastInputConn:Disconnect() end) end
pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    _G.JAF_MobileLastInputConn = UserInputService.LastInputTypeChanged:Connect(function(t) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if t ~= Enum.UserInputType.Touch then return end
        if _G.JAF_MobileAuto == true then return end
        if UserInputService.MouseEnabled then return end
        _G.JAF_MobileAuto = true
        if Config.MobileMode == "auto" then task.spawn(_G.JAF_MobileRefresh) end
    end) end)
function _G.JAF_SafeInsets(sg) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
sg.ScreenInsets = Enum.ScreenInsets.DeviceSafeInsets end)
end
function _G.JAF_TopInset() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local ok, y = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
local t = GuiService:GetGuiInset(); return t.Y end)
    return (ok and y and y > 0) and y or 36
end
function _G.JAF_Viewport() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local ok, v = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Workspace.CurrentCamera.ViewportSize end)
    if ok and v and v.X > 0 then return v end
    return Vector2.new(1280, 720)
end
function _G.JAF_PanelScale(frame) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local us = frame and frame:FindFirstChild("JAFUIScale")
    local k = us and tonumber(us.Scale) or 1
    if not k or k <= 0.01 then k = 1 end
    return k
end
function _G.JAF_LockScrolls(inst) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local locked, conn = {}, nil
    local n = inst and inst.Parent
    while n and n ~= game do
        if n:IsA("ScrollingFrame") and n.ScrollingEnabled then
            locked[#locked+1] = { sf = n, cp = n.CanvasPosition }
            n.ScrollingEnabled = false
        end
        n = n.Parent
    end
    if #locked > 0 then
        conn = RunService.RenderStepped:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            for _, e in ipairs(locked) do if e.sf.Parent then e.sf.CanvasPosition = e.cp end end
        end) end
    return function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if conn then conn:Disconnect(); conn = nil end
        for _, e in ipairs(locked) do if e.sf.Parent then e.sf.ScrollingEnabled = true; e.sf.CanvasPosition = e.cp end end
        table.clear(locked)
    end end
_G.JAF_SliderMinH  = 28
_G.JAF_SliderMinUse = 18
_G.JAF_MobileSlider = function(track) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not track or not track.Parent then return end
    local want = _G.JAF_MobileOn()
    local vis  = track:FindFirstChild("JAFTrackVisual")
    if want == (vis ~= nil) then return end
    local row  = track.Parent
    if want then
        local origH = track.Size.Y.Offset
        if origH <= 0 or origH >= _G.JAF_SliderMinH then return end
        local origY = track.Position.Y.Offset
        local rowH = row.Size.Y.Offset
        if rowH <= 0 then rowH = math.floor(row.AbsoluteSize.Y + 0.5) end
        local bandTop, bandBot = 0, rowH
        for _, sib in ipairs(row:GetChildren()) do
            if sib ~= track and sib:IsA("GuiObject") and sib.Visible
               and sib.Size.Y.Scale == 0 and sib.Size.Y.Offset > 0 then
                local sTop = sib.Position.Y.Offset
                local sBot = sTop + sib.Size.Y.Offset
                if sBot <= origY   and sBot > bandTop then bandTop = sBot end
                if sTop >= origY + origH and sTop < bandBot then bandBot = sTop end
            end end
        local band  = math.max(origH, bandBot - bandTop)
        local newH  = math.min(_G.JAF_SliderMinH, band)
        local grow  = math.max(0, math.min(_G.JAF_SliderMinH, _G.JAF_SliderMinUse) - newH)
        if grow > 0 then newH = newH + grow; bandBot = bandBot + grow end
        local centre = origY + origH / 2
        local newY   = math.floor(centre - newH / 2 + 0.5)
        if newY < bandTop then newY = bandTop end
        if newY + newH > bandBot then newY = bandBot - newH end
        vis = Instance.new("Frame")
        vis.Name = "JAFTrackVisual"
        vis.AnchorPoint = Vector2.new(0.5, 0.5)
        _P(vis, "Position", UDim2.new(0.5, 0, 0, (origY - newY) + math.floor(origH / 2 + 0.5)), "Size", UDim2.new(1, 0, 0, origH), "BackgroundColor3", track.BackgroundColor3, "BackgroundTransparency", track.BackgroundTransparency, "BorderSizePixel", 0, "ZIndex", track.ZIndex)
        vis:SetAttribute("JAFOrigH", origH)
        vis:SetAttribute("JAFOrigY", origY)
        for _, ch in ipairs(track:GetChildren()) do ch.Parent = vis end
        vis.Parent = track
        track:SetAttribute("JAFOrigTrans", track.BackgroundTransparency)
        track.BackgroundTransparency = 1
        track.Active = true
        track.Size = UDim2.new(track.Size.X.Scale, track.Size.X.Offset, 0, newH)
        track.Position = UDim2.new(track.Position.X.Scale, track.Position.X.Offset, track.Position.Y.Scale, newY)
        if grow > 0 and row:IsA("Frame") and row.Size.Y.Scale == 0 and row.Size.Y.Offset > 0 then
            row:SetAttribute("JAFRowGrow", grow)
            row.Size = UDim2.new(row.Size.X.Scale, row.Size.X.Offset, 0, row.Size.Y.Offset + grow)
        end
        if not track:GetAttribute("JAFScrollLock") then
            track:SetAttribute("JAFScrollLock", true)
            local unlock
            track.InputBegan:Connect(function(i) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if i.UserInputType ~= Enum.UserInputType.Touch and i.UserInputType ~= Enum.UserInputType.MouseButton1 then return end
                if unlock then unlock() end
                unlock = _G.JAF_LockScrolls(track)
                _G.JAF_OnInputEnd(i, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    if unlock then unlock(); unlock = nil end
                end) end) end
    else
        local origH = vis:GetAttribute("JAFOrigH") or track.Size.Y.Offset
        local origY = vis:GetAttribute("JAFOrigY")
        for _, ch in ipairs(vis:GetChildren()) do ch.Parent = track end
        track.BackgroundTransparency = track:GetAttribute("JAFOrigTrans") or 0
        track.Size = UDim2.new(track.Size.X.Scale, track.Size.X.Offset, 0, origH)
        track.Position = UDim2.new(track.Position.X.Scale, track.Position.X.Offset,
                                   track.Position.Y.Scale, origY or track.Position.Y.Offset)
        local grow = row:GetAttribute("JAFRowGrow")
        if grow then
            row.Size = UDim2.new(row.Size.X.Scale, row.Size.X.Offset, 0, math.max(1, row.Size.Y.Offset - grow))
            row:SetAttribute("JAFRowGrow", nil)
        end
        vis:Destroy()
    end end
function _G.JAF_MobileSliderPass(root) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not root then return end
    for _, d in ipairs(root:GetDescendants()) do
        if d:IsA("Frame") and d.Name ~= "JAFTrackVisual"
           and d.Size.Y.Scale == 0 and d.Size.Y.Offset > 0 and d.Size.Y.Offset <= 10 then
            local fill, knob
            for _, k in ipairs(d:GetChildren()) do
                if k:IsA("Frame") then
                    if k.Size.Y.Scale == 1 then fill = k end
                    if k.AnchorPoint.X == 0.5 and k.AnchorPoint.Y == 0.5 then knob = k end
                end end
            if fill and knob then pcall(_G.JAF_MobileSlider, d) end
        end end end
_G.JAF_BindSlider = function(track, onSet, onDone) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not track then return end
    local active, unlock
    local function stop() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not active then return end
        active = nil
        if unlock then unlock(); unlock = nil end
        if onDone then pcall(onDone) end
    end
    track.Active = true
    track.InputBegan:Connect(function(i) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if active then return end
        if i.UserInputType ~= Enum.UserInputType.MouseButton1 and i.UserInputType ~= Enum.UserInputType.Touch then return end
        active = i
        unlock = _G.JAF_LockScrolls(track)
        pcall(onSet, i.Position.X)
        _G.JAF_OnInputEnd(i, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false)) if active == i then stop() end end)
    end)
    local moveConn, endConn
    moveConn = UserInputService.InputChanged:Connect((function(i) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not track.Parent then
            if moveConn then moveConn:Disconnect(); moveConn = nil end
            if unlock then unlock(); unlock = nil end
            return
        end
        if not active then return end
        if active.UserInputType == Enum.UserInputType.Touch then
            if i ~= active then return end
        elseif i.UserInputType ~= Enum.UserInputType.MouseMovement then
            return
        end
        pcall(onSet, i.Position.X)
    end))
    endConn = UserInputService.InputEnded:Connect(function(i) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not track.Parent then if endConn then endConn:Disconnect(); endConn = nil end return end
        if not active then return end
        if i == active
        or (active.UserInputType ~= Enum.UserInputType.Touch and i.UserInputType == Enum.UserInputType.MouseButton1) then
            stop()
        end end) end
function _G.JAF_OnTap(btn, fn) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not btn then return end
    local pendingTouch = false
    btn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if pendingTouch then pendingTouch = false; return end
        fn()
    end)
    btn.InputBegan:Connect(function(i) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if i.UserInputType ~= Enum.UserInputType.Touch then return end
        pendingTouch = true
        task.delay(1.5, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pendingTouch = false end)
        fn()
    end) end
local function IS_MOBILE() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _G.JAF_MobileOn() end
local function AddMobileMinimizeAdmin(frame, labelText, headerRef) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not IS_MOBILE() or not frame then return end
    local header = headerRef or frame:FindFirstChildWhichIsA("Frame")
    if not header then return end
    local minimizeBtn = Instance.new("TextButton")
    _P(minimizeBtn, "Size", UDim2.new(0, 26, 0, 26), "Position", UDim2.new(1, -30, 0, 6), "BackgroundColor3", Theme.SurfaceHighlight, "Text", "-", "Font", Enum.Font.GothamBold, "TextSize", 18, "TextColor3", Theme.TextPrimary, "AutoButtonColor", false, "Parent", header)
    Instance.new("UICorner", minimizeBtn).CornerRadius = UDim.new(0, 8)
    local guiParent = frame.Parent
    local restoreBtn = Instance.new("TextButton")
    _P(restoreBtn, "Size", UDim2.new(0, 110, 0, 34), "Position", UDim2.new(0, 10, 1, -44), "BackgroundColor3", Theme.SurfaceHighlight, "Text", labelText or "OPEN", "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", Theme.TextPrimary, "Visible", false, "AutoButtonColor", false, "Parent", guiParent)
    Instance.new("UICorner", restoreBtn).CornerRadius = UDim.new(0, 10)
    MakeDraggable(restoreBtn, restoreBtn)
    minimizeBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        frame.Visible = false
        restoreBtn.Visible = true
    end)
    restoreBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        frame.Visible = true
        restoreBtn.Visible = false
    end) end
local function _killDarkOverlay() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local host = (gethui and gethui()) or HubGui
    local ov = host and host:FindFirstChild("JustAFanDarkOverlay")
    if ov then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
ov:Destroy() end) end
end
local function applyDarkBrightness(darkness) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local d = math.clamp(darkness or 0.4, 0, 1)
    Config.DarkBrightness = d
    _killDarkOverlay()
    if not Config.DarkMode then local cc0 = Lighting:FindFirstChild("JustAFanDarkModeCC"); if cc0 then cc0:Destroy() end return end
    local de = d ^ (_G.JAF_DarkGamma or 1.6)
    local cc = Lighting:FindFirstChild("JustAFanDarkModeCC")
    if not cc then
        cc = Instance.new("ColorCorrectionEffect"); cc.Name = "JustAFanDarkModeCC"
        cc.TintColor = Color3.new(1, 1, 1); cc.Parent = Lighting
    end
    cc.Enabled    = true
    cc.Brightness = -de
    cc.Contrast   = -0.05 * de
    cc.Saturation = -0.35 * de
end
local _darkMaintainTok = 0
local _darkRSConn = nil
local function startDarkMaintain() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    _darkMaintainTok = _darkMaintainTok + 1
    local tok = _darkMaintainTok
    if _darkRSConn then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_darkRSConn:Disconnect() end); _darkRSConn = nil end
    local acc = 0
    _darkRSConn = RunService.Heartbeat:Connect(function(dt) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if tok ~= _darkMaintainTok or not Config.DarkMode then
            if _darkRSConn then _darkRSConn:Disconnect(); _darkRSConn = nil end
            return
        end
        acc = acc + (dt or 0)
        if acc < 0.5 then return end
        acc = 0
        local cc = Lighting:FindFirstChild("JustAFanDarkModeCC")
        local de = math.clamp(Config.DarkBrightness or 0.4, 0, 1) ^ (_G.JAF_DarkGamma or 1.6)
        if (not cc) or cc.Enabled ~= true or math.abs(cc.Brightness + de) > 0.001 then applyDarkBrightness(Config.DarkBrightness or 0.4) end
    end) end
local function setDarkMode(enabled) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local on = not not enabled
    Config.DarkMode = on
    SaveConfig()
    _killDarkOverlay()
    if on then
        applyDarkBrightness(Config.DarkBrightness or 0.4)
        startDarkMaintain()
    else
        _darkMaintainTok = _darkMaintainTok + 1
        local cc = Lighting:FindFirstChild("JustAFanDarkModeCC"); if cc then cc:Destroy() end
    end end
_G.JAF_ReassertDark = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not Config.DarkMode then return end
    pcall(applyDarkBrightness, Config.DarkBrightness or 0.4)
    if not _darkRSConn then pcall(startDarkMaintain) end
end
if Config.DarkMode then task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    task.wait(1)
    pcall(setDarkMode, true)
    task.wait(0.5)
    if Config.DarkMode and not _darkRSConn then pcall(startDarkMaintain) end
end) end
local State = { carpetSpeedEnabled = false, infiniteJumpEnabled = Config.TpSettings.InfiniteJump, }
local ProximityAPActive = false
local Connections = { carpetSpeedConnection = nil, infiniteJumpConnection = nil, _ijInputBegan = nil, _ijInputEnded = nil, }
local UI = { }
local carpetSpeedEnabled = State.carpetSpeedEnabled
local function setCarpetSpeed(enabled) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if not SAB_LIVE() then return end
    if enabled and (LocalPlayer:GetAttribute("Stealing") or _G.JAF_TpActive) then enabled = false end
    State.carpetSpeedEnabled = enabled
    carpetSpeedEnabled = State.carpetSpeedEnabled
    _G.JAF_CarpetSpeedActive = enabled
    if Connections.carpetSpeedConnection then Connections.carpetSpeedConnection:Disconnect(); Connections.carpetSpeedConnection = nil end
    if not enabled then return end
    if SharedState.DisableStealSpeed then SharedState.DisableStealSpeed() end
    Connections.carpetSpeedConnection = RunService.Heartbeat:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not SAB_LIVE() then return end
        if _G.JAF_TpActive or LocalPlayer:GetAttribute("Stealing") then setCarpetSpeed(false) return end
        local c = LocalPlayer.Character
        if not c then return end
        local hum = c:FindFirstChild("Humanoid")
        local hrp = c:FindFirstChild("HumanoidRootPart")
        if not hum or not hrp then return end
        local toolName = Config.TpSettings.Tool
        local hasTool = c:FindFirstChild(toolName)
        if not hasTool then
            local bp = LocalPlayer:FindFirstChild("Backpack")
            local tb = bp and bp:FindFirstChild(toolName)
            if tb and not _G.JAF_ResetStowActive then hum:EquipTool(tb); hasTool = tb end
        end
        if hasTool then
            local md = hum.MoveDirection
            if md.Magnitude > 0 then
                hrp.AssemblyLinearVelocity = Vector3.new( md.X * 120, hrp.AssemblyLinearVelocity.Y, md.Z * 120 )
            else
                hrp.AssemblyLinearVelocity = Vector3.new(0, hrp.AssemblyLinearVelocity.Y, 0)
            end end end) end
local infiniteJumpEnabled = State.infiniteJumpEnabled
local function setInfiniteJump(enabled) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if not SAB_LIVE() then return end
    State.infiniteJumpEnabled = enabled
    infiniteJumpEnabled = State.infiniteJumpEnabled
    Config.TpSettings.InfiniteJump = enabled
    SaveConfig()
    if Connections.infiniteJumpConnection then Connections.infiniteJumpConnection:Disconnect(); Connections.infiniteJumpConnection = nil end
    if Connections._ijInputBegan then Connections._ijInputBegan:Disconnect(); Connections._ijInputBegan = nil end
    if Connections._ijInputEnded then Connections._ijInputEnded:Disconnect(); Connections._ijInputEnded = nil end
    if Connections._ijYRemove then pcall(Connections._ijYRemove); Connections._ijYRemove = nil end
    if not enabled then return end
    local isSpaceHeld = false
    local inputBegan = UserInputService.InputBegan:Connect(function(input, gp) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if gp then return end
        if input.KeyCode == Enum.KeyCode.Space then isSpaceHeld = true end
    end)
    local inputEnded = UserInputService.InputEnded:Connect(function(input) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if input.KeyCode == Enum.KeyCode.Space then isSpaceHeld = false end
    end)
    local _ijY, _ijYAtt
    local function _ijYRemove() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _ijY then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_ijY:Destroy() end); _ijY = nil end
        if _ijYAtt then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_ijYAtt:Destroy() end); _ijYAtt = nil end
    end
    Connections._ijYRemove = _ijYRemove
    Connections.infiniteJumpConnection = RunService.RenderStepped:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not SAB_LIVE() then _ijYRemove(); return end
        if not isSpaceHeld and not _ijY then return end
        local char = LocalPlayer.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        local hum = char and char:FindFirstChild("Humanoid")
        local _stealing = LocalPlayer:GetAttribute("Stealing") == true
        if _ijY and (not isSpaceHeld or not _stealing or not hrp or not hum or hum.Health <= 0
                     or _G.JAF_StealJumpLV == false or (_ijY.Parent ~= hrp)) then
            _ijYRemove()
        end
        if not isSpaceHeld then return end
        if not hrp or not hum or hum.Health <= 0 then return end
        if _G.JAF_DropFlingActive then if _ijY then _ijYRemove() end return end
        local _jv = tonumber(_G.JAF_InfJumpVel) or 50
        if _stealing and _G.JAF_StealJumpLV ~= false then
            if not _ijY or _ijY.Parent ~= hrp then
                _ijYRemove()
                _ijYAtt = Instance.new("Attachment"); _ijYAtt.Name = "JumpAttachment"; _ijYAtt.Parent = hrp
                _ijY = Instance.new("LinearVelocity")
                _ijY.Name = "JumpForce"
                _P(_ijY, "Attachment0", _ijYAtt, "RelativeTo", Enum.ActuatorRelativeTo.World, "VelocityConstraintMode", Enum.VelocityConstraintMode.Vector, "ForceLimitMode", Enum.ForceLimitMode.PerAxis)
                _ijY.MaxAxesForce = Vector3.new(0, math.huge, 0)
                _P(_ijY, "MaxForce", math.huge, "ReactionForceEnabled", true, "Parent", hrp)
            end
            _ijY.VectorVelocity = Vector3.new(0, _jv, 0)
        else
            hrp.AssemblyLinearVelocity = Vector3.new(hrp.AssemblyLinearVelocity.X, _jv, hrp.AssemblyLinearVelocity.Z)
        end end)
    Connections._ijInputBegan = inputBegan
    Connections._ijInputEnded = inputEnded
end
if infiniteJumpEnabled then setInfiniteJump(true) end
do
    local floatOn, conn, lastChar
    local function floatOff() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        floatOn = false
        if conn then conn:Disconnect(); conn = nil end
        lastChar = nil
        _G.JAF_FloatActive = false
    end
    _G.JAF_FloatTeardown = floatOff
    local function setFloat(on) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if not SAB_LIVE() then return end
        floatOff()
        if on ~= true then return end
        floatOn = true
        _G.JAF_FloatActive = true
        lastChar = LocalPlayer.Character
        conn = RunService.Heartbeat:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if not floatOn or not SAB_LIVE() then floatOff(); return end
            local char = LocalPlayer.Character
            if char ~= lastChar then floatOff(); return end
            local hrp = char and char:FindFirstChild("HumanoidRootPart")
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if not hrp or not hum or hum.Health <= 0 then floatOff(); return end
            if _G.JAF_TpActive or _G.JAF_DropFlingActive or _G.JAF_ResetStowActive then return end
            if LocalPlayer:GetAttribute("Stealing") == true then return end
            local spd = math.clamp(tonumber(_G.JAF_FloatSpeed) or tonumber(Config.FloatSpeed) or 40, 5, 200)
            local v = hrp.AssemblyLinearVelocity
            hrp.AssemblyLinearVelocity = Vector3.new(v.X, spd, v.Z)
        end) end
    _SABCAP.JAF_SetFloat    = setFloat
    _SABCAP.JAF_ToggleFloat = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
setFloat(not floatOn) end
    _SABCAP.JAF_FloatOn     = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return floatOn == true end
end
local stopAntiRagdollV2
local startAntiRagdollV2
local AntiRagdollV2Data = { conns = {}, charConn = nil, beat = nil, acs = nil, acsChar = nil }
local antiRagV2On = false
local _arv2_anyACDisabled; _arv2_anyACDisabled = function(ch) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if AntiRagdollV2Data.acsChar ~= ch or not AntiRagdollV2Data.acs then
        _P(AntiRagdollV2Data, "acsChar", ch, "acs", {}, "rootAC", nil)
        for _, d in ipairs(ch:GetDescendants()) do
            if d:IsA("AnimationConstraint") then
                table.insert(AntiRagdollV2Data.acs, d)
                if d.Name == "Root" then AntiRagdollV2Data.rootAC = d end
            end end end
    for _, ac in ipairs(AntiRagdollV2Data.acs) do if not ac.Enabled and ac.Parent then return true end end
    return false
end
local _ARV2_LAUNCH_CLASSES = {
    BodyVelocity = true, BodyForce = true, BodyThrust = true, BodyGyro = true,
    BodyAngularVelocity = true, VectorForce = true, AngularVelocity = true, RocketPropulsion = true,
}
local _ARV2_FORCE_WHITELIST = { SpeedForce = true, InvisSpeedForce = true, JumpForce = true,
    FlightPower = true, FlightSpin = true, FlightHold = true }
local _arv2_isFlying; _arv2_isFlying = function(ch) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if _G.JAF_CarpetFlightAware == false then return false end
    local hrp = ch and ch:FindFirstChild("HumanoidRootPart")
    if not hrp then return false end
    return hrp:FindFirstChild("FlightPower") ~= nil or hrp:FindFirstChild("FlightSpin") ~= nil or hrp:FindFirstChild("FlightHold") ~= nil
end
local _arv2_considerLaunch = function(d) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if _ARV2_FORCE_WHITELIST[d.Name] then return false end
    local cn = d.ClassName
    local launch = _ARV2_LAUNCH_CLASSES[cn]
    if not launch and cn == "LinearVelocity" then launch = true end
    if not launch and (cn:find("Force") or cn:find("Velocity") or cn:find("Body") or cn:find("Propulsion"))
       and (d.Name:find("Impulse") or d.Name:find("Knockback") or d.Name:find("Launch")) then
        launch = true
    end
    if launch then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
d:Destroy() end); return true end
    return false
end
local _arv2_stripLaunchForces; _arv2_stripLaunchForces = function(ch) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local found = false
    local root = ch:FindFirstChild("HumanoidRootPart")
    if root then for _, d in ipairs(root:GetChildren()) do found = _arv2_considerLaunch(d) or found end end
    for _, d in ipairs(ch:GetChildren()) do found = _arv2_considerLaunch(d) or found end
    return found
end
local _arv2_ctlState = true
local _arv2_controls; _arv2_controls = function(enable) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if _arv2_ctlState == enable then return end
    _arv2_ctlState = enable
    pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local ps = LocalPlayer:FindFirstChild("PlayerScripts")
        local pm = ps and ps:FindFirstChild("PlayerModule")
        if pm then local ctl = require(pm):GetControls() if enable then ctl:Enable() else ctl:Disable() end end
    end) end
local cleanRagdollV2; cleanRagdollV2 = (function(char) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    char = char or LocalPlayer.Character
    if not char then return end
    pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local et = LocalPlayer:GetAttribute("RagdollEndTime")
        if et and (et - Workspace:GetServerTimeNow()) > 0 then _G.JAF_RagdollUntil = et end
        LocalPlayer:SetAttribute("RagdollEndTime", 0)
    end)
    local _rootAC = nil
    for _, d in ipairs(char:GetDescendants()) do
        if d:IsA("BallSocketConstraint") or d:IsA("HingeConstraint") or d:IsA("NoCollisionConstraint") then
            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
d:Destroy() end)
        elseif d:IsA("Motor6D") and not d.Enabled then
            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
d.Enabled = true end)
        elseif d:IsA("AnimationConstraint") then
            if not d.Enabled then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
d.Enabled = true end) end
            if _rootAC == nil and d.Name == "Root" then _rootAC = d end
        end end
    do
        local _hrp = char:FindFirstChild("HumanoidRootPart")
        local _rra = _hrp and _hrp:FindFirstChild("RootRigAttachment")
        if _rra and _rootAC then
            if (_rootAC.Attachment0 == nil or _rootAC.Attachment0.Parent == nil) then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_rootAC.Attachment0 = _rra end) end
        end end
    local hum = char:FindFirstChildOfClass("Humanoid")
    if hum then
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if hum.PlatformStand then hum.PlatformStand = false end end)
        local st = hum:GetState()
        if st == Enum.HumanoidStateType.Physics or st == Enum.HumanoidStateType.Ragdoll
           or st == Enum.HumanoidStateType.FallingDown or st == Enum.HumanoidStateType.PlatformStanding then
            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hum:ChangeState(Enum.HumanoidStateType.GettingUp) end)
        end
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
Workspace.CurrentCamera.CameraSubject = hum end)
    end
    _arv2_controls(true)
end)
_G.JAF_UnRagdoll = cleanRagdollV2
LocalPlayer:GetAttributeChangedSignal("RagdollEndTime"):Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if _G.JAF_RagdollRootGuard == false then return end
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local et = tonumber(LocalPlayer:GetAttribute("RagdollEndTime")) or 0
        local dur = math.clamp((et - Workspace:GetServerTimeNow()) + 2, 1.5, 8)
        local stopAt = os.clock() + dur
        local _gCh, _gAC = nil, nil
        repeat
            if not _G.invisibleStealEnabled then
                local ch = LocalPlayer.Character
                local hrp = ch and ch:FindFirstChild("HumanoidRootPart")
                if hrp then
                    if _gCh ~= ch or not (_gAC and _gAC.Parent) then
                        _gCh, _gAC = ch, nil
                        for _, d in ipairs(ch:GetDescendants()) do
                            if d:IsA("AnimationConstraint") and d.Name == "Root" then _gAC = d; break end
                        end end
                    local d = _gAC
                    if d and (d.Attachment0 == nil or d.Attachment0.Parent == nil) then
                        local rra = hrp:FindFirstChild("RootRigAttachment")
                        if rra then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
d.Attachment0 = rra end) end
                    end end end
            RunService.Heartbeat:Wait()
        until os.clock() >= stopAt end) end)
do
    local S = { active = false, hum = nil, origMax = nil, hpConn = nil, died = {} }
    local STATES = {
        Enum.HumanoidStateType.Dead, Enum.HumanoidStateType.Ragdoll,
        Enum.HumanoidStateType.Physics, Enum.HumanoidStateType.FallingDown,
    }
    local _target; _target = function(hum) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local big = tonumber(_G.JAF_HpShieldHP)
        if big then return big end
        local m = hum.MaxHealth
        return (m > 0 and m < 1e5) and m or 100
    end
    local _lockBody = function(hum) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local t = _target(hum)
        if tonumber(_G.JAF_HpShieldHP) and hum.MaxHealth ~= t then hum.MaxHealth = t end
        if hum.Health < t then hum.Health = t end
    end
    local _lock; _lock = function(hum) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false)) pcall(_lockBody, hum) end
    local function shieldOn() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if S.active then return end
        local ch = LocalPlayer.Character
        local hum = ch and ch:FindFirstChildOfClass("Humanoid")
        if not hum then return end
        S.hum = hum
        S.origMax = (hum.MaxHealth > 0 and hum.MaxHealth < 1e5) and hum.MaxHealth or 100
        _lock(hum)
        for _, st in ipairs(STATES) do pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hum:SetStateEnabled(st, false) end) end
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hum.BreakJointsOnDeath = false end)
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if hum.PlatformStand then hum.PlatformStand = false end end)
        S.hpConn = hum.HealthChanged:Connect(function(h) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if h < _target(hum) then _lock(hum) end end)
        S.active = true
    end
    local function shieldOff() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not S.active then return end
        S.active = false
        if S.hpConn then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
S.hpConn:Disconnect() end); S.hpConn = nil end
        for _, c in ipairs(S.died) do pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if c.Enable then c:Enable() end end) end
        S.died = {}
        local hum = S.hum
        if hum and hum.Parent then
            for _, st in ipairs(STATES) do pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hum:SetStateEnabled(st, true) end) end
            pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local _om = S.origMax or 100
                hum.MaxHealth = _om
                if hum.Health > _om then hum.Health = _om end
            end) end
        S.hum = nil
    end
    _G.JAF_HpShieldOn  = shieldOn
    _G.JAF_HpShieldOff = shieldOff
    RunService.Heartbeat:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _G.JAF_HpShield == false then if S.active then shieldOff() end return end
        local want = (_G.JAF_HpShieldAlways == true) or (_G.JAF_TpActive == true)
        if want and not S.active then
            shieldOn()
        elseif not want and S.active then
            shieldOff()
        elseif S.active then
            local ch = LocalPlayer.Character
            local hum = ch and ch:FindFirstChildOfClass("Humanoid")
            if hum and hum ~= S.hum then S.active = false; shieldOn()
            elseif hum then _lock(hum) end
        end end) end
local function _arv2_killSource() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if typeof(getconnections) ~= "function" then return end
    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local pkg = ReplicatedStorage:FindFirstChild("Packages")
        local re
        local net = pkg and pkg:FindFirstChild("Net")
        if net then re = net:FindFirstChild("RE/Ragdoll") end
        if not re then local rag = pkg and pkg:FindFirstChild("Ragdoll") re = rag and rag:FindFirstChild("Ragdoll") end
        if re and re:IsA("RemoteEvent") then
            for _, c in ipairs(getconnections(re.OnClientEvent)) do
                local ok = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
c:Disable() end)
                if not ok then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
c:Disconnect() end) end
            end end end) end
local function _arv2_disconnectHooks() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    for _, c in ipairs(AntiRagdollV2Data.conns) do pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
c:Disconnect() end) end
    AntiRagdollV2Data.conns = {}
end
local function hookAntiRagV2(char) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if not antiRagV2On or not char then return end
    _arv2_disconnectHooks()
    _arv2_killSource()
    cleanRagdollV2(char)
    local cET = LocalPlayer:GetAttributeChangedSignal("RagdollEndTime"):Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if not antiRagV2On then return end
        local et = LocalPlayer:GetAttribute("RagdollEndTime")
        if et and (et - Workspace:GetServerTimeNow()) > 0 then cleanRagdollV2(LocalPlayer.Character) end
    end)
    table.insert(AntiRagdollV2Data.conns, cET)
    local hum = char:FindFirstChildOfClass("Humanoid")
    if hum then
        local cSt = hum.StateChanged:Connect(function(_, newState) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            if not antiRagV2On then return end
            if newState == Enum.HumanoidStateType.Physics or newState == Enum.HumanoidStateType.Ragdoll
               or newState == Enum.HumanoidStateType.PlatformStanding then
                cleanRagdollV2(LocalPlayer.Character)
            end end)
        table.insert(AntiRagdollV2Data.conns, cSt)
    end
    local _cleanQueued = false
    local cDA = char.DescendantAdded:Connect(function(d) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not antiRagV2On then return end
        if d:IsA("BallSocketConstraint") or d:IsA("HingeConstraint") or d:IsA("NoCollisionConstraint") then
            if _cleanQueued then return end
            _cleanQueued = true
            task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_cleanQueued = false; cleanRagdollV2(LocalPlayer.Character) end)
        end end)
    table.insert(AntiRagdollV2Data.conns, cDA)
end
stopAntiRagdollV2 = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    antiRagV2On = false
    _arv2_disconnectHooks()
    if AntiRagdollV2Data.charConn then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
AntiRagdollV2Data.charConn:Disconnect() end); AntiRagdollV2Data.charConn = nil end
    if AntiRagdollV2Data.beat then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
AntiRagdollV2Data.beat:Disconnect() end); AntiRagdollV2Data.beat = nil end
end
startAntiRagdollV2 = function(enabled) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if not SAB_LIVE() then return end
    stopAntiRagdollV2()
    antiRagV2On = enabled and true or false
    if not antiRagV2On then return end
    if LocalPlayer.Character then task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hookAntiRagV2(LocalPlayer.Character) end) end
    AntiRagdollV2Data.charConn = LocalPlayer.CharacterAdded:Connect(function(ch) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if not antiRagV2On then return end
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
ch:WaitForChild("Humanoid", 10) end)
        hookAntiRagV2(ch)
        task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
for _ = 1, 12 do task.wait(0.25); if antiRagV2On then _arv2_killSource() else break end end end)
    end)
    local _arvLastScan = 0
    local _arvLastVel = Vector3.zero
    AntiRagdollV2Data.beat = RunService.Heartbeat:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not antiRagV2On then return end
        local ch = LocalPlayer.Character; if not ch then return end
        local et = LocalPlayer:GetAttribute("RagdollEndTime")
        local ragdolled = et ~= nil and (et - Workspace:GetServerTimeNow()) > 0
        local _flying = _arv2_isFlying(ch)
        if not ragdolled and not _flying then
            local hum = ch:FindFirstChildOfClass("Humanoid")
            if hum then
                local s = hum:GetState()
                ragdolled = (s == Enum.HumanoidStateType.Physics or s == Enum.HumanoidStateType.Ragdoll
                             or s == Enum.HumanoidStateType.PlatformStanding or hum.PlatformStand == true)
            end end
        if not ragdolled then ragdolled = _arv2_anyACDisabled(ch) end
        if not _G.invisibleStealEnabled then
            local _r = AntiRagdollV2Data.rootAC
            if _r and _r.Parent and (_r.Attachment0 == nil or _r.Attachment0.Parent == nil) then
                local _hrp = ch:FindFirstChild("HumanoidRootPart")
                local _rra = _hrp and _hrp:FindFirstChild("RootRigAttachment")
                if _rra then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_r.Attachment0 = _rra end) end
            end end
        if not ragdolled then
            local _now = tick()
            if _now - _arvLastScan > (tonumber(_G.JAF_RagScanInterval) or 0.2) then
                _arvLastScan = _now
                ragdolled = ch:FindFirstChildWhichIsA("BallSocketConstraint", true) ~= nil
                         or ch:FindFirstChildWhichIsA("HingeConstraint", true) ~= nil
            end end
        if ragdolled then _G.JAF_RagdollPhysLastT = tick() end
        local launched = false
        if _G.JAF_AntiLaunch ~= false and (ragdolled or _G.JAF_TpActive) then launched = _arv2_stripLaunchForces(ch) end
        if ragdolled then cleanRagdollV2(ch) end
        if ragdolled or launched then
            local root = ch:FindFirstChild("HumanoidRootPart")
            if root then
                root.AssemblyAngularVelocity = Vector3.zero
                if _G.JAF_RagdollStayPut ~= false and not _G.JAF_TpActive and not _G.JAF_DropFlingActive then
                    root.AssemblyLinearVelocity = Vector3.zero
                end end end
        if Config.AntiRagdollV2 ~= false and not _G.JAF_TpActive and not _G.JAF_CarpetSpeedActive and not _flying and not _G.JAF_CarpetBuyFlying and not _G.JAF_DropFlingActive then
            local root = ch:FindFirstChild("HumanoidRootPart")
            if root then
                local v = root.AssemblyLinearVelocity
                if (v - _arvLastVel).Magnitude > (tonumber(_G.JAF_LaunchSpikeThreshold) or 110) then
                    root.AssemblyLinearVelocity = Vector3.zero
                    root.AssemblyAngularVelocity = Vector3.zero
                    v = Vector3.zero
                end
                _arvLastVel = v
            end
        else
            local root = ch:FindFirstChild("HumanoidRootPart")
            _arvLastVel = root and root.AssemblyLinearVelocity or Vector3.zero
        end end) end
if Config.AntiRagdollV2 ~= false then startAntiRagdollV2(true) end
do
    local plotBeam = nil
    local plotBeamAttachment0 = nil
    local plotBeamAttachment1 = nil
    local function findMyPlot() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local plots = workspace:FindFirstChild("Plots")
        if not plots then return nil end
        for _, plot in ipairs(plots:GetChildren()) do
            local sign = plot:FindFirstChild("PlotSign")
            if sign then
                local surfaceGui = sign:FindFirstChildWhichIsA("SurfaceGui", true)
                if surfaceGui then
                    local label = surfaceGui:FindFirstChildWhichIsA("TextLabel", true)
                    if label then
                        local text = label.Text:lower()
                        if text:find(LocalPlayer.DisplayName:lower(), 1, true) or text:find(LocalPlayer.Name:lower(), 1, true) then
                            return plot
                        end end end end end
        return nil
    end
    local function createPlotBeam() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not Config.LineToBase then return end
        local myPlot = findMyPlot()
        if not myPlot or not myPlot.Parent then return end
        local character = LocalPlayer.Character
        if not character or not character.Parent then return end
        local hrp = character:FindFirstChild("HumanoidRootPart")
        if not hrp or not hrp.Parent then return end
        if plotBeam then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
plotBeam:Destroy() end) end
        if plotBeamAttachment0 then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
plotBeamAttachment0:Destroy() end) end
        plotBeamAttachment0 = hrp:FindFirstChild("PlotBeamAttach_Player") or Instance.new("Attachment")
        _P(plotBeamAttachment0, "Name", "PlotBeamAttach_Player", "Position", Vector3.new(0, 0, 0), "Parent", hrp)
        local plotPart = myPlot:FindFirstChild("MainRootPart") or myPlot:FindFirstChildWhichIsA("BasePart")
        if not plotPart or not plotPart.Parent then return end
        plotBeamAttachment1 = plotPart:FindFirstChild("PlotBeamAttach_Plot") or Instance.new("Attachment")
        _P(plotBeamAttachment1, "Name", "PlotBeamAttach_Plot", "Position", Vector3.new(0, 5, 0), "Parent", plotPart)
        plotBeam = hrp:FindFirstChild("PlotBeam") or Instance.new("Beam")
        plotBeam.Name = "PlotBeam"
        plotBeam.Attachment0 = plotBeamAttachment0
        plotBeam.Attachment1 = plotBeamAttachment1
        plotBeam.FaceCamera = true
        plotBeam.LightEmission = 1
        plotBeam.Color = ColorSequence.new(Color3.fromRGB(255, 255, 255))
        plotBeam.Transparency = NumberSequence.new(0)
        plotBeam.Width0 = 0.7
        plotBeam.Width1 = 0.7
        plotBeam.TextureMode = Enum.TextureMode.Wrap
        plotBeam.TextureSpeed = 0
        plotBeam.Parent = hrp
    end
    local function resetPlotBeam() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if plotBeam then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
plotBeam:Destroy() end) end
        if plotBeamAttachment0 then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
plotBeamAttachment0:Destroy() end) end
        if plotBeamAttachment1 then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
plotBeamAttachment1:Destroy() end) end
        plotBeam = nil
        plotBeamAttachment0 = nil
        plotBeamAttachment1 = nil
    end
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local checkCounter = 0
        RunService.Heartbeat:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if not Config.LineToBase then return end
            checkCounter = checkCounter + 1
            if checkCounter >= 30 then
                checkCounter = 0
                if not plotBeam or not plotBeam.Parent or not plotBeamAttachment0 or not plotBeamAttachment0.Parent then
                    pcall(createPlotBeam)
                end end end) end)
    LocalPlayer.CharacterAdded:Connect(function(character) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        task.wait(0.5)
        if Config.LineToBase and character then pcall(createPlotBeam) end
    end)
    if LocalPlayer.Character then
        task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            task.wait(0.2)
            if Config.LineToBase then createPlotBeam() end
        end) end
    _G.createPlotBeam = createPlotBeam
    _G.resetPlotBeam = resetPlotBeam
end
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    _G.JAF_WaitHeavy()
    local COOLDOWNS = {rocket = 120, ragdoll = 30, balloon = 30, inverse = 60, nightvision = 60, jail = 60, tiny = 60, jumpscare = 60, morph = 60}
    local ALL_COMMANDS = {"balloon", "inverse", "jail", "jumpscare", "morph", "nightvision", "ragdoll", "rocket", "tiny"}
    local CMD_KNOWN = {}
    for _, c in ipairs(ALL_COMMANDS) do CMD_KNOWN[c] = true end
    local function _cmdEnabled(c) LPH_ATTRIBUTES(ERROR_HANDLING(false))
local a = Config.AdminAllowedCmds; return not (a and a[c] == false) end
    local function _mergedOrder() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local out, seen = {}, {}
        local saved = Config.AdminCmdOrder
        if type(saved) == "table" then
            for _, c in ipairs(saved) do
                if type(c) == "string" and CMD_KNOWN[c] and not seen[c] then seen[c] = true; out[#out + 1] = c end
            end end
        for _, c in ipairs(ALL_COMMANDS) do if not seen[c] then seen[c] = true; out[#out + 1] = c end end
        return out
    end
    local function getOrderedCommands() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local out = {}
        for _, c in ipairs(_mergedOrder()) do if _cmdEnabled(c) then out[#out + 1] = c end end
        return out
    end
    _G.JAF_GetOrderedCommands = getOrderedCommands
    local function buildCmdSequence(randomize) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local order = getOrderedCommands()
        if not randomize or #order < 2 then return order end
        local pinned = (type(Config.AdminCmdPinned) == "table") and Config.AdminCmdPinned or nil
        local seq, freeIdx, pool = {}, {}, {}
        for i, cmd in ipairs(order) do
            if pinned and pinned[cmd] then seq[i] = cmd
            else seq[i] = false; freeIdx[#freeIdx + 1] = i; pool[#pool + 1] = cmd end
        end
        local rng = Random.new()
        for i = #pool, 2, -1 do local j = rng:NextInteger(1, i); pool[i], pool[j] = pool[j], pool[i] end
        for k, idx in ipairs(freeIdx) do seq[idx] = pool[k] end
        return seq
    end
    _G.JAF_BuildCmdSequence = buildCmdSequence
    local function buildCmdOrderUI(parent, hdrOrder, opts) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        opts = opts or {}
        local rowH, hdrH, corner = opts.rowH or 26, opts.hdrH or 26, opts.corner or 6
        local tSize, save = opts.textSize or 11, opts.save
        local hdr = JUI.new("TextButton", parent, "Size", UDim2.new(1, 0, 0, hdrH), "BackgroundColor3", Theme.SurfaceHighlight, "BorderSizePixel", 0, "LayoutOrder", hdrOrder, "AutoButtonColor", false, "Font", Enum.Font.GothamBold, "TextSize", tSize, "TextColor3", Theme.Accent1, "TextXAlignment", Enum.TextXAlignment.Left, "Text", "  \u{25B6}  Command Order")
        JUI.corner(hdr, corner)
        local holder = JUI.new("Frame", parent, "BackgroundTransparency", 1, "BorderSizePixel", 0, "LayoutOrder", hdrOrder + 1, "Visible", false, "Size", UDim2.new(1, 0, 0, 0))
        JUI.new("UIListLayout", holder, "Padding", UDim.new(0, 6), "SortOrder", Enum.SortOrder.LayoutOrder)
        local open, refresh = false
        refresh = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            for _, c in ipairs(holder:GetChildren()) do if not c:IsA("UIListLayout") then c:Destroy() end end
            local order = _mergedOrder()
            Config.AdminCmdOrder = order
            JUI.new("TextLabel", holder, "Size", UDim2.new(1, 0, 0, 24), "BackgroundTransparency", 1, "LayoutOrder", 0, "Text", "  Arrows set the fire order. PIN locks a slot in Random mode.", "Font", Enum.Font.GothamMedium, "TextSize", 11, "TextColor3", Color3.fromRGB(150, 150, 165), "TextXAlignment", Enum.TextXAlignment.Left, "TextWrapped", true)
            local bH = rowH - 8
            for i, cmd in ipairs(order) do
                local enabled = _cmdEnabled(cmd)
                local pinned = (type(Config.AdminCmdPinned) == "table") and Config.AdminCmdPinned[cmd] == true
                local r = JUI.new("Frame", holder, "Size", UDim2.new(1, 0, 0, rowH), "BackgroundColor3", Theme.Surface, "BorderSizePixel", 0, "LayoutOrder", i)
                JUI.corner(r, corner)
                JUI.new("TextLabel", r, "Size", UDim2.new(1, -116, 1, 0), "Position", UDim2.new(0, 10, 0, 0), "BackgroundTransparency", 1, "Text", i .. ".  " .. cmd:sub(1, 1):upper() .. cmd:sub(2) .. (enabled and "" or "   (off)"), "Font", Enum.Font.GothamBold, "TextSize", tSize, "TextColor3", enabled and Theme.TextPrimary or Color3.fromRGB(120, 120, 132), "TextXAlignment", Enum.TextXAlignment.Left)
                local function move(delta) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    local j = i + delta
                    if j < 1 or j > #order then return end
                    order[i], order[j] = order[j], order[i]
                    if save then save() end
                    refresh()
                end
                local function mkBtn(txt, xOff, w, on) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    local b = JUI.new("TextButton", r, "Size", UDim2.new(0, w, 0, bH), "Position", UDim2.new(1, xOff, 0.5, -bH / 2), "AutoButtonColor", false, "BorderSizePixel", 0, "Text", txt, "Font", Enum.Font.GothamBold, "TextSize", 11, "BackgroundColor3", on and Theme.Accent1 or Theme.SurfaceHighlight, "TextColor3", on and _G.JAF_OnAccent() or Theme.TextPrimary)
                    JUI.corner(b, 5)
                    return b
                end
                mkBtn("\u{25B2}", -92, 22, false).MouseButton1Click:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
move(-1) end)
                mkBtn("\u{25BC}", -66, 22, false).MouseButton1Click:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
move(1) end)
                mkBtn("PIN", -40, 34, pinned).MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    if type(Config.AdminCmdPinned) ~= "table" then Config.AdminCmdPinned = {} end
                    Config.AdminCmdPinned[cmd] = (not pinned) or nil
                    if save then save() end
                    refresh()
                end) end
            holder.Size = UDim2.new(1, 0, 0, 24 + 6 + #order * (rowH + 6))
        end
        hdr.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            open = not open
            if open then refresh() end
            holder.Visible = open
            hdr.Text = (open and "  \u{25BC}  " or "  \u{25B6}  ") .. "Command Order"
        end)
        return hdr, holder
    end
    _G.JAF_BuildCmdOrderUI = buildCmdOrderUI
    local activeCooldowns = {}
    SharedState.AdminButtonCache = {}
    BlacklistedPlayers = Config.Blacklist or {}
    local function _blIndex(username) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not username then return nil end
        local lower = string.lower(tostring(username))
        for i, v in ipairs(BlacklistedPlayers) do if string.lower(tostring(v)) == lower then return i end end
        return nil
    end
    addToBlacklist = function(username) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not username or username == "" or _blIndex(username) then return false end
        table.insert(BlacklistedPlayers, username)
        Config.Blacklist = BlacklistedPlayers; SaveConfig(); return true
    end
    removeFromBlacklist = function(username) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not username or username == "" then return false end
        local i = _blIndex(username)
        if not i then return false end
        table.remove(BlacklistedPlayers, i)
        Config.Blacklist = BlacklistedPlayers; SaveConfig(); return true
    end
    isBlacklisted = function(username) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _blIndex(username) ~= nil end
    local function _detector(gate, probe) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        return function(username) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if gate() == false then return false end
            if not username then return false end
            local ok, result = pcall(probe, username)
            return ok and result == true
        end end
    isAtlasUser = _detector(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.AntiApAtlasUsers end, function(username) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local folder = Workspace:FindFirstChild("AtlasESPFolder")
        local pf = folder and folder:FindFirstChild(tostring(username) .. "_ESP")
        local hl = pf and pf:FindFirstChildWhichIsA("Highlight")
        if not hl then return false end
        local c = hl.FillColor
        return math.abs(c.R - 1) < 0.02 and math.abs(c.G - (215 / 255)) < 0.02 and math.abs(c.B - 0) < 0.02
    end)
    isKawaifuUser = _detector(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.AntiApKawaifuUsers end, function(username) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local p = Players:FindFirstChild(username)
        local char = p and p.Character
        return char ~= nil and char:FindFirstChild("KaWaifu_NeonHighlight") ~= nil
    end)
    local function _mkRoster(o) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local R = {names = {}, ids = {}, nN = 0, nI = 0, nD = 0, src = "none", ms = 0, err = nil}
        local function addName(c) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            c = tostring(c):lower()
            if #c < 3 or #c > 20 or not c:match("^[%w_]+$") or c:match("^%d+$") then return false end
            if not R.names[c] then R.names[c] = true; R.nN = R.nN + 1 end
            return true
        end
        local function addId(d) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local n = tonumber(d)
            if not n or n <= 0 or n >= 1e11 then return false end
            if not R.ids[n] then R.ids[n] = true; R.nI = R.nI + 1 end
            return true
        end
        local function apply(arr, src) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            R.names, R.ids, R.nN, R.nI, R.nD = {}, {}, 0, 0, 0
            for _, raw in ipairs(arr) do
                local s = tostring(raw):gsub("\\", ""):gsub("%*", ""):gsub("^%s+", ""):gsub("%s+$", ""):gsub("^[^%w_]+", ""):gsub("[^%w_]+$", "")
                local hit, rid = nil, s:match("^roblox_user_(%d+)$")
                if rid then hit = addId(rid)
                elseif s:match("^%d+$") then hit = addId(s)
                else
                    hit = addName((s:gsub("%s+", "")))
                    local best = ""
                    for tok in s:gmatch("[%w_]+") do if #tok > #best then best = tok end end
                    if best ~= "" and addName(best) then hit = true end
                end
                if not hit then R.nD = R.nD + 1 end
            end
            R.src = src
        end
        if isfile and readfile then
            pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if not isfile(o.cache) then return end
                local lines = {}
                for ln in tostring(readfile(o.cache)):gmatch("[^\r\n]+") do lines[#lines + 1] = ln end
                if #lines > 0 then apply(lines, "cache") end
            end) end
        R.refresh = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local ok, err = pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local t0, body = os.clock()
                if not o.headers and game.HttpGet then body = game:HttpGet(o.url, true) end
                if not body or body == "" then
                    local req = (syn and syn.request) or (http and http.request) or http_request or request or (fluxus and fluxus.request)
                    if not req then error("no http") end
                    local res = req({Url = o.url, Method = "GET", Headers = o.headers})
                    body = res and (res.Body or res.body)
                end
                if not body or body == "" then error("empty body") end
                local _, lb = body:find('"' .. o.field .. '"%s*:%s*%[')
                if not lb then error("no " .. o.field .. " array") end
                local rb = body:find("]", lb, true)
                if not rb then error("unterminated " .. o.field .. " array") end
                local arr = HttpService:JSONDecode(body:sub(lb, rb))
                if type(arr) ~= "table" or #arr == 0 then error("empty " .. o.field .. " array") end
                apply(arr, "live")
                R.ms = (os.clock() - t0) * 1000
                if writefile then
                    local out = {}
                    for n in pairs(R.names) do out[#out + 1] = n end
                    for i in pairs(R.ids) do out[#out + 1] = "#" .. tostring(i) end
                    writefile(o.cache, table.concat(out, "\n"))
                end end)
            R.err = (not ok) and tostring(err) or nil
            return ok
        end
        R.has = function(username) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if not username then return false end
            if R.names[tostring(username):lower()] then return true end
            if R.nI == 0 then return false end
            local ok, result = pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local p = Players:FindFirstChild(username)
                return p ~= nil and R.ids[p.UserId] == true
            end)
            return ok and result == true
        end
        R.dump = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            return ("%s roster: %d names + %d ids (src=%s, %.0fms, %d unresolved)%s"):format(o.tag, R.nN, R.nI, tostring(R.src), R.ms, R.nD, R.err and (" ERR=" .. R.err) or "")
        end
        return R
    end
    local _fmly = _mkRoster({tag = "FMLY", field = "good", cache = "JAF_FmlyGood.txt", url = "https://gist.githubusercontent.com/josecastle21/fcc5696b9d37ae086a324d99e6b8fa5e/raw/fmly_badboys.json"})
    local _son = _mkRoster({tag = "SON", field = "safe", cache = "JAF_SonSafe.txt", url = "http://169.128.190.109:3000/api/lists", headers = {["x-api-key"] = "son-esp-key-2026", ["Content-Type"] = "application/json"}})
    task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
task.wait(2)   _fmly.refresh() end)
    task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
task.wait(2.5) _son.refresh()  end)
    _G.JAF_FmlyRefresh = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _fmly.refresh() end
    _G.JAF_FmlyDump    = _fmly.dump
    _G.JAF_SonRefresh  = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _son.refresh() end
    _G.JAF_SonDump     = _son.dump
    _G.JAF_RosterDump  = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _fmly.dump() .. " | " .. _son.dump() end
    isFmlyUser = function(username) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.AntiApFmlyUsers ~= false and _fmly.has(username) end
    isSonUser  = function(username) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.AntiApSonUsers  ~= false and _son.has(username) end
    local _btScan = {host = nil, t = 0}
    local function _btHueOk(hl) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local f, o = hl.FillColor, hl.OutlineColor
        return math.abs(f.R - 100 / 255) < 0.02 and math.abs(f.G - 0 / 255) < 0.02 and math.abs(f.B - 220 / 255) < 0.02
           and math.abs(o.R - 170 / 255) < 0.02 and math.abs(o.G - 80 / 255) < 0.02 and math.abs(o.B - 255 / 255) < 0.02
    end
    isBraintopiaUser = _detector(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.AntiApBraintopiaUsers end, function(username) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local p = Players:FindFirstChild(username)
        local char = p and p.Character
        if not char then return false end
        local hl = char:FindFirstChild("BT_ESP", true)
        if hl and hl:IsA("Highlight") and _btHueOk(hl) then return true end
        if char:FindFirstChild("BT_ESP_Label", true) then return true end
        local host = _btScan.host
        if not (host and host.Parent) then
            local now = os.clock()
            if now - _btScan.t < 3 then return false end
            _btScan.t = now
            local cg = game:GetService("CoreGui")
            host = cg:FindFirstChild("BT_ESP") and cg or nil
            if not host then for _, a in ipairs(cg:GetChildren()) do if a:FindFirstChild("BT_ESP") then host = a; break end end end
            _btScan.host = host
            if not host then return false end
        end
        for _, ch in ipairs(host:GetChildren()) do
            if ch.Name == "BT_ESP" and ch:IsA("Highlight") and ch.Adornee == char and _btHueOk(ch) then return true end
        end
        return false
    end)
    getAutojoinerName = function(username) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        return (isFmlyUser(username) and "FMLY") or (isSonUser(username) and "SON") or (isAtlasUser(username) and "Atlas")
            or (isKawaifuUser(username) and "Kawaifu") or (isBraintopiaUser(username) and "Braintopia") or nil
    end
    canUseAdminAction = function(targetPlayer) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not targetPlayer then return false end
        if isBlacklisted(targetPlayer.Name) then return false end
        return getAutojoinerName(targetPlayer.Name) == nil
    end
    refreshBlacklistUI = nil
    local function adminGetSync() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _G.JAF_GetSynchronizer() end
    local adminGui = JUI.new("ScreenGui", nil, "Name", "XiAdminPanel", "ResetOnSpawn", false, "Enabled", not Config.HideAdminPanel, "Parent", HubGui)
    local frame = JUI.new("Frame", nil, "Size", UDim2.new(0, 440, 0, 540), "Position", UDim2.new(Config.Positions.AdminPanel.X, 0, Config.Positions.AdminPanel.Y, 0), "BackgroundColor3", Theme.Background, "BackgroundTransparency", 0.18, "BorderSizePixel", 0, "Parent", adminGui)
    local listFrame
    local layout
    local blFrame
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 12)
    local stroke = JUI.new("UIStroke", frame, "Color", Theme.Accent2, "Thickness", 1.5, "Transparency", 0.4)
    CreateGradient(stroke)
    if not IS_MOBILE() then frame:SetAttribute("JAFBaseScale", 0.9) end
    do
        local cdRemaining; cdRemaining = (function(cmd) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local ok, n = pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local tl = LocalPlayer:FindFirstChild("PlayerGui")
                tl = tl and tl:FindFirstChild("AdminPanel"); tl = tl and tl:FindFirstChild("AdminPanel")
                tl = tl and tl:FindFirstChild("Content");    tl = tl and tl:FindFirstChild("ScrollingFrame")
                tl = tl and tl:FindFirstChild(cmd);          tl = tl and tl:FindFirstChild("Timer")
                if tl and tl.Visible then
                    local txt = tostring(tl.Text)
                    local mm, ss = txt:match("(%d+):(%d+)")
                    if mm then return tonumber(mm) * 60 + tonumber(ss) end
                    return tonumber(txt:match("%d+"))
                end
                return nil
            end)
            if ok and n and n > 0 then
                n = math.clamp(n, 0, COOLDOWNS[cmd] or n)
                if _G.activeCooldowns and not _G.activeCooldowns[cmd] then
                    _G.activeCooldowns[cmd] = tick() - ((COOLDOWNS[cmd] or n) - n)
                end
                return n
            end
            local st = _G.activeCooldowns and _G.activeCooldowns[cmd]
            if st then local rem = (COOLDOWNS[cmd] or 0) - (tick() - st) if rem > 0 then return math.ceil(rem) end end
            return 0
        end)
        local cdStrip = JUI.new("Frame", frame, "Name", "CooldownStrip", "Size", UDim2.new(1, 0, 0, 132), "Position", UDim2.new(0, 0, 1, 8), "BackgroundColor3", Theme.Background, "BackgroundTransparency", 0.18, "BorderSizePixel", 0, "Visible", not Config.HideAdminCooldowns)
        _G.JAF_SetAdminCooldowns = function(show) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if cdStrip and cdStrip.Parent then cdStrip.Visible = show and true or false end
        end
        JUI.corner(cdStrip, 12)
        local cdStroke = JUI.stroke(cdStrip, Theme.Accent2, 1.5, 0.4)
        pcall(CreateGradient, cdStroke)
        local CD_GREEN = Color3.fromRGB(90, 222, 110)
        local cdHdr = JUI.new("TextLabel", cdStrip, "Size", UDim2.new(1, -28, 0, 22), "Position", UDim2.new(0, 14, 0, 8), "BackgroundTransparency", 1, "Text", "Cooldowns", "Font", Enum.Font.GothamBold, "TextSize", 14, "TextColor3", Theme.TextPrimary, "TextXAlignment", Enum.TextXAlignment.Left)
        local CD_LIST = {
            {cmd="rocket",name="Rocket"},   {cmd="inverse",name="Inverse"},
            {cmd="tiny",name="Tiny"},       {cmd="balloon",name="Balloon"},
            {cmd="ragdoll",name="Ragdoll"}, {cmd="jail",name="Jail"},
            {cmd="morph",name="Morph"},     {cmd="nightvision",name="NightVis"},
        }
        local cdCells = {}
        local grid = JUI.new("Frame", cdStrip, "Size", UDim2.new(1, -28, 0, 88), "Position", UDim2.new(0, 14, 0, 34), "BackgroundTransparency", 1)
        _P(Instance.new("UIGridLayout", grid), "FillDirection", Enum.FillDirection.Vertical, "FillDirectionMaxCells", 4, "CellSize", UDim2.new(0.5, -10, 0, 19), "CellPadding", UDim2.new(0, 20, 0, 4), "SortOrder", Enum.SortOrder.LayoutOrder)
        for i, e in ipairs(CD_LIST) do
            local cell = JUI.new("Frame", grid, "LayoutOrder", i, "BackgroundTransparency", 1)
            local nm = JUI.new("TextLabel", cell, "Size", UDim2.new(0.55, 0, 1, 0), "Position", UDim2.new(0, 0, 0, 0), "BackgroundTransparency", 1, "Text", e.name, "Font", Enum.Font.GothamMedium, "TextSize", 13, "TextColor3", Theme.TextSecondary, "TextXAlignment", Enum.TextXAlignment.Left)
            local stv = JUI.new("TextLabel", cell, "Size", UDim2.new(0.45, 0, 1, 0), "Position", UDim2.new(0.55, 0, 0, 0), "BackgroundTransparency", 1, "Font", Enum.Font.GothamBold, "TextSize", 13, "TextXAlignment", Enum.TextXAlignment.Right, "Text", "Ready", "TextColor3", CD_GREEN)
            cdCells[e.cmd] = stv
        end
        task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            while cdStrip.Parent do
                local showing = adminGui.Enabled and frame.Visible and cdStrip.Visible
                if showing then
                    for _, e in ipairs(CD_LIST) do
                        local r = cdRemaining(e.cmd)
                        local lbl = cdCells[e.cmd]
                        if lbl then
                            if r > 0 then lbl.Text = r .. "s"; lbl.TextColor3 = Theme.Error
                            else lbl.Text = "Ready"; lbl.TextColor3 = CD_GREEN end
                        end end end
                task.wait(showing and 0.25 or 0.7)
            end end) end
    local function _paintBtn(btn, stroke, on, onBg, onStroke, offBg) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        btn.BackgroundColor3 = on and onBg or (offBg or Theme.SurfaceHighlight)
        btn.TextColor3 = on and _G.JAF_OnAccent() or Theme.TextPrimary
        if stroke then stroke.Color = on and (onStroke or onBg) or Theme.SurfaceHighlight end
    end
    local header = JUI.new("Frame", frame, "Size", UDim2.new(1, 0, 0, 40), "BackgroundTransparency", 1)
    MakeDraggable(header, frame, "AdminPanel")
    pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if Config.AdminPanelW440Migrated then return end
        Config.AdminPanelW440Migrated = true
        local sv = Config.PanelSizes and Config.PanelSizes.XiAdminPanel
        if type(sv) == "table" and tonumber(sv.w) and tonumber(sv.w) < 440 then sv.w = 440 end
    end)
    attachResizeGrip(frame, "XiAdminPanel", 0.85)
    AddMobileMinimizeAdmin(frame, "ADMIN", header)
    local title = JUI.new("TextLabel", header, "Size", UDim2.new(1, -105, 1, 0), "Position", UDim2.new(0, 15, 0, 0), "BackgroundTransparency", 1, "Text", "Admin Panel", "Font", Enum.Font.GothamBold, "TextSize", 18, "TextColor3", Theme.TextPrimary, "TextXAlignment", Enum.TextXAlignment.Left)
    local refreshBtn = JUI.new("TextButton", header, "Size", UDim2.new(0, 88, 0, 32), "Position", UDim2.new(1, IS_MOBILE() and -125 or -93, 0.5, -16), "BackgroundColor3", Theme.SurfaceHighlight, "Text", "REFRESH", "Font", Enum.Font.GothamBold, "TextSize", 14, "TextColor3", Theme.TextPrimary)
    JUI.corner(refreshBtn, 6)
    local refreshStroke = JUI.stroke(refreshBtn, Theme.Accent2, 1, 0.3)
    local proxCont = JUI.new("Frame", frame, "Size", UDim2.new(1, -20, 0, 48), "Position", UDim2.new(0, 10, 0, 58), "BackgroundColor3", Theme.Surface, "BackgroundTransparency", 0.3)
    JUI.corner(proxCont, 10)
    local proxContStroke = JUI.stroke(proxCont, Theme.Accent2, 1, 0.6)
    local proxBtn = JUI.new("TextButton", proxCont, "Name", "ProximityAPButton", "Size", UDim2.new(0, 96, 0, 32), "Position", UDim2.new(0, 8, 0.5, -16), "BackgroundColor3", ProximityAPActive and Theme.Accent1 or Theme.SurfaceHighlight, "Text", "Prox", "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", ProximityAPActive and _G.JAF_OnAccent() or Theme.TextPrimary)
    JUI.corner(proxBtn, 6)
    local proxBtnStroke = JUI.new("UIStroke", proxBtn, "Color", ProximityAPActive and Theme.Accent2 or Theme.SurfaceHighlight, "Transparency", 0.3)
    SharedState.ProximityAPButton = proxBtn
    SharedState.ProximityAPButtonStroke = proxBtnStroke
    local spamBaseBtn = JUI.new("TextButton", proxCont, "Size", UDim2.new(0, 120, 0, 32), "Position", UDim2.new(0, 112, 0.5, -16), "BackgroundColor3", Theme.SurfaceHighlight, "Text", "Spam Owner", "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", Theme.TextPrimary)
    JUI.corner(spamBaseBtn, 6)
    local spamBaseBtnStroke = JUI.new("UIStroke", spamBaseBtn, "Color", Theme.SurfaceHighlight, "Transparency", 0.3)
    local ctapPanelBtn = JUI.new("TextButton", proxCont, "Size", UDim2.new(0, 100, 0, 32), "Position", UDim2.new(0, 240, 0.5, -16), "AutoButtonColor", false, "Text", "Click AP", "Font", Enum.Font.GothamBold, "TextSize", 13, "BorderSizePixel", 0)
    JUI.corner(ctapPanelBtn, 6)
    local ctapPanelStroke = JUI.new("UIStroke", ctapPanelBtn, "Color", Config.ClickToAP and Theme.Accent1 or Theme.SurfaceHighlight, "Transparency", 0.3)
    local function updateCtapPanelBtn() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        _paintBtn(ctapPanelBtn, ctapPanelStroke, Config.ClickToAP, Theme.Accent1)
    end
    updateCtapPanelBtn()
    _G.JAF_ToggleClickToAP = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        Config.ClickToAP = not Config.ClickToAP
        if Config.ClickToAP then Config.ClickToAPSingleCommand = true end
        SaveConfig()
        updateCtapPanelBtn()
    end
    ctapPanelBtn.MouseButton1Click:Connect(_G.JAF_ToggleClickToAP)
    local function _paintSpamBtn(active) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        _paintBtn(spamBaseBtn, spamBaseBtnStroke, active, Theme.Accent1, Theme.Accent2)
    end
    local function nearestBaseOwner() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local char = LocalPlayer.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        if not hrp then return nil end
        local nearestPlot = nil
        local nearestDist = math.huge
        local Plots = Workspace:FindFirstChild("Plots")
        if Plots then
            for _, plot in ipairs(Plots:GetChildren()) do
                local sign = plot:FindFirstChild("PlotSign")
                if sign then
                    local yourBase = sign:FindFirstChild("YourBase")
                    if not yourBase or not yourBase.Enabled then
                        local signPos = sign:IsA("BasePart") and sign.Position or (sign.PrimaryPart and sign.PrimaryPart.Position)
                        if not signPos then local part = sign:FindFirstChildWhichIsA("BasePart", true) signPos = part and part.Position end
                        if signPos then
                            local dist = (hrp.Position - signPos).Magnitude
                            if dist < nearestDist then nearestDist = dist nearestPlot = plot end
                        end end end end end
        if not nearestPlot then return nil end
        local targetPlayer = nil
        local ok, ch = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
local S = adminGetSync(); return S and S:Get(nearestPlot.Name) end)
        if ok and ch then local owner = ch:Get("Owner") if owner then targetPlayer = _G.JAF_OwnerPlayer(owner) end end
        if not targetPlayer then
            local sign = nearestPlot:FindFirstChild("PlotSign")
            local textLabel = sign and sign:FindFirstChild("SurfaceGui") and sign.SurfaceGui:FindFirstChild("Frame") and sign.SurfaceGui.Frame:FindFirstChild("TextLabel")
            if textLabel then
                local baseText = textLabel.Text
                local nickname = baseText and baseText:match("^(.-)'") or baseText
                if nickname then
                    for _, p in ipairs(Players:GetPlayers()) do
                        if p.DisplayName == nickname or p.Name == nickname then targetPlayer = p break end
                    end end end end
        if not targetPlayer or targetPlayer == LocalPlayer then return nil end
        if isBlacklisted(targetPlayer.Name) then return nil end
        if getAutojoinerName(targetPlayer.Name) then return nil end
        return targetPlayer
    end
    spamBaseBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _G.JAF_SpamBusy and _G.JAF_SpamBusy() then return end
        local targetPlayer = nearestBaseOwner()
        if not targetPlayer then return end
        if not _G.JAF_SpamOnce then return end
        _paintSpamBtn(true)
        if not _G.JAF_SpamOnce(targetPlayer, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_paintSpamBtn(false) end) then _paintSpamBtn(false) end
    end)
    local jafBtn = JUI.new("TextButton", proxCont, "Name", "JAFNightVisButton", "Size", UDim2.new(0, 64, 0, 32), "Position", UDim2.new(0, 348, 0.5, -16), "BackgroundColor3", Theme.SurfaceHighlight, "Text", "JAF", "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", Theme.TextPrimary)
    JUI.corner(jafBtn, 6)
    local jafBtnStroke = JUI.new("UIStroke", jafBtn, "Color", Theme.SurfaceHighlight, "Transparency", 0.3)
    local _jafBusy = false
    jafBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _jafBusy then return end
        if _G.JAF_SpamBusy and _G.JAF_SpamBusy() then return end
        if _G.isOnCooldown and _G.isOnCooldown("nightvision") then return end
        if not _G.runAdminCommand then return end
        local targetPlayer = nearestBaseOwner()
        if not targetPlayer then return end
        if not canUseAdminAction(targetPlayer) then return end
        _jafBusy = true
        _paintBtn(jafBtn, jafBtnStroke, true, Theme.Accent1, Theme.Accent2)
        task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local ok, res = pcall(_G.runAdminCommand, targetPlayer, "nightvision")
            if ok and res and _G.JAF_NoteCmdFired then _G.JAF_NoteCmdFired(targetPlayer, "nightvision") end
            _jafBusy = false
            if jafBtn.Parent then _paintBtn(jafBtn, jafBtnStroke, false, Theme.Accent1, Theme.Accent2) end
        end) end)
    local proxViz = nil
    local PROXVIZ_ROT = CFrame.Angles(0, 0, math.rad(90))
    local PROXVIZ_OFF = Vector3.new(0, -2.5, 0)
    local _proxVizR = false
    local updateProxViz = (function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if Config.ShowProxCircle then
            if not proxViz then
                proxViz = JUI.new("Part", nil, "Name", "XiProxViz", "Anchored", true, "CanCollide", false, "Shape", Enum.PartType.Cylinder, "Color", Theme.Accent1, "Transparency", 0.6, "CastShadow", false)
                proxViz.Parent = Workspace
                _proxVizR = false
            end
            local char = LocalPlayer.Character
            local hrp = char and char:FindFirstChild("HumanoidRootPart")
            if hrp then
                local r = Config.ProximityRange
                if r ~= _proxVizR then proxViz.Size = Vector3.new(0.5, r * 2, r * 2) _proxVizR = r end
                proxViz.CFrame = hrp.CFrame * PROXVIZ_ROT + PROXVIZ_OFF
            end
        else
            if proxViz then proxViz:Destroy(); proxViz = nil end
        end end)
    RunService.Heartbeat:Connect(updateProxViz)
    local function updateProximityAPButton() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if SharedState.ProximityAPButton then
            _paintBtn(SharedState.ProximityAPButton, SharedState.ProximityAPButtonStroke, ProximityAPActive, Theme.Accent1, Theme.Accent2)
        end end
    _G.JAF_ToggleProximityAP = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        ProximityAPActive = not ProximityAPActive
        updateProximityAPButton()
    end
    proxBtn.MouseButton1Click:Connect(_G.JAF_ToggleProximityAP)
    local tabBar = JUI.new("Frame", frame, "Size", UDim2.new(1, -20, 0, 28), "Position", UDim2.new(0, 10, 0, 114), "BackgroundTransparency", 1, "BorderSizePixel", 0)
    _P(Instance.new("UIListLayout", tabBar), "FillDirection", Enum.FillDirection.Horizontal, "Padding", UDim.new(0, 4), "SortOrder", Enum.SortOrder.LayoutOrder, "VerticalAlignment", Enum.VerticalAlignment.Center)
    local makeTabBtn; makeTabBtn = (function(parent, label, order) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local btn = JUI.new("TextButton", parent, "LayoutOrder", order, "Size", UDim2.new(0, 128, 0, 28), "AutoButtonColor", false, "Text", label, "Font", Enum.Font.GothamBold, "TextSize", 14, "BorderSizePixel", 0)
        JUI.corner(btn, 6)
        local s = JUI.new("UIStroke", btn, "Thickness", 1, "Transparency", 0.4)
        return btn, s
    end)
    local tabPlayers, tabPlayersStroke = makeTabBtn(tabBar, "PLAYERS", 1)
    local tabBlacklist, tabBlacklistStroke = makeTabBtn(tabBar, "SETTINGS", 2)
    local activeTab = "players"
    local function setActiveTab(name) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        activeTab = name
        local isP = (name == "players")
        _paintBtn(tabPlayers,   tabPlayersStroke,   isP,     Theme.Accent1, Theme.Accent1, Theme.Surface)
        _paintBtn(tabBlacklist, tabBlacklistStroke, not isP, Theme.Accent1, Theme.Accent2, Theme.Surface)
        listFrame.Visible = isP
        blFrame.Visible = not isP
    end
    tabPlayers.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
setActiveTab("players") end)
    tabBlacklist.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
setActiveTab("blacklist") end)
    blFrame = JUI.new("ScrollingFrame", frame, "Size", UDim2.new(1, -20, 1, -152), "Position", UDim2.new(0, 10, 0, 150), "BackgroundTransparency", 1, "BorderSizePixel", 0, "ZIndex", 5, "Active", true, "Visible", false, "ScrollBarThickness", 3, "ScrollBarImageColor3", Theme.TextSecondary, "CanvasSize", UDim2.new(0, 0, 0, 0), "AutomaticCanvasSize", Enum.AutomaticSize.Y)
    local blFrameLayout = JUI.new("UIListLayout", blFrame, "Padding", UDim.new(0, 5), "SortOrder", Enum.SortOrder.LayoutOrder)
    local _blAddRow
    do
        local function _mkSecHeader(text, order) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local r = JUI.new("Frame", blFrame, "Size", UDim2.new(1, 0, 0, 20), "BackgroundTransparency", 1, "LayoutOrder", order)
            local bar = JUI.new("Frame", r, "Size", UDim2.new(1, 0, 0, 1), "Position", UDim2.new(0, 0, 1, -1), "BackgroundColor3", Theme.SurfaceHighlight, "BorderSizePixel", 0)
            local tl = JUI.new("TextLabel", r, "Size", UDim2.new(1, 0, 1, 0), "BackgroundTransparency", 1, "Text", text, "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", Theme.TextSecondary, "TextXAlignment", Enum.TextXAlignment.Left)
            return r
        end
        _mkSecHeader("BLACKLIST", 40)
        _blAddRow = JUI.new("Frame", blFrame, "Name", "BLAddRow", "LayoutOrder", 41, "Size", UDim2.new(1, 0, 0, 28), "BackgroundTransparency", 1)
    end
    local blInput = JUI.new("TextBox", _blAddRow, "Size", UDim2.new(1, -58, 0, 26), "Position", UDim2.new(0, 0, 0, 0), "ZIndex", 6, "BackgroundColor3", Theme.Surface, "BorderSizePixel", 0, "Text", "", "PlaceholderText", "Roblox username...", "Font", Enum.Font.Gotham, "TextSize", 13, "TextColor3", Theme.TextPrimary, "PlaceholderColor3", Theme.TextSecondary, "ClearTextOnFocus", false)
    JUI.corner(blInput, 6)
    local blInputStroke = JUI.stroke(blInput, Theme.SurfaceHighlight, 1, 0.3)
    local blInputPad = JUI.new("UIPadding", blInput, "PaddingLeft", UDim.new(0, 8))
    local blAddBtn = JUI.new("TextButton", _blAddRow, "Size", UDim2.new(0, 50, 0, 26), "Position", UDim2.new(1, -50, 0, 0), "ZIndex", 6, "BackgroundColor3", Theme.Error, "Text", "ADD", "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", _G.JAF_OnAccent(), "AutoButtonColor", false)
    JUI.corner(blAddBtn, 6)
    local blAddStroke = JUI.stroke(blAddBtn, Theme.Error, 1, 0.4)
    local function rebuildBlList() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        for _, c in ipairs(blFrame:GetChildren()) do if c.Name == "BLEntry" then c:Destroy() end end
        local rowBg = Color3.fromRGB(math.floor(Theme.Error.R*255*0.35), math.floor(Theme.Error.G*255*0.35), math.floor(Theme.Error.B*255*0.35))
        local onErr = _G.JAF_OnAccent()
        for i, name in ipairs(BlacklistedPlayers) do
            local row = JUI.new("Frame", blFrame, "Name", "BLEntry", "LayoutOrder", 100 + i, "Size", UDim2.new(1, 0, 0, 28), "BackgroundColor3", rowBg, "BorderSizePixel", 0)
            JUI.corner(row, 6)
            local rowStroke = JUI.stroke(row, Theme.Error, 1, 0.5)
            local nameLabel = JUI.new("TextLabel", row, "Size", UDim2.new(1, -36, 1, 0), "Position", UDim2.new(0, 10, 0, 0), "BackgroundTransparency", 1, "Text", name, "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", Theme.TextPrimary, "TextXAlignment", Enum.TextXAlignment.Left)
            local removeBtn = JUI.new("TextButton", row, "Size", UDim2.new(0, 26, 0, 20), "Position", UDim2.new(1, -30, 0.5, -10), "BackgroundColor3", Theme.Error, "Text", "X", "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", onErr, "AutoButtonColor", false)
            JUI.corner(removeBtn, 4)
            local capName = name
            removeBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                for j, n in ipairs(BlacklistedPlayers) do
                    if n:lower() == capName:lower() then table.remove(BlacklistedPlayers, j) break end
                end
                Config.Blacklist = BlacklistedPlayers
                SaveConfig()
                rebuildBlList()
                if refreshBlacklistUI then refreshBlacklistUI() end
            end) end end
    local function addBlacklistUser(username) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        username = username:match("^%s*(.-)%s*$")
        if username == "" then return end
        local lower = username:lower()
        for _, n in ipairs(BlacklistedPlayers) do if n:lower() == lower then return end end
        table.insert(BlacklistedPlayers, username)
        Config.Blacklist = BlacklistedPlayers
        SaveConfig()
        rebuildBlList()
        if refreshBlacklistUI then refreshBlacklistUI() end
        blInput.Text = ""
    end
    blAddBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false)) addBlacklistUser(blInput.Text) end)
    blInput.FocusLost:Connect(function(enterPressed) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if enterPressed then addBlacklistUser(blInput.Text) end
    end)
    rebuildBlList()
    listFrame = JUI.new("ScrollingFrame", frame, "Size", UDim2.new(1, -20, 1, -152), "Position", UDim2.new(0, 10, 0, 150), "BackgroundTransparency", 1, "BorderSizePixel", 0, "ScrollBarThickness", 6, "ScrollBarImageColor3", Theme.Accent1, "ScrollBarImageTransparency", 0.3)
    layout = JUI.new("UIListLayout", listFrame, "Padding", UDim.new(0, 8), "SortOrder", Enum.SortOrder.LayoutOrder)
    setActiveTab("players")
    local getAdminPanelSortKey; getAdminPanelSortKey = (function(plr) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if not plr or not plr.Parent then return 3, 9999, "" end
        local stealing = plr:GetAttribute("Stealing")
        local brainrotName = plr:GetAttribute("StealingIndex")
        if not stealing then return 3, 9999, plr.Name or "" end
        if brainrotName then
            for i, pName in ipairs(PRIORITY_LIST) do if pName == brainrotName then return 1, i, plr.Name or "" end end
            return 2, 9999, plr.Name or ""
        end
        return 2, 9999, plr.Name or ""
    end)
    local function sortAdminPanelList() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not (listFrame.Visible and adminGui.Enabled and frame.Visible) then return end
        local rows = {}
        for _, child in ipairs(listFrame:GetChildren()) do
            if child:IsA("TextButton") and child.Name ~= "" then
                local plr = Players:FindFirstChild(child.Name)
                if plr then
                    local t, p, n = getAdminPanelSortKey(plr)
                    rows[#rows + 1] = {row = child, plr = plr, t = t, p = p, n = n or ""}
                end end end
        table.sort(rows, (function(a, b) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if a.t ~= b.t then return a.t < b.t end
            if a.p ~= b.p then return a.p < b.p end
            return a.n < b.n
        end))
        for i, entry in ipairs(rows) do if entry.row.LayoutOrder ~= i then entry.row.LayoutOrder = i end end
    end
    local function fireClick(button) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if not SAB_LIVE() then return end
        if button then
            if firesignal then
                firesignal(button.MouseButton1Click); firesignal(button.MouseButton1Down); firesignal(button.Activated)
            else
                local x = button.AbsolutePosition.X + (button.AbsoluteSize.X / 2)
                local y = button.AbsolutePosition.Y + (button.AbsoluteSize.Y / 2) + 58
                VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 0)
                VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 0)
            end end end
    local function runAdminCommand(targetPlayer, commandName, selfAction) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local _isSelf = (selfAction == true) and targetPlayer == LocalPlayer
        if not _isSelf then
            if targetPlayer and isBlacklisted(targetPlayer.Name) then return false end
            if targetPlayer then local aj = getAutojoinerName(targetPlayer.Name) if aj then return false end end
        end
        local realAdminGui = PlayerGui:WaitForChild("AdminPanel", 5)
        if not realAdminGui then return false end
        local profilesScroll = realAdminGui:WaitForChild("AdminPanel"):WaitForChild("Profiles"):WaitForChild("ScrollingFrame")
        local playerBtn = profilesScroll:FindFirstChild(targetPlayer.Name)
        if not playerBtn then return false end
        fireClick(playerBtn)
        task.wait(0.05)
        local contentScroll = realAdminGui.AdminPanel:WaitForChild("Content"):WaitForChild("ScrollingFrame")
        local cmdBtn = contentScroll:FindFirstChild(commandName)
        if not cmdBtn then return false end
        fireClick(cmdBtn)
        return true
    end
    _G.runAdminCommand = runAdminCommand
    local isOnCooldown
    local function getNextAvailableCommand() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        for _, cmd in ipairs(getOrderedCommands()) do if not isOnCooldown(cmd) then return cmd end end
        return nil
    end
    isOnCooldown = (function(cmd) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local t = hubFind("AdminPanel")
        t = t and t:FindFirstChild("AdminPanel"); t = t and t:FindFirstChild("Content")
        t = t and t:FindFirstChild("ScrollingFrame"); t = t and t:FindFirstChild(cmd)
        t = t and t:FindFirstChild("Timer")
        if t then return t.Visible end
        if not activeCooldowns[cmd] then return false end
        return (tick() - activeCooldowns[cmd]) < (COOLDOWNS[cmd] or 0)
    end)
    _G.isOnCooldown = isOnCooldown
    local function setGlobalVisualCooldown(cmd) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if SharedState.AdminButtonCache[cmd] then
            for _, b in ipairs(SharedState.AdminButtonCache[cmd]) do
                if b and b.Parent then
                    b.BackgroundColor3 = Theme.Error
                    task.delay(COOLDOWNS[cmd] or 5, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                        if b and b.Parent then
                            local hasBallooned = (cmd == "balloon" and SharedState.BalloonedPlayers and next(SharedState.BalloonedPlayers) ~= nil)
                            b.BackgroundColor3 = hasBallooned and Theme.Error or Theme.SurfaceHighlight
                        end end) end end end end
    _G.setGlobalVisualCooldown = setGlobalVisualCooldown
    _G.activeCooldowns = activeCooldowns
    local function updateBalloonButtons() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local hasBallooned = next(SharedState.BalloonedPlayers) ~= nil
        if SharedState.AdminButtonCache and SharedState.AdminButtonCache["balloon"] then
            for _, b in ipairs(SharedState.AdminButtonCache["balloon"]) do
                if b and b.Parent then b.BackgroundColor3 = hasBallooned and Theme.Error or Theme.SurfaceHighlight end
            end end end
    local getEnabledCommands = getOrderedCommands
    _G.getEnabledCommands = getEnabledCommands
    local function noteCmdFired(plr, cmd) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        activeCooldowns[cmd] = tick()
        setGlobalVisualCooldown(cmd)
        if cmd == "balloon" then SharedState.BalloonedPlayers[plr.UserId] = true updateBalloonButtons() end
    end
    _G.JAF_NoteCmdFired = noteCmdFired
    local function triggerAll(plr) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not canUseAdminAction(plr) then return end
        local count = 0
        for _, cmd in ipairs(buildCmdSequence(Config.AdminRandomCmd == true)) do
            if not isOnCooldown(cmd) then
                task.delay(count * 0.1, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    if runAdminCommand(plr, cmd) then noteCmdFired(plr, cmd) end
                end)
                count = count + 1
            end end end
    _G.triggerAll = triggerAll
    local _cycle = {}
    local function nextCycleCmd() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        for _ = 1, 2 do
            while #_cycle > 0 do local cmd = table.remove(_cycle, 1) if not isOnCooldown(cmd) then return cmd end end
            _cycle = buildCmdSequence(Config.AdminRandomCmd == true)
            if #_cycle == 0 then return nil end
        end
        return nil
    end
    local function fireRandomCmd(plr) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not canUseAdminAction(plr) then return end
        local cmd = nextCycleCmd()
        if not cmd then return end
        if runAdminCommand(plr, cmd) then noteCmdFired(plr, cmd) return cmd end
    end
    local function apAct(plr) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if Config.AdminRandomCmd then fireRandomCmd(plr) else triggerAll(plr) end
    end
    local _spamBusy = false
    local function spamBusy() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _spamBusy end
    local function spamOnce(plr, onDone) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _spamBusy then return false end
        if not plr or not plr.Parent then return false end
        if isBlacklisted(plr.Name) or getAutojoinerName(plr.Name) then return false end
        if not canUseAdminAction(plr) then return false end
        _spamBusy = true
        task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            for _, cmd in ipairs(buildCmdSequence(Config.AdminRandomCmd == true)) do
                if not isOnCooldown(cmd) then
                    local ok, res = pcall(runAdminCommand, plr, cmd)
                    if ok and res then noteCmdFired(plr, cmd) end
                    task.wait(0.15)
                end end
            _spamBusy = false
            if onDone then pcall(onDone) end
        end)
        return true
    end
    _G.JAF_SpamOnce = spamOnce
    _G.JAF_SpamBusy = spamBusy
    local function _slab(mn, mx, o, d) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local a, b = (mn - o) / d, (mx - o) / d
        if a > b then a, b = b, a end
        return a, b
    end
    local rayToCubeIntersect; rayToCubeIntersect = (function(rayOrigin, rayDirection, cubeCenter, cubeSize) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local halfSize = cubeSize / 2
        local minBounds = cubeCenter - Vector3.new(halfSize, halfSize, halfSize)
        local maxBounds = cubeCenter + Vector3.new(halfSize, halfSize, halfSize)
        local dx = rayDirection.X ~= 0 and rayDirection.X or 0.0001
        local dy = rayDirection.Y ~= 0 and rayDirection.Y or 0.0001
        local dz = rayDirection.Z ~= 0 and rayDirection.Z or 0.0001
        local tmin, tmax = _slab(minBounds.X, maxBounds.X, rayOrigin.X, dx)
        local tymin, tymax = _slab(minBounds.Y, maxBounds.Y, rayOrigin.Y, dy)
        if tmin > tymax or tymin > tmax then return false end
        if tymin > tmin then tmin = tymin end
        if tymax < tmax then tmax = tymax end
        local tzmin, tzmax = _slab(minBounds.Z, maxBounds.Z, rayOrigin.Z, dz)
        if tmin > tzmax or tzmin > tmax then return false end
        return true
    end)
    local _pickPlayerUnderRay; _pickPlayerUnderRay = (function(ray) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local hitboxSize, bestPlayer, bestDistance = 8, nil, math.huge
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Parent then
                local cubeCenter = p.Character.HumanoidRootPart.Position
                if rayToCubeIntersect(ray.Origin, ray.Direction, cubeCenter, hitboxSize) then
                    local distance = (ray.Origin - cubeCenter).Magnitude
                    if distance < bestDistance then bestDistance = distance; bestPlayer = p end
                end end end
        return bestPlayer
    end)
    local _hlParent = PlayerGui
    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_hlParent = game:GetService("CoreGui") end)
    local highlight = Instance.new("Highlight", _hlParent)
    _P(highlight, "FillColor", Theme.Error, "FillTransparency", 0.3, "OutlineColor", Theme.Error, "OutlineTransparency", 0, "Adornee", nil, "DepthMode", Enum.HighlightDepthMode.AlwaysOnTop)
    local _apHoverT = 0
    RunService.RenderStepped:Connect((function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local _now = tick()
        if _now - _apHoverT < 0.05 then return end
        _apHoverT = _now
        if Config.ClickToAP and UserInputService.MouseEnabled then
            local camera = Workspace.CurrentCamera
            local mousePos = UserInputService:GetMouseLocation()
            local ray = camera:ViewportPointToRay(mousePos.X, mousePos.Y)
            local bestPlayer = _pickPlayerUnderRay(ray)
            local newAdornee = bestPlayer and bestPlayer.Character or nil
            if highlight.Adornee ~= newAdornee then
                highlight.Adornee = newAdornee
                local _hoveredPlr = newAdornee and newAdornee.Parent and Players:GetPlayerFromCharacter(newAdornee)
                local hoveredName = _hoveredPlr and _hoveredPlr.Name or ""
                local hoverBg = Color3.fromRGB(math.floor(Theme.Error.R*255*0.35), math.floor(Theme.Error.G*255*0.35), math.floor(Theme.Error.B*255*0.35))
                for _, child in ipairs(listFrame:GetChildren()) do
                    if child:IsA("TextButton") then
                        local stroke = child:FindFirstChildOfClass("UIStroke")
                        if stroke then
                            local hot = newAdornee and child.Name == hoveredName
                            stroke.Color = hot and Theme.Error or Theme.Accent2
                            stroke.Transparency = hot and 0 or 0.7
                            child.BackgroundColor3 = hot and hoverBg or Theme.Surface
                        end end end end
        else
            highlight.Adornee = nil
        end end))
    UserInputService.InputBegan:Connect(function(inp, g) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local isTap = inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch
        if not g and isTap and Config.ClickToAP then
            local camera = Workspace.CurrentCamera
            local mousePos
            if inp.UserInputType == Enum.UserInputType.Touch then
                local ins = GuiService:GetGuiInset()
                mousePos = Vector2.new(inp.Position.X + ins.X, inp.Position.Y + ins.Y)
            else
                mousePos = UserInputService:GetMouseLocation()
            end
            local ray = camera:ViewportPointToRay(mousePos.X, mousePos.Y)
            local bestPlayer = _pickPlayerUnderRay(ray)
            if bestPlayer then
                if isBlacklisted(bestPlayer.Name) then return end
                local _clickAj = getAutojoinerName(bestPlayer.Name)
                if _clickAj then return end
                local nextCmd = getNextAvailableCommand()
                if nextCmd then
                    if Config.AdminRandomCmd then
                        fireRandomCmd(bestPlayer)
                    elseif Config.ClickToAPSingleCommand then
                        if runAdminCommand(bestPlayer, nextCmd) then noteCmdFired(bestPlayer, nextCmd) end
                    else
                        triggerAll(bestPlayer)
                    end
                else
                    local realAdminGui = PlayerGui:WaitForChild("AdminPanel", 5)
                    if realAdminGui then
                        local profilesScroll = realAdminGui:WaitForChild("AdminPanel"):WaitForChild("Profiles"):WaitForChild("ScrollingFrame")
                        local playerBtn = profilesScroll:FindFirstChild(bestPlayer.Name)
                        if playerBtn then fireClick(playerBtn) end
                    end end end end end)
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        while true do
            task.wait(0.2)
            if ProximityAPActive then
                local myChar = LocalPlayer.Character
                if myChar and myChar:FindFirstChild("HumanoidRootPart") then
                    for _, p in ipairs(Players:GetPlayers()) do
                        if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                            local dist = (p.Character.HumanoidRootPart.Position - myChar.HumanoidRootPart.Position).Magnitude
                            if dist <= Config.ProximityRange then
                                if not (isBlacklisted(p.Name) or getAutojoinerName(p.Name)) then
                                    if getNextAvailableCommand() then apAct(p) end
                                end end end end end end end end)
    local removePlayer
    local function _dropRow(row) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        for _, buttons in pairs(SharedState.AdminButtonCache) do
            for i = #buttons, 1, -1 do if buttons[i] and buttons[i]:IsDescendantOf(row) then table.remove(buttons, i) end end
        end
        row:Destroy()
    end
    local function _lit(c) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Color3.new(math.min(c.R*1.35,1), math.min(c.G*1.35,1), math.min(c.B*1.35,1)) end
    local ROW_CMDS = { {icon = "JAIL", cmd = "jail"}, {icon = "RAG", cmd = "ragdoll"}, {icon = "RKT", cmd = "rocket"}, {icon = "BLN", cmd = "balloon"} }
    local AJ_BADGE_COLORS = {
        Atlas      = Color3.fromRGB(255, 215,   0),
        Kawaifu    = Color3.fromRGB(255, 120, 200),
        Braintopia = Color3.fromRGB(170,  80, 255),
        FMLY       = Color3.fromRGB( 90, 220, 160),
        SON        = Color3.fromRGB( 90, 200, 220),
    }
    local createPlayerRow; createPlayerRow = (function(plr) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local row = JUI.new("TextButton", listFrame, "Name", plr.Name, "LayoutOrder", 0, "Size", UDim2.new(1, -4, 0, 68), "BackgroundColor3", Theme.Surface, "BackgroundTransparency", 0.05, "BorderSizePixel", 0, "AutoButtonColor", false, "Text", "")
        JUI.corner(row, 10)
        local rowStroke = JUI.stroke(row, Theme.Accent2, 1.5, 0.7)
        row.MouseEnter:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
row.BackgroundTransparency = 0.05; rowStroke.Transparency = 0.4; rowStroke.Color = Theme.Accent1 end)
        row.MouseLeave:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
row.BackgroundTransparency = 0.05; rowStroke.Transparency = 0.7; rowStroke.Color = Theme.Accent2 end)
        local dName = JUI.new("TextLabel", row, "Size", UDim2.new(1, -234, 0, 26), "Position", UDim2.new(0, 12, 0, 7), "BackgroundTransparency", 1, "RichText", true, "Text", plr.Name, "Font", Enum.Font.GothamBold, "TextSize", 16, "TextColor3", Theme.TextPrimary, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
        local NAME_MAX, NAME_MIN, BADGE_SIZE = 16, 13, 12
        local _badgeW, _fitKey, _lastBadgeAj = 0, nil, false
        local function _fitAdminName() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if not dName or not dName.Parent then return end
            local avail = dName.AbsoluteSize.X - _badgeW
            if avail <= 1 then return end
            local txt = plr.Name
            if txt == "" then return end
            local key = txt .. "|" .. tostring(math.floor(avail))
            if key == _fitKey then return end
            _fitKey = key
            pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local w = game:GetService("TextService"):GetTextSize(txt, NAME_MAX, dName.Font, Vector2.new(100000, 1000)).X
                dName.TextSize = (w <= avail) and NAME_MAX or math.clamp(math.floor(NAME_MAX * avail / w), NAME_MIN, NAME_MAX)
            end) end
        dName:GetPropertyChangedSignal("AbsoluteSize"):Connect(_fitAdminName)
        task.defer(_fitAdminName)
        local apBadgeLbl = JUI.new("TextLabel", row, "Name", "APBadgeLabel", "Size", UDim2.new(0, 138, 0, 18), "Position", UDim2.new(1, -146, 0, 38), "BackgroundTransparency", 1, "Font", Enum.Font.GothamBold, "TextSize", BADGE_SIZE, "TextColor3", Color3.fromRGB(255, 215, 0), "Text", "", "TextXAlignment", Enum.TextXAlignment.Right)
        local function _refreshAdminAtlasBadge() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if not dName or not dName.Parent then return end
            local _badgeAj = getAutojoinerName and getAutojoinerName(plr.Name)
            if _badgeAj == _lastBadgeAj then _fitAdminName() return end
            _lastBadgeAj = _badgeAj
            if _badgeAj then
                apBadgeLbl.Text = "[" .. string.upper(_badgeAj) .. " USER]"
                apBadgeLbl.TextColor3 = AJ_BADGE_COLORS[_badgeAj] or Color3.fromRGB(255, 215, 0)
            else
                apBadgeLbl.Text = ""
            end
            _fitKey = nil
            _fitAdminName()
        end
        _refreshAdminAtlasBadge()
        local apToolLbl = JUI.new("TextLabel", row, "Name", "APToolLabel", "Size", UDim2.new(1, -166, 0, 18), "Position", UDim2.new(0, 12, 0, 38), "BackgroundTransparency", 1, "Font", Enum.Font.GothamMedium, "TextSize", 13, "TextColor3", Theme.TextPrimary, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
        local function _scanHeldTool() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local ht = nil
            local c = plr.Character
            if c then for _, o in ipairs(c:GetChildren()) do if o:IsA("Tool") then ht = o.Name break end end end
            if ht and DANGER_TOOLS[ht] then dName.TextColor3 = Theme.TextSecondary else dName.TextColor3 = Theme.TextPrimary end
            local sIdx = plr:GetAttribute("StealingIndex")
            if plr:GetAttribute("Stealing") or sIdx then
                local raw = sIdx ~= nil and tostring(sIdx) or nil
                local disp = raw and ((_G.JAF_DisplayNameOf and _G.JAF_DisplayNameOf(raw)) or raw) or "Brainrot"
                apToolLbl.Text = "Stealing: " .. disp
                apToolLbl.Font = Enum.Font.GothamBold
                apToolLbl.TextColor3 = Theme.Accent1
            else
                apToolLbl.Text = ht or ""
                apToolLbl.Font = Enum.Font.GothamMedium
                apToolLbl.TextColor3 = Theme.TextPrimary
            end end
        _scanHeldTool()
        local btnCont = JUI.new("Frame", row, "Size", UDim2.new(0, 206, 1, 0), "Position", UDim2.new(1, -214, 0, 0), "BackgroundTransparency", 1, "ZIndex", 10)
        local _abMob = _G.JAF_MobileOn()
        local AB_H, AB_Y = _abMob and 26 or 22, _abMob and 4 or 10
        local _cmdBtns = {}
        for i, def in ipairs(ROW_CMDS) do
            local isOnCD = isOnCooldown(def.cmd)
            local b = JUI.new("TextButton", btnCont, "Size", UDim2.new(0, 38, 0, AB_H), "Position", UDim2.new(0, (i-1)*42, 0, AB_Y), "AutoButtonColor", false, "Text", def.icon, "TextSize", 12, "TextColor3", isOnCD and _G.JAF_OnAccent(Theme.Error) or Theme.TextPrimary, "Font", Enum.Font.GothamBold, "ZIndex", 11, "Active", true, "BackgroundColor3", isOnCD and Theme.Error or Theme.SurfaceHighlight, "BackgroundTransparency", 0)
            JUI.corner(b, 5)
            local bStroke = JUI.new("UIStroke", b, "Color", isOnCD and Theme.Error or Theme.Accent2, "Thickness", 1, "Transparency", 0.5, "ZIndex", 12)
            local function _cmdBusy() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                return isOnCooldown(def.cmd) or (def.cmd == "balloon" and SharedState.BalloonedPlayers and next(SharedState.BalloonedPlayers) ~= nil)
            end
            b.MouseEnter:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if not _cmdBusy() then b.BackgroundColor3 = _lit(Theme.SurfaceHighlight); bStroke.Transparency = 0.1 end
            end)
            b.MouseLeave:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if not _cmdBusy() then b.BackgroundColor3 = Theme.SurfaceHighlight; bStroke.Transparency = 0.5 end
            end)
            if not SharedState.AdminButtonCache[def.cmd] then SharedState.AdminButtonCache[def.cmd] = {} end
            table.insert(SharedState.AdminButtonCache[def.cmd], b)
            _cmdBtns[i] = {b = b, s = bStroke, icon = def.icon, busy = _cmdBusy}
            b.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if isBlacklisted(plr.Name) then return end
                if getAutojoinerName(plr.Name) then return end
                if runAdminCommand(plr, def.cmd) then
                    activeCooldowns[def.cmd] = tick()
                    setGlobalVisualCooldown(def.cmd)
                    if def.cmd == "balloon" then
                        SharedState.BalloonedPlayers[plr.UserId] = true
                        for _, btn in ipairs(SharedState.AdminButtonCache["balloon"] or {}) do
                            if btn and btn.Parent then btn.BackgroundColor3 = Theme.Error end
                        end end end end) end
        local allBtn = JUI.new("TextButton", btnCont, "Size", UDim2.new(0, 38, 0, AB_H), "Position", UDim2.new(0, 168, 0, AB_Y), "AutoButtonColor", false, "Text", "ALL", "TextSize", 12, "Font", Enum.Font.GothamBold, "TextColor3", Theme.Accent1, "ZIndex", 11, "Active", true, "BackgroundColor3", Theme.SurfaceHighlight, "BackgroundTransparency", 0)
        JUI.corner(allBtn, 5)
        local allStroke = JUI.new("UIStroke", allBtn, "Color", Theme.Accent2, "Thickness", 1, "Transparency", 0.4, "ZIndex", 12)
        local allHover = false
        allBtn.MouseEnter:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            allHover = true
            allBtn.BackgroundColor3 = _lit(Theme.SurfaceHighlight); allStroke.Transparency = 0.1
        end)
        allBtn.MouseLeave:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            allHover = false
            allBtn.BackgroundColor3 = Theme.SurfaceHighlight; allStroke.Transparency = 0.4
        end)
        allBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if isBlacklisted(plr.Name) then return end
            if getAutojoinerName(plr.Name) then return end
            for _, cmd in ipairs(getEnabledCommands()) do if not isOnCooldown(cmd) then triggerAll(plr) return end end
        end)
        task.spawn((function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            while row.Parent do
                task.wait(0.5)
                if not plr or not plr.Parent or not Players:FindFirstChild(plr.Name) then removePlayer(plr) break end
                if not (listFrame.Visible and adminGui.Enabled and frame.Visible) then continue end
                pcall(_scanHeldTool)
                pcall(_refreshAdminAtlasBadge)
                for _, e in ipairs(_cmdBtns) do
                    local b, s, hot = e.b, e.s, e.busy()
                    b.BackgroundColor3 = hot and Theme.Error or Theme.SurfaceHighlight
                    b.BackgroundTransparency = 0
                    s.Color = hot and Theme.Error or Theme.Accent2
                    s.Transparency = hot and 0.2 or 0.5
                    b.TextColor3 = hot and _G.JAF_OnAccent(Theme.Error) or Theme.TextPrimary
                    if b.Text ~= e.icon then _P(b, "Text", e.icon, "TextSize", 12, "Font", Enum.Font.GothamBold) end
                end
                allBtn.TextColor3 = Theme.Accent1
                if not allHover then allBtn.BackgroundColor3 = Theme.SurfaceHighlight allStroke.Color = Theme.Accent2 end
            end end))
        local rowHighlight = JUI.new("Frame", row, "Size", UDim2.new(1, 0, 1, 0), "BackgroundColor3", Theme.Accent1, "BackgroundTransparency", 1, "BorderSizePixel", 0, "ZIndex", 1)
        JUI.corner(rowHighlight, 6)
        row.MouseEnter:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
rowHighlight.BackgroundTransparency = 0.7 end)
        row.MouseLeave:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
rowHighlight.BackgroundTransparency = 1 end)
        return row
    end)
    local playerRows, playerRowsByUserId = {}, {}
    local function addPlayer(plr) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if plr == LocalPlayer or playerRowsByUserId[plr.UserId] then return end
        if not Players:FindFirstChild(plr.Name) then return end
        if playerRows[plr] then return end
        local row = createPlayerRow(plr)
        playerRows[plr] = row
        playerRowsByUserId[plr.UserId] = {player = plr, row = row}
        listFrame.CanvasSize = UDim2.new(0,0,0, layout.AbsoluteContentSize.Y + 30)
        sortAdminPanelList()
    end
    removePlayer = function(plr) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local userId = plr and plr.UserId or nil
        local entry = userId and playerRowsByUserId[userId] or nil
        local row = entry and entry.row or playerRows[plr]
        if row then
            if row.Parent then _dropRow(row) end
            if plr then playerRows[plr] = nil end
            if userId then playerRowsByUserId[userId] = nil end
            if SharedState.BalloonedPlayers and userId then SharedState.BalloonedPlayers[userId] = nil end
            listFrame.CanvasSize = UDim2.new(0,0,0, layout.AbsoluteContentSize.Y + 30)
        end end
    refreshBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        for _, row in pairs(playerRows) do if row and row.Parent then _dropRow(row) end end
        playerRows = {}
        playerRowsByUserId = {}
        SharedState.AdminButtonCache = {}
        SharedState.BalloonedPlayers = {}
        for _, child in ipairs(listFrame:GetChildren()) do if child:IsA("TextButton") then child:Destroy() end end
        task.wait(0.1)
        for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer then addPlayer(p) end end
        sortAdminPanelList()
    end)
    Players.PlayerAdded:Connect(function(plr) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        task.wait(0.1)
        if plr and plr.Parent then addPlayer(plr) end
    end)
    Players.PlayerRemoving:Connect(function(plr) LPH_ATTRIBUTES(ERROR_HANDLING(false))
removePlayer(plr) end)
    for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer then addPlayer(p) end end
    sortAdminPanelList()
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        while listFrame and listFrame.Parent do task.wait(0.5) pcall(sortAdminPanelList) end
    end)
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        while true do
            task.wait(1)
            local currentPlayerIds = {}
            for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer and p.Parent then currentPlayerIds[p.UserId] = true end end
            for userId, entry in pairs(playerRowsByUserId) do
                if not currentPlayerIds[userId] or not entry.player or not entry.player.Parent or not Players:FindFirstChild(entry.player.Name) then
                    removePlayer(entry.player)
                end end
            for _, p in ipairs(Players:GetPlayers()) do
                if p ~= LocalPlayer and p.Parent and not playerRowsByUserId[p.UserId] then addPlayer(p) end
            end end end)
    layout.Changed:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
listFrame.CanvasSize = UDim2.new(0,0,0, layout.AbsoluteContentSize.Y + 30) end)
    if _G.JAF_ThemeImageAsset and jafApplyThemeImageToPanels then
        task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(jafApplyThemeImageToPanels, _G.JAF_ThemeImageAsset) end)
    end end)
;(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
local T = { Ok   = Color3.fromRGB(100, 255, 160), Warn = Color3.fromRGB(255, 210,  80), Err  = Color3.fromRGB(255,  80,  80), }
local MUTCOL = {
    None        = Color3.fromRGB(160, 160, 160),
    Cursed      = Color3.fromRGB(180,   0, 255),
    Gold        = Color3.fromRGB(255, 215,   0),
    Diamond     = Color3.fromRGB(100, 200, 255),
    YinYang     = Color3.fromRGB(200, 200, 200),
    Rainbow     = Color3.fromRGB(255, 100, 200),
    Lava        = Color3.fromRGB(255,  80,  20),
    Candy       = Color3.fromRGB(255, 150, 200),
    Bloodrot    = Color3.fromRGB(200,  20,  20),
    Radioactive = Color3.fromRGB(120, 255,  50),
    Divine      = Color3.fromRGB(255, 230, 100),
    Crystal     = Color3.fromRGB(150, 240, 255),
    Phantom     = Color3.fromRGB(150, 130, 210),
    Cyber       = Color3.fromRGB( 40, 255, 190),
    Galaxy      = Color3.fromRGB(130,  90, 255),
}
function _G.JAF_MutColor(m) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return (m and MUTCOL[m]) or Color3.fromRGB(212, 140, 255) end
local _rarityMap
function _G.JAF_RarityOf(nameOrIndex) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not nameOrIndex then return nil end
    if not _rarityMap then
        _rarityMap = {}
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local AD = require(ReplicatedStorage:WaitForChild("Datas"):WaitForChild("Animals"))
            for idx, d in pairs(AD) do
                if type(d) == "table" and d.Rarity then
                    _rarityMap[idx] = d.Rarity
                    if d.DisplayName then _rarityMap[d.DisplayName] = d.Rarity end
                end end end) end
    return _rarityMap[nameOrIndex]
end
local _dispMap
function _G.JAF_DisplayNameOf(indexKey) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not indexKey then return nil end
    if not _dispMap then
        _dispMap = {}
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local AD = require(ReplicatedStorage:WaitForChild("Datas"):WaitForChild("Animals"))
            for idx, d in pairs(AD) do
                if type(d) == "table" and d.DisplayName and d.DisplayName ~= idx then
                    _dispMap[idx] = d.DisplayName
                    _dispMap[tostring(idx):lower()] = d.DisplayName
                end end end) end
    return _dispMap[indexKey] or _dispMap[tostring(indexKey):lower()]
end
local _RARCOL = {
    secret           = Color3.fromRGB(255,  70, 180),
    ["brainrot god"] = Color3.fromRGB(255,  80,  80),
    mythic           = Color3.fromRGB(255, 120,  40),
    legendary        = Color3.fromRGB(255, 200,  50),
    epic             = Color3.fromRGB(180,  90, 255),
    rare             = Color3.fromRGB( 80, 150, 255),
    og               = Color3.fromRGB( 70, 230, 230),
    common           = Color3.fromRGB(185, 185, 195),
    admin            = Color3.fromRGB(255,  90,  90),
    easter           = Color3.fromRGB(255, 180, 220),
    festive          = Color3.fromRGB( 90, 220, 140),
    spooky           = Color3.fromRGB(255, 140,  40),
    ["st patrick's"] = Color3.fromRGB( 60, 200,  90),
    taco             = Color3.fromRGB(255, 180,  60),
    valentines       = Color3.fromRGB(255, 100, 150),
}
function _G.JAF_RarityColor(r) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return (r and _RARCOL[tostring(r):lower()]) or Color3.fromRGB(200, 200, 210) end
local SAVE_FILE = "SammydawgHub_TPSteal.json"
do
    if type(isfile) == "function" and type(readfile) == "function" and type(writefile) == "function" then
        if not isfile(SAVE_FILE) then
            for _, old in ipairs({ "SammydawgHub_TPSteal_v1.json", "JustAFanTPSteal_v1.json" }) do
                if isfile(old) then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
writefile(SAVE_FILE, readfile(old)) end); break end
            end end end end
if not SharedState.AllAnimalsCache then SharedState.AllAnimalsCache = {} end
if not SharedState.WalkingCache     then SharedState.WalkingCache    = {} end
local stealNearest = Config.StealNearest or false
_G.isCloning       = false
local _panelRefs   = {}
local _cfgLoaded   = false
SharedState.SetManualTarget = function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
manualTarget = v end
local BASES_HIGH = {
    [1]=Vector3.new(-476.47,20.73,220.94), [5]=Vector3.new(-342.54,20.70,221.45),
    [2]=Vector3.new(-476.57,20.71,113.77), [6]=Vector3.new(-342.86,20.67,113.41),
    [3]=Vector3.new(-476.87,20.74,6.18),   [7]=Vector3.new(-342.42,20.69,6.25),
    [4]=Vector3.new(-476.63,20.74,-101.07),[8]=Vector3.new(-342.79,20.75,-99.73),
}
local CLONE_FLOOR = {
    Vector3.new(-476,-4,221),Vector3.new(-476,-4,114),
    Vector3.new(-476,-4,7),  Vector3.new(-476,-4,-100),
    Vector3.new(-342,-4,-100),Vector3.new(-342,-4,6),
    Vector3.new(-342,-4,114),Vector3.new(-342,-4,220),
}
if not PRIORITY_LIST or #PRIORITY_LIST == 0 then
    PRIORITY_LIST = {
        "Headless Horseman",
        "Signore Carapace",
        "Elefanto Frigo",
        "Arcadragon",
        "Strawberry Elephant",
        "Antonio",
        "John Pork",
        "Meowl",
        "Love Love Bear",
        "Skibidi Toilet",
        "Ginger Gerat",
        "Griffin",
        "Dragon Gingerini",
        "Fishino Clownino",
        "La Supreme Combinasion",
        "Digi Narwhal",
        "Dragon Cannelloni",
        "Hydra Dragon Cannelloni",
        "Ketupat Bros",
        "La Casa Boo",
        "Hydra Bunny",
        "Duggy Bros",
        "Bunny and Eggy",
        "Cerberus",
        "Celestial Pegasus",
        "Rosey and Teddy",
        "Reinito Sleighito",
        "Capitano Moby",
        "Los Sekolahs",
        "Fragrama and Chocrama",
        "Spooky and Pumpky",
        "Cooki and Milki",
        "La Food Combinasion",
        "Los Amigos",
        "Burguro And Fryuro",
        "Popcuru and Fizzuru",
        "Garama and Madundung",
        "La Secret Combinasion",
        "La Romantic Grande",
        "La Taco Combinasion",
        "Los Spaghettis",
        "Swaggy Bros",
        "Sammyni Fattini",
        "Festive 67",
        "Ketchuru and Musturu",
        "Tang Tang Keletang",
        "Ketupat Kepat",
        "Tictac Sahur",
        "Tralaledon",
        "W or L",
        "Eviledon",
        "Lavadorito Spinito",
        "Spaghetti Tualetti",
        "Foxini Lanternini",
    } end
local isTpMoving      = false
task.spawn((function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
local _hb = RunService.Heartbeat while true do _G.JAF_TpActive = isTpMoving; _hb:Wait() end end))
SharedState.AllAnimalsCache = {}
SharedState.WalkingCache = {}
_G.JAF_MachineBlocked = function(ad) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if _G.JAF_SkipMachinePets == false then return false end
    if type(ad) ~= "table" then return false end
    local m = ad.Machine
    if type(m) ~= "table" then return false end
    if m.Active then return true end
    if m.Type == "Fuse" then return _G.JAF_SkipInFusePets == true end
    return _G.JAF_SkipIdleMachinePets == true
end
local function _animalSlotEntry(a, memo) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local al = memo and memo[a.plot]
    if al == nil then
        local Sync = _G.JAF_GetSynchronizer and _G.JAF_GetSynchronizer()
        if not Sync then return nil, "noSync" end
        local ok, ch = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Sync:Get(a.plot) end)
        if ok and ch then
            local ok2, list = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return ch:Get("AnimalList") end)
            al = (ok2 and type(list) == "table") and list or false
        else
            al = false
        end
        if memo then memo[a.plot] = al end
    end
    if al == false then return nil, "noChan" end
    local e = al[a.slot]
    if e == nil then local n = tonumber(a.slot); if n ~= nil then e = al[n] end end
    return e
end
_G.JAF_PetAlive = (function(a, memo) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if _G.JAF_TpAliveCheck == false then return true, "off" end
    if type(a) ~= "table" then return true, "notEntry" end
    if a.isWalking then if a.model and a.model.Parent then return true, "model" end return false, "modelGone" end
    local ow = a.owner
    if type(ow) == "string" and ow ~= "" and ow ~= "?" and not Players:FindFirstChild(ow) then return false, "ownerLeft" end
    if type(a.plot) ~= "string" or a.slot == nil then return true, "noKey" end
    local e, why = _animalSlotEntry(a, memo)
    if why then return true, why end
    if type(e) ~= "table" then return false, "slotEmpty" end
    if _G.JAF_MachineBlocked(e) then return false, "machine" end
    return true, "ok"
end)
local _stealSpecies, _stealSpeciesAt = {}, 0
_G.JAF_StealSpeciesSet = (function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local ttl = tonumber(_G.JAF_StealSetTTL) or 0.25
    if (tick() - _stealSpeciesAt) < ttl then return _stealSpecies end
    _stealSpeciesAt = tick()
    for k in pairs(_stealSpecies) do _stealSpecies[k] = nil end
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then
            local idx = p:GetAttribute("StealingIndex")
            if idx ~= nil and idx ~= "" then
                local s = tostring(idx):lower()
                _stealSpecies[s] = true
                _stealSpecies[(s:gsub("%s+", ""))] = true
                local dn = _G.JAF_DisplayNameOf and _G.JAF_DisplayNameOf(tostring(idx))
                if dn then local d = tostring(dn):lower() _stealSpecies[d] = true _stealSpecies[(d:gsub("%s+", ""))] = true end
            end end end
    return _stealSpecies
end)
_G.JAF_IsBeingStolen = (function(a, memo) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if _G.JAF_StolenDetect == false then return false end
    if type(a) ~= "table" or a.isWalking then return false end
    if type(a.plot) ~= "string" or a.slot == nil then return false end
    local e, why = _animalSlotEntry(a, memo)
    if why then return false end
    if type(e) == "table" then
        local s = e.Steal
        if type(s) ~= "number" then return false end
        if s == LocalPlayer.UserId then return false end
        for _, p in ipairs(Players:GetPlayers()) do if p.UserId == s then return p:GetAttribute("Stealing") == true end end
        return false
    end
    local nm = a.nameLower or (a.name and tostring(a.name):lower())
    if not nm then return false end
    local set = _G.JAF_StealSpeciesSet()
    if set[nm] or set[(nm:gsub("%s+", ""))] then return true end
    return false
end)
task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if _G.JAF_NoSynchronizer == true then return end
        local Pkgs = ReplicatedStorage:WaitForChild("Packages", 10); if not Pkgs then return end
        local Sync = _G.JAF_GetSynchronizer(); if not Sync then return end
        local AData = require(ReplicatedStorage:WaitForChild("Datas"):WaitForChild("Animals"))
        local AShared = require(ReplicatedStorage:WaitForChild("Shared"):WaitForChild("Animals"))
        local NUtils = require(ReplicatedStorage:WaitForChild("Utils"):WaitForChild("NumberUtils"))
        local plots = Workspace:WaitForChild("Plots", 10); if not plots then return end
        local function scanOnce(plot) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local ok, ch = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Sync:Get(plot.Name) end)
            if not ok or not ch then return end
            local al = ch:Get("AnimalList"); if not al then return end
            local owner = ch:Get("Owner"); if not owner then return end
            local on = _G.JAF_OwnerName(owner) or "?"
            if not Players:FindFirstChild(on) then return end
            for i = #SharedState.AllAnimalsCache, 1, -1 do
                if SharedState.AllAnimalsCache[i].plot == plot.Name then table.remove(SharedState.AllAnimalsCache, i) end
            end
            for slot, ad in pairs(al) do
                if type(ad) == "table" and not _G.JAF_MachineBlocked(ad) then
                    local inf = AData[ad.Index]
                    if inf then
                        local mut = ad.Mutation or "None"; if mut == "Yin Yang" then mut = "YinYang" end
                        local gv = _G.JAF_SafeGen(ad.Index, ad.Mutation, ad.Traits, nil)
                        local _nm = inf.DisplayName or ad.Index
                        table.insert(SharedState.AllAnimalsCache, {
                            name = _nm, nameLower = string.lower(_nm), genText = "$"..NUtils:ToString(gv).."/s",
                            genValue = gv, mutation = mut, mutLower = string.lower(mut), owner = on, plot = plot.Name, rarity = inf.Rarity,
                            slot = tostring(slot), uid = plot.Name.."_"..tostring(slot),
                        }) end end end
            table.sort(SharedState.AllAnimalsCache, function(a, b) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return a.genValue > b.genValue end)
        end
        SharedState.ScanAllPlots = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local fresh = {}
            local _inst = nil
            if _G.JAF_ScanViaInstances ~= false and _G.JAF_InstanceChans then
                local _iok, _im = pcall(_G.JAF_InstanceChans)
                if _iok and type(_im) == "table" then _inst = _im end
            end
            for _, _p in ipairs(plots:GetChildren()) do
                local _al, _owner, _resolved = nil, nil, false
                local _ic = _inst and _inst[_p.Name]
                if _ic then
                    local _ct = rawget(_ic, "CacheTable")
                    if type(_ct) == "table" then
                        _al    = _G.JAF_AnimalsOf(_ct)
                        _owner = rawget(_ct, "Owner")
                        _resolved = true
                        _G.JAF_ScanViaInst = (tonumber(_G.JAF_ScanViaInst) or 0) + 1
                    end end
                local ch
                if not _resolved then
                    for _try = 1, (_G.JAF_ScanPlotTries or 3) do
                        local ok, r = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Sync:Get(_p.Name) end)
                        if ok and r then ch = r; break end
                        task.wait()
                    end
                    if ch then _G.JAF_ScanViaSync = (tonumber(_G.JAF_ScanViaSync) or 0) + 1 end
                end
                if _resolved or ch then
                  local _pok = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    local al    = _al    or (ch and ch:Get("AnimalList"))
                    local owner = _owner or (ch and ch:Get("Owner"))
                    local on = _G.JAF_OwnerName(owner)
                    if al and on and Players:FindFirstChild(on) then
                        for slot, ad in pairs(al) do
                            if type(ad) == "table" and ad.Index and not _G.JAF_MachineBlocked(ad) then
                                local inf = AData[ad.Index]
                                if inf then
                                    local mut = ad.Mutation or "None"; if mut == "Yin Yang" then mut = "YinYang" end
                                    local gv = _G.JAF_SafeGen(ad.Index, ad.Mutation, ad.Traits, nil)
                                    local _nm = inf.DisplayName or ad.Index
                                    fresh[#fresh + 1] = {
                                        name = _nm, nameLower = string.lower(_nm), genText = "$"..NUtils:ToString(gv).."/s",
                                        genValue = gv, mutation = mut, mutLower = string.lower(mut), owner = on, plot = _p.Name, rarity = inf.Rarity,
                                        slot = tostring(slot), uid = _p.Name.."_"..tostring(slot),
                                    } end end end end end)
                  if not _pok then _G.JAF_ScanAllErr = (_G.JAF_ScanAllErr or 0) + 1 end
                end end
            table.sort(fresh, function(a, b) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return a.genValue > b.genValue end)
            local cache = SharedState.AllAnimalsCache
            if cache then
                table.clear(cache)
                for i = 1, #fresh do cache[i] = fresh[i] end
            else
                SharedState.AllAnimalsCache = fresh
            end end
        local _t0        = os.clock()
        local _deadline  = _t0 + (tonumber(_G.JAF_ScanBootMax) or 20)
        local _pass      = 0
        repeat
            _pass = _pass + 1
            local _slice = os.clock()
            for _, p in ipairs(plots:GetChildren()) do
                pcall(scanOnce, p)
                if (os.clock() - _slice) >= 0.004 then RunService.Heartbeat:Wait() _slice = os.clock() end
            end
            _G.JAF_ScanBootPasses = _pass
            if #SharedState.AllAnimalsCache > 0 then break end
            task.wait(tonumber(_G.JAF_ScanBootPoll) or 0.05)
        until os.clock() > _deadline
        _G.JAF_ScanBootMs    = (os.clock() - _t0) * 1000
        _G.JAF_ScanBootCount = #SharedState.AllAnimalsCache
    end) end)
_G.isCloning          = false
local HttpService     = game:GetService("HttpService")
_G.JAF_PlotsTotal = 0
_G.JAF_PlotsScanned = 0
local getChar; getChar = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local c = LocalPlayer.Character
    return c, c and c:FindFirstChild("HumanoidRootPart"), c and c:FindFirstChildOfClass("Humanoid")
end
local _signCache, _signCacheAt = nil, 0
local function _rebuildSignCache() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local out = {}
    for _, label in ipairs(Workspace:GetDescendants()) do
        if label.ClassName == "TextLabel" then
            local txt = label.Text
            if type(txt) == "string" and #txt >= 4
               and (txt:find("base", 1, true) or txt:find("Base", 1, true) or txt:lower():find("base", 1, true)) then
                local gui = label:FindFirstAncestorWhichIsA("SurfaceGui")
                if gui then local part = gui.Adornee or gui.Parent if part and part:IsA("BasePart") then out[#out + 1] = part end end
            end end end
    _signCache, _signCacheAt = out, tick()
    return out
end
local function getClosestBaseSign(brainrotPart) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if not brainrotPart or not brainrotPart:IsA("BasePart") then return nil end
    local list
    if _G.JAF_NoSignCache == true then
        list = _rebuildSignCache()
    else
        list = _signCache
        local _want, _have = 0, 0
        if _G.JAF_SignCachePartialGuard ~= false then
            local _pf0 = Workspace:FindFirstChild("Plots")
            if _pf0 then
                _want = #_pf0:GetChildren()
                if list then
                    local _seen = {}
                    for i = 1, #list do
                        local _s = list[i]
                        if _s.Parent then
                            local _n = _s
                            while _n and _n.Parent do
                                if _n.Parent == _pf0 then if not _seen[_n] then _seen[_n] = true; _have = _have + 1 end break end
                                _n = _n.Parent
                            end end end end end end
        local _age = tick() - _signCacheAt
        if not list or #list == 0
           or (_have < _want and _age > (tonumber(_G.JAF_SignCachePartialTTL) or 0.5))
           or _age > (tonumber(_G.JAF_SignCacheTTL) or 15) then
            list = _rebuildSignCache()
        end end
    local _pf = Workspace:FindFirstChild("Plots")
    local _myPlot
    if _pf then
        local node = brainrotPart
        while node and node.Parent do if node.Parent == _pf then _myPlot = node; break end node = node.Parent end
    end
    local closestPart, closestDist = nil, math.huge
    local ownPart, ownDist = nil, math.huge
    for i = 1, #list do
        local part = list[i]
        if part.Parent then
            local dist = (part.Position - brainrotPart.Position).Magnitude
            if dist < closestDist then closestDist = dist closestPart = part end
            if _myPlot and dist < ownDist then
                local n2 = part
                while n2 and n2.Parent do
                    if n2.Parent == _pf then if n2 == _myPlot then ownDist = dist; ownPart = part end break end
                    n2 = n2.Parent
                end end end end
    if not ownPart and _myPlot and _G.JAF_SignOwnPlot ~= false then
        local _ps = _myPlot:FindFirstChild("PlotSign")
        if _ps then
            local _cand
            if _ps:IsA("BasePart") then _cand = _ps end
            for _, d in ipairs(_ps:GetDescendants()) do
                if d:IsA("SurfaceGui") then local _a = d.Adornee or d.Parent if _a and _a:IsA("BasePart") then _cand = _a; break end end
            end
            if _cand then ownPart = _cand end
        end end
    if ownPart and _G.JAF_SignOwnPlot ~= false then return ownPart end
    return closestPart
end
local function getSignInwardDir(signPart) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if not (signPart and signPart:IsA("BasePart")) then return Vector3.new(1, 0, 0) end
    local outward
    local gui = signPart:FindFirstChildWhichIsA("SurfaceGui", true)
    if gui then
        outward = signPart.CFrame:VectorToWorldSpace(Vector3.FromNormalId(gui.Face))
    else
        local s, cf = signPart.Size, signPart.CFrame
        if s.Z <= s.X and s.Z <= s.Y then outward = cf.LookVector
        elseif s.X <= s.Y then outward = cf.RightVector
        else outward = cf.UpVector end
    end
    outward = Vector3.new(outward.X, 0, outward.Z)
    if outward.Magnitude < 0.01 then return Vector3.new(1, 0, 0) end
    local intoBase = -outward.Unit
    if _G.JAF_FlipInto then intoBase = -intoBase end
    return intoBase
end
_G.JAF_HB = _G.JAF_HB or {}
_G.JAF_HB.width  = _G.JAF_HB.width  or 58
_G.JAF_HB.depth  = _G.JAF_HB.depth  or 88
_G.JAF_HB.height = _G.JAF_HB.height or 30
_G.JAF_HB.margin = _G.JAF_HB.margin or 28
_G.JAF_HB.podiumRange = _G.JAF_HB.podiumRange or 75
_G.JAF_HB.box = nil
do local _v = Workspace:FindFirstChild("JAF_HB_Viz"); if _v then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_v:Destroy() end) end end
function _G.JAF_HB.clear() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G.JAF_HB.box = nil end
local _jafHbBuild = (function(signPart, petPos) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not petPos then return end
    _G.JAF_HB.clear()
    local plot
    do
        local node = signPart
        while node and node ~= Workspace do
            if node:FindFirstChild("StealHitbox") or node:FindFirstChild("AnimalPodiums") then plot = node; break end
            node = node.Parent
        end end
    local mnx,mny,mnz =  math.huge, math.huge, math.huge
    local mxx,mxy,mxz = -math.huge,-math.huge,-math.huge
    local n = 0
    if plot then
        local _podMemo = {}
        for _, p in ipairs(plot:GetDescendants()) do
            if p:IsA("BasePart") and p.Anchored and p.Transparency < 1 then
                local par  = p.Parent
                local skip = _podMemo[par]
                if skip == nil then
                    skip = false
                    local anc = par
                    while anc and anc ~= plot do if anc.Name == "AnimalPodiums" then skip = true; break end anc = anc.Parent end
                    if par ~= nil then _podMemo[par] = skip end
                end
                local q = not skip and p.Position or nil
                if q and math.abs(q.X) < 50000 and math.abs(q.Y) < 50000 and math.abs(q.Z) < 50000 then
                    local s = p.Size
                    local hx, hy, hz = s.X*0.5, s.Y*0.5, s.Z*0.5
                    local pcx,pcy,pcz, r00,r01,r02, r10,r11,r12, r20,r21,r22 = p.CFrame:GetComponents()
                    local ex = math.abs(r00)*hx + math.abs(r01)*hy + math.abs(r02)*hz
                    local ey = math.abs(r10)*hx + math.abs(r11)*hy + math.abs(r12)*hz
                    local ez = math.abs(r20)*hx + math.abs(r21)*hy + math.abs(r22)*hz
                    if pcx-ex < mnx then mnx = pcx-ex end
                    if pcx+ex > mxx then mxx = pcx+ex end
                    if pcy-ey < mny then mny = pcy-ey end
                    if pcy+ey > mxy then mxy = pcy+ey end
                    if pcz-ez < mnz then mnz = pcz-ez end
                    if pcz+ez > mxz then mxz = pcz+ez end
                    n = n + 1
                end end end end
    if n == 0 then
        local w, d, h = _G.JAF_HB.width or 58, _G.JAF_HB.depth or 88, _G.JAF_HB.height or 30
        local _f2u = _G.JAF_F2UnderActive and _G.JAF_F2UnderActive(petPos.Y)
        local fY = (petPos.Y > 10 and not _f2u)
            and (signPart and signPart:IsA("BasePart") and signPart.Position.Y + 4 or petPos.Y - 8)
            or -4.8
        _G.JAF_HB.box = { cf = CFrame.new(petPos.X, fY + h*0.5, petPos.Z), size = Vector3.new(w, h, d) }
        return
    end
    local m = _G.JAF_HB.margin or 3
    mnx, mxx, mnz, mxz = mnx-m, mxx+m, mnz-m, mxz+m
    _G.JAF_HB.box = { cf   = CFrame.new((mnx+mxx)*0.5, (mny+mxy)*0.5, (mnz+mxz)*0.5), size = Vector3.new(mxx-mnx, mxy-mny, mxz-mnz), }
end)
_G.JAF_HB.build = _jafHbBuild
function _G.JAF_HB.contains(pos) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
    local b = _G.JAF_HB.box
    if not b then return nil end
    local fps = _G.JAF_BaseFootprints
    if fps then
        local ax, az = b.cf.Position.X, b.cf.Position.Z
        for _, f in ipairs(fps) do
            if ax >= f.minX and ax <= f.maxX and az >= f.minZ and az <= f.maxZ then
                local xz   = _G.JAF_HB.gateXZ    or 4
                local up   = _G.JAF_HB.knockUp   or 15
                local down = _G.JAF_HB.knockDown or 6
                return pos.X >= f.minX - xz and pos.X <= f.maxX + xz
                       and pos.Z >= f.minZ - xz and pos.Z <= f.maxZ + xz
                       and pos.Y >= f.minY - down and pos.Y <= f.maxY + up
            end end end
    local rel = b.cf:PointToObjectSpace(pos)
    local h = b.size * 0.5
    return math.abs(rel.X) <= h.X and math.abs(rel.Y) <= h.Y and math.abs(rel.Z) <= h.Z
end
local antiDieDisabled = false
local antiDieConn     = nil
_G._jafRespawning = _G._jafRespawning or false
_G._jafForceRespawn = function(reason) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if _G._jafRespawning then return end
    _G._jafRespawning = true
    task.spawn(function() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.tpAntiDieDisable then _G.tpAntiDieDisable() end end)
        local _origChar = LocalPlayer.Character
        local _newChar = nil
        for _ = 1, 12 do
            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
LocalPlayer:LoadCharacter() end)
            task.wait(0.1)
            if LocalPlayer.Character and LocalPlayer.Character ~= _origChar then _newChar = LocalPlayer.Character break end
        end
        if _newChar then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_newChar:WaitForChild("Humanoid", 8) end) end
        _G._jafRespawning = false
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.tpAntiDieEnable then _G.tpAntiDieEnable() end end)
    end) end
local function setupAntiDie() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
    _G.tpAntiDieDisable = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    antiDieDisabled = true
    if antiDieConn then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
antiDieConn:Disconnect() end); antiDieConn = nil end
end
_G.tpAntiDieEnable = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
antiDieDisabled = false setupAntiDie() end
    if antiDieDisabled then return end
    if _G.antiDieDisabled then return end
    local c, _, hum = getChar()
    if not hum then return end
    if antiDieConn then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
antiDieConn:Disconnect() end) end
    local deathConfirmed = false
    local zeroSince = nil
    antiDieConn = hum:GetPropertyChangedSignal("Health"):Connect(function() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
        if antiDieDisabled or deathConfirmed then return end
        if hum.Health <= 0 then
            zeroSince = zeroSince or os.clock()
            hum.Health = hum.MaxHealth
        else
            zeroSince = nil
        end end)
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local dead = false
        local function _readDead() LPH_ATTRIBUTES(ERROR_HANDLING(false))
dead = hum:GetState() == Enum.HumanoidStateType.Dead end
        local function _pollBody() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
            if hum.Parent == nil then deathConfirmed = true; return end
            dead = false
            pcall(_readDead)
            if dead or (zeroSince and (os.clock() - zeroSince) >= 0.5) then deathConfirmed = true _G._jafForceRespawn("antiDie-death") end
        end
        while antiDieConn and not antiDieDisabled and not deathConfirmed do
            local ok = pcall(_pollBody)
            if not ok then break end
            RunService.Heartbeat:Wait()
        end end)
    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        hum.Died:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if antiDieDisabled then return end deathConfirmed = true _G._jafForceRespawn("antiDie-died") end)
    end) end
setupAntiDie()
LocalPlayer.CharacterAdded:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
task.wait(0.3); setupAntiDie() end)
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local _hb = RunService.Heartbeat
    local function _healTo(h, v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
h.Health = v end
    while true do
        _hb:Wait()
        if _G.JAF_HealLock == false then continue end
        if _G._jafRespawning then continue end
        if antiDieDisabled or _G.antiDieDisabled then continue end
        local _hc = LocalPlayer.Character
        local hum = _hc and _hc:FindFirstChildOfClass("Humanoid")
        if hum and hum.Parent then
            local _mx = hum.MaxHealth
            if _mx > 0 then local _hp = hum.Health if _hp > 0 and _hp < _mx then pcall(_healTo, hum, _mx) end end
        end end end)
local function jafStripWatchers() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if _G.JAF_StripWatchers == false then return end
    if not getconnections then return end
    local _, hrp = getChar()
    if not hrp then return end
    local broad = (_G.JAF_StripWatchersBroad == true)
    local hits = 0
    for _, sig in ipairs({ "CFrame", "Position" }) do
        local ok, conns = pcall(getconnections, hrp:GetPropertyChangedSignal(sig))
        if ok and type(conns) == "table" then
            for _, c in ipairs(conns) do
                local fn = c.Function
                if type(fn) == "function" and not (isexecutorclosure and isexecutorclosure(fn)) then
                    local src = ""
                    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
src = debug.getinfo(fn).source or "" end)
                    local match = broad and src:find("ReplicatedFirst", 1, true) or src:find("ReplicatedFirst.test", 1, true)
                    if match then hits = hits + 1 pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
c:Disable() end) end
                end end end end end
_G.JAF_StripWatchers_run = jafStripWatchers
task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
while true do pcall(jafStripWatchers) task.wait(0.5) end end)
local function instantClone() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if not SAB_LIVE() then if _G.JAF_LandHoldRelease then pcall(_G.JAF_LandHoldRelease) end return end
    if _G.isCloning then return end
    _G.isCloning = true
    local _awOverride = tonumber(_G.JAF_CloneAnchorOverride)
    _G.JAF_CloneAnchorOverride = nil
    local cloneName = tostring(LocalPlayer.UserId).."_Clone"
    local stale = Workspace:FindFirstChild(cloneName)
    if stale then stale:Destroy() end
    local c, _, hum = getChar()
    if not c or not hum then _G.isCloning = false; return end
    local cloner = LocalPlayer.Backpack:FindFirstChild("Quantum Cloner") or c:FindFirstChild("Quantum Cloner")
    if not cloner then _G.isCloning = false; return end
    if cloner.Parent ~= c then
        if _G.JAF_FastEquip ~= false then
            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hum:EquipTool(cloner) end)
            if cloner.Parent ~= c then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
cloner.Parent = c end) end
            local _png = _G.JAF_Ping()
            local _w = math.clamp(_png * 1.5, 1/60, tonumber(_G.JAF_FastEquipMaxWait) or 0.12)
            local _t = tick()
            while tick() - _t < _w do RunService.Heartbeat:Wait() end
        else
            hum:EquipTool(cloner)
            local _equipStart = tick()
            while tick() - _equipStart < 0.3 do if cloner.Parent == c then break end RunService.Heartbeat:Wait() end
            RunService.Heartbeat:Wait()
        end end
    local tf = hubFind("ToolsFrames")
    local qc = tf and tf:FindFirstChild("QuantumCloner")
    local btn = qc and qc:FindFirstChild("TeleportToClone")
    if not btn then
        local _bt = tick()
        while tick() - _bt < (tonumber(_G.JAF_CloneBtnWait) or 0.3) do
            RunService.Heartbeat:Wait()
            tf  = hubFind("ToolsFrames")
            qc  = tf and tf:FindFirstChild("QuantumCloner")
            btn = qc and qc:FindFirstChild("TeleportToClone")
            if btn then break end
        end end
    do
        local _cvC, _cvHrp, _cvHum = getChar()
        if _cvHum then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_cvHum:Move(Vector3.zero, false) end) end
        if _cvC then
            for _, _p in ipairs(_cvC:GetDescendants()) do
                if _p:IsA("BasePart") then _p.AssemblyLinearVelocity  = Vector3.zero _p.AssemblyAngularVelocity = Vector3.zero end
            end end end
    if _G.JAF_LastQCTpFire and tick() - _G.JAF_LastQCTpFire < 5 then _G.isCloning = false; return end
    local _clAnchorHrp
    if not _G.JAF_CloneNoAnchor and _G.JAF_FreeFallClone ~= true and not _G.JAF_ClonePreAnchored then
        local _, _ah = getChar()
        if _ah then
            _P(_ah, "AssemblyLinearVelocity", Vector3.zero, "AssemblyAngularVelocity", Vector3.zero, "Anchored", true)
            _clAnchorHrp = _ah
            _G.JAF_CloneSelfAnchored = true
            task.delay(2.5, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                if _clAnchorHrp and _clAnchorHrp.Parent then _clAnchorHrp.Anchored = false end
                _G.JAF_CloneSelfAnchored = nil
            end)
            local _lockCF   = _ah.CFrame
            local _hammerN  = tonumber(_G.JAF_ClonePosHammerFrames) or 5
            local _cfgHold = tonumber(Config.TpSettings.CloneDelay) or 0.15
            local _aw = _awOverride or tonumber(_G.JAF_CloneAnchorWait) or _cfgHold
            local _t = tick(); local _hf = 0
            while tick() - _t < _aw do
                RunService.Heartbeat:Wait()
                if _G.JAF_ClonePosHammer ~= false and not _G.JAF_CloneNoHammer and _hf < _hammerN and _ah and _ah.Parent then
                    _P(_ah, "CFrame", _lockCF, "AssemblyLinearVelocity", Vector3.zero, "AssemblyAngularVelocity", Vector3.zero)
                    _hf = _hf + 1
                end end end end
    local function _releaseCloneAnchors() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if _clAnchorHrp and _clAnchorHrp.Parent then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_clAnchorHrp.Anchored = false end) end
        local _pre = _G.JAF_ClonePreAnchorHrp
        if _pre and _pre.Parent then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_pre.Anchored = false end) end
        _G.JAF_ClonePreAnchorHrp = nil; _G.JAF_ClonePreAnchored = nil; _G.JAF_ClonePreAnchorToken = nil
        _G.JAF_CloneSelfAnchored = nil
    end
    if _G.JAF_LandHoldGate then
        local _gok, _gpass = pcall(_G.JAF_LandHoldGate)
        if _gok and _gpass == false then _releaseCloneAnchors() _G.isCloning = false return end
    end
    do
        local _, _xh = getChar()
        if _xh then
            _G.JAF_ExpectedClonePos = _xh.Position + _xh.CFrame.LookVector * 3 + Vector3.new(0, 0.65, 0)
            _G.JAF_CloneSrcPos  = _xh.Position
            _G.JAF_CloneSrcLook = _xh.CFrame.LookVector
        end end
    local _placedVia = "FAILED"
    do
        if not SAB_LIVE() then
            if _G.JAF_LandHoldRelease then pcall(_G.JAF_LandHoldRelease) end
            _releaseCloneAnchors()
            _G.isCloning = false
            return
        end
        local _wasEnabled = true
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_wasEnabled = cloner.Enabled end)
        local okAct = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
cloner:Activate() end)
        if okAct then _placedVia = "GAME Activate" end
        if getconnections and (not okAct or not _wasEnabled) then
            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                local cn = getconnections(cloner.Activated)
                if cn and cn[1] then cn[1].Function(); _placedVia = "GAME closure(direct)" end
            end) end end
    do
        local _t0 = tick()
        local _png = LocalPlayer:GetNetworkPing() or 0.1
        local _cap = math.max(tonumber(_G.JAF_ClonePlaceTimeout) or 0.5, _png * 3 + 0.2)
        while not Workspace:FindFirstChild(cloneName) and tick() - _t0 < _cap do RunService.Heartbeat:Wait() end
        local _cm = Workspace:FindFirstChild(cloneName)
        if _cm and _G.JAF_CloneDiag == true then
            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                local _cp = _cm:GetPivot().Position
                _G.JAF_ActualClonePos = _cp
                local _ex = _G.JAF_ExpectedClonePos
                if _ex then
                    _G.JAF_CloneErr = string.format("clone err %.2f (xz %.2f): actual (%.1f,%.1f,%.1f) vs expected (%.1f,%.1f,%.1f)",
                        (_cp - _ex).Magnitude,
                        (Vector2.new(_cp.X, _cp.Z) - Vector2.new(_ex.X, _ex.Z)).Magnitude,
                        _cp.X, _cp.Y, _cp.Z, _ex.X, _ex.Y, _ex.Z)
                end
                local _sp, _sl = _G.JAF_CloneSrcPos, _G.JAF_CloneSrcLook
                local _cr = _cm:FindFirstChild("HumanoidRootPart", true)
                if _sp and _sl and _cr then
                    local _d  = _cr.Position - _sp
                    local _fw = _d.X * _sl.X + _d.Z * _sl.Z
                    local _lt = _d.X * (-_sl.Z) + _d.Z * _sl.X
                    _G.JAF_CloneOffset = string.format("TRUE clone offset: fwd %.2f  lat %.2f  up %.2f  (model assumes fwd 3.00 up 0.65) | pivotVsRoot %.2f",
                        _fw, _lt, _d.Y, (_cm:GetPivot().Position - _cr.Position).Magnitude)
                end end) end end
    if _G.JAF_LandHoldRelease then pcall(_G.JAF_LandHoldRelease) end
    if _G.JAF_UnanchorAfterSwap ~= true then
        if _G.JAF_InstantSwap ~= false then
            _releaseCloneAnchors()
        else
            if Workspace:FindFirstChild(cloneName) then
                local _ud = tonumber(_G.JAF_CloneUnanchorDelay) or 0.1
                if _ud > 0 then task.wait(_ud) end
            end
            _releaseCloneAnchors()
        end end
    local _tpVia = "NONE"
    do
        local fired = false
        if typeof(firesignal) == "function" then
            btn.Visible = true
            if pcall(firesignal, btn.MouseButton1Up) then fired = true; _tpVia = "firesignal(button)" end
        end
        if not fired and getconnections then
            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                local cn = getconnections(btn.MouseButton1Up)
                if cn and cn[1] then cn[1].Function(); fired = true; _tpVia = "GAME closure(direct fallback)" end
            end) end end
    if _tpVia ~= "NONE" then _G.JAF_LastQCTpFire = tick() end
    _G.JAF_CloneNoHammer = nil
    _G.isCloning = false
end
local findAdorneeGlobal; findAdorneeGlobal = function(a) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
    if not a then return nil end
        if a.isWalking then
        if a.model and a.model.Parent then
            return a.model:FindFirstChild("HumanoidRootPart", true)
                or a.model.PrimaryPart
                or a.model:FindFirstChildWhichIsA("BasePart", true)
        end
        return nil
    end
    local plots = Workspace:FindFirstChild("Plots")
    if not plots then return nil end
    local plot = plots:FindFirstChild(a.plot); if not plot then return nil end
    local pods = plot:FindFirstChild("AnimalPodiums"); if not pods then return nil end
    local pod  = pods:FindFirstChild(a.slot); if not pod then return nil end
    local base = pod:FindFirstChild("Base"); if not base then return nil end
    local sp   = base:FindFirstChild("Spawn")
    return sp or base:FindFirstChildWhichIsA("BasePart")
end
_G.findAdorneeGlobal = findAdorneeGlobal
local function isPlotUnlocked(plotName, skipChan) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local ok, res, why = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if (not skipChan) and _G.JAF_NoSynchronizer ~= true and _G.JAF_LockChannelCheck ~= false then
            local okC, chLocked = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                local Sync = _G.JAF_GetSynchronizer(); if not Sync then return false end
                local ch = Sync:Get(plotName)
                if not ch then return false end
                local now = Workspace:GetServerTimeNow()
                local function _lk(b) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return b ~= nil and (type(b) ~= "number" or b > now) end
                return _lk(ch:Get("BlockEndTimeFirstFloor")) or _lk(ch:Get("BlockEndTimeSecondFloor"))
            end)
            if okC and chLocked then return false, "chan" end
        end
        local plots = Workspace:FindFirstChild("Plots"); if not plots then return false, "noPlots" end
        local plot  = plots:FindFirstChild(plotName);   if not plot  then return false, "noPlot" end
        local laser = plot:FindFirstChild("LaserHitbox")
        laser = laser and (laser:FindFirstChild("Main") or laser:FindFirstChildWhichIsA("BasePart"))
        if not laser then return false, "noLaser" end
        if laser.CanCollide then return false, "laserCC" end
        local uf = plot:FindFirstChild("Unlock")
        if uf then
            for _, d in ipairs(uf:GetDescendants()) do if d:IsA("ProximityPrompt") and d.Enabled then return false, "prompt" end end
        end
        if _G.JAF_LockGuiCheck ~= false then
            local pur = plot:FindFirstChild("Purchases")
            if pur then
                for _, d in ipairs(pur:GetDescendants()) do
                    if d:IsA("TextLabel") and d.Name == "Locked" and d.Visible then return false, "gui" end
                end end end
        return true
    end)
    if not ok then return false, "err" end
    return res == true, why
end
_G.JAF_BrainrotApproachSpeed = function() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
    local _slider = math.clamp(tonumber(Config.TpSettings.BrainrotSpeed) or 175, 100, 500)
    if _G.JAF_BrainrotSpeedGate == false then return _slider end
    local _floor = math.clamp(tonumber(_G.JAF_BrainrotNoBoostSpeed) or 170, 100, 500)
    if _slider <= _floor then return _slider end
    local _c  = LocalPlayer.Character
    local _sa = _c and _c:GetAttribute("SpeedAllowance")
    if type(_sa) == "number" and _sa > 0 then return _slider end
    return _floor
end
_G.JAF_BrainrotSimpleOn = (function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local _ov = _G.JAF_BrainrotSimpleTp
    local _on
    if _ov ~= nil then _on = (_ov ~= false) else _on = (Config.TpSettings.InstaTpBrainrot ~= false) end
    if not _on then return false end
    if _G.JAF_BrainrotSimpleRequireSA == false then return true end
    local _c  = LocalPlayer.Character
    local _sa = _c and _c:GetAttribute("SpeedAllowance")
    return type(_sa) == "number" and _sa > 0
end)
_G.JAF_BrainrotSimpleSnap = (function(hrp, dst, rotCF) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if not _G.JAF_BrainrotSimpleOn() then return false end
    if not (hrp and hrp.Parent and dst) then return true end
    if _G.JAF_CancelTp or not SAB_LIVE() then
        hrp.AssemblyLinearVelocity  = Vector3.zero
        hrp.AssemblyAngularVelocity = Vector3.zero
        return true
    end
    local _snapWrite = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        hrp.AssemblyLinearVelocity  = Vector3.zero
        hrp.AssemblyAngularVelocity = Vector3.zero
        hrp.CFrame = CFrame.new(dst) * (rotCF or CFrame.new())
        hrp.AssemblyLinearVelocity  = Vector3.zero
        hrp.AssemblyAngularVelocity = Vector3.zero
        _G.JAF_BrainrotSnapT = tick()
    end
    local _tries  = math.max(1, math.floor(tonumber(_G.JAF_BrainrotSnapTries) or 2))
    local _verify = tonumber(_G.JAF_BrainrotSnapVerify); if _verify == nil then _verify = 0.05 end
    local _tol    = tonumber(_G.JAF_BrainrotSnapTol) or 3
    for _try = 1, _tries do
        _snapWrite()
        if _verify <= 0 then return true end
        local _v0 = tick()
        repeat
            RunService.Heartbeat:Wait()
            if _G.JAF_CancelTp or not SAB_LIVE() or not (hrp and hrp.Parent) then return true end
        until tick() - _v0 >= _verify
        local _off = (hrp.Position - dst).Magnitude
        if _off <= _tol then return true end
    end
    if type(_G.JAF_BrainrotTweenOnly) == "function" then _G.JAF_BrainrotTweenOnly(hrp, dst, rotCF) end
    return true
end)
_G.JAF_BrainrotTweenOnly = (function(hrp, dst, rotCF) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    for _ = 1, 6 do
        if not (hrp and hrp.Parent) then return end
        local d = (hrp.Position - dst).Magnitude
        if d <= 0.5 then return end
        local speed = _G.JAF_BrainrotApproachSpeed()
        local tw = TweenService:Create(hrp,
            TweenInfo.new(math.clamp(d / speed, 0.05, 0.6), Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
            {CFrame = CFrame.new(dst) * (rotCF or CFrame.new())})
        local done = false
        local conn = tw.Completed:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
done = true end)
        tw:Play()
        local changed = false
        while not done do
            RunService.Heartbeat:Wait()
            if not (hrp and hrp.Parent) or not SAB_LIVE() then tw:Cancel(); conn:Disconnect(); return end
            hrp.AssemblyLinearVelocity = Vector3.zero
            if _G.JAF_BrainrotApproachSpeed() ~= speed then tw:Cancel(); changed = true; break end
        end
        conn:Disconnect()
        if not changed then return end
    end end)
_G.JAF_BrainrotCFrameMove = function(hrp, dst, rotCF) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if _G.JAF_BrainrotSimpleSnap(hrp, dst, rotCF) then return end
    _G.JAF_BrainrotTweenOnly(hrp, dst, rotCF)
end
_SABCAP.JAF_TweenHrpBrainrot = function(hrp, dst, rotCF) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
    if not (hrp and hrp.Parent and dst) then return end
    if _G.JAF_BrainrotSimpleSnap(hrp, dst, rotCF) then return end
    if _G.JAF_BrainrotVelMove == false then return _G.JAF_BrainrotCFrameMove(hrp, dst, rotCF) end
    local _t0     = tick()
    local _budget = math.clamp((dst - hrp.Position).Magnitude / math.max(_G.JAF_BrainrotApproachSpeed(), 50) + 1.5, 1, 6)
    local _bestD, _stuckAt = (dst - hrp.Position).Magnitude, tick()
    local _dt = 1/60
    local _char0 = LocalPlayer.Character
    while tick() - _t0 < _budget do
        if not (hrp and hrp.Parent) then return end
        if _G.JAF_CancelTp or not SAB_LIVE() or LocalPlayer.Character ~= _char0 then
            hrp.AssemblyLinearVelocity  = Vector3.zero
            hrp.AssemblyAngularVelocity = Vector3.zero
            return
        end
        local _pos  = hrp.Position
        local _diff = dst - _pos
        local _dist = _diff.Magnitude
        if _dist <= 1 then break end
        local _speed = _G.JAF_BrainrotApproachSpeed()
        if _G.JAF_BrainrotLikeFlight ~= false and Config.TpSettings.TpMode == "grapple"
           and _G.JAF_GrappleTick and _G.JAF_GrappleSpeedCap then
            _G.JAF_GrappleTick()
            _speed = math.min(_speed, _G.JAF_GrappleSpeedCap())
        end
        hrp.AssemblyAngularVelocity = Vector3.zero
        if _dist > 8 then
            hrp.AssemblyLinearVelocity = _diff.Unit * _speed
        else
            local _stepVec = _diff.Unit * math.min(_speed * _dt, _dist * 0.45)
            if _G.JAF_BrainrotCloseInVel == false then
                hrp.AssemblyLinearVelocity = Vector3.zero
            else
                hrp.AssemblyLinearVelocity = _stepVec / math.max(_dt, 1/240)
            end
            hrp.CFrame = CFrame.new(_pos + _stepVec) * (rotCF or CFrame.new())
        end
        if _dist < _bestD - 0.5 then
            _bestD, _stuckAt = _dist, tick()
        elseif tick() - _stuckAt > (tonumber(_G.JAF_BrainrotStallSecs) or 0.35) then
            _G.JAF_BrainrotCFrameMove(hrp, dst, rotCF)
            break
        end
        _dt = RunService.Heartbeat:Wait()
    end
    if hrp and hrp.Parent then
        hrp.AssemblyLinearVelocity  = Vector3.zero
        hrp.AssemblyAngularVelocity = Vector3.zero
        if (dst - hrp.Position).Magnitude <= 8 then hrp.CFrame = CFrame.new(dst) * (rotCF or CFrame.new()) end
    end end
local function _jafReleasePreAnchor() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local _rp = _G.JAF_ClonePreAnchorHrp
    if _rp and _rp.Parent then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_rp.Anchored = false end) end
    _P(_G, "JAF_ClonePreAnchorHrp", nil, "JAF_ClonePreAnchored", nil, "JAF_ClonePreAnchorToken", nil)
end
local function _equipRefused(hum) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
    if not hum or hum.Health <= 0 then return true end
    if hum:GetState() == Enum.HumanoidStateType.Dead then return true end
    local _srv = Workspace:GetServerTimeNow()
    local ru = tonumber(_G.JAF_RagdollUntil) or 0
    if ru > 0 and ru - _srv > 0 then return true end
    local et = tonumber(LocalPlayer:GetAttribute("RagdollEndTime")) or 0
    if et > 0 and et - _srv > 0 then return true end
    return false
end
local _ceqFailAt = 0
local function equipCarpet() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
    if _G.JAF_ResetStowActive then return end
    local c,_,hum = getChar()
    if not c or not hum then return end
    if tick() - _ceqFailAt < (tonumber(_G.JAF_EquipFailGap) or 0.25) then return end
    if _equipRefused(hum) then _ceqFailAt = tick(); return end
    local t = LocalPlayer.Backpack:FindFirstChild(Config.TpSettings.Tool) or c:FindFirstChild(Config.TpSettings.Tool)
    if t then hum:EquipTool(t) if t.Parent ~= c then _ceqFailAt = tick() _G.JAF_EquipFailN = (tonumber(_G.JAF_EquipFailN) or 0) + 1 end end
end
local JAF_GrappleEngage, JAF_GrappleTick, JAF_GrappleSpeedCap, JAF_GrappleStop, JAF_GrapplePreFire
do
    local CARPET_NAMES = {"Flying Carpet","Witch's Broom","Cupid's Wings","Santa's Sleigh","Waverider","Magic Carpet","Carpet"}
    _G.JAF_CarpetNames = CARPET_NAMES
    local function REARM_SAFE() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return math.max(50, tonumber(_G.GrappleReArmSpeed) or 220) end
    local POST_SAFE; POST_SAFE = (function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return math.max(50, tonumber(_G.GrapplePostBoostSpeed) or 160) end)
    local SWAP_SAFE; SWAP_SAFE = (function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return math.max(30, tonumber(_G.GrappleSwapSpeed) or 60) end)
    local BOOST_SAFE; BOOST_SAFE = (function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return math.max(120, tonumber(_G.GrappleBoostSpeed) or tonumber(_G.JAF_GrappleFlightSpeed) or tonumber(Config.TpSettings and Config.TpSettings.GrappleSpeed) or 320) end)
    local Net
    local function loadNet() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if Net then return Net end
        local ok, mod = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return require(ReplicatedStorage.Packages.Net:FindFirstChildWhichIsA("ModuleScript", true)) end)
        if ok and type(mod) == "table" then Net = mod end
        return Net
    end
    local _gRem, _gRemJob, _gRemAt = nil, nil, 0
    local function resolveGrappleRemote() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local pkgs = ReplicatedStorage:FindFirstChild("Packages")
        local net  = pkgs and pkgs:FindFirstChild("Net")
        if not net then return nil end
        local slots, byLeaf = {}, {}
        for i, ch in ipairs(net:GetChildren()) do
            local p, rest = string.match(ch.Name, "^(%a+)/(.+)$")
            if p and #rest == 64 and string.match(rest, "^%x+$") then
                slots[i] = ch
                local l = byLeaf[rest]
                if not l then l = {}; byLeaf[rest] = l end
                l[#l + 1] = { slot = i, class = ch.ClassName }
            end end
        local anchor, dual = nil, 0
        for _, list in pairs(byLeaf) do
            if #list > 1 then dual = dual + 1 for _, e in ipairs(list) do if e.class == "RemoteEvent" then anchor = e.slot end end end
        end
        if dual ~= 1 or not anchor then return nil end
        local t = slots[anchor - 1]
        if t and t.ClassName == "RemoteEvent" and t.Parent == net then return t end
        return nil
    end
    local function grappleRemote() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if _gRemJob ~= game.JobId then _gRem, _gRemJob, _gRemAt = nil, game.JobId, 0 end
        if _gRem and _gRem.Parent then return _gRem end
        _gRem = nil
        if _gRemAt > 0 and tick() - _gRemAt < (tonumber(_G.JAF_GrappleRemoteRetry) or 2) then return nil end
        _gRemAt = tick()
        _gRem = resolveGrappleRemote()
        return _gRem
    end
    local function grappleFireMode() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local m = _G.JAF_GrappleFireMode
        if m == nil then m = Config.TpSettings and Config.TpSettings.GrappleFireMode end
        return (m == "remote") and "remote" or "firesignal"
    end
    local function revertToSignal(why) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if grappleFireMode() ~= "remote" then return end
        _G.JAF_GrappleFireMode = "firesignal"
        if Config.TpSettings then Config.TpSettings.GrappleFireMode = "firesignal" end
        _gRemAt = 0
        _G.JAF_GrappleRemoteRevert    = (tonumber(_G.JAF_GrappleRemoteRevert) or 0) + 1
        _G.JAF_GrappleRemoteRevertWhy = why
        pcall(SaveConfig)
        if type(_G.JAF_SyncGrappleFireRow) == "function" then pcall(_G.JAF_SyncGrappleFireRow) end
    end
    local function fireBoost() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local g = (LocalPlayer.Backpack and LocalPlayer.Backpack:FindFirstChild("Grapple Hook"))
               or (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Grapple Hook"))
        if not g then return end
        local mode = grappleFireMode()
        if mode ~= "remote" and typeof(firesignal) ~= "function" then return end
        local c   = LocalPlayer.Character
        local hrp = c and c:FindFirstChild("HumanoidRootPart")
        if not hrp then return end
        local dist = math.clamp(tonumber(_G.JAF_GrappleBoostDist) or 95, 12, 99)
        local _aimDir = hrp.CFrame.LookVector
        do
            local ap = _G.JAF_GrappleAimPos
            if typeof(ap) == "Vector3" then local d = ap - hrp.Position if d.Magnitude > 1 then _aimDir = d.Unit end end
        end
        if _G.JAF_GrappleFlatAim ~= false then
            local _flat = Vector3.new(_aimDir.X, 0, _aimDir.Z)
            if _flat.Magnitude > 0.01 then _aimDir = _flat.Unit end
        end
        local aimPos = hrp.Position + _aimDir * dist
        local function _killFlightPower() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
local fp = hrp:FindFirstChild("FlightPower") if fp then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
fp:Destroy() end) end end)
        end
        if mode == "remote" then
            local r = grappleRemote()
            if r then
                local okF = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
r:FireServer(dist / 120, aimPos) end)
                if okF then _G.JAF_GrappleRemoteFired = (tonumber(_G.JAF_GrappleRemoteFired) or 0) + 1 _killFlightPower() return end
                _G.JAF_GrappleRemoteErrs = (tonumber(_G.JAF_GrappleRemoteErrs) or 0) + 1
                revertToSignal("fire failed")
            else
                _G.JAF_GrappleRemoteUnresolved = (tonumber(_G.JAF_GrappleRemoteUnresolved) or 0) + 1
                revertToSignal("not found")
            end
            if typeof(firesignal) ~= "function" then return end
        end
        local okPm, pm = pcall(require, ReplicatedStorage.Packages.PlayerMouse)
        if not okPm or type(pm) ~= "table" then return end
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pm.Hit    = CFrame.new(aimPos) pm.Target = workspace.Terrain end)
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local Deb = require(ReplicatedStorage.Packages.Debounce)
            if type(Deb) == "function" and debug and debug.getupvalues then
                for _, uv in ipairs(debug.getupvalues(Deb)) do
                    if type(uv) == "table" then
                        for k in pairs(uv) do if type(k) == "string" and (k:find("rapple") or k:find("Grapple")) then uv[k] = nil end end
                    end end end end)
        pcall(firesignal, g.Activated)
        _killFlightPower()
    end
    _SABCAP.JAF_FireRemote = function(remoteName, ...) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local net = loadNet(); if not net then return false end
        local a = table.pack(...)
        return (pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local inst
            if _G.JAF_SafeCall then
                local ok, r = _G.JAF_SafeCall(net.RemoteEvent, net, remoteName)
                if not ok then error("safeCall RemoteEvent failed: " .. tostring(r), 0) end
                inst = r
            else
                inst = net:RemoteEvent(remoteName)
            end
            local child = ReplicatedStorage.Packages.Net:FindFirstChild(inst.Name)
            ;(child or inst):FireServer(table.unpack(a, 1, a.n))
        end)) end
    local function findTool(n) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local c = LocalPlayer.Character
        local bp = LocalPlayer:FindFirstChild("Backpack")
        return (c and c:FindFirstChild(n)) or (bp and bp:FindFirstChild(n))
    end
    local carpetEquipped; carpetEquipped = (function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local c = LocalPlayer.Character; if not c then return false end
        if c:FindFirstChild(Config.TpSettings.Tool) then return true end
        for _, n in ipairs(CARPET_NAMES) do if c:FindFirstChild(n) then return true end end
        return false
    end)
    local boostActive; boostActive = function() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
        local c = LocalPlayer.Character
        if c == nil then return false end
        local _sa = c:GetAttribute("SpeedAllowance")
        if _G.JAF_BoostStrict == true then return type(_sa) == "number" and _sa > 0 end
        return _sa ~= nil
    end
    local rearming, stopped, lastCycle = false, false, 0
    local _preArmed, _preArmedAt = false, 0
    local curShortHop = false
    local ensureCarpet; ensureCarpet = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if carpetEquipped() then return true end
        equipCarpet()
        return carpetEquipped()
    end
    local function boostCycle() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if stopped or rearming or _G.JAF_ResetStowActive then return end
        rearming = true; lastCycle = tick()
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local c   = LocalPlayer.Character
            local hum = c and c:FindFirstChildOfClass("Humanoid")
            local g   = c and findTool("Grapple Hook")
            if c and hum and g and (LocalPlayer:GetAttribute("Stealing") or _equipRefused(hum)) then
                g = nil
                _G.JAF_BoostEquipSkips = (tonumber(_G.JAF_BoostEquipSkips) or 0) + 1
            end
            if c and hum and g then
                if g.Parent ~= c then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hum:EquipTool(g) end); RunService.Heartbeat:Wait() end
                if g.Parent ~= c then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hum:EquipTool(g) end); RunService.Heartbeat:Wait() end
                if g.Parent == c then RunService.Heartbeat:Wait() end
                if g.Parent == c then fireBoost() end
                local _fg0 = tick()
                while not boostActive() and not stopped
                      and tick() - _fg0 < (tonumber(_G.JAF_GrapplePreArmMax) or 0.5) do
                    RunService.Heartbeat:Wait()
                end end
            local _ce0 = tick()
            while not ensureCarpet() and tick() - _ce0 < (tonumber(_G.JAF_CarpetEquipRetry) or 0.4) do RunService.Heartbeat:Wait() end
        end)
        rearming = false
    end
    JAF_GrapplePreFire = function(aimPos, shortHop) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if not SAB_LIVE() then return end
        if _preArmed and (tick() - _preArmedAt) > (tonumber(_G.JAF_PreArmTTL) or 2.5) then
            _preArmed = false
            _G.JAF_PreArmStaleReleases = (tonumber(_G.JAF_PreArmStaleReleases) or 0) + 1
        end
        if _G.JAF_NoGrapplePreFire == true or _preArmed then return end
        if not findTool("Grapple Hook") then return end
        stopped     = false
        curShortHop = (shortHop == true)
        rearming    = false
        lastCycle   = 0
        loadNet()
        if aimPos then _G.JAF_GrappleAimPos = aimPos end
        _preArmed, _preArmedAt = true, tick()
        task.spawn(boostCycle)
    end
    _G.JAF_GrapplePreFire = JAF_GrapplePreFire
    _G.JAF_GrappleBoostNow = function(aimPos) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if not SAB_LIVE() then return false end
        if _G.JAF_NoGrapplePreFire == true then return false end
        if _G.JAF_ResetStowActive then return false end
        if not findTool("Grapple Hook") then return false end
        if rearming then return boostActive() end
        local _wasStopped = stopped
        stopped = false
        loadNet()
        if typeof(aimPos) == "Vector3" then _G.JAF_GrappleAimPos = aimPos end
        local ok = pcall(boostCycle)
        stopped = _wasStopped
        return (ok and boostActive()) or false
    end
    JAF_GrappleEngage = function(alive, shortHop) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local _pre = _preArmed
        _preArmed  = false
        if _pre and not rearming and not boostActive() then _pre = false end
        if not _pre then
            stopped  = false
            curShortHop = (shortHop == true)
            rearming = false
            lastCycle = 0
            loadNet()
        end
        if _G.JAF_GrapplePreArm ~= false and findTool("Grapple Hook")
           and (shortHop ~= true or _G.JAF_ShortHopPreArm ~= false) then
            if not _pre then task.spawn(boostCycle) end
            local _t0  = tick()
            local _max = tonumber(_G.JAF_GrapplePreArmMax) or 0.5
            while (rearming or not carpetEquipped() or not boostActive())
                  and not stopped and tick() - _t0 < _max do
                RunService.Heartbeat:Wait()
            end
            if _G.JAF_EngageRetry ~= false and not stopped and not rearming
               and not boostActive() and findTool("Grapple Hook")
               and not LocalPlayer:GetAttribute("Stealing") then
                _G.JAF_EngageRetryN = (tonumber(_G.JAF_EngageRetryN) or 0) + 1
                pcall(boostCycle)
            end
        elseif not _pre then
            task.spawn(boostCycle)
        end end
    JAF_GrappleTick = function(alive, brMove) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false)) end
    JAF_GrappleSpeedCap = function() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
        if not carpetEquipped() then return SWAP_SAFE() end
        if rearming      then return SWAP_SAFE()  end
        if boostActive() then return BOOST_SAFE() end
        if curShortHop and _G.JAF_ShortHopMaxSpeed ~= false then return BOOST_SAFE() end
        return POST_SAFE()
    end
    _G.JAF_GrappleTick     = JAF_GrappleTick
    _G.JAF_GrappleSpeedCap = JAF_GrappleSpeedCap
    JAF_GrappleStop = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
stopped = true curShortHop = false _G.JAF_GrappleAimPos = nil ensureCarpet() end
end
_G.JAF_RepairGrapple = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return true end
local function _jafWaitHookReady(tool) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if _G.JAF_HookReady == false or not tool or tool.Name ~= "Grapple Hook" then return end
    local cap = tonumber(_G.JAF_HookReadyMax) or 3
    if type(getconnections) ~= "function" then task.wait(math.min(0.35, cap)); return end
    local t0 = tick()
    while tick() - t0 < cap do
        local ok, conns = pcall(getconnections, tool.Activated)
        if ok and conns and #conns > 0 then _G.JAF_HookReadyTook = tick() - t0 return true end
        task.wait(0.05)
    end
    _G.JAF_HookReadyTook = -1
    return false
end
local function _jafEquipTpToolFor(newChar) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
    local hrp = newChar:WaitForChild("HumanoidRootPart", 10)
    local hum = newChar:WaitForChild("Humanoid", 10)
    if not hrp or not hum then return end
    local _cw = tick()
    while not _cfgLoaded and tick() - _cw < 5 do task.wait(0.05) end
    local toolName = (Config.TpSettings.TpMode == "grapple") and "Grapple Hook" or Config.TpSettings.Tool
    if newChar:FindFirstChild(toolName) then
        if _G._jafEquipWaitConn then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G._jafEquipWaitConn:Disconnect() end); _G._jafEquipWaitConn = nil end
        return
    end
    local tool = LocalPlayer.Backpack:FindFirstChild(toolName)
    if not tool then
        if _G._jafEquipWaitConn then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G._jafEquipWaitConn:Disconnect() end); _G._jafEquipWaitConn = nil end
        local conn
        conn = LocalPlayer.Backpack.ChildAdded:Connect(function(child) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
            if child.Name == toolName then
                conn:Disconnect()
                if _G._jafEquipWaitConn == conn then _G._jafEquipWaitConn = nil end
                _jafWaitHookReady(child)
                if hum and hum.Parent and child and child.Parent then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hum:EquipTool(child) end) end
            end end)
        _G._jafEquipWaitConn = conn
    else
        if _G._jafEquipWaitConn then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G._jafEquipWaitConn:Disconnect() end); _G._jafEquipWaitConn = nil end
        _jafWaitHookReady(tool)
        if tool.Parent then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hum:EquipTool(tool) end) end
    end end
LocalPlayer.CharacterAdded:Connect(function(newChar) LPH_ATTRIBUTES(ERROR_HANDLING(false))
isTpMoving   = false _G.isCloning = false task.spawn(_jafEquipTpToolFor, newChar) end)
if LocalPlayer.Character then task.spawn(_jafEquipTpToolFor, LocalPlayer.Character) end
do
    local REQ = tostring(_G.JAF_WheelReqChild or "Main")
    local function sweepBrokenWheels() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if type(getconnections) ~= "function" or not (debug and debug.getupvalues and debug.info) then return 0 end
        local killed = 0
        for _, sname in ipairs({"PreRender","Heartbeat","RenderStepped","Stepped","PreSimulation","PostSimulation"}) do
            local oksig, sig = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return RunService[sname] end)
            if oksig and sig then
                local okc, conns = pcall(getconnections, sig)
                if okc and conns then
                    for _, c in ipairs(conns) do
                        local f = c.Function
                        if type(f) == "function" then
                            local src = ""; pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
src = tostring(debug.info(f, "s")) end)
                            local s = src:lower()
                            if s:find("wheel") or s:find("spin") then
                                local broken = false
                                pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                                    for _, uv in ipairs(debug.getupvalues(f)) do
                                        local inst
                                        if typeof(uv) == "Instance" then inst = uv
                                        elseif type(uv) == "table" and typeof(uv.Instance) == "Instance" then inst = uv.Instance end
                                        if inst and inst:IsA("Model") and not inst:FindFirstChild(REQ) then broken = true end
                                    end end)
                                if broken then
                                    if not pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
c:Disable() end) then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
c:Disconnect() end) end
                                    killed = killed + 1
                                end end end end end end end
        return killed
    end
    _G.JAF_SweepBrokenWheels = sweepBrokenWheels
    task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        task.wait(3)
        sweepBrokenWheels()
        local last = 0
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            game:GetService("ScriptContext").Error:Connect(function(msg) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                msg = tostring(msg)
                if msg:find("is not a valid member") then
                    local low = msg:lower()
                    if low:find("wheel") or low:find("spin") then if tick() - last > 1.5 then last = tick(); sweepBrokenWheels() end end
                end end) end)
        while true do task.wait(tonumber(_G.JAF_WheelSweepInterval) or 20); pcall(sweepBrokenWheels) end
    end) end
_G.JAF_HopToPet = function(entry) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if _G.JAF_HopDirect == false then return false end
    if _G.JAF_HopWhileCarrying ~= true
       and (LocalPlayer:GetAttribute("Stealing") or LocalPlayer:GetAttribute("StealingIndex")) then return false end
    if type(entry) ~= "table" or entry.isWalking then return false end
    local okP, part = pcall(_G.findAdorneeGlobal, entry)
    if not okP or not part then return false end
    local petPos = part.Position
    local _c0, hrp0 = getChar()
    if not hrp0 or not hrp0.Parent then return false end
    if (hrp0.Position - petPos).Magnitude > (tonumber(_G.JAF_HopMax) or 260) then return false end
    local boosted = false
    if type(_G.JAF_GrappleBoostNow) == "function" then
        local okB, r = pcall(_G.JAF_GrappleBoostNow, petPos)
        boosted = (okB and r) or false
    end
    equipCarpet()
    local _c1, _h1, _hum1 = getChar()
    local stand = (_hum1 and _h1 and (_hum1.HipHeight + _h1.Size.Y * 0.5)) or 4.1
    local lift  = tonumber(_G.JAF_BrainrotLift) or (0.5 + math.max(0, stand - 4.1))
    local underF2 = (type(_G.JAF_F2UnderActive) == "function") and _G.JAF_F2UnderActive(petPos.Y) or false
    local snapY
    if petPos.Y > 22 or underF2 then
        snapY = petPos.Y - (underF2 and (tonumber(_G.JAF_F2UnderDrop) or 8) or 8)
    else
        snapY = petPos.Y + 2.5 + lift
    end
    local dst = Vector3.new(petPos.X, snapY, petPos.Z)
    local _vertDiff = petPos.Y - ((_h1 and _h1.Position.Y) or hrp0.Position.Y)
    if (underF2 or _vertDiff > 2) and _G.JAF_CreateF3Platform then
        pcall(_G.JAF_CreateF3Platform, dst - Vector3.new(0, 5, 0))
        RunService.Heartbeat:Wait()
    end
    SharedState._currentTpTarget = entry
    local _c2, hrp = getChar()
    if not hrp or not hrp.Parent then return false end
    if type(_G.JAF_TweenHrpBrainrot) == "function" then
        _G.JAF_TweenHrpBrainrot(hrp, dst, nil)
    elseif type(_G.JAF_BrainrotCFrameMove) == "function" then
        _G.JAF_BrainrotCFrameMove(hrp, dst, nil)
    else
        return false
    end
    return true
end
local _retargetGen = 0
_G.JAF_Retarget = function(entry) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if type(entry) ~= "table" then return false end
    if _G.JAF_RetargetWhileCarrying ~= true
       and (LocalPlayer:GetAttribute("Stealing") or LocalPlayer:GetAttribute("StealingIndex")) then
        manualTarget = entry
        SharedState.SetManualTarget(entry)
        return false
    end
    _retargetGen = _retargetGen + 1
    local myGen = _retargetGen
    manualTarget = entry
    SharedState.SetManualTarget(entry)
    task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        _G.JAF_CancelTp = true
        if _G.JAF_TpActive then local t0 = tick() while _G.JAF_TpActive and tick() - t0 < 8 do task.wait(0.05) end end
        local _gf = math.max(1, math.floor(tonumber(_G.JAF_RetargetGraceFrames) or 3))
        for _ = 1, _gf do RunService.Heartbeat:Wait() end
        _G.JAF_CancelTp = false
        if myGen ~= _retargetGen then return end
        if manualTarget ~= entry then return end
        if _G.JAF_TpActive then return end
        local inBase = (type(_G.JAF_CurrentBase) == "function") and _G.JAF_CurrentBase() or nil
        if inBase ~= nil and inBase == entry.plot then
            local okH, hopped = pcall(_G.JAF_HopToPet, entry)
            if okH and hopped then return end
        end
        if _G.tpToBestBrainrot then pcall(_G.tpToBestBrainrot) end
    end)
    return true
end
_G.JAF_StolenSwap = function(cur, curY) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
    if Config and Config.SkipStolenTarget == false then return nil end
    if manualTarget ~= nil then return nil end
    if type(cur) ~= "table" or type(cur.plot) ~= "string" or cur.isWalking then return nil end
    local memo = {}
    if not _G.JAF_IsBeingStolen(cur, memo) then return nil end
    local _reqPrio = (_G.JAF_SwapRequirePrio ~= false)
    local best, bestScore, bestPos = nil, (_reqPrio and 999999 or math.huge), nil
    for _, a in ipairs(SharedState.AllAnimalsCache or {}) do
        if a and a ~= cur and a.uid ~= cur.uid and a.plot == cur.plot and not a.isWalking
           and a.owner ~= LocalPlayer.Name then
            local s = _G.JAF_PrioScore(a)
            if s < bestScore then
                local okA, alv = pcall(_G.JAF_PetAlive, a, memo)
                if okA and alv ~= false and not _G.JAF_IsBeingStolen(a, memo) then
                    local okP, pt = pcall(_G.findAdorneeGlobal, a)
                    local pos = (okP and pt and pt.Position) or nil
                    if pos and (not curY or math.abs(pos.Y - curY) <= (tonumber(_G.JAF_SwapFloorBand) or 10)) then
                        best, bestScore, bestPos = a, s, pos
                    end end end end end
    if not best then return nil end
    manualTarget = best
    SharedState.SetManualTarget(best)
    task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local t0 = tick()
        while tick() - t0 < (tonumber(_G.JAF_SwapPinMax) or 25) do
            if manualTarget ~= best then return end
            if LocalPlayer:GetAttribute("Stealing") then break end
            task.wait(0.1)
        end
        if manualTarget == best then manualTarget = nil; SharedState.SetManualTarget(nil) end
    end)
    return best, bestPos
end
local _frontRouteCache = nil
local _obsCache = {}
local _obsDirtyAt = 0
local _obsMem = {}
local _obsNew = {}
_G.JAF_ObsMemReset = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    for i = #_obsMem, 1, -1 do _obsMem[i] = nil end
    for i = #_obsNew, 1, -1 do _obsNew[i] = nil end
    _obsDirtyAt = tick()
    _G.JAF_ObsMemN, _G.JAF_ObsMemInjected = 0, 0
    return true
end
do
    if _G.JAF_ObstacleWatchConn  then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G.JAF_ObstacleWatchConn:Disconnect()  end) end
    if _G.JAF_ObstacleWatchConn2 then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G.JAF_ObstacleWatchConn2:Disconnect() end) end
    local _isStructural; _isStructural = function(p) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _G.JAF_ObstacleWatch == false then return false end
        if not p:IsA("BasePart") or not p.CanCollide then return false end
        if p.Name == "__HITBOX" then return false end
        local s = p.Size
        if math.max(s.X, s.Y, s.Z) < 4 then return false end
        if math.max(s.X, s.Z) > (_G.JAF_GroundXZ or 90) then return false end
        local n, top = p, p
        while n and n ~= Workspace do
            local nm = n.Name
            if nm=="Debris" or nm=="Sounds" or nm=="Events" or nm=="Interacts" or nm=="rods"
               or nm=="ToolsAdds" or nm=="__PROJECTILES" or nm=="RenderedMovingAnimals" or nm=="Road"
               or nm=="Plots" then return false end
            local low = nm:lower()
            if low:find("justafan",1,true) or low:find("esp",1,true) or low:find("tracer",1,true)
               or low:find("highlight",1,true) or low:find("projectile",1,true)
               or low:find("_clone",1,true) or low:find("vfx",1,true) or low:find("cybermap",1,true) then return false end
            if n.Parent == Workspace then top = n end
            n = n.Parent
        end
        if n ~= Workspace then return false end
        if top:FindFirstChildOfClass("Humanoid") then return false end
        return true
    end
    local _onAdd; _onAdd = function(p) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not p:IsA("BasePart") then return end
        local ok, res = pcall(_isStructural, p)
        if ok and res then
            _obsDirtyAt = tick()
            local s, cf = p.Size, p.CFrame
            local _hx = (math.abs(cf.RightVector.X)*s.X + math.abs(cf.UpVector.X)*s.Y + math.abs(cf.LookVector.X)*s.Z) * 0.5
            local _hy = (math.abs(cf.RightVector.Y)*s.X + math.abs(cf.UpVector.Y)*s.Y + math.abs(cf.LookVector.Y)*s.Z) * 0.5
            local _hz = (math.abs(cf.RightVector.Z)*s.X + math.abs(cf.UpVector.Z)*s.Y + math.abs(cf.LookVector.Z)*s.Z) * 0.5
            _obsNew[#_obsNew + 1] = { x = p.Position.X, z = p.Position.Z, r = math.max(_hx, _hz), top = p.Position.Y + _hy, t = tick() }
            if #_obsNew > 96 then table.remove(_obsNew, 1) end
        end end
    local _onRem; _onRem = function(p) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _G.JAF_TpActive == true and _G.JAF_ObsRemDuringTp ~= true then return end
        if not p:IsA("BasePart") then return end
        local ok, res = pcall(_isStructural, p)
        if ok and res then _obsDirtyAt = tick() end
    end
    _G.JAF_ObstacleWatchConn  = Workspace.DescendantAdded:Connect(_onAdd)
    _G.JAF_ObstacleWatchConn2 = Workspace.DescendantRemoving:Connect(_onRem)
end
local function f2UnderActive(y) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local on = _G.JAF_F2StealBelow
    if on == nil then on = Config.TpSettings and Config.TpSettings.F2StealBelow end
    return on == true and y > 10 and y <= 22
end
_G.JAF_F2UnderActive = f2UnderActive
local computeFrontRoute; computeFrontRoute = function(part, fromPos) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
    local exactPos = part.Position
    local isSecondFloor = exactPos.Y > 10 and not f2UnderActive(exactPos.Y)
    local signPart = getClosestBaseSign(part)
    local targetBasePos
    local waypoints, usedPathfinding = {}, false
    local _sideChosen, _f2DropIn = false, false
    local _baseAvoidRoute
    local function _segAABB(ax, az, bx, bz, x0, z0, x1, z1) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
        local dx, dz = bx - ax, bz - az; local tmin, tmax = 0, 1
        if math.abs(dx) < 1e-6 then if ax < x0 or ax > x1 then return false end
        else local t1, t2 = (x0 - ax) / dx, (x1 - ax) / dx; if t1 > t2 then t1, t2 = t2, t1 end tmin = math.max(tmin, t1); tmax = math.min(tmax, t2); if tmin > tmax then return false end end
        if math.abs(dz) < 1e-6 then if az < z0 or az > z1 then return false end
        else local t1, t2 = (z0 - az) / dz, (z1 - az) / dz; if t1 > t2 then t1, t2 = t2, t1 end tmin = math.max(tmin, t1); tmax = math.min(tmax, t2); if tmin > tmax then return false end end
        return true
    end
    local _cR, _lE
    local function _charDims() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if not _cR then
            local _c = LocalPlayer.Character
            local _h = _c and _c:FindFirstChild("HumanoidRootPart")
            local _hum = _c and _c:FindFirstChildOfClass("Humanoid")
            _cR = _h and math.clamp(math.max(_h.Size.X, _h.Size.Z) * 0.5 + 3, 4, 7) or 4
            _lE = _h and math.max(3, _h.Size.Y * 0.5 + ((_hum and _hum.HipHeight) or 2)) or 3
        end
        return _cR, _lE
    end
    local function _rpEx(extra, respect) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local ig = { LocalPlayer.Character }
        local cl = Workspace:FindFirstChild(tostring(LocalPlayer.UserId) .. "_Clone")
        if cl then ig[#ig + 1] = cl end
        if extra then for _, e in ipairs(extra) do ig[#ig + 1] = e end end
        local rp = RaycastParams.new()
        rp.FilterType = Enum.RaycastFilterType.Exclude
        rp.FilterDescendantsInstances = ig
        if respect then rp.RespectCanCollide = true end
        return rp, ig
    end
    local function tryPath(dst, srcY) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if _baseAvoidRoute then local _r = _baseAvoidRoute(dst, srcY) if _r and #_r >= 1 then return _r, true end end
        return nil, false
    end
    local function cruiseYFor(_floorY) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if not isSecondFloor then return -2.8 end
        local _cy2 = tonumber(_G.JAF_CruiseY_F2)
        if not _cy2 then
            _cy2 = 18
            if signPart and signPart:IsA("BasePart") then
                local _cfS, _sS = signPart.CFrame, signPart.Size
                local _eyS = math.abs(_cfS.RightVector.Y) * _sS.X + math.abs(_cfS.UpVector.Y) * _sS.Y + math.abs(_cfS.LookVector.Y) * _sS.Z
                local _, _legS = _charDims()
                _cy2 = signPart.Position.Y + _eyS * 0.5 + _legS + (tonumber(_G.JAF_F2SignClear) or 2)
            end end
        return _cy2
    end
    local plotsFolder = Workspace:FindFirstChild("Plots")
    local function plotOf(inst) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local node = inst
        while node and node.Parent do if node.Parent == plotsFolder then return node end node = node.Parent end
        return nil
    end
    local destPlot = signPart and plotOf(signPart) or nil
    local myPlot = nil
    if plotsFolder then
        local meName, meDisp = LocalPlayer.Name:lower(), LocalPlayer.DisplayName:lower()
        for _, plot in ipairs(plotsFolder:GetChildren()) do
            local sign = plot:FindFirstChild("PlotSign")
            local label = sign and sign:FindFirstChildWhichIsA("TextLabel", true)
            if label then
                local t = tostring(label.Text or ""):lower()
                if t:find(meName, 1, true) or t:find(meDisp, 1, true) then myPlot = plot; break end
            end end end
    local function collectObstacles() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
        local _abs, _min, _max, _huge = math.abs, math.min, math.max, math.huge
        local _charR = tonumber(_G.JAF_CharRadius) or _charDims()
        local _obPad, _groundXZ = _G.JAF_ObstaclePad or 4, _G.JAF_GroundXZ or 90
        local _obMaxR, _plotMrg = _G.JAF_ObstacleMaxR or 150, _G.JAF_PlotSkipMargin or 4
        local _flatDef, _noclipSkipOn = (_G.JAF_WheelFlat ~= false), (_G.JAF_ObstacleSkipNoclip ~= false and _G.JAF_NoclipMachines ~= false)
        local _noclipKW = _G.JAF_NoclipKW or { "truck", "fuse", "craft", "incubator", "cupid", "soccerball" }
        local function _plotXZ(plot) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            if not plot then return nil end
            local _ok, _cf, _sz = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return plot:GetBoundingBox() end)
            if not _ok or not _cf then return nil end
            return _cf.Position.X, _cf.Position.Z, _max(_sz.X, _sz.Z) * 0.5
        end
        local _dpx, _dpz, _dpr = _plotXZ(destPlot)
        local _mpx, _mpz, _mpr = _plotXZ(myPlot)
        local function _nearPlot(cx, cz) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            return (_dpx and Vector2.new(cx - _dpx, cz - _dpz).Magnitude < (_dpr + _plotMrg))
                or (_mpx and Vector2.new(cx - _mpx, cz - _mpz).Magnitude < (_mpr + _plotMrg)) or false
        end
        local function _inb(q) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _abs(q.X)<50000 and _abs(q.Y)<50000 and _abs(q.Z)<50000 end
        local function _pbox(p) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
            local q = p.Position
            if not _inb(q) then return nil end
            local s, cf = p.Size, p.CFrame
            local rv, uv, lv = cf.RightVector, cf.UpVector, cf.LookVector
            local hx = (_abs(rv.X)*s.X + _abs(uv.X)*s.Y + _abs(lv.X)*s.Z) * 0.5
            local hy = (_abs(rv.Y)*s.X + _abs(uv.Y)*s.Y + _abs(lv.Y)*s.Z) * 0.5
            local hz = (_abs(rv.Z)*s.X + _abs(uv.Z)*s.Y + _abs(lv.Z)*s.Z) * 0.5
            if _max(hx, hz) * 2 > _groundXZ then return nil end
            return q, hx, hy, hz
        end
        local _exName = { Debris=true, Sounds=true, Interacts=true, rods=true, __PROJECTILES=true, RenderedMovingAnimals=true, ToolsAdds=true, Road=true }
        local _exSub = { "justafan","esp","tracer","xitp","xitemp","_clone","highlight","projectile","cybermap","vfx" }
        local function _excludedTop(inst) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
            if inst == myPlot or inst == destPlot or inst == plotsFolder or inst:IsA("Terrain") or inst:IsA("Camera") then return true end
            local nm = inst.Name
            if _exName[nm] or (nm == "Events" and _G.JAF_ObstacleSkipEvents == true) then return true end
            local n = nm:lower()
            for _, k in ipairs(_exSub) do if n:find(k, 1, true) then return true end end
            return inst:FindFirstChildOfClass("Humanoid") ~= nil
        end
        local _c0 = LocalPlayer.Character
        local _hrp0 = _c0 and _c0:FindFirstChild("HumanoidRootPart")
        local out, seen, _grpOk, _psvc = {}, {}, {}, game:GetService("PhysicsService")
        local _chGrp = _hrp0 and _hrp0.CollisionGroup or "Default"
        local function _collidesMe(g) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
            if _G.JAF_CollGroupAware == false or g == _chGrp then return true end
            local v = _grpOk[g]
            if v == nil then
                local ok, res = pcall(_psvc.CollisionGroupsAreCollidable, _psvc, _chGrp, g)
                v = (not ok) or (res and true or false); _grpOk[g] = v
            end
            return v
        end
        local function _emit(nm, mnx, mny, mnz, mxx, mxy, mxz, isHaz, isSpin, nameSpin, parts) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local ex, ez = mxx - mnx, mxz - mnz
            local maxH = _max(ex, ez); local coreR = maxH * 0.5
            if maxH < 4 or coreR > _obMaxR then return end
            local cx, cz = (mnx + mxx) / 2, (mnz + mxz) / 2
            if _nearPlot(cx, cz) then return end
            local hx, hz = ex * 0.5, ez * 0.5
            local pad, wheel
            if isHaz then
                pad = (tonumber(_G.JAF_HazardPad) or 5) + _charR
            else
                local ar = _max(hx, hz) / _max(_min(hx, hz), 0.1)
                wheel = (isSpin or (nameSpin and (_G.JAF_SpinNameSquare == true or ar > (tonumber(_G.JAF_SpinArmRatio) or 1.6)))) and true or false
                pad = wheel and ((_G.JAF_WheelPad or 10) + _charR) or (_charR + _obPad)
            end
            out[#out + 1] = { center = Vector3.new(cx, (mny + mxy) / 2, cz), radius = coreR + pad, coreR = coreR, hx = hx, hz = hz, pad = pad, flat = _flatDef, name = nm, topY = mxy, wheel = wheel, haz = isHaz or nil, parts = parts }
        end
        local function _cluster(m) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
            if seen[m] then return end
            seen[m] = true
            local mn = m.Name
            local n = (mn or ""):lower()
            if _noclipSkipOn then for _, k in ipairs(_noclipKW) do if n:find(k, 1, true) then return end end end
            local nameSpin = n:find("wheel",1,true) or n:find("spin",1,true) or n:find("roulette",1,true) or n:find("rotat",1,true) or n:find("cyclone",1,true) or n:find("turbine",1,true)
            local _praw, _gY = {}, _huge
            for _, p in ipairs(m:IsA("BasePart") and { m } or m:GetDescendants()) do
                if p:IsA("BasePart") and p.CanCollide and _collidesMe(p.CollisionGroup) then
                    local q, hxp, hyp, hzp = _pbox(p)
                    if q then
                        local y0 = q.Y - hyp
                        if y0 < _gY then _gY = y0 end
                        _praw[#_praw + 1] = { x0 = q.X-hxp, x1 = q.X+hxp, z0 = q.Z-hzp, z1 = q.Z+hzp, y0 = y0, y1 = q.Y+hyp, ey = hyp * 2, spin = p.AssemblyAngularVelocity.Magnitude > 0.5 }
                    end end end
            local mnx, mny, mnz, mxx, mxy, mxz = _huge, _huge, _huge, -_huge, -_huge, -_huge
            local nC, isSpin, _pparts = 0, false, {}
            local _PCAP, _slabOff = tonumber(_G.JAF_ObstaclePartCap) or 40, (_G.JAF_SlabFilter == false)
            local _slabH, _slabTop = tonumber(_G.JAF_SlabMaxH) or 1.5, _gY + (tonumber(_G.JAF_SlabTopBand) or 1.2)
            local _slabGround = _gY <= (tonumber(_G.JAF_SlabGroundCeilY) or 2)
            for _, pp in ipairs(_praw) do
                if _slabOff or pp.spin or not (_slabGround and pp.ey <= _slabH and pp.y1 <= _slabTop) then
                    mnx=_min(mnx,pp.x0); mny=_min(mny,pp.y0); mnz=_min(mnz,pp.z0); mxx=_max(mxx,pp.x1); mxy=_max(mxy,pp.y1); mxz=_max(mxz,pp.z1)
                    if pp.spin then isSpin = true end
                    nC = nC + 1
                    if nC <= _PCAP then _pparts[nC] = pp end
                end end
            if nC == 0 then return end
            _emit(mn, mnx, mny, mnz, mxx, mxy, mxz, nil, isSpin, nameSpin, nC <= _PCAP and _pparts or nil)
        end
        local _containers = { Map = true, SummerMap = true, MapModels = true, Decorations = true }
        local function _scan(container, depth) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
            for _, child in ipairs(container:GetChildren()) do
                if not _excludedTop(child) then
                    local _m = child:IsA("Model")
                    if (child:IsA("Folder") or (_m and _containers[child.Name])) and depth < 4 then _scan(child, depth + 1)
                    elseif _m or child:IsA("BasePart") then _cluster(child) end
                end end end
        _scan(Workspace, 0)
        local _haz = _G.JAF_HazardNames or { "tradeplaza", "portal" }
        local _hazMemo = {}
        local function _isHaz(nm) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
            local v = _hazMemo[nm]
            if v ~= nil then return v end
            local l = nm:lower()
            v = false
            for _, k in ipairs(_haz) do if l:find(k, 1, true) then v = true; break end end
            _hazMemo[nm] = v
            return v
        end
        local _hseen, _ancC = {}, {}
        local function _ancHaz(node) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            if node == nil or node == Workspace then return false end
            local v = _ancC[node]
            if v == nil then v = _hseen[node] or _ancHaz(node.Parent); _ancC[node] = v end
            return v
        end
        for _, d in ipairs(Workspace:GetDescendants()) do
            if (d:IsA("Model") or d:IsA("BasePart")) and _isHaz(d.Name) and not _ancHaz(d.Parent) then
                _hseen[d] = true
                local mnx, mny, mnz, mxx, mxy, mxz = _huge, _huge, _huge, -_huge, -_huge, -_huge
                local nC = 0
                for _, p in ipairs(d:IsA("BasePart") and { d } or d:GetDescendants()) do
                    if p:IsA("BasePart") then
                        local q, hxp, hyp, hzp = _pbox(p)
                        if q then
                            mnx=_min(mnx,q.X-hxp); mny=_min(mny,q.Y-hyp); mnz=_min(mnz,q.Z-hzp); mxx=_max(mxx,q.X+hxp); mxy=_max(mxy,q.Y+hyp); mxz=_max(mxz,q.Z+hzp)
                            nC = nC + 1
                        end end end
                if nC > 0 then _emit(d.Name .. "*", mnx, mny, mnz, mxx, mxy, mxz, true) end
            end end
        if _G.JAF_ObstacleMemory ~= false then
            local _now, _mm = tick(), tonumber(_G.JAF_ObsMemMatch) or 10
            for _, o in ipairs(out) do
                if o.center then
                    local _hit
                    for _, m in ipairs(_obsMem) do
                        if m.name == o.name and _G.JAF_M2(m.center.X, m.center.Z, o.center.X, o.center.Z) <= _mm + _max(m.coreR or 0, o.coreR or 0) * 0.5 then _hit = m; break end
                    end
                    if _hit then
                        if (o.coreR or 0) > (_hit.coreR or 0) then
                            _hit.center, _hit.radius, _hit.coreR, _hit.hx, _hit.hz, _hit.pad, _hit.flat, _hit.wheel = o.center, o.radius, o.coreR, o.hx, o.hz, o.pad, o.flat, o.wheel
                        end
                        if (o.topY or -_huge) > (_hit.topY or -_huge) then _hit.topY = o.topY end
                        _hit.haz, _hit.seen = _hit.haz or o.haz, _now
                    else
                        _obsMem[#_obsMem + 1] = { name = o.name, center = o.center, radius = o.radius, coreR = o.coreR, hx = o.hx, hz = o.hz, pad = o.pad, topY = o.topY, haz = o.haz, wheel = o.wheel, flat = o.flat, seen = _now }
                    end end end
            local _meV, _seenMax = _hrp0 and _hrp0.Position, 0
            if _meV then
                for _, o in ipairs(out) do
                    local c = o.center
                    if c then local _sd = _G.JAF_M2(c.X, c.Z, _meV.X, _meV.Z); if _sd > _seenMax then _seenMax = _sd end end
                end end
            local _liveR = _max(tonumber(_G.JAF_ObsMemVerifyDist) or 60, _seenMax * (tonumber(_G.JAF_ObsMemLiveFrac) or 0.8))
            local _settling = (tick() - _obsDirtyAt) < (tonumber(_G.JAF_ObsSettleQuiet) or 1.0)
            local _memAdd = 0
            for _i = #_obsMem, 1, -1 do
                local m = _obsMem[_i]
                if m.seen ~= _now then
                    if not _settling and _meV and _G.JAF_M2(m.center.X, m.center.Z, _meV.X, _meV.Z) <= _liveR then
                        table.remove(_obsMem, _i)
                    elseif not _nearPlot(m.center.X, m.center.Z) then
                        out[#out + 1] = { center = m.center, radius = m.radius, coreR = m.coreR, hx = m.hx, hz = m.hz, pad = m.pad, topY = m.topY, name = (m.name or "?") .. "~", haz = m.haz, wheel = m.wheel, flat = m.flat, remembered = true }
                        _memAdd = _memAdd + 1
                    end end end
            while #_obsMem > (tonumber(_G.JAF_ObsMemMax) or 400) do table.remove(_obsMem, 1) end
            _G.JAF_ObsMemN, _G.JAF_ObsMemInjected = #_obsMem, _memAdd
        end
        return out
    end
    local _obsKey = destPlot or myPlot or "g"
    local _obsHit = _obsCache[_obsKey]
    local _cyberWheels
    if _obsHit and (tick() - _obsHit.at) < (_G.JAF_ObsCacheTTL or 12) and _obsDirtyAt <= _obsHit.at then
        _cyberWheels = _obsHit.wheels
    else
    _cyberWheels = (_G.JAF_ObstacleScan ~= false) and collectObstacles() or {}
    _obsCache[_obsKey] = { wheels = _cyberWheels, at = tick() }
    end
    if _G.JAF_LiftOverObstacles ~= false then
        local _cy = cruiseYFor(0)
        local _, _legExt = _charDims()
        local _wheelClr = math.max(tonumber(_G.JAF_LiftClearance)    or 7, _legExt + 1.5)
        local _flyClr   = math.max(tonumber(_G.JAF_FlyOverClearance) or 3, _legExt + 1.5)
        local _liftCap = tonumber(_G.JAF_LiftMaxUp) or (_cy < 5 and tonumber(_G.JAF_F1LiftMaxUp)) or 14
        local _noLift = _G.JAF_NoLiftNames or { "admin" }
        local _noLiftCeil = tonumber(_G.JAF_NoLiftMaxCruise)
        local function _forceDodge(w) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            if _noLiftCeil and _cy > _noLiftCeil then return false end
            local nm = (w.name or ""):lower()
            for _, k in ipairs(_noLift) do if nm:find(k, 1, true) then return true end end
            return false
        end
        local _lift = {}
        local _f1flat = (_G.JAF_F1FlatCruise ~= false) and _cy < 5
        for _, w in ipairs(_cyberWheels) do
            local _clr = w.wheel and _wheelClr or _flyClr
            if _f1flat then
                local _flyY = math.min(_cy, (fromPos and fromPos.Y) or _cy, (exactPos and exactPos.Y) or _cy)
                w.lifted = (w.topY and not w.haz and (w.topY <= _flyY - _legExt - 0.5)) or nil
            else
                local _canOver = (w.topY + _clr <= _cy + _liftCap)
                if _canOver and _G.JAF_ClimbAwareLift ~= false and not w.wheel and fromPos and w.center then
                    local _tgt = exactPos or targetBasePos
                    if _tgt then
                        local _span = 0.9 * math.max(Vector2.new(_tgt.X - fromPos.X, _tgt.Z - fromPos.Z).Magnitude, 1)
                        local _dO   = Vector2.new(w.center.X - fromPos.X, w.center.Z - fromPos.Z).Magnitude
                        local _flightY = fromPos.Y + (_cy - fromPos.Y) * math.clamp(_dO / _span, 0, 1)
                        if _flightY < w.topY + _clr then _canOver = false end
                    end end
                w.lifted = (w.topY and not w.haz and not _forceDodge(w) and (w.wheel or _canOver)) or nil
            end
            if w.lifted and not _f1flat then
                _lift[#_lift + 1] = { x = w.center.X, z = w.center.Z, r = w.radius, cr = w.coreR or w.radius, top = w.topY, liftY = w.topY + _clr }
            end end
        _G.JAF_FlightLiftObstacles = _lift
    else
        for _, w in ipairs(_cyberWheels) do w.lifted = nil end
        _G.JAF_FlightLiftObstacles = nil
    end
    if _G.JAF_ObstacleScan ~= false then
        local _obs = {}
        for _, w in ipairs(_cyberWheels) do
            if not w.lifted then _obs[#_obs + 1] = { x = w.center.X, z = w.center.Z, r = w.radius,
                                                     cr = (w.coreR or 0) + 2, strict = w.haz or nil } end
        end
        _G.JAF_FlightObstacles = _obs
    else
        _G.JAF_FlightObstacles = nil
    end
    local function _sweepHitsWheel(px, py, pz, dx, dy, dz) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
        local segLen2 = dx * dx + dy * dy + dz * dz
        if segLen2 <= 0.01 then return false end
        for _, w in ipairs(_cyberWheels) do
            local t = ((w.center.X - px) * dx + (w.center.Y - py) * dy + (w.center.Z - pz) * dz) / segLen2
            if t < 0 then t = 0 elseif t > 1 then t = 1 end
            local ox = px + dx * t - w.center.X
            local oy = w.flat and 0 or (py + dy * t - w.center.Y)
            local oz = pz + dz * t - w.center.Z
            if (ox * ox + oy * oy + oz * oz) < (w.radius * w.radius) then return true end
        end
        return false
    end
    local function wheelDetour(dst, cruiseY) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if #_cyberWheels == 0 then return nil end
        local origin = Vector3.new(fromPos.X, cruiseY, fromPos.Z)
        local d = Vector3.new(dst.X - origin.X, 0, dst.Z - origin.Z)
        local segLen2 = d.X * d.X + d.Z * d.Z
        if segLen2 < 4 then return nil end
        local _best, _bestT
        for _, w in ipairs(_cyberWheels) do
            local t = ((w.center.X - origin.X) * d.X + (w.center.Z - origin.Z) * d.Z) / segLen2
            if t > 0.02 and t < 0.98 then
                local ox = (origin.X + d.X * t) - w.center.X
                local oz = (origin.Z + d.Z * t) - w.center.Z
                local distXZ = math.sqrt(ox * ox + oz * oz)
                if distXZ < w.radius and (not _bestT or t < _bestT) then _bestT = t; _best = { w = w, ox = ox, oz = oz, dist = distXZ } end
            end end
        if not _best then return nil end
        local w = _best.w
        local sx, sz
        if _best.dist > 0.1 then
            sx, sz = _best.ox / _best.dist, _best.oz / _best.dist
        else
            local pm = math.sqrt(d.X * d.X + d.Z * d.Z)
            sx, sz = -d.Z / pm, d.X / pm
        end
        local clr = w.radius + (_G.JAF_WheelDetourPad or 6)
        return Vector3.new(w.center.X + sx * clr, cruiseY, w.center.Z + sz * clr)
    end
    local function baseDetour(dst, cruiseY) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local _dsN = destPlot and destPlot.Name
        local m = _G.JAF_BaseLineMargin or 7
        local function _crossAny(ax,az,bx,bz) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            for _, f in ipairs(_G.JAF_BaseFootprints or {}) do
                if f.minX and f.plot ~= _dsN
                   and not (fromPos.X >= f.minX - m and fromPos.X <= f.maxX + m and fromPos.Z >= f.minZ - m and fromPos.Z <= f.maxZ + m) then
                    if _segAABB(ax,az,bx,bz, f.minX-m, f.minZ-m, f.maxX+m, f.maxZ+m) then return f end
                end end
            return nil
        end
        local ax,az,bx,bz = fromPos.X, fromPos.Z, dst.X, dst.Z
        local f = _crossAny(ax,az,bx,bz)
        if not f then return nil end
        local cx,cz = (f.minX+f.maxX)*0.5, (f.minZ+f.maxZ)*0.5
        local reach = math.max(f.maxX-f.minX, f.maxZ-f.minZ)*0.5 + (_G.JAF_BaseDetourClear or 16)
        local dx,dz = bx-ax, bz-az
        local Llen = math.sqrt(dx*dx + dz*dz); if Llen < 1 then return nil end
        local px,pz = -dz/Llen, dx/Llen
        for _, s in ipairs({ 1, -1 }) do
            local ox,oz = cx + px*s*reach, cz + pz*s*reach
            if not _crossAny(ax,az,ox,oz) and not _crossAny(ox,oz,bx,bz) then return Vector3.new(ox, cruiseY, oz) end
        end
        return nil
    end
    _baseAvoidRoute = function(dst, cruiseY) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
        local M = tonumber(_G.JAF_BaseAvoidMargin) or (_charDims() + math.clamp(tonumber(_G.JAF_BaseMarginPad) or 2, 1, 4))
        _G.JAF_BaseAvoidMarginUsed = M
        local E = _G.JAF_BaseAvoidCornerOut or 3
        local _dsN = destPlot and destPlot.Name
        local fx, fz, dxp, dzp = fromPos.X, fromPos.Z, dst.X, dst.Z
        if _G.JAF_DstByContainment ~= false then
            local LM = tonumber(_G.JAF_DstNearMargin) or 12
            local _signN, _bestPlot, _bestD = _dsN, nil, nil
            for _, f in ipairs(_G.JAF_BaseFootprints or {}) do
                if f.minX then
                    local _dxg, _dzg = math.max(f.minX - dxp, dxp - f.maxX, 0), math.max(f.minZ - dzp, dzp - f.maxZ, 0)
                    local _ed = math.sqrt(_dxg * _dxg + _dzg * _dzg)
                    if _ed <= LM and _G.JAF_ContainPreferSign ~= false and _signN and f.plot == _signN then _bestPlot = f.plot; _bestD = -1
                    elseif _ed <= LM and _bestD ~= -1 and (not _bestD or _ed < _bestD) then _bestD = _ed; _bestPlot = f.plot end
                end end
            if _bestPlot then _dsN = _bestPlot end
        end
        local function _inM(f, x, z, m) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return x >= f.minX - m and x <= f.maxX + m and z >= f.minZ - m and z <= f.maxZ + m end
        local boxes = {}
        for _, f in ipairs(_G.JAF_BaseFootprints or {}) do
            if f.minX and f.plot ~= _dsN then
                local p = (not _inM(f, fx, fz, M) and not _inM(f, dxp, dzp, M)) and M
                       or ((not _inM(f, fx, fz, 0) and not _inM(f, dxp, dzp, 0)) and 1 or nil)
                if p then boxes[#boxes + 1] = { x0 = f.minX - p, z0 = f.minZ - p, x1 = f.maxX + p, z1 = f.maxZ + p, wall = true } end
            end end
        local _op, _skipFrac = _G.JAF_ObjAvoidPad or 2, tonumber(_G.JAF_ObstacleSkipCoreFrac) or 0.85
        local _flyHalf, _propBoxPad = tonumber(_G.JAF_FlyOverMaxHalf) or 6, tonumber(_G.JAF_PropBoxPad) or M
        local function _pushBox(bcx, bcz, brx, brz, hard, pad) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local b = { x0 = bcx - brx, z0 = bcz - brz, x1 = bcx + brx, z1 = bcz + brz, hard = hard, cx = bcx, cz = bcz, rhx = brx - pad, rhz = brz - pad, pad = pad }
            boxes[#boxes + 1] = b; return b
        end
        for _, w in ipairs(_cyberWheels or {}) do
            if w.lifted ~= true then
                local cx, cz = w.center.X, w.center.Z
                local _pad = (w.pad or (w.radius - (w.coreR or 0))) + _op
                if not w.wheel and not w.haz then
                    _pad = math.min(_pad, _propBoxPad)
                    local _rhx0, _rhz0 = (w.hx or w.coreR or 0), (w.hz or w.coreR or 0)
                    for _, e in ipairs({ { fx, fz }, { dxp, dzp } }) do
                        local _rg = math.max((cx - _rhx0) - e[1], e[1] - (cx + _rhx0), (cz - _rhz0) - e[2], e[2] - (cz + _rhz0), 0)
                        if _rg > 0 and _rg <= _pad + 0.5 then _pad = math.max(1, _rg - 0.75) end
                    end end
                local _rx, _rz = (w.wheel and w.coreR or (w.hx or w.coreR)) + _pad, (w.wheel and w.coreR or (w.hz or w.coreR)) + _pad
                local _sdx, _sdz, _ddx, _ddz = fx - cx, fz - cz, dxp - cx, dzp - cz
                local _sIn, _dIn
                if w.wheel or _G.JAF_SkipCoreRect == false or not (w.hx and w.hz) then
                    local _ex = (w.coreR or w.radius) * _skipFrac; local _ex2 = _ex * _ex
                    _sIn, _dIn = (_sdx * _sdx + _sdz * _sdz) < _ex2, (_ddx * _ddx + _ddz * _ddz) < _ex2
                else
                    local _chx, _chz = w.hx * _skipFrac, w.hz * _skipFrac
                    _sIn = math.abs(_sdx) < _chx and math.abs(_sdz) < _chz
                    _dIn = math.abs(_ddx) < _chx and math.abs(_ddz) < _chz
                end
                if not _sIn and not _dIn then
                    local _hard = ((math.max(w.hx or w.coreR or 0, w.hz or w.coreR or 0) > _flyHalf) and not w.wheel and _G.JAF_HardWideObstacles ~= false) or nil
                    local _parts = (_G.JAF_CapabilityBoxes ~= false) and not w.wheel and not w.haz and w.parts or nil
                    if _parts then
                        local _bandTop = cruiseY + (tonumber(_G.JAF_PassUnderHead) or 6)
                        local _lowY = math.min(cruiseY, (fromPos and fromPos.Y) or cruiseY, (exactPos and exactPos.Y) or cruiseY)
                        local _bandBot, _band = _lowY - (tonumber(_G.JAF_PassOverFeet) or 3.0), {}
                        for _, pp in ipairs(_parts) do if pp.y0 <= _bandTop and pp.y1 >= _bandBot then _band[#_band + 1] = pp end end
                        local _nb = #_band
                        if _nb > 0 and _nb <= (tonumber(_G.JAF_SplitMaxParts) or 20) then
                            for _, pp in ipairs(_band) do _pushBox((pp.x0 + pp.x1) * 0.5, (pp.z0 + pp.z1) * 0.5, (pp.x1 - pp.x0) * 0.5 + _pad, (pp.z1 - pp.z0) * 0.5 + _pad, _hard, _pad) end
                        elseif _nb > 0 then
                            local bx0, bz0, bx1, bz1 = math.huge, math.huge, -math.huge, -math.huge
                            for _, pp in ipairs(_band) do
                                bx0 = math.min(bx0, pp.x0); bx1 = math.max(bx1, pp.x1); bz0 = math.min(bz0, pp.z0); bz1 = math.max(bz1, pp.z1)
                            end
                            _pushBox((bx0 + bx1) * 0.5, (bz0 + bz1) * 0.5, (bx1 - bx0) * 0.5 + _pad, (bz1 - bz0) * 0.5 + _pad, _hard, _pad)
                        end
                    else
                        local _b = _pushBox(cx, cz, _rx, _rz, _hard, _pad)
                        _b.haz = w.haz or nil; _b.wheel = w.wheel or nil
                    end end end end
        local function _segBox(ax, az, bx, bz, b) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _segAABB(ax, az, bx, bz, b.x0, b.z0, b.x1, b.z1) end
        local baseCell, uwalls = math.max(tonumber(_G.JAF_GridCell) or 6, 1), {}
        local _aisleX, _dstF, _dstOpenFace, _stF, _stFace, _stWin, _dstWin = tonumber(_G.JAF_AisleX) or -410
        do
            local T, MU = math.max(tonumber(_G.JAF_BaseWallT) or (1.5 * baseCell), 1.5 * baseCell), tonumber(_G.JAF_DstExteriorBerth) or M
            local function _openFace(f, px, pz) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                local dn, dp, zn, zp = px - f.minX, f.maxX - px, pz - f.minZ, f.maxZ - pz
                local m = math.min(dn, dp, zn, zp)
                if m == dn then return "-X" elseif m == dp then return "+X" elseif m == zn then return "-Z" else return "+Z" end
            end
            local function _addU(f, face, wc) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                local TT = math.min(T, 0.4 * math.min(f.maxX - f.minX, f.maxZ - f.minZ))
                local eW, eE = (face == "-X") and 0 or MU, (face == "+X") and 0 or MU
                local eN, eP = (face == "-Z") and 0 or MU, (face == "+Z") and 0 or MU
                if face ~= "-X" then uwalls[#uwalls + 1] = { x0 = f.minX - MU, z0 = f.minZ - eN, x1 = f.minX + TT, z1 = f.maxZ + eP } end
                if face ~= "+X" then uwalls[#uwalls + 1] = { x0 = f.maxX - TT, z0 = f.minZ - eN, x1 = f.maxX + MU, z1 = f.maxZ + eP } end
                if face ~= "-Z" then uwalls[#uwalls + 1] = { x0 = f.minX - eW, z0 = f.minZ - MU, x1 = f.maxX + eE, z1 = f.minZ + TT } end
                if face ~= "+Z" then uwalls[#uwalls + 1] = { x0 = f.minX - eW, z0 = f.maxZ - TT, x1 = f.maxX + eE, z1 = f.maxZ + MU } end
                local dh = tonumber(_G.JAF_DoorHalf) or 19
                local _xf, _neg = (face == "-X" or face == "+X"), (face == "-X" or face == "-Z")
                local a0, a1, n0, n1 = _xf and f.minZ or f.minX, _xf and f.maxZ or f.maxX, _xf and f.minX or f.minZ, _xf and f.maxX or f.maxZ
                wc = math.max(a0 + dh, math.min(wc or (a0 + a1) * 0.5, a1 - dh))
                local _sdOK = (_G.JAF_SmoothDoorApproach == true) and not isSecondFloor
                local JB, JT = _sdOK and math.max(tonumber(_G.JAF_DoorJambBerth) or 4, 0) or MU, _sdOK or nil
                local b0, b1 = _neg and (n0 - JB) or (n1 - TT), _neg and (n0 + TT) or (n1 + JB)
                local function _uw(alo, ahi) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    uwalls[#uwalls + 1] = _xf and { x0 = b0, z0 = alo, x1 = b1, z1 = ahi, jamb = JT } or { x0 = alo, z0 = b0, x1 = ahi, z1 = b1, jamb = JT }
                end
                if wc - dh > a0 + 1 then _uw(a0 - MU, wc - dh) end
                if a1 - 1 > wc + dh then _uw(wc + dh, a1 + MU) end
                return { axis = _xf and "z" or "x", c = wc, h = dh, t = TT }
            end
            local function _laserDoorZ(plotName) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                local pf = Workspace:FindFirstChild("Plots")
                local lh = pf and plotName and pf:FindFirstChild(plotName)
                lh = lh and lh:FindFirstChild("LaserHitbox")
                local m = lh and (lh:FindFirstChild("Main") or lh:FindFirstChildWhichIsA("BasePart"))
                return m and m.Position.Z or nil
            end
            local startF, dstF
            for _, f in ipairs(_G.JAF_BaseFootprints or {}) do
                if f.minX and not startF and _inM(f, fx, fz, 0) then startF = f end
                if f.minX and not dstF and f.plot == _dsN then dstF = f end
            end
            if startF then
                local _sf = ((startF.minX + startF.maxX) * 0.5 <= _aisleX) and "+X" or "-X"
                _G.JAF_StartRoof = false
                _stWin = _addU(startF, _sf, _laserDoorZ(startF.plot))
                _stF = startF; _stFace = _sf
            end
            if dstF and dstF ~= startF then
                local _front = ((dstF.minX + dstF.maxX) * 0.5 <= _aisleX) and "+X" or "-X"
                local _back  = (_front == "+X") and "-X" or "+X"
                local _band  = tonumber(_G.JAF_SideFlushBand) or 8
                local _of    = _openFace(dstF, dxp, dzp)
                if _sideChosen then _of = (dzp >= (dstF.minZ + dstF.maxZ) * 0.5) and "+Z" or "-Z"
                elseif _of == _back then _of = _front
                elseif _of == "-Z" or _of == "+Z" then
                    local _flush = (dzp - dstF.minZ <= _band) and "-Z" or ((dstF.maxZ - dzp <= _band) and "+Z" or nil)
                    if _flush ~= _of then _of = _front end
                end
                local _wc
                if _of == "-X" or _of == "+X" then _wc = _laserDoorZ(dstF.plot) else _wc = dxp end
                _dstWin = _addU(dstF, _of, _wc)
                _dstF = dstF; _dstOpenFace = _of
            end end
        local function _clearW(ax, az, bx, bz) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            for _, b in ipairs(boxes) do if _segBox(ax, az, bx, bz, b) then return false end end
            for _, b in ipairs(uwalls) do if _segBox(ax, az, bx, bz, b) then return false end end
            return true
        end
        local MB = _G.JAF_MapBounds
        local haveMB = MB and MB.minX and MB.maxX and MB.minZ and MB.maxZ and MB.maxX > MB.minX and MB.maxZ > MB.minZ
        local function _inB(x, z) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return (not haveMB) or (x >= MB.minX and x <= MB.maxX and z >= MB.minZ and z <= MB.maxZ) end
        local function _shortcut(route, clearFn) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local pts = { Vector3.new(fx, cruiseY, fz) }
            for _, v in ipairs(route) do pts[#pts + 1] = v end
            for _pass = 1, 3 do
                local removed, i = false, 2
                while i <= #pts - 1 do
                    local a, c = pts[i - 1], pts[i + 1]
                    if clearFn(a.X, a.Z, c.X, c.Z) and _inB(c.X, c.Z) then table.remove(pts, i); removed = true else i = i + 1 end
                end
                if not removed then break end
            end
            local o = {} for k = 2, #pts do o[#o + 1] = pts[k] end return o
        end
        if _G.JAF_GridAStar ~= false then
            local SQ2, CLR = 1.4142135623730951, math.max(tonumber(_G.JAF_PathClearance) or 4, 0)
            local basePad = tonumber(_G.JAF_GridPad) or 24
            repeat
                local cell, pCLR = baseCell, math.max(tonumber(_G.JAF_PropPullClearance) or 2, 0)
                if _G.JAF_PropSmooth == false then pCLR = CLR end
                local function _softProp(b) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return b.cx ~= nil and not b.wall and not b.hard and not b.haz and not b.wheel end
                local function _mkClear(hardC, softC, core) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    local L = {}
                    for _, b in ipairs(boxes) do
                        local _sp = _softProp(b)
                        if core and _sp then L[#L + 1] = { x0 = b.cx - (b.rhx + softC), z0 = b.cz - (b.rhz + softC), x1 = b.cx + (b.rhx + softC), z1 = b.cz + (b.rhz + softC) }
                        else local _c = _sp and softC or hardC; L[#L + 1] = { x0 = b.x0 - _c, z0 = b.z0 - _c, x1 = b.x1 + _c, z1 = b.z1 + _c } end
                    end
                    for _, b in ipairs(uwalls) do local _c = b.jamb and 0 or hardC; L[#L + 1] = { x0 = b.x0 - _c, z0 = b.z0 - _c, x1 = b.x1 + _c, z1 = b.z1 + _c } end
                    return function(ax, az, bx, bz) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                        for _, b in ipairs(L) do if _segAABB(ax, az, bx, bz, b.x0, b.z0, b.x1, b.z1) then return false end end
                        return true
                    end end
                local _clearUC = (CLR > 0) and _mkClear(CLR, pCLR, false) or _clearW
                local PGAP = CLR + 2
                local function _portalPt(f, face, ax, az, win) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    if win then
                        if win.axis == "z" then az = math.clamp(az, win.c - win.h + 1.5, win.c + win.h - 1.5) else ax = math.clamp(ax, win.c - win.h + 1.5, win.c + win.h - 1.5) end
                    end
                    local cm0 = math.max(tonumber(_G.JAF_PortalCornerMargin) or 7, 0)
                    local function _cmFor(span) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_PortalClampGuard == false then return cm0 end return math.min(cm0, math.max(0, span * 0.5 - 0.5)) end
                    if face == "+X" then local cm=_cmFor(f.maxZ-f.minZ); return f.maxX + PGAP, math.clamp(az, f.minZ + cm, f.maxZ - cm)
                    elseif face == "-X" then local cm=_cmFor(f.maxZ-f.minZ); return f.minX - PGAP, math.clamp(az, f.minZ + cm, f.maxZ - cm)
                    elseif face == "+Z" then local cm=_cmFor(f.maxX-f.minX); return math.clamp(ax, f.minX + cm, f.maxX - cm), f.maxZ + PGAP
                    else local cm=_cmFor(f.maxX-f.minX); return math.clamp(ax, f.minX + cm, f.maxX - cm), f.minZ - PGAP end
                end
                local function _nearF(f, x, z) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    local _m = tonumber(_G.JAF_DstNearMargin) or 12
                    local _dxg, _dzg = math.max(f.minX - x, x - f.maxX, 0), math.max(f.minZ - z, z - f.maxZ, 0)
                    return (_dxg * _dxg + _dzg * _dzg) <= _m * _m
                end
                local rfx, rfz, rdx, rdz, sPortal, gPortal, sAlign, gAlign = fx, fz, dxp, dzp
                local function _alignPt(f, face, win, tpc) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    local tin = ((win and win.t) or 9) + 1.5
                    if face == "-X" then return Vector3.new(f.minX + tin, cruiseY, tpc) elseif face == "+X" then return Vector3.new(f.maxX - tin, cruiseY, tpc)
                    elseif face == "-Z" then return Vector3.new(tpc, cruiseY, f.minZ + tin) else return Vector3.new(tpc, cruiseY, f.maxZ - tin) end
                end
                local function _offWin(win, tc) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return win ~= nil and math.abs(tc - win.c) > win.h - 1.5 end
                if _G.JAF_PortalRoute ~= false then
                    local _bothSame = _stF and _dstF and _stF == _dstF
                    if _stF and _stFace and not _bothSame and _inM(_stF, fx, fz, 0)
                       and not (_dstF and _inM(_stF, dxp, dzp, 0)) then
                        local px, pz = _portalPt(_stF, _stFace, fx, fz, _stWin)
                        if _inB(px, pz) then
                            rfx, rfz = px, pz; sPortal = Vector3.new(px, cruiseY, pz)
                            local _tc = (_stWin and _stWin.axis == "z") and fz or fx
                            if _offWin(_stWin, _tc) then sAlign = _alignPt(_stF, _stFace, _stWin, (_stWin.axis == "z") and pz or px) end
                        end end
                    local _needDstPortal = true
                    if _G.JAF_PortalOnlyIfWalled == true and not isSecondFloor and _dstF and _dstF.plot then
                        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                            local _plotsF = Workspace:FindFirstChild("Plots")
                            local _pInst  = _plotsF and _plotsF:FindFirstChild(tostring(_dstF.plot))
                            if not _pInst then return end
                            local _ivx, _ivz = (_dstF.minX + _dstF.maxX) * 0.5 - dxp, (_dstF.minZ + _dstF.maxZ) * 0.5 - dzp
                            local _ivm = math.sqrt(_ivx * _ivx + _ivz * _ivz)
                            if _ivm <= 0.01 then return end
                            local _rpW = RaycastParams.new()
                            _P(_rpW, "FilterType", Enum.RaycastFilterType.Include, "FilterDescendantsInstances", { _pInst }, "RespectCanCollide", true)
                            local _pd = math.clamp(tonumber(_G.JAF_PortalWallProbe) or 3, 1, 8)
                            if Workspace:Raycast(Vector3.new(dxp, ((dst and dst.Y) or cruiseY) + 1, dzp),
                                                 Vector3.new(_ivx / _ivm * _pd, 0, _ivz / _ivm * _pd), _rpW) then _needDstPortal = false end
                        end) end
                    if _dstF and _dstOpenFace and not _bothSame and _needDstPortal and _nearF(_dstF, dxp, dzp) then
                        local px, pz = _portalPt(_dstF, _dstOpenFace, dxp, dzp, _dstWin)
                        if _inB(px, pz) then
                            rdx, rdz = px, pz; gPortal = Vector3.new(px, cruiseY, pz)
                            local _tc = (_dstWin and _dstWin.axis == "z") and dzp or dxp
                            if _offWin(_dstWin, _tc) then
                                local _skipAlign = false
                                if _G.JAF_PadAlignSkip ~= false and not _inM(_dstF, dxp, dzp, 0) then
                                    if _dstOpenFace == "+X" then _skipAlign = dxp >= _dstF.maxX
                                    elseif _dstOpenFace == "-X" then _skipAlign = dxp <= _dstF.minX
                                    elseif _dstOpenFace == "+Z" then _skipAlign = dzp >= _dstF.maxZ
                                    else _skipAlign = dzp <= _dstF.minZ end
                                end
                                if not _skipAlign then gAlign = _alignPt(_dstF, _dstOpenFace, _dstWin, (_dstWin.axis == "z") and pz or px) end
                            end end end end
                if _G.JAF_GridDirectWhenClear == true and _G.JAF_SmoothDoorApproach ~= false and not isSecondFloor
                   and _inB(fx, fz) and _inB(dxp, dzp) and _clearW(fx, fz, dxp, dzp) then
                    _G.JAF_BaseAvoidDbg = "GRID direct-true a1"; return { dst }
                end
                if _inB(rfx, rfz) and _inB(rdx, rdz) and _clearW(rfx, rfz, rdx, rdz) then
                    local fastOut = {}
                    if sAlign then fastOut[#fastOut + 1] = sAlign end; if sPortal then fastOut[#fastOut + 1] = sPortal end
                    if gPortal then fastOut[#fastOut + 1] = gPortal end; if gAlign then fastOut[#fastOut + 1] = gAlign end
                    fastOut[#fastOut + 1] = dst
                    _G.JAF_BaseAvoidDbg = (sPortal or gPortal) and string.format("GRID portal-direct %d hops", #fastOut) or "GRID direct"
                    return fastOut
                end
                local gMinX, gMaxX = math.min(fx, dxp) - basePad, math.max(fx, dxp) + basePad
                local gMinZ, gMaxZ = math.min(fz, dzp) - basePad, math.max(fz, dzp) + basePad
                if haveMB then
                    gMinX = math.max(gMinX, MB.minX); gMaxX = math.min(gMaxX, MB.maxX)
                    gMinZ = math.max(gMinZ, MB.minZ); gMaxZ = math.min(gMaxZ, MB.maxZ)
                end
                if gMaxX <= gMinX or gMaxZ <= gMinZ then break end
                local cols, rows = math.floor((gMaxX - gMinX) / cell) + 1, math.floor((gMaxZ - gMinZ) / cell) + 1
                if cols * rows > (tonumber(_G.JAF_GridMaxCells) or 12000) then break end
                local function _ci(x) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return math.clamp(math.floor((x - gMinX) / cell) + 1, 1, cols) end
                local function _cj(z) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return math.clamp(math.floor((z - gMinZ) / cell) + 1, 1, rows) end
                local function _wx(ci) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return gMinX + (ci - 1) * cell + cell * 0.5 end
                local function _wz(cj) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return gMinZ + (cj - 1) * cell + cell * 0.5 end
                local function _idx(ci, cj) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return (cj - 1) * cols + ci end
                local blocked, hc = {}, cell * 0.5
                local function _stamp(b) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
                    local bx0, bx1, bz0, bz1 = b.x0, b.x1, b.z0, b.z1
                    local ci0, ci1 = _ci(bx0 - hc), _ci(bx1 + hc)
                    for cj = _cj(bz0 - hc), _cj(bz1 + hc) do
                        local cz = _wz(cj)
                        if not (cz + hc < bz0 or cz - hc > bz1) then
                            local rowbase = (cj - 1) * cols
                            for ci = ci0, ci1 do
                                local cx = _wx(ci); if not (cx + hc < bx0 or cx - hc > bx1) then blocked[rowbase + ci] = true end
                            end end end end
                for _, b in ipairs(boxes) do _stamp(b) end
                for _, b in ipairs(uwalls) do _stamp(b) end
                local function _freeNear(ci, cj) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    if not blocked[_idx(ci, cj)] then return ci, cj end
                    for r = 1, tonumber(_G.JAF_GridEscapeRing) or 6 do
                        for dj = -r, r do for di = -r, r do
                            if math.abs(di) == r or math.abs(dj) == r then
                                local ni, nj = ci + di, cj + dj
                                if ni >= 1 and ni <= cols and nj >= 1 and nj <= rows and not blocked[_idx(ni, nj)] then return ni, nj end
                            end
                        end end
                    end
                    blocked[_idx(ci, cj)] = false
                    return ci, cj
                end
                local sCi, sCj = _freeNear(_ci(rfx), _cj(rfz))
                local gCi, gCj = _freeNear(_ci(rdx), _cj(rdz))
                local sIdx, gIdx = _idx(sCi, sCj), _idx(gCi, gCj)
                local g, parent, f, closed, heap = {}, {}, {}, {}, {}
                local function _h(ci, cj) LPH_ATTRIBUTES(ERROR_HANDLING(false))
local dx, dz = math.abs(ci - gCi), math.abs(cj - gCj); return cell * ((dx + dz) + (SQ2 - 2) * math.min(dx, dz)) end
                local function _push(id) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
                    heap[#heap + 1] = id; local c = #heap
                    while c > 1 do
                        local p = math.floor(c / 2); if f[heap[p]] <= f[heap[c]] then break end
                        heap[p], heap[c] = heap[c], heap[p]; c = p
                    end end
                local function _pop() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
                    local top, n = heap[1], #heap
                    heap[1] = heap[n]; heap[n] = nil; n = n - 1; local c = 1
                    while true do
                        local l, r, s = c * 2, c * 2 + 1, c
                        if l <= n and f[heap[l]] < f[heap[s]] then s = l end if r <= n and f[heap[r]] < f[heap[s]] then s = r end
                        if s == c then return top end
                        heap[c], heap[s] = heap[s], heap[c]; c = s
                    end end
                g[sIdx] = 0; f[sIdx] = _h(sCi, sCj); _push(sIdx)
                local OFF = { {1,0,cell},{-1,0,cell},{0,1,cell},{0,-1,cell},
                              {1,1,SQ2*cell},{1,-1,SQ2*cell},{-1,1,SQ2*cell},{-1,-1,SQ2*cell} }
                local _tpen, pdir, reached = tonumber(_G.JAF_TurnPenalty) or 3, {}, false
                while #heap > 0 do
                    local u = _pop()
                    if not closed[u] then
                        if u == gIdx then reached = true; break end
                        closed[u] = true
                        local uj = math.floor((u - 1) / cols); local ui = u - uj * cols; uj = uj + 1
                        local pd, gu = pdir[u], g[u]
                        for _, o in ipairs(OFF) do
                            local o1, o2, w = o[1], o[2], o[3]; local ni, nj = ui + o1, uj + o2
                            if ni >= 1 and ni <= cols and nj >= 1 and nj <= rows then
                                local nIdx = _idx(ni, nj)
                                local okn = (not blocked[nIdx]) and (not closed[nIdx])
                                if okn and o1 ~= 0 and o2 ~= 0 and (blocked[_idx(ni, uj)] or blocked[_idx(ui, nj)]) then okn = false end
                                if okn then
                                    local ng = gu + w + ((pd and (pd[1] ~= o1 or pd[2] ~= o2)) and _tpen or 0)
                                    if g[nIdx] == nil or ng < g[nIdx] then
                                        g[nIdx] = ng; parent[nIdx] = u; pdir[nIdx] = o; f[nIdx] = ng + _h(ni, nj); _push(nIdx)
                                    end end end end end end
                if reached then
                    local rev, cc = {}, gIdx
                    while cc do rev[#rev + 1] = cc; cc = parent[cc] end
                    local P = { { x = rfx, z = rfz } }
                    for i = #rev - 1, 2, -1 do
                        local cj = math.floor((rev[i] - 1) / cols)
                        P[#P + 1] = { x = _wx(rev[i] - cj * cols), z = _wz(cj + 1) }
                    end
                    P[#P + 1] = { x = rdx, z = rdz }
                    local out, anchor, i = {}, 1, 2
                    while i < #P do
                        if _clearUC(P[anchor].x, P[anchor].z, P[i + 1].x, P[i + 1].z) and _inB(P[i + 1].x, P[i + 1].z) then i = i + 1
                        else out[#out + 1] = Vector3.new(P[i].x, cruiseY, P[i].z); anchor = i; i = i + 1 end
                    end
                    local final = {}
                    if sAlign then final[#final + 1] = sAlign end; if sPortal then final[#final + 1] = sPortal end
                    for _, v in ipairs(out) do final[#final + 1] = v end
                    if gPortal then
                        final[#final + 1] = gPortal; if gAlign then final[#final + 1] = gAlign end
                    else
                        local _lf = final[#final]
                        local _tx, _tz = _lf and _lf.X or rfx, _lf and _lf.Z or rfz
                        if not _clearW(_tx, _tz, dxp, dzp) then
                            if #final == 0 then final[#final + 1] = Vector3.new(_wx(sCi), cruiseY, _wz(sCj)) end
                            final[#final + 1] = Vector3.new(_wx(gCi), cruiseY, _wz(gCj))
                        end end
                    final[#final + 1] = dst
                    if _G.JAF_RoutePolish ~= false and #final > 1 then
                        local MC = math.max(tonumber(_G.JAF_MergeClearance) or 6, 0)
                        local MCp = (_G.JAF_PropSmooth == false) and MC or math.max(tonumber(_G.JAF_PropMergeClearance) or 2, 0)
                        final = _shortcut(final, _mkClear(MC, MCp, false))
                    end
                    local _debloatFinal
                    if _G.JAF_FinalSmooth ~= false then
                        _debloatFinal = function(route) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                            if not route or #route <= 2 then return route end
                            return _shortcut(route, _mkClear(math.max(tonumber(_G.JAF_MergeClearance) or 6, 0), math.max(tonumber(_G.JAF_FinalSmoothClr) or ((_G.JAF_FinalSmoothSafe ~= false) and 6 or 3), 0), true))
                        end end
                    if _G.JAF_ColinearCollapse ~= false and #final > 2 then
                        local _px, _pz, k = fx, fz, 1
                        while k <= #final - 1 do
                            local b, c = final[k], final[k + 1]
                            local abx, abz, bcx, bcz = b.X - _px, b.Z - _pz, c.X - b.X, c.Z - b.Z
                            local la, lc = math.sqrt(abx * abx + abz * abz), math.sqrt(bcx * bcx + bcz * bcz)
                            if la > 0.01 and lc > 0.01 and math.abs(abx * bcz - abz * bcx) / (la * lc) < 0.026 then table.remove(final, k) else _px, _pz = b.X, b.Z; k = k + 1 end
                        end end
                    _G.JAF_BaseAvoidDbg = string.format("GRID A* %d hops %dx%d %db %du a1%s", #final, cols, rows, #boxes, #uwalls, (sPortal or gPortal) and " P" or "")
                    return _debloatFinal and _debloatFinal(final) or final
                end until true
            _G.JAF_BaseAvoidDbg = "GRID ENCLOSED -> corner-graph"
            if _G.JAF_GridEnclosedDirect == true then return { dst } end
        end
        if _clearW(fx, fz, dxp, dzp) then return { dst } end
        local nodes = { { x = fx, z = fz }, { x = dxp, z = dzp } }
        local function _addNode(x, z) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _inB(x, z) then nodes[#nodes + 1] = { x = x, z = z } end end
        for _, L in ipairs({ boxes, uwalls }) do for _, b in ipairs(L) do
            _addNode(b.x0 - E, b.z0 - E); _addNode(b.x0 - E, b.z1 + E); _addNode(b.x1 + E, b.z0 - E); _addNode(b.x1 + E, b.z1 + E)
        end end
        local N, INF, prev, reached = #nodes, math.huge, {}, false
        do
            local g, f, open, closed = {}, {}, {}, {}
            for i = 1, N do g[i] = INF; f[i] = INF end
            local function _h(i) LPH_ATTRIBUTES(ERROR_HANDLING(false))
local hx, hz = nodes[i].x - dxp, nodes[i].z - dzp; return math.sqrt(hx*hx + hz*hz) end
            g[1] = 0; f[1] = _h(1); open[1] = true
            while true do
                local u, best = nil, INF
                for i = 1, N do if open[i] and f[i] < best then best = f[i]; u = i end end
                if not u then break end
                if u == 2 then reached = true; break end
                open[u] = nil; closed[u] = true
                local nu = nodes[u]
                for v = 1, N do
                    local nv = nodes[v]
                    if not closed[v] and _clearW(nu.x, nu.z, nv.x, nv.z) then
                        local ddx, ddz = nv.x - nu.x, nv.z - nu.z
                        local tentative = g[u] + math.sqrt(ddx * ddx + ddz * ddz)
                        if tentative < g[v] then prev[v] = u; g[v] = tentative; f[v] = tentative + _h(v); open[v] = true end
                    end end end end
        if not reached then _G.JAF_BaseAvoidDbg = string.format("ENCLOSED: no base-free route (%d boxes)", #boxes); return { dst } end
        local order, c = {}, 2
        while c do order[#order + 1] = c; c = prev[c] end
        local out = {}
        for i = #order - 1, 1, -1 do local nd = nodes[order[i]]; out[#out + 1] = Vector3.new(nd.x, cruiseY, nd.z) end
        out[#out] = dst
        _G.JAF_BaseAvoidDbg = string.format("A* ROUTE %d hops around %d bases", #out, #boxes)
        return out
    end
    do
        local _barRaw, _barMemo = _baseAvoidRoute, {}
        local _barNoDbg = "\0jafBarNoDbg"
        _baseAvoidRoute = function(dst, cruiseY) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            if _G.JAF_RouteMemo == false or typeof(dst) ~= "Vector3" or type(cruiseY) ~= "number" then return _barRaw(dst, cruiseY) end
            local k = string.format("%.17g|%.17g|%.17g|%.17g", dst.X, dst.Y, dst.Z, cruiseY)
            local m = _barMemo[k]
            if m then
                if m.dbg ~= _barNoDbg then _G.JAF_BaseAvoidDbg = m.dbg end
            else
                local _prev = _G.JAF_BaseAvoidDbg
                _G.JAF_BaseAvoidDbg = _barNoDbg
                local r = _barRaw(dst, cruiseY)
                local d = _G.JAF_BaseAvoidDbg
                if d == _barNoDbg then _G.JAF_BaseAvoidDbg = _prev end
                m = { r = r, dbg = d }
                _barMemo[k] = m
            end
            local mr = m.r
            if not mr then return nil end
            local c = table.create(#mr)
            for i = 1, #mr do c[i] = mr[i] end
            return c
        end end
    local function straightClear(dst, cruiseY) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local origin = Vector3.new(fromPos.X, cruiseY, fromPos.Z)
        local destC  = Vector3.new(dst.X, cruiseY, dst.Z)
        local d = destC - origin
        if d.Magnitude < 2 then return true end
        if _sweepHitsWheel(origin.X, origin.Y, origin.Z, d.X, d.Y, d.Z) then return false end
        if _G.JAF_FootprintCheck ~= false then
            local _ax, _az, _bx, _bz = origin.X, origin.Z, destC.X, destC.Z
            local _dsN = destPlot and destPlot.Name
            local _m = _G.JAF_BaseLineMargin or 7
            for _, f in ipairs(_G.JAF_BaseFootprints or {}) do
                if f.minX and f.plot ~= _dsN then
                    local _x0, _z0, _x1, _z1 = f.minX - _m, f.minZ - _m, f.maxX + _m, f.maxZ + _m
                    local _rc = (_G.JAF_Step15RawContain ~= false)
                    local _cx0,_cz0,_cx1,_cz1 = (_rc and f.minX or _x0), (_rc and f.minZ or _z0), (_rc and f.maxX or _x1), (_rc and f.maxZ or _z1)
                    if not (dst.X >= _cx0 and dst.X <= _cx1 and dst.Z >= _cz0 and dst.Z <= _cz1)
                       and not (_ax >= _cx0 and _ax <= _cx1 and _az >= _cz0 and _az <= _cz1)
                       and _segAABB(_ax, _az, _bx, _bz, _x0, _z0, _x1, _z1) then
                        return false
                    end end end end
        local rp, ignore = _rpEx()
        for _, c in ipairs(Workspace:GetChildren()) do if c.Name == "XiTempPlatform" then table.insert(ignore, c) end end
        if destPlot and _G.JAF_DodgeTargetBase == false then table.insert(ignore, destPlot) end
        if myPlot and _G.JAF_DodgeOwnBase ~= true then table.insert(ignore, myPlot) end
        rp.FilterDescendantsInstances = ignore
        local function _rayBlocked(o) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local hit = Workspace:Raycast(o, d, rp)
            if not hit then return false end
            local _node = hit.Instance
            while _node and _node ~= Workspace do if _node.Name:find("Trade") then return true end _node = _node.Parent end
            if _G.JAF_NearDestPlotStrict ~= false and plotsFolder and destPlot then
                local _p = hit.Instance
                while _p and _p.Parent do
                    if _p.Parent == plotsFolder then if _p ~= destPlot and _p ~= myPlot then return true end break end
                    _p = _p.Parent
                end end
            return Vector2.new(hit.Position.X - dst.X, hit.Position.Z - dst.Z).Magnitude > (_G.JAF_NearDestTol or 8)
        end
        local _perp = Vector3.new(-d.Z, 0, d.X)
        if _perp.Magnitude > 0.01 then _perp = _perp.Unit * 5 else _perp = Vector3.zero end
        if _rayBlocked(origin) then return false end
        if _rayBlocked(origin + _perp) then return false end
        if _rayBlocked(origin - _perp) then return false end
        return true
    end
    local function pathClearOfWheels(wps) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if not wps or #wps == 0 or #_cyberWheels == 0 then return true end
        local prev = Vector3.new(fromPos.X, fromPos.Y, fromPos.Z)
        for _, wp in ipairs(wps) do
            if _sweepHitsWheel(prev.X, prev.Y, prev.Z, wp.X - prev.X, wp.Y - prev.Y, wp.Z - prev.Z) then return false end
            prev = wp
        end
        return true
    end
    if signPart then
        local intoBase = getSignInwardDir(signPart)
        local _sPos = signPart.Position; local _rpR = _rpEx(nil, true)
        local floorY = isSecondFloor and (_sPos.Y + 4) or -4.8
        if isSecondFloor then
            local _in6 = _sPos + intoBase * 6
            local _gF = Workspace:Raycast(Vector3.new(_in6.X, _sPos.Y + 8, _in6.Z), Vector3.new(0, -20, 0), _rpR)
            if _gF and _gF.Position.Y < _sPos.Y + 6 and _gF.Position.Y > _sPos.Y - 8 then floorY = _gF.Position.Y end
        end
        local cruiseY = cruiseYFor(floorY)
        local _hiCast = isSecondFloor and _G.JAF_F2SideHighCast ~= false
        local _standOff = 3.5
        local _lc = LocalPlayer.Character
        local _lh, _lhum = _lc and _lc:FindFirstChild("HumanoidRootPart"), _lc and _lc:FindFirstChildOfClass("Humanoid")
        if _lh and _lhum then _standOff = math.clamp(_lhum.HipHeight + _lh.Size.Y * 0.5 + (_G.JAF_FootClearance or 0.3), 2.5, 9) end
        local _fs = floorY + _standOff; local _downBd = isSecondFloor and (tonumber(_G.JAF_F2FloorDownBand) or 8) or 18
        local function _groundY(x, z) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local _g = Workspace:Raycast(Vector3.new(x, floorY + 16, z), Vector3.new(0, -70, 0), _rpR)
            if _g and (_g.Position.Y - floorY) <= 18 and (floorY - _g.Position.Y) <= _downBd then return math.min(_g.Position.Y + _standOff, _fs), true end
            return _fs, false
        end
        local _probeY = floorY + 3
        if isSecondFloor and signPart:IsA("BasePart") then
            local _cfP, _sP = signPart.CFrame, signPart.Size
            _probeY = math.max(_probeY, _sPos.Y + (math.abs(_cfP.RightVector.Y) * _sP.X + math.abs(_cfP.UpVector.Y) * _sP.Y + math.abs(_cfP.LookVector.Y) * _sP.Z) * 0.5 + 1)
        end
        local _hit = Workspace:Raycast(Vector3.new(_sPos.X - intoBase.X * 6, _probeY, _sPos.Z - intoBase.Z * 6), intoBase * 36, (_rpEx()))
        local _distIn = _hit and (_hit.Position - _sPos):Dot(intoBase) or 999
        if _distIn < 0 then _hit = nil; _distIn = 999 end
        local _landXZ = (_hit and _distIn < 12) and (_hit.Position - intoBase * (_G.JAF_LaserStandoff or 1.0)) or (_sPos + intoBase * (_G.JAF_CloneStandoff or 5))
        local _perp = Vector3.new(-intoBase.Z, 0, intoBase.X); _perp = (_perp.Magnitude > 0.01) and _perp.Unit or Vector3.new(0, 0, 1)
        local function _wallBlocked(cand, cy) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local o = Vector3.new(fromPos.X, cy, fromPos.Z); local dir = Vector3.new(cand.X, cy, cand.Z) - o; local dist = dir.Magnitude
            if dist < 2 then return false end
            local rpw, igw = _rpEx()
            if myPlot and _G.JAF_DodgeOwnBase ~= true then table.insert(igw, myPlot) end
            for _ = 1, 6 do
                rpw.FilterDescendantsInstances = igw
                local hw = Workspace:Raycast(o, dir, rpw)
                if not hw then return false end
                if hw.Instance.CanCollide then return (hw.Position - o).Magnitude < dist - 4 end
                table.insert(igw, hw.Instance)
            end
            return false
        end
        local _sideNavOut
        local function _computeSidePoint(force) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            if not destPlot then return nil end
            local _signFlat = Vector3.new(_sPos.X, 0, _sPos.Z)
            local _sideDir = ((Vector3.new(fromPos.X, 0, fromPos.Z) - _signFlat):Dot(_perp) >= 0) and 1 or -1
            local _nx, _nz = _perp.X * _sideDir, _perp.Z * _sideDir; local _castDir = _perp * (-_sideDir) * 60
            local _wallEdge, _fMinX, _fMaxX, _fMinZ, _fMaxZ
            for _, f in ipairs(_G.JAF_BaseFootprints or {}) do
                if f.plot == destPlot.Name then
                    _fMinX, _fMaxX, _fMinZ, _fMaxZ = f.minX, f.maxX, f.minZ, f.maxZ
                    local _lx0, _lx1 = (f.minX - _signFlat.X) * _perp.X, (f.maxX - _signFlat.X) * _perp.X
                    local _lz0, _lz1 = (f.minZ - _signFlat.Z) * _perp.Z, (f.maxZ - _signFlat.Z) * _perp.Z
                    _wallEdge = (_sideDir > 0) and (math.max(_lx0, _lx1) + math.max(_lz0, _lz1)) or (math.min(_lx0, _lx1) + math.min(_lz0, _lz1))
                    break
                end end
            local _pods = destPlot:FindFirstChild("AnimalPodiums")
            local function _podSpawn(n) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                local p = _pods and _pods:FindFirstChild(n)
                p = p and p:FindFirstChild("Base"); p = p and p:FindFirstChild("Spawn")
                return (p and p:IsA("BasePart")) and p or nil
            end
            local _corner
            for _, _sn in ipairs({ "1", "10" }) do
                local s = _podSpawn(_sn); local _r = s and (s.Position - _sPos)
                if _r and (((_r.X * _perp.X + _r.Z * _perp.Z) >= 0) == (_sideDir > 0)) then _corner = s end
            end
            _corner = _corner or _podSpawn("1") or _podSpawn("10")
            local _cr = _corner and (_corner.Position - _sPos)
            local _cornerDepth = _cr and (_cr.X * intoBase.X + _cr.Z * intoBase.Z) or math.clamp((Vector3.new(exactPos.X, 0, exactPos.Z) - _signFlat):Dot(intoBase), 8, 14)
            local _depthFront = _cornerDepth
            local _rpS = RaycastParams.new(); _P(_rpS, "FilterType", Enum.RaycastFilterType.Include, "FilterDescendantsInstances", { destPlot }, "RespectCanCollide", true)
            local _ss = math.max(_G.JAF_SideStandoff or 1, 0.5); local _clr = _G.JAF_SidePathClearance or 12
            if type(_cornerDepth) ~= "number" or _cornerDepth ~= _cornerDepth then _cornerDepth = (type(_depthFront) == "number" and _depthFront == _depthFront) and _depthFront or 11 end
            local _ledgeDn = tonumber(_G.JAF_F2LedgeDownBand) or 6; local _ledgeNy = tonumber(_G.JAF_F2LedgeFlatNy) or 0.95
            local function _entryY(x, z) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                local _g = Workspace:Raycast(Vector3.new(x, floorY + 3, z), Vector3.new(0, -22, 0), _rpR)
                if _g and _g.Normal.Y > 0.5 and (not isSecondFloor or ((floorY - _g.Position.Y) <= _ledgeDn and _g.Normal.Y > _ledgeNy)) then return _g.Position.Y + _standOff end
                return _fs
            end
            if _G.JAF_SideDepthGuard ~= false and math.abs(_cornerDepth - _depthFront) > 0.5 then
                local _gProbeY = _hiCast and (floorY + 4) or (floorY + 1)
                local _fBand = tonumber(_G.JAF_SideDepthFaceBand) or 1.5; local _band = tonumber(_G.JAF_SideDepthGroundBand) or 3
                local function _sideProbe(_d) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    local _ia2 = _sPos + intoBase * _d
                    local _h3 = Workspace:Raycast(Vector3.new(_ia2.X + _nx * 60, _gProbeY, _ia2.Z + _nz * 60), _castDir, _rpS)
                    if not _h3 then return nil, nil end
                    local _g3 = Workspace:Raycast(Vector3.new(_h3.Position.X + _nx * _ss, floorY + 10, _h3.Position.Z + _nz * _ss), Vector3.new(0, -40, 0), _rpR)
                    return (_h3.Position.X - _sPos.X) * _perp.X + (_h3.Position.Z - _sPos.Z) * _perp.Z, (_g3 and _g3.Position.Y or nil)
                end
                local _latF, _gF = _sideProbe(_depthFront)
                local function _depthOK(_d) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    local _lat, _gy = _sideProbe(_d)
                    if (not _lat) or (not _gy) then return false end
                    if _latF and _fBand > 0 and math.abs(_lat - _latF) > _fBand then return false end
                    return not (_gF and math.abs(_gy - _gF) > _band)
                end
                if not _depthOK(_cornerDepth) then
                    local _ideal, _found = _cornerDepth, nil
                    for _k = 1, math.clamp(tonumber(_G.JAF_SideDepthSteps) or 6, 1, 12) do
                        local _up, _dn = _ideal + _k, _ideal - _k
                        if _up <= _depthFront and _depthOK(_up) then _found = _up break end
                        if _dn >= _depthFront and _depthOK(_dn) then _found = _dn break end
                    end
                    _cornerDepth = _found or _depthFront
                end end
            local _inAt = _sPos + intoBase * _cornerDepth
            local _o = Vector3.new(_inAt.X + _nx * 60, floorY + 1, _inAt.Z + _nz * 60)
            local _sh = Workspace:Raycast(_o, _castDir, _rpS); local _wallOk = (_sh ~= nil)
            if _hiCast then
                local _shHigh = Workspace:Raycast(Vector3.new(_o.X, floorY + 4, _o.Z), _castDir, _rpS)
                if _shHigh then _sh = _shHigh end
                _wallOk = (_shHigh ~= nil)
            end
            local _faceX, _faceZ
            if _sh then _faceX, _faceZ = _sh.Position.X, _sh.Position.Z else
                local _lat = _sideDir * math.abs(_wallEdge or 22)
                _faceX, _faceZ = _sPos.X + intoBase.X * _cornerDepth + _perp.X * _lat, _sPos.Z + intoBase.Z * _cornerDepth + _perp.Z * _lat
            end
            local _ssUse = _ss
            if not isSecondFloor then
                local _pb = tonumber(_G.JAF_F1SidePullback)
                if _pb == nil then
                    local _pc = LocalPlayer.Character; local _ph = _pc and _pc:FindFirstChild("HumanoidRootPart")
                    _pb = math.max(0, ((_ph and math.max(_ph.Size.X, _ph.Size.Z) * 0.5) or 1) + (tonumber(_G.JAF_SideWallClear) or 0.6) - _ss)
                end
                _ssUse = _ss + _pb
            end
            local _sX, _sZ = _faceX + _nx * _ssUse, _faceZ + _nz * _ssUse
            if _fMinX and _G.JAF_SideOutsideClamp == true then
                if math.abs(_nx) >= math.abs(_nz) then
                    local _flush = ((_nx >= 0) and _fMaxX or _fMinX) + ((_nx >= 0) and _ss or -_ss)
                    if (_nx >= 0 and _sX < _flush) or (_nx < 0 and _sX > _flush) then _sX = _flush end
                else
                    local _flush = ((_nz >= 0) and _fMaxZ or _fMinZ) + ((_nz >= 0) and _ss or -_ss)
                    if (_nz >= 0 and _sZ < _flush) or (_nz < 0 and _sZ > _flush) then _sZ = _flush end
                end end
            local _oX, _oZ = _sX + _nx * _clr, _sZ + _nz * _clr; local _need = _G.JAF_SignClearance or 8
            local function _clearSign(x, z) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                if not signPart:IsA("BasePart") then return x, z end
                local _cf, _sz = signPart.CFrame, signPart.Size
                local _rel = _cf:PointToObjectSpace(Vector3.new(x, _sPos.Y, z))
                local _nr = _cf:PointToWorldSpace(Vector3.new(math.clamp(_rel.X, -_sz.X * 0.5, _sz.X * 0.5), _rel.Y, math.clamp(_rel.Z, -_sz.Z * 0.5, _sz.Z * 0.5)))
                local _dx, _dz = x - _nr.X, z - _nr.Z; local _d = math.sqrt(_dx * _dx + _dz * _dz)
                if _d >= _need then return x, z end
                if _d < 0.01 then _dx, _dz, _d = -intoBase.X, -intoBase.Z, 1 end
                return x + (_dx / _d) * (_need - _d), z + (_dz / _d) * (_need - _d)
            end
            _sX, _sZ = _clearSign(_sX, _sZ); _oX, _oZ = _clearSign(_oX, _oZ)
            return Vector3.new(_sX, _entryY(_sX, _sZ), _sZ), Vector3.new(_oX, _entryY(_oX, _oZ), _oZ), false, _wallOk
        end
        local function _applySideMode(mode) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            if mode == "front" or not destPlot then return end
            local _sp, _sno, _cdrop, _rayOk = _computeSidePoint(mode == "side")
            if not _sp then return end
            local _take = (mode == "side")
            if not _take then
                local function _cost(dst) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    local _len = Vector2.new(dst.X - fromPos.X, dst.Z - fromPos.Z).Magnitude; local _cy = cruiseYFor(dst.Y)
                    if not straightClear(dst, _cy) or _wallBlocked(dst, _cy) then _len = _len + (_G.JAF_DetourPenalty or 30) end
                    return _len
                end
                local _bias, _fpen = _G.JAF_SideBias or 0, _G.JAF_FrontPenalty or 0
                local _frontCost, _sideCost = _cost(targetBasePos) + _fpen, _cost(_sp) - _bias
                local _chose = (_G.JAF_AutoSidePick ~= false and _sideCost < _frontCost) and "side" or "front"
                if _chose == "side" and _G.JAF_AutoSideRevert ~= false and not _rayOk then _chose = "front" end
                _take = (_chose == "side")
            end
            if _take then targetBasePos = _sp; _sideNavOut = _sno; _sideChosen = true; _f2DropIn = _cdrop or false end
        end
        local function _navToTarget(cy) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            if not (_sideNavOut or not straightClear(targetBasePos, cy) or _wallBlocked(targetBasePos, cy)) then return end
            local wps = _baseAvoidRoute(((_G.JAF_OwnPathfinder ~= false) and targetBasePos) or _sideNavOut or targetBasePos, cy)
            if wps and #wps > 0 then
                waypoints, usedPathfinding = wps, true
                local _e = wps[#wps]
                if Vector2.new(_e.X - targetBasePos.X, _e.Z - targetBasePos.Z).Magnitude > 2 then table.insert(waypoints, targetBasePos) end
            end end
        if not isSecondFloor then
            local _xz; local _laser = destPlot and destPlot:FindFirstChild("LaserHitbox")
            _laser = _laser and (_laser:FindFirstChild("Main") or _laser:FindFirstChildWhichIsA("BasePart"))
            if _laser then
                local _front = _laser.Position - intoBase * ((math.abs(_laser.Size.X * intoBase.X) + math.abs(_laser.Size.Z * intoBase.Z)) * 0.5)
                local _so = tonumber(_G.JAF_LaserStandoff)
                if _so == nil then
                    local _reach, _bc = 0.57, LocalPlayer.Character
                    for _, _bn in ipairs({ "HumanoidRootPart", "UpperTorso", "LowerTorso", "Head", "Torso" }) do
                        local _bp = _bc and _bc:FindFirstChild(_bn)
                        if _bp and _bp:IsA("BasePart") then
                            local _hd = (_bn == "Head") and (_bp.Size.Z * 0.5) or (math.min(_bp.Size.X, _bp.Size.Z) * 0.5)
                            if _hd > _reach then _reach = _hd end
                        end end
                    _so = _reach + (tonumber(_G.JAF_LaserGap) or 0.15)
                end
                _xz = _front - intoBase * _so
            else
                _xz = _sPos + intoBase * (_G.JAF_CloneStandoff or 5)
            end
            targetBasePos = Vector3.new(_xz.X, _groundY(_xz.X, _xz.Z), _xz.Z)
            local _f1m = _G.JAF_F1Mode or (Config.TpSettings and Config.TpSettings.F1SpotMode) or "auto"
            if _G.JAF_F1SideApproach == false then _f1m = "front" end
            if _f1m == "auto" and destPlot and _G.JAF_UnlockedFront ~= false then
                local _ok, _ul = pcall(isPlotUnlocked, destPlot.Name)
                if _ok and _ul == true then _f1m = "front" end
            end
            _applySideMode(_f1m)
            _navToTarget(cruiseY)
        else
            local _LAD = { 3, 7, 11, 15, 19, 24 }
            local _maxDep = _hit and (_distIn - math.max(_G.JAF_LaserStandoff or 1.0, 1.0)) or (tonumber(_G.JAF_F2MaxDepth) or 8)
            local _shallow = _G.JAF_F2ShallowLanding ~= false; local _dynC, _dynScore = nil, -math.huge
            for _, _dep in ipairs(_LAD) do
                if _dep <= _maxDep then
                    local _xz = _sPos + intoBase * _dep
                    local _gy, _gFloor = _groundY(_xz.X, _xz.Z); local _cand = Vector3.new(_xz.X, _gy, _xz.Z)
                    if _gFloor and straightClear(_cand, cruiseY) and not _wallBlocked(_cand, cruiseY) then
                        local _sc = _shallow and (-_dep * 1.5) or (_dep * 1.5 - Vector2.new(_cand.X - exactPos.X, _cand.Z - exactPos.Z).Magnitude)
                        if _sc > _dynScore then _dynScore = _sc; _dynC = _cand end
                    end end end
            if not _dynC then
                local _fbY, _fbOk = _groundY(_landXZ.X, _landXZ.Z)
                if not _fbOk then
                    for _, _dep in ipairs(_LAD) do
                        local _xz2 = _sPos + intoBase * _dep
                        local _fy2, _fok2 = _groundY(_xz2.X, _xz2.Z)
                        if _fok2 then _dynC = Vector3.new(_xz2.X, _fy2, _xz2.Z) break end
                    end end
                targetBasePos = _dynC or Vector3.new(_landXZ.X, _fbY, _landXZ.Z)
            else
                targetBasePos = _dynC
            end
            _applySideMode(_G.JAF_F2Mode or (Config.TpSettings and Config.TpSettings.F2SpotMode) or "front")
            _navToTarget(cruiseY)
        end
    elseif isSecondFloor then
        local bd2 = math.huge
        for _, v in pairs(BASES_HIGH) do
            local d = (Vector2.new(exactPos.X, exactPos.Z) - Vector2.new(v.X, v.Z)).Magnitude
            if d < bd2 then bd2 = d; targetBasePos = Vector3.new(v.X, v.Y + 2, v.Z) end
        end
    else
        local bd2 = math.huge
        for _, v in ipairs(CLONE_FLOOR) do local d = (exactPos - v).Magnitude if d < bd2 then bd2 = d; targetBasePos = v end end
    end
    if not targetBasePos then return nil, {}, false, signPart, isSecondFloor, exactPos end
    if not usedPathfinding and not _f2DropIn and (_sideChosen or not straightClear(targetBasePos, cruiseYFor(targetBasePos.Y))) then
        local wps, ok = tryPath(targetBasePos, cruiseYFor(targetBasePos.Y))
        if ok and pathClearOfWheels(wps) then
            waypoints = wps
            usedPathfinding = true
            local lw = wps[#wps]
            if Vector2.new(lw.X - targetBasePos.X, lw.Z - targetBasePos.Z).Magnitude > 2 then table.insert(waypoints, targetBasePos) end
        end end
    if not usedPathfinding and not _f2DropIn and targetBasePos then
        local _det = ((_G.JAF_BaseDetour == true) and baseDetour(targetBasePos, cruiseYFor(targetBasePos.Y)) or nil)
                  or wheelDetour(targetBasePos, cruiseYFor(targetBasePos.Y))
        if _det then waypoints = { _det, targetBasePos } usedPathfinding = true end
    end
    local function _routeCrosses(route) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
        if not (route and targetBasePos) then return nil end
        local _vm  = _G.JAF_RouteValidateMargin or 3
        local _dsN = destPlot and destPlot.Name
        local function _inBox(x, z, f) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return x >= f.minX and x <= f.maxX and z >= f.minZ and z <= f.maxZ end
        local _px, _pz = fromPos.X, fromPos.Z
        for _, w in ipairs(route) do
            for _, f in ipairs(_G.JAF_BaseFootprints or {}) do
                if f.minX and f.plot ~= _dsN
                   and not _inBox(fromPos.X, fromPos.Z, f) and not _inBox(targetBasePos.X, targetBasePos.Z, f)
                   and _segAABB(_px, _pz, w.X, w.Z, f.minX - _vm, f.minZ - _vm, f.maxX + _vm, f.maxZ + _vm) then
                    return "base " .. string.format("(%.0f,%.0f)", (f.minX + f.maxX) * 0.5, (f.minZ + f.maxZ) * 0.5)
                end end
            for _, o in ipairs(_cyberWheels or {}) do
                local ox, oz, r = o.center.X, o.center.Z, o.radius
                if not (fromPos.X >= ox - r and fromPos.X <= ox + r and fromPos.Z >= oz - r and fromPos.Z <= oz + r)
                   and not (targetBasePos.X >= ox - r and targetBasePos.X <= ox + r and targetBasePos.Z >= oz - r and targetBasePos.Z <= oz + r) then
                    local _dx, _dz = w.X - _px, w.Z - _pz
                    local _s2 = _dx * _dx + _dz * _dz
                    local _t = (_s2 > 0.001) and (((ox - _px) * _dx + (oz - _pz) * _dz) / _s2) or 0
                    if _t < 0 then _t = 0 elseif _t > 1 then _t = 1 end
                    local _cx, _cz = _px + _dx * _t - ox, _pz + _dz * _t - oz
                    if _cx * _cx + _cz * _cz < r * r then return "obj " .. string.format("(%.0f,%.0f)", ox, oz) end
                end end
            _px, _pz = w.X, w.Z
        end
        return nil
    end
    if _G.JAF_RouteValidate ~= false and targetBasePos and waypoints and #waypoints > 0 then
        local _crossed = _routeCrosses(waypoints)
        if _crossed then
            local _safe = _baseAvoidRoute(targetBasePos, cruiseYFor(targetBasePos.Y))
            if _safe and #_safe >= 1 and not _routeCrosses(_safe) then
                waypoints = _safe
                usedPathfinding = true
                _G.JAF_BaseAvoidDbg = "REVALIDATED: route crossed " .. _crossed .. " -> geometric reroute (" .. #_safe .. " hops)"
            end end end
    if _G.JAF_RouteLenGuard ~= false and targetBasePos and waypoints and #waypoints > 0 then
        local function _routeLen(wl) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local _l, _px, _pz = 0, fromPos.X, fromPos.Z
            for _, w in ipairs(wl) do _l = _l + Vector2.new(w.X - _px, w.Z - _pz).Magnitude; _px, _pz = w.X, w.Z end
            return _l
        end
        local _geo = _baseAvoidRoute(targetBasePos, cruiseYFor(targetBasePos.Y))
        if _geo and #_geo >= 1 and not _routeCrosses(_geo) then
            local _curLen, _geoLen = _routeLen(waypoints), _routeLen(_geo)
            if _geoLen > 1 and _curLen > _geoLen * (_G.JAF_RouteLenFactor or 1.3) then
                waypoints = _geo
                usedPathfinding = true
                _G.JAF_BaseAvoidDbg = string.format("LENGUARD: chosen %.0f > %.2fx geo %.0f -> geometric (%d hops)",
                    _curLen, (_G.JAF_RouteLenFactor or 1.3), _geoLen, #_geo)
            end end end
    if _G.JAF_F2LandGuard ~= false and isSecondFloor and targetBasePos
       and targetBasePos.Y < (tonumber(_G.JAF_F2MinLandY) or 0) then
        local _safeY = tonumber(_G.JAF_F2GuardLandY) or (signPart and signPart:IsA("BasePart") and (signPart.Position.Y + 8)) or 14.4
        targetBasePos = Vector3.new(targetBasePos.X, _safeY, targetBasePos.Z)
    end
    return targetBasePos, waypoints, usedPathfinding, signPart, isSecondFloor, exactPos, _sideChosen, _f2DropIn
end
local _wasStealingHit = false
local _jafMinGenRaw, _jafMinGenVal = nil, 0
local jafMinGenThreshold; jafMinGenThreshold = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local raw = Config.TpSettings and Config.TpSettings.MinGenForTp
    if type(raw) ~= "string" or raw == "" then return 0 end
    if raw == _jafMinGenRaw then return _jafMinGenVal end
    local s = raw:gsub("%s", ""):lower()
    local num, suf = s:match("^([%d%.]+)([kmb]?)$")
    num = tonumber(num)
    local v
    if not num or num < 0 then v = 0
    elseif suf == "k" then v = num * 1e3
    elseif suf == "m" then v = num * 1e6
    elseif suf == "b" then v = num * 1e9
    else v = num end
    _jafMinGenRaw, _jafMinGenVal = raw, v
    return v
end
local jafPassesMinGen; jafPassesMinGen = (function(genValue) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local th = jafMinGenThreshold()
    if th <= 0 then return true end
    return (tonumber(genValue) or 0) >= th
end)
_G.JAF_PassesMinGen = jafPassesMinGen
_G.JAF_MyBaseXZ = (function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if _G.__jafMyBaseXZ and (tick() - (_G.__jafMyBaseAt or 0)) < (tonumber(_G.JAF_MyBaseTTL) or 10) then return _G.__jafMyBaseXZ end
    local xz = nil
    local pf = Workspace:FindFirstChild("Plots")
    if pf then
        local dn, un = LocalPlayer.DisplayName:lower(), LocalPlayer.Name:lower()
        for _, pl in ipairs(pf:GetChildren()) do
            local sg = pl:FindFirstChild("PlotSign")
            local mine = false
            if sg then
                local yb = sg:FindFirstChild("YourBase")
                if yb and yb:IsA("BillboardGui") and yb.Enabled then mine = true
                else
                    local lb = sg:FindFirstChildWhichIsA("TextLabel", true)
                    if lb then local t = (lb.Text or ""):lower(); if t:find(dn, 1, true) or t:find(un, 1, true) then mine = true end end
                end end
            if mine then
                local ok, cf = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return pl:GetBoundingBox() end)
                if ok and cf then xz = Vector2.new(cf.Position.X, cf.Position.Z) end
                break
            end end end
    _G.__jafMyBaseXZ = xz; _G.__jafMyBaseAt = tick()
    return xz
end)
_G.JAF_IsNeighbourBase = function(pos) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not pos then return false end
    local m = _G.JAF_MyBaseXZ()
    if not m then return nil end
    return Vector2.new(pos.X - m.X, pos.Z - m.Y).Magnitude < (tonumber(_G.JAF_F2NeighbourDist) or 130)
end
local function runAutoSnipe(retryCount, committed) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if isTpMoving then
        _G.JAF_CancelTp = true
        _G.JAF_HitRecCooldownUntil = tick() + 3
        SharedState._armedRecoveryTarget = nil
        SharedState._armedRecoveryPos    = nil
        if JAF_GrappleStop then pcall(JAF_GrappleStop) end
        task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
local _cs = tick() while isTpMoving and tick() - _cs < 16 do task.wait(0.05) end _G.JAF_CancelTp = false end)
        pcall(_G.JAF_HB.clear)
        return
    end
    _G.JAF_TpStartedFlight = false
    if _G.JAF_StripWatchers_run then pcall(_G.JAF_StripWatchers_run) end
    if _G.JAF_CarpetBuyOwnsTp and not manualTarget then
        isTpMoving = false
        SharedState.FirstLoadSteal = false
        if _G.JAF_LoadHeavyNow then _G.JAF_LoadHeavyNow() end
        return
    end
    if LocalPlayer:GetAttribute("StealingIndex") or LocalPlayer:GetAttribute("Stealing") then return end
    SharedState.FirstLoadSteal = false
    _G.JAF_TpGen = (_G.JAF_TpGen or 0) + 1
    local _myTpGen = _G.JAF_TpGen
    task.delay(10, function() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
        if isTpMoving and _G.JAF_TpGen == _myTpGen then
            _G.JAF_CancelTp = true
            isTpMoving = false
            if JAF_GrappleStop then pcall(JAF_GrappleStop) end
            task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
RunService.Heartbeat:Wait(); RunService.Heartbeat:Wait(); _G.JAF_CancelTp = false end)
        end end)
    local _stealAbortConn
    _stealAbortConn = LocalPlayer:GetAttributeChangedSignal("StealingIndex"):Connect(function() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
        if LocalPlayer:GetAttribute("StealingIndex") and isTpMoving then
            isTpMoving = false
            if _stealAbortConn then _stealAbortConn:Disconnect(); _stealAbortConn = nil end
        end
        if LocalPlayer:GetAttribute("StealingIndex") then
            SharedState._currentTpTarget = nil
            SharedState._armedRecoveryTarget = nil
            SharedState._armedRecoveryPos    = nil
            _G.JAF_RecConsecutive = 0
            pcall(_G.JAF_HB.clear)
        end end)
    task.delay(15, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _stealAbortConn then _stealAbortConn:Disconnect(); _stealAbortConn = nil end end)
    local activeTpMode = "front"
    local startChar = LocalPlayer.Character
    local alive; alive = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _G.JAF_CancelTp then return false end
        if LocalPlayer.Character ~= startChar or startChar == nil then return false end
        local hum = startChar:FindFirstChildOfClass("Humanoid")
        if hum and hum.Health <= 0 then return false end
        return true
    end
    local targetPetData = nil
    if manualTarget then
        targetPetData = manualTarget
    else
        if SharedState.ScanAllPlots then pcall(SharedState.ScanAllPlots) end
        local pickStart = tick()
        repeat
            local bestRank = math.huge
            local bestGen  = -math.huge
            local _aliveMemo, _ghosts = {}, nil
            local _meName = LocalPlayer.Name
            for _, a in ipairs(SharedState.AllAnimalsCache) do
                local _live = true
                if a and _G.JAF_TpAliveCheck ~= false then
                    local _aok, _al, _awhy = pcall(_G.JAF_PetAlive, a, _aliveMemo)
                    if _aok and _al == false then
                        _live  = false
                        _ghosts = _ghosts or {}; _ghosts[#_ghosts + 1] = a
                        _G.JAF_PetDeadSkips = (_G.JAF_PetDeadSkips or 0) + 1
                        _G.JAF_PetDeadWhy   = _awhy
                    end end
                if a and _live and a.owner ~= _meName and _G.JAF_PassesMinGen(a.genValue) then
                    if Config.AutoTPPriority ~= false then
                        local rank = _G.JAF_PrioScore(a)
                        if rank < bestRank or (rank == bestRank and a.genValue > bestGen) then
                            bestRank = rank
                            bestGen  = a.genValue
                            targetPetData = a
                        end
                    else
                        if a.genValue > bestGen then bestGen = a.genValue targetPetData = a end
                    end end end
            if _ghosts then
                for _g = 1, #_ghosts do
                    if _G.JAF_ScanStale and _ghosts[_g].plot then pcall(_G.JAF_ScanStale, _ghosts[_g].plot) end
                    for _i = #SharedState.AllAnimalsCache, 1, -1 do
                        if SharedState.AllAnimalsCache[_i] == _ghosts[_g] then table.remove(SharedState.AllAnimalsCache, _i); break end
                    end end end
            if not targetPetData then task.wait(0.05) end
        until targetPetData or tick()-pickStart > 3 or not alive() end
    SharedState._currentTpTarget = targetPetData
    if not targetPetData then isTpMoving = false if _G.JAF_LoadHeavyNow then _G.JAF_LoadHeavyNow() end return end
    if not alive() then isTpMoving = false; return end
    local _routeDrawn = _frontRouteCache and targetPetData and _frontRouteCache.uid == targetPetData.uid and _frontRouteCache.waypoints
    local _routeBypass = _routeDrawn and (_G.JAF_CarpetGateHard == false)
    if _G.JAF_CarpetGate ~= false and _G.JAF_NoCarpetScan ~= true and not manualTarget and not committed and not _routeBypass and SharedState.WalkingCache then
        local _prio = (_G.JAF_CarpetPriorityWins ~= false) and (Config.AutoTPPriority ~= false) and type(PRIORITY_LIST) == "table"
        local _rankOf; _rankOf = (function(nm) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
            if not _prio or not nm then return math.huge end
            local low = nm:lower()
            for i, pName in ipairs(PRIORITY_LIST) do if pName:lower() == low then return i end end
            return math.huge
        end)
        local _bestWalking, _bestWalkingRank, _bestWalkingGen = nil, math.huge, -math.huge
        for _, w in ipairs(SharedState.WalkingCache) do
            local _live = w and w.model and w.model.Parent
                          and (_G.JAF_CarpetNeedPrompt ~= true or (w.prompt and w.prompt.Parent and w.prompt.Enabled))
            if _live and w.genValue then
                local r = _rankOf(w.name)
                if r < _bestWalkingRank or (r == _bestWalkingRank and w.genValue > _bestWalkingGen) then
                    _bestWalkingRank = r; _bestWalkingGen = w.genValue; _bestWalking = w
                end end end
        local _baseRank = _rankOf(targetPetData.name)
        local _carpetWins = _bestWalking and (
            _bestWalkingRank < _baseRank
            or (_bestWalkingRank == _baseRank and _bestWalkingGen > (targetPetData.genValue or 0))
        )
        if _carpetWins then isTpMoving = false if _G.JAF_LoadHeavyNow then _G.JAF_LoadHeavyNow() end return end
    end
    local c, hrp, hum = getChar()
    if not hrp or not hum or hum.Health<=0 then isTpMoving = false; return end
    isTpMoving = true
    _G.JAF_TpStartedFlight = true
    do
        local c2, _, hum2 = getChar()
        if Config.TpSettings.TpMode ~= "grapple" and c2 and not c2:FindFirstChild(Config.TpSettings.Tool) then
            local carpetWaitStart = tick()
            local done = false
            local _eqTry = 0
            local hbConn
            hbConn = RunService.Heartbeat:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if done then return end
                local cc, _, hh = getChar()
                if cc and cc:FindFirstChild(Config.TpSettings.Tool) then done = true; hbConn:Disconnect() return end
                local inBp = LocalPlayer.Backpack:FindFirstChild(Config.TpSettings.Tool)
                if inBp and hh and hh.Parent and not _equipRefused(hh)
                   and tick() - _eqTry >= (tonumber(_G.JAF_EquipFailGap) or 0.25) then
                    _eqTry = tick(); hh:EquipTool(inBp)
                end
                if tick()-carpetWaitStart > 3 then done = true; hbConn:Disconnect() end
            end)
            while not done and alive() do RunService.Heartbeat:Wait() end
            hbConn:Disconnect()
        end end
    if not alive() then isTpMoving=false; return end
    local part = findAdorneeGlobal(targetPetData)
    if not part or not alive() then isTpMoving=false; return end
    if targetPetData.isWalking then
        equipCarpet()
        task.wait(0.01)
        if not alive() then isTpMoving=false; return end
        c, hrp = getChar()
        if hrp then hrp.AssemblyLinearVelocity = Vector3.zero hrp.CFrame = CFrame.new(part.Position + Vector3.new(0, 3, 0)) end
        isTpMoving = false
        return
    end
    if activeTpMode == "front" then
    local exactPos = part.Position
    if Config.TpSettings.TpMode == "grapple" and JAF_GrapplePreFire then
        local _fpP  = _G.JAF_BaseFootprints
        local _want = tonumber(_G.JAF_PlotsTotal); if not _want or _want <= 0 then _want = 8 end
        if (_fpP and #_fpP >= _want) or _G.JAF_GrapplePreFireEarly ~= false then pcall(JAF_GrapplePreFire, exactPos, false) end
    end
    local isSecondFloor = exactPos.Y > 10 and not _G.JAF_F2UnderActive(exactPos.Y)
    local _skipClone = false
    if (not isSecondFloor) and _G.JAF_SkipCloneUnlockedF1 ~= false then
        local _ok, _ul, _scWhy = pcall(isPlotUnlocked, targetPetData.plot)
        _skipClone = _ok and (_ul == true)
    end
    if Config.TpSettings.TpMode ~= "grapple" then equipCarpet(); task.wait(0.01)
    else RunService.Heartbeat:Wait() end
    if not alive() then isTpMoving=false; return end
    c, hrp = getChar()
    local _fgN0, _fgN1, _fgWhy = -1, -1, "skip"
    if _G.JAF_TpWaitFootprints ~= false then
        local _fgWant = tonumber(_G.JAF_PlotsTotal); if not _fgWant or _fgWant <= 0 then _fgWant = 8 end
        local _fgT0, _fgCap = tick(), (tonumber(_G.JAF_TpFootprintWaitMax) or 5)
        local _fgLast, _fgStall = -1, tick()
        _fgN0 = (_G.JAF_BaseFootprints and #_G.JAF_BaseFootprints) or 0
        _fgWhy = "cap"
        while alive() and tick() - _fgT0 < _fgCap do
            local _fps = _G.JAF_BaseFootprints
            local _n = (_fps and #_fps) or 0
            if _n >= _fgWant then _fgWhy = "full"; break end
            if _n ~= _fgLast then _fgLast = _n; _fgStall = tick() end
            if _n > 0 and (tick() - _fgStall) > (tonumber(_G.JAF_TpFootprintStall) or 1.6) then _fgWhy = "stall"; break end
            RunService.Heartbeat:Wait()
        end
        _fgN1 = (_G.JAF_BaseFootprints and #_G.JAF_BaseFootprints) or 0
        if not alive() then isTpMoving = false; return end
    end
    local signPart, targetBasePos, waypoints, usedPathfinding, sideChosen
    do
        local cached = _frontRouteCache
        local _f1mNow = _G.JAF_F1Mode or (Config.TpSettings and Config.TpSettings.F1SpotMode) or "auto"
        if _G.JAF_RouteReuseCache == true and (not isSecondFloor) and cached and cached.usedPathfinding and cached.uid == targetPetData.uid and cached.targetBasePos
           and (tick() - cached.at) < 6 and cached.f1mode == _f1mNow
           and (cached.f2under == true) == _G.JAF_F2UnderActive(exactPos.Y)
           and Vector2.new(hrp.Position.X - cached.startFlat.X, hrp.Position.Z - cached.startFlat.Y).Magnitude < (_G.JAF_RouteReuseMove or 12) then
            signPart        = cached.signPart
            targetBasePos   = cached.targetBasePos
            waypoints       = cached.waypoints
            usedPathfinding = cached.usedPathfinding
            sideChosen      = cached.sideChosen
        else
            targetBasePos, waypoints, usedPathfinding, signPart, _, _, sideChosen = computeFrontRoute(part, hrp.Position)
        end end
    if sideChosen then
        _skipClone = false
    elseif (not _skipClone) and (not isSecondFloor) and _G.JAF_SkipCloneUnlockedF1 ~= false
       and signPart and signPart.Parent then
        local _ok2, _ul2 = pcall(isPlotUnlocked, signPart.Parent.Name)
        if _ok2 and _ul2 == true then _skipClone = true end
    end
    if signPart then pcall(_G.JAF_HB.build, signPart, exactPos) end
    if not targetBasePos then isTpMoving = false; return end
    local _faceCF, _faceRot, _sideApproach
    local _jafLockOK; _jafLockOK = function(_cf, _tag) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _G.JAF_LockSanity == false or not targetBasePos then return true end
        local _off = Vector2.new(_cf.X - targetBasePos.X, _cf.Z - targetBasePos.Z).Magnitude
        if _off <= (tonumber(_G.JAF_ClonePosHammerMaxOff) or 12) then return true end
        return false
    end
    local _jafStolenSwap; _jafStolenSwap = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not _G.JAF_StolenSwap then return end
        local _ok, _e, _p = pcall(_G.JAF_StolenSwap, targetPetData, exactPos.Y)
        if _ok and _e and _p then
            exactPos = _p
            SharedState._currentTpTarget = _e
            if signPart then pcall(_G.JAF_HB.build, signPart, exactPos) end
        end end
    do
        local _into = getSignInwardDir(signPart)
        if sideChosen then
            local _perpF = Vector3.new(-_into.Z, 0, _into.X)
            if _perpF.Magnitude > 0.01 then
                _perpF = _perpF.Unit
                local _rel = targetBasePos - signPart.Position
                local _lat = _rel.X * _perpF.X + _rel.Z * _perpF.Z
                _into = (_lat >= 0) and (-_perpF) or _perpF
            end
            _sideApproach = true
        else
            local _perpF = Vector3.new(-_into.Z, 0, _into.X)
            if _perpF.Magnitude > 0.01 and signPart and signPart:IsA("BasePart") then
                _perpF = _perpF.Unit
                local _rel = targetBasePos - signPart.Position
                local _lat = _rel.X * _perpF.X + _rel.Z * _perpF.Z
                if math.abs(_lat) > (_G.JAF_SideFaceThresh or 16) then _into = (_lat >= 0) and (-_perpF) or _perpF _sideApproach = true end
            end end
        _faceCF  = CFrame.lookAt(targetBasePos, targetBasePos + _into)
        _faceRot = _faceCF - _faceCF.Position
    end
    local _columnOpenAir; _columnOpenAir = (function(xz, fromY, floorY) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
        local _cc = getChar()
        local rp = RaycastParams.new()
        rp.FilterType = Enum.RaycastFilterType.Exclude
        local ig = {}
        if _cc then table.insert(ig, _cc) end
        for _, ch in ipairs(Workspace:GetChildren()) do if ch.Name == "XiTempPlatform" then table.insert(ig, ch) end end
        local _cl = Workspace:FindFirstChild(tostring(LocalPlayer.UserId) .. "_Clone")
        if _cl then table.insert(ig, _cl) end
        rp.FilterDescendantsInstances = ig
        local span = fromY - (floorY - 2)
        if span <= 0 then return true end
        local hit = Workspace:Raycast(Vector3.new(xz.X, fromY, xz.Z), Vector3.new(0, -span, 0), rp)
        return (not hit) or (not hit.Instance.CanCollide) or (hit.Position.Y <= floorY + 4)
    end)
    if not hrp or not alive() then isTpMoving=false; return end
    hrp.AssemblyLinearVelocity = Vector3.new(hrp.AssemblyLinearVelocity.X, 0, hrp.AssemblyLinearVelocity.Z)
    if not alive() then isTpMoving=false; return end
    local _tpFinalY, _tpShortHop, _tpArriveOff, _tpArriveAt
    do
        c, hrp = getChar()
        if hrp and alive() then
            local _grap = (Config.TpSettings.TpMode == "grapple")
            local _shortHop = _grap and _G.JAF_M2(hrp.Position.X, hrp.Position.Z, targetBasePos.X, targetBasePos.Z) < (tonumber(_G.JAF_GrappleShortHop) or 70)
            _tpShortHop = _shortHop
            local _nearBase = false
            if _G.JAF_NearBaseRiseSlow ~= false then
                local _edge = _G.JAF_M2(hrp.Position.X, hrp.Position.Z, targetBasePos.X, targetBasePos.Z)
                local _tf, _td
                for _, f in ipairs(_G.JAF_BaseFootprints or {}) do
                    if f.minX then
                        local a = math.max(f.minX - targetBasePos.X, targetBasePos.X - f.maxX, 0)
                        local b = math.max(f.minZ - targetBasePos.Z, targetBasePos.Z - f.maxZ, 0)
                        local d = a*a + b*b
                        if not _td or d < _td then _td = d; _tf = f end
                    end end
                if _tf then
                    local a = math.max(_tf.minX - hrp.Position.X, hrp.Position.X - _tf.maxX, 0)
                    local b = math.max(_tf.minZ - hrp.Position.Z, hrp.Position.Z - _tf.maxZ, 0)
                    _edge = math.sqrt(a*a + b*b)
                end
                _nearBase = _edge < (tonumber(_G.JAF_NearBaseDist) or 45)
            end
            local VL_SPEED
            do
                local T = Config.TpSettings
                if _grap then
                    local legacy = tonumber(T.GrappleSpeed) or 400
                    if isSecondFloor then
                        VL_SPEED = tonumber(sideChosen and T.GrappleSpeedF2Side or T.GrappleSpeedF2Front) or legacy
                    else
                        VL_SPEED = tonumber(sideChosen and T.GrappleSpeedF1Side or T.GrappleSpeedF1Front) or legacy
                    end
                    _G.JAF_GrappleFlightSpeed = VL_SPEED
                else
                    if isSecondFloor then
                        VL_SPEED = tonumber(sideChosen and T.TpSpeedF2Side or T.TpSpeedF2Front) or tonumber(T.TpSpeedF2) or 130
                    else
                        VL_SPEED = tonumber(sideChosen and T.TpSpeedF1Side or T.TpSpeedF1Front) or tonumber(T.TpSpeedF1) or 130
                    end end end
            do
                local _on = (_G.JAF_TPFAST ~= false)
                _G.JAF_TPFAST = _on
                local function _T(k, fast, stock) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    if _G["JAF_TPFAST_" .. k] == false then _G[k] = stock
                    elseif _on then _G[k] = fast
                    else _G[k] = stock end
                end
                _T("JAF_TakeoffRampFrom",   380,  160)
                _T("JAF_TakeoffRampTime",   0,    0.25)
                _T("JAF_F1TaperK",          25,   20)
                _T("JAF_F1ApproachTaper",   true,  true)
                _T("JAF_F2SideTaper",       true,  true)
                _T("JAF_SideTaper",         true,  true)
                _T("JAF_ArriveZeroVel",     true, false)
                _T("JAF_LookaheadMax",      32,   48)
                _T("JAF_MaxCut",            1.6,  2.5)
                _T("JAF_XtMaxSin",          0.38, 0.25)
                _T("JAF_LbEaseFrac",        0.5,  0.6)
                _T("JAF_LbStable",          0.6,  0.6)
                _T("JAF_LbRamp",            0.35, 0.5)
                _T("JAF_GroundDropAfter",   0.05, 0.25)
                _T("JAF_F1GroundDropAfter", 0.05, 0.1)
                _T("JAF_LandRampTime",      0.04, 0.1)
                _T("JAF_DropTaperK",        18,   12)
                _T("JAF_GrapplePreArmMax",  0.35, 0.5)
                _T("JAF_LandTweenSpeed",    260,   130)
                _T("GrapplePostBoostSpeed", 160,  160)
            end
            if _G.JAF_NoSpeedLimits ~= false then
                _G.JAF_TotalSpeedPace    = false
                _G.JAF_ComboBudget       = false
                _G.JAF_NearBaseRiseSlow  = false
                _G.GrappleBoostSpeed     = VL_SPEED
                if _G.JAF_NoLimitFrameStep == true then _G.JAF_MaxFrameStep = 0 end
                _G.GrapplePostBoostSpeed = VL_SPEED
                _G.GrappleSwapSpeed      = VL_SPEED
                if _G.JAF_NoLimitTaper == true then
                    _G.JAF_SideTaper     = false
                    _G.JAF_F2SideTaper   = false
                    _G.JAF_F2FrontTaper  = false
                    _G.JAF_OpenBaseTaper = false
                end end
            if _G.JAF_LookaheadSpeedScale ~= false then
                local _laBase = tonumber(_G.JAF_LookaheadSpeedBase) or 400
                local _laMax  = tonumber(_G.JAF_LookaheadMax)
                if _laMax and VL_SPEED > _laBase then _G.JAF_LookaheadMax = _laMax * (VL_SPEED / _laBase) end
            end
            if _G.JAF_PreStream ~= false and targetBasePos then
                local _psTarget = targetBasePos
                task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    pcall(function() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
                        local _lp = LocalPlayer
                        if not _lp or type(_lp.RequestStreamAroundAsync) ~= "function" then return end
                        local _psT0 = tick()
                        _lp:RequestStreamAroundAsync(_psTarget, tonumber(_G.JAF_PreStreamTimeout) or 1)
                        _G.JAF_PreStreamMs = math.floor((tick() - _psT0) * 1000)
                        _G.JAF_PreStreamN  = (tonumber(_G.JAF_PreStreamN) or 0) + 1
                    end) end) end
            if _grap then _G.JAF_GrappleAimPos = targetBasePos; JAF_GrappleEngage(alive, _shortHop) end
            local _riseCap; _riseCap = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local _v = tonumber(_G.JAF_RiseSpeed) or VL_SPEED
                if _grap then _v = math.min(_v, JAF_GrappleSpeedCap()) end
                local _hardCap = tonumber(_G.JAF_RiseVelCap)
                if _hardCap then _v = math.min(_v, _hardCap) end
                return _v
            end
            local _descCap; _descCap = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                return math.min(_riseCap(), tonumber(_G.JAF_DescCap) or 95)
            end
            local _f1FrontDrive = (not isSecondFloor) and (not sideChosen) and (not _sideApproach) and (not _skipClone)
                                  and (_G.JAF_F1FrontAnchor ~= false) and (_G.JAF_SideRiseDrop ~= false) and (_G.JAF_SxeF1Front ~= false)
            local _frontHeadOn = (not sideChosen) and (not _sideApproach) and (not _skipClone)
            local _openFront = (not isSecondFloor) and (not sideChosen) and (not _sideApproach)
                               and _skipClone and _G.JAF_OpenFrontArrive ~= false
            local _sideArr = (sideChosen or _sideApproach) and _G.JAF_SideArrive ~= false
            local ARRIVE_DIST   = (_f1FrontDrive or _openFront) and (tonumber(_G.JAF_F1FrontArrive) or 1.6)
                                  or (_sideArr and (tonumber(_G.JAF_SideArriveDist) or 2))
                                  or 6
            if not usedPathfinding or #waypoints == 0 then waypoints = { hrp.Position, targetBasePos } end
            local NEAR_END_DIST = 42
            local CRUISE_Y_F1 = sideChosen and (_G.JAF_CruiseY_F1_Side or 0) or (_G.JAF_CruiseY_F1_Front or -2.8)
            local CRUISE_Y_F2 = tonumber(_G.JAF_CruiseY_F2)
            if not CRUISE_Y_F2 then
                CRUISE_Y_F2 = 18
                if signPart and signPart:IsA("BasePart") then
                    local _cfS, _sS = signPart.CFrame, signPart.Size
                    local _eyS = math.abs(_cfS.RightVector.Y) * _sS.X + math.abs(_cfS.UpVector.Y) * _sS.Y + math.abs(_cfS.LookVector.Y) * _sS.Z
                    local _legS = 3
                    do
                        local _cS, _hS, _humS = getChar()
                        if _hS then _legS = math.max(3, _hS.Size.Y * 0.5 + ((_humS and _humS.HipHeight) or 2)) end
                    end
                    CRUISE_Y_F2 = signPart.Position.Y + _eyS * 0.5 + _legS + (tonumber(_G.JAF_F2SignClear) or 2)
                end end
            if isSecondFloor and _G.JAF_F2WallCruise == true and signPart and signPart.Parent then
                pcall(function() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
                    local _lw = signPart.Parent:FindFirstChild("LaserHitbox")
                    _lw = _lw and _lw:FindFirstChild("SecondFloor")
                    if _lw and _lw:IsA("BasePart") and _lw.CanCollide then
                        local _cfW, _sW = _lw.CFrame, _lw.Size
                        local _eyW = math.abs(_cfW.RightVector.Y) * _sW.X
                                   + math.abs(_cfW.UpVector.Y)    * _sW.Y
                                   + math.abs(_cfW.LookVector.Y)  * _sW.Z
                        local _topW = _lw.Position.Y + _eyW * 0.5 + (tonumber(_G.JAF_F2WallClear) or 2.5)
                        if _topW > CRUISE_Y_F2 then CRUISE_Y_F2 = _topW _G.JAF_F2WallCruiseN = (tonumber(_G.JAF_F2WallCruiseN) or 0) + 1 end
                    end end) end
            local CRUISE_Y    = isSecondFloor and CRUISE_Y_F2 or CRUISE_Y_F1
            local riseTargetY, finalY
            if isSecondFloor then
                riseTargetY = CRUISE_Y
                finalY      = targetBasePos.Y + (_G.JAF_F2Drop or 2)
            else
                local _c0F1, _h0F1 = getChar()
                local _startYF1 = (_h0F1 and _h0F1.Position.Y) or CRUISE_Y
                local _f1CruiseAnchorY
                if _G.JAF_F1FrontAnchor ~= false and _G.JAF_LaserNoDrop ~= false and signPart and signPart.Parent then
                    local _lf = signPart.Parent:FindFirstChild("LaserHitbox")
                    local _lp = _lf and (_lf:FindFirstChild("Main") or _lf:FindFirstChildWhichIsA("BasePart"))
                    if _lp then _f1CruiseAnchorY = _lp.Position.Y + (tonumber(_G.JAF_LaserMidYOff) or 0) end
                end
                local _f1StandCruiseY
                if _G.JAF_F1StandLevel ~= false and signPart and signPart.Parent then
                    local _ftop
                    ;(function() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
                    for _, _p in ipairs(signPart.Parent:GetDescendants()) do
                        if _p:IsA("BasePart") and _p.CanCollide then
                            local _cf, _s = _p.CFrame, _p.Size
                            local _ex = math.abs(_cf.RightVector.X) * _s.X + math.abs(_cf.UpVector.X) * _s.Y + math.abs(_cf.LookVector.X) * _s.Z
                            local _ey = math.abs(_cf.RightVector.Y) * _s.X + math.abs(_cf.UpVector.Y) * _s.Y + math.abs(_cf.LookVector.Y) * _s.Z
                            local _ez = math.abs(_cf.RightVector.Z) * _s.X + math.abs(_cf.UpVector.Z) * _s.Y + math.abs(_cf.LookVector.Z) * _s.Z
                            if _ey <= 4 and math.min(_ex, _ez) >= 2
                               and math.abs(_p.Position.X - targetBasePos.X) <= _ex * 0.5 + 1
                               and math.abs(_p.Position.Z - targetBasePos.Z) <= _ez * 0.5 + 1 then
                                local _top = _p.Position.Y + _ey * 0.5
                                if _top <= targetBasePos.Y + 2 and (not _ftop or _top > _ftop) then _ftop = _top end
                            end end end end)()
                    if _ftop then
                        local _sc, _sh2, _shum2 = getChar()
                        local _stOff = (_shum2 and _sh2 and (_shum2.HipHeight + _sh2.Size.Y * 0.5)) or 4.1
                        _f1StandCruiseY = _ftop + _stOff + (tonumber(_G.JAF_F1StandClear) or 0.3)
                    end end
                if sideChosen then
                    if tonumber(_G.JAF_CruiseY_F1_Side) then
                        riseTargetY = CRUISE_Y
                        finalY      = math.max(targetBasePos.Y + (_G.JAF_F1LandHover or 2), CRUISE_Y)
                    else
                        finalY      = _f1StandCruiseY or (_startYF1 + (tonumber(_G.JAF_F1FlatOffset) or 0))
                        riseTargetY = finalY + (tonumber(_G.JAF_F1CruiseLift) or 0)
                    end
                else
                    finalY      = _f1StandCruiseY or _f1CruiseAnchorY or (_startYF1 + (tonumber(_G.JAF_F1FlatOffset) or 0))
                    riseTargetY = finalY + (tonumber(_G.JAF_F1CruiseLift) or 3)
                end end
            _tpFinalY = finalY
            if #waypoints >= 1 then
                local _w1SL = waypoints[1]
                local _c0SL, _h0SL = getChar()
                if _h0SL and (#waypoints < 2
                              or Vector2.new(_w1SL.X - _h0SL.Position.X, _w1SL.Z - _h0SL.Position.Z).Magnitude > 8) then
                    table.insert(waypoints, 1, Vector3.new(_h0SL.Position.X, _w1SL.Y, _h0SL.Position.Z))
                end end
            local n = #waypoints
            local suffix = {}
            suffix[n] = 0
            for i = n - 1, 1, -1 do
                local a, b = waypoints[i], waypoints[i + 1]
                suffix[i] = suffix[i + 1] + Vector2.new(a.X - b.X, a.Z - b.Z).Magnitude
            end
            local remainingFrom; remainingFrom = function(px, pz, idx) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local w = waypoints[idx] or targetBasePos
                return _G.JAF_M2(px, pz, w.X, w.Z) + (suffix[idx] or 0)
            end
            local _pp = (_G.JAF_PurePursuit ~= false) and n >= 2
            local _routeS = 0
            local _arcTotal; _arcTotal = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false)) return suffix[1] or 0 end
            local _pointAt; _pointAt = function(s) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local total = suffix[1] or 0
                if s <= 0 then local w = waypoints[1]; return w.X, w.Z, math.min(2, n) end
                if s >= total then local w = waypoints[n]; return w.X, w.Z, n end
                for i = 1, n - 1 do
                    local segStart = total - (suffix[i] or 0)
                    local segLen   = (suffix[i] or 0) - (suffix[i + 1] or 0)
                    if s <= segStart + segLen then
                        local a, b = waypoints[i], waypoints[i + 1]
                        local t = (segLen > 1e-6) and ((s - segStart) / segLen) or 0
                        return a.X + (b.X - a.X) * t, a.Z + (b.Z - a.Z) * t, i + 1
                    end end
                local w = waypoints[n]; return w.X, w.Z, n
            end
            local _projectS; _projectS = function(px, pz, fromS, maxStep) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local total = suffix[1] or 0
                local bestS, bestD2 = fromS, math.huge
                for i = 1, n - 1 do
                    local segStart = total - (suffix[i] or 0)
                    local segLen   = (suffix[i] or 0) - (suffix[i + 1] or 0)
                    if segLen > 1e-6 and segStart + segLen >= fromS then
                        local a, b = waypoints[i], waypoints[i + 1]
                        local abx, abz = b.X - a.X, b.Z - a.Z
                        local t = ((px - a.X) * abx + (pz - a.Z) * abz) / (abx * abx + abz * abz)
                        local tLo = (segStart < fromS) and ((fromS - segStart) / segLen) or 0
                        t = math.clamp(t, tLo, 1)
                        local qx, qz = a.X + abx * t, a.Z + abz * t
                        local d2 = (px - qx) * (px - qx) + (pz - qz) * (pz - qz)
                        if d2 < bestD2 - 0.01 then bestD2 = d2; bestS = segStart + t * segLen end
                    end end
                if maxStep then bestS = math.min(bestS, fromS + maxStep) end
                return math.max(fromS, math.min(bestS, total))
            end
            local _segDirAt; _segDirAt = function(s) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if type(s) ~= "number" or n < 2 then return nil, nil end
                local _sTot = suffix[1] or 0
                local _sEps = tonumber(_G.JAF_VtxEps) or 0.35
                local _si = nil
                for _k = 1, n - 1 do
                    local _sa0 = _sTot - (suffix[_k] or 0)
                    local _sln = (suffix[_k] or 0) - (suffix[_k + 1] or 0)
                    if _sln > 1e-6 and s < _sa0 + _sln - _sEps then _si = _k break end
                end
                if not _si then _si = n - 1 end
                local _sA, _sB2 = waypoints[_si], waypoints[_si + 1]
                if not _sA or not _sB2 then return nil, nil end
                local _sdx, _sdz = _sB2.X - _sA.X, _sB2.Z - _sA.Z
                local _sm = math.sqrt(_sdx * _sdx + _sdz * _sdz)
                if _sm < 1e-3 then return nil, nil end
                return (_sdx / _sm) * 6, (_sdz / _sm) * 6
            end
            if _pp then local _c0s, _h0s = getChar() if _h0s then _routeS = _projectS(_h0s.Position.X, _h0s.Position.Z, 0, nil) end end
            local descentStartIdx = n + 1
            do
                local _ignore = { c }
                for _, ch in ipairs(Workspace:GetChildren()) do if ch.Name == "XiTempPlatform" then table.insert(_ignore, ch) end end
                local _cloneInst = Workspace:FindFirstChild(tostring(LocalPlayer.UserId) .. "_Clone")
                if _cloneInst then table.insert(_ignore, _cloneInst) end
                local _rp = RaycastParams.new()
                _rp.FilterType = Enum.RaycastFilterType.Exclude
                _rp.FilterDescendantsInstances = _ignore
                local _safeAt; _safeAt = function(idx) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    local wp = waypoints[idx]; if not wp then return false end
                    if finalY >= riseTargetY - 0.5 then return true end
                    local origin = Vector3.new(wp.X, riseTargetY + 1, wp.Z)
                    local dir = Vector3.new(0, -(riseTargetY - finalY + 3), 0)
                    local hit = Workspace:Raycast(origin, dir, _rp)
                    if not hit then return true end
                    return hit.Position.Y <= finalY + 1.5
                end
                for i = n, 1, -1 do if _safeAt(i) then descentStartIdx = i else break end end
                local MAX_EARLY_DESCENT_DIST = 80
                while descentStartIdx <= n and (suffix[descentStartIdx] or 0) > MAX_EARLY_DESCENT_DIST do
                    descentStartIdx = descentStartIdx + 1
                end end
            local _planY
            local function _buildPlanY(_pStartY, _pStartXZ) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                local _pTotal = suffix[1] or 0
                local _pDrop  = math.abs(riseTargetY - finalY)
                local _pDescS
                if _G.JAF_TailRunwayFixed == true then
                    _pDescS = math.max(_pTotal - NEAR_END_DIST, 0)
                else
                    _pDescS = math.max(_pTotal - math.clamp(_pDrop * (tonumber(_G.JAF_SmallDropTailK) or 2), 3, NEAR_END_DIST), 0)
                end
                if _G.JAF_EarlyDescent == true and descentStartIdx <= n then
                    _pDescS = math.min(_pDescS, math.max(_pTotal - (suffix[descentStartIdx] or 0), 0))
                end
                local _pClimb
                do
                    local _crTailRuns = not ((_G.JAF_SnapDescent ~= false) and finalY < riseTargetY - (tonumber(_G.JAF_SnapMinDrop) or 0))
                    local _crClear = math.min(tonumber(_G.JAF_ClimbClearS) or 30, _pTotal * (tonumber(_G.JAF_ClimbClearFrac) or 0.35))
                    local _crEnd = (_G.JAF_ClimbRunway ~= false and not _crTailRuns)
                                   and math.max(_pDescS - 1, _pTotal - _crClear) or (_pDescS - 1)
                    local _cdMax = math.max(_G.JAF_RiseClimbMin or 12, _crEnd)
                    local _rcm = tonumber(_G.JAF_RiseClimbMax)
                    if _rcm then _cdMax = math.min(_rcm, _cdMax) end
                    _pClimb = math.clamp(_pTotal * (tonumber(_G.JAF_RiseSpreadFrac) or 0.96),
                                         _G.JAF_RiseClimbMin or 12, math.max(_cdMax, _G.JAF_RiseClimbMin or 12))
                    _G.JAF_ClimbRunS = math.floor(_pClimb)
                end
                local _pLift   = _G.JAF_FlightLiftObstacles
                if _pLift and _pStartXZ then
                    local _fl = {}
                    local _mrgT = _G.JAF_LiftMargin or 6
                    local _routeMinDist; _routeMinDist = function(ox, oz) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                        local _bd = math.huge
                        for _i = 1, n - 1 do
                            local _a, _b = waypoints[_i], waypoints[_i + 1]
                            local _abx, _abz = _b.X - _a.X, _b.Z - _a.Z
                            local _L2 = _abx * _abx + _abz * _abz
                            local _t = (_L2 > 1e-6) and math.clamp(((ox - _a.X) * _abx + (oz - _a.Z) * _abz) / _L2, 0, 1) or 0
                            local _qx, _qz = _a.X + _abx * _t, _a.Z + _abz * _t
                            local _d2 = (ox - _qx) * (ox - _qx) + (oz - _qz) * (oz - _qz)
                            if _d2 < _bd then _bd = _d2 end
                        end
                        return math.sqrt(_bd)
                    end
                    for _, o in ipairs(_pLift) do
                        local _R = ((_G.JAF_LiftCoreEngage ~= false) and (o.cr or o.r) or o.r) + _mrgT
                        local _dx0, _dz0 = _pStartXZ.X - o.x, _pStartXZ.Z - o.z
                        if (_dx0 * _dx0 + _dz0 * _dz0) > _R * _R
                           and (_G.JAF_LiftCrossOnly == false or _routeMinDist(o.x, o.z) <= _R) then
                            _fl[#_fl + 1] = o
                        end end
                    _pLift = _fl
                end
                local _pHold = 0
                _G.JAF_CeilHoldS = _pHold
                local _pBudget = riseTargetY + (tonumber(_G.JAF_LiftMaxUp) or (riseTargetY < 5 and tonumber(_G.JAF_F1LiftMaxUp)) or 14)
                local _snapFlat = _G.JAF_SnapDescent ~= false
                return function(s) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    if s < 0 then s = 0 elseif s > _pTotal then s = _pTotal end
                    local y
                    if _pHold > 0 and s < _pHold then
                        y = _pStartY
                    elseif _pClimb > _pHold and s < _pClimb then
                        y = _pStartY + (riseTargetY - _pStartY) * ((s - _pHold) / (_pClimb - _pHold))
                    else
                        y = riseTargetY
                    end
                    if s < _pDescS then
                        if _pLift and #_pLift > 0 then
                            local px, pz = _pointAt(s)
                            local _mrg  = _G.JAF_LiftMargin or 6
                            local _lead = math.max(_G.JAF_LiftLead or 70, 1)
                            for _, o in ipairs(_pLift) do
                                local _ot = o.liftY or (o.top + (_G.JAF_LiftClearance or 7))
                                if _ot > y then
                                    local _R = ((_G.JAF_LiftCoreEngage ~= false) and (o.cr or o.r) or o.r) + _mrg
                                    local ddx, ddz = px - o.x, pz - o.z
                                    local d = math.sqrt(ddx * ddx + ddz * ddz)
                                    if d < _R + _lead then
                                        local w = (d <= _R) and 1 or (1 - (d - _R) / _lead)
                                        local lifted = y + (_ot - y) * w
                                        if lifted > y then y = math.min(lifted, _pBudget) end
                                    end end end end
                    elseif _pTotal > _pDescS then
                        local _snapMinDrop = tonumber(_G.JAF_SnapMinDrop) or 0
                        if not (_snapFlat and finalY < y - _snapMinDrop) then
                            local f = (s - _pDescS) / (_pTotal - _pDescS)
                            y = y + (finalY - y) * f
                        end end
                    return y
                end end
            if _pp and _G.JAF_FollowRouteY ~= false then _planY = _buildPlanY(hrp.Position.Y, hrp.Position) end
            local wpIndex = 1
            local _lastFlightPos = nil
            local _spdPrev = nil
            local _stableSpd = nil
            local _yvPrev, _yvSlewN = nil, 0
            local _fN, _fSum, _fWorst, _fOver33, _fOver50 = 0, 0, 0, 0, 0
            local _dtRecent = 0
            local _projPrevPos = nil
            local _flightStartPos = hrp.Position
            local _maxFlightMove = 0
            local _sagMax = 0
            local _minSpd, _maxSpd = math.huge, 0
            local _tgGoneN = 0
            local _tkT0 = 0
            local _neMin, _neMax = math.huge, 0
            local _rerouteN = 0
            local _lateObsN, _lateObsChk = 0, 0
            local _planAt = tick()
            local _ppL = 14
            local _lbCap, _lbCapAt, _lbStreak = math.huge, 0, 0
            local velDone = false
            local descending = false
            local _snapHoldY = nil
            local _prevRemaining = nil
            local _stuckTime     = 0
            local _exitWhy, _exitRem = "arrive", -1
            local _bestRemaining = math.huge
            local _bestRemAt     = nil
            local _stallRecoverN = 0
            local _lbN, _lbMaxJump = 0, 0
            local _arriveFixN = 0
            local _paceN, _paceNbN, _paceMin = 0, 0, math.huge
            local _lbPrevRem = nil
            local _lbPrevArc = nil
            local _ykT0 = 0
            local _npAimRaw      = false
            local _, lockHrp, lockHum = getChar()
            if lockHum then lockHum.AutoRotate = false end
            if lockHrp then lockHrp.CFrame = CFrame.new(lockHrp.Position) * _faceRot end
            local _wallAway = Vector3.zero
            do
                local _, prHrp = getChar()
                if prHrp then
                    local _rp = RaycastParams.new()
                    _rp.FilterType = Enum.RaycastFilterType.Exclude
                    local _ig = { c }
                    for _, ch in ipairs(Workspace:GetChildren()) do if ch.Name == "XiTempPlatform" then table.insert(_ig, ch) end end
                    _rp.FilterDescendantsInstances = _ig
                    local WALL_BUFFER = _G.JAF_WallBuffer or 12
                    local _dirs = {
                        Vector3.new(1,0,0),  Vector3.new(-1,0,0),
                        Vector3.new(0,0,1),  Vector3.new(0,0,-1),
                        Vector3.new(0.707,0,0.707),  Vector3.new(0.707,0,-0.707),
                        Vector3.new(-0.707,0,0.707), Vector3.new(-0.707,0,-0.707),
                    }
                    for _, d in ipairs(_dirs) do
                        if Workspace:Raycast(prHrp.Position, d * WALL_BUFFER, _rp) then _wallAway = _wallAway - d end
                    end
                    if _wallAway.Magnitude > 0.01 then _wallAway = _wallAway.Unit end
                end end
            local function _aimWp(vHrp, _maxStep) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if _pp then
                    _routeS = _projectS(vHrp.Position.X, vHrp.Position.Z, _routeS, _maxStep)
                    local _qx, _qz, _si = _pointAt(_routeS + (tonumber(_G.JAF_RiseLookahead) or 10))
                    wpIndex = _si
                    return Vector3.new(_qx, 0, _qz)
                end
                local _wp = waypoints[wpIndex] or targetBasePos
                local _to = _G.JAF_M2(vHrp.Position.X, vHrp.Position.Z, _wp.X, _wp.Z)
                while _to <= 6 and wpIndex < n do
                    wpIndex = wpIndex + 1
                    _wp = waypoints[wpIndex]
                    _to = _G.JAF_M2(vHrp.Position.X, vHrp.Position.Z, _wp.X, _wp.Z)
                end
                return _wp
            end
            do
                local _, rHrp = getChar()
                if rHrp and alive() and rHrp.Position.Y < riseTargetY - 0.5 and not _planY then
                    local _startY = rHrp.Position.Y
                    local riseDone = false
                    local riseConn
                    local WALL_PUSH_SPEED = 14
                    local _climbDist
                    do
                        local _rrem = remainingFrom(rHrp.Position.X, rHrp.Position.Z, wpIndex)
                        local _cdMax = math.min(_G.JAF_RiseClimbMax or 160,
                                                math.max(_G.JAF_RiseClimbMin or 12, _rrem - (_G.JAF_RiseCloseDist or 45) - 15))
                        _climbDist = math.clamp(_rrem * (tonumber(_G.JAF_RiseSpreadFrac) or 0.65), _G.JAF_RiseClimbMin or 12, _cdMax)
                    end
                    local _traveled = 0
                    local _lastP = rHrp.Position
                    if _pp then _routeS = _projectS(rHrp.Position.X, rHrp.Position.Z, _routeS, nil) end
                    riseConn = RunService.Heartbeat:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                        if not alive() then riseConn:Disconnect(); riseDone = true; return end
                        local _, vHrp = getChar()
                        if not vHrp then riseConn:Disconnect(); riseDone = true; return end
                        local _obLift, _obTop, _obClose = 0, -math.huge, false
                        local _obHit, _obRamp = false, -math.huge
                        do
                            local _ll = _G.JAF_FlightLiftObstacles
                            if _ll and _G.JAF_LiftOverObstacles ~= false then
                                local _wpH = (waypoints and (waypoints[wpIndex] or targetBasePos)) or targetBasePos
                                local _hx, _hz = _wpH.X - vHrp.Position.X, _wpH.Z - vHrp.Position.Z
                                local _hm = math.sqrt(_hx*_hx + _hz*_hz)
                                if _hm > 0.5 then _hx, _hz = _hx/_hm, _hz/_hm else _hx, _hz = 0, 0 end
                                for _, o in ipairs(_ll) do
                                    local _rr = ((_G.JAF_LiftCoreEngage ~= false) and (o.cr or o.r) or o.r) + (_G.JAF_LiftMargin or 6)
                                    local _tox, _toz = o.x - vHrp.Position.X, o.z - vHrp.Position.Z
                                    local _along = _tox * _hx + _toz * _hz
                                    local _perp2 = (_tox*_tox + _toz*_toz) - _along*_along
                                    if _perp2 < _rr * _rr and _along > -_rr then
                                        local _ot = o.liftY or (o.top + (_G.JAF_LiftClearance or 7))
                                        _obHit = true
                                        local _entry = _along - _rr
                                        local _fr = (_entry <= 0) and 1 or math.clamp(1 - _entry / math.max(_G.JAF_LiftLead or 70, 1), 0, 1)
                                        local _rt = vHrp.Position.Y + (_ot - vHrp.Position.Y) * _fr
                                        if _rt > _obRamp then _obRamp = _rt end
                                        if _ot > _obLift then _obLift = _ot end
                                        if o.top > _obTop then _obTop = o.top end
                                        if _along < _rr then _obClose = true end
                                    end end
                                if _obLift > 0 then _obLift = math.min(_obLift, riseTargetY + (tonumber(_G.JAF_LiftMaxUp) or (riseTargetY < 5 and tonumber(_G.JAF_F1LiftMaxUp)) or 14)) end
                                if _obHit then _obRamp = math.min(_obRamp, _obLift) end
                            end end
                        local _riseGoal = _obHit and math.max(riseTargetY, _obLift) or riseTargetY
                        if vHrp.Position.Y >= _riseGoal - 0.5 then riseConn:Disconnect(); riseDone = true; return end
                        local riseSpeedCap = _riseCap()
                        if _nearBase then riseSpeedCap = math.min(riseSpeedCap, tonumber(_G.JAF_NearBaseRiseSpeed) or 60) end
                        vHrp.AssemblyAngularVelocity = Vector3.zero
                        if _G.JAF_DiagonalRise ~= false and waypoints and #waypoints > 0 then
                            local _wp = _aimWp(vHrp, _pp
                                and (_G.JAF_M2(vHrp.Position.X, vHrp.Position.Z, _lastP.X, _lastP.Z) * 1.5 + 8) or 0)
                            _traveled = _traveled + _G.JAF_M2(vHrp.Position.X, vHrp.Position.Z, _lastP.X, _lastP.Z)
                            _lastP = vHrp.Position
                            local _hd = Vector3.new(_wp.X - vHrp.Position.X, 0, _wp.Z - vHrp.Position.Z)
                            local _hdist = _hd.Magnitude
                            local _u = (_hdist > 0.5) and (_hd / _hdist) or Vector3.zero
                            local _baseRemain = remainingFrom(vHrp.Position.X, vHrp.Position.Z, wpIndex)
                            local _closeDist  = _G.JAF_RiseCloseDist or 45
                            local _tgtY, _hSpd
                            if _obClose and _obLift > riseTargetY and vHrp.Position.Y < _obTop + 1.5 then
                                _tgtY = _obLift
                                _hSpd = _G.JAF_RiseObstacleMinH or 6
                            elseif _G.JAF_RiseCloseThrottle ~= false and _baseRemain < _closeDist and vHrp.Position.Y < _riseGoal - 0.5 then
                                _tgtY = riseTargetY
                                if _G.JAF_NearBaseRiseSlow ~= false then
                                    if _G.JAF_RiseSyncClose ~= false then
                                        local _tLeft = math.max((_riseGoal - vHrp.Position.Y) / math.max(riseSpeedCap, 1), 0.02)
                                        _hSpd = math.clamp((_baseRemain - (tonumber(_G.JAF_RiseSyncEarly) or 10)) / _tLeft,
                                                           _G.JAF_RiseCloseMinH or 10, VL_SPEED)
                                    else
                                        _hSpd = VL_SPEED
                                    end
                                else
                                    local _closeFrac = 1 - math.clamp(_baseRemain / _closeDist, 0, 1)
                                    _hSpd = VL_SPEED * (1 - _closeFrac * (_G.JAF_RiseCloseCut or 0.9))
                                    _hSpd = math.max(_hSpd, _G.JAF_RiseCloseMinH or 10)
                                end
                            else
                                local _prog = math.clamp(_traveled / _climbDist, 0, 1)
                                _tgtY = _startY + (riseTargetY - _startY) * _prog
                                local _f0 = _G.JAF_DiagonalRiseFrac or 0.6
                                _hSpd = VL_SPEED * (_f0 + (1 - _f0) * _prog)
                            end
                            if _obHit then
                                local _hold = (_G.JAF_RiseLiftRamp ~= false) and _obRamp or _obLift
                                if _hold > _tgtY then _tgtY = _hold end
                            end
                            local _yCap = riseSpeedCap
                            if _obLift > riseTargetY then _yCap = math.min(_yCap, tonumber(_G.JAF_CruiseRiseCap) or 90) end
                            local yVel = math.clamp((_tgtY - vHrp.Position.Y) * 20, 0, _yCap)
                            if _grap then JAF_GrappleTick(alive) _hSpd = math.min(_hSpd, JAF_GrappleSpeedCap()) end
                            vHrp.AssemblyLinearVelocity = Vector3.new(_u.X * _hSpd, yVel, _u.Z * _hSpd)
                        else
                            local yVel = math.clamp((riseTargetY - vHrp.Position.Y) * 20, 14, riseSpeedCap)
                            vHrp.AssemblyLinearVelocity = Vector3.new(_wallAway.X * WALL_PUSH_SPEED, yVel, _wallAway.Z * WALL_PUSH_SPEED)
                        end end)
                    local riseStart = tick()
                    repeat RunService.Heartbeat:Wait()
                    until riseDone or tick()-riseStart > 1.5 or not alive()
                    riseConn:Disconnect()
                end end
            do
                local _, dHrp = getChar()
                if dHrp and alive() and not _planY and dHrp.Position.Y > riseTargetY + 0.5
                   and _columnOpenAir(dHrp.Position, dHrp.Position.Y, riseTargetY) then
                    local descDone = false
                    local descConn
                    if _pp then _routeS = _projectS(dHrp.Position.X, dHrp.Position.Z, _routeS, nil) end
                    descConn = RunService.Heartbeat:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                        if not alive() then descConn:Disconnect(); descDone = true; return end
                        local _, vHrp = getChar()
                        if not vHrp then descConn:Disconnect(); descDone = true; return end
                        if vHrp.Position.Y <= riseTargetY + 0.5 then descConn:Disconnect(); descDone = true; return end
                        local _wp = _aimWp(vHrp, VL_SPEED * (2.5 / 30) + 8)
                        local _hd = Vector3.new(_wp.X - vHrp.Position.X, 0, _wp.Z - vHrp.Position.Z)
                        local _u  = (_hd.Magnitude > 0.5) and _hd.Unit or Vector3.zero
                        local _hSpd = VL_SPEED
                        if _grap then JAF_GrappleTick(alive); _hSpd = math.min(_hSpd, JAF_GrappleSpeedCap()) end
                        local descCap = _descCap()
                        local yVel = math.clamp((riseTargetY - vHrp.Position.Y) * 20, -descCap, -14)
                        vHrp.AssemblyAngularVelocity = Vector3.zero
                        vHrp.AssemblyLinearVelocity  = Vector3.new(_u.X * _hSpd, yVel, _u.Z * _hSpd)
                    end)
                    local descStart = tick()
                    repeat RunService.Heartbeat:Wait()
                    until descDone or tick()-descStart > 2 or not alive()
                    descConn:Disconnect()
                end end
            local _live = { rp = false, last = 0, pend = nil }
            do
                local _fn; _fn = (function(px, pz) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    for _, f in ipairs(_G.JAF_BaseFootprints or {}) do
                        if f.minX and px>=f.minX and px<=f.maxX and pz>=f.minZ and pz<=f.maxZ then return f.plot end
                    end end)
                _live.dst = _fn(targetBasePos.X, targetBasePos.Z)
            end
            local _segBox; _segBox = function(ax,az,bx,bz,x0,z0,x1,z1) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local dx,dz=bx-ax,bz-az; local tmin,tmax=0,1
                if math.abs(dx)<1e-6 then if ax<x0 or ax>x1 then return false end else local t1,t2=(x0-ax)/dx,(x1-ax)/dx; if t1>t2 then t1,t2=t2,t1 end tmin=math.max(tmin,t1) tmax=math.min(tmax,t2) if tmin>tmax then return false end end
                if math.abs(dz)<1e-6 then if az<z0 or az>z1 then return false end else local t1,t2=(z0-az)/dz,(z1-az)/dz if t1>t2 then t1,t2=t2,t1 end tmin=math.max(tmin,t1) tmax=math.min(tmax,t2) if tmin>tmax then return false end end
                return true
            end
            local _liveRP = RaycastParams.new()
            _liveRP.FilterType = Enum.RaycastFilterType.Exclude
            do
                local ig = { c }
                for _, ch in ipairs(Workspace:GetChildren()) do if ch.Name == "XiTempPlatform" then table.insert(ig, ch) end end
                local cl = Workspace:FindFirstChild(tostring(LocalPlayer.UserId).."_Clone"); if cl then table.insert(ig, cl) end
                local _plots = Workspace:FindFirstChild("Plots")
                if _plots then
                    local node = signPart
                    while node and node.Parent do if node.Parent == _plots then table.insert(ig, node); break end node = node.Parent end
                end
                _liveRP.FilterDescendantsInstances = ig
            end
            local _chordCut; _chordCut = function(px, pz, tx, tz, remaining) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local _cm = (_G.JAF_TurnClearMargin or 6)
                for _, _f in ipairs(_G.JAF_BaseFootprints or {}) do
                    if _f.minX then
                        local _inNow   = px >= _f.minX-_cm and px <= _f.maxX+_cm and pz >= _f.minZ-_cm and pz <= _f.maxZ+_cm
                        local _entryOk = (_f.plot == _live.dst) and remaining <= (NEAR_END_DIST + 20)
                        if not _inNow and not _entryOk
                           and _segBox(px, pz, tx, tz, _f.minX-_cm, _f.minZ-_cm, _f.maxX+_cm, _f.maxZ+_cm) then
                            return true
                        end end end
                if _G.JAF_FlightObstacles then
                    local _bx, _bz = tx - px, tz - pz
                    local _seg2 = _bx*_bx + _bz*_bz
                    for _, _o in ipairs(_G.JAF_FlightObstacles) do
                        local _tt = (_seg2 > 0.001) and (((_o.x-px)*_bx + (_o.z-pz)*_bz) / _seg2) or 0
                        if _tt < 0 then _tt = 0 elseif _tt > 1 then _tt = 1 end
                        local _qx, _qz = px + _bx*_tt - _o.x, pz + _bz*_tt - _o.z
                        local _rr = (_o.strict or _G.JAF_GapSmooth == false) and _o.r
                                    or math.max(_o.cr or _o.r, (_o.r or (_o.cr or 0)) - (tonumber(_G.JAF_ChordPadRelief) or 2))
                        if _qx*_qx + _qz*_qz < _rr*_rr then return true end
                    end end
                return false
            end
            local _rePathNow; _rePathNow = function(_fromNow) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                if _live.rp then return end
                _live.rp = true; _live.last = tick()
                local _pinTbp  = targetBasePos
                local _pinMode = sideChosen and "side" or "front"
                task.spawn(function() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
                    local _pf1, _pf2 = _G.JAF_F1Mode, _G.JAF_F2Mode
                    _G.JAF_F1Mode, _G.JAF_F2Mode = _pinMode, _pinMode
                    local ok, _tbp, _wps = pcall(computeFrontRoute, signPart, _fromNow)
                    _G.JAF_F1Mode, _G.JAF_F2Mode = _pf1, _pf2
                    if ok and type(_wps) == "table" and #_wps > 0 and _tbp
                       and Vector2.new(_tbp.X - _pinTbp.X, _tbp.Z - _pinTbp.Z).Magnitude <= 12 then
                        _wps[#_wps] = _pinTbp
                        _live.pend = _wps
                    end
                    _live.rp = false
                end) end
            local _fwdFlipDir; _fwdFlipDir = function(vHrp, _gx, _gz, dir) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local _m = math.sqrt(_gx * _gx + _gz * _gz)
                local _tX = targetBasePos.X - vHrp.Position.X
                local _tZ = targetBasePos.Z - vHrp.Position.Z
                local _pastEnd = (_routeS >= _arcTotal() - (tonumber(_G.JAF_PpEndSlack) or 2))
                if not _pastEnd and n >= 2 then
                    local _wn, _wp2 = waypoints[n], waypoints[n - 1]
                    if _wn and _wp2 then
                        local _lx, _lz = _wn.X - _wp2.X, _wn.Z - _wp2.Z
                        _pastEnd = ((vHrp.Position.X - _wn.X) * _lx + (vHrp.Position.Z - _wn.Z) * _lz) > 0
                    end end
                if _G.JAF_PpFwdFlipStrict == false then _pastEnd = true end
                if _G.JAF_PpFwdFlipGuard ~= false and _pastEnd and _m > 0.01
                   and (_gx * _tX + _gz * _tZ) < 0 then
                    local _tl = math.sqrt(_tX * _tX + _tZ * _tZ)
                    if _tl > 0.01 then return Vector3.new(_tX / _tl, 0, _tZ / _tl) end
                elseif _m > 0.01 then
                    return Vector3.new(_gx / _m, 0, _gz / _m)
                end
                return dir
            end
            local vlConn
            if _pp then local _cM, _hM = getChar() if _hM then _routeS = _projectS(_hM.Position.X, _hM.Position.Z, _routeS, nil) end end
            vlConn = RunService.Heartbeat:Connect(function(dt) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if not alive() then vlConn:Disconnect(); velDone = true; return end
                if dt and dt > 0 then
                    _fN = _fN + 1; _fSum = _fSum + dt
                    if dt > _fWorst then _fWorst = dt end
                    if dt > 0.033 then _fOver33 = _fOver33 + 1 end
                    if dt > 0.050 then _fOver50 = _fOver50 + 1 end
                    local _dtFade = _dtRecent * math.max(0, 1 - dt / (tonumber(_G.JAF_StepDtFade) or 0.25))
                    _dtRecent = (dt > _dtFade) and dt or _dtFade
                end
                local _, vHrp = getChar()
                if not vHrp then vlConn:Disconnect(); velDone = true; return end
                if _ykT0 == 0 then _ykT0 = tick() end
                if not SAB_LIVE() then
                    _exitWhy = "revoked"
                    _exitRem = _G.JAF_M2(vHrp.Position.X, vHrp.Position.Z, targetBasePos.X, targetBasePos.Z)
                    vHrp.AssemblyLinearVelocity = Vector3.zero; vHrp.AssemblyAngularVelocity = Vector3.zero
                    vlConn:Disconnect(); velDone = true; return
                end
                if _G.JAF_FlightOffMapGuard ~= false then
                    local _MB = _G.JAF_MapBounds
                    if _MB and _MB.minX and _MB.maxX and _MB.minZ and _MB.maxZ and _MB.maxX > _MB.minX and _MB.maxZ > _MB.minZ then
                        local _om = tonumber(_G.JAF_FlightOffMapMargin) or 40
                        local _px, _pz = vHrp.Position.X, vHrp.Position.Z
                        if _px < _MB.minX - _om or _px > _MB.maxX + _om or _pz < _MB.minZ - _om or _pz > _MB.maxZ + _om then
                            _exitWhy = "offmap"
                            _exitRem = _G.JAF_M2(_px, _pz, targetBasePos.X, targetBasePos.Z)
                            local _oc, _oh = getChar(); if _oh then _oh.AssemblyLinearVelocity = Vector3.zero; _oh.AssemblyAngularVelocity = Vector3.zero end
                            vlConn:Disconnect(); velDone = true; return
                        end end end
                if _G.JAF_FlightTargetGuard ~= false and signPart then
                    if signPart.Parent == nil then
                        _tgGoneN = (_tgGoneN or 0) + 1
                        if _tgGoneN >= (tonumber(_G.JAF_TargetGoneFrames) or 3) then
                            _exitWhy = "targetgone"
                            _exitRem = _G.JAF_M2(vHrp.Position.X, vHrp.Position.Z, targetBasePos.X, targetBasePos.Z)
                            local _tc, _th = getChar()
                            if _th then _th.AssemblyLinearVelocity = Vector3.zero; _th.AssemblyAngularVelocity = Vector3.zero end
                            vlConn:Disconnect(); velDone = true; return
                        end
                    else
                        _tgGoneN = 0
                    end end
                if _lastFlightPos then
                    local _move = vHrp.Position - _lastFlightPos
                    local _jumpDist = _move.Magnitude
                    local _rbSpd = VL_SPEED
                    if _G.JAF_LbUseActualSpd ~= false and _spdPrev then _rbSpd = math.max(_spdPrev, tonumber(_G.JAF_LbSpdFloor) or 120) end
                    local _rbThresh = math.max(20, (_rbSpd * dt) * 1.8)
                    local _isRubberband = _jumpDist > _rbThresh
                    local _lbBack, _remGrew = false, 0
                    local _remNow = _G.JAF_M2(vHrp.Position.X, vHrp.Position.Z, targetBasePos.X, targetBasePos.Z)
                    local _arcFwd = nil
                    if _pp and n >= 3 and _G.JAF_RouteAwareLb ~= false then
                        local _aNow = _projectS(vHrp.Position.X, vHrp.Position.Z, 0, nil)
                        if _lbPrevArc then _arcFwd = math.clamp(_aNow - _lbPrevArc, -_jumpDist - 1, _jumpDist + 1) end
                        _lbPrevArc = _aNow
                    end
                    if _G.JAF_LbBackstop ~= false then
                        _remGrew = _lbPrevRem and (_remNow - _lbPrevRem) or 0
                        _lbPrevRem = _remNow
                        if not _isRubberband and _remGrew > (tonumber(_G.JAF_LbBackGrow) or 25)
                           and not (_arcFwd and _arcFwd > 0) then _isRubberband, _lbBack = true, true end
                    end
                    if _G.JAF_LbBackDetect ~= false and not _isRubberband
                       and tick() - _live.last > (tonumber(_G.JAF_LbBackQuiet) or 0.5) then
                        if _arcFwd then
                            if _arcFwd < -(tonumber(_G.JAF_LbBackDist) or 12) then _isRubberband = true end
                        else
                        local _wpB = waypoints[wpIndex] or targetBasePos
                        local _ibx, _ibz = _wpB.X - _lastFlightPos.X, _wpB.Z - _lastFlightPos.Z
                        local _ibMag = math.sqrt(_ibx * _ibx + _ibz * _ibz)
                        if _ibMag > 1 then
                            local _bc = (_move.X * _ibx + _move.Z * _ibz) / _ibMag
                            if _bc < -(tonumber(_G.JAF_LbBackDist) or 12) then _isRubberband = true end
                        end end end
                    local _fwdDot = 1
                    if _isRubberband and _G.JAF_LbDirGate ~= false then
                        local _wpNow = waypoints[wpIndex] or targetBasePos
                        local _inx, _inz = _wpNow.X - _lastFlightPos.X, _wpNow.Z - _lastFlightPos.Z
                        local _mxz, _mzz = _move.X, _move.Z
                        local _intendMag = math.sqrt(_inx * _inx + _inz * _inz)
                        local _moveMag = math.sqrt(_mxz * _mxz + _mzz * _mzz)
                        if _intendMag > 1 and _moveMag > 1 then
                            _fwdDot = (_mxz * _inx + _mzz * _inz) / (_moveMag * _intendMag)
                            if _fwdDot >= (_G.JAF_LbFwdDot or 0.2) then _isRubberband = false end
                        elseif _arcFwd and _arcFwd > (tonumber(_G.JAF_LbArcFwdMin) or 1) then
                            _isRubberband = false
                            _G.JAF_LbArcClearN = (tonumber(_G.JAF_LbArcClearN) or 0) + 1
                        end end
                    if _lbBack and _isRubberband then if _G.JAF_LbBackArm == false then _isRubberband = false end end
                    if _G.JAF_LbRePath ~= false and _isRubberband and signPart and signPart.Parent and not _live.rp
                       and _remNow > (NEAR_END_DIST + 12)
                       and tick() - _live.last > (_G.JAF_LbRePathMin or 0.3) then
                        _rePathNow(vHrp.Position)
                    end
                    if _isRubberband then
                        _lbN = _lbN + 1
                        if _jumpDist > _lbMaxJump then _lbMaxJump = _jumpDist end
                        local _curXZ = Vector2.new(vHrp.Position.X, vHrp.Position.Z)
                        local _guard = (_G.JAF_LbRepickSegGuard ~= false)
                        local _fps = _guard and (_G.JAF_BaseFootprints or {}) or {}
                        local _mrg = _G.JAF_BaseAvoidMargin or ((_G.JAF_MarginMatch ~= false) and tonumber(_G.JAF_BaseAvoidMarginUsed)) or 8
                        local function _inFp(f, x, z, m) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return x >= f.minX-m and x <= f.maxX+m and z >= f.minZ-m and z <= f.maxZ+m end
                        local function _segHitsBase(ax, az, bx, bz) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
                            for _, f in ipairs(_fps) do
                                if f.minX
                                   and not _inFp(f, targetBasePos.X, targetBasePos.Z, _mrg)
                                   and not _inFp(f, ax, az, 0) then
                                    local x0,z0,x1,z1 = f.minX-_mrg, f.minZ-_mrg, f.maxX+_mrg, f.maxZ+_mrg
                                    local ddx,ddz = bx-ax, bz-az; local tmin,tmax = 0,1; local ok = true
                                    if math.abs(ddx) < 1e-6 then if ax < x0 or ax > x1 then ok=false end
                                    else local t1,t2=(x0-ax)/ddx,(x1-ax)/ddx; if t1>t2 then t1,t2=t2,t1 end; tmin=math.max(tmin,t1); tmax=math.min(tmax,t2); if tmin>tmax then ok=false end end
                                    if ok then
                                        if math.abs(ddz) < 1e-6 then if az < z0 or az > z1 then ok=false end
                                        else local t1,t2=(z0-az)/ddz,(z1-az)/ddz; if t1>t2 then t1,t2=t2,t1 end; tmin=math.max(tmin,t1); tmax=math.min(tmax,t2); if tmin>tmax then ok=false end end
                                    end
                                    if ok then return true end
                                end end
                            return false
                        end
                        if _pp then
                            local _sNew = _projectS(vHrp.Position.X, vHrp.Position.Z, 0, nil)
                            local _lbWin = _jumpDist * (tonumber(_G.JAF_LbFootWinK) or 1.5) + (tonumber(_G.JAF_LbFootWinPad) or 24)
                            local _lbFootOK = (_G.JAF_LbFootGuard == false) or (math.abs(_sNew - _routeS) <= _lbWin)
                            if not _lbFootOK then _G.JAF_LbFootRejN = (tonumber(_G.JAF_LbFootRejN) or 0) + 1 end
                            local _qx, _qz = _pointAt(math.min(_sNew + 6, _arcTotal()))
                            if _lbFootOK and not (_guard and _segHitsBase(_curXZ.X, _curXZ.Y, _qx, _qz)) then
                                _routeS = _sNew
                                local _qx2, _qz2, _si = _pointAt(_routeS)
                                wpIndex = _si
                            end
                        else
                            local _bestIdx, _bestDist = nil, math.huge
                            for i = wpIndex, n do
                                local _wpv = waypoints[i]
                                local _d = (_curXZ - Vector2.new(_wpv.X, _wpv.Z)).Magnitude
                                if _d < _bestDist and not (_guard and _segHitsBase(_curXZ.X, _curXZ.Y, _wpv.X, _wpv.Z)) then
                                    _bestDist = _d; _bestIdx = i
                                end end
                            if _bestIdx then wpIndex = _bestIdx end
                        end
                        if tick() - _lbCapAt > (tonumber(_G.JAF_LbStreakDecay) or 3) then _lbStreak = 0 end
                        _lbStreak = _lbStreak + 1
                        _lbCap   = math.min(tonumber(_G.JAF_LbEaseSpeed) or 175, VL_SPEED)
                        _lbCapAt = tick()
                    end end
                _lastFlightPos = vHrp.Position
                do local _md = (Vector3.new(vHrp.Position.X,0,vHrp.Position.Z) - Vector3.new(_flightStartPos.X,0,_flightStartPos.Z)).Magnitude; if _md > _maxFlightMove then _maxFlightMove = _md end end
                local wp, toWp
                if _pp then
                    local _stepXZ = _projPrevPos
                        and _G.JAF_M2(vHrp.Position.X, vHrp.Position.Z, _projPrevPos.X, _projPrevPos.Z)
                        or (VL_SPEED * dt)
                    _projPrevPos = vHrp.Position
                    local _projCap = _stepXZ * (tonumber(_G.JAF_ProjStepK) or 1.5) + (tonumber(_G.JAF_ProjStepPad) or 2)
                    if _G.JAF_ProjStepBound == false then _projCap = VL_SPEED * dt * 2.5 + 8 end
                    _routeS = _projectS(vHrp.Position.X, vHrp.Position.Z, _routeS, _projCap)
                    local _qxA, _qzA, _siA = _pointAt(_routeS)
                    wpIndex = _siA
                    wp = waypoints[wpIndex] or targetBasePos
                    toWp = _G.JAF_M2(vHrp.Position.X, vHrp.Position.Z, wp.X, wp.Z)
                else
                wp = waypoints[wpIndex] or targetBasePos
                toWp = _G.JAF_M2(vHrp.Position.X, vHrp.Position.Z, wp.X, wp.Z)
                while wpIndex < n do
                    local _adv = (toWp <= 6)
                    if not _adv then
                        local _pv = waypoints[wpIndex - 1] or _flightStartPos
                        local _sx, _sz = wp.X - _pv.X, wp.Z - _pv.Z
                        if (vHrp.Position.X - wp.X) * _sx + (vHrp.Position.Z - wp.Z) * _sz > 0 then _adv = true end
                    end
                    local _finalHopGuard = false
                    if (_G.JAF_NoFinalCornerCut ~= false) and (wpIndex == n - 1) then
                        local _fw = waypoints[n]
                        if _fw then
                            local _m = tonumber(_G.JAF_DstNearMargin) or 12
                            for _, f in ipairs(_G.JAF_BaseFootprints or {}) do
                                if f.minX then
                                    local _dxg = math.max(f.minX - _fw.X, _fw.X - f.maxX, 0)
                                    local _dzg = math.max(f.minZ - _fw.Z, _fw.Z - f.maxZ, 0)
                                    if (_dxg * _dxg + _dzg * _dzg) <= _m * _m then _finalHopGuard = true; break end
                                end end end end
                    if not _adv and not _finalHopGuard then
                        local _nw = waypoints[wpIndex + 1]
                        local _rad = tonumber(_G.JAF_WpNextCloserRad) or math.max(14, math.min(VL_SPEED * dt * 2.5, 45))
                        if _nw and toWp <= _rad then
                            local _dn = _G.JAF_M2(vHrp.Position.X, vHrp.Position.Z, _nw.X, _nw.Z)
                            if _dn < toWp then _adv = true end
                        end end
                    if not _adv then break end
                    wpIndex = wpIndex + 1
                    _npAimRaw = false
                    wp = waypoints[wpIndex]
                    toWp = _G.JAF_M2(vHrp.Position.X, vHrp.Position.Z, wp.X, wp.Z)
                end end
                local remaining = remainingFrom(vHrp.Position.X, vHrp.Position.Z, wpIndex)
                if _G.JAF_LateObstacleGuard ~= false and #_obsNew > 0 and not _live.rp
                   and signPart and signPart.Parent
                   and _lateObsN < (tonumber(_G.JAF_LateObsMaxRePath) or 3)
                   and remaining > NEAR_END_DIST + 12
                   and (tick() - _lateObsChk) > (tonumber(_G.JAF_LateObsEvery) or 0.15)
                   and (tick() - _live.last) > (_G.JAF_LbRePathMin or 0.3) then
                    _lateObsChk = tick()
                    local _berth = (tonumber(_G.JAF_CharRadius) or 4) + (_G.JAF_ObstaclePad or 4)
                    local _hitNew = false
                    for _i = #_obsNew, 1, -1 do
                        local _o = _obsNew[_i]
                        if _o.t < _planAt - 0.5 then break end
                        local _rr = _o.r + _berth
                        local _ax, _az = vHrp.Position.X, vHrp.Position.Z
                        for _w = wpIndex, n do
                            local _wp2 = waypoints[_w]
                            local _dx, _dz = _wp2.X - _ax, _wp2.Z - _az
                            local _l2 = _dx * _dx + _dz * _dz
                            local _t2 = (_l2 > 0.001) and (((_o.x - _ax) * _dx + (_o.z - _az) * _dz) / _l2) or 0
                            if _t2 < 0 then _t2 = 0 elseif _t2 > 1 then _t2 = 1 end
                            local _qx, _qz = _ax + _dx * _t2 - _o.x, _az + _dz * _t2 - _o.z
                            if _qx * _qx + _qz * _qz < _rr * _rr then _hitNew = true; break end
                            _ax, _az = _wp2.X, _wp2.Z
                        end
                        if _hitNew then break end
                    end
                    if _hitNew then _lateObsN = _lateObsN + 1 _rePathNow(vHrp.Position) end
                end
                if remaining <= ARRIVE_DIST then
                    local _okArrive = true
                    if _G.JAF_ArriveVerify ~= false then
                        local _tvx = vHrp.Position.X - targetBasePos.X
                        local _tvz = vHrp.Position.Z - targetBasePos.Z
                        if math.sqrt(_tvx * _tvx + _tvz * _tvz) > (tonumber(_G.JAF_ArriveVerifyMax) or 12) then
                            _okArrive = false
                            _arriveFixN = _arriveFixN + 1
                            if _pp then
                                _routeS = _projectS(vHrp.Position.X, vHrp.Position.Z, 0, nil)
                                local _qxV, _qzV, _siV = _pointAt(_routeS)
                                wpIndex = _siV
                            end
                            remaining = remainingFrom(vHrp.Position.X, vHrp.Position.Z, wpIndex)
                            _prevRemaining = nil; _stuckTime = 0
                            _bestRemaining = remaining; _bestRemAt = tick()
                            _npAimRaw = false
                        end end
                    if _okArrive then
                        _exitWhy, _exitRem = "arrive", remaining
                        _tpArriveOff = Vector2.new(vHrp.Position.X - targetBasePos.X, vHrp.Position.Z - targetBasePos.Z).Magnitude
                        _tpArriveAt  = tick()
                        if _G.JAF_ArriveZeroVel ~= false and not _f1FrontDrive and vHrp and vHrp.Parent then
                            local _av = vHrp.AssemblyLinearVelocity
                            vHrp.AssemblyLinearVelocity = Vector3.new(0, _av.Y, 0)
                        end
                        vlConn:Disconnect(); velDone = true; return
                    end end
                if _G.JAF_StallRecover ~= false
                   and (_lbCap == math.huge or (tick() - _lbCapAt) > (tonumber(_G.JAF_StallWindow) or 0.8)) then
                    local _stallEps = tonumber(_G.JAF_StallEps) or 1.5
                    local _stallWin = tonumber(_G.JAF_StallWindow) or 0.8
                    if _bestRemAt == nil then _bestRemaining = remaining; _bestRemAt = tick() end
                    if remaining < _bestRemaining - _stallEps then
                        _bestRemaining = remaining; _bestRemAt = tick()
                        if _pp then _npAimRaw = false end
                    elseif tick() - _bestRemAt > _stallWin then
                        _stallRecoverN = _stallRecoverN + 1
                        if not _npAimRaw then
                            _npAimRaw = true
                        elseif (_pp and _routeS < _arcTotal() - 2) or (not _pp and wpIndex < n) then
                            if _pp then
                                _routeS = math.min(_routeS + (tonumber(_G.JAF_StallAdvance) or 12), _arcTotal())
                                local _qxL, _qzL, _siL = _pointAt(_routeS)
                                wpIndex = _siL
                            else
                                wpIndex = wpIndex + 1
                            end
                            wp = waypoints[wpIndex] or targetBasePos
                            remaining = remainingFrom(vHrp.Position.X, vHrp.Position.Z, wpIndex)
                        else
                            _exitWhy, _exitRem = "stall-final", remaining
                            vlConn:Disconnect(); velDone = true; return
                        end
                        _bestRemaining = remaining; _bestRemAt = tick()
                        _prevRemaining = nil; _stuckTime = 0
                    end end
                local _progRate = _prevRemaining and ((_prevRemaining - remaining) / math.max(dt, 1/240)) or math.huge
                if _prevRemaining and _progRate < (tonumber(_G.JAF_DeadStopRate) or 18) then
                    _stuckTime = _stuckTime + dt
                    if _stuckTime >= (tonumber(_G.JAF_DeadStopTime) or 0.3) then
                        if remaining <= (tonumber(_G.JAF_DeadStopNear) or 35) then
                            _exitWhy, _exitRem = "dead-stop", remaining
                            vlConn:Disconnect(); velDone = true; return
                        end
                        _stuckTime = 0
                        _npAimRaw = true
                    end
                else
                    _stuckTime = 0
                end
                _prevRemaining = remaining
                local dir
                if _pp then
                    local _vLA = vHrp.AssemblyLinearVelocity
                    local _L = tonumber(_G.JAF_TurnLookahead)
                        or math.min(math.max(14, math.sqrt(_vLA.X * _vLA.X + _vLA.Z * _vLA.Z) * (tonumber(_G.JAF_LookaheadT) or 0.12)),
                                    tonumber(_G.JAF_LookaheadMax) or 48)
                    if _G.JAF_WpChase ~= false and n >= 2 then
                        local _wcTot = suffix[1] or 0
                        local _wcDot = tonumber(_G.JAF_WpChaseTurnDot) or 0.985
                        for _wi = 2, n do
                            local _wcArc = _wcTot - (suffix[_wi] or 0)
                            if _wcArc > _routeS + 0.5 then
                                local _isCorner = true
                                local _wa, _wb, _wc = waypoints[_wi - 1], waypoints[_wi], waypoints[_wi + 1]
                                if _wa and _wb and _wc then
                                    local _d1x, _d1z = _wb.X - _wa.X, _wb.Z - _wa.Z
                                    local _d2x, _d2z = _wc.X - _wb.X, _wc.Z - _wb.Z
                                    local _m1 = math.sqrt(_d1x * _d1x + _d1z * _d1z)
                                    local _m2 = math.sqrt(_d2x * _d2x + _d2z * _d2z)
                                    if _m1 > 0.1 and _m2 > 0.1 then
                                        if ((_d1x * _d2x + _d1z * _d2z) / (_m1 * _m2)) > _wcDot then _isCorner = false end
                                    end end
                                if _isCorner then
                                    local _wcTo = _wcArc - _routeS
                                    if _L > _wcTo then
                                        local _wcMin = tonumber(_G.JAF_WpChaseLead) or 6
                                        _L = (_wcTo < _wcMin) and _wcMin or _wcTo
                                    end
                                    break
                                end end end end
                    if _G.JAF_TurnBlend == false or _npAimRaw then
                        _L = 4
                    elseif remaining <= NEAR_END_DIST then
                        if _G.JAF_NearEndBlend ~= false then
                            _L = math.clamp(remaining * (tonumber(_G.JAF_NearEndLookFrac) or 0.5), 4, _L)
                        else
                            _L = 4
                        end end
                    local _ppx, _ppz = vHrp.Position.X, vHrp.Position.Z
                    local _tx, _tz
                    local _cutBudget = tonumber(_G.JAF_MaxCut) or 2.5
                    while true do
                        _tx, _tz = _pointAt(_routeS + _L)
                        local _cutOk = true
                        if _cutBudget > 0 and _L > 8 then
                            local _mx, _mz = _pointAt(_routeS + _L * 0.5)
                            local _chx, _chz = _tx - _ppx, _tz - _ppz
                            local _chl = math.sqrt(_chx * _chx + _chz * _chz)
                            if _chl > 1e-3 then
                                local _worst = math.abs((_mx - _ppx) * _chz - (_mz - _ppz) * _chx) / _chl
                                if _G.JAF_CutVertex ~= false then
                                    local _tot = suffix[1] or 0
                                    local _sEnd = _routeS + _L
                                    for _vi = 2, n - 1 do
                                        local _vs = _tot - (suffix[_vi] or 0)
                                        if _vs >= _sEnd then break end
                                        if _vs > _routeS then
                                            local _w = waypoints[_vi]
                                            local _d = math.abs((_w.X - _ppx) * _chz - (_w.Z - _ppz) * _chx) / _chl
                                            if _d > _worst then _worst = _d end
                                        end end end
                                _cutOk = _worst <= _cutBudget
                            end end
                        if _L <= 4 or (_cutOk and not _chordCut(_ppx, _ppz, _tx, _tz, remaining)) then break end
                        _L = math.max(4, _L * 0.5)
                    end
                    if _G.JAF_PpLookaheadEase ~= false then
                        local _upStep = tonumber(_G.JAF_PpLookaheadUp) or ((tonumber(_G.JAF_PpLookaheadUpRate) or 480) * dt)
                        if _L >= _ppL then _ppL = math.min(_L, _ppL + _upStep) else _ppL = _L end
                        _tx, _tz = _pointAt(_routeS + _ppL)
                    else
                        _ppL = _L
                    end
                    dir = Vector3.new(_tx - _ppx, 0, _tz - _ppz)
                    if dir.Magnitude > 0.05 then
                        dir = dir.Unit
                    else
                        local _fx, _fz = _pointAt(math.min(_routeS + 6, _arcTotal()))
                        dir = Vector3.new(_fx - _ppx, 0, _fz - _ppz)
                        dir = (dir.Magnitude > 0.01) and dir.Unit or Vector3.zero
                    end
                    local _tgx, _tgz
                    if _G.JAF_VtxTangent ~= false and n >= 3 then
                        _tgx, _tgz = _segDirAt(_routeS)
                        if _tgx then _G.JAF_VtxTanN = (tonumber(_G.JAF_VtxTanN) or 0) + 1 end
                    end
                    if not _tgx then
                        local _fx2, _fz2 = _pointAt(math.min(_routeS + 3, _arcTotal()))
                        local _bx2, _bz2 = _pointAt(math.max(_routeS - 3, 0))
                        _tgx, _tgz = _fx2 - _bx2, _fz2 - _bz2
                    end
                    if dir.X * _tgx + dir.Z * _tgz < -0.001 then
                        if _G.JAF_PpEnforceFwd ~= false then
                            local _reAllow = _G.JAF_PpReanchor ~= false
                            if _reAllow and _G.JAF_PpReanchorEndOff ~= false
                               and remaining <= (tonumber(_G.JAF_PpReanchorOffDist) or NEAR_END_DIST) then
                                _reAllow = false
                            end
                            if _reAllow then
                                local _bodyS = _projectS(_ppx, _ppz, 0, nil)
                                if _bodyS < _routeS then _routeS = math.max(_bodyS, _routeS - (tonumber(_G.JAF_PpReanchorMax) or 40)) end
                            end
                            dir = _fwdFlipDir(vHrp, _tgx, _tgz, dir)
                        end end
                else
                    dir = Vector3.new(wp.X - vHrp.Position.X, 0, wp.Z - vHrp.Position.Z)
                    if dir.Magnitude > 0 then dir = dir.Unit end
                end
                if _live.pend then
                    _rerouteN = _rerouteN + 1
                    waypoints = _live.pend; _live.pend = nil
                    if #waypoints >= 1 and Vector2.new(waypoints[1].X - vHrp.Position.X, waypoints[1].Z - vHrp.Position.Z).Magnitude > 8 then
                        table.insert(waypoints, 1, Vector3.new(vHrp.Position.X, waypoints[1].Y, vHrp.Position.Z))
                    end
                    n = #waypoints; wpIndex = 1
                    suffix = {}; suffix[n] = 0
                    for i = n - 1, 1, -1 do
                        suffix[i] = suffix[i + 1] + Vector2.new(waypoints[i].X - waypoints[i + 1].X, waypoints[i].Z - waypoints[i + 1].Z).Magnitude
                    end
                    _lbPrevArc = nil
                    descentStartIdx = n + 1
                    descending = false
                    if _planY then _planY = _buildPlanY(vHrp.Position.Y, vHrp.Position) end
                    if _pp then
                        _routeS = _projectS(vHrp.Position.X, vHrp.Position.Z, 0, nil)
                        local _qxS, _qzS, _siS = _pointAt(_routeS)
                        wpIndex = _siS
                    elseif _G.JAF_LbSwapAdvance ~= false then
                        while wpIndex < n do
                            local _w = waypoints[wpIndex]
                            local _wn = waypoints[wpIndex + 1] or targetBasePos
                            local _lx, _lz = _wn.X - _w.X, _wn.Z - _w.Z
                            local _close = Vector2.new(vHrp.Position.X - _w.X, vHrp.Position.Z - _w.Z).Magnitude <= 6
                            local _past  = ((vHrp.Position.X - _w.X) * _lx + (vHrp.Position.Z - _w.Z) * _lz) > 0
                            if not (_close or _past) then break end
                            wpIndex = wpIndex + 1
                        end end
                    wp = waypoints[wpIndex] or targetBasePos
                    if _pp then
                        local _txS, _tzS = _pointAt(math.min(_routeS + 8, _arcTotal()))
                        dir = Vector3.new(_txS - vHrp.Position.X, 0, _tzS - vHrp.Position.Z)
                    else
                        dir = Vector3.new(wp.X - vHrp.Position.X, 0, wp.Z - vHrp.Position.Z)
                    end
                    if dir.Magnitude > 0 then dir = dir.Unit end
                    if _pp and _G.JAF_PpEnforceFwd ~= false and dir.Magnitude > 0 then
                        local _stx, _stz
                        if _G.JAF_VtxTangent ~= false and n >= 3 then _stx, _stz = _segDirAt(_routeS) end
                        if not _stx then
                            local _sfx, _sfz = _pointAt(math.min(_routeS + 3, _arcTotal()))
                            local _sbx, _sbz = _pointAt(math.max(_routeS - 3, 0))
                            _stx, _stz = _sfx - _sbx, _sfz - _sbz
                        end
                        if dir.X * _stx + dir.Z * _stz < -0.001 then dir = _fwdFlipDir(vHrp, _stx, _stz, dir) end
                    end
                    remaining = remainingFrom(vHrp.Position.X, vHrp.Position.Z, wpIndex)
                    _bestRemaining = remaining; _bestRemAt = tick(); _npAimRaw = false
                end
                if remaining <= NEAR_END_DIST or wpIndex >= descentStartIdx then descending = true end
                local targetY = descending and finalY or riseTargetY
                local _planNow
                if _planY then _planNow = _planY(_routeS); targetY = _planNow end
                local _holdBind = false
                if _G.JAF_SnapDescent ~= false then
                    if remaining <= (tonumber(_G.JAF_SnapHoldDist) or 18) then
                        if not _snapHoldY then
                            local _hy0 = vHrp.Position.Y
                            if finalY and (_hy0 - finalY) <= (tonumber(_G.JAF_SnapHoldMaxDrop) or 200) then _snapHoldY = _hy0 end
                        end
                        if _snapHoldY and targetY < _snapHoldY then targetY = _snapHoldY; _holdBind = true end
                    elseif _snapHoldY then
                        _snapHoldY = nil
                    end end
                local _liftList = _G.JAF_FlightLiftObstacles
                if _liftList and not descending and not _planY then
                    local _liftClr  = _G.JAF_LiftClearance or 7
                    local _liftMrg  = _G.JAF_LiftMargin    or 6
                    local _liftLead = _G.JAF_LiftLead or 70
                    local _px, _pz  = vHrp.Position.X, vHrp.Position.Z
                    local _wantY = targetY
                    for _, o in ipairs(_liftList) do
                        local _ot = o.liftY or (o.top + _liftClr)
                        if _ot > targetY then
                            local _trr = ((_G.JAF_LiftCoreEngage ~= false) and (o.cr or o.r) or o.r) + _liftMrg
                            local _tox, _toz = o.x - _px, o.z - _pz
                            local _along = _tox * dir.X + _toz * dir.Z
                            local _perp2 = (_tox*_tox + _toz*_toz) - _along*_along
                            if _perp2 < _trr * _trr and _along > -_trr then
                                local _entry = _along - _trr
                                local _frac  = (_entry <= 0) and 1 or math.clamp(1 - _entry / math.max(_liftLead, 1), 0, 1)
                                local _ramp  = targetY + (_ot - targetY) * _frac
                                if _ramp > _wantY then _wantY = _ramp end
                            end end end
                    targetY = math.min(_wantY, riseTargetY + (tonumber(_G.JAF_LiftMaxUp) or (riseTargetY < 5 and tonumber(_G.JAF_F1LiftMaxUp)) or 14))
                end
                local yVel
                if _planY then
                    local _v0  = vHrp.AssemblyLinearVelocity
                    local _sp0 = math.sqrt(_v0.X * _v0.X + _v0.Z * _v0.Z)
                    local _ds  = math.max(_sp0 * dt, 0.25)
                    local _vff = (_planY(_routeS + _ds) - (_planNow or targetY)) / math.max(dt, 1/240)
                    if _holdBind then _vff = 0 end
                    local _errY = targetY - vHrp.Position.Y
                    local _corr
                    if _errY > (tonumber(_G.JAF_SnapBand) or 2.5) then
                        _corr = _errY / math.max(tonumber(_G.JAF_SnapTau) or 0.05, dt)
                    else
                        _corr = _errY / math.max(tonumber(_G.JAF_ProfileCorrTau) or 0.08, dt)
                        if _corr < -(tonumber(_G.JAF_ProfileCorrDown) or 40) then _corr = -(tonumber(_G.JAF_ProfileCorrDown) or 40) end
                    end
                    yVel = _vff + _corr
                    local _dc = _descCap()
                    if yVel < -_dc then yVel = -_dc end
                    local _pUp = tonumber(_G.JAF_ProfileRiseCap)
                    if _pUp == nil then _pUp = tonumber(_G.JAF_CruiseRiseCap) or 90 end
                    if _pUp > 0 and yVel > _pUp then yVel = _pUp end
                else
                local _downCap = descending and _descCap() or (_G.JAF_CruiseSinkCap or 16)
                local _riseCapUp = tonumber(_G.JAF_CruiseRiseCap) or 90
                local _upCapA = math.min(tonumber(_G.JAF_AntiSinkUp) or 160, _riseCapUp)
                local _upCapB = math.min(120, _riseCapUp)
                if _nearBase then
                    local _nbr = tonumber(_G.JAF_NearBaseRiseSpeed) or 60
                    _upCapA = math.min(_upCapA, _nbr); _upCapB = math.min(_upCapB, _nbr)
                end
                if _G.JAF_AntiSink ~= false then
                    local _toLine = (targetY - vHrp.Position.Y) / math.max(dt, 1/240)
                    yVel = math.clamp(_toLine, -_downCap, _upCapA)
                else
                    yVel = math.clamp((targetY - vHrp.Position.Y) * (_G.JAF_CruiseYGain or 20), -_downCap, _upCapB)
                end end
                if not descending then local _sag = riseTargetY - vHrp.Position.Y; if _sag > _sagMax then _sagMax = _sag end end
                if vHrp.Position.Y > math.max(riseTargetY, finalY, targetY) + (_G.JAF_FlyCeil or 8) then yVel = math.min(yVel, -25) end
                vHrp.AssemblyAngularVelocity = Vector3.zero
                local spd = VL_SPEED
                if _grap then JAF_GrappleTick(alive); spd = math.min(spd, JAF_GrappleSpeedCap()) end
                if _planY then
                    local _slpW = 12
                    local _slpAhead = _planY(math.min(_routeS + _slpW, _arcTotal()))
                    local _slp = math.abs(_slpAhead - _planNow) / _slpW
                    if _G.JAF_ClimbFeasClamp ~= false and _slp > 0.05 and _slpAhead > _planNow + 0.5 then
                        local _fcUp = tonumber(_G.JAF_ProfileRiseCap)
                        if _fcUp == nil then _fcUp = tonumber(_G.JAF_CruiseRiseCap) or 90 end
                        local _fcNeed = (_fcUp * (tonumber(_G.JAF_ClimbFeasSlack) or 1.15)) / _slp
                        if _fcNeed < spd then
                            spd = math.max(_fcNeed, tonumber(_G.JAF_ClimbFeasFloor) or 50)
                            _G.JAF_ClimbFeasN = (tonumber(_G.JAF_ClimbFeasN) or 0) + 1
                        end end
                    if _slp > 0.25 and _nearBase and _G.JAF_NearBasePace == true then
                        spd = math.min(spd, (tonumber(_G.JAF_NearBaseRiseSpeed) or 60) / _slp)
                    end
                    local _vSafeV = nil
                    if _G.JAF_F2NearRiseCap ~= false and isSecondFloor and targetY < riseTargetY - 0.5 then
                        local _nb = _G.JAF_IsNeighbourBase and _G.JAF_IsNeighbourBase(targetBasePos)
                        if _nb == nil then _nb = (_arcTotal() < (tonumber(_G.JAF_F2NearDist) or 130)) end
                        if _nb then
                            _vSafeV = tonumber(_G.JAF_F2NearVertMax)
                                or math.min(tonumber(_G.JAF_CruiseRiseCap) or 90,
                                            tonumber(_G.JAF_DescCap) or 95)
                        end end
                    if _G.JAF_TotalSpeedPace ~= false and _slp > 0.05 then
                        local _h = math.max(VL_SPEED / math.sqrt(1 + _slp * _slp), VL_SPEED * (tonumber(_G.JAF_PaceFloorFrac) or 0))
                        if _vSafeV then
                            local _hv = _vSafeV / math.max(_slp, 0.01)
                            if _hv < _h then _h = _hv; _paceNbN = _paceNbN + 1 end
                        end
                        if _h < spd then spd = _h _paceN = _paceN + 1 if _h < _paceMin then _paceMin = _h end end
                    end end
                if _lbCap ~= math.huge then
                    local _lbHold = (_G.JAF_LbStable or 0.6) * math.min(math.max(_lbStreak, 1), tonumber(_G.JAF_LbStreakHoldMax) or 4)
                    if tick() - _lbCapAt > _lbHold then
                        _lbCap = _lbCap + VL_SPEED * (_G.JAF_LbRamp or 0.5) * dt
                        if _lbCap >= VL_SPEED then _lbCap = math.huge end
                    end
                    if _lbCap ~= math.huge then spd = math.min(spd, _lbCap) end
                end
                if _G.JAF_StableSpeed == true then
                    local _sc2 = LocalPlayer and LocalPlayer.Character
                    local _sa2 = _sc2 and _sc2:GetAttribute("SpeedAllowance")
                    local _boostLive = (type(_sa2) == "number" and _sa2 > 0)
                    _stableSpd = _boostLive
                        and math.max(50, tonumber(_G.JAF_StableSpeedVal) or VL_SPEED)
                        or  math.max(50, tonumber(_G.JAF_StablePostVal) or tonumber(_G.GrapplePostBoostSpeed) or 160)
                    if _stableSpd > VL_SPEED then _stableSpd = VL_SPEED end
                    spd = _stableSpd
                    if _grap then
                        local _gc = JAF_GrappleSpeedCap()
                        if _gc <= (tonumber(_G.GrappleSwapSpeed) or 60) + 1 then spd = math.min(spd, _gc) end
                    end end
                if not _pp and _G.JAF_TurnBlend ~= false and remaining > NEAR_END_DIST and not _npAimRaw then
                    local _rem = _G.JAF_TurnLookahead or 14
                    local _ax, _az = vHrp.Position.X, vHrp.Position.Z
                    local _tx, _tz
                    for _li = wpIndex, n do
                        local _w = waypoints[_li] or targetBasePos
                        local _sx, _sz = _w.X - _ax, _w.Z - _az
                        local _sl = math.sqrt(_sx*_sx + _sz*_sz)
                        if _sl >= _rem then local _t = (_sl > 0.001) and (_rem / _sl) or 0 _tx, _tz = _ax + _sx*_t, _az + _sz*_t break end
                        _rem = _rem - _sl
                        _ax, _az = _w.X, _w.Z
                    end
                    if not _tx then _tx, _tz = targetBasePos.X, targetBasePos.Z end
                    local _cut, _cm = false, (_G.JAF_TurnClearMargin or 6)
                    local _px0, _pz0 = vHrp.Position.X, vHrp.Position.Z
                    for _, _f in ipairs(_G.JAF_BaseFootprints or {}) do
                        if _f.minX then
                            local _inNow   = _px0 >= _f.minX-_cm and _px0 <= _f.maxX+_cm and _pz0 >= _f.minZ-_cm and _pz0 <= _f.maxZ+_cm
                            local _entryOk = (_f.plot == _live.dst) and remaining <= (NEAR_END_DIST + 20)
                            if not _inNow and not _entryOk
                               and _segBox(_px0, _pz0, _tx, _tz, _f.minX-_cm, _f.minZ-_cm, _f.maxX+_cm, _f.maxZ+_cm) then
                                _cut = true; break
                            end end end
                    if not _cut and _G.JAF_FlightObstacles then
                        local _ax, _az = vHrp.Position.X, vHrp.Position.Z
                        local _bx, _bz = _tx - _ax, _tz - _az
                        local _seg2 = _bx*_bx + _bz*_bz
                        for _, _o in ipairs(_G.JAF_FlightObstacles) do
                            local _tt = (_seg2 > 0.001) and (((_o.x-_ax)*_bx + (_o.z-_az)*_bz) / _seg2) or 0
                            if _tt < 0 then _tt = 0 elseif _tt > 1 then _tt = 1 end
                            local _px, _pz = _ax + _bx*_tt - _o.x, _az + _bz*_tt - _o.z
                            local _rr = (_o.strict or _G.JAF_GapSmooth == false) and _o.r or (_o.cr or _o.r)
                            if _px*_px + _pz*_pz < _rr*_rr then _cut = true; break end
                        end end
                    if not _cut then
                        local _ddx, _ddz = _tx - vHrp.Position.X, _tz - vHrp.Position.Z
                        local _ddm = math.sqrt(_ddx*_ddx + _ddz*_ddz)
                        if _ddm > 2 then dir = Vector3.new(_ddx/_ddm, 0, _ddz/_ddm) end
                    end end
                do
                    local _scDt = math.max(dt, tonumber(_G.JAF_StepClampDt) or (1/240))
                    if _G.JAF_StepDtAware == true and _dtRecent > _scDt then
                        _scDt = math.min(_dtRecent, _scDt * (tonumber(_G.JAF_StepDtAwareMax) or 3))
                    end
                    spd = math.min(spd, remaining / _scDt)
                end
                if _G.JAF_ClimbGovern == true and yVel > (tonumber(_G.JAF_VertSafeMax) or 88) then
                    local _gf = (tonumber(_G.JAF_VertSafeMax) or 88) / yVel
                    yVel = yVel * _gf
                    spd  = spd * _gf
                end
                local _xtGate = (_G.JAF_XtCornerGate == false) and 8 or 5
                if _pp and _G.JAF_PpCrossTrack ~= false and spd > 1 and dir.Magnitude > 0.5
                   and not _npAimRaw and remaining > NEAR_END_DIST and _ppL > _xtGate then
                    local _xtT = tick()
                    if (_xtT - (tonumber(_G.JAF_XtStamp) or 0)) > 1 then _G.JAF_XtMax, _G.JAF_XtTilt, _G.JAF_XtActN = 0, 0, 0 end
                    _G.JAF_XtStamp = _xtT
                    local _sB = _projectS(vHrp.Position.X, vHrp.Position.Z, 0, nil)
                    if math.abs(_sB - _routeS) > (tonumber(_G.JAF_XtFootWindow) or 24) then
                        _G.JAF_XtRejN = (tonumber(_G.JAF_XtRejN) or 0) + 1
                    else
                    local _qx, _qz = _pointAt(_sB)
                    local _ox, _oz = _qx - vHrp.Position.X, _qz - vHrp.Position.Z
                    local _eT = math.sqrt(_ox * _ox + _oz * _oz)
                    if _eT > (tonumber(_G.JAF_XtMax) or 0) then _G.JAF_XtMax = math.floor(_eT * 10) / 10 end
                    if _eT > (tonumber(_G.JAF_XtDead) or 0.05) then
                        local _ux, _uz = _ox / _eT, _oz / _eT
                        local _have  = spd * (dir.X * _ux + dir.Z * _uz)
                        local _want  = _eT / math.max(tonumber(_G.JAF_XtTau) or 0.08, dt)
                        local _extra = _want - _have
                        if _extra > 0 then
                            local _al = _ox * dir.X + _oz * dir.Z
                            local _lx, _lz = _ox - dir.X * _al, _oz - dir.Z * _al
                            local _lm = math.sqrt(_lx * _lx + _lz * _lz)
                            if _lm > 0.02 then
                                local _eff = math.max(_lm / _eT, 0.3)
                                local _sn  = _extra / (spd * _eff)
                                local _cap = math.clamp(tonumber(_G.JAF_XtMaxSin) or 0.25, 0, 0.5)
                                local _capD = (tonumber(_G.JAF_XtMaxDelta) or math.huge) / math.max(spd, 1)
                                if _capD < _cap then _cap = _capD end
                                if _sn > _cap then _sn = _cap end
                                local _cs = math.sqrt(1 - _sn * _sn)
                                local _nx, _nz = _lx / _lm, _lz / _lm
                                local _dx2, _dz2 = dir.X * _cs + _nx * _sn, dir.Z * _cs + _nz * _sn
                                local _dm2 = math.sqrt(_dx2 * _dx2 + _dz2 * _dz2)
                                if _dm2 > 0.01 then
                                    _dx2, _dz2 = _dx2 / _dm2, _dz2 / _dm2
                                    local _tx2, _tz2
                                    if _G.JAF_VtxTangent ~= false and n >= 3 then _tx2, _tz2 = _segDirAt(_sB) end
                                    if not _tx2 then
                                        local _at = _arcTotal()
                                        local _fx, _fz = _pointAt(math.min(_sB + 3, _at))
                                        local _bx, _bz = _pointAt(math.max(_sB - 3, 0))
                                        _tx2, _tz2 = _fx - _bx, _fz - _bz
                                    end
                                    local _tm2 = math.sqrt(_tx2 * _tx2 + _tz2 * _tz2)
                                    local _okX = true
                                    if _tm2 > 0.01 then
                                        _tx2, _tz2 = _tx2 / _tm2, _tz2 / _tm2
                                        local _fN = _dx2 * _tx2 + _dz2 * _tz2
                                        local _fO = dir.X * _tx2 + dir.Z * _tz2
                                        _okX = (_fN >= 0.2) or (_fN >= _fO)
                                    end
                                    if _okX then
                                        dir = Vector3.new(_dx2, 0, _dz2)
                                        if _sn > (tonumber(_G.JAF_XtTilt) or 0) then _G.JAF_XtTilt = math.floor(_sn * 100) / 100 end
                                        _G.JAF_XtActN = (tonumber(_G.JAF_XtActN) or 0) + 1
                                    end end end end end end end
                if ((_f1FrontDrive and _G.JAF_F1FrontTaper ~= false) or (isSecondFloor and _frontHeadOn and _G.JAF_F2FrontTaper ~= false)
                    or ((not isSecondFloor) and (sideChosen or _sideApproach) and _G.JAF_SideTaper ~= false)
                    or (isSecondFloor and (sideChosen or _sideApproach) and _G.JAF_F2SideTaper ~= false)
                    or (_skipClone and (not sideChosen) and (not _sideApproach) and _G.JAF_OpenBaseTaper ~= false)
                    or (_frontHeadOn and _G.JAF_FrontTaper == true)) and _G.JAF_F1ApproachTaper ~= false and remaining <= NEAR_END_DIST then
                    local _fdx = targetBasePos.X - vHrp.Position.X
                    local _fdz = targetBasePos.Z - vHrp.Position.Z
                    local _fRem = math.sqrt(_fdx * _fdx + _fdz * _fdz)
                    if _fRem < (tonumber(_G.JAF_F1TaperDist) or 20) then
                        local _fCap = math.max(_fRem * (tonumber(_G.JAF_F1TaperK) or 20), tonumber(_G.JAF_F1TaperMin) or 30)
                        if spd > _fCap then spd = _fCap end
                    end end
                if _G.JAF_SpdSlew == true then
                    if _spdPrev then
                        local _acc = (tonumber(_G.JAF_SpdAccel) or 250) * dt
                        if spd > _spdPrev + _acc then spd = _spdPrev + _acc _G.JAF_SpdSlewN = (tonumber(_G.JAF_SpdSlewN) or 0) + 1 end
                    end end
                if _G.JAF_TakeoffRamp ~= false then
                    if _tkT0 == 0 then _tkT0 = tick() end
                    local _trT = math.max(tonumber(_G.JAF_TakeoffRampTime) or 0.25, 0)
                    if _trT > 0 then
                        local _el = tick() - _tkT0
                        if _el < _trT then
                            local _from = math.max(tonumber(_G.JAF_TakeoffRampFrom) or 160, 0)
                            if spd > _from then local _cap = _from + (spd - _from) * (_el / _trT) if spd > _cap then spd = _cap end end
                        end end end
                _spdPrev = spd
                do
                    local _vs = tonumber(_G.JAF_VertSlew) or 2600
                    if _vs > 0 and _yvPrev then
                        local _step = _vs * math.max(dt, 1 / 240)
                        local _dy = yVel - _yvPrev
                        if _dy > _step then
                            yVel = _yvPrev + _step
                            _yvSlewN = _yvSlewN + 1
                        elseif _dy < -_step then
                            yVel = _yvPrev - _step
                            _yvSlewN = _yvSlewN + 1
                        end end
                    _yvPrev = yVel
                end
                if (tonumber(_G.JAF_MaxFrameStep) or 20) > 0 then
                    local _ms = tonumber(_G.JAF_MaxFrameStep) or 20
                    local _dtR = math.max(dt, 1 / 240)
                    if _G.JAF_StepHitchArm == true and _dtRecent > _dtR then _dtR = _dtRecent end
                    if _dtR > 0.02 then
                        local _stepCap = _ms / _dtR
                        if _G.JAF_StepWpClamp ~= false and type(suffix) == "table" and n and n >= 2
                           and tonumber(_routeS) then
                            local _swTot = suffix[1] or 0
                            local _swCorner = (_G.JAF_StepWpCornerOnly ~= false)
                            local _swDot = tonumber(_G.JAF_WpChaseTurnDot) or 0.985
                            for _wi = 2, n do
                                local _swArc = _swTot - (suffix[_wi] or 0)
                                if _swArc > _routeS + 0.5 then
                                    local _swIsC = true
                                    if _swCorner then
                                        local _sa, _sb, _sc = waypoints[_wi - 1], waypoints[_wi], waypoints[_wi + 1]
                                        if _sa and _sb and _sc then
                                            local _s1x, _s1z = _sb.X - _sa.X, _sb.Z - _sa.Z
                                            local _s2x, _s2z = _sc.X - _sb.X, _sc.Z - _sb.Z
                                            local _sm1 = math.sqrt(_s1x * _s1x + _s1z * _s1z)
                                            local _sm2 = math.sqrt(_s2x * _s2x + _s2z * _s2z)
                                            if _sm1 > 0.1 and _sm2 > 0.1
                                               and ((_s1x * _s2x + _s1z * _s2z) / (_sm1 * _sm2)) > _swDot then _swIsC = false end
                                        end end
                                    if _swIsC then
                                    local _swTo = _swArc - _routeS
                                    local _swFloor = _swCorner and (tonumber(_G.JAF_StepWpLead) or tonumber(_G.JAF_WpChaseLead) or 6)
                                                     or (tonumber(_G.JAF_StepWpMin) or 4)
                                    if _swTo < _swFloor then _swTo = _swFloor end
                                    local _swCap = _swTo / _dtR
                                    if _swCap < _stepCap then _stepCap = _swCap end
                                    break
                                    end
                                end end end
                        if spd > _stepCap then spd = _stepCap end
                    end end
                if _G.JAF_CornerGov ~= false and _pp and n >= 3 and remaining > NEAR_END_DIST
                   and type(suffix) == "table" and tonumber(_routeS) then
                    local _cgDt  = math.max(dt, 1 / 240)
                    local _cgDiv = tonumber(_G.JAF_CornerGovDiv) or 6
                    local _cgWin = math.max(spd * _cgDt * (tonumber(_G.JAF_CornerGovWin) or 1.5),
                                            tonumber(_G.JAF_CornerGovWinMin) or 10)
                    local _cgTot = suffix[1] or 0
                    for _ci = 2, n - 1 do
                        local _cArc = _cgTot - (suffix[_ci] or 0)
                        if _cArc > _routeS then
                            if (_cArc - _routeS) <= _cgWin then
                                local _ca, _cb, _cc = waypoints[_ci - 1], waypoints[_ci], waypoints[_ci + 1]
                                if _ca and _cb and _cc then
                                    local _c1x, _c1z = _cb.X - _ca.X, _cb.Z - _ca.Z
                                    local _c2x, _c2z = _cc.X - _cb.X, _cc.Z - _cb.Z
                                    local _cm1 = math.sqrt(_c1x * _c1x + _c1z * _c1z)
                                    local _cm2 = math.sqrt(_c2x * _c2x + _c2z * _c2z)
                                    if _cm1 > 0.1 and _cm2 > 0.1 then
                                        local _cdot = math.clamp((_c1x * _c2x + _c1z * _c2z) / (_cm1 * _cm2), -1, 1)
                                        local _chs  = math.sqrt(math.max((1 - _cdot) * 0.5, 0))
                                        if _chs > 0.02 then
                                            local _cv = _cgDiv / (_cgDt * 2 * _chs)
                                            local _cfl = tonumber(_G.JAF_CornerGovFloor) or 200
                                            if _cv < _cfl then _cv = _cfl end
                                            if _cv < spd then
                                                spd = _cv
                                                _G.JAF_CornerGovN = (tonumber(_G.JAF_CornerGovN) or 0) + 1
                                            end end end end end
                            break
                        end end end
                if _G.JAF_ComboBudget ~= false then
                    local _bud = tonumber(_G.JAF_ComboBudgetMax) or VL_SPEED
                    if _bud and _bud > 0 then
                        local _vy2 = yVel * yVel
                        if (spd * spd + _vy2) > (_bud * _bud) then
                            local _room = (_bud * _bud) - _vy2
                            local _hMax = math.max((_room > 0) and math.sqrt(_room) or 0, tonumber(_G.JAF_ComboMinH) or 60)
                            if spd > _hMax then spd = _hMax end
                        end end end
                if remaining > NEAR_END_DIST then
                    if spd < _minSpd then _minSpd = spd end
                    if spd > _maxSpd then _maxSpd = spd end
                else
                    if spd < _neMin then _neMin = spd end
                    if spd > _neMax then _neMax = spd end
                end
                if _G.JAF_SpdPrevLate ~= false then _spdPrev = spd end
                vHrp.AssemblyLinearVelocity = Vector3.new(dir.X * spd, yVel, dir.Z * spd)
            end)
            local vlStart = tick()
            local _vlCap = tonumber(_G.JAF_TpMaxTime) or math.max(6, 3 + (suffix[1] or 0) / 80)
            repeat RunService.Heartbeat:Wait()
            until velDone or tick()-vlStart > _vlCap or not alive()
            if not velDone then _exitWhy = alive() and "timecap" or "died" end
            vlConn:Disconnect()
            if _grap then JAF_GrappleStop() end
            if lockHum and lockHum.Parent then lockHum.AutoRotate = true end
            if _exitWhy == "offmap" or _exitWhy == "revoked" then
                local _oc2, _oh2 = getChar(); if _oh2 then _oh2.AssemblyLinearVelocity = Vector3.zero; _oh2.AssemblyAngularVelocity = Vector3.zero end
                isTpMoving = false; return
            end
            c, hrp = getChar()
            if hrp and alive() then
                hrp.AssemblyLinearVelocity, hrp.AssemblyAngularVelocity = Vector3.zero, Vector3.zero
                if _G.JAF_SnapTeleport == true and finalY and targetBasePos then
                    local _landY = math.max(finalY, targetBasePos.Y)
                    local _xz = _G.JAF_M2(hrp.Position.X, hrp.Position.Z, targetBasePos.X, targetBasePos.Z)
                    if _xz <= (tonumber(_G.JAF_SnapDescentXZTol) or 12)
                       and hrp.Position.Y > _landY + (tonumber(_G.JAF_SnapDescentMinDrop) or 3) then
                        local _rot = _faceRot or (hrp.CFrame - hrp.CFrame.Position)
                        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hrp.CFrame = CFrame.new(hrp.Position.X, _landY, hrp.Position.Z) * _rot end)
                    end end
                if _faceRot and _G.JAF_UprightSnap ~= false then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hrp.CFrame = CFrame.new(hrp.Position) * _faceRot end) end
                pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
local _, _, vHum = getChar() if vHum then vHum:ChangeState(Enum.HumanoidStateType.GettingUp) end end)
            end end end
    if not alive() then isTpMoving=false; return end
    if _G.JAF_LaserLockRecheck ~= false and signPart and signPart.Parent then
        local _lz = signPart.Parent:FindFirstChild("LaserHitbox")
        _lz = _lz and (_lz:FindFirstChild("Main") or _lz:FindFirstChildWhichIsA("BasePart"))
        if _skipClone and _lz and _lz.CanCollide then
            _skipClone = false
        elseif (not _skipClone) and _G.JAF_SkipClonePromote ~= false and (not isSecondFloor)
           and (not sideChosen) and (not _sideApproach)
           and _G.JAF_SkipCloneUnlockedF1 ~= false then
            local _okP, _ulP = pcall(isPlotUnlocked, signPart.Parent.Name, true)
            if _okP and _ulP == true then _skipClone = true end
        end end
    local _intendedCF
    local _snapLand = _G.JAF_SnapDescent ~= false
    local _f1FrontSideMethod = (_G.JAF_F1FrontAnchor ~= false) and (_G.JAF_SideRiseDrop ~= false)
                           and not _skipClone and not _sideApproach and not isSecondFloor
                           and (_G.JAF_F1FrontSideMethod ~= false)
    local _cloneDelayCfg = (_f1FrontSideMethod
                            and tonumber(Config.TpSettings.F1FrontCloneDelay))
                           or tonumber(Config.TpSettings.CloneDelay) or 0.15
    local _f1DwellSpent = 0
    local _slSnapped, _slSnapY = false, nil
    local _sepBottomY
    local _plotDesc
    if (not _snapLand) and _G.JAF_SideRiseDrop ~= false and signPart and signPart.Parent then
        _plotDesc = signPart.Parent:GetDescendants()
        ;(function() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
        for _, _p in ipairs(_plotDesc) do
            if _p:IsA("BasePart") and _p.CanCollide then
                local _cf, _s = _p.CFrame, _p.Size
                local _ex = math.abs(_cf.RightVector.X) * _s.X + math.abs(_cf.UpVector.X) * _s.Y + math.abs(_cf.LookVector.X) * _s.Z
                local _ey = math.abs(_cf.RightVector.Y) * _s.X + math.abs(_cf.UpVector.Y) * _s.Y + math.abs(_cf.LookVector.Y) * _s.Z
                local _ez = math.abs(_cf.RightVector.Z) * _s.X + math.abs(_cf.UpVector.Z) * _s.Y + math.abs(_cf.LookVector.Z) * _s.Z
                if _ey <= 4 and math.max(_ex, _ez) >= 30 and math.min(_ex, _ez) >= 12 then
                    local _bot = _p.Position.Y - _ey * 0.5
                    if _bot > targetBasePos.Y + 2 and (not _sepBottomY or _bot < _sepBottomY) then _sepBottomY = _bot end
                end end end end)()
        if signPart:IsA("BasePart") and signPart.CanCollide then
            local _cf, _s = signPart.CFrame, signPart.Size
            local _ey = math.abs(_cf.RightVector.Y) * _s.X + math.abs(_cf.UpVector.Y) * _s.Y + math.abs(_cf.LookVector.Y) * _s.Z
            local _bot = signPart.Position.Y - _ey * 0.5
            if _bot > targetBasePos.Y + 2 and (not _sepBottomY or _bot < _sepBottomY) then _sepBottomY = _bot end
        end end
    do
        c, hrp = getChar()
        if hrp and alive() then
            local _finalCF = _faceCF
            if _snapLand and not _skipClone then
                local _hyN = hrp.Position.Y
                if isSecondFloor and _sideApproach and _G.JAF_F2SideCloseIn ~= false and _G.JAF_F2SideCloseInArm == true and _tpFinalY then
                    local _dropInY = _tpFinalY + (tonumber(_G.JAF_F2SideDropIn) or 4)
                    if _hyN > _dropInY then _hyN = _dropInY end
                end
                if _hyN > _faceCF.Y then _finalCF = CFrame.new(Vector3.new(_faceCF.X, _hyN, _faceCF.Z)) * _faceRot end
            elseif _sepBottomY and not _skipClone and not _f1FrontSideMethod
               and not (isSecondFloor and not _sideApproach and _G.JAF_F2FrontNoRise ~= false) then
                local _halfY = (targetBasePos.Y + (_sepBottomY - 1)) * 0.5
                if _halfY > _faceCF.Y then _finalCF = CFrame.new(Vector3.new(_faceCF.X, _halfY, _faceCF.Z)) * _faceRot end
            end
            _intendedCF = _finalCF
            equipCarpet()
            c, hrp = getChar()
            if hrp and alive() then
                if _sideApproach and _G.JAF_SideKeepXZ == true then
                    local _kd = Vector2.new(hrp.Position.X - _finalCF.X, hrp.Position.Z - _finalCF.Z).Magnitude
                    if _kd <= (tonumber(_G.JAF_SideKeepMax) or 12) then
                        _finalCF = CFrame.new(Vector3.new(hrp.Position.X, _finalCF.Y, hrp.Position.Z)) * _faceRot
                        _intendedCF = _finalCF
                    end end
                local _tbk = tonumber(_G.JAF_F1TweenBack) or 0.6
                local _f1TweenIn = _f1FrontSideMethod and _G.JAF_F1TweenIn == true
                    and Vector2.new(targetBasePos.X - hrp.Position.X, targetBasePos.Z - hrp.Position.Z).Magnitude > (_tbk + 0.2)
                if _f1TweenIn then
                    local _outV = -_faceCF.LookVector
                    _finalCF    = CFrame.new(Vector3.new(targetBasePos.X + _outV.X * _tbk, hrp.Position.Y, targetBasePos.Z + _outV.Z * _tbk)) * _faceRot
                    _intendedCF = _finalCF
                end
                hrp.AssemblyLinearVelocity, hrp.AssemblyAngularVelocity = Vector3.zero, Vector3.zero
                local _gd = (hrp.Position - targetBasePos).Magnitude
                local _flatGap = _G.JAF_M2(hrp.Position.X, hrp.Position.Z, targetBasePos.X, targetBasePos.Z)
                local _sxeF1Skip = _f1FrontSideMethod and _G.JAF_SxeF1Front ~= false and _G.JAF_SideRiseDrop ~= false
                local _gapTol = _sxeF1Skip and (tonumber(_G.JAF_LaserApproachMax) or 30)
                               or ((_tpShortHop and _G.JAF_ShortHopGapArm == true) and (tonumber(_G.JAF_ShortHopAbortGap) or 24) or 12)
                if _flatGap > _gapTol and not _skipClone then
                    if hrp and hrp.Parent then hrp.AssemblyLinearVelocity = Vector3.zero; hrp.AssemblyAngularVelocity = Vector3.zero end
                    isTpMoving = false; return
                end
                if not _skipClone and (not _sxeF1Skip or _f1TweenIn) and _G.JAF_LandTween ~= false then
                    local _snapOK = true
                    if _G.JAF_LockSanity ~= false and targetBasePos and _finalCF then
                        local _snO = Vector2.new(_finalCF.X - targetBasePos.X, _finalCF.Z - targetBasePos.Z).Magnitude
                        if _snO > (tonumber(_G.JAF_LandSnapMaxOff) or 20) then _snapOK = false end
                    end
                    if _snapOK and not isSecondFloor and _G.JAF_LandSnap ~= false and hrp and hrp.Parent then
                        hrp.AssemblyLinearVelocity, hrp.AssemblyAngularVelocity = Vector3.zero, Vector3.zero
                        hrp.CFrame                  = _finalCF
                        hrp.AssemblyLinearVelocity, hrp.AssemblyAngularVelocity = Vector3.zero, Vector3.zero
                    elseif _snapOK then
                        local _legs
                        if _G.JAF_LandTweenAvoid ~= false and hrp and hrp.Parent then
                            pcall(function() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
                                local _fp = _finalCF.Position
                                local _rp2 = RaycastParams.new()
                                _rp2.FilterType = Enum.RaycastFilterType.Exclude
                                _rp2.RespectCanCollide = true
                                local _ex2 = { c }
                                local _cl2 = Workspace:FindFirstChild(tostring(LocalPlayer.UserId) .. "_Clone")
                                if _cl2 then _ex2[#_ex2 + 1] = _cl2 end
                                for _, _tp2 in ipairs(Workspace:GetChildren()) do
                                    if _tp2.Name == "XiTempPlatform" then _ex2[#_ex2 + 1] = _tp2 end
                                end
                                _rp2.FilterDescendantsInstances = _ex2
                                local _d0 = _fp - hrp.Position
                                if _d0.Magnitude > 2 and Workspace:Raycast(hrp.Position, _d0, _rp2) then
                                    local _sY = math.max(hrp.Position.Y, _fp.Y)
                                    for _ = 1, 3 do
                                        local _a2 = Vector3.new(hrp.Position.X, _sY, hrp.Position.Z)
                                        local _b2 = Vector3.new(_fp.X, _sY, _fp.Z)
                                        local _h2 = ((_b2 - _a2).Magnitude > 0.5) and Workspace:Raycast(_a2, _b2 - _a2, _rp2) or nil
                                        if not _h2 then break end
                                        local _i2 = _h2.Instance
                                        local _cf2, _s2 = _i2.CFrame, _i2.Size
                                        local _ey2 = math.abs(_cf2.RightVector.Y) * _s2.X
                                                   + math.abs(_cf2.UpVector.Y)    * _s2.Y
                                                   + math.abs(_cf2.LookVector.Y)  * _s2.Z
                                        _sY = math.max(_sY + 3, _i2.Position.Y + _ey2 * 0.5 + (tonumber(_G.JAF_LandLegClear) or 3))
                                    end
                                    _legs = { Vector3.new(hrp.Position.X, _sY, hrp.Position.Z), Vector3.new(_fp.X, _sY, _fp.Z) }
                                end end) end
                        local _lts = tonumber(_G.JAF_LandTweenSpeed) or 130
                        local _rotL = _finalCF - _finalCF.Position
                        local _legTween; _legTween = function(_toCF) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
                            local _, _h3 = getChar()
                            if not (_h3 and alive()) then return false end
                            local _dl = (_toCF.Position - _h3.Position).Magnitude
                            if _dl < 0.5 then return true end
                            local _tw3 = TweenService:Create(_h3,
                                TweenInfo.new(math.clamp(_dl / _lts, 0.03, 0.6), Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
                                {CFrame = _toCF})
                            _tw3:Play()
                            _tw3.Completed:Wait()
                            return true
                        end
                        if _legs then
                            if _legTween(CFrame.new(_legs[1]) * _rotL)
                               and _legTween(CFrame.new(_legs[2]) * _rotL) then
                                _legTween(_finalCF)
                            end
                        else
                            local _gt = math.clamp(_gd / _lts, 0.03, 0.6)
                            local _gTween = TweenService:Create(hrp,
                                TweenInfo.new(_gt, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
                                {CFrame = _finalCF})
                            _gTween:Play()
                            _gTween.Completed:Wait()
                        end
                        local _, _h4 = getChar()
                        if _h4 then _h4.AssemblyLinearVelocity, _h4.AssemblyAngularVelocity = Vector3.zero, Vector3.zero end
                    end end end end
        if not alive() then isTpMoving=false; return end
        c, hrp = getChar()
    end
    if not alive() then isTpMoving=false; return end
    do
        c, hrp = getChar()
        if not hrp then isTpMoving=false; return end
        local flatPos = Vector3.new(hrp.Position.X, 0, hrp.Position.Z)
        local flatTarget = (_G.JAF_LandGateIntended ~= false and _intendedCF) and Vector3.new(_intendedCF.X, 0, _intendedCF.Z) or Vector3.new(targetBasePos.X, 0, targetBasePos.Z)
        local _sxeF1NoTween = _f1FrontSideMethod and _G.JAF_SxeF1Front ~= false
        if (flatPos - flatTarget).Magnitude > 5 and not _skipClone and not _sxeF1NoTween then
            if hrp and hrp.Parent then hrp.AssemblyLinearVelocity = Vector3.zero; hrp.AssemblyAngularVelocity = Vector3.zero end
            isTpMoving = false; return
        end end
    if _skipClone then
        if _G.JAF_SkipCloneGapGate ~= false then
            c, hrp = getChar()
            if hrp then
                local _scgX = hrp.Position.X - targetBasePos.X
                local _scgZ = hrp.Position.Z - targetBasePos.Z
                local _scgGap = math.sqrt(_scgX * _scgX + _scgZ * _scgZ)
                if _scgGap > (tonumber(_G.JAF_SkipCloneMaxGap) or 45) then
                    hrp.AssemblyLinearVelocity, hrp.AssemblyAngularVelocity = Vector3.zero, Vector3.zero
                    _wasStealingHit = false
                    isTpMoving = false
                    return
                end end end
        equipCarpet()
        _jafStolenSwap()
        local _blC2, _blH2, _blHum2 = getChar()
        local _blStand2 = (_blHum2 and _blH2 and (_blHum2.HipHeight + _blH2.Size.Y * 0.5)) or 4.1
        local _lift  = tonumber(_G.JAF_BrainrotLift) or (0.5 + math.max(0, _blStand2 - 4.1))
        local _underF2 = _G.JAF_F2UnderActive(exactPos.Y)
        local _snapY = _underF2 and (exactPos.Y - (tonumber(_G.JAF_F2UnderDrop) or 8)) or (exactPos.Y + 2.5 + _lift)
        local _snap  = Vector3.new(exactPos.X, _snapY, exactPos.Z)
        if _underF2 and _G.JAF_CreateF3Platform then
            pcall(_G.JAF_CreateF3Platform, _snap - Vector3.new(0, 5, 0))
            RunService.Heartbeat:Wait()
        end
        c, hrp = getChar()
        if hrp and alive() then
            hrp.AssemblyLinearVelocity, hrp.AssemblyAngularVelocity = Vector3.zero, Vector3.zero
            local _scDelay = tonumber(_G.JAF_SkipCloneDelay) or 0
            if _scDelay > 0 then task.wait(_scDelay) end
            for _att = 1, 2 do
                if not alive() then break end
                c, hrp = getChar(); if not hrp then break end
                hrp.AssemblyLinearVelocity, hrp.AssemblyAngularVelocity = Vector3.zero, Vector3.zero
                local _rlDeadEnd = false
                if _G.JAF_SkipCloneRelay ~= false and not _G.JAF_BrainrotSimpleOn() and hrp then
                    pcall(function() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
                        local _rlDir = _snap - hrp.Position
                        local _rlLen = _rlDir.Magnitude
                        if _rlLen > 12 then
                            local _rlP = RaycastParams.new()
                            _rlP.FilterType = Enum.RaycastFilterType.Exclude
                            _rlP.RespectCanCollide = true
                            local _rlEx = {}
                            for _, _rlPl in ipairs(game.Players:GetPlayers()) do
                                if _rlPl.Character then _rlEx[#_rlEx + 1] = _rlPl.Character end
                            end
                            local _rlCl = Workspace:FindFirstChild(tostring(LocalPlayer.UserId) .. "_Clone")
                            if _rlCl then _rlEx[#_rlEx + 1] = _rlCl end
                            if part and part.Parent then
                                _rlEx[#_rlEx + 1] = part.Parent
                                if part.Parent.Parent then _rlEx[#_rlEx + 1] = part.Parent.Parent end
                            end
                            for _, _rlCh in ipairs(Workspace:GetChildren()) do
                                if _rlCh.Name == "XiTempPlatform" then _rlEx[#_rlEx + 1] = _rlCh end
                            end
                            _rlP.FilterDescendantsInstances = _rlEx
                            local _rlHit = Workspace:Raycast(hrp.Position, _rlDir * ((_rlLen - 3.5) / _rlLen), _rlP)
                            if _rlHit then
                                local _rlRel  = Vector3.new(targetBasePos.X, _snap.Y, targetBasePos.Z)
                                local _rlDir2 = _rlRel - hrp.Position
                                local _rlLen2 = _rlDir2.Magnitude
                                local _rlHit2 = (_rlLen2 > 4)
                                    and Workspace:Raycast(hrp.Position, _rlDir2 * ((_rlLen2 - 2) / _rlLen2), _rlP) or nil
                                if _rlHit2 then _rlDeadEnd = true return end
                                _G.JAF_TweenHrpBrainrot(hrp, _rlRel, _faceCF and _faceCF.Rotation or nil)
                                c, hrp = getChar()
                            end end end)
                    if _rlDeadEnd then
                        if hrp then hrp.AssemblyLinearVelocity = Vector3.zero; hrp.AssemblyAngularVelocity = Vector3.zero end
                        _wasStealingHit = false
                        isTpMoving = false
                        return
                    end
                    if not (hrp and alive()) then break end
                end
                _G.JAF_TweenHrpBrainrot(hrp, _snap, _faceCF and _faceCF.Rotation or nil)
                c, hrp = getChar()
                if hrp then hrp.AssemblyLinearVelocity = Vector3.zero end
                if hrp and (hrp.Position - _snap).Magnitude <= 3 then break end
                RunService.Heartbeat:Wait()
            end end
        if _G.JAF_SkipCloneReachCheck ~= false then
            local _, _fh = getChar()
            local _fd = _fh and (_fh.Position - _snap).Magnitude or 999
            if _fd > 6 then _wasStealingHit = false isTpMoving = false return end
        end
        _wasStealingHit = false
        isTpMoving = false
        return
    end
    local _restoreAutoRotate
    do
        local function _abort(msg) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _restoreAutoRotate then _restoreAutoRotate() end isTpMoving = false end
        local function _qcRem() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if not _G.JAF_LastQCTpFire then return 0 end return math.max(0, 5 - (tick() - _G.JAF_LastQCTpFire)) end
        local _lhConn, _lhRelease
        local _jafArmLandHold; _jafArmLandHold = function(_lockCF) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
            if _lhConn then return end
            local _eps     = tonumber(_G.JAF_LandHoldEps) or 0.75
            local _snapMax = tonumber(_G.JAF_LandHoldSnapMax) or 8
            local _walkV   = tonumber(_G.JAF_LandHoldWalkVel) or tonumber(_G.JAF_LbEaseSpeed) or 175
            local _tMax    = (tonumber(_G.JAF_LandHoldMax) or 6) + _qcRem() + _cloneDelayCfg
            local _t0, _lockPos, _lockRot = tick(), _lockCF.Position, _lockCF - _lockCF.Position
            local _lhN, _lhWorst, _lhWasOff, _lhHp = 0, 0, false, 0
            local _lhTok, _lhGen = {}, _G.JAF_TpGen
            _G.JAF_LandHoldTok, _G.JAF_LandHoldYankAt = _lhTok, nil
            _lhRelease = function(_why) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
                if _lhConn then _lhConn:Disconnect(); _lhConn = nil end
                local _lhMine = (_G.JAF_LandHoldTok == _lhTok)
                if _lhMine then _G.JAF_LandHoldTok = nil; _G.JAF_LandHoldGate = nil; _G.JAF_LandHoldRelease = nil end
                if _lhMine and _why ~= "place" then
                    local _, _rh = getChar()
                    if _rh and _rh.Anchored and not _G.JAF_ClonePreAnchored then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_rh.Anchored = false end) end
                elseif _lhMine and _why == "place" and _G.JAF_ClonePreAnchorHrp == nil
                   and not _G.JAF_CloneSelfAnchored then
                    local _, _rh = getChar()
                    if _rh and _rh.Anchored then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_rh.Anchored = false end) end
                end end
            _lhConn = RunService.Heartbeat:Connect(function(_lhDt) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
                if not isTpMoving or _G.JAF_CancelTp or _G.JAF_LandHoldTok ~= _lhTok
                   or _G.JAF_TpGen ~= _lhGen
                   or tick() - _t0 > _tMax then _lhRelease("done") return end
                local _, _hh = getChar()
                if not _hh then _lhRelease("dead") return end
                if _G.JAF_LandHoldCombatBail ~= false then
                    local _hum2 = _hh.Parent and _hh.Parent:FindFirstChildOfClass("Humanoid")
                    local _hpNow = _hum2 and _hum2.Health
                    if _hpNow then
                        if _hpNow > _lhHp then _lhHp = _hpNow end
                        if _hpNow < _lhHp - (tonumber(_G.JAF_LandHoldHurtEps) or 1.5) then
                            _lhRelease("combat")
                            if _G.JAF_ClonePreAnchored then _jafReleasePreAnchor() end
                            return
                        end end end
                local _d = (_hh.Position - _lockPos).Magnitude
                if _d <= _eps then _lhWasOff = false return end
                _G.JAF_LandHoldYankAt = tick()
                if not _lhWasOff then _lhWasOff = true; _lhN = _lhN + 1 end
                if _lhN > (tonumber(_G.JAF_LandHoldMaxFights) or 2) then
                    _lhRelease("fights")
                    if _G.JAF_ClonePreAnchored then _jafReleasePreAnchor() end
                    return
                end
                if _d > _lhWorst then _lhWorst = _d end
                local _tp
                if _d <= _snapMax then _tp = _lockPos
                else _tp = _hh.Position + (_lockPos - _hh.Position).Unit * math.min(_d, _walkV * math.min(_lhDt, 0.1)) end
                _P(_hh, "CFrame", CFrame.new(_tp) * _lockRot,
                       "AssemblyLinearVelocity", Vector3.zero, "AssemblyAngularVelocity", Vector3.zero,
                       "Anchored", true)
            end)
            _G.JAF_LandHoldRelease = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_lhRelease("place") end
            _G.JAF_LandHoldGate = function() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
                local _calm = tonumber(_G.JAF_LandHoldCalm) or 0.3
                local _gMax = tonumber(_G.JAF_LandHoldGateMax) or 1.5
                local _g0 = tick()
                while tick() - _g0 < _gMax do
                    local _, _gh = getChar()
                    local _ya = _G.JAF_LandHoldYankAt
                    if _gh and (_gh.Position - _lockPos).Magnitude <= _eps and (not _ya or tick() - _ya >= _calm) then return true end
                    RunService.Heartbeat:Wait()
                end
                _lhRelease("gatefail")
                return false
            end end
        local _tpLandY = nil
        do local _, _lh = getChar(); if _lh then _tpLandY = _lh.Position.Y end end
        do
            local _, _, _aHum = getChar()
            if _aHum then
                _aHum.AutoRotate = false
                local _restored = false
                _restoreAutoRotate = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    if _restored then return end
                    _restored = true
                    if _aHum and _aHum.Parent then _aHum.AutoRotate = true end
                end
                task.delay(3, _restoreAutoRotate)
            end end
        pcall(equipCarpet); RunService.Heartbeat:Wait()
        local _f2NoDrop = isSecondFloor and (not _sideApproach) and (_G.JAF_F2NoDrop ~= false)
        local _riseDropLand = (_G.JAF_SideRiseDrop ~= false)
        local _dropRan = false
        if _riseDropLand then
            if _G.JAF_DropGate ~= false and _intendedCF then
                local _gTol = tonumber(_G.JAF_DropGateXZ) or 5
                local _gGap; _gGap = (function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    local _, _gh0 = getChar()
                    if not _gh0 then return 0 end
                    return Vector2.new(_gh0.Position.X - _intendedCF.X, _gh0.Position.Z - _intendedCF.Z).Magnitude
                end)
                if _gGap() > _gTol then
                    local _g0 = tick()
                    while tick() - _g0 < (tonumber(_G.JAF_DropGateMax) or 2.5) do
                        if _G.JAF_CancelTp then break end
                        local _, _gh = getChar(); if not _gh then break end
                        local _dx, _dz = _intendedCF.X - _gh.Position.X, _intendedCF.Z - _gh.Position.Z
                        local _dm = math.sqrt(_dx * _dx + _dz * _dz)
                        if _dm <= _gTol then break end
                        local _gs = math.min(tonumber(_G.JAF_DropGateSpeed) or 120, _dm * 6)
                        _gh.AssemblyLinearVelocity = Vector3.new((_dx / _dm) * _gs, 0, (_dz / _dm) * _gs)
                        RunService.Heartbeat:Wait()
                    end
                    do local _, _gz0 = getChar(); if _gz0 then _gz0.AssemblyLinearVelocity = Vector3.zero end end
                    if not _G.JAF_CancelTp and _gGap() > (tonumber(_G.JAF_DropGateAbort) or 12) then
                        if _G.JAF_AbortDropsCarpet ~= false then
                            local _, _, _abH2 = getChar()
                            if _abH2 then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_abH2:UnequipTools() end) end
                        end
                        return _abort()
                    end end end
            local _sepY = _sepBottomY
            local _lndXZ do local _, _lh0 = getChar(); if _lh0 then _lndXZ = Vector2.new(_lh0.Position.X, _lh0.Position.Z) end end
            if _sepY and not _snapLand
               and not (isSecondFloor and not _sideApproach and _G.JAF_F2FrontNoRise ~= false)
               and not (_f1FrontSideMethod and _G.JAF_F1FrontNoRise ~= false) then
                local _rVel   = tonumber(_G.JAF_SideRiseVel) or 150
                local _target = _sepY - (tonumber(_G.JAF_SepClear) or 3)
                local _rStart = tick()
                while tick() - _rStart < (tonumber(_G.JAF_SideRiseMax) or 1.5) do
                    if _G.JAF_CancelTp then break end
                    local _rc, _rh = getChar(); if not _rh then break end
                    local _head = _rc and _rc:FindFirstChild("Head")
                    local _hy   = (_head and (_head.Position.Y + _head.Size.Y * 0.5)) or (_rh.Position.Y + 2.2)
                    if _hy >= _target then break end
                    local _rRamp = math.clamp((tick() - _rStart) / math.max(tonumber(_G.JAF_LandRampTime) or 0.1, 1e-4), 0, 1)
                local _pinXZ = (not _sideApproach) or _G.JAF_SidePinXZ == true
                    local _rv = _rh.AssemblyLinearVelocity
                    local _rSpd = math.min(_rVel, math.max((_target - _hy) * 18, 25))
                    local _rx, _rz = _rv.X, _rv.Z
                    if _pinXZ then _rx, _rz = 0, 0
                    elseif _lndXZ and _G.JAF_SideRecenter ~= false then
                        _rx = math.clamp((_lndXZ.X - _rh.Position.X) * 10, -18, 18)
                        _rz = math.clamp((_lndXZ.Y - _rh.Position.Z) * 10, -18, 18)
                    end
                    _rh.AssemblyLinearVelocity = Vector3.new(_rx, _rSpd * _rRamp, _rz)
                    RunService.Heartbeat:Wait()
                end
                local _, _rz = getChar(); if _rz then _rz.AssemblyLinearVelocity = Vector3.zero end
            end
            local _sVel  = tonumber(_G.JAF_SideSettleVel) or 35
            if isSecondFloor and not _sideApproach then _sVel = tonumber(_G.JAF_F2FrontDropVel) or 150 end
            if _sideApproach then _sVel = tonumber(_G.JAF_SideDropVel) or 150 end
            if not isSecondFloor and not _sideApproach then _sVel = tonumber(_G.JAF_F1FrontDropVel) or 150 end
            if _snapLand and not _f1FrontSideMethod then
                _sVel = tonumber(_G.JAF_SnapDropVel) or 95
                if isSecondFloor then _sVel = tonumber(_G.JAF_SnapDropVelF2) or 150 end
            end
            local _sRate = tonumber(_G.JAF_DropSettleRate) or 8
            local _sNeed = tonumber(_G.JAF_DropSettleFrames) or 3
            local _tapRp
            local _ig2 = {}
            if _G.JAF_DropTaper ~= false then _tapRp = RaycastParams.new(); _tapRp.FilterType = Enum.RaycastFilterType.Exclude end
            local _snapSkipDrop = false
            if _snapLand and _G.JAF_SnapNoDropWhenClose ~= false and _G.JAF_SnapSkipDropArm == true and _tpFinalY
               and ((not _f1FrontSideMethod) or _G.JAF_F1FrontSnapNoDrop == true) then
                local _, _shG = getChar()
                if _shG and (_shG.Position.Y - _tpFinalY) <= (tonumber(_G.JAF_SnapDropMinGap) or 3) then _snapSkipDrop = true end
            end
            local _f1FreezeOn = _f1FrontSideMethod and _G.JAF_F1AnimFreeze ~= false
            local _f1Freeze; _f1Freeze = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if not _f1FreezeOn then return end
                pcall(function() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
                    local _fc = LocalPlayer.Character
                    local _fh = _fc and _fc:FindFirstChildOfClass("Humanoid")
                    local _fa = _fh and _fh:FindFirstChildOfClass("Animator")
                    if _fa then for _, _tr in ipairs(_fa:GetPlayingAnimationTracks()) do _tr:Stop(0) end end
                end) end
            local _f1FastLand = false
            if _f1FrontSideMethod and _G.JAF_F1FastLand ~= false and not _G.JAF_CancelTp then
                local _, _flH, _flHum = getChar()
                if _flH and _flHum then
                    local _flTop
                    do
                        local _flRp = RaycastParams.new()
                        _flRp.FilterType = Enum.RaycastFilterType.Exclude
                        local _flIg = { LocalPlayer.Character }
                        local _flFrom = Vector3.new(targetBasePos.X, _flH.Position.Y + 2, targetBasePos.Z)
                        for _ = 1, 5 do
                            _flRp.FilterDescendantsInstances = _flIg
                            local _flHit = Workspace:Raycast(_flFrom, Vector3.new(0, -90, 0), _flRp)
                            if not _flHit then break end
                            if _flHit.Instance.CanCollide then _flTop = _flHit.Position.Y; break end
                            table.insert(_flIg, _flHit.Instance)
                        end end
                    if _flTop then
                        local _flStandY = _flTop + _flHum.HipHeight + _flH.Size.Y * 0.5 + (tonumber(_G.JAF_F1FastLandClear) or 0.1)
                        local _flDy  = _flH.Position.Y - _flStandY
                        local _flGap = Vector2.new(targetBasePos.X - _flH.Position.X, targetBasePos.Z - _flH.Position.Z).Magnitude
                        if _flGap <= (tonumber(_G.JAF_F1AlignMax) or 3)
                           and _flDy <= (tonumber(_G.JAF_F1FastLandDown) or 8)
                           and _flDy >= -(tonumber(_G.JAF_F1FastLandUp) or 1.5) then
                            _f1Freeze()
                            _flH.AssemblyLinearVelocity, _flH.AssemblyAngularVelocity = Vector3.zero, Vector3.zero
                            _flH.CFrame = CFrame.new(Vector3.new(targetBasePos.X, _flStandY, targetBasePos.Z))
                                          * (_faceRot or (_flH.CFrame - _flH.CFrame.Position))
                            _flH.AssemblyLinearVelocity, _flH.AssemblyAngularVelocity = Vector3.zero, Vector3.zero
                            _f1FastLand   = true
                            _snapSkipDrop = true
                        end end end end
            local _sStart, _sPrevY, _sStill, _sSettled, _sDt = tick(), nil, 0, false, 1/60
            if _snapSkipDrop then _sSettled = true end
            while not _snapSkipDrop and tick() - _sStart < (tonumber(_G.JAF_SideSettleMax) or 2.2) do
                if _G.JAF_CancelTp then break end
                local _, _sh = getChar(); if not _sh then break end
                _f1Freeze()
                local _y = _sh.Position.Y
                local _restAnchorY = _intendedCF and _intendedCF.Y
                if _snapLand and _restAnchorY and targetBasePos then _restAnchorY = math.min(_restAnchorY, targetBasePos.Y) end
                local _restOk = (_restAnchorY == nil) or (_y <= _restAnchorY + (tonumber(_G.JAF_RestMaxAbove) or 4)
                                                      and _y >= _restAnchorY - (tonumber(_G.JAF_RestMaxBelow) or 10))
                local _sBlind = (_f1FrontSideMethod and _G.JAF_F1FastSettle ~= false) and 0.05 or 0.1
                if _G.JAF_FastSettleAll ~= false then
                    local _fsB = math.max((tonumber(_G.JAF_LandRampTime) or 0.1) + 0.01, 0.05)
                    if _fsB < _sBlind then _sBlind = _fsB end
                end
                if _sPrevY and (tick() - _sStart) > _sBlind and _restOk
                   and (math.abs(_sPrevY - _y) / math.max(_sDt, 1/240)) < _sRate then
                    _sStill = _sStill + 1
                    if _sStill >= _sNeed then _sSettled = true; break end
                else _sStill = 0 end
                _sPrevY = _y
                local _dRamp = math.clamp((tick() - _sStart) / math.max(tonumber(_G.JAF_LandRampTime) or 0.1, 1e-4), 0, 1)
                local _pinXZ = (not _sideApproach) or _G.JAF_SidePinXZ == true
                local _dv = _sh.AssemblyLinearVelocity
                local _dx2, _dz2 = _dv.X, _dv.Z
                if _pinXZ then _dx2, _dz2 = 0, 0
                    if _f1FrontSideMethod and _G.JAF_F1DropSweep ~= false then
                        local _swx = targetBasePos.X - _sh.Position.X
                        local _swz = targetBasePos.Z - _sh.Position.Z
                        local _swd = math.sqrt(_swx * _swx + _swz * _swz)
                        local _swRem = _swd - (_f1FreezeOn and (tonumber(_G.JAF_F1SweepBackFrozen) or 0.15)
                                               or (tonumber(_G.JAF_F1SweepBack) or 1.2))
                        if _swRem > 0.05 and _swd > 0.01 then
                            local _swv = math.min(tonumber(_G.JAF_F1SweepVel) or 25, _swRem * 12)
                            _dx2, _dz2 = (_swx / _swd) * _swv, (_swz / _swd) * _swv
                        end end
                elseif _lndXZ and _G.JAF_SideRecenter ~= false then
                    _dx2 = math.clamp((_lndXZ.X - _sh.Position.X) * 10, -18, 18)
                    _dz2 = math.clamp((_lndXZ.Y - _sh.Position.Z) * 10, -18, 18)
                end
                local _effV = _sVel
                if _tapRp then
                    table.clear(_ig2)
                    _ig2[1] = LocalPlayer.Character
                    for _ = 1, 4 do
                        _tapRp.FilterDescendantsInstances = _ig2
                        local _hitF = Workspace:Raycast(_sh.Position, Vector3.new(0, -90, 0), _tapRp)
                        if not _hitF then break end
                        if _hitF.Instance.CanCollide then
                            local _dRest = _sh.Position.Y - (_hitF.Position.Y + (tonumber(_G.JAF_DropRestH) or 2.8))
                            _effV = math.min(_sVel, math.max(_dRest * (tonumber(_G.JAF_DropTaperK) or 12), 20))
                            if _G.JAF_DropHitchClamp ~= false and _dRest > 0 then
                                _effV = math.min(_effV, _dRest / math.max(_sDt, 1/240) + 5)
                            end
                            break
                        end
                        table.insert(_ig2, _hitF.Instance)
                    end end
                _sh.AssemblyLinearVelocity = Vector3.new(_dx2, -_effV * _dRamp, _dz2)
                _sDt = RunService.Heartbeat:Wait()
            end
            local _, _zz2 = getChar()
            if _zz2 then _zz2.AssemblyLinearVelocity = Vector3.zero; _zz2.AssemblyAngularVelocity = Vector3.zero end
            if _sSettled and _G.JAF_GroundConfirm ~= false then
                local _gOK = false
                local _g0 = tick()
                local _gDropped = false
                local _f1ReEquip = _f1FrontSideMethod and (_G.JAF_F1GroundDropCarpet ~= false)
                local _gDropOK = (_G.JAF_GroundDropCarpet ~= false) and ((not _f1FrontSideMethod) or _f1ReEquip)
                local _gPinXZ = (not _sideApproach) and (_G.JAF_F1GroundPinXZ ~= false)
                local _gPinN, _gPinMax = 0, 0
                repeat
                    local _gc, _gHrp, _gh = getChar()
                    _f1Freeze()
                    if _gh and _gh.FloorMaterial ~= Enum.Material.Air then _gOK = true break end
                    if _gDropOK and (tick() - _g0) > (_f1ReEquip and (tonumber(_G.JAF_F1GroundDropAfter) or 0.1) or (tonumber(_G.JAF_GroundDropAfter) or 0.25)) then
                        if not _gDropped and _gh then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_gh:UnequipTools() end); _gDropped = true end
                        local _gdv = (tonumber(_G.JAF_GroundDropVel) or 35)
                        if _gHrp then _gHrp.AssemblyLinearVelocity = Vector3.new(0, -_gdv, 0) end
                    elseif _gPinXZ and _gHrp then
                        local _gv = _gHrp.AssemblyLinearVelocity
                        local _gm = math.sqrt(_gv.X * _gv.X + _gv.Z * _gv.Z)
                        if _gm > 0.5 then _gPinN = _gPinN + 1; if _gm > _gPinMax then _gPinMax = _gm end end
                        _gHrp.AssemblyLinearVelocity  = Vector3.new(0, _gv.Y, 0)
                        _gHrp.AssemblyAngularVelocity = Vector3.zero
                    end
                    if _G.JAF_CancelTp then break end
                    RunService.Heartbeat:Wait()
                until tick() - _g0 > (tonumber(_G.JAF_GroundWait) or 0.8)
                if not _gOK then _sSettled = false end
                if _gDropped and _f1ReEquip and _gOK and not _G.JAF_CancelTp then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
equipCarpet() end) end
            end
            if not _sSettled and not _G.JAF_CancelTp and _G.JAF_SettleTimeoutAbort ~= false then
                return _abort("abort: never settled onto the floor (settle timeout)")
            end
            local _f2FrontLand = isSecondFloor and not _sideApproach
            if _sSettled and _G.JAF_LandNudge ~= false and (not _sideApproach or _G.JAF_SideNudge == true)
               and not (_f2FrontLand and _G.JAF_F2FrontNudge ~= true)
               and not (_f1FrontSideMethod and _G.JAF_F1FrontNudge ~= true) then
                local _, _nh = getChar()
                if _nh then
                    local _noffNow; _noffNow = function(_h) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                        return _G.JAF_M2(_h.Position.X, _h.Position.Z, targetBasePos.X, targetBasePos.Z)
                    end
                    local _noff   = _noffNow(_nh)
                    local _minOff = _sideApproach and (tonumber(_G.JAF_LandNudgeMinSide) or 1.0) or (tonumber(_G.JAF_LandNudgeMin) or 0.25)
                    if _noff > _minOff and _noff <= (tonumber(_G.JAF_LandNudgeMax) or 8) then
                        local _nspd = tonumber(_G.JAF_LandNudgeSpeed) or 4
                        local _nt0, _nPrev, _nStuck, _nDt = tick(), _noff, 0, 1/60
                        while tick() - _nt0 < (tonumber(_G.JAF_LandNudgeTime) or 0.6) do
                            if _G.JAF_CancelTp then break end
                            local _, _wh = getChar(); if not _wh then break end
                            local _d = _noffNow(_wh)
                            if _d <= _minOff then break end
                            if (_nPrev - _d) / math.max(_nDt, 1/240) < 1 then
                                _nStuck = _nStuck + 1
                                if _nStuck >= 3 then break end
                            else _nStuck = 0 end
                            _nPrev = _d
                            local _dir = Vector2.new(targetBasePos.X - _wh.Position.X, targetBasePos.Z - _wh.Position.Z).Unit
                            _wh.AssemblyLinearVelocity = Vector3.new(_dir.X * _nspd, _wh.AssemblyLinearVelocity.Y, _dir.Y * _nspd)
                            _nDt = RunService.Heartbeat:Wait()
                        end
                        local _, _nz = getChar()
                        if _nz then _nz.AssemblyLinearVelocity = Vector3.zero; _nz.AssemblyAngularVelocity = Vector3.zero end
                    end end end
            if _f1FrontSideMethod and _sSettled then
                do
                    local _, _agH = getChar()
                    if _agH then
                        local _agap = Vector2.new(targetBasePos.X - _agH.Position.X, targetBasePos.Z - _agH.Position.Z).Magnitude
                        if _agap > (tonumber(_G.JAF_F1AlignMax) or 3) then
                            return _abort(string.format("abort: settled %.1f studs off the laser spot (align envelope %.1f)", _agap, tonumber(_G.JAF_F1AlignMax) or 3))
                        end end end
                local _dwY0 do local _, _dyH = getChar(); if _dyH then _dwY0 = _dyH.Position.Y end end
                local _dwPing = _G.JAF_Ping()
                local _dw = (tonumber(_G.JAF_F1SpotDwell) or 0.15) + _dwPing
                local _glT = tonumber(_G.JAF_F1AlignGlide) or 0.18
                if _G.JAF_F1SnapAlign ~= false then _glT = 0 end
                local _gx0, _gz0
                do local _, _sh0 = getChar(); if _sh0 then _gx0, _gz0 = _sh0.Position.X, _sh0.Position.Z end end
                local _dwGap
                if _gx0 then
                    local _gdx, _gdz = targetBasePos.X - _gx0, targetBasePos.Z - _gz0
                    _dwGap = math.sqrt(_gdx * _gdx + _gdz * _gdz)
                end
                if _dwGap and _glT > 0 and _G.JAF_F1AlignGlideScale ~= false then
                    _glT = math.min(_glT, _dwGap / (tonumber(_G.JAF_F1AlignGlideDiv) or 16))
                end
                if _G.JAF_F1DwellSlider ~= false then
                    _dw = tonumber(_G.JAF_CloneAnchorWait) or _cloneDelayCfg
                    if _G.JAF_F1DwellRepFloor ~= false and _G.JAF_PingAdapt == true then
                        _dw = math.max(_dw, _dwPing + (tonumber(_G.JAF_F1DwellRepMin) or 0.1))
                    end
                    if _G.JAF_F1DwellFloor == true
                       and (_f1FastLand or (_dwGap or 99) > (tonumber(_G.JAF_F1DwellFlushGap) or 0.35)) then
                        _dw = math.max(_dw, (tonumber(_G.JAF_F1SpotDwell) or 0.15) + _dwPing)
                    end
                elseif _dwGap and _f1FreezeOn and _G.JAF_F1DwellScale ~= false and not _f1FastLand
                   and _dwGap <= (tonumber(_G.JAF_F1DwellFlushGap) or 0.35) then
                    _dw = _dwPing + (tonumber(_G.JAF_F1SpotDwellMin) or 0.04)
                end
                local _f1Ragged; _f1Ragged = function() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
                    local _rc = LocalPlayer.Character
                    if not _rc then return false end
                    local _rok, _rr = pcall(_arv2_anyACDisabled, _rc)
                    if _rok and _rr then return true end
                    local _rh = _rc:FindFirstChildOfClass("Humanoid")
                    if _rh then
                        local _sok, _st = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _rh:GetState() end)
                        if _sok and (_st == Enum.HumanoidStateType.Physics or _st == Enum.HumanoidStateType.FallingDown) then return true end
                    end
                    return false
                end
                local _ragWaited = 0
                if _G.JAF_F1RagWaitOn ~= false and _f1Ragged() then
                    local _ragT0 = tick()
                    while tick() - _ragT0 < (tonumber(_G.JAF_F1RagWait) or 0.4) do
                        if _G.JAF_CancelTp then break end
                        local _, _rgH = getChar(); if not _rgH then break end
                        _rgH.AssemblyLinearVelocity  = Vector3.new(0, _rgH.AssemblyLinearVelocity.Y, 0)
                        _rgH.AssemblyAngularVelocity = Vector3.zero
                        if not _f1Ragged() then break end
                        RunService.Heartbeat:Wait()
                    end
                    _ragWaited = tick() - _ragT0
                end
                local _dw0 = tick()
                _dw = _dw + _glT
                if _G.JAF_CarpetDuringWait == false and _G.JAF_F1DwellPreEquip ~= false and _G.JAF_ClonePreEquipInDwell ~= false and not _G.JAF_CancelTp then
                    local _peNeed = math.clamp(_dwPing * 1.5, 1/60, tonumber(_G.JAF_FastEquipMaxWait) or 0.12)
                    if _dw >= _peNeed then
                        local _peC, _, _peHum = getChar()
                        local _peCl = (_peC and _peC:FindFirstChild("Quantum Cloner")) or LocalPlayer.Backpack:FindFirstChild("Quantum Cloner")
                        if _peHum and _peCl and _peCl.Parent ~= _peC then
                            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_peHum:EquipTool(_peCl) end)
                            if _peCl.Parent ~= _peC then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_peCl.Parent = _peC end) end
                        end
                    end
                end
                local _upB = _f1FastLand and 0 or (tonumber(_G.JAF_F1AlignUp) or 0.6)
                local _jN, _jMax, _jPrev = 0, 0, nil
                repeat
                    if _G.JAF_CancelTp then break end
                    local _, _sh2 = getChar(); if not _sh2 then break end
                    if _jPrev then
                        local _jd = (_sh2.Position - _jPrev).Magnitude
                        if _jd > (tonumber(_G.JAF_F1JitterEps) or 0.15) then _jN = _jN + 1 if _jd > _jMax then _jMax = _jd end end
                    end
                    _f1Freeze()
                    local _tx, _tz = targetBasePos.X, targetBasePos.Z
                    if _gx0 and _glT > 0 then
                        local _a = math.clamp((tick() - _dw0) / _glT, 0, 1)
                        _a = _a * _a * (3 - 2 * _a)
                        _tx = _gx0 + (targetBasePos.X - _gx0) * _a
                        _tz = _gz0 + (targetBasePos.Z - _gz0) * _a
                    end
                    local _dwY = _sh2.Position.Y
                    if _dwY0 and _G.JAF_F1DwellYClamp ~= false
                       and math.abs(_dwY - _dwY0) > (tonumber(_G.JAF_F1DwellYTol) or 4) then
                        _dwY = _dwY0
                    end
                    _sh2.CFrame = CFrame.new(Vector3.new(_tx, _dwY + _upB, _tz)) * (_faceRot or (_sh2.CFrame - _sh2.CFrame.Position))
                    _sh2.AssemblyLinearVelocity  = Vector3.new(0, _sh2.AssemblyLinearVelocity.Y, 0)
                    _sh2.AssemblyAngularVelocity = Vector3.zero
                    _upB = 0
                    _jPrev = _sh2.Position
                    RunService.Heartbeat:Wait()
                until tick() - _dw0 >= _dw
                _f1DwellSpent = tick() - _dw0
                do
                    local _, _cvH = getChar()
                    local _cvOff = _cvH and Vector2.new(_cvH.Position.X - targetBasePos.X, _cvH.Position.Z - targetBasePos.Z).Magnitude or -1
                    local _cvRag = _f1Ragged()
                    if _G.JAF_F1AlignConverge ~= false and not _G.JAF_CancelTp
                       and (_cvRag or (_cvOff >= 0 and _cvOff > (tonumber(_G.JAF_F1AlignTol) or 2.0))) then
                        return _abort(string.format("abort: align never settled -- %.2f studs off%s (jitter %d frames, worst %.2f)",
                            _cvOff, _cvRag and ", STILL RAGDOLLED" or "", _jN, _jMax))
                    end end
                if not _G.JAF_CloneNoAnchor and _G.JAF_FreeFallClone ~= true and not _G.JAF_CancelTp then
                    local _, _aH = getChar()
                    if _aH then
                        _P(_aH, "AssemblyLinearVelocity", Vector3.zero, "AssemblyAngularVelocity", Vector3.zero, "Anchored", true)
                        if _G.JAF_F1LandHold ~= false then _jafArmLandHold(_aH.CFrame) end
                        task.delay((tonumber(_G.JAF_F1AnchorFailsafe) or 3) + _qcRem() + _cloneDelayCfg, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _aH.Parent and _aH.Anchored then _aH.Anchored = false end end)
                        end) end end end
        elseif _G.JAF_DropForce ~= false and not _f2NoDrop then
            _dropRan = true
            do
                local _, _dh0, _dhum0 = getChar()
                if _dhum0 and _G.JAF_DropCarpetForSettle ~= false then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_dhum0:UnequipTools() end) end
            end
            local _dropVel = math.clamp(tonumber(_G.JAF_DropVel) or 35, 12, 120)
            local _eps     = tonumber(_G.JAF_DropSettleEps) or 0.06
            local _needN   = tonumber(_G.JAF_DropSettleFrames) or 3
            local _minT    = tonumber(_G.JAF_DropSettleMinTime) or 0.08
            local _dStart, _prevY, _stillN = tick(), nil, 0
            while tick() - _dStart < (tonumber(_G.JAF_DropMax) or 1.2) do
                if _G.JAF_CancelTp then break end
                local _, _dHrp = getChar(); if not _dHrp then break end
                local _y = _dHrp.Position.Y
                if _prevY and (_prevY - _y) < _eps and (tick() - _dStart) >= _minT then
                    _stillN = _stillN + 1
                    if _stillN >= _needN then break end
                else
                    _stillN = 0
                end
                _prevY = _y
                local _v = _dHrp.AssemblyLinearVelocity
                _dHrp.AssemblyLinearVelocity = Vector3.new(_v.X, -_dropVel, _v.Z)
                RunService.Heartbeat:Wait()
            end end
        if _dropRan then local _, _sh = getChar(); if _sh then _tpLandY = _sh.Position.Y end end
        _G.JAF_ClonePreAnchored  = nil
        _G.JAF_ClonePreAnchorHrp = nil
        _G.JAF_ClonePreAnchorToken = nil
        local _offSpotAbort = false
        do
            local _, _zh = getChar()
            if _zh then _zh.AssemblyLinearVelocity = Vector3.zero; _zh.AssemblyAngularVelocity = Vector3.zero end
            local _atSpot; _atSpot = function(_h) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local _tol = tonumber(_G.JAF_AnchorPosTol) or 12
                return _h and _G.JAF_M2(_h.Position.X, _h.Position.Z, targetBasePos.X, targetBasePos.Z) <= _tol
            end
            local _preOn = _zh and _G.JAF_NoPreAnchor ~= true and not _G.JAF_CloneNoAnchor and _G.JAF_FreeFallClone ~= true
                           and (not _riseDropLand)
            if _preOn and not _atSpot(_zh) then
                _offSpotAbort = true
            elseif _preOn then
                if _G.JAF_AnchorSnapY ~= false and _tpLandY then
                    _zh.CFrame = _zh.CFrame + Vector3.new(0, _tpLandY - _zh.Position.Y, 0)
                    _zh.AssemblyLinearVelocity = Vector3.zero; _zh.AssemblyAngularVelocity = Vector3.zero
                end
                _zh.Anchored = true
                local _paTok = {}
                _P(_G, "JAF_ClonePreAnchorHrp", _zh, "JAF_ClonePreAnchorToken", _paTok, "JAF_ClonePreAnchored", true)
                task.delay(2.5 + _qcRem(), function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    if _G.JAF_ClonePreAnchorToken == _paTok then
                        if _zh and _zh.Parent then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_zh.Anchored = false end) end
                        _G.JAF_ClonePreAnchorHrp = nil; _G.JAF_ClonePreAnchored = nil; _G.JAF_ClonePreAnchorToken = nil
                    end end)
                local _ping = _G.JAF_Ping()
                local _cwBase = tonumber(_G.JAF_CloneAnchorWait) or _cloneDelayCfg
                local _aw = _cwBase + _ping * (tonumber(_G.JAF_AnchorPingFrac) or 0)
                local _lockCF  = _zh.CFrame
                local _lockOK = _jafLockOK(_lockCF, "lockCF")
                local _hammerN = tonumber(_G.JAF_ClonePosHammerFrames) or 5
                if _G.JAF_CarpetDuringWait == false and _G.JAF_ClonePreEquipInDwell ~= false then
                    local _peNeed = math.clamp(_ping * 1.5, 1/60, tonumber(_G.JAF_FastEquipMaxWait) or 0.12)
                    if _aw >= _peNeed then
                        local _peC, _, _peHum = getChar()
                        local _peCl = (_peC and _peC:FindFirstChild("Quantum Cloner")) or LocalPlayer.Backpack:FindFirstChild("Quantum Cloner")
                        if _peHum and _peCl and _peCl.Parent ~= _peC then
                            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_peHum:EquipTool(_peCl) end)
                            if _peCl.Parent ~= _peC then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_peCl.Parent = _peC end) end
                        end
                    end
                end
                local _t = tick(); local _hf = 0
                while tick() - _t < _aw do
                    local _, _hh = getChar()
                    if not _hh then break end
                    if _lockOK and _G.JAF_ClonePosHammer ~= false and _hh.Parent and _hf < _hammerN then
                        _hh.CFrame = _lockCF
                        _hh.AssemblyLinearVelocity = Vector3.zero; _hh.AssemblyAngularVelocity = Vector3.zero
                        _hf = _hf + 1
                    elseif not _atSpot(_hh) then
                        if _hh.Parent then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_hh.Anchored = false end) end
                        _G.JAF_ClonePreAnchorHrp = nil; _G.JAF_ClonePreAnchorToken = nil; _G.JAF_ClonePreAnchored = nil
                        _offSpotAbort = true
                        break
                    end
                    RunService.Heartbeat:Wait()
                end end end
        if _offSpotAbort then
            local _, _oh = getChar()
            local _od = _oh and Vector2.new(_oh.Position.X - targetBasePos.X, _oh.Position.Z - targetBasePos.Z).Magnitude or -1
            return _abort(string.format("abort: off the anchor spot (%.1f studs out)", _od))
        end
        if _G.JAF_CloneDelayPreEquip ~= false and not _G.JAF_ClonePreAnchored and not _G.JAF_CancelTp
           and not _G.JAF_CloneNoAnchor and _G.JAF_FreeFallClone ~= true and _G.JAF_NoPreAnchor ~= true then
            local _cdC, _cdH = getChar()
            if _cdH then
                local _cdWant = tonumber(_G.JAF_CloneAnchorWait) or _cloneDelayCfg
                if _G.JAF_F1DwellNoDouble ~= false and _f1DwellSpent > 0 then _cdWant = math.max(0, _cdWant - _f1DwellSpent) end
                _cdH.AssemblyLinearVelocity, _cdH.AssemblyAngularVelocity = Vector3.zero, Vector3.zero
                _cdH.Anchored = true
                local _cdTok = {}
                _P(_G, "JAF_ClonePreAnchorHrp", _cdH, "JAF_ClonePreAnchorToken", _cdTok, "JAF_ClonePreAnchored", true)
                task.delay(2.5 + _cdWant + _qcRem(), function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_ClonePreAnchorToken == _cdTok then _jafReleasePreAnchor() end end)
                if _G.JAF_CarpetDuringWait == false and _G.JAF_ClonePreEquipInDwell ~= false then
                    local _pePing = _G.JAF_Ping()
                    local _peNeed = math.clamp(_pePing * 1.5, 1/60, tonumber(_G.JAF_FastEquipMaxWait) or 0.12)
                    if _cdWant >= _peNeed then
                        local _peC, _, _peHum = getChar()
                        local _peCl = (_peC and _peC:FindFirstChild("Quantum Cloner")) or LocalPlayer.Backpack:FindFirstChild("Quantum Cloner")
                        if _peHum and _peCl and _peCl.Parent ~= _peC then
                            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_peHum:EquipTool(_peCl) end)
                            if _peCl.Parent ~= _peC then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_peCl.Parent = _peC end) end
                            _cdC = getChar()
                        end
                    end
                end
                local _cdHeldT = _cdC and _cdC:FindFirstChildWhichIsA("Tool")
                if _cdWant > 0 and _cdC and _G.JAF_CloneDelayReEquip ~= false
                   and ((not _cdHeldT) or (_G.JAF_CarpetDuringWait ~= false
                        and _cdHeldT.Name ~= (Config.TpSettings and Config.TpSettings.Tool or "Flying Carpet"))) then
                    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
equipCarpet() end)
                    RunService.Heartbeat:Wait()
                    _cdC = getChar()
                end
                local _cdNoHammer = (isSecondFloor
                    and (not _sideApproach)
                    and _skipClone ~= true and _G.JAF_F2FrontNoHammer ~= false) or false
                local _cdLock = _cdH.CFrame
                local _cdHamOK = _jafLockOK(_cdLock, "cdLock")
                local _cdN    = tonumber(_G.JAF_ClonePosHammerFrames) or 5
                local _cdT0, _cdF = tick(), 0
                local _cdHamOn  = _G.JAF_ClonePosHammer ~= false and not _cdNoHammer and _cdHamOK
                local _cdHamMax = tonumber(_G.JAF_ClonePosHammerMaxT) or 0.12
                if _cdHamOn and _G.JAF_LandHoldAll ~= false then _jafArmLandHold(_cdLock) end
                while (tick() - _cdT0 < _cdWant)
                   or (_cdHamOn and _cdF < _cdN and (tick() - _cdT0) < _cdHamMax) do
                    if _G.JAF_CancelTp then break end
                    local _, _cdW = getChar(); if not _cdW then break end
                    if _cdHamOn and _cdF < _cdN and _cdW.Parent then
                        _P(_cdW, "CFrame", _cdLock, "AssemblyLinearVelocity", Vector3.zero, "AssemblyAngularVelocity", Vector3.zero)
                        _cdF = _cdF + 1
                    end
                    RunService.Heartbeat:Wait()
                end
                if _G.JAF_CancelTp then _jafReleasePreAnchor() return _abort("abort: T-cancel during the clone delay dwell") end
            end end
        do
            local _eqC, _, _eqHum = getChar()
            local _cloner = (_eqC and _eqC:FindFirstChild("Quantum Cloner")) or LocalPlayer.Backpack:FindFirstChild("Quantum Cloner")
            if _G.JAF_CarpetDuringWait == false and _eqHum and _cloner and _cloner.Parent ~= _eqC then
                pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_eqHum:EquipTool(_cloner) end)
                local _es = tick()
                while tick() - _es < (tonumber(_G.JAF_CloneSwapWait) or 0.4) do
                    local _c2 = getChar()
                    if _c2 and _cloner.Parent == _c2 then break end
                    RunService.Heartbeat:Wait()
                end
                local _rw = tonumber(_G.JAF_CloneEquipRegWait)
                if _rw ~= 0 then
                    local _rp = _G.JAF_Ping()
                    local _rt = _rw or math.clamp(_rp * 1.5, 1/60, tonumber(_G.JAF_FastEquipMaxWait) or 0.12)
                    local _r0 = tick()
                    while tick() - _r0 < _rt do RunService.Heartbeat:Wait() end
                end end end end
    do
        c, hrp = getChar()
        if not hrp then _jafReleasePreAnchor(); isTpMoving = false; return end
        local _off = Vector2.new(hrp.Position.X - targetBasePos.X, hrp.Position.Z - targetBasePos.Z).Magnitude
        if _tpArriveOff then local _yank = _off - _tpArriveOff _G.JAF_LandYank = _yank end
        if _off > 15 then
            _jafReleasePreAnchor()
            if _restoreAutoRotate then _restoreAutoRotate() end
            isTpMoving = false
            return
        end end
    if _G.JAF_LastQCTpFire and (tick() - _G.JAF_LastQCTpFire) < 5 then
        local _dbRem = 5 - (tick() - _G.JAF_LastQCTpFire)
        local _db0 = tick()
        while tick() - _db0 < _dbRem do if _G.JAF_CancelTp then break end RunService.Heartbeat:Wait() end
        if _G.JAF_CancelTp then
            if _G.JAF_ClonePreAnchorHrp then
                pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G.JAF_ClonePreAnchorHrp.Anchored = false end)
                _G.JAF_ClonePreAnchorHrp = nil; _G.JAF_ClonePreAnchored = nil; _G.JAF_ClonePreAnchorToken = nil
            end
            do local _, _cxh = getChar() if _cxh and _cxh.Anchored then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_cxh.Anchored = false end) end end
            isTpMoving = false
            return
        end end
    do
        local _eqC, _, _eqHum = getChar()
        local _cloner = LocalPlayer.Backpack:FindFirstChild("Quantum Cloner") or (_eqC and _eqC:FindFirstChild("Quantum Cloner"))
        if _eqHum and _cloner and _cloner.Parent ~= _eqC then
            local _held = _eqC and _eqC:FindFirstChildWhichIsA("Tool")
            if _held and _held ~= _cloner then
                pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_eqHum:UnequipTools() end)
                local _dw = tick()
                while tick() - _dw < (tonumber(_G.JAF_CloneEquipDropWait) or 0.25) do
                    local _wc = getChar()
                    if not (_wc and _wc:FindFirstChildWhichIsA("Tool")) then break end
                    RunService.Heartbeat:Wait()
                end
                local _rc, _, _rh = getChar(); _eqC, _eqHum = _rc, _rh
            end
            if _eqHum then
                do
                    local _eqPing = _G.JAF_Ping()
                    local _hpDelay = tonumber(_G.JAF_HighPingEquipDelay) or 0
                    if _hpDelay > 0 and _eqPing >= (tonumber(_G.JAF_HighPingThresh) or 0.12) then task.wait(_hpDelay) end
                end
                if _G.JAF_FastEquip ~= false then
                    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_eqHum:EquipTool(_cloner) end)
                    if _cloner.Parent ~= _eqC then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_cloner.Parent = _eqC end) end
                    local _png = _G.JAF_Ping()
                    local _w = math.clamp(_png * 1.5, 1/60, tonumber(_G.JAF_FastEquipMaxWait) or 0.12)
                    local _t = tick()
                    while tick() - _t < _w do RunService.Heartbeat:Wait() end
                else
                    _eqHum:EquipTool(_cloner)
                    local _es = tick()
                    while tick() - _es < 0.3 do
                        local _c2 = getChar()
                        if _c2 and _cloner.Parent == _c2 then break end
                        RunService.Heartbeat:Wait()
                    end end end end end
    if _G.JAF_CarpetDuringWait ~= false and _G.JAF_CloneEquipReGate ~= false
       and isSecondFloor and (not _sideApproach) and _skipClone ~= true and _G.JAF_F2FrontNoHammer ~= false then
        c, hrp = getChar()
        if not hrp then _jafReleasePreAnchor(); isTpMoving = false; return end
        local _reOff = Vector2.new(hrp.Position.X - targetBasePos.X, hrp.Position.Z - targetBasePos.Z).Magnitude
        if _reOff > 15 or (_slSnapped and _slSnapY and _G.JAF_SnapAllYGateOn ~= false
                           and math.abs(hrp.Position.Y - _slSnapY) > (tonumber(_G.JAF_SnapAllYGate) or 6)) then
            _jafReleasePreAnchor()
            if _restoreAutoRotate then _restoreAutoRotate() end
            isTpMoving = false
            return
        end end
    local _swAnch do local _, _swH = getChar(); _swAnch = _swH and _swH.Anchored end
    if not _G.JAF_ClonePreAnchored and not _swAnch then
        local _ss = tick(); local _still = 0
        while tick() - _ss < (tonumber(_G.JAF_SettleWaitMax) or 1.2) do
            if _G.JAF_CancelTp then break end
            local _, _sHrp = getChar(); if not _sHrp then break end
            if _sHrp.AssemblyLinearVelocity.Magnitude < (tonumber(_G.JAF_SettleWaitEps) or 1.5) then
                _still = _still + 1; if _still >= 2 then break end
            else
                _still = 0
            end
            RunService.Heartbeat:Wait()
        end
        local _, _ch = getChar()
        if _ch then _ch.AssemblyLinearVelocity = Vector3.zero; _ch.AssemblyAngularVelocity = Vector3.zero end
    end
    _G.isCloning = false
    _G.JAF_CloneNoHammer = (isSecondFloor and (not _sideApproach) and _skipClone ~= true and _G.JAF_F2FrontNoHammer ~= false) or nil
    local _f1NC
    if _f1FrontSideMethod and _G.JAF_F1LaserNoClip ~= false then
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local _cs = game:GetService("CollectionService")
            local _best, _bd
            for _, _lp in ipairs(_cs:GetTagged("PlotLaserHitbox")) do
                if _lp:IsA("BasePart") and (_lp:GetAttribute("Floor") == 1 or _lp.Name == "Main") then
                    local _d = (Vector2.new(_lp.Position.X, _lp.Position.Z) - Vector2.new(targetBasePos.X, targetBasePos.Z)).Magnitude
                    if not _bd or _d < _bd then _bd = _d; _best = _lp end
                end end
            if _best and _bd and _bd < 40 then
                _f1NC = { laser = _best, cc = _best.CanCollide }
                _best.CanCollide = false
                task.delay(tonumber(_G.JAF_F1NoClipMax) or 6, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _f1NC and _f1NC.laser and _f1NC.laser.Parent then _f1NC.laser.CanCollide = _f1NC.cc end end)
                end) end end) end
    if _G.JAF_FastPlace ~= false then
        local _apng = _G.JAF_Ping()
        _G.JAF_CloneAnchorOverride = tonumber(_G.JAF_F1AnchorWait) or math.max(0.05, _apng)
    end
    if _G.JAF_F2CloneFloorGuard ~= false and isSecondFloor then
        local _, _fgH = getChar()
        if _fgH and _fgH.Position.Y < targetBasePos.Y - (tonumber(_G.JAF_F2CloneMinBelow) or 12) then
            _G.JAF_CloneAnchorOverride = nil
            _G.JAF_CloneNoHammer = nil
            _jafReleasePreAnchor()
            if _restoreAutoRotate then _restoreAutoRotate() end
            isTpMoving = false
            return
        end end
    instantClone()
    local _clWaitT0 = tick()
    while _G.isCloning and tick() - _clWaitT0 < 5 do RunService.Heartbeat:Wait() end
    if not alive() then isTpMoving=false; return end
    local _cloneInstName = tostring(LocalPlayer.UserId) .. "_Clone"
    local _cloneAppearStart = tick()
    local _cloneInst = nil
    while tick() - _cloneAppearStart < 2 do
        _cloneInst = Workspace:FindFirstChild(_cloneInstName)
        if _cloneInst then break end
        RunService.Heartbeat:Wait()
    end
    if not _cloneInst then _jafReleasePreAnchor() isTpMoving = false return end
    local _cloneSpawnPos, _preSwapPos = _cloneInst:GetPivot().Position, nil
    do
        c, hrp = getChar()
        if hrp then
            local _d = (hrp.Position - _cloneSpawnPos).Magnitude
            if _d > 25 then if _restoreAutoRotate then _restoreAutoRotate() end isTpMoving = false return end
        end
        c, hrp = getChar()
        if hrp then
            local _brXZ     = Vector2.new(exactPos.X, exactPos.Z)
            local _cloneXZ  = Vector2.new(_cloneSpawnPos.X, _cloneSpawnPos.Z)
            local _playerXZ = Vector2.new(hrp.Position.X, hrp.Position.Z)
            local _cloneToBr  = (_cloneXZ  - _brXZ).Magnitude
            local _playerToBr = (_playerXZ - _brXZ).Magnitude
            if _cloneToBr > _playerToBr + (_G.JAF_CloneBehindTol or 10) then
                if _restoreAutoRotate then _restoreAutoRotate() end
                isTpMoving = false
                return
            end end
        local _swPing = _G.JAF_Ping()
        local _swWin  = math.clamp(1.5 + _swPing * (_G.JAF_SwapDetectPingMult or 3), 1.5, _G.JAF_SwapDetectWinMax or 4)
        c, hrp = getChar()
        if not hrp then isTpMoving = false; return end
        _preSwapPos = hrp.Position
        local _gapX, _gapZ = _cloneSpawnPos.X - _preSwapPos.X, _cloneSpawnPos.Z - _preSwapPos.Z
        local _gap = math.sqrt(_gapX * _gapX + _gapZ * _gapZ)
        if _gap < 1.25 then if _restoreAutoRotate then _restoreAutoRotate() end isTpMoving = false return end
        local _uX, _uZ = _gapX / _gap, _gapZ / _gap
        local _need = _gap * (tonumber(_G.JAF_SwapMovedFrac) or 0.5)
        local _pX0, _pZ0 = _preSwapPos.X, _preSwapPos.Z
        local _waitStart, _swapDetected = tick(), false
        local _bestMe, _bestCl = 0, 0
        while tick() - _waitStart < _swWin do
            local _me = 0
            c, hrp = getChar()
            if hrp then _me = (hrp.Position.X - _pX0) * _uX + (hrp.Position.Z - _pZ0) * _uZ end
            local _cl = _gap
            if _cloneInst.Parent then
                local _cp = _cloneInst:GetPivot().Position
                local _lX, _lZ = _cp.X - _cloneSpawnPos.X, _cp.Z - _cloneSpawnPos.Z
                _cl = math.sqrt(_lX * _lX + _lZ * _lZ)
            end
            if _me > _bestMe then _bestMe = _me end
            if _cl > _bestCl then _bestCl = _cl end
            if _me >= _need and _cl >= _need then _swapDetected = true; break end
            if _f1NC then local _, _zH = getChar() if _zH then _zH.AssemblyLinearVelocity = Vector3.zero end end
            RunService.Heartbeat:Wait()
        end
        if not _swapDetected then if _restoreAutoRotate then _restoreAutoRotate() end isTpMoving = false return end
        if _restoreAutoRotate then _restoreAutoRotate() end
        local _commitWait
        if _G.JAF_ClonePingAdaptive == true then
            _commitWait = math.clamp(_swPing * (_G.JAF_SwapCommitMult or 1), _G.JAF_SwapCommitFloor or 0, _G.JAF_SwapCommitMax or 0.6)
        else
            _commitWait = _G.JAF_SwapCommitFloor or 0
        end
        if _commitWait > 0 then task.wait(_commitWait) end
        c, hrp = getChar()
        if hrp then
            local _pXZ = Vector2.new(hrp.Position.X, hrp.Position.Z)
            local _cXZ = Vector2.new(_cloneSpawnPos.X, _cloneSpawnPos.Z)
            local _revTol = _G.JAF_SwapRevertTol or math.clamp(4 + _swPing * 3, 4, 8)
            if (_pXZ - _cXZ).Magnitude > _revTol then isTpMoving = false return end
        end end
    if _G.JAF_StealSwapRearm ~= false and type(_G.JAF_StealPingGateRearm) == "function" then pcall(_G.JAF_StealPingGateRearm) end
    if not alive() then isTpMoving=false; return end
    c, hrp = getChar()
    if not hrp then isTpMoving=false; return end
    equipCarpet()
    if not alive() then isTpMoving=false; return end
    _jafStolenSwap()
    local verticalDiff = exactPos.Y - (hrp and hrp.Position.Y or 0)
    local _blC, _blH, _blHum = getChar()
    local _blStand = (_blHum and _blH and (_blHum.HipHeight + _blH.Size.Y * 0.5)) or 4.1
    local _lift = tonumber(_G.JAF_BrainrotLift) or (0.5 + math.max(0, _blStand - 4.1))
    local isThirdFloor = exactPos.Y > 22
    local isUnderF2 = _G.JAF_F2UnderActive(exactPos.Y)
    local snapY
    if isThirdFloor or isUnderF2 then
        snapY = exactPos.Y - (isUnderF2 and (tonumber(_G.JAF_F2UnderDrop) or 8) or 8)
    else
        snapY = exactPos.Y + 2.5 + _lift
    end
    local snapPos = Vector3.new(exactPos.X, snapY, exactPos.Z)
    if verticalDiff > 2 or isUnderF2 then
        local platPos = snapPos - Vector3.new(0, 5, 0)
        if _G.JAF_CreateF3Platform then pcall(_G.JAF_CreateF3Platform, platPos) end
        RunService.Heartbeat:Wait()
    end
    local _solidBetween; _solidBetween = (function(a, b, _inclPlat) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
        local _rp = RaycastParams.new(); _rp.FilterType = Enum.RaycastFilterType.Exclude
        local _ig = { LocalPlayer.Character }
        local _cl = Workspace:FindFirstChild(tostring(LocalPlayer.UserId) .. "_Clone"); if _cl then table.insert(_ig, _cl) end
        for _, ch in ipairs(Workspace:GetChildren()) do if ch.Name == "XiTempPlatform" and not _inclPlat then table.insert(_ig, ch) end end
        local _d = b - a
        for _ = 1, 6 do
            _rp.FilterDescendantsInstances = _ig
            local h = Workspace:Raycast(a, _d, _rp)
            if not h then return false end
            if h.Instance.CanCollide then return true end
            table.insert(_ig, h.Instance)
        end
        return false
    end)
    local _tweenHrpTo; _tweenHrpTo = (function(_hrp, _dst) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
        if _solidBetween(_hrp.Position, _dst, true) then
            _G.JAF_BrainrotCFrameMove(_hrp, _dst, nil)
        else
            _G.JAF_TweenHrpBrainrot(_hrp, _dst, nil)
        end end)
    local function _fireBrainrotTween() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local _c, _hrp = getChar()
        if not _hrp or not _hrp.Parent then return end
        if _preSwapPos then
            local _gX, _gZ = _cloneSpawnPos.X - _preSwapPos.X, _cloneSpawnPos.Z - _preSwapPos.Z
            local _mX, _mZ = _hrp.Position.X - _preSwapPos.X, _hrp.Position.Z - _preSwapPos.Z
            if (_mX * _mX + _mZ * _mZ) * 9 < (_gX * _gX + _gZ * _gZ) then return end
        end
        do
            local _bd = (_hrp.Position - snapPos).Magnitude
            if _bd > (tonumber(_G.JAF_BrainrotTweenMax) or 45) then
                local _sameBase = false
                for _, f in ipairs(_G.JAF_BaseFootprints or {}) do
                    if f.minX
                       and _hrp.Position.X >= f.minX - 8 and _hrp.Position.X <= f.maxX + 8
                       and _hrp.Position.Z >= f.minZ - 8 and _hrp.Position.Z <= f.maxZ + 8
                       and snapPos.X >= f.minX - 8 and snapPos.X <= f.maxX + 8
                       and snapPos.Z >= f.minZ - 8 and snapPos.Z <= f.maxZ + 8 then
                        _sameBase = true; break
                    end end
                if not _sameBase then return end
            end end
        _hrp.AssemblyLinearVelocity, _hrp.AssemblyAngularVelocity = Vector3.zero, Vector3.zero
        if not _G.JAF_BrainrotSimpleOn()
           and (snapPos.Y - _hrp.Position.Y) > 3 and _solidBetween(_hrp.Position, snapPos) then
            _tweenHrpTo(_hrp, Vector3.new(snapPos.X, _hrp.Position.Y, snapPos.Z))
            local _, _h2 = getChar()
            if _h2 then _tweenHrpTo(_h2, snapPos) end
        else
            _tweenHrpTo(_hrp, snapPos)
        end
        local _, _afterHrp = getChar()
        if _afterHrp then _afterHrp.AssemblyLinearVelocity = Vector3.zero end
    end
    if _G.JAF_InstaSkipPreFire == false or not _G.JAF_BrainrotSimpleOn() then
        local _cc0, _ccMax, _ccOk = tick(), tonumber(_G.JAF_CarpetConfirmMax) or 0.5, false
        repeat
            local _ccChar = getChar()
            if _ccChar then
                for _, _ccT in ipairs(_ccChar:GetChildren()) do
                    if _ccT:IsA("Tool") and _ccT:GetAttribute("IgnoreAntiCheat") then _ccOk = true; break end
                end end
            if _ccOk then break end
            RunService.Heartbeat:Wait()
        until tick() - _cc0 >= _ccMax
        local _settle = tonumber(_G.JAF_BrainrotPreFire); if _settle == nil then _settle = 0.08 end
        local _s0 = tick()
        while tick() - _s0 < _settle do RunService.Heartbeat:Wait() end
        local _pf = math.max(0, math.floor(tonumber(_G.JAF_PostSwapFrameWait) or 0))
        for _ = 1, _pf do RunService.Heartbeat:Wait() end
    end
    if not alive() then isTpMoving=false; return end
    _fireBrainrotTween()
    task.wait(0.05)
    do local _, _retryHrp = getChar() if _retryHrp and (_retryHrp.Position - snapPos).Magnitude > 3 then _fireBrainrotTween() end end
    if _f1NC then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _f1NC.laser and _f1NC.laser.Parent then _f1NC.laser.CanCollide = _f1NC.cc end end) end
    _wasStealingHit = false
    isTpMoving = false
    end end
_SABCAP.tpToBestBrainrot = runAutoSnipe
_G.instantClone = instantClone
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local _lastPre = 0
    while true do
        task.wait(_G.JAF_PreComputePoll or 0.5)
        if _G.JAF_NoPreCompute then continue end
        if isTpMoving then continue end
        if not SharedState.AllAnimalsCache or #SharedState.AllAnimalsCache == 0 then continue end
        local _, hrp = getChar()
        if not hrp then continue end
        local target = manualTarget
        if not target then
            local bestRank, bestGen = math.huge, -math.huge
            for _, a in ipairs(SharedState.AllAnimalsCache) do
                if a and a.owner ~= LocalPlayer.Name and _G.JAF_PassesMinGen(a.genValue) then
                    if Config.AutoTPPriority ~= false then
                        local rank = _G.JAF_PrioScore(a)
                        if rank < bestRank or (rank == bestRank and a.genValue > bestGen) then
                            bestRank = rank; bestGen = a.genValue; target = a
                        end
                    elseif a.genValue > bestGen then
                        bestGen = a.genValue; target = a
                    end end end end
        if not target or target.isWalking then continue end
        local part = findAdorneeGlobal(target)
        if not part then continue end
        do
            local _fc = _frontRouteCache
            if _fc and _fc.uid == target.uid and (tick() - (_fc.at or 0)) < (_G.JAF_RouteCacheMax or 4) then
                local _moved = _fc.startFlat and (Vector2.new(hrp.Position.X, hrp.Position.Z) - _fc.startFlat).Magnitude or 999
                if _moved < (_G.JAF_RouteCacheMove or 20) then continue end
            end end
        if (tick() - _lastPre) < (_G.JAF_RouteRecomputeMin or 4) then continue end
        _lastPre = tick()
        local ok, tbp, wps, used, sp, _, _, sc = pcall(computeFrontRoute, part, hrp.Position)
        if ok and tbp then
            _frontRouteCache = {
                uid = target.uid,
                startFlat = Vector2.new(hrp.Position.X, hrp.Position.Z),
                targetBasePos = tbp,
                waypoints = wps,
                usedPathfinding = used,
                signPart = sp,
                sideChosen = sc,
                f1mode = _G.JAF_F1Mode or (Config.TpSettings and Config.TpSettings.F1SpotMode) or "auto",
                f2under = _G.JAF_F2UnderActive(part.Position.Y),
                at = tick(),
            } end end end)
_SABCAP.JAF_CreateF3Platform = function(position) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
    if not position then return end
    for _, c in ipairs(Workspace:GetChildren()) do if c.Name == "XiTempPlatform" then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
c:Destroy() end) end end
    local plat = Instance.new("Part")
    _P(plat, "Name", "XiTempPlatform", "Size", Vector3.new(3, 1, 3), "Position", position, "Color", Color3.new(1, 0, 0), "Material", Enum.Material.Neon, "Anchored", true, "CanCollide", true, "Transparency", 0.3, "Parent", Workspace)
    task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
task.wait(tonumber(_G.JAF_F3PlatformMaxLife) or 30); while plat.Parent and game.Players.LocalPlayer:GetAttribute("Stealing") == true do task.wait(0.5) end; pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
plat:Destroy() end) end)
    return plat
end
_G.JAF_DestroyAllF3Platforms = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    for _, c in ipairs(Workspace:GetChildren()) do if c.Name == "XiTempPlatform" then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
c:Destroy() end) end end
end
_wasStealingHit = false
local _orphHRP; _orphHRP = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local cam = Workspace.CurrentCamera
    for _, p in ipairs(cam and cam:GetChildren() or {}) do if p:IsA("BasePart") and p.Name == "HumanoidRootPart" then return true end end
    return false
end
local _hbBuildAt; _hbBuildAt = function(prt) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local s = prt and getClosestBaseSign(prt)
    if s then pcall(_G.JAF_HB.build, s, prt.Position) end
end
local _findRetry; _findRetry = function(a) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    for _try = 1, (_G.JAF_RecArmedRetries or 8) do if findAdorneeGlobal(a) then return true end RunService.Heartbeat:Wait() end
end
LocalPlayer:GetAttributeChangedSignal("Stealing"):Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if LocalPlayer:GetAttribute("Stealing") then
        pcall(_G.JAF_DestroyAllF3Platforms)
        local _, hrp2 = getChar()
        if not hrp2 then _wasStealingHit = false; return end
        local tgt2 = manualTarget or SharedState._currentTpTarget
        if tgt2 and tgt2.owner == LocalPlayer.Name then tgt2 = nil end
        if not tgt2 then
            local _aBR, _aBV = math.huge, -math.huge
            local _aUP = (Config.AutoTPPriority ~= false) and type(PRIORITY_LIST) == "table"
            for _, a in ipairs(SharedState.AllAnimalsCache) do
                if a and a.owner ~= LocalPlayer.Name then
                    if _aUP then
                        local _aRk, _aV = _G.JAF_PrioScore(a), tonumber(a.genValue) or 0
                        if _aRk < _aBR or (_aRk == _aBR and _aV > _aBV) then _aBR, _aBV, tgt2 = _aRk, _aV, a end
                    elseif not tgt2 then tgt2 = a end
                end end end
        local part2 = tgt2 and findAdorneeGlobal(tgt2)
        if part2 then
            _hbBuildAt(part2)
            SharedState._stealFloorY = part2.Position.Y
            local _, _, hum2 = getChar()
            local _alive2 = hum2 and hum2.Health > 0 and LocalPlayer.Character == (hrp2.Parent)
            local inBox = _G.JAF_HB.contains(hrp2.Position) == true
            _wasStealingHit = (_alive2 and inBox) or false
            SharedState._armedRecoveryTarget = _wasStealingHit and tgt2 or nil
            SharedState._armedRecoveryPos    = _wasStealingHit and part2.Position or nil
        else
            _wasStealingHit = false
            SharedState._armedRecoveryTarget, SharedState._armedRecoveryPos = nil, nil
        end
        return
    end
    local _afterDrop = false
    local _tok = _G.JAF_DropRecToken
    if _tok then
        _G.JAF_DropRecToken = nil
        if tick() < _tok then
            _afterDrop = true
            local _t0 = tick()
            while type(_G.JAF_DropBrainrotActive) == "function" and _G.JAF_DropBrainrotActive() and tick() - _t0 < (tonumber(_G.JAF_DropRecoverWaitMax) or 8) do
                RunService.Heartbeat:Wait()
            end
            local _sT   = tick()
            local _sMax = tonumber(_G.JAF_DropRecoverSettleMax) or 4
            local _vCap = tonumber(_G.JAF_DropRecoverSettleVel) or 40
            local _spd, _grounded = -1, false
            while tick() - _sT < _sMax do
                local _, _hs, _hums = getChar()
                if not _hs then break end
                _spd = _hs.AssemblyLinearVelocity.Magnitude
                _grounded = (_hums ~= nil) and _hums.FloorMaterial ~= Enum.Material.Air
                if _spd <= _vCap and _grounded then break end
                RunService.Heartbeat:Wait()
            end
            local _, _hz = getChar()
            if _hz then _hz.AssemblyLinearVelocity = Vector3.zero; _hz.AssemblyAngularVelocity = Vector3.zero end
        end end
    if not _wasStealingHit then return end
    _wasStealingHit = false
    local _, _h9, _hum9 = getChar()
    local _orph = _orphHRP()
    if not Config.HitRecovery then return end
    if not SAB_LIVE() then return end
    if isTpMoving then return end
    if _G.JAF_AutoTurretActive and _G.JAF_AutoTurretActive() and _G.JAF_DestroyTurretIfThreat then pcall(_G.JAF_DestroyTurretIfThreat) end
    if _G.JAF_HitRecCooldownUntil and tick() < _G.JAF_HitRecCooldownUntil then return end
    if _G.RecoveryInProgress then return end
    if _G.invisibleStealEnabled then
        if _G.JAF_RecoverDuringInvis == false then return end
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.toggleInvisibleSteal then _G.toggleInvisibleSteal() end end)
        for _ = 1, (tonumber(_G.JAF_RecoverInvisRevertFrames) or 4) do RunService.Heartbeat:Wait() end
    end
    if _orphHRP() then return end
    if _G.JAF_RecLastRun and tick() - _G.JAF_RecLastRun < 1.5 then return end
    _G.JAF_RecLastRun = tick()
    local c, hrp, hum = getChar()
    if not hrp or not hum or hum.Health <= 0 then return end
    if hum:GetState() == Enum.HumanoidStateType.Dead or not hrp.Parent then pcall(_G.JAF_HB.clear); return end
    local target = manualTarget
    if not target and _G.JAF_HitRecFollowBest ~= false then
        local best = _G.JAF_ResolveBest and _G.JAF_ResolveBest()
        if best and best.owner ~= LocalPlayer.Name and _findRetry(best) then target = best end
    end
    local armed = not target and SharedState._armedRecoveryTarget
    if armed and armed.owner ~= LocalPlayer.Name and _findRetry(armed) then target = armed end
    local cur = not target and SharedState._currentTpTarget
    if cur and cur.owner ~= LocalPlayer.Name then
        local _cp = findAdorneeGlobal(cur)
        local _aY = (SharedState._armedRecoveryPos and SharedState._armedRecoveryPos.Y) or SharedState._stealFloorY
        if _cp and (not _aY or math.abs(_cp.Position.Y - _aY) <= (_G.JAF_RecoverFloorBand or 12)) then target = cur end
    end
    if not target then
        local _, _rhrp = getChar()
        local _refY = (SharedState._armedRecoveryPos and SharedState._armedRecoveryPos.Y) or SharedState._stealFloorY
        if _rhrp and _refY then
            local _band, _range = _G.JAF_RecoverFloorBand or 12, (_G.JAF_HB.podiumRange or 75)
            local bestV, _bestRank = -1, math.huge
            local _usePrioRec = (Config.AutoTPPriority ~= false) and type(PRIORITY_LIST) == "table"
            for _, a in ipairs(SharedState.AllAnimalsCache) do
                if a and a.owner ~= LocalPlayer.Name then
                    local prt = findAdorneeGlobal(a)
                    if prt and math.abs(prt.Position.Y - _refY) <= _band and (prt.Position - _rhrp.Position).Magnitude <= _range then
                        local v = tonumber(a.genValue) or 0
                        if _usePrioRec then
                            local _rank = _G.JAF_PrioScore(a)
                            if _rank < _bestRank or (_rank == _bestRank and v > bestV) then _bestRank, bestV, target = _rank, v, a end
                        elseif v > bestV then bestV = v; target = a end
                    end end end end end
    if not target then return end
    local part = findAdorneeGlobal(target)
    _hbBuildAt(part)
    if not part then return end
    local petPos = part.Position
    local _raw = _G.JAF_HB.contains(hrp.Position)
    if _raw ~= true then
        local _dd = (hrp.Position - petPos).Magnitude
        local _cap = tonumber(_G.JAF_DropRecoverMaxDist) or 400
        if not (_afterDrop and _dd <= _cap) then return end
    end
    local MAX = tonumber(_G.JAF_RecMaxRetries) or 3
    if tick() - (_G.JAF_RecWindowStart or 0) > (tonumber(_G.JAF_RecRetryWindow) or 8) then _G.JAF_RecConsecutive = 0; _G.JAF_RecWindowStart = tick() end
    _G.JAF_RecConsecutive = (_G.JAF_RecConsecutive or 0) + 1
    if _G.JAF_RecConsecutive > MAX then pcall(_G.JAF_HB.clear) return end
    _G.JAF_HitRecCooldownUntil = tick() + 1.5
    task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        isTpMoving = true
        _G.JAF_TpGen = (_G.JAF_TpGen or 0) + 1
        local _, _hrH0 = getChar()
        local _hrP0 = _hrH0 and _hrH0.Position
        local snapChar = LocalPlayer.Character
        local snapAlive; snapAlive = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if _G.JAF_CancelTp then return false end
            return LocalPlayer.Character == snapChar and snapChar ~= nil
        end
        equipCarpet()
        task.wait(0.01)
        if target.isWalking then
            local LERP_SPEED, HOVER_HEIGHT = 0.35, 3
            local anchorDone, lastGoodPos, missedFrames = false, nil, 0
            local done = Instance.new("BindableEvent")
            task.delay(tonumber(_G.JAF_WalkRecoverMax) or 10, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if not anchorDone then anchorDone = true; done:Fire() end end)
            local conn = RunService.Heartbeat:Connect(function(dt) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if anchorDone then return end
                if not snapAlive() then anchorDone=true; done:Fire(); return end
                c, hrp = getChar()
                if not hrp then return end
                local currentPart = findAdorneeGlobal(target)
                if not currentPart or not target.model or not target.model.Parent then
                    missedFrames = missedFrames + 1
                    if missedFrames >= 90 then anchorDone=true; done:Fire(); return end
                    if lastGoodPos then
                        hrp.AssemblyLinearVelocity = Vector3.zero
                        hrp.CFrame = hrp.CFrame:Lerp(CFrame.new(lastGoodPos + Vector3.new(0, HOVER_HEIGHT, 0)), LERP_SPEED)
                    end
                    return
                end
                missedFrames = 0
                lastGoodPos = currentPart.Position
                hrp.AssemblyLinearVelocity = Vector3.zero
                hrp.CFrame = hrp.CFrame:Lerp(CFrame.new(lastGoodPos + Vector3.new(0, HOVER_HEIGHT, 0)), LERP_SPEED)
            end)
            done.Event:Wait()
            done:Destroy()
            conn:Disconnect()
            isTpMoving = false
            return
        end
        local isUnderF2 = _G.JAF_F2UnderActive(petPos.Y)
        local snapY = (petPos.Y > 22) and (petPos.Y - 8) or isUnderF2 and (petPos.Y - (tonumber(_G.JAF_F2UnderDrop) or 8)) or (petPos.Y + 2.5)
        local snapPos = Vector3.new(petPos.X, snapY, petPos.Z)
        if (petPos.Y - (hrp and hrp.Position.Y or 0)) > 2 or isUnderF2 then
            if _G.JAF_CreateF3Platform then pcall(_G.JAF_CreateF3Platform, snapPos - Vector3.new(0, 5, 0)) end
            RunService.Heartbeat:Wait()
        end
        equipCarpet(); task.wait(0.01)
        c, hrp = getChar()
        if hrp and snapAlive() then
            local _useMover = _G.JAF_HitRecUseMover ~= false and type(_G.JAF_TweenHrpBrainrot) == "function"
            for _attempt = 1, 2 do
                if not snapAlive() then break end
                c, hrp = getChar(); if not hrp then break end
                hrp.AssemblyLinearVelocity  = Vector3.zero
                hrp.AssemblyAngularVelocity = Vector3.zero
                if _useMover then
                    if type(_G.JAF_GrappleBoostNow) == "function" then pcall(_G.JAF_GrappleBoostNow, snapPos) end
                    equipCarpet()
                    pcall(_G.JAF_TweenHrpBrainrot, hrp, snapPos, nil)
                else
                    local _hrTime = math.clamp((hrp.Position - snapPos).Magnitude / math.max(tonumber(_G.JAF_HitRecSpeed) or 150, 1), 0.05, 2)
                    local _hrTween = TweenService:Create(hrp, TweenInfo.new(_hrTime, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {CFrame = CFrame.new(snapPos)})
                    _hrTween:Play()
                    _hrTween.Completed:Wait()
                end
                c, hrp = getChar(); if not hrp then break end
                hrp.AssemblyLinearVelocity = Vector3.zero
                if (hrp.Position - snapPos).Magnitude <= 8 then break end
                task.wait(0.1)
            end end
        local _, _h8 = getChar()
        local _end = _h8 and _h8.Position
        local _orph8 = _orphHRP()
        pcall(_G.JAF_HB.clear)
        isTpMoving = false
    end) end)
local isAutoStealing = false
local StealData      = setmetatable({}, {__mode = "k"})
local PromptCache    = {}
local PromptIdx      = setmetatable({}, {__mode = "k"})
local PromptIdxDirty = setmetatable({}, {__mode = "k"})
local PromptIdxConn  = setmetatable({}, {__mode = "k"})
local _stealStart    = 0
local _stealProgress = 0
local _holdEpoch     = 0
local _ragUntil; _ragUntil = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    return math.max(tonumber(LocalPlayer:GetAttribute("RagdollEndTime")) or 0, tonumber(_G.JAF_RagdollUntil) or 0)
end
local _ragBlockedNow; _ragBlockedNow = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    return (_ragUntil() - workspace:GetServerTimeNow() + (tonumber(_G.JAF_RagdollLatePad) or 0.1)) > 0
        or (tick() - (tonumber(_G.JAF_RagdollPhysLastT) or 0)) < (tonumber(_G.JAF_RagdollPhysWindow) or 0.4)
end
local _podOf; _podOf = function(uid) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not uid then return nil end
    local pn, sl = tostring(uid):match("^(.*)_([^_]+)$")
    if not (pn and sl) then return nil end
    local pl = Workspace:FindFirstChild("Plots")
    pl = pl and pl:FindFirstChild(pn)
    pl = pl and pl:FindFirstChild("AnimalPodiums")
    return pl and pl:FindFirstChild(sl)
end
local _grabCB; _grabCB = function(p) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local d = { hold = {}, trigger = {} }
    local ok, cs = pcall(getconnections, p.PromptButtonHoldBegan)
    if ok and type(cs) == "table" then
        for _, c in ipairs(cs) do if type(c.Function) == "function" then d.hold[#d.hold + 1] = c.Function end end
    end
    ok, cs = pcall(getconnections, p.Triggered)
    if ok and type(cs) == "table" then
        for _, c in ipairs(cs) do if type(c.Function) == "function" then d.trigger[#d.trigger + 1] = c.Function end end
    end
    return d
end
local _fireTrigger; _fireTrigger = function(p, d) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if fireproximityprompt and _G.JAF_StealUseFPP ~= false then pcall(fireproximityprompt, p); return end
    if d then for _, f in ipairs(d.trigger) do task.spawn(f) end
        if #d.trigger == 0 and fireproximityprompt then pcall(fireproximityprompt, p) end end
end
local _apCache = setmetatable({}, {__mode = "k"})
local _apFromHold; _apFromHold = function(p, d) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local ap = _apCache[p]
    if ap ~= nil then return ap or nil end
    if debug and debug.getupvalues and d then
        for _, f in ipairs(d.hold) do
            local ok, ups = pcall(debug.getupvalues, f)
            if ok then for _, v in pairs(ups) do
                if type(v) == "table" and rawget(v, "State") ~= nil and rawget(v, "ProximityPrompt") ~= nil then
                    _apCache[p] = v
                    return v
                end
            end end
        end
    end
    _apCache[p] = false
    return nil
end
local _fireHoldForced; _fireHoldForced = function(p, d) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if not d or #d.hold == 0 then return false end
    local ap = _apFromHold(p, d)
    if ap and ap.State ~= "Steal" then
        local _os = ap.State
        ap.State = "Steal"
        for _, f in ipairs(d.hold) do pcall(f) end
        ap.State = _os
        return true
    end
    if not ap and p and p.Enabled == false then return false end
    for _, f in ipairs(d.hold) do task.spawn(f) end
    return true
end
local isMyPlot; isMyPlot = function(plotName) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
    local plots = Workspace:FindFirstChild("Plots")
    local plot  = plots and plots:FindFirstChild(plotName)
    local sign  = plot and plot:FindFirstChild("PlotSign")
    local yb    = sign and sign:FindFirstChild("YourBase")
    return (yb and yb:IsA("BillboardGui") and yb.Enabled == true) or false
end
local function findPrompt(a) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
    if not a then return nil end
    if a.isWalking then if a.prompt and a.prompt.Parent and a.prompt.Enabled then return a.prompt end return nil end
    local cp = PromptCache[a.uid]
    if cp and cp.Parent and cp.Enabled and cp.ActionText == "Steal" then return cp end
    local plots = Workspace:FindFirstChild("Plots"); if not plots then return nil end
    local plot  = plots:FindFirstChild(a.plot);     if not plot  then return nil end
    local pods  = plot:FindFirstChild("AnimalPodiums"); if not pods then return nil end
    local pod   = pods:FindFirstChild(a.slot);      if not pod   then return nil end
    local base  = pod:FindFirstChild("Base");        if not base  then return nil end
    local sp    = base:FindFirstChild("Spawn");      if not sp    then return nil end
    local att   = sp:FindFirstChild("PromptAttachment")
    if att then
        for _, p in ipairs(att:GetChildren()) do
            if p:IsA("ProximityPrompt") and p.Enabled and p.ActionText == "Steal" then PromptCache[a.uid] = p; return p end
        end end
    local startPos = sp.Position
    local best, bd = nil, math.huge
    local _pl = PromptIdx[plot]
    if not _pl or PromptIdxDirty[plot] then
        _pl = {}
        for _, d in ipairs(plot:GetDescendants()) do if d:IsA("ProximityPrompt") then _pl[#_pl + 1] = d end end
        PromptIdx[plot], PromptIdxDirty[plot] = _pl, nil
        if not PromptIdxConn[plot] then
            local function _mark(d) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if d:IsA("ProximityPrompt") then PromptIdxDirty[plot] = true end end
            PromptIdxConn[plot] = { plot.DescendantAdded:Connect(_mark), plot.DescendantRemoving:Connect(_mark), }
        end end
    for _, d in ipairs(_pl) do
        if d.Parent and d.Enabled and d.ActionText == "Steal" then
            local part = d.Parent
            local pp
            if part:IsA("BasePart") then pp = part.Position
            elseif part:IsA("Attachment") and part.Parent:IsA("BasePart") then pp = part.Parent.Position end
            if pp then
                local dx, dz = pp.X - startPos.X, pp.Z - startPos.Z
                if (dx * dx + dz * dz) < 25 and pp.Y > startPos.Y then
                    local yd = pp.Y - startPos.Y
                    if yd < bd then bd = yd; best = d end
                end end end end
    if best then PromptCache[a.uid] = best end
    return best
end
local _ownerPresent = {}
do
    for _, pl in ipairs(Players:GetPlayers()) do _ownerPresent[pl.Name] = true end
    Players.PlayerAdded:Connect(function(pl) LPH_ATTRIBUTES(ERROR_HANDLING(false))
_ownerPresent[pl.Name] = true end)
    Players.PlayerRemoving:Connect(function(pl) LPH_ATTRIBUTES(ERROR_HANDLING(false))
_ownerPresent[pl.Name] = nil end)
end
local _stealable; _stealable = function(a, me) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if not a or a.owner == me then return false end
    if _G.JAF_SkipLeftOwners == false then return true end
    return a.owner ~= nil and _ownerPresent[a.owner] == true
end
local resolvePriorityTarget; resolvePriorityTarget = function() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
    if manualTarget then return manualTarget end
    local _lpName = LocalPlayer.Name
    local _all = SharedState.AllAnimalsCache
    if stealNearest then
        local _, hrp = getChar()
        if hrp then
            local bestEntry, bestDist = nil, math.huge
            local _hrpPos = hrp.Position
            for _, a in ipairs(_all) do
                if _stealable(a, _lpName) then
                    local part = findAdorneeGlobal(a)
                    if part then local d = (_hrpPos - part.Position).Magnitude if d < bestDist then bestDist = d; bestEntry = a end end
                end end
            if bestEntry then return bestEntry end
        end end
    if (Config.UsePriority ~= false) then
        local best, bestScore = nil, math.huge
        local _prio = _G.JAF_PrioScore
        for _, a in ipairs(_all) do
            if a and a.name and _stealable(a, _lpName) then local s = _prio(a) if s < bestScore then bestScore, best = s, a end end
        end
        if best and bestScore < 999999 then return best end
    end
    for _, a in ipairs(_all) do if _stealable(a, _lpName) then return a end end
    return nil
end
_G.JAF_ResolveBest = resolvePriorityTarget
local findNearestPrompt; findNearestPrompt = function(priorityTarget) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
    local _, hrp = getChar()
    if not hrp then return end
    local plots = Workspace:FindFirstChild("Plots")
    if not plots then return end
    priorityTarget = priorityTarget or resolvePriorityTarget()
    if not priorityTarget then return end
    local _hrpPos = hrp.Position
    local _radius = Config.StealRadius or 95
    if priorityTarget.isWalking then
        local pr = priorityTarget.prompt
        if pr and pr.Parent and pr.Enabled then
            local model = priorityTarget.model
            local part = model and model.Parent and model:FindFirstChildWhichIsA("BasePart")
            if part then local pp = part.Position if _G.JAF_M2(pp.X, pp.Z, _hrpPos.X, _hrpPos.Z) <= _radius then return pr end end
        end
        return nil
    end
    local plot = plots:FindFirstChild(priorityTarget.plot)
    if not plot or isMyPlot(plot.Name) then return nil end
    local pods = plot:FindFirstChild("AnimalPodiums")
    local pod  = pods and pods:FindFirstChild(priorityTarget.slot)
    local base = pod and pod:FindFirstChild("Base")
    local spawn = base and base:FindFirstChild("Spawn")
    if not spawn then return nil end
    local sp = spawn.Position
    if _G.JAF_M2(sp.X, sp.Z, _hrpPos.X, _hrpPos.Z) > _radius then return nil end
    local att = spawn:FindFirstChild("PromptAttachment")
    if not att then return nil end
    local nearest
    for _, p in ipairs(att:GetChildren()) do if p:IsA("ProximityPrompt") and p.Enabled and p.ActionText:find("Steal") then nearest = p end end
    return nearest
end
local StealCache = setmetatable({}, {__mode = "k"})
local promptDistXZ; promptDistXZ = function(prompt) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local c = LocalPlayer.Character
    local hrp = c and c:FindFirstChild("HumanoidRootPart")
    if not hrp then return nil end
    local par = prompt and prompt.Parent
    local pos
    if par then
        if par:IsA("Attachment") then pos = par.WorldPosition
        elseif par:IsA("BasePart") then pos = par.Position
        else
            local bp = par:FindFirstAncestorWhichIsA("BasePart")
            if bp then pos = bp.Position end
        end end
    if not pos then return nil end
    local hp = hrp.Position
    return _G.JAF_M2(pos.X, pos.Z, hp.X, hp.Z), (pos - hp).Magnitude
end
local _pivotPos = function(m) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return m:GetPivot().Position end
local function executeSteal(prompt, heldUid, preHold) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if not SAB_LIVE() then return end
    if isAutoStealing then return end
    local function _tpBanking() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        return preHold == true and _G.JAF_StealPreHold ~= false and _G.JAF_TpActive == true and heldUid ~= nil
           and SharedState._currentTpTarget ~= nil and SharedState._currentTpTarget.uid == heldUid
    end
    local _d = promptDistXZ(prompt)
    if _d and _d > (Config.StealRadius or 95) and not _tpBanking() then return end
    local data = StealData[prompt]
    if not data then data = _grabCB(prompt); data.ready = true if #data.hold > 0 and (#data.trigger > 0 or _G.JAF_StealUseFPP ~= false) then StealData[prompt] = data end end
    if not data.ready then return end
    data.ready = false
    isAutoStealing = true
    _stealStart = tick()
    task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local oldDuration = prompt.HoldDuration
        prompt.HoldDuration = 0
        local _beginT, _inherited = 0, false
        local _hAge = _holdEpoch > 0 and (tick() - _holdEpoch) or math.huge
        if _G.JAF_StealHoldCarry ~= false and prompt.Enabled and _hAge <= (tonumber(_G.JAF_StealCarryMax) or 1.25) then
            _beginT, _inherited = _holdEpoch, true
        else
            for _, f in ipairs(data.hold) do task.spawn(f) end
            _beginT = prompt.Enabled and tick() or 0
            if _beginT > 0 and #data.hold > 0 then _holdEpoch = _beginT end
        end
        local _beganOk, _recapN, _instaCommitted = (_inherited or #data.hold > 0), 0, false
        local _dur = Config.StealDuration
        if not _dur then
            local _srv = (_G.JAF_StealUsePromptHold == true) and tonumber(oldDuration) or nil
            _dur = ((_srv and _srv > 0.1 and _srv) or 1.3) + (tonumber(_G.JAF_StealHoldPad) or 0)
        end
        _G.JAF_StealDurNow = _dur
        local _maxHold = tonumber(_G.JAF_StealMaxHold) or (_dur + (tonumber(_G.JAF_StealExtendWindow) or 1.3))
        _G.JAF_StealCycleDur = _dur
        local _t0 = _inherited and _beginT or tick(); local _holdAbsStart = tick()
        if _inherited then _stealStart = _beginT end
        local _ragRet0 = _ragUntil()
        local _postGrace = (tonumber(_G.JAF_StealPostGrace) or 0) + ((_ragRet0 > 0) and (tonumber(_G.JAF_RagdollPostGrace) or 0) or 0)
        local _abort, _grabbed, _recycle, _didEnter = false, false, false, false
        local _clearFireUntil, _lastCommitT, _commitN = 0, 0, 0
        local _eligSince, _commitGraceUntil, _tpHold = 0, 0, false
        local _tpWasBlk, _wasDis, _disAt = false, false, nil
        local _carryChkT, _carryFlag, _carryLastT, _bankT, _campChkT = 0, false, 0, 0, 0
        local _promptGone, _goneAt, _goneConn = false, nil, nil
        local activePrompt, activeData = prompt, data
        local _podiumForDist = _podOf(heldUid)
        local function _petDistNow() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local _, _h = getChar()
            if not _h then return nil end
            if _podiumForDist and _podiumForDist.Parent then
                local ok, piv = pcall(_pivotPos, _podiumForDist)
                if ok and piv then return (piv - _h.Position).Magnitude end
            end
            local _, d3 = promptDistXZ(activePrompt); return d3
        end
        if _G.JAF_StealReacquire ~= false and heldUid and _podiumForDist then
            _goneConn = _podiumForDist.DescendantAdded:Connect(function(desc) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                if _promptGone and desc:IsA("ProximityPrompt") and tostring(desc.ActionText or ""):find("Steal") then
                    activePrompt, activeData = desc, _grabCB(desc)
                    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
desc.HoldDuration = 0 end)
                    for _, f in ipairs(activeData.hold) do task.spawn(f) end
                    _beginT = desc.Enabled and tick() or 0
                    if _beginT > 0 and #activeData.hold > 0 then _holdEpoch = _beginT end
                    _beganOk = #activeData.hold > 0
                    _wasDis, _disAt, _bankT = false, nil, 0
                    _G.JAF_StealCarryWait = nil
                    _promptGone, _goneAt = false, nil
                    _stealStart = tick()
                    _t0, _commitN, _lastCommitT, _recycle = tick(), 0, 0, false
                end end) end
        repeat
            if manualTarget ~= nil and heldUid ~= nil and manualTarget.uid ~= heldUid then _abort = true; break end
            if LocalPlayer:GetAttribute("Stealing") then _grabbed = true; break end
            if _goneConn and not activePrompt.Parent and not _promptGone then _promptGone = true; _goneAt = tick() end
            local _bestSwitch = manualTarget == nil and heldUid ~= nil and _G.JAF_StealBestSwitch ~= false and _G.JAF_ResolveBest
            if _promptGone then
                _G.JAF_StealInCommitRange = false
                if _bankT == 0 or (tick() - _bankT) > (tonumber(_G.JAF_StealBankRefresh) or 2.5) then
                    if _fireHoldForced(activePrompt, activeData) then _bankT = tick() _holdEpoch = _bankT end
                end
                _G.JAF_StealCarryWait = tick() - _goneAt
                _G.JAF_StealCarryT0 = _bankT
                if (tick() - _goneAt) > (tonumber(_G.JAF_StealPromptGoneWait) or 6.5) then break end
                if _bestSwitch then local _lb = _G.JAF_ResolveBest(); if _lb and _lb.uid and _lb.uid ~= heldUid then _abort = true; break end end
                _t0 = _t0 + (_G.JAF_StealFireInterval or 0.12)
                task.wait(_G.JAF_StealFireInterval or 0.12)
                continue
            end
            if activePrompt.Parent and not activePrompt.Enabled then
                if not _disAt then _disAt = tick() end
                _G.JAF_StealInCommitRange = false
                if (tick() - _carryChkT) > 0.25 then
                    _carryChkT = tick()
                    if #activeData.hold == 0 then activeData = _grabCB(activePrompt) end
                    local _cnow = false
                    if _G.JAF_IsBeingStolen then
                        for _, _sa in ipairs(SharedState.AllAnimalsCache) do if _sa and _sa.uid == heldUid then
                            local _cok, _cv = pcall(_G.JAF_IsBeingStolen, _sa)
                            _cnow = _cok and _cv == true
                            break
                        end end
                    end
                    if _cnow then _carryLastT = tick() end
                    _carryFlag = _cnow or (tick() - _carryLastT) < 1.0
                end
                if _bankT > 0 and (tick() - _bankT) > (tonumber(_G.JAF_StealBankRefresh) or 2.5) then _bankT = 0 end
                if _bankT == 0 and _G.JAF_StealPreBank ~= false then
                    if _fireHoldForced(activePrompt, activeData) then _bankT = tick() _holdEpoch = _bankT end
                end
                if (tick() - _campChkT) > 0.25 then
                    _campChkT = tick()
                    if _bankT == 0 and _G.JAF_StealPreBank ~= false then
                        for _, _sa in ipairs(SharedState.AllAnimalsCache) do
                            if _sa and _sa.uid ~= heldUid and _stealable(_sa, LocalPlayer.Name) then
                                local _bp = findPrompt(_sa)
                                if _bp and _bp.Enabled and _bp:GetAttribute("State") == "Steal" then
                                    local _bd = _grabCB(_bp)
                                    if #_bd.hold > 0 then for _, f in ipairs(_bd.hold) do task.spawn(f) end _bankT = tick() _holdEpoch = _bankT break end
                                end
                            end
                        end
                    end
                    if _bestSwitch then local _lb = _G.JAF_ResolveBest() if _lb and _lb.uid and _lb.uid ~= heldUid then
                        local _keep = false
                        for _, _sa in ipairs(SharedState.AllAnimalsCache) do if _sa and _sa.uid == heldUid then _keep = _stealable(_sa, LocalPlayer.Name) ~= false; break end end
                        if not _keep then _abort = true end
                    end end
                    if _abort then break end
                end
                if (not _carryFlag) and (tick() - math.max(_disAt, _carryLastT)) > (tonumber(_G.JAF_StealDisabledGrace) or 3) then break end
                _wasDis = true
                _G.JAF_StealCarryWait = tick() - _disAt
                _G.JAF_StealCarryT0 = _bankT
                local _dt = tonumber(_G.JAF_StealPoll) or 0.03
                _stealStart = _bankT > 0 and _bankT or tick()
                _t0, _commitN, _lastCommitT, _recycle = tick(), 0, 0, false
                _G.JAF_StealExtending = false
                _G.JAF_StealCycleLeft = _maxHold
                _holdAbsStart = _holdAbsStart + (task.wait(_dt) or _dt)
                continue
            end
            if _wasDis then
                _wasDis, _disAt = false, nil
                _G.JAF_StealCarryWait = nil
                activeData = _grabCB(activePrompt)
                local _bAge = _bankT > 0 and (tick() - _bankT) or math.huge
                if _bAge <= (tonumber(_G.JAF_StealBankUseMax) or 2.3) then
                    _beginT = _bankT
                    _t0 = _bankT
                    _stealStart = _bankT
                    _beganOk = true
                else
                    for _, f in ipairs(activeData.hold) do task.spawn(f) end
                    _beginT = tick()
                    _beganOk = #activeData.hold > 0
                    if _beganOk then _holdEpoch = _beginT end
                    _t0 = tick()
                    _stealStart = tick()
                end
                _bankT = 0
                _commitN, _lastCommitT, _recycle = 0, 0, false
                _eligSince = tick() - (tonumber(_G.JAF_StealEntryDelay) or 0.3)
                _clearFireUntil = math.max(_clearFireUntil, tick() + (tonumber(_G.JAF_StealDropSettle) or 0.5))
            end
            if not _beganOk then _stealStart = tick() end
            local _needTrig = _G.JAF_StealUseFPP == false
            if (#activeData.hold == 0 or (_needTrig and #activeData.trigger == 0)) and _recapN < (tonumber(_G.JAF_StealRecapMax) or 40) then
                _recapN = _recapN + 1
                local _rd = _grabCB(activePrompt)
                if #_rd.hold > 0 and (not _needTrig or #_rd.trigger > 0) then
                    activeData = _rd
                    if not _beganOk then
                        for _, f in ipairs(_rd.hold) do task.spawn(f) end
                        if activePrompt.Enabled then _beginT = tick() _holdEpoch = _beginT end
                        _beganOk = true
                        _stealStart = tick()
                        _t0, _commitN, _lastCommitT, _recycle = tick(), 0, 0, false
                    end end end
            local _hrDist, _hr3D = promptDistXZ(activePrompt)
            local _preBank = _tpBanking()
            if _hrDist and _hrDist > (Config.StealRadius or 95) and not _preBank then _abort = true; break end
            if _bestSwitch then local _lb = _G.JAF_ResolveBest() if _lb and _lb.uid and _lb.uid ~= heldUid then
                local _pd0 = _petDistNow()
                if _beginT == 0 or (tick() - _beginT) < (tonumber(_G.JAF_StealSrvHold) or _dur) or (_pd0 and _pd0 > (tonumber(_G.JAF_StealSwitchNear) or 25)) then _abort = true; break end
                local _keep = false
                for _, _sa in ipairs(SharedState.AllAnimalsCache) do if _sa and _sa.uid == heldUid then _keep = _stealable(_sa, LocalPlayer.Name) ~= false; break end end
                if not _keep then _abort = true; break end
            end end
            local _ragSync = _G.JAF_RagdollStealSync ~= false
            local _now = tick()
            if _ragSync then
                local _nr = _ragUntil(); local _remNow = _nr - workspace:GetServerTimeNow()
                if _nr > _ragRet0 + 0.3 and _remNow > 0.1 then
                    _ragRet0 = _nr
                    _t0 = math.max(_t0, _now + _remNow - _dur + (tonumber(_G.JAF_RagdollLatePad) or 0.1))
                    _stealStart = math.max(_stealStart, _t0)
                    _postGrace = (tonumber(_G.JAF_StealPostGrace) or 0) + (tonumber(_G.JAF_RagdollPostGrace) or 0)
                end end
            local _holdDone = (_now - _t0) >= (_dur + _postGrace)
            local _ragBlocked = _ragSync and _ragBlockedNow()
            if _ragSync and _G.JAF_RagdollHoldUntilClear ~= false and _holdDone and _ragBlocked then
                _clearFireUntil = _now + (tonumber(_G.JAF_RagdollClearWindow) or 0.35)
            end
            if _holdDone and not _grabbed and _G.JAF_StealRangeHold ~= false then _G.JAF_StealExtending = true end
            if _commitN == 0 and not _ragBlocked and (_beginT == 0 or (_now - _beginT) > (tonumber(_G.JAF_StealSrvHoldMax) or 2.5)) and activePrompt.Parent and activePrompt.Enabled then
                local _rhOk = _fireHoldForced(activePrompt, activeData)
                _beginT = tick()
                _holdEpoch = _rhOk and _beginT or 0
                _beganOk = #activeData.hold > 0
                _stealStart = tick()
                _t0 = math.max(_t0, tick())
            end
            local _petDist = _petDistNow()
            local _petInRange = _petDist and _petDist <= (tonumber(_G.JAF_StealCommitRange) or 10)
            local _tpBlocking = (_G.JAF_StealTpGate ~= false) and (_G.JAF_TpActive == true) and SharedState._currentTpTarget ~= nil and SharedState._currentTpTarget.uid == heldUid
            if _tpWasBlk and not _tpBlocking and _G.JAF_StealTpRearm ~= false then
                local _relBar = tonumber(_G.JAF_StealTpRelBar) or 0
                if _relBar > 0 then
                    _t0 = math.max(_t0, _now - (_dur + _postGrace) + _relBar)
                    _stealStart = math.max(_stealStart, _t0)
                end
                _clearFireUntil = math.max(_clearFireUntil, _now + (tonumber(_G.JAF_StealTpRelGrace) or 0.75))
            end
            _tpWasBlk = _tpBlocking
            if _petInRange and not _tpBlocking then if _eligSince == 0 then _eligSince = _now end else _eligSince = 0 end
            local _snapT = tonumber(_G.JAF_BrainrotSnapT) or 0
            local _instaFresh = _snapT > 0 and (_now - _snapT) < (tonumber(_G.JAF_StealInstaWindow) or 5)
            local _settleAnchor = _eligSince
            if _instaFresh and _snapT > _settleAnchor then _settleAnchor = _snapT end
            local _instaDelay = tonumber(_G.JAF_StealEntryDelayInsta)
            if not _instaDelay then
                _instaDelay = tonumber(_G.JAF_InstaSettle) or (tonumber(_G.JAF_InstaSettleStart) or 0.35)
                _G.JAF_InstaSettle = _instaDelay
            end
            local _effDelay = _instaFresh and _instaDelay or (tonumber(_G.JAF_StealEntryDelay) or 0.3)
            local _settled = (_eligSince > 0) and ((_now - _settleAnchor) >= _effDelay)
            local _inCommit = _holdDone and not _grabbed and not _ragBlocked and _petInRange and not _tpBlocking and _settled
                              and _beginT > 0 and (_now - _beginT) >= (tonumber(_G.JAF_StealSrvHold) or _dur)
                              and (_now - _beginT) <= (tonumber(_G.JAF_StealSrvHoldMax) or 2.5)
            _tpHold = (_G.JAF_StealTpHold ~= false) and _tpBlocking and not _grabbed and _commitN == 0 and _holdDone
                      and (_now - _holdAbsStart) < (tonumber(_G.JAF_StealTpHoldMax) or 8)
            if _inCommit and _commitN == 0 then
                _lastCommitT, _commitN = _now, 1
                if _instaFresh then _instaCommitted = true end
                _fireTrigger(activePrompt, activeData)
            end
            if _inCommit and not _didEnter then
                _didEnter = true
                if _G.JAF_StealCommitSettle == true and ((not _preBank) or (_hr3D and _hr3D <= (tonumber(_G.JAF_StealSettleNear) or 6))) then
                    local _, _shrp = getChar()
                    local _v = _shrp and _shrp.AssemblyLinearVelocity
                    local _minSpd = tonumber(_G.JAF_StealCommitSettleSpeed) or 30
                    if _v and (_v.X * _v.X + _v.Z * _v.Z) > (_minSpd * _minSpd) then
                        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_shrp.AssemblyLinearVelocity = Vector3.new(0, _v.Y, 0) end)
                    end end end
            if _commitN > 0 and not _grabbed and (_now - _lastCommitT) >= (tonumber(_G.JAF_StealCommitWait) or 0.4) then _recycle = true end
            _commitGraceUntil = 0
            if _G.JAF_StealHoldForSettle ~= false and not _grabbed and (_eligSince > 0 or _ragBlocked) then
                local _fm = tonumber(_G.JAF_StealCommitFireMargin) or 0.06
                local _due = 0
                if _commitN == 0 then
                    if _eligSince > 0 and not _settled then _due = _settleAnchor + _effDelay + _fm end
                    if _beginT > 0 then
                        local _srvDue = _beginT + (tonumber(_G.JAF_StealSrvHold) or _dur) + _fm
                        if _srvDue > _due then _due = _srvDue end
                    end
                    local _hdDue = _t0 + _dur + _postGrace + _fm
                    if _hdDue > _due then _due = _hdDue end
                else _due = _lastCommitT + (tonumber(_G.JAF_StealCommitWait) or 0.4) end
                if _due > 0 then _commitGraceUntil = _due end
            end
            _G.JAF_StealInCommitRange = _inCommit
            task.wait(tonumber(_G.JAF_StealPoll) or 0.03)
            if not _grabbed and (tick() - _t0) >= (_dur + _postGrace) then _G.JAF_StealExtending = true end
            local _base = _maxHold - (tick() - _t0)
            if _base > 0 then
                _G.JAF_StealCycleLeft = _base
            else
                local _ext = _commitGraceUntil - tick()
                if _tpHold then _ext = math.max(_ext, (_holdAbsStart + (tonumber(_G.JAF_StealTpHoldMax) or 8)) - tick()) end
                _G.JAF_StealCycleLeft = math.max(_ext, 0)
            end
        until (not _promptGone and not _tpHold and not _disAt and ((tick() - _t0) >= _maxHold or _recycle) and tick() >= _clearFireUntil and tick() >= _commitGraceUntil)
              or (not _goneConn and not activePrompt.Parent)
              or (tick() - _holdAbsStart > (tonumber(_G.JAF_StealHoldMaxTotal) or 20))
        _G.JAF_StealExtending = false
        _G.JAF_StealInCommitRange = false
        if _commitN > 0 or not _abort then _holdEpoch = 0 end
        do
            _G.JAF_StealCarryWait = nil
            local _why = _grabbed and "grab" or (_abort and "abort") or ((tick() - _holdAbsStart) > (tonumber(_G.JAF_StealHoldMaxTotal) or 20) and "cap") or (_disAt and "carry") or (_promptGone and "gone") or (_commitN > 0 and "fired") or "expire"
            local _pd = _petDistNow()
            local _r = _G.JAF_StealWhy if type(_r) ~= "table" then _r = {} _G.JAF_StealWhy = _r end
            _r[#_r + 1] = string.format("%s c=%d pet=%.1f tp=%s en=%s dur=%.2f age=%.2f", _why, _commitN, _pd or -1, tostring(_G.JAF_TpActive), tostring(activePrompt.Parent ~= nil and activePrompt.Enabled), _dur, tick() - _holdAbsStart)
            if #_r > 10 then table.remove(_r, 1) end
        end
        if _instaCommitted and tonumber(_G.JAF_StealEntryDelayInsta) == nil then
            local _cur = tonumber(_G.JAF_InstaSettle) or 0.35
            if _grabbed then
                _G.JAF_InstaSettle = math.max(_cur - (tonumber(_G.JAF_InstaSettleDecay) or 0.02), tonumber(_G.JAF_InstaSettleStart) or 0.35)
            else
                _G.JAF_InstaSettle = math.min(_cur + (tonumber(_G.JAF_InstaSettleStep) or 0.1), tonumber(_G.JAF_InstaSettleMax) or 0.45)
            end end
        if _goneConn then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_goneConn:Disconnect() end) end
        if activePrompt.Parent and not _abort and not _grabbed and _commitN == 0 then
            local _fd = _petDistNow()
            if _fd and _fd <= (tonumber(_G.JAF_StealCommitRange) or 10) and activePrompt.Enabled and _beginT > 0 and (tick() - _beginT) >= (tonumber(_G.JAF_StealSrvHold) or _dur) and (tick() - _beginT) <= (tonumber(_G.JAF_StealSrvHoldMax) or 2.5) then _fireTrigger(activePrompt, activeData) end
        end
        if activePrompt.Parent then activePrompt.HoldDuration = oldDuration end
        data.ready = true
        isAutoStealing = false
    end) end
local _asAcc, _asResolveDt = 1, 0.03
local _asP, _asFp, _asUid = nil, nil, nil
local _asPre = false
local _pgKey, _pgEntryT = nil, 0
local _pgPingEma, _pgPingJit, _pgPingT = nil, 0, 0
local stealPingSample; stealPingSample = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local now = tick()
    if now - _pgPingT < (tonumber(_G.JAF_StealPingSampleDt) or 0.25) then return end
    _pgPingT = now
    local p = _G.JAF_Ping()
    if type(p) ~= "number" or p <= 0 then return end
    if not _pgPingEma then _pgPingEma, _pgPingJit = p, 0
    else
        local a = tonumber(_G.JAF_StealPingEmaAlpha) or 0.25
        _pgPingJit = _pgPingJit + (math.abs(p - _pgPingEma) - _pgPingJit) * a
        _pgPingEma = _pgPingEma + (p - _pgPingEma) * a
    end end
local stealPingWindow; stealPingWindow = function() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
    local rtt = _pgPingEma
    if not rtt then rtt = _G.JAF_Ping() end
    local ms = (rtt + (tonumber(_G.JAF_StealPingJitterMult) or 1) * (_pgPingJit or 0)) * 1000
    ms = math.min(ms, tonumber(_G.JAF_StealPingDelayCapMs) or 1000)
    if ms < (tonumber(_G.JAF_StealPingDelayMinMs) or 0) then ms = 0 end
    return ms / 1000
end
local stealPingGateReady; stealPingGateReady = function(prompt, uid) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if _G.JAF_StealPingDelay ~= true then return true end
    if not prompt then _pgKey = nil; return false end
    stealPingSample()
    local _d = promptDistXZ(prompt)
    if _d == nil then return true end
    if _d > (Config.StealRadius or 95) then _pgKey = nil; return false end
    local key = uid or prompt
    if _pgKey ~= key then _pgKey = key _pgEntryT = tick() end
    return tick() >= _pgEntryT + stealPingWindow()
end
_G.JAF_StealPingGateRearm = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _pgKey ~= nil then _pgEntryT = tick() end end
local _asEdgePrompt, _asEdgeConn = nil, nil
local function enableEdgeBurst(prompt) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if _G.JAF_EnableEdgeBurst ~= true then return end
    if not SAB_LIVE() then return end
    if not prompt or not prompt.Parent or not prompt.Enabled then return end
    if not (Config.AutoSteal ~= false) then return end
    if _G.JAF_StealCarryGate ~= false and LocalPlayer:GetAttribute("Stealing") then return end
    if isAutoStealing then return end
    local _ebDist = promptDistXZ(prompt)
    if _ebDist and _ebDist > (Config.StealRadius or 95) then return end
    if not stealPingGateReady(prompt, _asUid) then return end
    local d = StealCache[prompt]
    if not d then d = _grabCB(prompt) if #d.hold == 0 and #d.trigger == 0 then return end StealCache[prompt] = d end
    local od = prompt.HoldDuration; prompt.HoldDuration = 0
    for _, fn in ipairs(d.hold) do pcall(fn) end
    for _ = 1, (tonumber(_G.JAF_EnableEdgeBurstCount) or 8) do
        for _, fn in ipairs(d.trigger) do pcall(fn) end
        if fireproximityprompt and #d.trigger == 0 then pcall(fireproximityprompt, prompt) end
    end
    if prompt.Parent then prompt.HoldDuration = od end
end
local armEnableEdge; armEnableEdge = function(prompt) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if _G.JAF_EnableEdgeBurst ~= true then prompt = nil end
    if prompt == _asEdgePrompt then return end
    if _asEdgeConn then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_asEdgeConn:Disconnect() end); _asEdgeConn = nil end
    _asEdgePrompt = prompt
    if not prompt then return end
    _asEdgeConn = prompt:GetPropertyChangedSignal("Enabled"):Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if prompt == _asEdgePrompt and prompt.Enabled then enableEdgeBurst(prompt) end
    end) end
RunService.Heartbeat:Connect(function(dt) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not (Config.AutoSteal ~= false) then return end
    if isAutoStealing then return end
    if _G.JAF_StealCarryGate ~= false and LocalPlayer:GetAttribute("Stealing") then return end
    if _G.JAF_StealPingDelay ~= false then stealPingSample() end
    _asAcc = _asAcc + dt
    if _asAcc >= _asResolveDt or (_asP and not _asP.Parent) or (_asFp and not _asFp.Parent) then
        _asAcc = 0
        local tgt = resolvePriorityTarget()
        _asP = tgt and findNearestPrompt(tgt) or nil
        _asPre = false
        if not _asP and _G.JAF_StealPreHold ~= false and _G.JAF_TpActive == true and tgt and tgt.uid then
            local _ct = SharedState._currentTpTarget
            if _ct and _ct.uid == tgt.uid then
                local _cand = findPrompt(tgt)
                if _cand then
                    local _, _d3 = promptDistXZ(_cand)
                    if not _d3 or _d3 <= (tonumber(_G.JAF_StealPreHoldDist) or 95) then _asP, _asPre = _cand, true end
                end end end
        if not _asP and tgt and tgt.uid and _G.JAF_CampCarried ~= false and _G.JAF_IsBeingStolen then
            local _cok, _cv = pcall(_G.JAF_IsBeingStolen, tgt)
            if _cok and _cv == true then
                local _pod = _podOf(tgt.uid)
                local _sp = _pod and _pod:FindFirstChild("Base")
                _sp = _sp and _sp:FindFirstChild("Spawn")
                local _att = _sp and _sp:FindFirstChild("PromptAttachment")
                if _att then for _, _pp in ipairs(_att:GetChildren()) do
                    if _pp:IsA("ProximityPrompt") and tostring(_pp.ActionText or ""):find("Steal") then
                        local _cd = promptDistXZ(_pp)
                        if _cd and _cd <= (Config.StealRadius or 95) then _asP = _pp end
                        break
                    end
                end end
            end
        end
        armEnableEdge(_asP)
        _asUid = tgt and tgt.uid or nil
        _asFp = nil
        if _asP and tgt then _asFp = findPrompt(tgt) end
        _G.JAF_StealTgt = tgt and { name = tgt.name, gen = tgt.genValue, genText = tgt.genText, mut = tgt.mutation } or nil
    end
    if _asP then
        if not _asPre and not stealPingGateReady(_asP, _asUid) then return end
        if _G.JAF_RagdollStealSync ~= false and not _asPre then
            local _ret = _ragUntil()
            if _ret > 0 then
                local _win = tonumber(_G.JAF_RagdollPreArm) or ((Config.StealDuration or 1.3) - (tonumber(_G.JAF_RagdollLatePad) or 0.1))
                if (_ret - workspace:GetServerTimeNow()) > _win then return end
            end end
        executeSteal(_asP, _asUid, _asPre)
    elseif _asUid and _G.JAF_StealPingDelay ~= false then
        local _pod = _podOf(_asUid)
        local _ba = _pod and _pod:FindFirstChild("Base")
        local _sp = _ba and _ba:FindFirstChild("Spawn")
        local _c = LocalPlayer.Character
        local _hr = _c and _c:FindFirstChild("HumanoidRootPart")
        if not (_sp and _hr) then _pgKey = nil
        else
            local _spp, _hrp = _sp.Position, _hr.Position
            if _G.JAF_M2(_spp.X, _spp.Z, _hrp.X, _hrp.Z) > (Config.StealRadius or 95) then _pgKey = nil end
        end
    else
        _pgKey = nil
    end end)
task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local Sync   = _G.JAF_GetSynchronizer()
    if not Sync then return end
    local AData   = require(ReplicatedStorage:WaitForChild("Datas"):WaitForChild("Animals"))
    local AShared = require(ReplicatedStorage:WaitForChild("Shared"):WaitForChild("Animals"))
    local NUtils  = require(ReplicatedStorage:WaitForChild("Utils"):WaitForChild("NumberUtils"))
    local lastH   = {}
    _G.JAF_ScanStale = function(pn) LPH_ATTRIBUTES(ERROR_HANDLING(false))
lastH[pn] = nil end
    local _scanPending = {}
    local function scan(plot) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local ok, ch = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Sync:Get(plot.Name) end)
        if not ok or not ch then return end
        local al = ch:Get("AnimalList")
        local h  = ""
        if al then for s, d in pairs(al) do if type(d) == "table" then h = h..s..(d.Index or "")..(d.Mutation or "")..(d.Machine and ("#"..tostring(d.Machine.Type or "1")..(d.Machine.Active and "!" or "")) or "") end end end
        if lastH[plot.Name] == h then return end
        local owner, on = nil, nil
        if al then
            owner = ch:Get("Owner"); if not owner then return end
            on = _G.JAF_OwnerName(owner) or "?"
            if not Players:FindFirstChild(on) then return end
        end
        lastH[plot.Name] = h
        for i = #SharedState.AllAnimalsCache, 1, -1 do if SharedState.AllAnimalsCache[i].plot == plot.Name then table.remove(SharedState.AllAnimalsCache, i) end end
        if not al then return end
        for slot, ad in pairs(al) do
            if type(ad) == "table" and not _G.JAF_MachineBlocked(ad) then
                local inf = AData[ad.Index]
                if inf then
                    local mut = ad.Mutation or "None"
                    if mut == "Yin Yang" then mut = "YinYang" end
                    local gv = _G.JAF_SafeGen(ad.Index, ad.Mutation, ad.Traits, nil)
                    local _nm = inf.DisplayName or ad.Index
                    table.insert(SharedState.AllAnimalsCache, {
                        name     = _nm,
                        nameLower = string.lower(_nm),
                        genText  = "$"..NUtils:ToString(gv).."/s",
                        genValue = gv,
                        mutation = mut,
                        mutLower = string.lower(mut),
                        owner    = on,
                        plot     = plot.Name,
                        rarity   = inf.Rarity,
                        slot     = tostring(slot),
                        uid      = plot.Name.."_"..tostring(slot),
                    }) end end end
        table.sort(SharedState.AllAnimalsCache, function(a, b) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return a.genValue > b.genValue end)
    end
    local scheduleScan; scheduleScan = function(plot) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _scanPending[plot] then return end
        _scanPending[plot] = true
        task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
task.wait(0.1) _scanPending[plot] = nil pcall(scan, plot) end)
    end
    local function _plotSignText(plot) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local sign = plot:FindFirstChild("PlotSign")
        local txt  = sign and sign:FindFirstChildWhichIsA("TextLabel", true)
        return txt and txt.Text or nil
    end
    local _chanFolder = nil
    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local pk = game:GetService("ReplicatedStorage"):FindFirstChild("Packages")
        local sy = pk and pk:FindFirstChild("Synchronizer")
        _chanFolder = sy and sy:FindFirstChild("Channel")
    end)
    local function hookChannelEvent(plot) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if not _chanFolder then return false end
        local re = _chanFolder:FindFirstChild(plot.Name)
        if not re or not re:IsA("RemoteEvent") then return false end
        local ok = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            re.OnClientEvent:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G.JAF_ChanEvtScans = (tonumber(_G.JAF_ChanEvtScans) or 0) + 1 scheduleScan(plot) end)
        end)
        return ok
    end
    local function armChannelEvent(plot) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
for _ = 1, 20 do if hookChannelEvent(plot) then return end task.wait(0.25) end end)
    end
    local function pollPlot(plot) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
while plot.Parent do task.wait(tonumber(_G.JAF_PlotPoll) or 1.5) pcall(scan, plot) end end)
    end
    local function setup(plot) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if _G.JAF_SkipEmptyPlots ~= false and _plotSignText(plot) == "Empty Base" then
            _G.JAF_PlotsScanned = (_G.JAF_PlotsScanned or 0) + 1
            plot.DescendantAdded:Connect((function()    LPH_ATTRIBUTES(ERROR_HANDLING(false))
scheduleScan(plot) end))
            plot.DescendantRemoving:Connect((function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
scheduleScan(plot) end))
            armChannelEvent(plot)
            pollPlot(plot)
            return
        end
        local ch    = nil
        local _s0   = tick()
        local _step = tonumber(_G.JAF_PlotSetupPoll)
        while not ch and tick() - _s0 < (tonumber(_G.JAF_PlotSetupWait) or 1.2) do
            local ok, r = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Sync:Get(plot.Name) end)
            if ok and r then ch = r; break end
            if _step then task.wait(_step) else RunService.Heartbeat:Wait() end
        end
        if not ch then
            _G.JAF_PlotsScanned = (_G.JAF_PlotsScanned or 0) + 1
            _G.JAF_PlotsLate    = (tonumber(_G.JAF_PlotsLate) or 0) + 1
            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                plot.DescendantAdded:Connect((function()    LPH_ATTRIBUTES(ERROR_HANDLING(false))
scheduleScan(plot) end))
                plot.DescendantRemoving:Connect((function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
scheduleScan(plot) end))
            end)
            armChannelEvent(plot)
            pollPlot(plot)
            return
        end
        scan(plot)
        _G.JAF_PlotsScanned = (_G.JAF_PlotsScanned or 0) + 1
        plot.DescendantAdded:Connect((function()    LPH_ATTRIBUTES(ERROR_HANDLING(false))
scheduleScan(plot) end))
        plot.DescendantRemoving:Connect((function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
scheduleScan(plot) end))
        armChannelEvent(plot)
        pollPlot(plot)
    end
    local plots = Workspace:WaitForChild("Plots", 8)
    if plots then
        local _plotList = plots:GetChildren()
        _G.JAF_PlotsScanned = 0
        _G.JAF_PlotsTotal = #_plotList
        for _, p in ipairs(_plotList) do task.spawn(setup, p) end
        plots.ChildAdded:Connect(function(p)   LPH_ATTRIBUTES(ERROR_HANDLING(false))
task.wait(0.3); setup(p) end)
        plots.ChildRemoved:Connect(function(p) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            lastH[p.Name] = nil
            for i = #SharedState.AllAnimalsCache, 1, -1 do if SharedState.AllAnimalsCache[i].plot == p.Name then table.remove(SharedState.AllAnimalsCache, i) end end
        end)
        Players.PlayerRemoving:Connect(function(plr) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            if _G.JAF_EvictOnLeave == false then return end
            local nm = plr and plr.Name; if not nm or nm == "" then return end
            local n = 0
            for i = #SharedState.AllAnimalsCache, 1, -1 do
                local e = SharedState.AllAnimalsCache[i]
                if e and e.owner == nm then
                    if e.plot then lastH[e.plot] = nil end
                    table.remove(SharedState.AllAnimalsCache, i)
                    n = n + 1
                end end
            local cur = SharedState._currentTpTarget
            if cur and cur.owner == nm then SharedState._currentTpTarget = nil end
            if n > 0 then _G.JAF_EvictedOnLeave = (_G.JAF_EvictedOnLeave or 0) + n end
        end) end end)
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if _G.JAF_NoCarpetScan == true then return end
    local AData   = require(ReplicatedStorage:WaitForChild("Datas"):WaitForChild("Animals"))
    local AShared = require(ReplicatedStorage:WaitForChild("Shared"):WaitForChild("Animals"))
    local NUtils  = require(ReplicatedStorage:WaitForChild("Utils"):WaitForChild("NumberUtils"))
    local BLACKLIST = {
        ["arcadopus"] = true, ["festive lucky block"] = true,
        ["spooky lucky block"] = true, ["leprechaun lucky block"] = true,
    }
    local TRAIT_NAMES = {}
    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local _td = ReplicatedStorage:WaitForChild("Datas"):WaitForChild("Traits", 5)
        if _td then for _t in pairs(require(_td)) do TRAIT_NAMES[_t] = true end end
    end)
    local nameMap = {}
    for index, data in pairs(AData) do
        local iL = index:lower()
        local dL = (data.DisplayName or ""):lower()
        local entry = {index = index, data = data}
        nameMap[iL] = entry; nameMap[iL:gsub("%s+", "")] = entry
        if dL ~= "" then nameMap[dL] = entry; nameMap[dL:gsub("%s+", "")] = entry end
    end
    local isInsidePlots; isInsidePlots = (function(obj) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local p = obj.Parent
        while p do if p == Workspace then return false end if p.Name == "Plots" then return true end p = p.Parent end
        return false
    end)
    local findPromptOnModel; findPromptOnModel = (function(model) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        for _, d in ipairs(model:GetDescendants()) do
            if d:IsA("ProximityPrompt") and d.Enabled and d.ActionText == "Steal" then return d end
        end
        return nil
    end)
    local isInCache; isInCache = (function(obj) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        for _, e in ipairs(SharedState.WalkingCache) do if e.model == obj then return true end end
        return false
    end)
    local isAnyoneCarryingBrainrot; isAnyoneCarryingBrainrot = (function(animalIndex, displayName) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if not animalIndex and not displayName then return false end
        for _, p in ipairs(Players:GetPlayers()) do
            local idx = p:GetAttribute("StealingIndex")
            if idx ~= nil and idx ~= "" then
                local s = tostring(idx)
                if animalIndex and s == tostring(animalIndex) then return true end
                if displayName and s == tostring(displayName) then return true end
                if displayName and s:lower():gsub("%s", "") == tostring(displayName):lower():gsub("%s", "") then return true end
            end end
        return false
    end)
    local tryAdd; tryAdd = (function(obj) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if not obj or not obj:IsA("Model") then return end
        if isInCache(obj) then return end
        if isInsidePlots(obj) then return end
        if Players:GetPlayerFromCharacter(obj) then return end
        local mLow  = obj.Name:lower()
        local found = nameMap[mLow] or nameMap[mLow:gsub("%s+", "")]
        if not found then return end
        local dispLow = (found.data.DisplayName or found.index):lower()
        if BLACKLIST[dispLow] or BLACKLIST[mLow] then return end
        if not obj:FindFirstChildWhichIsA("Humanoid", true) then return end
        if isAnyoneCarryingBrainrot(found.index, found.data and found.data.DisplayName) then return end
        local _onConveyor, _nested = false, false
        local _anc = obj.Parent
        while _anc and _anc ~= Workspace do
            if _anc.Name == "RenderedMovingAnimals" then _onConveyor = true; break end
            if _anc:IsA("Model") then
                local _aLow = _anc.Name:lower()
                if TRAIT_NAMES[_anc.Name] or nameMap[_aLow] or nameMap[_aLow:gsub("%s+", "")] then _nested = true; break end
            end
            _anc = _anc.Parent
        end
        if _nested and _G.JAF_CarpetAllowNested ~= true then
            _G.JAF_CarpetTraitSkips = (_G.JAF_CarpetTraitSkips or 0) + 1
            _G.JAF_CarpetLastSkip   = obj.Name.." nested in "..tostring(_anc and _anc.Name)
            return
        end
        local _w = obj:GetAttribute("Walking")
        local _walkTagged = (_w ~= nil and _w ~= false)
        if TRAIT_NAMES[obj.Name] and not _walkTagged and not findPromptOnModel(obj) then
            _G.JAF_CarpetTraitSkips = (_G.JAF_CarpetTraitSkips or 0) + 1
            _G.JAF_CarpetLastSkip   = obj.Name.." unanchored trait model"
            return
        end
        if _G.JAF_CarpetRequireConveyor ~= false and not _onConveyor and not _walkTagged then return end
        local _stealPrompt = findPromptOnModel(obj)
        if _G.JAF_CarpetNeedPrompt == true and not _stealPrompt then return end
        if _G.JAF_CarpetOnCarpet ~= false then
            local _p = obj.PrimaryPart or obj:FindFirstChild("HumanoidRootPart", true) or obj:FindFirstChildWhichIsA("BasePart", true)
            if _p then
                local _cx = tonumber(_G.JAF_CarpetCenterX) or tonumber(_G.JAF_AisleX) or -410
                local _hw = tonumber(_G.JAF_CarpetHalfWidth) or 50
                if math.abs(_p.Position.X - _cx) > _hw then return end
                local _mb = _G.JAF_MapBounds
                if _mb and _mb.minZ and (_p.Position.Z < _mb.minZ - 10 or _p.Position.Z > _mb.maxZ + 10) then return end
            end end
        local gv = 0
        local ok1, val = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _G.JAF_SafeGen(found.index, nil, nil, nil) end)
        if ok1 and val then gv = val end
        local genStr = "$0/s"
        local ok2, str = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return NUtils:ToString(gv) end)
        if ok2 and str then genStr = "$"..str.."/s" end
        local entry = {
            name = found.data.DisplayName or found.index, genValue = gv,
            genText = genStr, mutation = "None", owner = "[Walking]",
            plot = "walking", slot = "walking", uid = "walk_"..tostring(obj),
            isWalking = true, model = obj, prompt = _stealPrompt,
            _animalIndex = found.index,
            _animalDisplay = found.data and found.data.DisplayName,
        }
        table.insert(SharedState.WalkingCache, entry)
        obj.AncestryChanged:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            if not obj:IsDescendantOf(Workspace) then
                for i = #SharedState.WalkingCache, 1, -1 do if SharedState.WalkingCache[i].model == obj then table.remove(SharedState.WalkingCache, i); break end end
            end end)
        task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local t = tick()
            while tick()-t < 10 do
                task.wait(0.5)
                if not obj.Parent then break end
                if entry.prompt and entry.prompt.Parent and entry.prompt.Enabled then break end
                entry.prompt = findPromptOnModel(obj)
            end end) end)
    local function fullScan() LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
        for i = #SharedState.WalkingCache, 1, -1 do
            local e = SharedState.WalkingCache[i]
            if not e.model or not e.model.Parent then
                table.remove(SharedState.WalkingCache, i)
            elseif e.isWalking and isAnyoneCarryingBrainrot(e._animalIndex, e._animalDisplay) then
                table.remove(SharedState.WalkingCache, i)
            end end
        local _src
        local _rma = (_G.JAF_CarpetRequireConveyor ~= false) and (_G.JAF_WalkScanNarrow ~= false)
                     and Workspace:FindFirstChild("RenderedMovingAnimals")
        if _rma then _src = _rma:GetDescendants() else _src = Workspace:GetDescendants() end
        local _ws = 0
        for _, obj in ipairs(_src) do
            pcall(tryAdd, obj)
            _ws = _ws + 1
            if _ws % (_G.JAF_WalkScanBatch or 300) == 0 then RunService.Heartbeat:Wait() end
        end end
    Workspace.DescendantAdded:Connect((function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not obj:IsA("Humanoid") then return end
        local model = obj.Parent
        if not model or not model:IsA("Model") then return end
        if isInsidePlots(model) then return end
        local mLow = model.Name:lower()
        if nameMap[mLow] or nameMap[mLow:gsub("%s+", "")] then task.wait(0.1); pcall(tryAdd, model) end
    end))
    pcall(fullScan)
    task.spawn((function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        while true do
            local _empty = #SharedState.WalkingCache == 0
            task.wait(_empty and (tonumber(_G.JAF_WalkScanEmptyEvery) or 1) or (tonumber(_G.JAF_WalkScanEvery) or 3))
            pcall(fullScan)
        end end)) end)
do local o = hubFind("AutoStealUI"); if o then o:Destroy() end end
local sg = JUI.new("ScreenGui", nil, "Name", "AutoStealUI", "ResetOnSpawn", false, "ZIndexBehavior", Enum.ZIndexBehavior.Sibling, "IgnoreGuiInset", true, "Parent", HubGui)
local function saveConfig() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if not _cfgLoaded then return end
    local data = { }
    for k, p in pairs(_panelRefs) do if p and p.Parent then data[k] = {x = p.Position.X.Offset, y = p.Position.Y.Offset} end end
    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
writefile(SAVE_FILE, HttpService:JSONEncode(data)) end)
end
local function loadConfig() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local ok, raw = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return readfile(SAVE_FILE) end)
    if not ok or not raw or raw == "" then _cfgLoaded = true; return end
    local ok2, data = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return HttpService:JSONDecode(raw) end)
    if not ok2 or type(data) ~= "table" then _cfgLoaded = true; return end
    for k, p in pairs(_panelRefs) do
        if data[k] and p and p.Parent and type(data[k].x) == "number" and type(data[k].y) == "number" then
            p.Position = UDim2.new(0, data[k].x, 0, data[k].y)
        end end
    _cfgLoaded = true
end
local function drag(handle, target) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local down, ds, sp
    handle.InputBegan:Connect(function(i) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if _dragLocked(target) then return end
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            down = true; ds = i.Position; sp = target.Position
        end end)
    UserInputService.InputEnded:Connect(function(i) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if down and (i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch) then
            down = false; pcall(saveConfig); pcall(SaveConfig)
        end end)
    UserInputService.InputChanged:Connect((function(i) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not down or _dragLocked(target) then return end
        if i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch then
            local d = i.Position - ds
            target.Position = UDim2.new(0, sp.X.Offset + d.X, 0, sp.Y.Offset + d.Y)
        end end)) end
local PANEL_W = 260
local PANEL_H = 430
local panel = JUI.new("Frame", sg, "Size", UDim2.new(0, PANEL_W, 0, PANEL_H), "Position", UDim2.new(0, 16, 0, 100), "BackgroundColor3", Theme.Background, "BackgroundTransparency", 0.18, "BorderSizePixel", 0, "ClipsDescendants", true)
Instance.new("UICorner", panel).CornerRadius = UDim.new(0, 14)
local panelStroke = Instance.new("UIStroke", panel)
_P(panelStroke, "Color", Theme.Accent1, "Thickness", 1.5, "Transparency", 0.3)
_panelRefs.panel = panel
panel.Visible = false
task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G.JAF_WaitHeavy(); if panel and panel.Parent then panel.Visible = true end end)
local hdr = JUI.new("Frame", panel, "Size", UDim2.new(1, 0, 0, 38), "BackgroundColor3", Theme.Surface, "BackgroundTransparency", 0.0, "BorderSizePixel", 0)
Instance.new("UICorner", hdr).CornerRadius = UDim.new(0, 14)
local hdrBot = JUI.new("Frame", hdr, "Size", UDim2.new(1, 0, 0, 10), "Position", UDim2.new(0, 0, 1, -10), "BackgroundColor3", Theme.Surface, "BorderSizePixel", 0)
local hdrTitle = Instance.new("TextLabel", hdr)
_P(hdrTitle, "Size", UDim2.new(1, -16, 1, 0), "Position", UDim2.new(0, 14, 0, 0), "BackgroundTransparency", 1, "Text", "AUTO STEAL", "Font", Enum.Font.GothamBold, "TextSize", 16, "TextColor3", Theme.TextPrimary, "TextXAlignment", Enum.TextXAlignment.Left)
drag(hdr, panel)
attachResizeGrip(panel, "AutoStealUI")
local hdrLine = JUI.new("Frame", panel, "Size", UDim2.new(1, 0, 0, 1), "Position", UDim2.new(0, 0, 0, 38), "BackgroundColor3", Theme.Accent1, "BackgroundTransparency", 0.5, "BorderSizePixel", 0)
local statusRow = JUI.new("Frame", panel, "Size", UDim2.new(1, -16, 0, 20), "Position", UDim2.new(0, 8, 0, 44), "BackgroundTransparency", 1, "BorderSizePixel", 0)
local manualLbl = Instance.new("TextLabel", statusRow)
_P(manualLbl, "Size", UDim2.new(1, -96, 1, 0), "BackgroundTransparency", 1, "Text", "AUTO SELECT", "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", T.Ok, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
local clearBtn = Instance.new("TextButton", statusRow)
_P(clearBtn, "Size", UDim2.new(0, 88, 1, 0), "Position", UDim2.new(1, -88, 0, 0), "BackgroundColor3", Theme.SurfaceHighlight, "Text", "CLEAR TARGET", "Font", Enum.Font.GothamBold, "TextSize", 11, "BorderSizePixel", 0, "TextColor3", Theme.TextPrimary)
Instance.new("UICorner", clearBtn).CornerRadius = UDim.new(0, 5)
do local s = Instance.new("UIStroke", clearBtn); s.Color = Theme.Accent2; s.Thickness = 1; s.Transparency = 0.4 end
local scroll = JUI.new("ScrollingFrame", panel, "Size", UDim2.new(1, -10, 1, -180))
_P(scroll, "Position", UDim2.new(0, 5, 0, 98), "BackgroundTransparency", 1, "BorderSizePixel", 0, "ScrollBarImageTransparency", 0.7, "ScrollBarThickness", 3, "ScrollingDirection", Enum.ScrollingDirection.Y)
local uiList = JUI.new("UIListLayout", scroll, "Padding", UDim.new(0, 4), "SortOrder", Enum.SortOrder.LayoutOrder)
local uiPad = JUI.new("UIPadding", scroll, "PaddingTop", UDim.new(0, 2), "PaddingBottom", UDim.new(0, 2))
uiList:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    scroll.CanvasSize = UDim2.new(0, 0, 0, uiList.AbsoluteContentSize.Y + 6)
end)
local editPrioBtn = JUI.new("TextButton", panel, "Size", UDim2.new(1, -16, 0, 32), "Position", UDim2.new(0, 8, 1, -40), "BackgroundColor3", Theme.Surface, "BackgroundTransparency", 0.1, "Text", "Edit Priority List", "Font", Enum.Font.GothamBold, "TextSize", 13, "BorderSizePixel", 0, "TextColor3", Theme.TextPrimary)
Instance.new("UICorner", editPrioBtn).CornerRadius = UDim.new(0, 7)
local epStroke = JUI.new("UIStroke", editPrioBtn, "Color", Theme.Accent2, "Thickness", 1)
local nearestRow = JUI.new("Frame", panel, "Size", UDim2.new(1, -16, 0, 28), "Position", UDim2.new(0, 8, 1, -74), "BackgroundColor3", Theme.Surface, "BackgroundTransparency", 0.1, "BorderSizePixel", 0)
Instance.new("UICorner", nearestRow).CornerRadius = UDim.new(0, 7)
local nearestStroke = JUI.new("UIStroke", nearestRow, "Color", stealNearest and Theme.Success or Color3.fromRGB(140, 140, 155), "Thickness", 1, "Transparency", 0.3)
local nearestLbl = Instance.new("TextLabel", nearestRow)
_P(nearestLbl, "Size", UDim2.new(1, -56, 1, 0), "Position", UDim2.new(0, 8, 0, 0), "BackgroundTransparency", 1, "Text", stealNearest and "Nearest: ON" or "Nearest: OFF", "Font", Enum.Font.GothamBold, "TextSize", 13)
nearestLbl.TextColor3 = Theme.TextPrimary
nearestLbl.TextXAlignment = Enum.TextXAlignment.Left
local nearestPill = JUI.new("Frame", nearestRow, "Size", UDim2.new(0, 36, 0, 18), "Position", UDim2.new(1, -42, 0.5, -9), "BackgroundColor3", stealNearest and Theme.Success or Color3.fromRGB(140, 140, 155), "BorderSizePixel", 0)
Instance.new("UICorner", nearestPill).CornerRadius = UDim.new(1, 0)
local nearestKnob = JUI.new("Frame", nearestPill, "Size", UDim2.new(0, 13, 0, 13), "Position", stealNearest and UDim2.new(1, -15, 0.5, -6.5) or UDim2.new(0, 2, 0.5, -6.5), "BackgroundColor3", _G.JAF_OnAccent(), "BorderSizePixel", 0)
Instance.new("UICorner", nearestKnob).CornerRadius = UDim.new(1, 0)
local nearestBtn = JUI.new("TextButton", nearestRow, "Size", UDim2.new(1, 0, 1, 0), "BackgroundTransparency", 1, "Text", "", "BorderSizePixel", 0)
local function toggleNearest() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    stealNearest = not stealNearest
    Config.StealNearest = stealNearest
    pcall(SaveConfig); pcall(saveConfig)
    nearestLbl.Text = stealNearest and "Nearest: ON" or "Nearest: OFF"
    nearestLbl.TextColor3 = Theme.TextPrimary
    nearestPill.BackgroundColor3 = stealNearest and Theme.Success or Color3.fromRGB(140, 140, 155)
    nearestKnob.Position = stealNearest and UDim2.new(1, -15, 0.5, -6.5) or UDim2.new(0, 2, 0.5, -6.5)
    nearestStroke.Color = stealNearest and Theme.Success or Color3.fromRGB(140, 140, 155)
end
nearestBtn.MouseButton1Click:Connect(toggleNearest)
local toggleRow = JUI.new("Frame", panel, "Size", UDim2.new(1, -16, 0, 36), "Position", UDim2.new(0, 8, 1, -116), "BackgroundColor3", Theme.Surface, "BackgroundTransparency", 0.1, "BorderSizePixel", 0)
Instance.new("UICorner", toggleRow).CornerRadius = UDim.new(0, 8)
local tgStroke = Instance.new("UIStroke", toggleRow)
_P(tgStroke, "Color", (Config.AutoSteal ~= false) and Theme.Success or Color3.fromRGB(140, 140, 155), "Thickness", 1.2, "Transparency", 0.2)
local toggleLabel = Instance.new("TextLabel", toggleRow)
_P(toggleLabel, "Size", UDim2.new(1, -56, 1, 0), "Position", UDim2.new(0, 12, 0, 0), "BackgroundTransparency", 1, "Text", (Config.AutoSteal ~= false) and "Auto Stealing" or "Stealing Off", "Font", Enum.Font.GothamBold, "TextSize", 14)
toggleLabel.TextColor3 = Theme.TextPrimary
toggleLabel.TextXAlignment = Enum.TextXAlignment.Left
local pillBg = JUI.new("Frame", toggleRow, "Size", UDim2.new(0, 42, 0, 22), "Position", UDim2.new(1, -50, 0.5, -11), "BackgroundColor3", (Config.AutoSteal ~= false) and Theme.Success or Color3.fromRGB(140, 140, 155), "BorderSizePixel", 0)
Instance.new("UICorner", pillBg).CornerRadius = UDim.new(1, 0)
local pillKnob = JUI.new("Frame", pillBg, "Size", UDim2.new(0, 16, 0, 16), "Position", (Config.AutoSteal ~= false) and UDim2.new(1, -19, 0.5, -8) or UDim2.new(0, 3, 0.5, -8), "BackgroundColor3", _G.JAF_OnAccent(), "BorderSizePixel", 0)
Instance.new("UICorner", pillKnob).CornerRadius = UDim.new(1, 0)
local toggleBtn = JUI.new("TextButton", toggleRow, "Size", UDim2.new(1, 0, 1, 0), "BackgroundTransparency", 1, "Text", "", "BorderSizePixel", 0)
local function updateToggleUI() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local on = (Config.AutoSteal ~= false)
    toggleLabel.Text = on and "Auto Stealing" or "Stealing Off"
    toggleLabel.TextColor3 = Theme.TextPrimary
    pillBg.BackgroundColor3 = on and Theme.Success or Color3.fromRGB(140, 140, 155)
    pillKnob.Position = on and UDim2.new(1, -19, 0.5, -8) or UDim2.new(0, 3, 0.5, -8)
    tgStroke.Color = on and Theme.Success or Color3.fromRGB(140, 140, 155)
end
toggleBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    Config.AutoSteal = not (Config.AutoSteal ~= false)
    updateToggleUI()
    pcall(SaveConfig)
    pcall(saveConfig)
end)
local selectedUID = nil
local function parseGenShort(s) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if type(s) ~= "string" then return 0 end
    s = s:gsub("[%s%+]", ""):lower()
    if s == "" then return 0 end
    local num, suf = s:match("^([%d%.]+)([kmb]?)$")
    num = tonumber(num)
    if not num or num < 0 then return 0 end
    if suf == "k" then return num * 1e3
    elseif suf == "m" then return num * 1e6
    elseif suf == "b" then return num * 1e9 end
    return num
end
_G.JAF_ParseGen = parseGenShort
local function animalGen(a) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if type(a.genValue) == "number" then return a.genValue end
    local t = tostring(a.genText or ""):lower():gsub("[%$,%s]", "")
    local num = tonumber(t:match("([%d%.]+)"))
    if not num then return 0 end
    if t:find("b") then return num * 1e9 elseif t:find("m") then return num * 1e6 elseif t:find("k") then return num * 1e3 end
    return num
end
local listGenFilter = parseGenShort(Config.AutoStealListFilter or "")
local _top10Cache, _top10At = nil, 0
local getTop10; getTop10 = (function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local _ttl = tonumber(_G.JAF_Top10TTL) or 0.2
    if _top10Cache and _ttl > 0 and (tick() - _top10At) < _ttl then return _top10Cache end
    local out, seen = {}, {}
    local cache = SharedState.AllAnimalsCache
    local ranked = {}
    for _, a in ipairs(cache) do
        if a and a.name and a.owner ~= LocalPlayer.Name and not seen[a.uid] then
            ranked[#ranked + 1] = { e = a, s = _G.JAF_PrioScore(a), i = #ranked }
        end end
    table.sort(ranked, function(x, y) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if x.s ~= y.s then return x.s < y.s end return x.i < y.i end)
    for _, r in ipairs(ranked) do
        if r.s >= 999999 then break end
        seen[r.e.uid] = true; table.insert(out, r.e)
        if #out >= 10 then break end
    end
    if #out < 10 then
        for _, a in ipairs(cache) do
            if a and a.owner ~= LocalPlayer.Name and not seen[a.uid] then
                seen[a.uid] = true; table.insert(out, a); if #out >= 10 then break end
            end end end
    _top10Cache, _top10At = out, tick()
    return out
end)
SharedState.GetTop10 = getTop10
local _lastListSig = nil
local rebuildList; rebuildList = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local top10 = getTop10()
    if listGenFilter and listGenFilter > 0 then
        local _f = {}
        for _, a in ipairs(top10) do if animalGen(a) >= listGenFilter then _f[#_f + 1] = a end end
        top10 = _f
    end
    local _stolen, _stMemo = {}, {}
    local sigParts = { tostring(selectedUID) }
    for _, a in ipairs(top10) do
        local _isSt = false
        if _G.JAF_IsBeingStolen then local _sok, _sr = pcall(_G.JAF_IsBeingStolen, a, _stMemo) _isSt = (_sok and _sr) or false end
        _stolen[a.uid] = _isSt
        sigParts[#sigParts + 1] = tostring(a.uid) .. "|" .. tostring(a.mutation) .. "|" .. tostring(_isSt)
    end
    local sig = table.concat(sigParts, ";")
    if sig == _lastListSig then return end
    _lastListSig = sig
    for _, b in ipairs(scroll:GetChildren()) do if b:IsA("Frame") or b:IsA("TextButton") then b:Destroy() end end
    for i, a in ipairs(top10) do
        local isSel = (selectedUID == a.uid)
        local mutColor = MUTCOL[a.mutation] or Theme.Accent1
        local function lerpColor(c1,c2,t) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Color3.new(c1.R+(c2.R-c1.R)*t, c1.G+(c2.G-c1.G)*t, c1.B+(c2.B-c1.B)*t) end
        local baseRowColor = a.mutation and a.mutation ~= "None" and lerpColor(Theme.Background, mutColor, 0.12) or Theme.Surface
        local selRowColor  = a.mutation and a.mutation ~= "None" and lerpColor(Theme.Surface, mutColor, 0.22) or Theme.SurfaceHighlight
        local row = JUI.new("TextButton", scroll, "Size", UDim2.new(1, -4, 0, 34), "LayoutOrder", i, "BackgroundColor3", isSel and selRowColor or baseRowColor, "BackgroundTransparency", isSel and 0.0 or 0.15, "BorderSizePixel", 0, "Text", "")
        Instance.new("UICorner", row).CornerRadius = UDim.new(0, 7)
        local ss = Instance.new("UIStroke", row)
        _P(ss, "Color", isSel and Theme.Accent1 or mutColor, "Thickness", isSel and 1.2 or 0.7, "Transparency", isSel and 0.1 or 0.7)
        local bar = JUI.new("Frame", row, "Size", UDim2.new(0, 3, 1, -10), "Position", UDim2.new(0, 4, 0, 5), "BackgroundColor3", mutColor, "BorderSizePixel", 0)
        Instance.new("UICorner", bar).CornerRadius = UDim.new(1, 0)
        local rk = JUI.new("TextLabel", row, "Size", UDim2.new(0, 24, 1, 0), "Position", UDim2.new(0, 10, 0, 0), "BackgroundTransparency", 1, "Text", tostring(i), "Font", Enum.Font.GothamBold, "TextSize", 13)
        rk.TextColor3 = Theme.TextPrimary
        rk.TextXAlignment = Enum.TextXAlignment.Center
        local nameStr = a.name
        local mutTag = ""
        if a.mutation and a.mutation ~= "None" then mutTag = a.mutation .. " " end
        local nm = Instance.new("TextLabel", row)
        _P(nm, "Size", UDim2.new(1, -110, 1, 0), "Position", UDim2.new(0, 36, 0, 0), "BackgroundTransparency", 1, "Font", Enum.Font.GothamBold, "TextSize", 13, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
        if a.mutation and a.mutation ~= "None" then
            nm.Text = a.mutation .. "  " .. nameStr
            nm.TextColor3 = isSel and Theme.TextPrimary or mutColor
        else
            nm.Text = nameStr
            nm.TextColor3 = Theme.TextPrimary
        end
        if _stolen[a.uid] == true then
            _P(nm, "Size", UDim2.new(1, -110, 0, 18), "Position", UDim2.new(0, 36, 0, 2))
            local st = JUI.new("TextLabel", row, "Size", UDim2.new(1, -110, 0, 12), "Position", UDim2.new(0, 36, 0, 20), "BackgroundTransparency", 1, "Text", "being stolen", "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", T.Err, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
        end
        local gn = Instance.new("TextLabel", row)
        _P(gn, "Size", UDim2.new(0, 70, 1, 0), "Position", UDim2.new(1, -74, 0, 0), "BackgroundTransparency", 1, "Text", a.genText, "Font", Enum.Font.GothamBold, "TextSize", 12)
        gn.TextColor3 = isSel and Theme.TextPrimary or mutColor
        gn.TextXAlignment = Enum.TextXAlignment.Right
        local ca = a
        row.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            selectedUID = ca.uid
            manualLbl.Text = ca.name
            manualLbl.TextColor3 = Theme.Accent1
            local _carrying = (LocalPlayer:GetAttribute("Stealing") or LocalPlayer:GetAttribute("StealingIndex")) and true or false
            local _allow = (not _carrying) and (Config.RetargetOnSelect ~= false)
            if _allow and _G.JAF_Retarget then
                _G.JAF_Retarget(ca)
            else
                manualTarget = ca
                SharedState.SetManualTarget(ca)
            end
            rebuildList()
        end) end
    scroll.CanvasSize = UDim2.new(0, 0, 0, uiList.AbsoluteContentSize.Y + 6)
end
clearBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    manualTarget = nil
    selectedUID = nil
    SharedState.SetManualTarget(nil)
    manualLbl.Text = "AUTO SELECT"
    manualLbl.TextColor3 = T.Ok
    rebuildList()
end)
task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    while panel.Parent do
        task.wait(0.5)
        if manualTarget then
            local found = false
            for _, a in ipairs(SharedState.AllAnimalsCache) do if a.uid == manualTarget.uid then found = true; break end end
            if not found then for _, a in ipairs(SharedState.WalkingCache) do if a.uid == manualTarget.uid then found = true; break end end end
            if found then local _aok, _al = pcall(_G.JAF_PetAlive, manualTarget) if _aok and _al == false then found = false end end
            if not found then
                manualTarget = nil; selectedUID = nil
                manualLbl.Text = "AUTO SELECT"; manualLbl.TextColor3 = T.Ok
            end end
        if manualTarget then
            local _mn = tostring(manualTarget.name)
            if manualLbl.Text ~= _mn then manualLbl.Text = _mn; manualLbl.TextColor3 = Theme.Accent1 end
        elseif manualLbl.Text ~= "AUTO SELECT" then
            manualLbl.Text = "AUTO SELECT"; manualLbl.TextColor3 = T.Ok
        end
        rebuildList()
    end end)
do
    local filterRow = JUI.new("Frame", panel, "Size", UDim2.new(1, -10, 0, 24), "Position", UDim2.new(0, 5, 0, 71), "BackgroundColor3", Theme.Surface, "BackgroundTransparency", 0.1, "BorderSizePixel", 0)
    Instance.new("UICorner", filterRow).CornerRadius = UDim.new(0, 6)
    local fl = JUI.new("TextLabel", filterRow, "Size", UDim2.new(0, 62, 1, 0), "Position", UDim2.new(0, 8, 0, 0), "BackgroundTransparency", 1, "Text", "Min Gen/s", "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", Theme.TextPrimary, "TextXAlignment", Enum.TextXAlignment.Left)
    local fb = JUI.new("TextBox", filterRow, "Size", UDim2.new(1, -78, 0, 18), "Position", UDim2.new(0, 72, 0.5, -9))
    _P(fb, "BackgroundColor3", Theme.SurfaceHighlight, "Text", tostring(Config.AutoStealListFilter or ""), "PlaceholderText", "e.g. 10m+, 20m, 1b", "PlaceholderColor3", Theme.TextSecondary)
    fb.Font = Enum.Font.GothamMedium; fb.TextSize = 12; fb.TextColor3 = Theme.TextPrimary
    fb.ClearTextOnFocus = false; fb.BorderSizePixel = 0
    Instance.new("UICorner", fb).CornerRadius = UDim.new(0, 5)
    fb.FocusLost:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        Config.AutoStealListFilter = fb.Text
        listGenFilter = parseGenShort(fb.Text)
        pcall(SaveConfig); pcall(saveConfig)
        _lastListSig = nil
        rebuildList()
    end) end
editPrioBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if _G.JAF_TogglePriorityList then _G.JAF_TogglePriorityList()
    elseif _G.JAF_OpenPriorityList then _G.JAF_OpenPriorityList() end
end)
do
    local pill = JUI.new("Frame", sg, "Name", "StealBarPill", "Position", UDim2.new(0, 16, 0, 560))
    _P(pill, "Size", UDim2.new(0, 320, 0, 46), "BackgroundColor3", Theme.Background, "BackgroundTransparency", 0.02, "BorderSizePixel", 0)
    Instance.new("UICorner", pill).CornerRadius = UDim.new(0, 9)
    local pillStroke = JUI.new("UIStroke", pill, "Color", Theme.Accent1, "Thickness", 2, "Transparency", 0.1)
    _panelRefs.stealBar = pill
    local nameLbl = JUI.new("TextLabel", pill, "Size", UDim2.new(1, -96, 0, 16), "Position", UDim2.new(0, 12, 0, 5), "BackgroundTransparency", 1, "Text", "—", "Font", Enum.Font.GothamBold, "TextSize", 14)
    _P(nameLbl, "TextColor3", Color3.fromRGB(255,255,255), "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
    local genLbl = JUI.new("TextLabel", pill, "Size", UDim2.new(0, 82, 0, 16), "Position", UDim2.new(1, -90, 0, 5), "BackgroundTransparency", 1, "Text", "", "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", Color3.fromRGB(120,235,150), "TextXAlignment", Enum.TextXAlignment.Right)
    local pctLbl = JUI.new("TextLabel", pill, "Size", UDim2.new(0, 150, 0, 13), "Position", UDim2.new(0, 12, 0, 23), "BackgroundTransparency", 1, "Text", "0%", "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", Theme.Accent1, "TextXAlignment", Enum.TextXAlignment.Left)
    local secLbl = JUI.new("TextLabel", pill, "Size", UDim2.new(0, 110, 0, 13), "Position", UDim2.new(1, -122, 0, 23), "BackgroundTransparency", 1, "Text", "", "Font", Enum.Font.GothamMedium, "TextSize", 12, "TextColor3", Color3.fromRGB(210,210,215), "TextXAlignment", Enum.TextXAlignment.Right)
    local track = JUI.new("Frame", pill, "Size", UDim2.new(1,-24,0,4), "Position", UDim2.new(0,12,1,-7), "BackgroundColor3", Color3.fromRGB(30,14,50), "BorderSizePixel", 0)
    Instance.new("UICorner", track).CornerRadius = UDim.new(1,0)
    local fill = JUI.new("Frame", track, "Size", UDim2.new(0,0,1,0), "BackgroundColor3", Theme.Accent1, "BorderSizePixel", 0)
    Instance.new("UICorner", fill).CornerRadius = UDim.new(1,0)
    drag(pill, pill)
    local DONE = Color3.fromRGB(30,185,100)
    local READY_EXT = Color3.fromRGB(255,190,60)
    local RAG  = Color3.fromRGB(235,110,60)
    local _mutColCache = {}
    local _mutColorOf; _mutColorOf = function(t) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local m = t and t.mut
        if type(m) == "string" then
            local c = _mutColCache[m]
            if c ~= nil then return c or nil end
            local ms = (m:gsub("^%s*(.-)%s*$", "%1"))
            local ml = ms:lower()
            if ms ~= "" and ml ~= "none" and ml ~= "n/a" and ml ~= "nil" and ml ~= "normal" and ml ~= "default" then
                local col = _G.JAF_MutColor(ms)
                _mutColCache[m] = col
                return col
            end
            _mutColCache[m] = false
            return nil
        end
        return nil
    end
    local _ragTotal = 0
    local _noTgtLatched = false
    local _WHITE = Color3.fromRGB(255,255,255)
    local _lastTgt = nil
local _readyLatched = false
local _readyDur = nil
local _idleT = 0
    RunService.Heartbeat:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local _t = _G.JAF_StealTgt
        local _dur = Config.StealDuration or tonumber(_G.JAF_StealCycleDur) or tonumber(_G.JAF_StealDurNow) or 1.3
        if not _t then
            _stealProgress = 0
            _ragTotal = 0
            if not _noTgtLatched then
                _noTgtLatched = true
                fill.Size = UDim2.new(0,0,1,0); fill.BackgroundColor3 = Theme.Accent1
                nameLbl.Text = "No Target"; nameLbl.TextColor3 = Color3.fromRGB(150,150,158)
                genLbl.Text = ""; pctLbl.Text = ""; secLbl.Text = ""
                _lastTgt = nil
                _readyLatched = false
            end
            return
        end
        _noTgtLatched = false
        if _t ~= _lastTgt then
            _lastTgt = _t
            nameLbl.Text = _t.name or "Brainrot"
            nameLbl.TextColor3 = _mutColorOf(_t) or _WHITE
            genLbl.Text = _t.genText or (_t.gen and ("$"..tostring(_t.gen).."/s")) or ""
        end
        local _ret = math.max(tonumber(LocalPlayer:GetAttribute("RagdollEndTime")) or 0, tonumber(_G.JAF_RagdollUntil) or 0)
        local _rem = _ret > 0 and (_ret - workspace:GetServerTimeNow()) or 0
        if _rem > 0.05 then
            if _rem > _ragTotal then _ragTotal = _rem end
            _stealProgress = 0
            fill.Size = UDim2.new(math.clamp(_rem / math.max(_ragTotal, 0.1), 0, 1), 0, 1, 0)
            fill.BackgroundColor3 = RAG
            pctLbl.Text = "RAGDOLLED"; pctLbl.TextColor3 = RAG
            secLbl.Text = string.format("%.1fs", _rem)
            _readyLatched = false
            return
        end
        _ragTotal = 0
        if not isAutoStealing then
            if _idleT == 0 then _idleT = tick() end
            if (tick() - _idleT) < (tonumber(_G.JAF_StealReadyDebounce) or 0.12) then return end
            if not _readyLatched or _readyDur ~= _dur then
                _readyLatched, _readyDur = true, _dur
                _stealProgress = 0
                fill.Size = UDim2.new(0,0,1,0); fill.BackgroundColor3 = Theme.Accent1
                pctLbl.Text = "READY"; pctLbl.TextColor3 = Theme.Accent1
                secLbl.Text = string.format("%.1fs", _dur)
            end
            _stealProgress = 0
            return
        end
        _idleT = 0
        _readyLatched = false
        if _G.JAF_StealCarryWait then
            local _ct0 = tonumber(_G.JAF_StealCarryT0) or 0
            local _age = _ct0 > 0 and (tick() - _ct0) or 0
            if _age > _dur then
                _stealProgress = 1
                fill.Size = UDim2.new(1,0,1,0)
                fill.BackgroundColor3 = READY_EXT
                pctLbl.Text = "HOLD"
                pctLbl.TextColor3 = READY_EXT
                secLbl.Text = string.format("restart in %.1fs", math.max((tonumber(_G.JAF_StealBankRefresh) or 2.5) - _age, 0))
            else
                local _cp = math.clamp(_age / _dur, 0, 1)
                _stealProgress = _cp
                fill.Size = UDim2.new(_cp,0,1,0)
                fill.BackgroundColor3 = Theme.Accent1
                pctLbl.Text = ("STEALING  %d%%"):format(math.floor(_cp*100))
                pctLbl.TextColor3 = Theme.Accent1
                secLbl.Text = ""
            end
            return
        end
        local _cyc = tonumber(_G.JAF_StealCycleDur) or _dur
        _stealProgress = math.clamp((tick()-_stealStart)/_cyc,0,1)
        local pct = math.floor(_stealProgress*100)
        local done = _stealProgress >= 0.99
        local left = math.max(_cyc - (tick()-_stealStart), 0)
        fill.Size = UDim2.new(_stealProgress,0,1,0)
        if _G.JAF_StealInCommitRange then
            fill.Size = UDim2.new(1,0,1,0)
            fill.BackgroundColor3 = DONE
            pctLbl.Text = "IN RANGE  -  GRABBING"
            pctLbl.TextColor3 = DONE
            secLbl.Text = ""
            return
        end
        local _reallyStolen = LocalPlayer:GetAttribute("Stealing") and true or false
        local _gleft = tonumber(_G.JAF_StealCycleLeft) or 0
        local _holding = done and not _reallyStolen and _gleft > 0.05
        fill.BackgroundColor3 = _reallyStolen and DONE or (_holding and READY_EXT or Theme.Accent1)
        pctLbl.Text = _reallyStolen and "STOLEN" or (done and (_holding and "HOLD" or "RESTARTING") or ("STEALING  "..pct.."%"))
        pctLbl.TextColor3 = _reallyStolen and DONE or (_holding and READY_EXT or Theme.Accent1)
        secLbl.Text = _reallyStolen and "" or (_holding and string.format("restart in %.1fs", _gleft) or (done and "" or string.format("%.1fs left", left)))
    end) end
pcall(loadConfig)
end)()
local playerESPToggleRef = {setFn=nil}
local espToggleRef = {enabled=true, setFn=nil}
local executeReset = nil
local xrayEnabled   = Config.XrayEnabled or false
local xraySpoof     = {}
local _xrayDescConn = nil
local _isaC = _G.JAF_IsA
local xrayShouldApply; xrayShouldApply = (function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not _isaC(obj, "BasePart") or not obj.Anchored then return false end
    local par0 = obj.Parent
    local insidePlots = false
    local anc = par0
    while anc do
        local _an = anc.Name
        if _an == "AnimalPodiums" then return false end
        if _an == "Plots" then insidePlots = true end
        if anc == Workspace then break end
        anc = anc.Parent
    end
    if insidePlots then return true end
    local n = obj.Name:lower()
    local p = par0 and par0.Name:lower() or ""
    return n:find("base") or n:find("claim") or p:find("base") or p:find("claim")
end)
local _xrayHookInstalled = false
local function installXrayHook() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if _xrayHookInstalled then return end
    if _G.JAF_XrayHook == false then return end
    if not (hookmetamethod and checkcaller) then return end
    _xrayHookInstalled = true
    local oldIndex
    local inHook = false
    oldIndex = hookmetamethod(game, "__index", (function(self, key) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if key == "LocalTransparencyModifier" and not inHook then
            inHook = true
            local spoof    = xraySpoof[self]
            local fromGame = not checkcaller()
            inHook = false
            if spoof ~= nil and fromGame then return spoof end
        end
        return oldIndex(self, key)
    end)) end
local _baseFootprints = {}
_G.JAF_InBase = false
local _inPlotFolder; _inPlotFolder = function(p, fname, plot) LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local x = p.Parent
    while x and x ~= plot do if x.Name == fname then return true end x = x.Parent end
    return false
end
local _fallbackBox; _fallbackBox = function(plot) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if _G.JAF_FootprintFallbackBox == false then return nil end
    local ok, cf, sz = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return plot:GetBoundingBox() end)
    if not ok or not cf or not sz then return nil end
    if math.abs(cf.Position.X) > 50000 or math.abs(cf.Position.Z) > 50000 then return nil end
    local hx, hz = sz.X * 0.5, sz.Z * 0.5
    local cap = tonumber(_G.JAF_FallbackMaxHalf) or 70
    if hx > cap or hz > cap then return nil end
    local _u = _G.JAF_UnfootedPlots or {}; _u[plot.Name] = true; _G.JAF_UnfootedPlots = _u
    return cf.Position.X - hx, cf.Position.X + hx, cf.Position.Z - hz, cf.Position.Z + hz,
           cf.Position.Y - (_G.JAF_BaseFloorDrop or 4), cf.Position.Y + (_G.JAF_BaseFloorWallH or 16)
end
local _baseBoxFromXray; _baseBoxFromXray = function(plot) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if _G.JAF_XrayFootprint == false then return nil end
    local function _inF(o, name) LPH_ATTRIBUTES(ERROR_HANDLING(false))
local a = o.Parent; while a and a ~= plot do if a.Name == name then return true end; a = a.Parent end; return false end
    local pods = plot:FindFirstChild("AnimalPodiums")
    local mnF, mxF = math.huge, -math.huge
    if pods then for _, pod in ipairs(pods:GetChildren()) do local b = pod:FindFirstChild("Base"); local sp = b and b:FindFirstChild("Spawn"); if sp and sp:IsA("BasePart") then if sp.Position.Y < mnF then mnF = sp.Position.Y end; if sp.Position.Y > mxF then mxF = sp.Position.Y end end end end
    local haveFloor = mnF ~= math.huge
    local floorY = haveFloor and (mnF - (_G.JAF_BaseFloorDrop or 4)) or nil
    local roofY  = haveFloor and (mxF + (_G.JAF_BaseFloorWallH or 16)) or nil
    local mnx, mxx, mnz, mxz, ny, ymn, ymx = math.huge, -math.huge, math.huge, -math.huge, 0, math.huge, -math.huge
    for _, d in ipairs(plot:GetDescendants()) do
        if d:IsA("BasePart") and d.Anchored and d.CanCollide
           and math.abs(d.Position.X) < 50000 and math.abs(d.Position.Z) < 50000
           and not _inF(d, "AnimalPodiums")
           and not _inF(d, "CashPad") and not _inF(d, "Cash")
           and not _inF(d, "Laser") and d.Name ~= "LaserHitbox" then
            local q, s, cf = d.Position, d.Size, d.CFrame
            local hx = (math.abs(cf.RightVector.X)*s.X + math.abs(cf.UpVector.X)*s.Y + math.abs(cf.LookVector.X)*s.Z) * 0.5
            local hy = (math.abs(cf.RightVector.Y)*s.X + math.abs(cf.UpVector.Y)*s.Y + math.abs(cf.LookVector.Y)*s.Z) * 0.5
            local hz = (math.abs(cf.RightVector.Z)*s.X + math.abs(cf.UpVector.Z)*s.Y + math.abs(cf.LookVector.Z)*s.Z) * 0.5
            if (not haveFloor) or ((q.Y + hy >= floorY - 3) and (q.Y - hy <= roofY + 5)) then
                if q.X-hx < mnx then mnx = q.X-hx end
                if q.X+hx > mxx then mxx = q.X+hx end
                if q.Z-hz < mnz then mnz = q.Z-hz end
                if q.Z+hz > mxz then mxz = q.Z+hz end
                if q.Y-hy < ymn then ymn = q.Y-hy end
                if q.Y+hy > ymx then ymx = q.Y+hy end
                ny = ny + 1
            end end end
    if ny < 3 then return nil end
    return mnx, mxx, mnz, mxz, (floorY or ymn), (roofY or ymx)
end
local _baseBox; _baseBox = function(plot) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local pods = plot:FindFirstChild("AnimalPodiums")
    local mnF, mxF = math.huge, -math.huge
    if pods then
        for _, pod in ipairs(pods:GetChildren()) do
            local b  = pod:FindFirstChild("Base")
            local sp = b and b:FindFirstChild("Spawn")
            if sp and sp:IsA("BasePart") then
                if sp.Position.Y < mnF then mnF = sp.Position.Y end
                if sp.Position.Y > mxF then mxF = sp.Position.Y end
            end end end
    if mnF == math.huge then return _fallbackBox(plot) end
    local floorY = mnF - (_G.JAF_BaseFloorDrop or 4)
    local roofY  = mxF + (_G.JAF_BaseFloorWallH or 16)
    local lx0,lx1,lz0,lz1,ln = math.huge,-math.huge, math.huge,-math.huge, 0
    for _, p in ipairs(plot:GetDescendants()) do
        if p:IsA("BasePart") and _inPlotFolder(p, "Laser", plot) then
            local q,s = p.Position, p.Size
            if math.abs(q.X) < 50000 and math.abs(q.Z) < 50000 then
                if q.X-s.X*0.5 < lx0 then lx0 = q.X-s.X*0.5 end
                if q.X+s.X*0.5 > lx1 then lx1 = q.X+s.X*0.5 end
                if q.Z-s.Z*0.5 < lz0 then lz0 = q.Z-s.Z*0.5 end
                if q.Z+s.Z*0.5 > lz1 then lz1 = q.Z+s.Z*0.5 end
                ln = ln + 1
            end end end
    if ln == 0 then return _fallbackBox(plot) end
    local sx0,sx1,sn = math.huge,-math.huge, 0
    local zbins = {}
    local zEdge = {}
    for _, p in ipairs(plot:GetDescendants()) do
        if p:IsA("BasePart") and p.Name:lower():find("structure base", 1, true)
           and not (_inPlotFolder(p, "CashPad", plot) or _inPlotFolder(p, "Cash", plot)) then
            local q,s = p.Position, p.Size
            local _yOK
            if _G.JAF_TallWallOverlap ~= false then
                _yOK = (q.Y + s.Y*0.5 >= floorY-3) and (q.Y - s.Y*0.5 <= roofY+5)
            else
                _yOK = (q.Y >= floorY-3 and q.Y <= roofY+5)
            end
            if _yOK and math.abs(q.X) < 50000 and math.abs(q.Z) < 50000 then
                if q.X-s.X*0.5 < sx0 then sx0 = q.X-s.X*0.5 end
                if q.X+s.X*0.5 > sx1 then sx1 = q.X+s.X*0.5 end
                local zb = math.floor(q.Z/4 + 0.5)
                zbins[zb] = (zbins[zb] or 0) + 1
                local _pmn, _pmx = q.Z - s.Z*0.5, q.Z + s.Z*0.5
                local _e = zEdge[zb]
                if not _e then zEdge[zb] = { _pmn, _pmx } else if _pmn < _e[1] then _e[1]=_pmn end; if _pmx > _e[2] then _e[2]=_pmx end end
                sn = sn + 1
            end end end
    if sn < 3 then return _fallbackBox(plot) end
    local maxc = 0
    for _, c in pairs(zbins) do if c > maxc then maxc = c end end
    local zmin, zmax = math.huge, -math.huge
    local thr = maxc * (_G.JAF_BaseSideBandFrac or 0.25)
    for zb, c in pairs(zbins) do
        if c >= thr then
            local _e = zEdge[zb]
            local _emn = (_G.JAF_ZExtentRealEdges ~= false and _e) and _e[1] or (zb*4 - 2)
            local _emx = (_G.JAF_ZExtentRealEdges ~= false and _e) and _e[2] or (zb*4 + 2)
            if _emn < zmin then zmin = _emn end
            if _emx > zmax then zmax = _emx end
        end end
    if zmin == math.huge then zmin, zmax = lz0, lz1 end
    local xmin, xmax
    if (lx0+lx1)*0.5 >= (sx0+sx1)*0.5 then xmin, xmax = sx0, lx1 else xmin, xmax = lx0, sx1 end
    return xmin, xmax, zmin, zmax, floorY, roofY
end
_G.JAF_CurrentBase = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local c = LocalPlayer.Character
    local hrp = c and c:FindFirstChild("HumanoidRootPart")
    if not hrp then return nil end
    local q = hrp.Position
    for _, b in ipairs(_baseFootprints) do
        if q.X >= b.minX and q.X <= b.maxX and q.Z >= b.minZ and q.Z <= b.maxZ
           and q.Y >= b.minY - 5 and q.Y <= b.maxY + 8 then
            return b.plot
        end end
    return nil
end
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local _fpCache = {}
    local _mbDone = false
    local _mbTries = 0
    local function _publishFootprints() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local fps = {}
        for _, c in pairs(_fpCache) do if c.fp then fps[#fps+1] = c.fp end end
        _baseFootprints = fps
        _G.JAF_BaseFootprints = fps
        return fps
    end
    while true do
        local plots = Workspace:FindFirstChild("Plots")
        if plots then
            local now = tick()
            local backup = _G.JAF_BaseRebuildInterval or 8
            for _, plot in ipairs(plots:GetChildren()) do
                local pn = plot.Name
                local c = _fpCache[pn]
                if (not c) or c.plotRef ~= plot or (now - c.t) > backup then
                    local mnx,mxx,mnz,mxz,floorY,roofY = _baseBoxFromXray(plot)
                    if not mnx then mnx,mxx,mnz,mxz,floorY,roofY = _baseBox(plot) end
                    if mnx then
                        _fpCache[pn] = { plotRef = plot, t = now, fp = { plot=pn, minX=mnx, maxX=mxx, minZ=mnz, maxZ=mxz, minY=floorY, maxY=roofY } }
                        if _G.JAF_FootprintIncremental ~= false then _publishFootprints() end
                    else
                        _fpCache[pn] = nil
                    end
                    RunService.Heartbeat:Wait()
                end end
            for pn, c in pairs(_fpCache) do if not (c.plotRef and c.plotRef.Parent) then _fpCache[pn] = nil end end
        end
        local fps = _publishFootprints()
        if not _mbDone and not _G.JAF_MapBoundsUserSet and #fps > 0 then
            local mb
            local bbMinX, bbMaxX, bbMinZ, bbMaxZ = math.huge, -math.huge, math.huge, -math.huge
            for _, f in ipairs(fps) do
                if f.minX then
                    bbMinX = math.min(bbMinX, f.minX); bbMaxX = math.max(bbMaxX, f.maxX)
                    bbMinZ = math.min(bbMinZ, f.minZ); bbMaxZ = math.max(bbMaxZ, f.maxZ)
                end end
            pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local map = Workspace:FindFirstChild("Map")
                if map then
                    local mnx, mxx, mnz, mxz = math.huge, -math.huge, math.huge, -math.huge
                    local found = false
                    for _, p in ipairs(map:GetDescendants()) do
                        if p:IsA("BasePart") then
                            local s = p.Size
                            if math.max(s.X, s.Z) > (_G.JAF_MapWallMinLen or 90) and math.min(s.X, s.Z) < 12 and s.Y > 6 then
                                local cf = p.CFrame
                                local wx = math.abs(cf.RightVector.X) * s.X + math.abs(cf.UpVector.X) * s.Y + math.abs(cf.LookVector.X) * s.Z
                                local wz = math.abs(cf.RightVector.Z) * s.X + math.abs(cf.UpVector.Z) * s.Y + math.abs(cf.LookVector.Z) * s.Z
                                local c = p.Position
                                mnx = math.min(mnx, c.X - wx / 2); mxx = math.max(mxx, c.X + wx / 2)
                                mnz = math.min(mnz, c.Z - wz / 2); mxz = math.max(mxz, c.Z + wz / 2)
                                found = true
                            end end end
                    if found then
                        local ins = _G.JAF_MapBoundsInset or 4
                        mb = { minX = mnx + ins, maxX = mxx - ins, minZ = mnz + ins, maxZ = mxz - ins }
                    end end end)
            local _lm = _G.JAF_MapBoundsLandingMargin or 4
            if mb and bbMinX < math.huge
               and mb.minX <= bbMinX and mb.maxX >= bbMaxX and mb.minZ <= bbMinZ and mb.maxZ >= bbMaxZ then
                mb.minX = math.min(mb.minX, bbMinX - _lm); mb.maxX = math.max(mb.maxX, bbMaxX + _lm)
                mb.minZ = math.min(mb.minZ, bbMinZ - _lm); mb.maxZ = math.max(mb.maxZ, bbMaxZ + _lm)
                _G.JAF_MapBounds = mb
                _mbDone = true
            else
                if not _G.JAF_MapBounds then _G.JAF_MapBounds = { minX = -545, maxX = -278, minZ = -150, maxZ = 268 } end
                _mbTries = _mbTries + 1
                if _mbTries >= (_G.JAF_MapBoundsMaxTries or 15) then _mbDone = true end
            end end
        local cur = _G.JAF_CurrentBase()
        _G.JAF_InBase = (cur ~= nil)
        task.wait(_G.JAF_BaseScanInterval or 1)
    end end)
local function enableXray() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    xrayEnabled = true
    installXrayHook()
    local _apply; _apply = (function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if xrayShouldApply(obj) then xraySpoof[obj] = 0 pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
obj.LocalTransparencyModifier = 0.5 end) end
    end)
    local _xrSweep; _xrSweep = function(list, checkEach) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local _cap = _G.JAF_XrayBatch or 300
        local _budgetMs = (_G.JAF_XrayBudgetMs or 4) / 1000
        local _n = 0
        local _t0 = os.clock()
        for _i = 1, #list do
            if checkEach and not xrayEnabled then return true end
            _apply(list[_i])
            _n = _n + 1
            if _n >= _cap or (os.clock() - _t0) >= _budgetMs then
                RunService.Heartbeat:Wait()
                if not xrayEnabled then return true end
                _n = 0
                _t0 = os.clock()
            end end
        return false
    end
    local _sweepBase; _sweepBase = function(base) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false)) _xrSweep(base:GetDescendants(), false) end
    local _xrPending, _xrScheduled = {}, false
    local _xrDrain; _xrDrain = (function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        _xrScheduled = false
        local _batch = _xrPending
        _xrPending = {}
        _xrSweep(_batch, true)
    end)
    if _xrayDescConn then _xrayDescConn:Disconnect() end
    _xrayDescConn = Workspace.DescendantAdded:Connect((function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        _xrPending[#_xrPending + 1] = obj
        if not _xrScheduled then _xrScheduled = true task.delay(_G.JAF_XraySettle or 0.05, _xrDrain) end
    end))
    local function _watchBase(base) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        _sweepBase(base)
        task.delay(0.2, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if xrayEnabled and base.Parent then _sweepBase(base) end end)
        task.delay(0.7, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if xrayEnabled and base.Parent then _sweepBase(base) end end)
        task.delay(1.5, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if xrayEnabled and base.Parent then _sweepBase(base) end end)
    end
    local function _watchPlots(plots) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _G._jafXrayPlotsChildConn then _G._jafXrayPlotsChildConn:Disconnect() end
        _G._jafXrayPlotsChildConn = plots.ChildAdded:Connect((function(base) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if xrayEnabled then _watchBase(base) end
        end))
        for _, base in ipairs(plots:GetChildren()) do _watchBase(base) end
    end
    local plots = Workspace:FindFirstChild("Plots")
    if plots then _watchPlots(plots) end
    if _G._jafXrayPlotsAddConn then _G._jafXrayPlotsAddConn:Disconnect() end
    _G._jafXrayPlotsAddConn = Workspace.ChildAdded:Connect((function(child) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if xrayEnabled and child.Name == "Plots" then _watchPlots(child) end
    end))
    _xrSweep(Workspace:GetDescendants(), false)
end
local function disableXray() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    xrayEnabled = false
    if _xrayDescConn then _xrayDescConn:Disconnect(); _xrayDescConn = nil end
    if _G._jafXrayPlotsChildConn then _G._jafXrayPlotsChildConn:Disconnect(); _G._jafXrayPlotsChildConn = nil end
    if _G._jafXrayPlotsAddConn   then _G._jafXrayPlotsAddConn:Disconnect();   _G._jafXrayPlotsAddConn   = nil end
    for obj in pairs(xraySpoof) do if obj and obj.Parent then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
obj.LocalTransparencyModifier = 0 end) end end
    xraySpoof = {}
end
if xrayEnabled then task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G.JAF_WaitHeavy(); enableXray() end) end
local _initPanel = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
local function _resetMoveToolsToBackpack(character) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not character then return end
    for _, ch in ipairs(character:GetChildren()) do if ch:IsA("Tool") then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
ch.Parent = LocalPlayer.Backpack end) end end
end
executeReset = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if not SAB_LIVE() then return end
    if _G.JAF_InstantReset and _G.JAF_InstantReset(50) then return end
    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
game:GetService("Players").RespawnTime = 0 end)
    if _G.tpAntiDieDisable then pcall(_G.tpAntiDieDisable) end
    local respawnConn
    respawnConn = LocalPlayer.CharacterAdded:Connect(function(newChar) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if respawnConn then respawnConn:Disconnect(); respawnConn = nil end
        task.defer(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
newChar:WaitForChild("Humanoid", 12) end)
            RunService.Heartbeat:Wait()
            if _G.tpAntiDieEnable then pcall(_G.tpAntiDieEnable) end
        end) end)
    local character = LocalPlayer.Character
    if not character then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
LocalPlayer:LoadCharacter() end); return end
    pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local function _hr() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return character:FindFirstChildOfClass("Humanoid"), character:FindFirstChild("HumanoidRootPart") end
        local humanoid, rootPart = _hr()
        if not (rootPart and humanoid) then return end
        _resetMoveToolsToBackpack(character)
        rootPart.CFrame = CFrame.new(0, 15000, 0)
        RunService.Heartbeat:Wait()
        _resetMoveToolsToBackpack(character)
        RunService.Heartbeat:Wait()
        humanoid, rootPart = _hr()
        if not (humanoid and rootPart) then return end
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
humanoid.Health = 0 end)
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
humanoid:ChangeState(Enum.HumanoidStateType.Dead) end)
        if humanoid.Health > 0 then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
humanoid:TakeDamage(humanoid.MaxHealth * 99) end) end
        if humanoid.Health > 0 then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
character:BreakJoints() end) end
        humanoid, rootPart = _hr()
        if humanoid and rootPart and humanoid.Health > 0 then
            pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                rootPart.AssemblyLinearVelocity = Vector3.zero
                rootPart.CFrame = CFrame.new(rootPart.Position.X, Workspace.FallenPartsDestroyHeight - 500, rootPart.Position.Z)
            end) end end)
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local _origChar = LocalPlayer.Character
        for _ = 1, 8 do
            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
LocalPlayer:LoadCharacter() end)
            task.wait(0.05)
            if LocalPlayer.Character and LocalPlayer.Character ~= _origChar then break end
        end end) end
_G.JAF_ExecuteReset = executeReset
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local function checkSteal(gui) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local txt = (gui:IsA("TextLabel") or gui:IsA("TextButton")) and gui.Text
        if not (txt and string.find(txt, "You stole")) then return end
        if not SAB_LIVE() then return end
        if not (Config.AutoKickOnSteal or Config.KickToPrivateOnSteal) then return end
        if Config.KickToPrivateOnSteal then
            if _G.JAF_KickToPrivateServer then _G.JAF_KickToPrivateServer() end
        elseif Config.AutoKickOnSteal then kickPlayer() end
    end
    PlayerGui.DescendantAdded:Connect(function(gui) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        checkSteal(gui)
        if gui:IsA("TextLabel") or gui:IsA("TextButton") then gui:GetPropertyChangedSignal("Text"):Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
checkSteal(gui) end) end
    end)
    for _, gui in ipairs(PlayerGui:GetDescendants()) do checkSteal(gui) end
end)
local function _toggleCarpet() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    carpetSpeedEnabled = not carpetSpeedEnabled
    setCarpetSpeed(carpetSpeedEnabled)
end
local function _clearPin() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if _G._activePinConn then _G._activePinConn:Disconnect(); _G._activePinConn = nil end
    if _G._activePlat and _G._activePlat.Parent then _G._activePlat:Destroy(); _G._activePlat = nil end
end
local _keyBinds = {
    {function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return safeKeyCode(Config.TpSettings.TpKey, Enum.KeyCode.T) end, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _G.JAF_CarpetBuyBusy and _G.JAF_CarpetBuyBusy() then
            if _G.JAF_CancelCarpetBuy then pcall(_G.JAF_CancelCarpetBuy) end
            return true
        end
        _clearPin(); pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.tpToBestBrainrot then _G.tpToBestBrainrot() end end)
    end},
    {function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return safeKeyCode(Config.TpSettings.CloneKey, Enum.KeyCode.V) end,
     function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_clearPin(); pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.instantClone then _G.instantClone() end end) end},
    {function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return safeKeyCode(Config.TpSettings.CarpetSpeedKey, Enum.KeyCode.Q) end, _toggleCarpet},
    {function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return safeKeyCode(Config.StealSpeedKey, Enum.KeyCode.Z) end,
     function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if SharedState.StealSpeedToggleFunc then SharedState.StealSpeedToggleFunc() end end},
    {function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return safeKeyCode(Config.ResetKey, Enum.KeyCode.X) end, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
executeReset() end},
    {function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return safeKeyCode(Config.AutoBuyKey, nil) end,
     function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_ToggleAutoBuy then _G.JAF_ToggleAutoBuy() end end},
    {function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return safeKeyCode(Config.RagdollSelfKey, Enum.KeyCode.R) end,
     function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_RagdollSelf then _G.JAF_RagdollSelf() end end},
    {function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return safeKeyCode(Config.KickKey, nil) end,
     function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_KickPlayer then _G.JAF_KickPlayer() end end},
    {function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return safeKeyCode(Config.DropBrainrotKey, nil) end,
     function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_DropBrainrot then pcall(_G.JAF_DropBrainrot) end end},
    {function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return safeKeyCode(Config.ClickToAPKeybind, nil) end,
     function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_ToggleClickToAP then _G.JAF_ToggleClickToAP() end end},
    {function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return safeKeyCode(Config.ProximityAPKeybind, nil) end,
     function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_ToggleProximityAP then _G.JAF_ToggleProximityAP() end end},
    {function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return safeKeyCode(Config.FloatKey, nil) end,
     function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_ToggleFloat then _G.JAF_ToggleFloat() end end},
}
UserInputService.InputBegan:Connect(function(input, processed) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if processed then return end
    local kc = input.KeyCode
    for _, b in ipairs(_keyBinds) do if kc == b[1]() and b[2]() then return end end
end)
_G.JAF_ToggleCarpetSpeed = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_toggleCarpet(); return carpetSpeedEnabled end
_G.JAF_CarpetSpeedOn = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return carpetSpeedEnabled == true end
_G.JAF_DoReset = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
executeReset() end
_G.JAF_RagdollSelf = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.runAdminCommand then _G.runAdminCommand(LocalPlayer, "ragdoll", true) end end)
end
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local function applyHides() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        for _, e in ipairs({
            {"XiAdminPanel",           Config.HideAdminPanel},
            {"AutoStealUI",            Config.HideAutoSteal},
            {"JustAFanWalkingBestBar", Config.HideWalkingBest},
        }) do
            if e[2] then local g = hubFind(e[1]); if g then g.Enabled = false end end
        end end
    for _, w in ipairs({1, 2, 3}) do task.wait(w); applyHides() end
end)
if Config.TpSettings.TpOnLoad then
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local function _hrpPos() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local c = LocalPlayer.Character
            local h = c and c:FindFirstChild("HumanoidRootPart")
            return h and h.Position
        end
        local w = 0
        repeat RunService.Heartbeat:Wait(); w = w + 1 until _G.tpToBestBrainrot or w > 120
        local cw = Config.TpSettings.CacheWait or 0
        if cw > 0 then task.wait(cw) end
        local waitStart = tick()
        local _poll = _G.JAF_FirstLoadPoll or 0.01
        local _lastScanned, _scanStallAt = -1, tick()
        repeat task.wait(_poll)
            local _ps = _G.JAF_PlotsScanned or 0
            if _ps ~= _lastScanned then _lastScanned = _ps; _scanStallAt = tick() end
            if _G.JAF_PreWarmGrapple ~= false and _G.JAF_GrapplePreFire
               and Config.TpSettings.TpMode == "grapple" and #SharedState.AllAnimalsCache > 0 then
                local _fp   = _G.JAF_BaseFootprints
                local _wc   = LocalPlayer.Character
                local _want = tonumber(_G.JAF_PlotsTotal); if not _want or _want <= 0 then _want = 8 end
                if game:IsLoaded() and _fp and #_fp >= _want
                   and _wc and _wc:FindFirstChild("HumanoidRootPart") and _wc:FindFirstChildOfClass("Humanoid") then
                    pcall(_G.JAF_GrapplePreFire, nil, false)
                end
            end
        until ((_G.JAF_PlotsTotal or 0) > 0 and (_G.JAF_PlotsScanned or 0) >= (_G.JAF_PlotsTotal or 0) and #SharedState.AllAnimalsCache > 0)
           or (#SharedState.AllAnimalsCache > 0 and (_G.JAF_PlotsTotal or 0) > 0
               and (_G.JAF_PlotsScanned or 0) >= (_G.JAF_PlotsTotal or 0) - (_G.JAF_FastFireMissing or 1)
               and (tick() - _scanStallAt) > (_G.JAF_FastFireStall or 0.5))
           or ((tick() - _scanStallAt) > (_G.JAF_ScanStallTime or 3) and #SharedState.AllAnimalsCache > 0)
           or tick()-waitStart > (_G.JAF_FirstLoadScanWait or 12)
        if _G.JAF_LoadReadyGate ~= false then
            local _rdT0, _rdCap = tick(), tonumber(_G.JAF_LoadReadyMax) or 8
            local _settle = tonumber(_G.JAF_LoadInvSettle) or 0.7
            local _nLast, _nAt, _sampAt = -1, tick(), 0
            local function _findTool(n) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if n == nil then return nil end
                local c = LocalPlayer.Character
                local t = c and c:FindFirstChild(n)
                if t then return t end
                local bp = LocalPlayer:FindFirstChildOfClass("Backpack")
                return (bp and bp:FindFirstChild(n)) or nil
            end
            local function _toolCount() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local n = 0
                local bp = LocalPlayer:FindFirstChildOfClass("Backpack")
                if bp then for _, ch in ipairs(bp:GetChildren()) do if ch:IsA("Tool") then n = n + 1 end end end
                local c = LocalPlayer.Character
                if c then for _, ch in ipairs(c:GetChildren()) do if ch:IsA("Tool") then n = n + 1 end end end
                return n
            end
            local function _flightTool() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if Config.TpSettings.TpMode == "grapple" then return _findTool("Grapple Hook") end
                local t = _findTool(Config.TpSettings.Tool)
                if t then return t end
                for _, n in ipairs(_G.JAF_CarpetNames or {}) do local a = _findTool(n); if a then return a end end
                return nil
            end
            local _why = "cap"
            while tick() - _rdT0 < _rdCap do
                local _c   = LocalPlayer.Character
                local _hrp = _c and _c:FindFirstChild("HumanoidRootPart")
                local _hum = _c and _c:FindFirstChildOfClass("Humanoid")
                if game:IsLoaded() and _hrp and _hum and _hum.Health > 0
                   and LocalPlayer:FindFirstChildOfClass("Backpack") then
                    local _t  = _flightTool()
                    local _ok = _t ~= nil
                    if _ok and _t.Name == "Grapple Hook" and _G.JAF_HookReady ~= false
                       and type(getconnections) == "function" then
                        local _cok, _cs = pcall(getconnections, _t.Activated)
                        _ok = _cok and _cs ~= nil and #_cs > 0
                    end
                    if _ok and _G.JAF_LoadWaitCloner ~= false and not _findTool("Quantum Cloner") then _ok = false end
                    if _ok then _why = "ready"; break end
                    if tick() - _sampAt >= 0.1 then
                        _sampAt = tick()
                        local _n = _toolCount()
                        if _n ~= _nLast then _nLast, _nAt = _n, tick() end
                        if _n > 0 and (tick() - _nAt) > _settle then _why = "settled"; break end
                    end end
                RunService.Heartbeat:Wait()
            end
            _G.JAF_LoadReadyWhy = _why
            _G.JAF_LoadReadyMs  = math.floor((tick() - _rdT0) * 1000)
        end
        if _G.JAF_WaitStableFps then
            local _need  = _G.JAF_FpsStableFrames or 8
            local _minDt = 1 / (_G.JAF_FpsStableThresh or 30)
            local _ok, _fpsStart, _worstDt = 0, tick(), 0
            while _ok < _need and tick() - _fpsStart < (_G.JAF_LoadTpFpsWait or 3) do
                local _dt = RunService.Heartbeat:Wait()
                if _dt > _worstDt then _worstDt = _dt end
                if _dt <= _minDt then _ok = _ok + 1 else _ok = 0 end
            end
            _G.JAF_JoinFpsWaitMs = math.floor((tick() - _fpsStart) * 1000)
            _G.JAF_JoinFpsOk     = (_ok >= _need)
            _G.JAF_JoinFpsWorst  = math.floor(_worstDt * 1000)
        end
        if _G.tpToBestBrainrot then
            SharedState.FirstLoadSteal = true
            local _seqCommitted = false
            for _try = 1, (_G.JAF_FirstLoadTries or 3) do
                if #SharedState.AllAnimalsCache == 0 then
                    local _w2 = tick()
                    repeat task.wait(0.2) until #SharedState.AllAnimalsCache > 0 or tick()-_w2 > 6
                end
                local _cw2 = tick()
                while tick() - _cw2 < (tonumber(_G.JAF_LoadCharWait) or 5) do
                    local _rc  = LocalPlayer.Character
                    local _rh  = _rc and _rc:FindFirstChild("HumanoidRootPart")
                    local _rhu = _rc and _rc:FindFirstChildOfClass("Humanoid")
                    if _rh and _rhu and _rhu.Health > 0 then break end
                    task.wait(0.05)
                end
                local _before = _hrpPos()
                local _sc = _seqCommitted
                pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G.tpToBestBrainrot(nil, _sc) end)
                if _G.JAF_TpStartedFlight then _seqCommitted = true end
                if _G.JAF_CancelTp then break end
                task.wait(0.3)
                local _after = _hrpPos()
                if _before and _after and (_after - _before).Magnitude > 20 then break end
                if _try < (_G.JAF_FirstLoadTries or 3) then task.wait(0.5) end
            end end
        _G.JAF_FirstLoadTpDone = true
    end)
else
    _G.JAF_FirstLoadTpDone = true
end
LocalPlayer:GetAttributeChangedSignal("Stealing"):Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if LocalPlayer:GetAttribute("Stealing") and Config.AutoUnlockOnSteal then task.defer(triggerClosestUnlock, nil, 19) end
end)
task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local oldSS = hubFind("StealSpeedUI")
    if oldSS then oldSS:Destroy() end
    local MIN_SPEED, MAX_SPEED = 5, 31
    local stealSpeedEnabled = false
    local STEAL_SPEED = math.clamp(math.floor((tonumber(Config.StealSpeed) or 20) + 0.5), MIN_SPEED, MAX_SPEED)
    if Config.StealSpeed ~= STEAL_SPEED then Config.StealSpeed = STEAL_SPEED; SaveConfig() end
    local stealConn, _sfForce, _sfAtt, _sfMassRef = nil, nil, nil, nil
    local _sfRayParams = RaycastParams.new()
    _sfRayParams.FilterType = Enum.RaycastFilterType.Exclude
    _sfRayParams.RespectCanCollide = true
    local _sfRayChar, _sfRayCam = nil, nil
    local function _sfRemove() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _sfForce then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_sfForce:Destroy() end); _sfForce = nil end
        if _sfAtt then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_sfAtt:Destroy() end); _sfAtt = nil end
    end
    local function _sfBuild(hrp) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        _sfRemove()
        _sfMassRef = nil
        _sfAtt = JUI.new("Attachment", hrp, "Name", "LVAttachment")
        _sfForce = JUI.new("LinearVelocity", hrp, "Name", "SpeedForce", "Attachment0", _sfAtt, "RelativeTo", Enum.ActuatorRelativeTo.World, "VelocityConstraintMode", Enum.VelocityConstraintMode.Vector, "ForceLimitMode", Enum.ForceLimitMode.PerAxis, "MaxAxesForce", Vector3.new(math.huge, 0, math.huge), "MaxForce", math.huge, "ReactionForceEnabled", true, "VectorVelocity", Vector3.zero, "Enabled", false)
    end
    local function doDisable() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        stealSpeedEnabled = false
        if stealConn then stealConn:Disconnect(); stealConn = nil end
        _sfRemove()
    end
    local function doEnable() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        stealSpeedEnabled = true
        if stealConn then stealConn:Disconnect(); stealConn = nil end
        stealConn = RunService.Heartbeat:Connect((function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            if not SAB_LIVE() then _sfRemove(); return end
            local char = LocalPlayer.Character
            if not char then _sfRemove(); return end
            if not LocalPlayer:GetAttribute("Stealing") then if _sfForce then _sfRemove() end return end
            if _G.JAF_DropFlingActive then if _sfForce then _sfForce.Enabled = false end return end
            local hum = char:FindFirstChildOfClass("Humanoid")
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if not hum or not hrp then return end
            if hum.Health <= 0 then if _sfForce then _sfRemove() end return end
            if not _sfForce or _sfForce.Parent ~= hrp then _sfBuild(hrp) end
            if hum.Sit or hum:GetState() == Enum.HumanoidStateType.Climbing then _sfForce.Enabled = false; return end
            if _G.JAF_StealSpeedFiniteForce ~= false then
                local m = hrp.AssemblyMass
                if not (m > 0) then m = 1 end
                if not _sfMassRef or math.abs(m - _sfMassRef) > _sfMassRef * 0.15 then
                    _sfMassRef = m
                    local f = m * Workspace.Gravity * (_G.JAF_StealSpeedForceMult or 6)
                    _sfForce.MaxAxesForce = Vector3.new(f, 0, f)
                end
            elseif _sfMassRef then
                _sfMassRef = nil
                _sfForce.MaxAxesForce = Vector3.new(math.huge, 0, math.huge)
            end
            local md = hum.MoveDirection
            local flat = Vector3.new(md.X, 0, md.Z)
            if flat.Magnitude <= 1e-3 then _sfForce.Enabled = false; return end
            local dir = flat.Unit
            local vel = dir * STEAL_SPEED
            if _G.JAF_StealSpeedWallSlide ~= false then
                local _cam = Workspace.CurrentCamera
                if _sfRayChar ~= char or _sfRayCam ~= _cam then
                    _sfRayChar, _sfRayCam = char, _cam
                    _sfRayParams.FilterDescendantsInstances = { char, _cam }
                end
                local hit = Workspace:Raycast(hrp.Position, dir * (_G.JAF_StealSpeedWallLook or 2.5), _sfRayParams)
                if hit and math.abs(hit.Normal.Y) < 0.5 then
                    local n = Vector3.new(hit.Normal.X, 0, hit.Normal.Z)
                    if n.Magnitude > 1e-3 then n = n.Unit local into = vel:Dot(n) if into < 0 then vel = vel - n * into end end
                end end
            if vel.Magnitude < 3 then
                _sfForce.Enabled = false
            else
                _sfForce.VectorVelocity = Vector3.new(vel.X, 0, vel.Z)
                _sfForce.Enabled = true
            end end)) end
    SharedState.applyStealSpeedValue = function(s) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        s = math.clamp(math.floor((tonumber(s) or STEAL_SPEED) + 0.5), MIN_SPEED, MAX_SPEED)
        STEAL_SPEED = s
        Config.StealSpeed = s
        SaveConfig()
        if SharedState._refreshMiniStealSpeedSlider then pcall(SharedState._refreshMiniStealSpeedSlider) end
    end
    SharedState.DisableStealSpeed = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
doDisable() end
    SharedState.StealSpeedToggleFunc = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if stealSpeedEnabled then doDisable() else doEnable() end
        return stealSpeedEnabled
    end
    SharedState.IsStealSpeedOn = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return stealSpeedEnabled end
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local lastHadSteal = nil
        while true do
            task.wait(0.3)
            if not Config.AutoStealSpeed then lastHadSteal = nil; continue end
            local hasSteal = (LocalPlayer:GetAttribute("Stealing") == true)
            if lastHadSteal == hasSteal then continue end
            lastHadSteal = hasSteal
            if hasSteal and not stealSpeedEnabled then doEnable()
            elseif not hasSteal and stealSpeedEnabled then doDisable() end
        end end) end)
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    _G.JAF_WaitHeavy()
    local brainrotESPEnabled = Config.BrainrotESP
    JUI.new("Folder", Workspace, "Name", "JustAFanBrainrotESP")
    local brainrotBillboards = {}
    local ESP_OFFSET  = Vector3.new(0, 1.8, 0)
    local ESP_BLACK   = Color3.fromRGB(0, 0, 0)
    local ESP_WHITE   = Color3.fromRGB(255, 255, 255)
    local MIN_ESP_GEN = 10000000
    local RANK_COLORS = { Color3.fromRGB(255, 215, 0), Color3.fromRGB(0, 200, 255), Color3.fromRGB(170, 90, 255) }
    local DEFAULT_ESP_COLOR = Color3.fromRGB(175, 175, 175)
    local function createBrainrotBillboard(data, color) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local bb = JUI.new("BillboardGui", nil, "Name", "BrainrotESP_" .. data.uid, "Size", UDim2.new(0, 160, 0, 38), "StudsOffset", ESP_OFFSET, "AlwaysOnTop", true, "LightInfluence", 0, "MaxDistance", 3000)
        local hasMut = data.mutation and data.mutation ~= "None" and data.mutation ~= "N/A"
        color = color or ESP_WHITE
        local container = JUI.new("Frame", bb, "Name", "Box", "Size", UDim2.new(1, 0, 1, 0), "BackgroundColor3", ESP_BLACK, "BackgroundTransparency", 0.5, "BorderSizePixel", 0)
        JUI.corner(container, 4)
        JUI.stroke(container, color, 1.5, 0.2)
        JUI.new("TextLabel", container, "Name", "Nm", "Size", UDim2.new(1, -6, 0, 18), "Position", UDim2.new(0, 3, 0, 2), "BackgroundTransparency", 1, "Font", Enum.Font.GothamBlack, "TextSize", 13, "TextColor3", color, "TextStrokeTransparency", 0, "TextStrokeColor3", ESP_BLACK, "Text", (data.name or data.petName) or "???", "TextXAlignment", Enum.TextXAlignment.Center)
        JUI.new("TextLabel", container, "Size", UDim2.new(1, -6, 0, 14), "Position", UDim2.new(0, 3, 0, 20), "BackgroundTransparency", 1, "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", ESP_WHITE, "TextStrokeTransparency", 0, "TextStrokeColor3", ESP_BLACK, "Text", data.genText or "", "TextXAlignment", Enum.TextXAlignment.Center)
        if hasMut then
            JUI.corner(JUI.new("TextLabel", bb, "Name", "Badge", "Size", UDim2.new(0, 60, 0, 14), "Position", UDim2.new(0.5, -30, 0, -16), "BackgroundColor3", color, "BackgroundTransparency", 0.3, "Font", Enum.Font.GothamBlack, "TextSize", 9, "TextColor3", ESP_WHITE, "TextStrokeTransparency", 0, "TextStrokeColor3", ESP_BLACK, "Text", data.mutation:upper()), 3)
        end
        return bb
    end
    local function cacheBillboardRefs(entry) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local bb = entry.bb; if not bb then return end
        local box = bb:FindFirstChild("Box")
        if box then entry.st = box:FindFirstChildOfClass("UIStroke"); entry.nm = box:FindFirstChild("Nm") end
        entry.badge = bb:FindFirstChild("Badge")
    end
    local function recolorEntry(entry, color) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if entry.color == color then return end
        entry.color = color
        if entry.st then entry.st.Color = color end
        if entry.nm then entry.nm.TextColor3 = color end
        if entry.badge then entry.badge.BackgroundColor3 = color end
    end
    local function refreshBrainrotESP() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not brainrotESPEnabled then return end
        local cache = SharedState.AllAnimalsCache
        if not cache or #cache == 0 then return end
        local rankByUid, rank = {}, 0
        for _, a in ipairs((SharedState.GetTop10 and SharedState.GetTop10()) or {}) do
            if a and a.uid and a.genValue and not a.isWalking and a.genValue >= MIN_ESP_GEN then
                rank = rank + 1; rankByUid[a.uid] = rank
                if rank >= 3 then break end
            end end
        local _hc = LocalPlayer.Character
        local _hh = _hc and _hc:FindFirstChild("HumanoidRootPart")
        local _rootPos = _hh and _hh.Position
        local seen = {}
        for _, data in ipairs(cache) do
            if data.genValue ~= nil and not data.isWalking and data.genValue >= MIN_ESP_GEN then
                seen[data.uid] = true
                local color = RANK_COLORS[rankByUid[data.uid]] or DEFAULT_ESP_COLOR
                local entry = brainrotBillboards[data.uid]
                if entry then
                    if entry.bb then recolorEntry(entry, color) end
                else
                    local _fag = data.plot and data.slot and _G.findAdorneeGlobal
                    local adornee = _fag and _fag(data)
                    if adornee and (not _rootPos or (adornee.Position - _rootPos).Magnitude <= 3000) then
                        local newEntry = {bb = _P(createBrainrotBillboard(data, color), "Adornee", adornee, "StudsOffset", ESP_OFFSET, "Parent", adornee), color = color}
                        cacheBillboardRefs(newEntry)
                        brainrotBillboards[data.uid] = newEntry
                    end end end end
        for uid, entry in pairs(brainrotBillboards) do
            if not seen[uid] then if entry.bb then entry.bb:Destroy() end; brainrotBillboards[uid] = nil end
        end end
    local function clearBrainrotESP() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        for _, e in pairs(brainrotBillboards) do if e.bb then e.bb:Destroy() end end
        brainrotBillboards = {}
    end
    espToggleRef.setFn = function(enabled) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        brainrotESPEnabled = enabled
        if enabled then pcall(refreshBrainrotESP) else clearBrainrotESP() end
    end
    for _, d in ipairs({
        {0.3, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(refreshBrainrotESP) end},
        {2,   function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if next(brainrotBillboards) == nil then clearBrainrotESP() end end},
    }) do
        task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            while true do
                task.wait(d[1])
                if brainrotESPEnabled then local cache = SharedState.AllAnimalsCache if cache and #cache > 0 then d[2]() end end
            end end) end end) end
_initPanel()
_initPanel = nil
task.spawn((function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
	local animPlaying = false
	local tracks = {}
	local clone, oldRoot, hip, connection
	local _invisSF, _invisSFAtt
	local _invisPoseAtt, _invisPoseBase, _invisPoseRestDY
		local invisRagdollACs, invisRagdollACChar
	local folderConnections = {}
	local serverGhosts = {}
	local lagbackCallCount = 0
	local lagbackWindowStart = 0
	local lastLagbackTime = 0
	local errorOrbActive = false
	local errorOrb = nil
	local function clearErrorOrb() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
		if errorOrb and errorOrb.Parent then errorOrb:Destroy() end
		errorOrb = nil; errorOrbActive = false
	end
	local createServerGhost; createServerGhost = (function(position) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
		local now = tick()
		if now - lastLagbackTime < 0.05 then return end
		lastLagbackTime = now
		if now - lagbackWindowStart > 1 then lagbackCallCount = 0; lagbackWindowStart = now end
		lagbackCallCount = lagbackCallCount + 1
		if lagbackCallCount >= 5 then
			if _G.AutoRecoverLagback and _G.invisibleStealEnabled and _G.toggleInvisibleSteal and not _G.RecoveryInProgress then
				_G.RecoveryInProgress = true
				task.defer(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
					pcall(_G.toggleInvisibleSteal)
					task.wait(0.3)
					_G.RecoveryInProgress = false
				end)
				return
			end end end)
	local function clearAllGhosts() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
		serverGhosts = {}; clearErrorOrb(); lagbackCallCount = 0; lastLagbackTime = 0
	end
	local function removeFolders() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
		local pf = Workspace:FindFirstChild(LocalPlayer.Name)
		if not pf then return end
		local dr = pf:FindFirstChild("DoubleRig")
		if dr then
			local rr = dr:FindFirstChild("HumanoidRootPart") or dr:FindFirstChildWhichIsA("BasePart")
			if rr then createServerGhost(rr.Position) end
			dr:Destroy()
		end
		local cs = pf:FindFirstChild("Constraints")
		if cs then cs:Destroy() end
		local conn = pf.ChildAdded:Connect(function(child) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
			if child.Name == "DoubleRig" then
				task.defer(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
					local rr = child:FindFirstChild("HumanoidRootPart") or child:FindFirstChildWhichIsA("BasePart")
					if rr then createServerGhost(rr.Position) end
					child:Destroy()
				end)
			elseif child.Name == "Constraints" then child:Destroy() end
		end)
		table.insert(folderConnections, conn)
	end
	local function doClone() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
		local character = LocalPlayer.Character
		if character and character:FindFirstChild("Humanoid") and character.Humanoid.Health > 0 then
			hip = character.Humanoid.HipHeight
			oldRoot = character:FindFirstChild("HumanoidRootPart")
			if not oldRoot or not oldRoot.Parent then return false end
			for _, c in pairs(oldRoot:GetChildren()) do if c:IsA("Attachment") and c.Name:find("Beam") then c:Destroy() end end
			for _, c in pairs(oldRoot:GetChildren()) do if c:IsA("Beam") then c:Destroy() end end
			local tmp = Instance.new("Model"); tmp.Parent = game
			character.Parent = tmp
			clone = oldRoot:Clone()
			for _, c in ipairs(clone:GetChildren()) do
				if c:IsA("LinearVelocity") or c:IsA("BodyVelocity") or c:IsA("VectorForce")
					or (c:IsA("Attachment") and (c.Name == "LVAttachment" or c.Name == "InvisLVAtt")) then
					pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
c:Destroy() end)
				end end
			clone.Parent = character
			clone.Anchored = false
			oldRoot.Parent = Workspace.CurrentCamera
			clone.CFrame = oldRoot.CFrame; character.PrimaryPart = clone
			character.Parent = Workspace
			for _, v in pairs(character:GetDescendants()) do
				if v:IsA("Weld") or v:IsA("Motor6D") or v:IsA("WeldConstraint") then
					if v.Part0 == oldRoot then v.Part0 = clone end
					if v.Part1 == oldRoot then v.Part1 = clone end
				end end
			_invisPoseAtt, _invisPoseBase, _invisPoseRestDY = nil, nil, nil
			pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
				local _a0 = clone:FindFirstChild("RootRigAttachment")
				local _lt = character:FindFirstChild("LowerTorso")
				local _a1 = _lt and _lt:FindFirstChild("RootRigAttachment")
				if _a0 and _a1 then _invisPoseAtt = _a0 _invisPoseBase = _a0.CFrame _invisPoseRestDY = _a0.CFrame.Position.Y - _a1.CFrame.Position.Y end
			end)
			tmp:Destroy(); return true
		end
		return false
	end
	local function revertClone() LPH_ATTRIBUTES(ERROR_HANDLING(false))
		local character = LocalPlayer.Character
		local g_hum = character and character:FindFirstChildOfClass("Humanoid")
		if not oldRoot or not oldRoot:IsDescendantOf(Workspace) or not character or not g_hum or g_hum.Health <= 0 then return end
		local tmp = Instance.new("Model"); tmp.Parent = game
		character.Parent = tmp
		oldRoot.Parent = character; character.PrimaryPart = oldRoot
		character.Parent = Workspace; oldRoot.CanCollide = true
		local _rootAC
		for _, v in pairs(character:GetDescendants()) do
			if v:IsA("Weld") or v:IsA("Motor6D") or v:IsA("WeldConstraint") then
				if v.Part0 == clone then v.Part0 = oldRoot end
				if v.Part1 == clone then v.Part1 = oldRoot end
			elseif v.Name == "Root" and v:IsA("AnimationConstraint") then
				_rootAC = v
			end end
		if clone then local p = clone.CFrame; clone:Destroy(); clone = nil; oldRoot.CFrame = p end
		_invisPoseAtt, _invisPoseBase, _invisPoseRestDY = nil, nil, nil
		pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
			local _rra = oldRoot and oldRoot:FindFirstChild("RootRigAttachment")
			if _rra and _rootAC and (_rootAC.Attachment0 == nil or _rootAC.Attachment0.Parent == nil) then _rootAC.Attachment0 = _rra end
		end)
		oldRoot = nil
		if character and character.Humanoid then character.Humanoid.HipHeight = hip end
			_G.JAF_InvisSurface = false
		clearAllGhosts()
	end
	local function animationTrickery() LPH_ATTRIBUTES(ERROR_HANDLING(false))
		if not (animPlaying and _G.invisibleStealEnabled) then return end
		local character = LocalPlayer.Character
		if character and character:FindFirstChild("Humanoid") and character.Humanoid.Health > 0 then
			local anim = Instance.new("Animation")
			anim.AnimationId = "http://www.roblox.com/asset/?id=18537363391"
			local humanoid = character.Humanoid
			local animator = humanoid:FindFirstChild("Animator") or Instance.new("Animator", humanoid)
			local animTrack = animator:LoadAnimation(anim)
			animTrack.Priority = Enum.AnimationPriority.Action4
			animTrack:Play(0, 1, 0); anim:Destroy()
			for _, t in ipairs(tracks) do pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
t:Stop() end) end
			table.clear(tracks)
			table.insert(tracks, animTrack)
			animTrack.Stopped:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if animPlaying and _G.invisibleStealEnabled then animationTrickery() end end)
			task.delay(0, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
				animTrack.TimePosition = 0.7
				task.delay(0.3, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if animTrack then animTrack:AdjustSpeed(math.huge) end end)
			end) end end
	local function turnOff() LPH_ATTRIBUTES(ERROR_HANDLING(false))
		animPlaying = false
		_G.invisibleStealEnabled = false
		_G.invisAutoActive = false
		for _, t in pairs(tracks) do pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
t:Stop() end) end
		tracks = {}
		do
			local _ch = LocalPlayer.Character
			local _hum = _ch and _ch:FindFirstChildOfClass("Humanoid")
			local _anr = _hum and _hum:FindFirstChildOfClass("Animator")
			if _anr then
				for _, ft in ipairs(_anr:GetPlayingAnimationTracks()) do
					if ft.Animation and tostring(ft.Animation.AnimationId):find("18537363391") then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
ft:AdjustSpeed(1); ft:Stop(0) end) end
				end end end
		if connection then connection:Disconnect(); connection = nil end
		if _invisSF then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_invisSF:Destroy() end); _invisSF = nil end
		if _invisSFAtt then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_invisSFAtt:Destroy() end); _invisSFAtt = nil end
		for _, c in ipairs(folderConnections) do if c then c:Disconnect() end end
		folderConnections = {}
		pcall(revertClone)
		local character = LocalPlayer.Character
		local humanoid = character and character:FindFirstChildOfClass("Humanoid")
		local hrp = character and (character.PrimaryPart or character:FindFirstChild("HumanoidRootPart"))
		if character then
			pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
				for _, p in ipairs(character:GetDescendants()) do if p:IsA("BasePart") then p.Anchored = false end end
			end) end
		if hrp then
			pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
				local pos = hrp.Position
				hrp.CFrame = CFrame.new(pos.X, pos.Y, pos.Z) * CFrame.Angles(0, hrp.Orientation.Y * math.pi / 180, 0)
				hrp.AssemblyLinearVelocity = Vector3.zero
				hrp.AssemblyAngularVelocity = Vector3.zero
			end) end
		if humanoid then
			pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
humanoid.PlatformStand = false end)
			pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
humanoid.Sit = false end)
			pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
humanoid.AutoRotate = true end)
			pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if humanoid.WalkSpeed <= 0 then humanoid.WalkSpeed = 16 end end)
			pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if humanoid.JumpPower <= 0 and humanoid.UseJumpPower then humanoid.JumpPower = 50 end end)
			pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
humanoid.PlatformStand = false end)
			pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
humanoid:ChangeState(Enum.HumanoidStateType.GettingUp) end)
			pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
humanoid:ChangeState(Enum.HumanoidStateType.Freefall) end)
		end
		pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
			local cam = Workspace.CurrentCamera
			if cam and humanoid then cam.CameraSubject = humanoid cam.CameraType = Enum.CameraType.Custom end
		end)
		pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
			local pm = LocalPlayer:FindFirstChild("PlayerScripts")
			if pm then pm = pm:FindFirstChild("PlayerModule") end
			if pm then require(pm):GetControls():Enable() end
		end)
		clearAllGhosts()
		if _G.updateMovementPanelInvisVisual then pcall(_G.updateMovementPanelInvisVisual, false) end
	end
	local function turnOn() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
		if not SAB_LIVE() then return end
		if animPlaying then return end
		local character = LocalPlayer.Character
		if not character then return end
		local humanoid = character:FindFirstChildOfClass("Humanoid")
		if not humanoid then return end
		animPlaying = true; _G.invisibleStealEnabled = true
		if _G.updateMovementPanelInvisVisual then pcall(_G.updateMovementPanelInvisVisual, true) end
		tracks = {}; removeFolders()
		local success = doClone()
		if success then
			if _G.JAF_InvisFreezeOnEngage ~= false then
				pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
					local pm = LocalPlayer:FindFirstChild("PlayerScripts")
					pm = pm and pm:FindFirstChild("PlayerModule")
					if pm then require(pm):GetControls():Enable() end
				end)
				local _fc = LocalPlayer.Character
				local _froot = _fc and (_fc.PrimaryPart or _fc:FindFirstChild("HumanoidRootPart"))
				local _fhum = _fc and _fc:FindFirstChildOfClass("Humanoid")
				if _froot then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_froot.AssemblyLinearVelocity = Vector3.zero; _froot.AssemblyAngularVelocity = Vector3.zero; _froot.Anchored = false end) end
				if oldRoot then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
oldRoot.AssemblyLinearVelocity = Vector3.zero; oldRoot.AssemblyAngularVelocity = Vector3.zero end) end
				if _fhum then
					pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_fhum:Move(Vector3.zero, false) end)
					pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _fhum.PlatformStand then _fhum.PlatformStand = false end end)
					pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _fhum.Sit then _fhum.Sit = false end end)
					pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _fhum.WalkSpeed <= 0 then _fhum.WalkSpeed = 16 end end)
				end end
				if _G.JAF_InvisFold ~= false then task.wait(0.05); animationTrickery() end
			task.defer(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
				if _G.resetBrainrotBeam then pcall(_G.resetBrainrotBeam) end
				if _G.resetPlotBeam then pcall(_G.resetPlotBeam) end
				task.wait(0.1)
				if _G.updateBrainrotBeam then pcall(_G.updateBrainrotBeam) end
				if _G.createPlotBeam then pcall(_G.createPlotBeam) end
			end)
			local lastSetPosition = nil; local skipFrames = 5
			local _invisFastMode = false; local _invisSlowAccum = 0
			local _invisRagGuard = function(_char, _hum) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
				if (LocalPlayer:GetAttribute("RagdollEndTime") or 0) > 0 then LocalPlayer:SetAttribute("RagdollEndTime", 0) end
				if _hum then
					if _hum.PlatformStand then _hum.PlatformStand = false end
					local _s = _hum:GetState()
					if _s == Enum.HumanoidStateType.Physics or _s == Enum.HumanoidStateType.Ragdoll or _s == Enum.HumanoidStateType.FallingDown then
						_hum:ChangeState(Enum.HumanoidStateType.GettingUp)
					end end
				if invisRagdollACChar ~= _char then
					invisRagdollACChar = _char; invisRagdollACs = {}
					for _, d in ipairs(_char:GetDescendants()) do if d:IsA("AnimationConstraint") then table.insert(invisRagdollACs, d) end end
				end
				for _, ac in ipairs(invisRagdollACs) do if ac.Parent and not ac.Enabled then ac.Enabled = true end end
			end
			connection = RunService.PreSimulation:Connect(function(_psDt) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
				local _invHum = character and character:FindFirstChildOfClass("Humanoid")
				if _invHum and _invHum.Health > 0 and oldRoot then
					local root = character.PrimaryPart or character:FindFirstChild("HumanoidRootPart")
					if root then
							if _G.JAF_InvisRagdollGuard ~= false then pcall(_invisRagGuard, character, _invHum) end
							if _G.JAF_InvisPoseFix ~= false and _invisPoseAtt and _invisPoseAtt.Parent and _invisPoseBase and _invisPoseRestDY then
								local _plt = character:FindFirstChild("LowerTorso")
								if _plt then
									local _appl = _invisPoseBase.Position.Y - _invisPoseAtt.CFrame.Position.Y
									local _lift = (_plt.Position.Y - root.Position.Y) + _appl - _invisPoseRestDY
									_lift = math.clamp(_lift, 0, tonumber(_G.JAF_InvisPoseMaxLift) or 8)
									if math.abs(_lift - _appl) > 0.01 then _invisPoseAtt.CFrame = _invisPoseBase * CFrame.new(0, -_lift, 0) end
								end end
							if skipFrames > 0 then skipFrames = skipFrames - 1; lastSetPosition = nil
						elseif lastSetPosition then
							local currentPos = oldRoot.Position
							local jumpDist = (currentPos - lastSetPosition).Magnitude
							local _lbDt = tonumber(_psDt) or (1/60)
							local _lbThresh = math.max(3, root.AssemblyLinearVelocity.Magnitude * _lbDt * 3 + 0.5)
							if jumpDist > _lbThresh and not _G.RecoveryInProgress then
								lastSetPosition = nil; createServerGhost(currentPos)
									if _G.AutoRecoverLagback and _G.toggleInvisibleSteal then
										_G.RecoveryInProgress = true
										task.defer(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
											pcall(_G.toggleInvisibleSteal); task.wait(0.5)
											pcall(_G.toggleInvisibleSteal); _G.RecoveryInProgress = false
										end) end end end
						if clone then clone.CanCollide = (_G.JAF_InvisCloneCollide ~= false) end
						if not oldRoot then return end
						for _, c in pairs(oldRoot:GetChildren()) do if c:IsA("Beam") or (c:IsA("Attachment") and c.Name:find("Beam")) then c:Destroy() end end
						local rotAngle = _G.InvisStealAngle or 180
						local sa = math.clamp(_G.SinkSliderValue or Config.SinkSliderValue or 2.5, 0.5, 10)
						local _rv = root.AssemblyLinearVelocity
						local _xzSpd = Vector3.new(_rv.X, 0, _rv.Z).Magnitude
						local _hyDt = tonumber(_psDt) or (1/60)
						if _invisFastMode then
							if _xzSpd < (_G.JAF_InvisFastExit or 17) then
								_invisSlowAccum = _invisSlowAccum + _hyDt
								if _invisSlowAccum > 0.15 then _invisFastMode = false end
							else
								_invisSlowAccum = 0
							end
						elseif _xzSpd > (_G.JAF_InvisFastEnter or 22) then
							_invisFastMode = true; _invisSlowAccum = 0
						end
						if _invisFastMode and _G.JAF_InvisSpeedFix ~= false and not _G.JAF_InvisSurface then
							if not _invisSF or _invisSF.Parent ~= oldRoot then
								if _invisSF then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_invisSF:Destroy() end) end
								if _invisSFAtt then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_invisSFAtt:Destroy() end) end
								_invisSFAtt = Instance.new("Attachment"); _invisSFAtt.Name = "InvisLVAtt"; _invisSFAtt.Parent = oldRoot
								_invisSF = Instance.new("LinearVelocity"); _invisSF.Name = "InvisSpeedForce"
								_P(_invisSF, "Attachment0", _invisSFAtt, "RelativeTo", Enum.ActuatorRelativeTo.World, "VelocityConstraintMode", Enum.VelocityConstraintMode.Vector, "ForceLimitMode", Enum.ForceLimitMode.PerAxis)
								_invisSF.MaxAxesForce = Vector3.new(math.huge, math.huge, math.huge)
								_invisSF.Parent = oldRoot
							end
							local _yv = math.clamp(((root.Position.Y - sa) - oldRoot.Position.Y) * (_G.JAF_InvisYGain or 25), -150, 150)
							local _gx = _G.JAF_InvisXZGain or 8
							local _vx = _rv.X + math.clamp((root.Position.X - oldRoot.Position.X) * _gx, -15, 15)
							local _vz = _rv.Z + math.clamp((root.Position.Z - oldRoot.Position.Z) * _gx, -15, 15)
							local _hv = Vector3.new(_vx, 0, _vz)
							local _mxz = _G.JAF_InvisMaxXZ or 65
							if _hv.Magnitude > _mxz then _hv = _hv.Unit * _mxz end
							_invisSF.VectorVelocity = Vector3.new(_hv.X, _yv, _hv.Z)
							_invisSF.Enabled = true
							oldRoot.CFrame = CFrame.new(oldRoot.Position) * root.CFrame.Rotation * CFrame.Angles(math.rad(rotAngle), 0, 0)
							oldRoot.CanCollide = false
						else
							if _invisSF and _invisSF.Enabled then _invisSF.Enabled = false end
							local cf = root.CFrame - Vector3.new(0, sa, 0)
							oldRoot.CFrame = cf * CFrame.Angles(math.rad(rotAngle), 0, 0)
							if _G.JAF_InvisSurface then oldRoot.CFrame = root.CFrame end
							oldRoot.AssemblyLinearVelocity = root.AssemblyLinearVelocity; oldRoot.CanCollide = false
						end
						lastSetPosition = _G.JAF_InvisSurface and nil or oldRoot.Position
					end end end) end end
	local function _invisOrphanInCamera() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
		local found = nil
		pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
			if Workspace.CurrentCamera then
				for _, c in ipairs(Workspace.CurrentCamera:GetChildren()) do
					if c:IsA("BasePart") and c.Name == "HumanoidRootPart" then found = c break end
				end end end)
		return found
	end
	local function _smartRevert() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
		local character = LocalPlayer.Character
		if not character then return false end
		local orphans = {}
		if Workspace.CurrentCamera then
			for _, c in ipairs(Workspace.CurrentCamera:GetChildren()) do
				if c:IsA("BasePart") and c.Name == "HumanoidRootPart" then table.insert(orphans, c) end
			end end
		local fakeHRP = character.PrimaryPart
		if not fakeHRP or fakeHRP.Name ~= "HumanoidRootPart" then fakeHRP = character:FindFirstChild("HumanoidRootPart") end
		if #orphans == 0 then return false end
		local realHRP = orphans[1]
		local ok = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
			local tmp = Instance.new("Model"); tmp.Parent = game
			local origParent = character.Parent
			character.Parent = tmp
			realHRP.Parent = character
			character.PrimaryPart = realHRP
			character.Parent = origParent or Workspace
			realHRP.CanCollide = true
			if fakeHRP and fakeHRP ~= realHRP then
				for _, v in pairs(character:GetDescendants()) do
					if v:IsA("Weld") or v:IsA("Motor6D") or v:IsA("WeldConstraint") then
						if v.Part0 == fakeHRP then v.Part0 = realHRP end
						if v.Part1 == fakeHRP then v.Part1 = realHRP end
					end end
				local p = fakeHRP.CFrame
				fakeHRP:Destroy()
				realHRP.CFrame = p
				pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
					local rra = realHRP:FindFirstChild("RootRigAttachment")
					if rra then
						for _, d in ipairs(character:GetDescendants()) do
							if d:IsA("AnimationConstraint") and d.Name == "Root" then
								if (d.Attachment0 == nil or d.Attachment0.Parent == nil) then d.Attachment0 = rra end
								break
							end end end end) end
			for i = 2, #orphans do
				pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false)) if orphans[i].Parent then orphans[i]:Destroy() end end)
			end
			local humanoid = character:FindFirstChildOfClass("Humanoid")
			if humanoid then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
humanoid:ChangeState(Enum.HumanoidStateType.GettingUp) end) end
		end)
		return ok
	end
	_G.toggleInvisibleSteal = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
		local invisLooksOn = animPlaying
			or _G.invisibleStealEnabled
			or (oldRoot and oldRoot.Parent ~= nil)
			or (clone and clone.Parent ~= nil)
			or (_invisOrphanInCamera() ~= nil)
		if invisLooksOn then
			pcall(turnOff)
			if _invisOrphanInCamera() then pcall(_smartRevert) end
			task.delay(0.25, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
				if _invisOrphanInCamera() then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
LocalPlayer:LoadCharacter() end) end
			end)
		else
			turnOn()
		end end
	task.defer(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
		task.wait(0.5)
		if _invisOrphanInCamera() then pcall(_smartRevert) end
	end)
	UserInputService.InputBegan:Connect(function(input) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
		if UserInputService:GetFocusedTextBox() then return end
		if _G.INVISIBLE_STEAL_KEY and input.KeyCode == _G.INVISIBLE_STEAL_KEY then pcall(_G.toggleInvisibleSteal) end
	end)
	local function onCharacterAdded(newChar) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
		clearErrorOrb(); clearAllGhosts(); lagbackCallCount = 0
		pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
for _, c in pairs(Workspace.CurrentCamera:GetChildren()) do if c:IsA("BasePart") and c.Name == "HumanoidRootPart" then c:Destroy() end end end)
		if oldRoot then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
oldRoot:Destroy() end); oldRoot = nil end
		if clone then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
clone:Destroy() end); clone = nil end
		animPlaying = false; _G.invisibleStealEnabled = false
		task.wait(0.2)
		local camera = Workspace.CurrentCamera
		if camera and newChar then
			local h = newChar:FindFirstChildOfClass("Humanoid")
			if h then camera.CameraSubject = h; camera.CameraType = Enum.CameraType.Custom end
		end end
    LocalPlayer.CharacterAdded:Connect(onCharacterAdded)
    local function setupDeathListener() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local ch = LocalPlayer.Character
        if ch then
            local h = ch:FindFirstChildOfClass("Humanoid")
            if h then h.Died:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                clearErrorOrb(); clearAllGhosts(); lagbackCallCount = 0
                if _G._jafForceRespawn then _G._jafForceRespawn("deathListener") end
            end) end
        end end
    setupDeathListener()
    LocalPlayer.CharacterAdded:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
task.wait(0.1); setupDeathListener() end)
end))
LocalPlayer.CharacterAdded:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false)) _G.invisAutoActive = false end)
local function _jaf_onOwnPlotSteal() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    local plots = Workspace:FindFirstChild("Plots")
    if not hrp or not plots then return false end
    local myPlot
    for _, plot in ipairs(plots:GetChildren()) do
        local sign = plot:FindFirstChild("PlotSign")
        if sign then
            local yb = sign:FindFirstChild("YourBase")
            if yb and yb:IsA("BillboardGui") and yb.Enabled then myPlot = plot; break end
        end end
    if not myPlot then return false end
    local sh = myPlot:FindFirstChild("StealHitbox")
    if not sh or not sh:IsA("BasePart") then return false end
    local rel = sh.CFrame:PointToObjectSpace(hrp.Position)
    local half = sh.Size * 0.5
    local m = 5
    return math.abs(rel.X) <= half.X + m and math.abs(rel.Y) <= half.Y + m and math.abs(rel.Z) <= half.Z + m
end
_G.JAF_OnOwnPlotSteal = _jaf_onOwnPlotSteal
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    task.wait(1)
    local lastStealing = LocalPlayer:GetAttribute("Stealing") == true
    local notStealTicks = 0
    local invisDelaySeq = 0
    while true do
        task.wait(0.1)
        if _G.AutoInvisDuringSteal ~= false and _G.toggleInvisibleSteal then
            local stealing = LocalPlayer:GetAttribute("Stealing") == true
            if stealing and not lastStealing then
                if not _G.invisibleStealEnabled and not _jaf_onOwnPlotSteal() then
                    local delayMs = (_G.JAF_InvisDelayEnabled and tonumber(_G.JAF_InvisDelayMs)) or 0
                    if delayMs > 0 then
                        invisDelaySeq = invisDelaySeq + 1
                        local mySeq = invisDelaySeq
                        task.delay(delayMs / 1000, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                            if mySeq ~= invisDelaySeq then return end
                            if _G.AutoInvisDuringSteal == false then return end
                            if LocalPlayer:GetAttribute("Stealing") ~= true then return end
                            if _G.invisibleStealEnabled then return end
                            pcall(_G.toggleInvisibleSteal)
                            _G.invisAutoActive = true
                        end)
                    else
                        pcall(_G.toggleInvisibleSteal)
                        _G.invisAutoActive = true
                    end end
            elseif (not stealing) and lastStealing then
                invisDelaySeq = invisDelaySeq + 1
                if _G.invisibleStealEnabled and _G.invisAutoActive then
                    pcall(_G.toggleInvisibleSteal)
                    if not _G.invisibleStealEnabled then _G.invisAutoActive = false end
                end end
            if (not stealing) and _G.invisibleStealEnabled then
                notStealTicks = notStealTicks + 1
                if (notStealTicks >= 4 and not _G.RecoveryInProgress) or notStealTicks >= 20 then
                    pcall(_G.toggleInvisibleSteal); _G.invisAutoActive = false; _G.RecoveryInProgress = false; notStealTicks = 0
                end
            else
                notStealTicks = 0
            end
            if (not stealing) and not _G.invisibleStealEnabled then
                local _ch = LocalPlayer.Character
                local _hum = _ch and _ch:FindFirstChildOfClass("Humanoid")
                local _anr = _hum and _hum:FindFirstChildOfClass("Animator")
                if _anr then
                    for _, ft in ipairs(_anr:GetPlayingAnimationTracks()) do
                        if ft.Animation and tostring(ft.Animation.AnimationId):find("18537363391") then
                            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
ft:AdjustSpeed(1); ft:Stop(0) end)
                        end end end end
            lastStealing = stealing
        end end end)
LocalPlayer:GetAttributeChangedSignal("Stealing"):Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    if _G.JAF_RootLinkGuard == false then return end
    if LocalPlayer:GetAttribute("Stealing") == true then return end
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local rootAC
        for _ = 1, (tonumber(_G.JAF_RootLinkFrames) or 24) do
            if LocalPlayer:GetAttribute("Stealing") == true then return end
            local c = LocalPlayer.Character
            local hrp = c and c:FindFirstChild("HumanoidRootPart")
            if hrp then
                if not (rootAC and rootAC.Parent and rootAC:IsDescendantOf(c)) then
                    rootAC = nil
                    for _, d in ipairs(c:GetDescendants()) do
                        if d.Name == "Root" and d:IsA("AnimationConstraint") then rootAC = d; break end
                    end end
                if rootAC and (rootAC.Attachment0 == nil or rootAC.Attachment0.Parent == nil) then
                    local rra = hrp:FindFirstChild("RootRigAttachment")
                    if rra then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
rootAC.Attachment0 = rra end) end
                end end
            RunService.Heartbeat:Wait()
        end end) end)
SharedState.ANTI_BEE_DISCO = {
    running = false,
    connections = {},
    originalMoveFunction = nil,
    controlsProtected = false,
    badLightingNames = { Blue = true, DiscoEffect = true, BeeBlur = true, ColorCorrection = true },
}
function SharedState.ANTI_BEE_DISCO.nuke(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not obj or not obj.Parent then return end
    if SharedState.ANTI_BEE_DISCO.badLightingNames[obj.Name] then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
obj:Destroy() end) end
end
function SharedState.ANTI_BEE_DISCO.disconnectAll() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    for _, conn in ipairs(SharedState.ANTI_BEE_DISCO.connections) do if typeof(conn) == "RBXScriptConnection" then conn:Disconnect() end end
    SharedState.ANTI_BEE_DISCO.connections = {}
end
function SharedState.ANTI_BEE_DISCO.protectControls() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if SharedState.ANTI_BEE_DISCO.controlsProtected then return end
    pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local PlayerScripts = LocalPlayer.PlayerScripts
        local PlayerModule = PlayerScripts:FindFirstChild("PlayerModule")
        if not PlayerModule then return end
        local Controls = require(PlayerModule):GetControls()
        if not Controls then return end
        local ab = SharedState.ANTI_BEE_DISCO
        if not ab.originalMoveFunction then ab.originalMoveFunction = Controls.moveFunction end
        local protectedMoveFunction; protectedMoveFunction = function(self, moveVector, relativeToCamera) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if ab.originalMoveFunction then ab.originalMoveFunction(self, moveVector, relativeToCamera) end
        end
        table.insert(ab.connections, RunService.Heartbeat:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if not ab.running or not Config.AntiBeeDisco then return end
            if Controls.moveFunction ~= protectedMoveFunction then Controls.moveFunction = protectedMoveFunction end
        end))
        Controls.moveFunction = protectedMoveFunction
        ab.controlsProtected = true
    end) end
function SharedState.ANTI_BEE_DISCO.restoreControls() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not SharedState.ANTI_BEE_DISCO.controlsProtected then return end
    pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local PlayerModule = LocalPlayer.PlayerScripts:FindFirstChild("PlayerModule")
        if not PlayerModule then return end
        local Controls = require(PlayerModule):GetControls()
        local ab = SharedState.ANTI_BEE_DISCO
        if Controls and ab.originalMoveFunction then Controls.moveFunction = ab.originalMoveFunction ab.controlsProtected = false end
    end) end
function SharedState.ANTI_BEE_DISCO.blockBuzzingSound() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local beeScript = LocalPlayer.PlayerScripts:FindFirstChild("Bee", true)
        if beeScript then
            local buzzing = beeScript:FindFirstChild("Buzzing")
            if buzzing and buzzing:IsA("Sound") then buzzing:Stop(); buzzing.Volume = 0 end
        end end) end
SharedState.ANTI_BEE_DISCO.normalizeControls = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if LocalPlayer:GetAttribute("InverseControls") then LocalPlayer:SetAttribute("InverseControls", false) end
    end) end
function SharedState.ANTI_BEE_DISCO.Enable() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local ab = SharedState.ANTI_BEE_DISCO
    if ab.running then return end
    ab.running = true
    for _, inst in ipairs(Lighting:GetDescendants()) do ab.nuke(inst) end
    table.insert(ab.connections, Lighting.DescendantAdded:Connect(function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not ab.running or not Config.AntiBeeDisco then return end
        ab.nuke(obj)
    end))
    ab.protectControls()
    ab.normalizeControls()
    ab._nextBuzzCheck = 0
    table.insert(ab.connections, RunService.Heartbeat:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not ab.running or not Config.AntiBeeDisco then return end
        ab.normalizeControls()
        local _now = tick()
        if _now >= ab._nextBuzzCheck then ab._nextBuzzCheck = _now + 0.5 ab.blockBuzzingSound() end
    end)) end
function SharedState.ANTI_BEE_DISCO.Disable() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local ab = SharedState.ANTI_BEE_DISCO
    if not ab.running then return end
    ab.running = false
    ab.restoreControls()
    ab.disconnectAll()
end
if Config.AntiBeeDisco then
    task.delay(1, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if SharedState.ANTI_BEE_DISCO.Enable then SharedState.ANTI_BEE_DISCO.Enable() end
    end) end
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    while true do
        local cam = Workspace.CurrentCamera
        local fov = Config.FOV
        if cam and fov and fov ~= cam.FieldOfView then cam.FieldOfView = fov end
        task.wait(0.1)
    end end)
task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local buildHUD; buildHUD = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local existing = hubFind("JustAFanStatusHUD")
        if existing then existing:Destroy() end
        if _G._jafHudHeartbeat then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G._jafHudHeartbeat:Disconnect() end); _G._jafHudHeartbeat = nil end
        local hudGui = Instance.new("ScreenGui")
        _P(hudGui, "Name", "JustAFanStatusHUD", "ResetOnSpawn", false, "DisplayOrder", 10, "Parent", HubGui)
        local main = Instance.new("Frame", hudGui)
        main.Name = "Main"
        main.Size = UDim2.new(0, 520, 0, 50)
        _P(main, "AnchorPoint", Vector2.new(0.5, 1), "Position", UDim2.new(0.5, 0, 1, -82), "BackgroundColor3", Theme.Background, "BackgroundTransparency", 0.40, "BorderSizePixel", 0)
        Instance.new("UICorner", main).CornerRadius = UDim.new(0, 12)
        local mainStroke = Instance.new("UIStroke", main)
        _P(mainStroke, "Color", Theme.Accent1, "Thickness", 1.3, "Transparency", 0.5)
        if Config.PanelScales then Config.PanelScales["JustAFanStatusHUD"] = nil end
        local _splitX = 322
        local hubName = JUI.new("TextLabel", main, "Size", UDim2.new(0, _splitX, 0, 26), "Position", UDim2.new(0, 0, 0, 5), "BackgroundTransparency", 1, "Text", "SammyDawg Public", "Font", Enum.Font.GothamBold, "TextSize", 24, "TextColor3", Theme.Accent1, "TextXAlignment", Enum.TextXAlignment.Center)
        local hubOwner = JUI.new("TextLabel", main, "Size", UDim2.new(0, _splitX, 0, 16), "Position", UDim2.new(0, 0, 0, 32), "BackgroundTransparency", 1, "Text", "discord.gg/sammydawg", "Font", Enum.Font.GothamBold, "TextSize", 15)
        hubOwner.TextColor3 = Color3.fromRGB(255, 255, 255)
        hubOwner.TextXAlignment = Enum.TextXAlignment.Center
        local div = JUI.new("Frame", main, "Size", UDim2.new(0, 1, 0, 30), "Position", UDim2.new(0, _splitX, 0.5, -15), "BackgroundColor3", Theme.Accent1, "BackgroundTransparency", 0.45, "BorderSizePixel", 0)
        local _rightW = 520 - _splitX
        local fpsLbl = JUI.new("TextLabel", main, "Size", UDim2.new(0, _rightW, 0, 15), "Position", UDim2.new(0, _splitX, 0, 9), "BackgroundTransparency", 1)
        fpsLbl.RichText = true; fpsLbl.Text = ""
        fpsLbl.Font = Enum.Font.GothamBold; fpsLbl.TextSize = 14
        fpsLbl.TextColor3 = Theme.TextPrimary
        fpsLbl.TextXAlignment = Enum.TextXAlignment.Center
        local pingLbl = JUI.new("TextLabel", main, "Size", UDim2.new(0, _rightW, 0, 15), "Position", UDim2.new(0, _splitX, 0, 26), "BackgroundTransparency", 1)
        pingLbl.RichText = true; pingLbl.Text = ""
        pingLbl.Font = Enum.Font.GothamBold; pingLbl.TextSize = 14
        pingLbl.TextColor3 = Theme.TextPrimary
        pingLbl.TextXAlignment = Enum.TextXAlignment.Center
        local fr2, ac3 = 0, 0
        _G._jafHudHeartbeat = RunService.Heartbeat:Connect((function(dt) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            fr2 += 1; ac3 += dt
            if ac3 >= 1 then
                local ping3 = math.floor(LocalPlayer:GetNetworkPing()*1000)
                local fc3 = fr2>=50 and "rgb(80,255,150)" or (fr2>=30 and "rgb(255,210,80)" or "rgb(255,80,80)")
                local pc3 = ping3<100 and "rgb(80,255,150)" or (ping3<200 and "rgb(255,210,80)" or "rgb(255,80,80)")
                local secRgb = _G.JAF_Rgb(Theme.TextSecondary)
                fpsLbl.Text  = string.format("<font color='%s'>FPS</font>   <font color='%s'><b>%d</b></font>",  secRgb, fc3, fr2)
                pingLbl.Text = string.format("<font color='%s'>PING</font>  <font color='%s'><b>%dms</b></font>", secRgb, pc3, ping3)
                fr2, ac3 = 0, 0
            end end))
        return hudGui
    end
    buildHUD()
    _G.jafRebuildStatusHUD = buildHUD
end)
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local old = hubFind("JustAFanWalkingBestBar")
    if old then old:Destroy() end
    if not Config.Positions.WalkingBestBar then Config.Positions.WalkingBestBar = { X = 0.5, Y = 0.86 } end
    local sg = Instance.new("ScreenGui")
    _P(sg, "Name", "JustAFanWalkingBestBar", "ResetOnSpawn", false, "DisplayOrder", 11)
    sg.Parent       = HubGui
    local pill = JUI.new("Frame", sg, "Name", "WalkingBestPill", "AnchorPoint", Vector2.new(0.5, 0), "Size", UDim2.new(0, 260, 0, 78))
    pill.Position          = UDim2.new(Config.Positions.WalkingBestBar.X, 0, Config.Positions.WalkingBestBar.Y, 0)
    MakeDraggable(pill, pill, "WalkingBestBar")
    _P(pill, "BackgroundColor3", Theme.Background, "BackgroundTransparency", 0.18, "BorderSizePixel", 0)
    pill.ClipsDescendants       = true
    Instance.new("UICorner", pill).CornerRadius = UDim.new(0, 10)
    local pillStroke = Instance.new("UIStroke", pill)
    _P(pillStroke, "Color", Theme.Accent1, "Thickness", 1.5, "Transparency", 0.3)
    local viewport = Instance.new("ViewportFrame", pill)
    _P(viewport, "Name", "Preview3D", "Size", UDim2.new(0, 70, 0, 70), "Position", UDim2.new(0, 4, 0, 4), "BackgroundColor3", Color3.fromRGB(15, 15, 22), "BackgroundTransparency", 0.3, "BorderSizePixel", 0)
    Instance.new("UICorner", viewport).CornerRadius = UDim.new(0, 8)
    local viewportStroke = Instance.new("UIStroke", viewport)
    _P(viewportStroke, "Color", Theme.Accent2, "Thickness", 1, "Transparency", 0.5)
    local worldModel = Instance.new("WorldModel", viewport)
    local previewCam = Instance.new("Camera", viewport)
    previewCam.FieldOfView = 32
    viewport.CurrentCamera = previewCam
    local headerLbl = JUI.new("TextLabel", pill, "Size", UDim2.new(1, -84, 0, 18), "Position", UDim2.new(0, 80, 0, 6), "BackgroundTransparency", 1, "Text", "BEST BRAINROT", "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", Theme.TextSecondary, "TextXAlignment", Enum.TextXAlignment.Left, "RichText", true, "TextTruncate", Enum.TextTruncate.AtEnd)
    local nameLbl = JUI.new("TextLabel", pill, "Size", UDim2.new(1, -84, 0, 18), "Position", UDim2.new(0, 80, 0, 25), "BackgroundTransparency", 1, "Text", "--", "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", Color3.fromRGB(255, 255, 255), "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
    nameLbl.RichText             = true
    local genLbl = JUI.new("TextLabel", pill, "Size", UDim2.new(1, -84, 0, 14), "Position", UDim2.new(0, 80, 0, 45), "BackgroundTransparency", 1, "Text", "", "Font", Enum.Font.GothamBold, "TextSize", 12)
    genLbl.TextColor3           = Theme.Accent1
    genLbl.TextXAlignment       = Enum.TextXAlignment.Left
    genLbl.RichText             = true
    genLbl.TextTruncate         = Enum.TextTruncate.AtEnd
    local locLbl = JUI.new("TextLabel", pill, "Size", UDim2.new(1, -84, 0, 14), "Position", UDim2.new(0, 80, 0, 61), "BackgroundTransparency", 1, "Text", "", "Font", Enum.Font.GothamMedium, "TextSize", 12, "TextColor3", Color3.fromRGB(255, 255, 255), "TextXAlignment", Enum.TextXAlignment.Left, "RichText", true, "TextTruncate", Enum.TextTruncate.AtEnd, "TextStrokeColor3", Color3.fromRGB(0, 0, 0), "TextStrokeTransparency", 0.45)
    local function _genTextOf(entry) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not entry then return "?" end
        if entry.genText and entry.genText ~= "" then return entry.genText end
        local v = entry.genValue or 0
        if v >= 1e9 then return string.format("$%.2fB/s", v / 1e9) end
        if v >= 1e6 then return string.format("$%.2fM/s", v / 1e6) end
        if v >= 1e3 then return string.format("$%.2fK/s", v / 1e3) end
        return string.format("$%d/s", math.floor(v))
    end
    local _lockLbls = {}
    local function _lockTextOf(plotName) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not plotName then return nil, nil end
        local lbl = _lockLbls[plotName]
        if not (lbl and lbl.Parent and lbl:IsDescendantOf(Workspace)) then
            lbl = nil
            local plots = Workspace:FindFirstChild("Plots")
            local plot = plots and plots:FindFirstChild(plotName)
            if plot then
                for _, d in ipairs(plot:GetDescendants()) do if d:IsA("TextLabel") and d.Name == "Locked" then lbl = d; break end end
            end
            _lockLbls[plotName] = lbl
        end
        if not lbl then return nil, nil end
        if lbl.Visible then
            local rt = lbl.Parent and lbl.Parent:FindFirstChild("RemainingTime")
            local t = rt and tostring(rt.Text) or ""
            if t ~= "" then return t, true end
            return "?", true
        end
        return nil, false
    end
    local function _animalModelFolders() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local rs = ReplicatedStorage
        local fs = {}
        local asc = rs:FindFirstChild("Controllers")
        asc = asc and asc:FindFirstChild("AssetStreamController")
        local cache = asc and asc:FindFirstChild("AssetCache")
        if cache then fs[#fs + 1] = cache end
        local a = rs:FindFirstChild("Animals"); if a then fs[#fs + 1] = a end
        local m = rs:FindFirstChild("Models"); m = m and m:FindFirstChild("Animals"); if m then fs[#fs + 1] = m end
        local s = rs:FindFirstChild("Assets"); s = s and s:FindFirstChild("Animals"); if s then fs[#fs + 1] = s end
        return fs
    end
    local function _findAnimalModel(name) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not name then return nil end
        local folders = _animalModelFolders()
        if #folders == 0 then return nil end
        local norm = (tostring(name) or ""):lower():gsub("[%s%-_]", "")
        for _, folder in ipairs(folders) do
            for _, ch in ipairs(folder:GetChildren()) do
                local cn = (ch.Name or ""):lower():gsub("[%s%-_]", "")
                if cn == norm then return ch end
            end end
        local _found
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local AData = require(ReplicatedStorage:WaitForChild("Datas"):WaitForChild("Animals"))
            for k, v in pairs(AData) do
                if type(v) == "table" and v.DisplayName and v.DisplayName:lower() == tostring(name):lower() then
                    for _, folder in ipairs(folders) do local m = folder:FindFirstChild(k) if m then _found = m; return end end
                end end end)
        if not _found and tostring(name) ~= "" and tonumber(name) == nil then
            pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local ctrls = ReplicatedStorage:FindFirstChild("Controllers")
                local ascMod = ctrls and ctrls:FindFirstChild("AssetStreamController")
                if ascMod then require(ascMod).requestAssets("m", { tostring(name) }) end
            end) end
        return _found
    end
    local _currentPreviewName = nil
    local _previewModel       = nil
    local _previewRotation    = 0
    local function _setPreview(name) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if name == _currentPreviewName and _previewModel and _previewModel.Parent then return end
        if name ~= _currentPreviewName then
            if _previewModel then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_previewModel:Destroy() end); _previewModel = nil end
            _currentPreviewName = name
        end
        if not name then return end
        local original = _findAnimalModel(name)
        if not original then return end
        local ok, clone = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return original:Clone() end)
        if not ok or not clone then return end
        for _, d in ipairs(clone:GetDescendants()) do
            if d:IsA("BasePart") then
                d.Anchored = true
                d.CanCollide = false
            elseif d:IsA("Script") or d:IsA("LocalScript") then
                d.Disabled = true
            end end
        clone:PivotTo(CFrame.new(0, 0, 0))
        clone.Parent = worldModel
        _previewModel = clone
        local cf, size = clone:GetBoundingBox()
        local maxDim = math.max(size.X, size.Y, size.Z)
        local dist = (maxDim / 2) / math.tan(math.rad(previewCam.FieldOfView / 2)) * 1.35
        local center = cf.Position
        previewCam.CFrame = CFrame.new( center + Vector3.new(dist * 0.7, dist * 0.4, dist * 0.7), center)
    end
    do
        local _rotAccum = 0
        local _spinPreview; _spinPreview = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if _previewModel and _previewModel.Parent then
                _previewModel:PivotTo(CFrame.new(0, 0, 0) * CFrame.Angles(0, _previewRotation, 0))
            end end
        RunService.Heartbeat:Connect(function(dt) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if not (_previewModel and _previewModel.Parent) then return end
            _previewRotation = _previewRotation + dt * 0.8
            _rotAccum = _rotAccum + dt
            if _rotAccum < 0.0333 then return end
            _rotAccum = 0
            pcall(_spinPreview)
        end) end
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        while pill.Parent do
            task.wait(0.5)
            if not sg.Enabled then continue end
            local _plist = PRIORITY_LIST
            local _usePrio = (Config.AutoTPPriority ~= false) and type(_plist) == "table"
            local _bestBase, _bestBaseGen, _bestRank = nil, -math.huge, math.huge
            for _, a in ipairs(SharedState.AllAnimalsCache or {}) do
                if a and a.genValue and a.owner ~= LocalPlayer.Name then
                    if _usePrio then
                        local rank = _G.JAF_PrioScore(a)
                        if rank < _bestRank or (rank == _bestRank and a.genValue > _bestBaseGen) then
                            _bestRank, _bestBaseGen, _bestBase = rank, a.genValue, a
                        end
                    elseif a.genValue > _bestBaseGen then
                        _bestBaseGen, _bestBase = a.genValue, a
                    end end end
            local function _rankOf(nm) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if not _usePrio or not nm then return math.huge end
                return _G.JAF_PrioScore(nm)
            end
            local _bestWalk, _bestWalkRank, _bestWalkGen = nil, math.huge, -math.huge
            for _, w in ipairs(SharedState.WalkingCache or {}) do
                if w and w.genValue then
                    local r = _rankOf(w.name)
                    if r < _bestWalkRank or (r == _bestWalkRank and w.genValue > _bestWalkGen) then
                        _bestWalkRank, _bestWalkGen, _bestWalk = r, w.genValue, w
                    end end end
            local _winner, _isCarpet
            local _carpetWins = _bestWalk and ( _bestWalkRank < _bestRank or (_bestWalkRank == _bestRank and _bestWalkGen > _bestBaseGen) )
            if _carpetWins then
                _winner = _bestWalk
                _isCarpet = true
            elseif _bestBase then
                _winner = _bestBase
                _isCarpet = false
            else
                _winner = _bestWalk
                _isCarpet = _bestWalk ~= nil
            end
            if _winner then
                if _isCarpet then
                    headerLbl.Text = "<font color='" .. _G.JAF_Rgb(Theme.Accent2) .. "'><b>CARPET BEST</b></font>"
                    pillStroke.Color = Theme.Accent2
                    pillStroke.Transparency = 0.1
                else
                    headerLbl.Text = "<font color='" .. _G.JAF_Rgb(Theme.Accent1) .. "'><b>BASE BEST</b></font>"
                    pillStroke.Color = Theme.Accent1
                    pillStroke.Transparency = 0.3
                end
                locLbl.Text = _isCarpet and "" or ("<font color='" .. _G.JAF_Rgb(Theme.TextSecondary) .. "'>Owner</font>  <b>" .. tostring(_winner.owner or "?") .. "</b>")
                local _mut = _winner.mutation
                local _mutTxt = ""
                if not _isCarpet then
                    local _ms = ""
                    if type(_mut) == "string" then _ms = (_mut:gsub("^%s*(.-)%s*$", "%1")) end
                    local _ml = _ms:lower()
                    if _ms ~= "" and _ml ~= "none" and _ml ~= "n/a" and _ml ~= "nil"
                        and _ml ~= "normal" and _ml ~= "default" then
                        local _mc = _G.JAF_MutColor(_ms)
                        _mutTxt = ("  <font color='rgb(%d,%d,%d)'><b>%s</b></font>"):format(
                            math.floor(_mc.R * 255 + 0.5), math.floor(_mc.G * 255 + 0.5),
                            math.floor(_mc.B * 255 + 0.5), _ms:upper())
                    else
                        _mutTxt = "  <font color='" .. _G.JAF_Rgb(Theme.TextSecondary) .. "'>mut: none</font>"
                    end end
                nameLbl.Text = (_winner.name or "?") .. _mutTxt
                local _lockTxt = ""
                if not _isCarpet then
                    local lt, locked = _lockTextOf(_winner.plot)
                    if locked then
                        _lockTxt = "   <font color='rgb(255,95,95)'><b>" .. lt .. "</b></font>"
                    elseif locked == false then
                        _lockTxt = "   <font color='rgb(90,230,110)'><b>UNLOCKED</b></font>"
                    end end
                local _rar = _winner.rarity or _G.JAF_RarityOf(_winner.name)
                local _rarTxt = ""
                if _rar and _rar ~= "" then
                    local _rc = _G.JAF_RarityColor(_rar)
                    _rarTxt = ("   <font color='rgb(%d,%d,%d)'><b>%s</b></font>"):format(
                        math.floor(_rc.R * 255 + 0.5), math.floor(_rc.G * 255 + 0.5),
                        math.floor(_rc.B * 255 + 0.5), tostring(_rar):upper())
                end
                genLbl.Text  = _genTextOf(_winner) .. _rarTxt .. _lockTxt
                pcall(_setPreview, _winner.name)
            else
                headerLbl.Text = "BEST BRAINROT"
                nameLbl.Text   = "no targets"
                genLbl.Text    = ""
                locLbl.Text    = ""
                pillStroke.Color       = Theme.Accent1
                pillStroke.Transparency = 0.3
                pcall(_setPreview, nil)
            end
            pill.BackgroundColor3 = Theme.Background
        end end) end)
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    _G.JAF_WaitHeavy()
    local playerESPEnabled = Config.PlayerESP
    local playerBillboards = {}
    local makePlayerBillboard; makePlayerBillboard = (function(player) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local bb = Instance.new("BillboardGui")
        _P(bb, "Name", "PlayerESP_"..tostring(player.UserId), "Size", UDim2.new(0, 100, 0, 20), "StudsOffsetWorldSpace", Vector3.new(0, 2.8, 0))
        bb.AlwaysOnTop = true; bb.LightInfluence = 0; bb.ResetOnSpawn = false
        local nameLbl = JUI.new("TextLabel", bb, "Size", UDim2.new(1,0,1,0), "BackgroundTransparency", 1, "Font", Enum.Font.GothamBlack, "TextSize", 13)
        _P(nameLbl, "TextColor3", Theme.Accent1, "TextXAlignment", Enum.TextXAlignment.Center, "TextStrokeTransparency", 0.4, "TextStrokeColor3", Color3.fromRGB(0,0,0), "Text", player.Name)
        return bb, nameLbl
    end)
    local function getHRP(player) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local char = player.Character; if not char then return nil end
        return char:FindFirstChild("HumanoidRootPart")
    end
    local createOrRefresh; createOrRefresh = (function(player) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if player == LocalPlayer then return end
        local hrp = getHRP(player); if not hrp then return end
        local hum = player.Character and player.Character:FindFirstChild("Humanoid")
        if hum then hum.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.None end
        local uid = player.UserId
        local entry = playerBillboards[uid]
        if not entry or not entry.bb or not entry.bb.Parent then
            if entry and entry.bb then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
entry.bb:Destroy() end) end
            local bb, nameLbl = makePlayerBillboard(player)
            bb.Adornee = hrp; bb.Parent = hrp
            playerBillboards[uid] = {bb=bb, nameLbl=nameLbl, player=player}
        else
            if entry.bb.Adornee ~= hrp then entry.bb.Adornee = hrp; entry.bb.Parent = hrp end
        end end)
    local function clearAll() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        for uid, entry in pairs(playerBillboards) do
            if entry.bb and entry.bb.Parent then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
entry.bb:Destroy() end) end
            local p = Players:GetPlayerByUserId(uid)
            if p and p.Character then
                local h = p.Character:FindFirstChild("Humanoid")
                if h then h.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.Viewer end
            end
            playerBillboards[uid] = nil
        end end
    playerESPToggleRef.setFn = function(enabled) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        playerESPEnabled = enabled
        if not enabled then clearAll() end
    end
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        while true do
            task.wait(0.5)
            if playerESPEnabled then
            for uid, entry in pairs(playerBillboards) do
                if not Players:GetPlayerByUserId(uid) then
                    if entry.bb and entry.bb.Parent then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
entry.bb:Destroy() end) end
                    playerBillboards[uid] = nil
                end end
            for _, player in ipairs(Players:GetPlayers()) do if player ~= LocalPlayer then pcall(createOrRefresh, player) end end
            end end end)
    local function _espWait() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        task.wait(0.5 + math.random() * (tonumber(_G.JAF_EspJitter) or 0.4))
    end
    Players.PlayerAdded:Connect(function(p) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        p.CharacterAdded:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            _espWait()
            if playerESPEnabled then pcall(createOrRefresh, p) end
        end) end)
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then
            p.CharacterAdded:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                _espWait()
                if playerESPEnabled then pcall(createOrRefresh, p) end
            end) end end end)
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    _G.JAF_WaitHeavy()
    local STEALER_COL = Color3.fromRGB(255, 55, 55)
    local OWNER_COL   = Color3.fromRGB(70, 165, 255)
    local state = {}
    local ownerCache = {}
    local function removeEsp(uid) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local s = state[uid]; if not s then return end
        if s.hl then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
s.hl:Destroy() end) end
        if s.bb then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
s.bb:Destroy() end) end
        state[uid] = nil
    end
    local function clearAll() LPH_ATTRIBUTES(ERROR_HANDLING(false))
for uid in pairs(state) do removeEsp(uid) end end
    local function ensureEsp(plr, kind, col, tagText) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local char = plr.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        if not char or not hrp then removeEsp(plr.UserId); return end
        local head = char:FindFirstChild("Head") or hrp
        local s = state[plr.UserId]
        if not s then s = {}; state[plr.UserId] = s end
        if not s.hl or s.hl.Parent ~= char then
            if s.hl then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
s.hl:Destroy() end) end
            local hl = Instance.new("Highlight")
            hl.Name = "JAF_StealBaseESP"; hl.Adornee = char
            hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            hl.FillTransparency = 0.6; hl.OutlineTransparency = 0
            hl.Parent = char
            s.hl = hl
        end
        s.hl.FillColor = col; s.hl.OutlineColor = col
        if not s.bb or s.bb.Parent ~= char then
            if s.bb then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
s.bb:Destroy() end) end
            local bb = JUI.new("BillboardGui", nil, "Name", "JAF_StealBaseTag", "Size", UDim2.new(0, 116, 0, 22), "StudsOffsetWorldSpace", Vector3.new(0, 3.4, 0), "AlwaysOnTop", true, "LightInfluence", 0, "ResetOnSpawn", false, "MaxDistance", 100000, "Adornee", head, "Parent", char)
            local bg = JUI.new("Frame", bb, "Size", UDim2.new(1,0,1,0), "BackgroundColor3", Color3.fromRGB(18,18,22), "BackgroundTransparency", 0.15, "BorderSizePixel", 0)
            Instance.new("UICorner", bg).CornerRadius = UDim.new(1,0)
            local stroke = JUI.new("UIStroke", bg, "Thickness", 1.5, "Transparency", 0.1)
            local lbl = JUI.new("TextLabel", bg, "Size", UDim2.new(1,-8,1,0), "Position", UDim2.new(0,4,0,0), "BackgroundTransparency", 1, "Font", Enum.Font.GothamBlack, "TextSize", 12, "TextXAlignment", Enum.TextXAlignment.Center)
            s.bb = bb; s.tag = lbl; s.stroke = stroke
        end
        s.bb.Adornee = head
        s.tag.Text = tagText; s.tag.TextColor3 = col
        if s.stroke then s.stroke.Color = col end
        s.kind = kind
    end
    local function resolvePlotOwner(plot) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local c = ownerCache[plot.Name]
        if c and (tick() - c.t) < 3 then return c.plr or nil end
        local found = nil
        local sign = plot:FindFirstChild("PlotSign")
        if sign then
            local sg = sign:FindFirstChildWhichIsA("SurfaceGui", true)
            local lbl = sg and sg:FindFirstChildWhichIsA("TextLabel", true)
            if lbl then
                local text = (lbl.Text or ""):lower()
                for _, p in ipairs(Players:GetPlayers()) do
                    if p ~= LocalPlayer then
                        local dn = (p.DisplayName or ""):lower()
                        local un = (p.Name or ""):lower()
                        if (dn ~= "" and text:find(dn,1,true)) or (un ~= "" and text:find(un,1,true)) then found = p; break end
                    end end end end
        if not found and _G.JAF_NoSynchronizer ~= true then
            local Sync = _G.JAF_GetSynchronizer()
            if Sync then
                local okCh, ch = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Sync:Get(plot.Name) end)
                if okCh and ch then
                    local okO, owner = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return ch:Get("Owner") end)
                    if okO and owner then found = _G.JAF_OwnerPlayer(owner) end
                end end end
        ownerCache[plot.Name] = { plr = found or false, t = tick() }
        return found
    end
    local function nearestBaseOwner(pos) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local plots = Workspace:FindFirstChild("Plots")
        if not plots then return nil end
        local bestName, bestD = nil, math.huge
        local fps = _G.JAF_BaseFootprints
        if fps and #fps > 0 then
            for _, f in ipairs(fps) do
                if f.minX then
                    local cx, cz = (f.minX + f.maxX)/2, (f.minZ + f.maxZ)/2
                    local dx, dz = pos.X - cx, pos.Z - cz
                    local d = dx*dx + dz*dz
                    if d < bestD then bestD = d; bestName = f.plot end
                end end end
        if not bestName then return nil end
        local plotInst = plots:FindFirstChild(bestName)
        if not plotInst then return nil end
        return resolvePlotOwner(plotInst)
    end
    Players.PlayerRemoving:Connect(function(p) LPH_ATTRIBUTES(ERROR_HANDLING(false))
removeEsp(p.UserId) end)
    while true do
        local sOn = Config.StealerESP == true
        local oOn = Config.BaseOwnerESP == true
        if not sOn and not oOn then
            if next(state) then clearAll() end
            task.wait(0.5)
        else
            local ownerPlr = nil
            if oOn then
                local mc = LocalPlayer.Character
                local myHrp = mc and mc:FindFirstChild("HumanoidRootPart")
                if myHrp then ownerPlr = nearestBaseOwner(myHrp.Position) end
            end
            local desired = {}
            for _, p in ipairs(Players:GetPlayers()) do
                if p ~= LocalPlayer then
                    if sOn and p:GetAttribute("Stealing") then
                        desired[p.UserId] = { p = p, kind = "stealer", col = STEALER_COL, text = "STEALER" }
                    elseif oOn and ownerPlr and p == ownerPlr then
                        desired[p.UserId] = { p = p, kind = "owner", col = OWNER_COL, text = "BASE OWNER" }
                    end end end
            for uid in pairs(state) do if not desired[uid] then removeEsp(uid) end end
            for _, d in pairs(desired) do ensureEsp(d.p, d.kind, d.col, d.text) end
            task.wait(0.4)
        end end end)
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    _G.JAF_WaitHeavy()
    if Config.TurretESP == nil then Config.TurretESP = true end
    if Config.TrapESP == nil then Config.TrapESP = true end
    local HESP_KINDS = {
        turret = { color = Color3.fromRGB(255, 40, 40),   text = "⚠ TURRET ⚠",        cfg = "TurretESP" },
        trap   = { color = Color3.fromRGB(255, 150, 20),  text = "⚠ TRAP ⚠",          cfg = "TrapESP"   },
        mine   = { color = Color3.fromRGB(167, 142, 255), text = "⚠ SUBSPACE MINE ⚠", cfg = "SubspaceMineESP" },
    }
    local hazardESPData = {}
    local sfind = string.find
    local TRAP_PATS = { "trap", "mine", "hive" }
    local hazardKind; hazardKind = (function(name) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local ln = name:lower()
        if sfind(ln, "sentrybullet", 1, true) then return nil end
        if sfind(ln, "tripmine", 1, true) then return "mine" end
        if sfind(ln, "sentry", 1, true) then return "turret" end
        for _, p in ipairs(TRAP_PATS) do if sfind(ln, p, 1, true) then return "trap" end end
        return nil
    end)
    local createHazardESP; createHazardESP = (function(target, kind) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local spec = HESP_KINDS[kind]
        local labelText = spec.text
        if kind == "mine" then
            local owner = target.Name:match("SubspaceTripmine(.+)")
            if owner then local pl = Players:FindFirstChild(owner) labelText = "⚠ " .. ((pl and pl.DisplayName) or owner) .. "'s MINE ⚠" end
        end
        local hl
        if target:IsA("Model") then
            hl = Instance.new("Highlight")
            _P(hl, "Name", "JAF_HazardESP_HL", "Adornee", target, "FillColor", spec.color, "FillTransparency", 0.25, "OutlineColor", Color3.fromRGB(255, 255, 255), "OutlineTransparency", 0)
            hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            hl.Parent = target
        else
            hl = Instance.new("SelectionBox")
            _P(hl, "Name", "JAF_HazardESP_HL", "Adornee", target, "Color3", spec.color, "LineThickness", 0.12, "SurfaceColor3", spec.color, "SurfaceTransparency", 0.6, "Parent", target)
        end
        local bbAd = target:IsA("BasePart") and target or target:FindFirstChildWhichIsA("BasePart", true) or target
        local bb = Instance.new("BillboardGui")
        bb.Name = "JAF_HazardESP_Label"
        _P(bb, "Adornee", bbAd, "Size", UDim2.new(0, 200, 0, 44), "StudsOffset", Vector3.new(0, 6, 0), "AlwaysOnTop", true, "Parent", target)
        local lbl = JUI.new("TextLabel", bb, "Size", UDim2.new(1, 0, 1, 0), "BackgroundTransparency", 1, "Text", labelText, "TextColor3", spec.color, "TextStrokeColor3", Color3.fromRGB(0, 0, 0), "TextStrokeTransparency", 0, "Font", Enum.Font.GothamBold, "TextScaled", true)
        return { hl = hl, bb = bb, kind = kind }
    end)
    local function destroyEntry(obj, data) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if data.hl and data.hl.Parent then data.hl:Destroy() end
        if data.bb and data.bb.Parent then data.bb:Destroy() end
        hazardESPData[obj] = nil
    end
    local refreshHazardESP; refreshHazardESP = (function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local kindOn = { turret = Config.TurretESP ~= false, trap   = Config.TrapESP ~= false, mine   = Config.TrapESP ~= false, }
        local current = {}
        if kindOn.turret or kindOn.trap or kindOn.mine then
            local _hz = workspace:GetDescendants()
            local _isaCN = _G.JAF_IsACN
            local _cap = _G.JAF_HazardBatch or 400
            local _budgetMs = (_G.JAF_XrayBudgetMs or 4) / 1000
            local _hn, _ht0 = 0, os.clock()
            for _hi = 1, #_hz do
                local inst = _hz[_hi]
                _hn = _hn + 1
                if _hn >= _cap or (os.clock() - _ht0) >= _budgetMs then RunService.Heartbeat:Wait() _hn = 0 _ht0 = os.clock() end
                local _cn = inst.ClassName
                if _cn == "Model" or _isaCN(_cn, inst, "BasePart") then
                    local kind = hazardKind(inst.Name)
                    if kind and kindOn[kind] then
                        local par, skip = inst, false
                        if inst:FindFirstChildWhichIsA("Humanoid", true) then skip = true end
                        while not skip and par and par ~= workspace do
                            if Players:GetPlayerFromCharacter(par)
                            or (par.ClassName == "Model" and par:FindFirstChildOfClass("Humanoid")) then
                                skip = true; break
                            end
                            if par ~= inst and hazardKind(par.Name) then skip = true; break end
                            par = par.Parent
                        end
                        if not skip then
                            current[inst] = true
                            local data = hazardESPData[inst]
                            if data and data.kind ~= kind then destroyEntry(inst, data); data = nil end
                            if not data then hazardESPData[inst] = createHazardESP(inst, kind) end
                        end end end end end
        for obj, data in pairs(hazardESPData) do if not current[obj] or not obj.Parent then destroyEntry(obj, data) end end
    end)
    while true do task.wait(tonumber(_G.JAF_HazardESPEvery) or 1.5) pcall(refreshHazardESP) end
end)
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local priorityGui = Instance.new("ScreenGui")
    _P(priorityGui, "Name", "PriorityListGUI", "ResetOnSpawn", false, "Enabled", false)
    priorityGui.DisplayOrder = 1600000000
    local function _pgEnsureParent() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if priorityGui.Parent then return true end
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
priorityGui.Parent = HubGui end)
        return priorityGui.Parent ~= nil
    end
    _pgEnsureParent()
    local Packages = _G.JAF_WaitPackages()
    local Datas = ReplicatedStorage:WaitForChild("Datas")
    local AnimalsData = require(Datas:WaitForChild("Animals"))
    local function getPetsByRarity(rarityName) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local petList = {}
        for petName, data in pairs(AnimalsData) do
            if data.Rarity == rarityName and not petName:find("Lucky Block") then table.insert(petList, petName) end
        end
        table.sort(petList)
        return petList
    end
    local secretPets = {}
    local _seen = {}
    for _, rar in ipairs({"Secret", "OG"}) do
        for _, nm in ipairs(getPetsByRarity(rar)) do
            if not _seen[nm:lower()] then _seen[nm:lower()]=true; table.insert(secretPets,nm) end
        end end
    table.sort(secretPets)
    local mutRoster = _G.JAF_MutRoster or {}
    local function _mutMod(nm) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        for _, m in ipairs(mutRoster) do if m.name == nm then return m.mod end end
        return 0
    end
    local function _mutModText(nm) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local mod = _mutMod(nm)
        if mod == 0 then return "" end
        return string.format("x%g", 1 + mod)
    end
    local function _mutColor(nm) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false)) return _G.JAF_MutColor((tostring(nm):gsub("%s+", ""))) end
    local _pgMode = (Config.PriorityLastTab == "mutation") and "mutation" or "brainrot"
    local function _curList() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return (_pgMode == "mutation") and MUTATION_LIST or PRIORITY_LIST end
    local function _idxOf(l, n) LPH_ATTRIBUTES(ERROR_HANDLING(false))
for i2, v in ipairs(l) do if v == n then return i2 end end end
    local function _curSave() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _pgMode == "mutation" then saveMutationPriorityToConfig() else savePriorityToConfig() end
    end
    local _petBaseGen = {}
    do
        for petName, data in pairs(AnimalsData) do
            if data.Rarity == "Secret" or data.Rarity == "OG" then
                local idx = data.Index or petName
                local okG, gv = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _G.JAF_SafeGen(idx, nil, nil, nil) end)
                local g = (okG and tonumber(gv)) or 0
                _petBaseGen[petName] = g
                if data.DisplayName then _petBaseGen[data.DisplayName] = g end
            end end end
    local priorityGenFilter = _G.JAF_ParseGen(Config.PriorityGenFilter or "")
    if type(Config.KnownSecrets) ~= "table" then Config.KnownSecrets = {} end
    if type(Config.KnownSecretRarities) ~= "table" then Config.KnownSecretRarities = {} end
    do
        if next(Config.KnownSecrets) ~= nil and Config.KnownSecretRarities.Secret == nil then Config.KnownSecretRarities.Secret = true end
        local _changed = false
        for _, rar in ipairs({ "Secret", "OG" }) do
            if not Config.KnownSecretRarities[rar] then
                for _, nm in ipairs(getPetsByRarity(rar)) do Config.KnownSecrets[nm] = true end
                Config.KnownSecretRarities[rar] = true
                _changed = true
            end end
        if _changed then pcall(SaveConfig) end
    end
    if type(Config.NewSecretsBatch) ~= "table" then Config.NewSecretsBatch = {} end
    do
        local _rosterSet, _batchSet = {}, {}
        for _, nm in ipairs(secretPets) do _rosterSet[nm] = true end
        for _, nm in ipairs(Config.NewSecretsBatch) do _batchSet[nm] = true end
        local _fresh = {}
        for _, nm in ipairs(secretPets) do if not Config.KnownSecrets[nm] and not _batchSet[nm] then _fresh[#_fresh + 1] = nm end end
        if #_fresh > 0 then
            for _, nm in ipairs(Config.NewSecretsBatch) do Config.KnownSecrets[nm] = true end
            Config.NewSecretsBatch = _fresh
            pcall(SaveConfig)
        else
            local _kept = {}
            for _, nm in ipairs(Config.NewSecretsBatch) do if _rosterSet[nm] then _kept[#_kept + 1] = nm end end
            if #_kept ~= #Config.NewSecretsBatch then Config.NewSecretsBatch = _kept; pcall(SaveConfig) end
        end end
    local newSecrets = {}
    for _, nm in ipairs(Config.NewSecretsBatch) do newSecrets[nm] = true end
    local function _markSecretsSeen() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        for _, nm in ipairs(secretPets) do Config.KnownSecrets[nm] = true end
        Config.NewSecretsBatch = {}
        pcall(SaveConfig)
    end
    local mainFrame = JUI.new("Frame", priorityGui, "Size", UDim2.new(0, 650, 0, 600), "AnchorPoint", Vector2.new(0.5, 0.5), "Position", UDim2.new(0.5, 0, 0.5, 0), "BackgroundColor3", Theme.Background, "BackgroundTransparency", 0.05, "BorderSizePixel", 0, "ClipsDescendants", true)
    JUI.corner(mainFrame, 12)
    CreateGradient(JUI.stroke(mainFrame, Theme.Accent2, 1.5, 0.4))
    local header = JUI.new("Frame", mainFrame, "Size", UDim2.new(1, 0, 0, 40), "BackgroundTransparency", 1)
    MakeDraggable(header, mainFrame, nil)
    JUI.new("TextLabel", header, "Size", UDim2.new(0, 88, 1, 0), "Position", UDim2.new(0, 15, 0, 0), "BackgroundTransparency", 1, "Text", "PRIORITY", "Font", Enum.Font.GothamBold, "TextSize", 16, "TextColor3", Theme.TextPrimary, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
    local function _hoverLift(btn, stroke, tint) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        btn.MouseEnter:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
stroke.Transparency = 0.2; if tint then btn.TextColor3 = Theme.TextPrimary end end)
        btn.MouseLeave:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
stroke.Transparency = 0.6; if tint then btn.TextColor3 = Theme.TextSecondary end end)
    end
    local tabBrainrotBtn, tabMutationBtn
    local _pgApplyMode
    do
        local wrap = JUI.new("Frame", header, "Size", UDim2.new(0, 208, 0, 26), "Position", UDim2.new(0, 112, 0.5, 0), "AnchorPoint", Vector2.new(0, 0.5), "BackgroundColor3", Theme.Surface, "BorderSizePixel", 0)
        JUI.corner(wrap, 8); JUI.stroke(wrap, Theme.Accent2, 1, 0.6)
        local function _tab(txt, xoff, mode) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local b = JUI.new("TextButton", wrap, "Size", UDim2.new(0, 100, 0, 22), "Position", UDim2.new(0, xoff, 0.5, 0), "AnchorPoint", Vector2.new(0, 0.5), "BackgroundColor3", Theme.SurfaceHighlight, "Text", txt, "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", Theme.TextSecondary, "BorderSizePixel", 0, "AutoButtonColor", false)
            JUI.corner(b, 6)
            b.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _pgApplyMode then _pgApplyMode(mode) end end)
            return b
        end
        tabBrainrotBtn = _tab("BRAINROTS", 3, "brainrot")
        tabMutationBtn = _tab("MUTATIONS", 105, "mutation")
    end
    local closeBtn = JUI.new("TextButton", header, "Size", UDim2.new(0, 80, 0, 30), "Position", UDim2.new(1, -95, 0.5, 0), "AnchorPoint", Vector2.new(0, 0.5), "BackgroundColor3", Theme.Error, "Text", "CLOSE", "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", _G.JAF_OnAccent())
    JUI.corner(closeBtn, 6)
    closeBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
priorityGui.Enabled = false; _markSecretsSeen() end)
    local contentFrame = JUI.new("Frame", mainFrame, "Size", UDim2.new(1, -30, 1, -145), "Position", UDim2.new(0, 15, 0, 50), "BackgroundTransparency", 1)
    local function _mkScroll(size, pos) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local s = JUI.new("ScrollingFrame", contentFrame, "Size", size, "Position", pos, "BackgroundColor3", Theme.Surface, "BorderSizePixel", 0, "ScrollBarThickness", 4, "ScrollBarImageColor3", Theme.Accent1, "CanvasSize", UDim2.new(0, 0, 0, 0), "AutomaticCanvasSize", Enum.AutomaticSize.Y)
        JUI.corner(s, 8)
        JUI.new("UIPadding", s, "PaddingTop", UDim.new(0, 5), "PaddingLeft", UDim.new(0, 5), "PaddingRight", UDim.new(0, 5), "PaddingBottom", UDim.new(0, 5))
        return s
    end
    local availableLabel = JUI.new("TextLabel", contentFrame, "Size", UDim2.new(0.45, 0, 0, 25), "Position", UDim2.new(0, 0, 0, 0), "BackgroundTransparency", 1, "Text", "AVAILABLE SECRET / OG BRAINROTS", "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", Theme.TextPrimary)
    local availableScroll = _mkScroll(UDim2.new(0.45, 0, 1, -58), UDim2.new(0, 0, 0, 58))
    JUI.new("UIListLayout", availableScroll, "Padding", UDim.new(0, 5), "SortOrder", Enum.SortOrder.Name)
    local priorityLabel = JUI.new("TextLabel", contentFrame, "Size", UDim2.new(0.45, 0, 0, 25), "Position", UDim2.new(0.55, 0, 0, 0), "BackgroundTransparency", 1, "Text", "PRIORITY LIST", "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", Theme.TextPrimary)
    local priorityScroll = _mkScroll(UDim2.new(0.45, 0, 1, -30), UDim2.new(0.55, 0, 0, 30))
    JUI.new("UIListLayout", priorityScroll, "Padding", UDim.new(0, 5))
    local priorityButtons, availableButtons = {}, {}
    local refreshAvailableList
    local availableFilter = ""
    local function _resetRows(scroll, bucket) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local keep = scroll and scroll.CanvasPosition
        for _, btn in pairs(bucket) do if btn and btn.Parent then btn:Destroy() end end
        if keep and keep.Y > 0 then
            task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                for _ = 1, 3 do
                    RunService.Heartbeat:Wait()
                    if not (scroll and scroll.Parent) then return end
                    scroll.CanvasPosition = keep
                end end) end end
    local function _mkBox(size, pos, txt, ph) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local b = JUI.new("TextBox", contentFrame, "Size", size, "Position", pos, "BackgroundColor3", Theme.Surface, "BorderSizePixel", 0, "Text", txt, "PlaceholderText", ph, "Font", Enum.Font.Gotham, "TextSize", 12, "TextColor3", Theme.TextPrimary, "PlaceholderColor3", Theme.TextSecondary, "ClearTextOnFocus", false, "TextXAlignment", Enum.TextXAlignment.Left, "ZIndex", 3)
        JUI.corner(b, 6)
        JUI.new("UIPadding", b, "PaddingLeft", UDim.new(0, 8))
        return b
    end
    local searchBox = _mkBox(UDim2.new(0.26, 0, 0, 26), UDim2.new(0, 0, 0, 28), "", "Search to add...")
    searchBox:GetPropertyChangedSignal("Text"):Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        availableFilter = (searchBox.Text:lower():gsub("^%s*(.-)%s*$", "%1"))
        if refreshAvailableList then refreshAvailableList() end
    end)
    local priGenBox = _mkBox(UDim2.new(0.17, 0, 0, 26), UDim2.new(0.28, 0, 0, 28), tostring(Config.PriorityGenFilter or ""), "Min Gen/s")
    priGenBox.FocusLost:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        Config.PriorityGenFilter = priGenBox.Text
        priorityGenFilter = _G.JAF_ParseGen(priGenBox.Text)
        pcall(SaveConfig)
        if refreshAvailableList then refreshAvailableList() end
    end)
    local function _mkFootBtn(w, xoff, txt) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local b = JUI.new("TextButton", mainFrame, "Size", UDim2.new(0, w, 0, 35), "Position", UDim2.new(0, xoff, 1, -45), "BackgroundColor3", Theme.SurfaceHighlight, "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", Theme.TextSecondary, "BorderSizePixel", 0, "AutoButtonColor", false, "Text", txt, "Visible", false)
        JUI.corner(b, 6)
        return b
    end
    local resetOrderBtn = _mkFootBtn(130, 120, "RESET ORDER")
    _hoverLift(resetOrderBtn, JUI.stroke(resetOrderBtn, Theme.Accent2, 1, 0.6), true)
    local mutOnOffBtn = _mkFootBtn(140, 392, "USE MUTATIONS: ON")
    _hoverLift(mutOnOffBtn, JUI.stroke(mutOnOffBtn, Theme.Accent2, 1, 0.6), false)
    local refreshPriorityList; refreshPriorityList = (function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        _resetRows(priorityScroll, priorityButtons)
        priorityButtons = {}
        local _mob = _G.JAF_MobileOn()
        local ROW_H, BTN, REM_W = _mob and 46 or 35, _mob and 32 or 25, _mob and 38 or 35
        local POS_W, POS_H, NAME_X, NAME_CUT = _mob and 34 or 30, _mob and 32 or 26, _mob and 50 or 44, _mob and 176 or 154
        local X_REM, X_DOWN, X_UP = _mob and -46 or -38, _mob and -84 or -70, _mob and -122 or -100
        local LIST = _curList()
        local _isMut = (_pgMode == "mutation")
        local _acts = _isMut
            and { { BTN, X_DOWN, Theme.Accent1, "^", 14, -1 }, { BTN, X_REM, Theme.Accent1, "v", 14, 1 } }
            or  { { BTN, X_UP, Theme.Accent1, "^", 14, -1 }, { BTN, X_DOWN, Theme.Accent1, "v", 14, 1 }, { REM_W, X_REM, Theme.Error, "X", 12, 0 } }
        for i, petName in ipairs(LIST) do
            local itemFrame = JUI.new("Frame", nil, "Size", UDim2.new(1, -10, 0, ROW_H), "BackgroundColor3", Theme.SurfaceHighlight, "BorderSizePixel", 0)
            JUI.corner(itemFrame, 6)
            itemFrame.Parent = priorityScroll
            if _isMut then
                JUI.corner(JUI.new("Frame", itemFrame, "Size", UDim2.new(0, 3, 1, -10), "Position", UDim2.new(0, 0, 0.5, 0), "AnchorPoint", Vector2.new(0, 0.5), "BackgroundColor3", _mutColor(petName), "BorderSizePixel", 0), 2)
            end
            local posBox = JUI.new("TextBox", itemFrame, "Size", UDim2.new(0, POS_W, 0, POS_H), "Position", UDim2.new(0, 6, 0.5, 0), "AnchorPoint", Vector2.new(0, 0.5), "BackgroundColor3", Theme.Surface, "BorderSizePixel", 0, "Text", tostring(i), "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", Theme.TextPrimary, "ClearTextOnFocus", true, "TextXAlignment", Enum.TextXAlignment.Center)
            JUI.corner(posBox, 4)
            JUI.stroke(posBox, Theme.Accent1, 1, 0.45)
            posBox.FocusLost:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local cur, target = _idxOf(LIST, petName), tonumber(posBox.Text)
                if cur and target then
                    target = math.clamp(math.floor(target), 1, #LIST)
                    if target ~= cur then
                        table.insert(LIST, target, table.remove(LIST, cur))
                        _curSave(); refreshPriorityList(); refreshAvailableList()
                        return
                    end end
                posBox.Text = tostring(cur or i)
            end)
            local nameLabel = JUI.new("TextLabel", itemFrame, "Size", UDim2.new(1, -NAME_CUT, 1, 0), "Position", UDim2.new(0, NAME_X, 0, 0), "BackgroundTransparency", 1, "Text", petName, "Font", Enum.Font.GothamMedium, "TextSize", 12, "TextColor3", Theme.TextPrimary, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
            if _isMut then
                local _mt = _mutModText(petName)
                nameLabel.RichText = true
                nameLabel.Text = "<font color='" .. _G.JAF_Rgb(_mutColor(petName)) .. "'><b>" .. petName .. "</b></font>"
                    .. ((_mt ~= "") and ("  <font color='" .. _G.JAF_Rgb(Theme.TextSecondary) .. "'>" .. _mt .. "</font>") or "")
            end
            local function _apply() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_curSave(); refreshPriorityList(); refreshAvailableList() end
            for _, d in ipairs(_acts) do
                local step = d[6]
                local b = JUI.new("TextButton", itemFrame, "Size", UDim2.new(0, d[1], 0, BTN), "Position", UDim2.new(1, d[2], 0.5, 0), "AnchorPoint", Vector2.new(0, 0.5), "BackgroundColor3", d[3], "Text", d[4], "Font", Enum.Font.GothamBold, "TextSize", d[5], "TextColor3", _G.JAF_OnAccent())
                JUI.corner(b, 4)
                b.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    local c = _idxOf(LIST, petName)
                    if not c then return end
                    if step == 0 then table.remove(LIST, c); _apply(); return end
                    if c + step >= 1 and c + step <= #LIST then LIST[c], LIST[c + step] = LIST[c + step], LIST[c]; _apply() end
                end) end
            priorityButtons[#priorityButtons + 1] = itemFrame
        end end)
    refreshAvailableList = (function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        _resetRows(availableScroll, availableButtons)
        availableButtons = {}
        _G.JAF_AvailToken = (_G.JAF_AvailToken or 0) + 1
        local _myTok = _G.JAF_AvailToken
        local _bi = 0
        local LIST = _curList()
        if _pgMode == "mutation" then return end
        local _amob = _G.JAF_MobileOn()
        local A_ROW_H, A_BTN_W, A_BTN_H = (_amob and 42 or 30), (_amob and 48 or 40), (_amob and 32 or 25)
        for _ri, petName in ipairs(secretPets) do
            if availableFilter ~= "" and not petName:lower():find(availableFilter, 1, true) then continue end
            if priorityGenFilter > 0 and (_petBaseGen[petName] or 0) < priorityGenFilter then continue end
            local isNew = newSecrets[petName] == true
            local itemFrame = JUI.new("Frame", nil, "Name", (isNew and "0_" or "1_") .. petName, "LayoutOrder", _ri, "Size", UDim2.new(1, -10, 0, A_ROW_H), "BackgroundColor3", Theme.SurfaceHighlight, "BorderSizePixel", 0)
            JUI.corner(itemFrame, 6)
            itemFrame.Parent = availableScroll
            JUI.new("TextLabel", itemFrame, "Size", UDim2.new(1, -(A_BTN_W + 15), 1, 0), "Position", UDim2.new(0, 10, 0, 0), "BackgroundTransparency", 1, "RichText", true, "Text", isNew and ("<font color='rgb(170,255,40)'><b>NEW</b></font>  " .. petName) or petName, "Font", Enum.Font.GothamMedium, "TextSize", 11, "TextColor3", Theme.TextPrimary, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
            local inPri = false
            for _, pName in ipairs(LIST) do if pName:lower() == petName:lower() then inPri = true; break end end
            local addBtn = JUI.new("TextButton", itemFrame, "Size", UDim2.new(0, A_BTN_W, 0, A_BTN_H), "Position", UDim2.new(1, -(A_BTN_W + 5), 0.5, 0), "AnchorPoint", Vector2.new(0, 0.5), "BackgroundColor3", inPri and Theme.Error or Theme.Success, "Text", inPri and "REM" or "ADD", "Font", Enum.Font.GothamBold, "TextSize", 10, "TextColor3", _G.JAF_OnAccent())
            JUI.corner(addBtn, 4)
            addBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if inPri then
                    local c
                    for i, pName in ipairs(LIST) do if pName:lower() == petName:lower() then c = i; break end end
                    if not c then return end
                    table.remove(LIST, c)
                else
                    table.insert(LIST, petName)
                end
                _curSave(); refreshPriorityList(); refreshAvailableList()
            end)
            availableButtons[#availableButtons + 1] = itemFrame
            _bi = _bi + 1
            if _bi % 12 == 0 then RunService.Heartbeat:Wait() if _myTok ~= _G.JAF_AvailToken then return end end
        end end)
    SharedState.RefreshPriorityGUI = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
refreshAvailableList(); refreshPriorityList() end) end
    priorityGui:GetPropertyChangedSignal("Enabled"):Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if priorityGui.Enabled then SharedState.RefreshPriorityGUI() end end)
    local saveBtn = JUI.new("TextButton", mainFrame, "Size", UDim2.new(0, 120, 0, 35), "Position", UDim2.new(0.5, -60, 1, -45), "BackgroundColor3", Theme.Success, "Text", "SAVE PRIORITY", "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", _G.JAF_OnAccent())
    JUI.corner(saveBtn, 6)
    saveBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        _curSave()
        local _isMut = (_pgMode == "mutation")
        local successLabel = JUI.new("TextLabel", mainFrame, "Size", UDim2.new(0, 200, 0, 30), "Position", UDim2.new(0.5, -100, 1, -80), "BackgroundColor3", Theme.Success, "Text", (_isMut and "Mutation List Saved! (" or "Priority List Saved! (") .. #_curList() .. (_isMut and " mutations)" or " pets)"), "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", _G.JAF_OnAccent(), "TextXAlignment", Enum.TextXAlignment.Center)
        JUI.corner(successLabel, 6)
        task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
task.wait(2); if successLabel and successLabel.Parent then successLabel:Destroy() end end)
    end)
    local priImportBox
    do
        local box = JUI.new("Frame", mainFrame, "Size", UDim2.new(1, -30, 0, 36), "Position", UDim2.new(0, 15, 1, -88), "BackgroundColor3", Color3.fromRGB(28, 44, 66), "BorderSizePixel", 0)
        JUI.corner(box, 8)
        JUI.stroke(box, Color3.fromRGB(90, 160, 255), 1.6, 0.1)
        JUI.new("UIPadding", box, "PaddingLeft", UDim.new(0, 8), "PaddingRight", UDim.new(0, 8), "PaddingTop", UDim.new(0, 4), "PaddingBottom", UDim.new(0, 4))
        priImportBox = JUI.new("TextBox", box, "Size", UDim2.new(1, 0, 1, 0), "BackgroundTransparency", 1, "Font", Enum.Font.Gotham, "TextSize", 12, "TextColor3", Color3.fromRGB(235, 245, 255), "PlaceholderText", "Paste a priority list code here, then press IMPORT LIST ->", "PlaceholderColor3", Color3.fromRGB(140, 180, 235), "Text", "", "ClearTextOnFocus", false, "TextXAlignment", Enum.TextXAlignment.Left, "TextYAlignment", Enum.TextYAlignment.Center, "ClipsDescendants", true)
    end
    local function _priBtn(txt, xpos, color) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local b = JUI.new("TextButton", mainFrame, "Size", UDim2.new(0, 100, 0, 35), "Position", xpos, "BackgroundColor3", color, "Text", txt, "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", _G.JAF_OnAccent(), "BorderSizePixel", 0)
        JUI.corner(b, 6)
        return b
    end
    _priBtn("EXPORT LIST", UDim2.new(0, 12, 1, -45), Theme.Accent1).MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local isMut = (_pgMode == "mutation")
        local payload = isMut and { t = "mut", v = MUTATION_LIST } or PRIORITY_LIST
        local ok, json = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return HttpService:JSONEncode(payload) end)
        if not ok or not json then return end
        if setclipboard then pcall(setclipboard, json) end
        if writefile then pcall(writefile, isMut and "SammyDawg_mutation_export.json" or "SammyDawg_priority_export.json", json) end
        if priImportBox then priImportBox.Text = json end
    end)
    _priBtn("IMPORT LIST", UDim2.new(1, -112, 1, -45), Theme.Accent2).MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local raw = priImportBox and priImportBox.Text or ""
        if raw == "" and getclipboard then local okc, c = pcall(getclipboard); if okc and type(c) == "string" then raw = c end end
        if raw == "" then return end
        local ok, decoded = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return HttpService:JSONDecode(raw) end)
        if not ok or type(decoded) ~= "table" then return end
        local isMut = (_pgMode == "mutation")
        if (decoded.t == "mut") ~= isMut then return end
        local arr = decoded
        if isMut then arr = decoded.v end
        if type(arr) ~= "table" then return end
        local newList = {}
        for _, v in ipairs(arr) do if type(v) == "string" then newList[#newList + 1] = v end end
        if #newList == 0 then return end
        if isMut then
            local seen, merged, inRoster = {}, {}, {}
            for _, m in ipairs(mutRoster) do inRoster[m.name] = true end
            for _, v in ipairs(newList) do if inRoster[v] and not seen[v] then seen[v] = true; merged[#merged + 1] = v end end
            for _, m in ipairs(mutRoster) do if not seen[m.name] then seen[m.name] = true; merged[#merged + 1] = m.name end end
            newList = merged
        end
        local LIST = _curList()
        for idx = #LIST, 1, -1 do LIST[idx] = nil end
        for _, v in ipairs(newList) do LIST[#LIST + 1] = v end
        _curSave(); refreshPriorityList(); refreshAvailableList()
        if priImportBox then priImportBox.Text = "" end
    end)
    local function _paintSeg(btn, on) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        btn.BackgroundColor3 = on and Theme.Accent1 or Theme.SurfaceHighlight
        btn.TextColor3       = on and _G.JAF_OnAccent() or Theme.TextSecondary
    end
    _pgApplyMode = function(mode, skipRefresh) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        _pgMode = (mode == "mutation") and "mutation" or "brainrot"
        local isMut = (_pgMode == "mutation")
        if Config.PriorityLastTab ~= _pgMode then Config.PriorityLastTab = _pgMode pcall(SaveConfig) end
        _paintSeg(tabBrainrotBtn, not isMut)
        _paintSeg(tabMutationBtn, isMut)
        local _mutOn = Config.MutationPriorityEnabled ~= false
        local _mutOff = isMut and not _mutOn
        availableLabel.Text = "AVAILABLE SECRET / OG BRAINROTS"
        availableLabel.Visible, availableScroll.Visible, searchBox.Visible, priGenBox.Visible = not isMut, not isMut, not isMut, not isMut
        resetOrderBtn.Visible, mutOnOffBtn.Visible = isMut, isMut
        priorityScroll.Size, priorityScroll.Position = isMut and UDim2.new(1, 0, 1, -30) or UDim2.new(0.45, 0, 1, -30), isMut and UDim2.new(0, 0, 0, 30) or UDim2.new(0.55, 0, 0, 30)
        priorityLabel.Size, priorityLabel.Position = isMut and UDim2.new(1, 0, 0, 25) or UDim2.new(0.45, 0, 0, 25), isMut and UDim2.new(0, 0, 0, 0) or UDim2.new(0.55, 0, 0, 0)
        priorityLabel.Text      = (not isMut) and "PRIORITY LIST"
            or (_mutOff and "MUTATIONS OFF  --  this order is saved but not used"
                        or  "TOP = PREFERRED  --  only decides between two of the SAME brainrot")
        priorityLabel.TextColor3 = _mutOff and Theme.Error or Theme.TextPrimary
        priorityScroll.BackgroundTransparency = _mutOff and 0.55 or 0
        if isMut and availableFilter ~= "" then availableFilter = ""; searchBox.Text = "" end
        mutOnOffBtn.Text             = "USE MUTATIONS: " .. (_mutOn and "ON" or "OFF")
        mutOnOffBtn.BackgroundColor3 = _mutOn and Theme.SurfaceHighlight or Theme.Error
        mutOnOffBtn.TextColor3       = _mutOn and Theme.TextSecondary or _G.JAF_OnAccent(Theme.Error)
        saveBtn.Text = isMut and "SAVE MUTATIONS" or "SAVE PRIORITY"
        priImportBox.PlaceholderText = isMut
            and "Paste a MUTATION list code here, then press IMPORT LIST ->"
            or  "Paste a priority list code here, then press IMPORT LIST ->"
        if not skipRefresh then refreshAvailableList(); refreshPriorityList() end
    end
    mutOnOffBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        Config.MutationPriorityEnabled = (Config.MutationPriorityEnabled == false)
        pcall(SaveConfig)
        _G.JAF_PrioDirty()
        _pgApplyMode(_pgMode, true)
    end)
    resetOrderBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        table.clear(MUTATION_LIST)
        for i, m in ipairs(mutRoster) do MUTATION_LIST[i] = m.name end
        saveMutationPriorityToConfig()
        refreshPriorityList()
    end)
    _pgApplyMode(_pgMode, true)
        local function _pgOpen() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            _pgEnsureParent()
            priorityGui.DisplayOrder = 1600000000
            priorityGui.Enabled = true
            _pgApplyMode(Config.PriorityLastTab, true)
            if SharedState.RefreshPriorityGUI then pcall(SharedState.RefreshPriorityGUI) end
        end
        _G.JAF_OpenPriorityList = _pgOpen
        _G.JAF_TogglePriorityList = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if priorityGui.Enabled then
                priorityGui.Enabled = false
                pcall(_markSecretsSeen)
            else
                _pgOpen()
            end end
        UserInputService.InputBegan:Connect(function(input, processed) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if processed then return end
            if input.KeyCode == Enum.KeyCode.P and UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
                if _G.JAF_TogglePriorityList then _G.JAF_TogglePriorityList() else priorityGui.Enabled = not priorityGui.Enabled end
            end end) end)
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local GuiService = cloneref and cloneref(game:GetService("GuiService")) or game:GetService("GuiService")
    while true do
        if Config.CleanErrorGUIs then
            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
GuiService:ClearError() end)
            task.wait(_G.JAF_ClearErrorInterval or 0.1)
        else
            task.wait(0.5)
        end end end)
buildJustAFanSettingsUI = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local pg = HubGui
    if not pg then return end
    local oldBSG2 = hubFind("JustAFanSettings")
    if oldBSG2 then oldBSG2:Destroy() end
    _G.JAF_SettingsUIConns = _G.JAF_SettingsUIConns or {}
    for _, _c in ipairs(_G.JAF_SettingsUIConns) do pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_c:Disconnect() end) end
    table.clear(_G.JAF_SettingsUIConns)
    local function _setUISConn(sig, fn) LPH_ATTRIBUTES(ERROR_HANDLING(false))
local c = sig:Connect(fn); table.insert(_G.JAF_SettingsUIConns, c); return c end
    local bsg = JUI.new("ScreenGui", nil, "Name", "JustAFanSettings", "ResetOnSpawn", false)
    _P(bsg, "DisplayOrder", 1550000000, "ZIndexBehavior", Enum.ZIndexBehavior.Sibling, "Parent", pg)
    local function C() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
return {
        BG   = Theme.Background,
        SURF = Theme.Surface,
        SH   = Theme.SurfaceHighlight,
        AC   = Theme.Accent1,
        TP   = Theme.TextPrimary,
        TS   = Theme.TextSecondary,
        ERR  = Theme.Error,
        SUC  = Theme.Success,
    } end
    local CS = C()
    local _mob = _G.JAF_MobileOn()
    local function _sliderRow(parent, order, label, lo, hi, step, saveOnDrag, init, apply, fmt, hint, valW, roundStep) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local span = hi - lo
        local vw = valW or 60
        local cs = C()
        local r = JUI.new("Frame", parent, "Size", UDim2.new(1,0,0,52), "BackgroundColor3", cs.SURF, "BorderSizePixel", 0, "LayoutOrder", order)
        JUI.corner(r, 7)
        JUI.new("TextLabel", r, "Size", UDim2.new(1,-(vw+20),0,18), "Position", UDim2.new(0,10,0,4), "BackgroundTransparency", 1, "Text", label, "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", cs.TP, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
        local val = JUI.new("TextLabel", r, "Size", UDim2.new(0,vw,0,18), "Position", UDim2.new(1,-(vw+8),0,4), "BackgroundTransparency", 1, "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", Theme.Accent1, "TextXAlignment", Enum.TextXAlignment.Right)
        if hint then
            JUI.new("TextLabel", r, "Size", UDim2.new(0,130,0,14), "Position", UDim2.new(1,-140,0,20), "BackgroundTransparency", 1, "Text", hint, "Font", Enum.Font.GothamMedium, "TextSize", 10, "TextColor3", cs.TS, "TextXAlignment", Enum.TextXAlignment.Right, "TextTruncate", Enum.TextTruncate.AtEnd)
        end
        local bg = JUI.new("Frame", r, "Size", UDim2.new(1,-20,0,6), "Position", UDim2.new(0,10,0,34), "BackgroundColor3", cs.SH, "BorderSizePixel", 0)
        JUI.round(bg)
        local fill = JUI.new("Frame", bg, "BackgroundColor3", Theme.Accent1, "BorderSizePixel", 0, "Size", UDim2.new(0,0,1,0))
        JUI.round(fill)
        local knob = JUI.new("Frame", bg, "Size", UDim2.new(0,13,0,13), "AnchorPoint", Vector2.new(0.5,0.5), "BackgroundColor3", cs.TP, "BorderSizePixel", 0)
        JUI.round(knob)
        local function upd(v) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if roundStep and step and step > 0 then v = math.floor(v/step + 0.5) * step
            elseif step == 1 then v = math.floor(v)
            elseif step == 0.1 then v = math.floor(v*10)/10
            elseif step and step > 0 then v = math.floor(v/step + 0.5) * step end
            v = math.clamp(v, lo, hi)
            apply(v)
            val.Text = fmt and fmt(v) or tostring(v)
            local p2 = (v - lo) / span
            fill.Size = UDim2.new(p2,0,1,0)
            knob.Position = UDim2.new(p2,0,0.5,0)
            if saveOnDrag then SaveConfig() end
        end
        upd(init)
        local function _at(x) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return lo + math.clamp((x - bg.AbsolutePosition.X) / bg.AbsoluteSize.X, 0, 1) * span end
        local drag = false
        bg.InputBegan:Connect(function(i) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
                drag = true; upd(_at(i.Position.X))
            end end)
        _setUISConn(UserInputService.InputEnded, function(i) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if drag and (i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch) then
                drag = false
                if not saveOnDrag then SaveConfig() end
            end end)
        _setUISConn(UserInputService.InputChanged, function(i) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if drag and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
                upd(_at(i.Position.X))
            end end)
        return r, upd
    end
    local function _segRow(parent, order, label, opts, get, set) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local n = #opts
        local x0 = -(58 + (n - 1) * 57)
        local cs = C()
        local row = JUI.new("Frame", parent, "Size", UDim2.new(1,0,0,40), "BackgroundColor3", cs.SURF, "BorderSizePixel", 0, "LayoutOrder", order)
        JUI.corner(row, 7)
        JUI.new("TextLabel", row, "Size", UDim2.new(1,x0-14,1,0), "Position", UDim2.new(0,12,0,0), "BackgroundTransparency", 1, "Text", label, "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", cs.TP, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
        local btns = {}
        local function refresh() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local cur = get()
            local c2 = C()
            for _, b in ipairs(btns) do
                local on = (b.v == cur)
                b.btn.BackgroundColor3 = on and Theme.Accent1 or c2.SH
                b.btn.TextColor3       = on and _G.JAF_OnAccent() or c2.TP
            end end
        for i, o in ipairs(opts) do
            local b = JUI.new("TextButton", row, "Size", UDim2.new(0,52,0,26), "Position", UDim2.new(1, x0 + (i-1)*57, 0.5, -13), "Font", Enum.Font.GothamBold, "TextSize", 10, "Text", o.t, "AutoButtonColor", false, "BorderSizePixel", 0)
            JUI.corner(b, 5)
            btns[#btns+1] = {btn=b, v=o.v}
            _G.JAF_OnTap(b, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if get() == o.v then return end
                set(o.v); SaveConfig(); refresh()
            end) end
        refresh()
        return row, refresh
    end
    local function makeMiniSlider(parent, lbl, lo, hi, step, initV, fmt, onChange, lo_idx) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local cs = C()
        local r = JUI.new("Frame", parent, "Size", UDim2.new(1,0,0,34), "BackgroundColor3", cs.SURF, "BorderSizePixel", 0, "LayoutOrder", lo_idx)
        JUI.corner(r, 7)
        JUI.new("TextLabel", r, "Size", UDim2.new(0.5,0,1,0), "Position", UDim2.new(0,10,0,0), "BackgroundTransparency", 1, "Text", lbl, "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", cs.TP, "TextXAlignment", Enum.TextXAlignment.Left)
        local vl = JUI.new("TextLabel", r, "Size", UDim2.new(0,38,0,18), "Position", UDim2.new(1,-42,0.5,-9), "BackgroundTransparency", 1, "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", Theme.TextPrimary, "TextXAlignment", Enum.TextXAlignment.Right)
        local bg2 = JUI.new("Frame", r, "Size", UDim2.new(0,80,0,4), "Position", UDim2.new(1,-128,0.5,-2), "BackgroundColor3", cs.SH, "BorderSizePixel", 0)
        JUI.round(bg2)
        local fi2 = JUI.new("Frame", bg2, "BackgroundColor3", Theme.Accent1, "Size", UDim2.new(0,0,1,0), "BorderSizePixel", 0)
        JUI.round(fi2)
        local kn2 = JUI.new("Frame", bg2, "Size", UDim2.new(0,10,0,10), "BackgroundColor3", Theme.TextPrimary, "AnchorPoint", Vector2.new(0.5,0.5), "Position", UDim2.new(0,0,0.5,0), "BorderSizePixel", 0)
        JUI.round(kn2)
        local function upd(v, save) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            v=math.floor(v/step+0.5)*step; v=math.clamp(v,lo,hi)
            vl.Text=string.format(fmt,v)
            local p=(v-lo)/(hi-lo)
            fi2.Size=UDim2.new(p,0,1,0); kn2.Position=UDim2.new(p,0,0.5,0)
            onChange(v, save)
        end
        upd(initV, true)
        _G.JAF_BindSlider(bg2, function(px) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local bsz2 = bg2.AbsoluteSize.X
            if bsz2 > 0 then upd(lo + math.clamp((px - bg2.AbsolutePosition.X) / bsz2, 0, 1) * (hi - lo), false) end
        end, SaveConfig)
        return r
    end
    local function _collapse(parent, order, h, bgc, tc, ts, cr, labelFn, rows, open) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local hdr = JUI.new("TextButton", parent, "Size", UDim2.new(1,0,0,h), "BackgroundColor3", bgc, "BorderSizePixel", 0, "LayoutOrder", order, "AutoButtonColor", false, "Font", Enum.Font.GothamBold, "TextSize", ts, "TextColor3", tc, "TextXAlignment", Enum.TextXAlignment.Left)
        JUI.corner(hdr, cr)
        local function ref(setOpen) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if setOpen ~= nil then open = setOpen end
            hdr.Text = (open and "  \u{25BC}  " or "  \u{25B6}  ") .. labelFn()
            for _, rr in ipairs(rows) do rr.Visible = open end
        end
        ref()
        hdr.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
ref(not open) end)
        return hdr, ref
    end
    local function _cfgTog(parent, lbl, key, order, dflt, full) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local get
        if dflt == 1 then get = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config[key] ~= false end
        elseif dflt == 2 then get = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config[key] == true end
        else get = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config[key] end end
        return JUI.toggle(parent, lbl, get, function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config[key] = v; SaveConfig() end, order, full)
    end
    local searchBox
    local searchResults
    local searchRegistry = {}
    local searchMoved = {}
    local _sp = Config.JustAFanSettingsPos
    local panel = JUI.new("Frame", bsg, "Name", "MainPanel", "Size", UDim2.new(0, 700, 0, 560), "Position", _sp and UDim2.new(_sp.X, 0, _sp.Y, 0) or UDim2.new(0, 60, 0.5, -280), "BackgroundColor3", CS.BG, "BackgroundTransparency", 0.31, "BorderSizePixel", 0, "Visible", false)
    JUI.corner(panel, 12)
    local panStroke = JUI.stroke(panel, Theme.Accent1, 1.5, 0.45)
    local function attachDragSave(handle) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        handle.InputBegan:Connect(function(inp) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then
                _G.JAF_OnInputEnd(inp, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    if panel and panel.Parent then
                        local ps = panel.Parent.AbsoluteSize
                        Config.JustAFanSettingsPos = { X = panel.AbsolutePosition.X / ps.X, Y = panel.AbsolutePosition.Y / ps.Y, }
                        _G.JAF_SaveConfigSoon()
                    end end) end end) end
    local side = JUI.new("Frame", panel, "Name", "Sidebar", "Size", UDim2.new(0, 210, 1, 0), "BackgroundColor3", CS.SURF, "BackgroundTransparency", 0.25, "BorderSizePixel", 0)
    JUI.corner(side, 12)
    JUI.new("Frame", side, "Size", UDim2.new(0, 14, 1, 0), "Position", UDim2.new(1, -14, 0, 0), "BackgroundColor3", CS.SURF, "BackgroundTransparency", 0.25, "BorderSizePixel", 0)
    JUI.new("Frame", side, "Size", UDim2.new(0, 1, 1, -16), "Position", UDim2.new(1, -1, 0, 8), "BackgroundColor3", CS.SH, "BorderSizePixel", 0)
    local brand = JUI.new("Frame", side, "Size", UDim2.new(1, -24, 0, 84), "Position", UDim2.new(0, 12, 0, 14), "BackgroundTransparency", 1)
    MakeDraggable(brand, panel); attachDragSave(brand)
    attachResizeGrip(panel, "JustAFanSettings")
    local mark = JUI.new("ImageLabel", brand, "Size", UDim2.new(0, 40, 0, 40), "Position", UDim2.new(0, 0, 0, 0), "BackgroundColor3", CS.SH, "BackgroundTransparency", 0.2, "BorderSizePixel", 0, "ScaleType", Enum.ScaleType.Crop, "Image", _G.JAF_DawgResolved or "")
    JUI.corner(mark, 10)
    local markStroke = JUI.stroke(mark, Theme.Accent1, 1.2, 0.35)
    if not (_G.JAF_DawgResolved and _G.JAF_DawgResolved ~= "") then
        task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local ok, a = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return resolveDawgImage() end)
            if ok and type(a) == "string" and a ~= "" and mark and mark.Parent then mark.Image = a end
        end) end
    JUI.new("TextLabel", brand, "Size", UDim2.new(1, -40, 0, 26), "Position", UDim2.new(0, 46, 0, 6), "BackgroundTransparency", 1, "Text", "SAMMYDAWG HUB", "Font", Enum.Font.GothamBold, "TextSize", 16, "TextColor3", CS.TP, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
    JUI.new("TextLabel", brand, "Size", UDim2.new(1, 0, 0, 19), "Position", UDim2.new(0, 0, 0, 45), "BackgroundTransparency", 1, "Text", "Public Build", "Font", Enum.Font.GothamMedium, "TextSize", 13, "TextColor3", CS.TS, "TextXAlignment", Enum.TextXAlignment.Center)
    local hDisc = JUI.new("TextButton", brand, "Size", UDim2.new(1, 0, 0, 19), "Position", UDim2.new(0, 0, 0, 64), "BackgroundTransparency", 1, "AutoButtonColor", false, "Text", "discord.gg/sammydawg", "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", Theme.Accent1, "TextXAlignment", Enum.TextXAlignment.Left)
    hDisc.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if setclipboard then pcall(setclipboard, "https://discord.gg/sammydawg") end
        local old = hDisc.Text; hDisc.Text = "copied!"
        task.delay(1.2, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if hDisc and hDisc.Parent then hDisc.Text = old end end)
    end)
    local navBox = JUI.new("Frame", side, "Name", "Nav", "Size", UDim2.new(1, -16, 1, -116), "Position", UDim2.new(0, 10, 0, 108), "BackgroundTransparency", 1)
    _P(Instance.new("UIListLayout", navBox), "Padding", UDim.new(0, 4), "SortOrder", Enum.SortOrder.LayoutOrder)
    local content = JUI.new("Frame", panel, "Name", "Content", "Size", UDim2.new(1, -210, 1, 0), "Position", UDim2.new(0, 210, 0, 0), "BackgroundTransparency", 1)
    local topbar = JUI.new("Frame", content, "Size", UDim2.new(1, -16, 0, 36), "Position", UDim2.new(0, 8, 0, 12), "BackgroundTransparency", 1)
    MakeDraggable(topbar, panel); attachDragSave(topbar)
    local hcSz = _mob and 40 or 26
    local hClose = JUI.new("TextButton", topbar, "Size", UDim2.new(0, hcSz, 0, hcSz), "Position", UDim2.new(1, -hcSz, 0.5, -hcSz / 2), "BackgroundColor3", CS.SURF, "Text", "\u{2013}", "Font", Enum.Font.GothamBold, "TextSize", _mob and 24 or 18, "TextColor3", CS.TP, "AutoButtonColor", false, "BorderSizePixel", 0)
    JUI.corner(hClose, 6)
    hClose.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
panel.Visible = false end)
    local fpsW = _mob and 190 or 170
    local hFpsBox = JUI.new("Frame", topbar, "Size", UDim2.new(0, fpsW, 1, 0), "Position", UDim2.new(1, -(fpsW + 10 + hcSz), 0, 0), "BackgroundTransparency", 1)
    MakeDraggable(hFpsBox, panel); attachDragSave(hFpsBox)
    local hFps = JUI.new("TextLabel", hFpsBox, "Size", UDim2.new(0.5, -8, 1, 0), "Position", UDim2.new(0, 0, 0, 0), "BackgroundTransparency", 1, "RichText", true, "Text", "", "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", CS.TP, "TextXAlignment", Enum.TextXAlignment.Right, "TextTruncate", Enum.TextTruncate.AtEnd)
    local hPing = JUI.new("TextLabel", hFpsBox, "Size", UDim2.new(0.5, -8, 1, 0), "Position", UDim2.new(0.5, 8, 0, 0), "BackgroundTransparency", 1, "RichText", true, "Text", "", "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", CS.TP, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
    local function _rgb(c) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return string.format("rgb(%d,%d,%d)", math.floor(c.R*255+0.5), math.floor(c.G*255+0.5), math.floor(c.B*255+0.5)) end
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local fr, ac2 = 0, 0
        if _G.JAF_SettingsFpsConn then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G.JAF_SettingsFpsConn:Disconnect() end) end
        _G.JAF_SettingsFpsConn = RunService.Heartbeat:Connect((function(dt) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if not (hFps and hFps.Parent) then if _G.JAF_SettingsFpsConn then _G.JAF_SettingsFpsConn:Disconnect() end return end
            if not panel.Visible then return end
            fr += 1; ac2 += dt
            if ac2 >= 1 then
                local p2 = math.floor(LocalPlayer:GetNetworkPing()*1000)
                hFps.Text  = string.format("<font color='%s'>FPS %d</font>", _rgb(fr>=50 and Theme.Success or Theme.Error), fr)
                hPing.Text = string.format("<font color='%s'>PING %dms</font>", _rgb(p2<100 and Theme.Success or Theme.Error), p2)
                fr, ac2 = 0, 0
            end end)) end)
    local srow = JUI.new("Frame", topbar, "Name", "FeatureSearchRow", "Size", UDim2.new(1, -(fpsW + 16 + hcSz), 1, 0), "Position", UDim2.new(0, 0, 0, 0), "BackgroundColor3", CS.SURF, "BorderSizePixel", 0)
    JUI.corner(srow, 8)
    local srStroke = JUI.stroke(srow, CS.SH, 1, 0.3)
    local srAccent = JUI.new("Frame", srow, "Size", UDim2.new(0,3,1,-12), "Position", UDim2.new(0,7,0,6), "BackgroundColor3", Theme.Accent1, "BorderSizePixel", 0)
    JUI.round(srAccent)
    searchBox = JUI.new("TextBox", srow, "Name", "FeatureSearchBox", "Size", UDim2.new(1,-24,1,0), "Position", UDim2.new(0,18,0,0), "BackgroundTransparency", 1, "Text", "", "PlaceholderText", "Search features...", "PlaceholderColor3", CS.TS, "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", CS.TP, "TextXAlignment", Enum.TextXAlignment.Left, "ClearTextOnFocus", false, "ClipsDescendants", true)
    local cArea = JUI.new("Frame", content, "Size", UDim2.new(1, -16, 1, -66), "Position", UDim2.new(0, 8, 0, 58), "BackgroundTransparency", 1, "ClipsDescendants", true)
    local makeScroll; makeScroll = (function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local sf = JUI.new("ScrollingFrame", cArea, "Size", UDim2.new(1,0,1,0), "BackgroundTransparency", 1, "BorderSizePixel", 0, "ScrollBarThickness", 0, "ScrollBarImageColor3", Theme.Accent1, "CanvasSize", UDim2.new(0,0,0,0), "AutomaticCanvasSize", Enum.AutomaticSize.Y, "Visible", false)
        local ll = Instance.new("UIListLayout", sf)
        _P(ll, "FillDirection", Enum.FillDirection.Horizontal, "Wraps", true, "HorizontalAlignment", Enum.HorizontalAlignment.Left, "Padding", UDim.new(0,8), "SortOrder", Enum.SortOrder.LayoutOrder)
        local pp = Instance.new("UIPadding", sf)
        _P(pp, "PaddingTop", UDim.new(0,6), "PaddingBottom", UDim.new(0,8), "PaddingLeft", UDim.new(0,4), "PaddingRight", UDim.new(0,4))
        return sf
    end)
    local makeSec, makeNote, makeToggle = JUI.section, JUI.note, JUI.toggle
    local TABS2 = {
        {name="Home",        id="home"},
        {name="Visuals",     id="vis"},
        {name="Movement",    id="mov"},
        {name="Teleport",    id="tp"},
        {name="Invisibility",id="inv"},
        {name="Auto Buy",    id="autobuy"},
        {name="Admin",       id="adm"},
        {name="Keybinds",    id="cfg"},
        {name="UI",          id="uih"},
        {name="Config",      id="cfgio"},
        {name="Priority List", id="__prio", action=function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if _G.JAF_TogglePriorityList then _G.JAF_TogglePriorityList()
            elseif _G.JAF_OpenPriorityList then _G.JAF_OpenPriorityList() end
        end},
    }
    local tBtns2 = {}
    local tScrolls2 = {}
    local activTab2 = "home"
    local _tabBuilders, _tabBuilt = {}, {}
    local _indexTabRows
    local function _addTab(id, fn) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local t = _tabBuilders[id]
        if not t then t = {}; _tabBuilders[id] = t end
        t[#t+1] = fn
    end
    local function _ensureTab(id) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _tabBuilt[id] or not tScrolls2[id] then return end
        _tabBuilt[id] = true
        for _, fn in ipairs(_tabBuilders[id] or {}) do
            local ok, err = pcall(fn)
            if not ok then _G.JAF_TabBuildErr = tostring(id) .. ': ' .. tostring(err) end
        end
        if _indexTabRows then _indexTabRows(tScrolls2[id]) end
        if _G.JAF_MobileApplyRows then pcall(_G.JAF_MobileApplyRows, tScrolls2[id]) end
    end
    local function _ensureAllTabs() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false)) for id in pairs(tScrolls2) do _ensureTab(id) end end
    local function switchTab2(id) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not tScrolls2[id] then id = "vis" end
        _ensureTab(id)
        if id == activTab2 and tScrolls2[id].Visible then return end
        if searchResults and searchResults.Visible and searchBox and searchBox.Text ~= "" then searchBox.Text = "" end
        activTab2 = id
        if _G.JustAFanSettingsUI then _G.JustAFanSettingsUI.currentTab = id end
        for tid, sc in pairs(tScrolls2) do sc.Visible = (tid==id) end
        local cs = C()
        for tid, tb in pairs(tBtns2) do
            local on = tid==id
            tb.BackgroundColor3 = Theme.Accent1
            tb.BackgroundTransparency = on and 0 or 1
            local col = on and _G.JAF_OnAccent() or cs.TP
            local lb = tb:FindFirstChild("TabLbl"); if lb then lb.TextColor3 = col end
            local ic = tb:FindFirstChild("TabIcon"); if ic then ic.ImageColor3 = col end
            local dt = tb:FindFirstChild("TabDot"); if dt then dt.BackgroundColor3 = on and _G.JAF_OnAccent() or Theme.Accent1 end
        end end
    local TAB_ICONS = {
        home    = "7733960981",
        vis     = "7733774602",
        mov     = "7743870731",
        tp      = "7733992789",
        inv     = "7743868000",
        autobuy = "7734056813",
        adm     = "7734056608",
        cfg     = "7733924046",
        uih     = "7734058803",
        cfgio   = "7733799185",
        __prio  = "7743869411",
    }
    for i,td in ipairs(TABS2) do
        local tb = JUI.new("TextButton", navBox, "Size", UDim2.new(1,0,0,36), "LayoutOrder", i, "BackgroundColor3", Theme.Accent1, "BackgroundTransparency", 1, "Text", "", "AutoButtonColor", false, "BorderSizePixel", 0)
        JUI.corner(tb, 8)
        local iconId = TAB_ICONS[td.id]
        if iconId then
            JUI.new("ImageLabel", tb, "Name", "TabIcon", "Size", UDim2.new(0,18,0,18), "Position", UDim2.new(0,14,0.5,-9), "BackgroundTransparency", 1, "Image", "rbxassetid://"..iconId, "ImageColor3", CS.TP)
        else
            local dot = JUI.new("Frame", tb, "Name", "TabDot", "Size", UDim2.new(0,8,0,8), "Position", UDim2.new(0,19,0.5,-4), "BackgroundColor3", Theme.Accent1, "BorderSizePixel", 0)
            JUI.round(dot)
        end
        JUI.new("TextLabel", tb, "Name", "TabLbl", "Size", UDim2.new(1, -50, 1, 0), "Position", UDim2.new(0, 42, 0, 0), "BackgroundTransparency", 1, "Text", td.name, "Font", Enum.Font.GothamBold, "TextSize", 14, "TextColor3", CS.TP, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
        if td.action then
            tb.MouseButton1Click:Connect(td.action)
        else
            tBtns2[td.id]=tb; tScrolls2[td.id]=makeScroll()
            local tid=td.id; tb.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
switchTab2(tid) end)
        end end
    _addTab("home", function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local hS = tScrolls2["home"]
        local cs = C()
        local function _applyDawg(img) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if not img then return end
            local a = _G.JAF_DawgResolved
            if a and a ~= "" then img.Image = a
            else task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local ok, res = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return resolveDawgImage() end)
                if ok and type(res) == "string" and res ~= "" and img and img.Parent then img.Image = res end
            end) end
        end
        local hero = JUI.new("Frame", hS, "Size", UDim2.new(1, 0, 0, 152), "BackgroundColor3", cs.SURF, "BorderSizePixel", 0, "LayoutOrder", 1)
        hero:SetAttribute("JAFNoSearch", true)
        JUI.corner(hero, 14)
        JUI.stroke(hero, cs.SH, 1, 0.3)
        local logo = JUI.new("ImageLabel", hero, "Size", UDim2.new(0, 118, 0, 118), "Position", UDim2.new(0, 16, 0.5, -59), "BackgroundColor3", cs.SH, "BackgroundTransparency", 0.2, "BorderSizePixel", 0, "ScaleType", Enum.ScaleType.Crop, "Image", "")
        JUI.corner(logo, 12)
        JUI.stroke(logo, Theme.Accent1, 1.5, 0.3)
        _applyDawg(logo)
        JUI.new("TextLabel", hero, "Size", UDim2.new(1, -162, 0, 40), "Position", UDim2.new(0, 148, 0, 32), "BackgroundTransparency", 1, "Text", "SAMMYDAWG HUB", "Font", Enum.Font.GothamBold, "TextSize", 26, "TextColor3", cs.TP, "TextXAlignment", Enum.TextXAlignment.Left, "TextYAlignment", Enum.TextYAlignment.Bottom, "TextTruncate", Enum.TextTruncate.AtEnd)
        JUI.new("TextLabel", hero, "Size", UDim2.new(1, -162, 0, 26), "Position", UDim2.new(0, 150, 0, 76), "BackgroundTransparency", 1, "Text", "PUBLIC", "Font", Enum.Font.GothamBold, "TextSize", 16, "TextColor3", Theme.Accent1, "TextXAlignment", Enum.TextXAlignment.Left)
        JUI.new("TextLabel", hero, "Size", UDim2.new(1, -162, 0, 16), "Position", UDim2.new(0, 150, 0, 104), "BackgroundTransparency", 1, "Text", "Steal a Brainrot", "Font", Enum.Font.GothamMedium, "TextSize", 12, "TextColor3", cs.TS, "TextXAlignment", Enum.TextXAlignment.Left)
        local wc = JUI.new("Frame", hS, "Size", UDim2.new(1, 0, 0, 110), "BackgroundColor3", cs.SURF, "BorderSizePixel", 0, "LayoutOrder", 2)
        wc:SetAttribute("JAFNoSearch", true)
        JUI.corner(wc, 14)
        JUI.stroke(wc, cs.SH, 1, 0.35)
        local accent = JUI.new("Frame", wc, "Size", UDim2.new(0, 4, 1, -28), "Position", UDim2.new(0, 14, 0, 14), "BackgroundColor3", Theme.Accent1, "BorderSizePixel", 0)
        JUI.round(accent)
        JUI.new("TextLabel", wc, "Size", UDim2.new(1, -46, 0, 20), "Position", UDim2.new(0, 30, 0, 24), "BackgroundTransparency", 1, "Text", "WELCOME BACK", "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", cs.TS, "TextXAlignment", Enum.TextXAlignment.Left)
        JUI.new("TextLabel", wc, "Size", UDim2.new(1, -46, 0, 38), "Position", UDim2.new(0, 30, 0, 46), "BackgroundTransparency", 1, "Text", tostring(LocalPlayer.DisplayName or LocalPlayer.Name), "Font", Enum.Font.GothamBold, "TextSize", 26, "TextColor3", Theme.Accent1, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
        makeSec(hS, "WHAT'S NEW", 3)
        for i, txt in ipairs({
            "All detections fixed — no more random kicks",
            "Brainrot scanning rewritten — instant sync, zero stutter",
            "TP more stable, fewer lagbacks + ~5,000 lines cut for more FPS",
            "Check Discord for the full changelog",
        }) do
            local b = JUI.new("Frame", hS, "Size", UDim2.new(1, 0, 0, 56), "BackgroundColor3", cs.SURF, "BorderSizePixel", 0, "LayoutOrder", 3 + i)
            b:SetAttribute("JAFNoSearch", true)
            JUI.corner(b, 10)
            JUI.new("TextLabel", b, "Size", UDim2.new(0, 18, 1, 0), "Position", UDim2.new(0, 14, 0, 0), "BackgroundTransparency", 1, "Text", "\u{2022}", "Font", Enum.Font.GothamBold, "TextSize", 14, "TextColor3", Theme.Accent1, "TextXAlignment", Enum.TextXAlignment.Center)
            JUI.new("TextLabel", b, "Size", UDim2.new(1, -50, 1, -10), "Position", UDim2.new(0, 38, 0, 5), "BackgroundTransparency", 1, "Text", txt, "Font", Enum.Font.GothamMedium, "TextSize", 13, "TextColor3", cs.TP, "TextXAlignment", Enum.TextXAlignment.Left, "TextYAlignment", Enum.TextYAlignment.Center, "TextWrapped", true)
        end end)
    _addTab("cfg", function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local cS = tScrolls2["cfg"]
    local makeKeyX = JUI.keybindX
    local function _kbNone(lbl, tp, key, order) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        makeKeyX(cS, lbl,
            function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
local v = (tp and Config.TpSettings or Config)[key]; return (v and v ~= "") and v or "NONE" end,
            function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false));
(tp and Config.TpSettings or Config)[key] = (v == "NONE" and "" or v); SaveConfig() end,
            order)
    end
    makeSec(cS,"KEYBINDS",0)
    makeKeyX(cS,"Menu",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.MenuKey end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.MenuKey=(v=="" and "LeftControl" or v); SaveConfig() end,1)
    _kbNone("Steal Speed",   false, "StealSpeedKey",   2)
    _kbNone("Reset",         false, "ResetKey",        3)
    makeKeyX(cS,"Invis Toggle",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.InvisToggleKey end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.InvisToggleKey=v; local ok,kc=pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Enum.KeyCode[v] end); _G.INVISIBLE_STEAL_KEY=(ok and kc) or nil; SaveConfig() end,5)
    makeKeyX(cS,"Ragdoll Self",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.RagdollSelfKey ~= "" and Config.RagdollSelfKey or "NONE" end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.RagdollSelfKey=v; SaveConfig() end,6)
    _kbNone("Kick",          false, "KickKey",         7)
    _kbNone("Drop Brainrot", false, "DropBrainrotKey", 8)
    makeKeyX(cS,"Click To AP",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.ClickToAPKeybind or "L" end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.ClickToAPKeybind=v; SaveConfig() end,9)
    makeKeyX(cS,"Proximity AP",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.ProximityAPKeybind or "P" end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.ProximityAPKeybind=v; SaveConfig() end,10)
    _kbNone("Auto Buy",      false, "AutoBuyKey",      11)
    _kbNone("Float",         false, "FloatKey",        12)
    makeSec(cS,"TP KEYBINDS",20)
    _kbNone("TP",           true, "TpKey",           21)
    _kbNone("Clone",        true, "CloneKey",        22)
    _kbNone("Carpet Speed", true, "CarpetSpeedKey",  23)
    end)
    _addTab("vis", function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local vS = tScrolls2["vis"]
    makeSec(vS,"ESP",1)
    for _, t in ipairs({
        {"Stealer ESP",    "StealerESP",    8},
        {"Base Owner ESP", "BaseOwnerESP",  9},
        {"Conveyor ESP",   "ConveyorESP",  14},
        {"Turret ESP",     "TurretESP",    18, 1},
        {"Trap ESP",       "TrapESP",      20, 1},
    }) do _cfgTog(vS, t[1], t[2], t[3], t[4]) end
    makeToggle(vS,"X-Ray Base",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.XrayEnabled end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.XrayEnabled=v; if v then enableXray() else disableXray() end; SaveConfig() end,11)
    makeToggle(vS,"Player ESP",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.PlayerESP end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.PlayerESP=v; if playerESPToggleRef and playerESPToggleRef.setFn then playerESPToggleRef.setFn(v) end; SaveConfig() end,12)
    makeToggle(vS,"Brainrot ESP",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.BrainrotESP end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.BrainrotESP=v; if espToggleRef and espToggleRef.setFn then espToggleRef.setFn(v) end; SaveConfig() end,13)
    makeSec(vS,"OVERLAYS",20)
    makeToggle(vS,"Line to Base",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.LineToBase end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.LineToBase=v; if not v and _G.resetPlotBeam then pcall(_G.resetPlotBeam) end; SaveConfig() end,21)
    makeSec(vS,"CAMERA",30)
    _sliderRow(vS, 31, "FOV", 30, 120, 1, false, Config.FOV or 70,
        function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.FOV=v; if Workspace.CurrentCamera then Workspace.CurrentCamera.FieldOfView=v end end)
    makeSec(vS,"PERFORMANCE",40)
    makeToggle(vS,"Dark Mode",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.DarkMode end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(setDarkMode,v) end,42)
    _sliderRow(vS, 43, "Darkness Level", 0, 1, 0, true, Config.DarkBrightness or 0.4,
        function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.DarkBrightness=v; if Config.DarkMode then pcall(applyDarkBrightness,v) end end,
        function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return tostring(math.floor(v*100)).."%" end)
    makeToggle(vS,"FFlags",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.FFlagsEnabled end,function(v) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        Config.FFlagsEnabled=v; SaveConfig()
        if v then if _G.JAF_ApplyFFlags then _G.JAF_ApplyFFlags() end end
    end,44)
    makeToggle(vS,"FPS Optimizer",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.FpsOptimizer ~= false end,function(v) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        Config.FpsOptimizer = v
        _G.JAF_FpsOptimizer = v
        _G.JAF_OptimizeFloorSmooth       = v and Config.OptimizeFloorSmooth ~= false
        _G.JAF_OptimizeStripMeshTextures = v and Config.OptimizeStripMeshTextures == true
        SaveConfig()
    end,45)
    makeToggle(vS,"Remove Animations",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.RemoveAnimations ~= false end,function(v) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        Config.RemoveAnimations = v
        _G.JAF_RemoveAnims = v
        SaveConfig()
    end,46)
    end)
    _addTab("mov", function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local mS = tScrolls2["mov"]
    local cs = C()
    makeSec(mS,"MOVEMENT",1)
    makeToggle(mS,"Infinite Jump",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return State.infiniteJumpEnabled end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
setInfiniteJump(v) end,11)
    _cfgTog(mS,"Auto Steal Speed","AutoStealSpeed",12)
    do
        local _ssBusy = false
        local _, _ssSet = _sliderRow(mS, 13, "Steal Speed", 5, 31, 1, false,
            math.clamp(math.floor((tonumber(Config.StealSpeed) or 20) + 0.5), 5, 31),
            function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                if _ssBusy then return end
                if SharedState.applyStealSpeedValue then pcall(SharedState.applyStealSpeedValue, v)
                else Config.StealSpeed = math.clamp(v, 5, 31); SaveConfig() end
            end)
        SharedState._refreshMiniStealSpeedSlider = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if _ssBusy or not _ssSet then return end
            _ssBusy = true
            pcall(_ssSet, math.clamp(tonumber(Config.StealSpeed) or 20, 5, 31))
            _ssBusy = false
        end end
    makeSec(mS,"ANTI-RAGDOLL",20)
    makeToggle(mS,"Anti-Ragdoll",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.AntiRagdollV2 end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.AntiRagdollV2=v; startAntiRagdollV2(v); SaveConfig() end,22)
    local autoS = mS
    makeSec(autoS,"AUTOMATION",30)
    _cfgTog(autoS,"Auto Unlock on Steal","AutoUnlockOnSteal",31)
    makeToggle(autoS,"Auto Kick on Steal",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.AutoKickOnSteal end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.setAutoKickFromSettings then _G.setAutoKickFromSettings(v) else Config.AutoKickOnSteal=v; SaveConfig() end end,32)
    _cfgTog(autoS,"Auto Reset on Balloon","AutoResetOnBalloon",33)
    makeToggle(autoS,"Anti-Bee & Disco",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.AntiBeeDisco end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.AntiBeeDisco=v; SaveConfig(); if v and SharedState.ANTI_BEE_DISCO then SharedState.ANTI_BEE_DISCO.Enable() elseif SharedState.ANTI_BEE_DISCO then SharedState.ANTI_BEE_DISCO.Disable() end end,34)
    _cfgTog(autoS,"Clean Error GUIs","CleanErrorGUIs",35)
    makeToggle(autoS,"Kick to Private Server",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.KickToPrivateOnSteal end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.setKickToPrivateFromSettings then _G.setKickToPrivateFromSettings(v) else Config.KickToPrivateOnSteal=v; SaveConfig() end end,36)
    do
        local info = JUI.new("TextLabel", autoS, "Size", UDim2.new(1,0,0,0), "AutomaticSize", Enum.AutomaticSize.Y, "LayoutOrder", 37, "BackgroundColor3", cs.SURF, "BackgroundTransparency", 0.4, "BorderSizePixel", 0, "Font", Enum.Font.Gotham, "TextSize", 13, "TextColor3", cs.TS, "TextXAlignment", Enum.TextXAlignment.Left, "TextYAlignment", Enum.TextYAlignment.Top, "TextWrapped", true, "RichText", true, "Text", "<b>How to get your link:</b>\n1. Paste your private server link in a browser\n2. Copy the link the browser opens\n3. Paste that here and press SAVE")
        JUI.corner(info, 6)
        _P(Instance.new("UIPadding", info), "PaddingLeft", UDim.new(0,10), "PaddingRight", UDim.new(0,10), "PaddingTop", UDim.new(0,6), "PaddingBottom", UDim.new(0,6))
        local box = JUI.new("TextBox", autoS, "Size", UDim2.new(1,0,0,36), "LayoutOrder", 38, "BackgroundColor3", cs.SURF, "BorderSizePixel", 0, "Font", Enum.Font.Gotham, "TextSize", 12, "TextColor3", cs.TP, "PlaceholderText", "Paste your link here", "PlaceholderColor3", cs.TS, "Text", tostring(Config.PrivateServerCode or ""), "ClearTextOnFocus", false, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
        JUI.corner(box, 8)
        JUI.new("UIPadding", box, "PaddingLeft", UDim.new(0,10), "PaddingRight", UDim.new(0,10))
        JUI.stroke(box, cs.SH, 1, 0.3)
        local saveBtn = JUI.new("TextButton", autoS, "Size", UDim2.new(1,0,0,30), "LayoutOrder", 39, "BackgroundColor3", Theme.Accent1, "AutoButtonColor", false, "BorderSizePixel", 0, "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", _G.JAF_OnAccent(), "Text", "SAVE CODE")
        JUI.corner(saveBtn, 6)
        local function doSave() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            Config.PrivateServerCode = box.Text
            SaveConfig()
            local linkCode = (_G.JAF_ParsePSInput and _G.JAF_ParsePSInput(box.Text)) or nil
            local msg, col
            if box.Text:gsub("%s","") == "" then msg, col = "SAVED (empty)", C().SH
            elseif not linkCode then msg, col = "SAVED - couldn't read a link code", Theme.Error
            else msg, col = "SAVED!", Theme.Success end
            saveBtn.Text = msg; saveBtn.BackgroundColor3 = col; saveBtn.TextColor3 = _G.JAF_OnAccent(col)
            task.delay(1.8, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if saveBtn and saveBtn.Parent then saveBtn.Text = "SAVE CODE"; saveBtn.BackgroundColor3 = Theme.Accent1; saveBtn.TextColor3 = _G.JAF_OnAccent() end
            end) end
        saveBtn.MouseButton1Click:Connect(doSave)
        box.FocusLost:Connect(function(enter) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if enter then doSave() end end)
    end end)
    _addTab("tp", function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local tpS = tScrolls2["tp"]
    local cs = C()
    makeSec(tpS,"AUTO TP",10)
    makeToggle(tpS,"Auto TP on Script Load",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.TpSettings.TpOnLoad end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.TpSettings.TpOnLoad=v; SaveConfig() end,11)
    do
        local r = JUI.new("Frame", tpS, "Size", UDim2.new(1,0,0,36), "BackgroundColor3", cs.SURF, "BorderSizePixel", 0, "LayoutOrder", 12); JUI.corner(r, 7)
        JUI.new("TextLabel", r, "Size", UDim2.new(0.6,0,0,16), "Position", UDim2.new(0,10,0,10), "BackgroundTransparency", 1, "Text", "Min Gen for Auto TP", "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", cs.TP, "TextXAlignment", Enum.TextXAlignment.Left)
        local tb = JUI.new("TextBox", r, "Size", UDim2.new(0,110,0,24), "Position", UDim2.new(1,-118,0.5,-12), "BackgroundColor3", cs.SH, "Text", tostring(Config.TpSettings.MinGenForTp or ""), "Font", Enum.Font.Gotham, "TextSize", 11, "TextColor3", cs.TP, "PlaceholderText", "e.g. 5k, 1m, 1b", "ClearTextOnFocus", false); JUI.corner(tb, 5)
        tb.FocusLost:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.TpSettings.MinGenForTp=tb.Text:gsub("%s",""); SaveConfig() end)
    end
    makeSec(tpS,"TOOL",19)
    do
        local tools={"Flying Carpet","Cupid's Wings","Santa's Sleigh","Witch's Broom","Waverider"}
        local optFrames, optRefs = {}, {}
        local refHdr
        for idx,tn in ipairs(tools) do
            local r = JUI.new("TextButton", tpS, "Size", UDim2.new(1,0,0,30), "BackgroundColor3", cs.SH, "BorderSizePixel", 0, "LayoutOrder", 20+idx, "AutoButtonColor", false, "Visible", false, "Font", Enum.Font.GothamBold, "TextSize", 11, "TextXAlignment", Enum.TextXAlignment.Left, "Text", "        "..tn)
            JUI.corner(r, 6)
            local function refOpt() LPH_ATTRIBUTES(ERROR_HANDLING(false))
r.TextColor3=(Config.TpSettings.Tool==tn) and Theme.Accent1 or C().TP end
            refOpt(); optFrames[idx]=r; optRefs[idx]=refOpt
            r.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                Config.TpSettings.Tool=tn; SaveConfig()
                for _,f in ipairs(optRefs) do pcall(f) end
                refHdr(false)
            end) end
        local _, _rh = _collapse(tpS, 20, 34, cs.SURF, cs.TP, 11, 7,
            function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return "Tool: "..(Config.TpSettings.Tool or "Flying Carpet") end,
            optFrames, false)
        refHdr = _rh
        local T = Config.TpSettings
        local _twF1 = makeMiniSlider(tpS,"F1 Speed",50,300,5,tonumber(T.TpSpeedF1Front) or tonumber(T.TpSpeedF1) or 130,"%d",function(v, save) LPH_ATTRIBUTES(ERROR_HANDLING(false))
T.TpSpeedF1Front=v; T.TpSpeedF1Side=v; T.TpSpeedF1=v; if save then SaveConfig() end end,33)
        local _twF2 = makeMiniSlider(tpS,"F2 Speed",50,300,5,tonumber(T.TpSpeedF2Front) or tonumber(T.TpSpeedF2) or 130,"%d",function(v, save) LPH_ATTRIBUTES(ERROR_HANDLING(false))
T.TpSpeedF2Front=v; T.TpSpeedF2Side=v; T.TpSpeedF2=v; if save then SaveConfig() end end,34)
        local _grF1 = makeMiniSlider(tpS,"F1 Speed",200,1000,10,tonumber(T.GrappleSpeedF1Front) or tonumber(T.GrappleSpeed) or 400,"%d",function(v, save) LPH_ATTRIBUTES(ERROR_HANDLING(false))
T.GrappleSpeedF1Front=v; T.GrappleSpeedF1Side=v; if save then SaveConfig() end end,35)
        local _grF2 = makeMiniSlider(tpS,"F2 Speed",200,1000,10,tonumber(T.GrappleSpeedF2Front) or tonumber(T.GrappleSpeed) or 400,"%d",function(v, save) LPH_ATTRIBUTES(ERROR_HANDLING(false))
T.GrappleSpeedF2Front=v; T.GrappleSpeedF2Side=v; if save then SaveConfig() end end,36)
        makeToggle(tpS,"Insta TP To Brainrot",
            function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.TpSettings.InstaTpBrainrot ~= false end,
            function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.TpSettings.InstaTpBrainrot = v; _G.JAF_BrainrotSimpleTp = v; SaveConfig() end,
            37, true)
        makeMiniSlider(tpS,"Brainrot Speed",100,500,5,T.BrainrotSpeed or 175,"%d",function(v, save) LPH_ATTRIBUTES(ERROR_HANDLING(false))
T.BrainrotSpeed=v; if save then SaveConfig() end end,38)
        makeMiniSlider(tpS,"Clone Delay",0,1,0.05,tonumber(T.CloneDelay) or 0.15,"%.2f",function(v, save) LPH_ATTRIBUTES(ERROR_HANDLING(false))
T.CloneDelay=v; if save then SaveConfig() end end,39)
        local advGui, advRebuild
        do
            local _oldAdv = HubGui:FindFirstChild("JAFAdvTpSpeed")
            if _oldAdv then _oldAdv:Destroy() end
            advGui = JUI.new("ScreenGui", nil, "Name", "JAFAdvTpSpeed", "ResetOnSpawn", false, "ZIndexBehavior", Enum.ZIndexBehavior.Sibling, "IgnoreGuiInset", true, "DisplayOrder", 1590000000, "Enabled", false)
            advGui.Parent = HubGui
            local scrim = JUI.new("TextButton", advGui, "Size", UDim2.new(1, 0, 1, 0), "Text", "", "AutoButtonColor", false, "BackgroundColor3", Color3.new(0, 0, 0), "BackgroundTransparency", 0.45, "BorderSizePixel", 0, "ZIndex", 1)
            scrim.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
advGui.Enabled = false end)
            local ap = JUI.new("Frame", advGui, "Size", UDim2.new(0, 380, 0, 316), "AnchorPoint", Vector2.new(0.5, 0.5), "Position", UDim2.new(0.5, 0, 0.5, 0), "BackgroundColor3", Theme.Background, "BorderSizePixel", 0, "ZIndex", 2, "Active", true)
            JUI.corner(ap, 12)
            JUI.stroke(ap, Theme.Accent1, 1.4, 0.3)
            JUI.new("TextLabel", ap, "Size", UDim2.new(1, -60, 0, 40), "Position", UDim2.new(0, 16, 0, 4), "BackgroundTransparency", 1, "Text", "ADVANCED SETTINGS", "Font", Enum.Font.GothamBold, "TextSize", 16, "TextColor3", Theme.TextPrimary, "TextXAlignment", Enum.TextXAlignment.Left, "ZIndex", 3)
            local _xbS = _mob and 36 or 26
            local xb = JUI.new("TextButton", ap, "Size", UDim2.new(0, _xbS, 0, _xbS), "Position", UDim2.new(1, -(_xbS + 10), 0, 10), "BackgroundColor3", cs.SH, "Text", "X", "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", Theme.Error, "AutoButtonColor", false, "ZIndex", 3)
            JUI.corner(xb, 6)
            xb.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
advGui.Enabled = false end)
            local sub = JUI.new("TextLabel", ap, "Size", UDim2.new(1, -32, 0, 16), "Position", UDim2.new(0, 16, 0, 42), "BackgroundTransparency", 1, "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", Theme.Accent1, "TextXAlignment", Enum.TextXAlignment.Left, "ZIndex", 3)
            local body = JUI.new("Frame", ap, "Size", UDim2.new(1, -32, 1, -82), "Position", UDim2.new(0, 16, 0, 68), "BackgroundTransparency", 1, "ZIndex", 3)
            _P(Instance.new("UIListLayout", body), "Padding", UDim.new(0, 9), "SortOrder", Enum.SortOrder.LayoutOrder)
            advRebuild = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                for _, ch in ipairs(body:GetChildren()) do if ch:IsA("Frame") then ch:Destroy() end end
                local g = (T.TpMode == "grapple")
                sub.Text = "Mode: " .. (g and "GRAPPLE" or "TWEEN")
                local lo, hi, st = (g and 200 or 50), (g and 1000 or 300), (g and 10 or 5)
                local dflt = g and (tonumber(T.GrappleSpeed) or 400) or 130
                local rows = {
                    { lbl = "F1 Front", keyA = g and "GrappleSpeedF1Front" or "TpSpeedF1Front", mirror = (not g) and "TpSpeedF1" or nil, fb = (not g) and tonumber(T.TpSpeedF1) or nil },
                    { lbl = "F1 Side",  keyA = g and "GrappleSpeedF1Side"  or "TpSpeedF1Side",  fb = (not g) and tonumber(T.TpSpeedF1) or nil },
                    { lbl = "F2 Front", keyA = g and "GrappleSpeedF2Front" or "TpSpeedF2Front", mirror = (not g) and "TpSpeedF2" or nil, fb = (not g) and tonumber(T.TpSpeedF2) or nil },
                    { lbl = "F2 Side",  keyA = g and "GrappleSpeedF2Side"  or "TpSpeedF2Side",  fb = (not g) and tonumber(T.TpSpeedF2) or nil },
                }
                for i, rd in ipairs(rows) do
                    makeMiniSlider(body, rd.lbl, lo, hi, st, tonumber(T[rd.keyA]) or rd.fb or dflt, "%d", function(v, save) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                        T[rd.keyA] = v
                        if rd.mirror then T[rd.mirror] = v end
                        if save then SaveConfig() end
                    end, i)
                end
                makeMiniSlider(body, "F1 Front Clone Delay", 0, 1, 0.05,
                    tonumber(T.F1FrontCloneDelay) or 0.15, "%.2f",
                    function(v, save) LPH_ATTRIBUTES(ERROR_HANDLING(false))
T.F1FrontCloneDelay = v; if save then SaveConfig() end end, 5)
            end end
        do
            local _fireRow, _fireRefresh = _segRow(tpS, 32, "Fire Grapple",
                {{t="SIGNAL",v="firesignal"},{t="REMOTE",v="remote"}},
                function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return (T.GrappleFireMode == "remote") and "remote" or "firesignal" end,
                function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
T.GrappleFireMode = v; _G.JAF_GrappleFireMode = v end)
            _G.JAF_SyncGrappleFireRow = _fireRefresh
            local function _tpModeSync() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                local g = (T.TpMode == "grapple")
                _twF1.Visible = not g; _twF2.Visible = not g
                _grF1.Visible = g;     _grF2.Visible = g
                _fireRow.Visible = g
                if advGui and advGui.Enabled and advRebuild then advRebuild() end
            end
            _segRow(tpS, 31, "TP Method", {{t="TWEEN",v="tween"},{t="GRAPPLE",v="grapple"}},
                function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return (T.TpMode == "grapple") and "grapple" or "tween" end,
                function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
T.TpMode = v; _tpModeSync() end)
            _tpModeSync()
        end
        local advBtn = JUI.new("TextButton", tpS, "Size", UDim2.new(1, 0, 0, 34), "BackgroundColor3", cs.SURF, "BorderSizePixel", 0, "LayoutOrder", 40, "AutoButtonColor", false, "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", Theme.Accent1, "TextXAlignment", Enum.TextXAlignment.Left, "Text", "  Advanced Settings")
        JUI.corner(advBtn, 7)
        advBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            advGui.Enabled = not advGui.Enabled
            if advGui.Enabled then advRebuild() end
        end) end
    makeSec(tpS,"FLIGHT",30)
    makeSec(tpS,"TARGET SWITCHING",41)
    _cfgTog(tpS,"Fly To New Pick Instantly","RetargetOnSelect",42,1,true)
    makeNote(tpS,"Click a brainrot in the list -> go there and steal it now.",43)
    _cfgTog(tpS,"Skip Brainrots Being Stolen","SkipStolenTarget",44,1,true)
    makeNote(tpS,"Target taken on arrival? Take the next free one in that base.",45)
    makeSec(tpS,"RECOVERY",50)
    _cfgTog(tpS,"Hit Recovery","HitRecovery",51,1)
    makeToggle(tpS,"Wait for Stable FPS",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.TpSettings.WaitStableFps == true end,function(v) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        Config.TpSettings.WaitStableFps = v; _G.JAF_WaitStableFps = v; SaveConfig()
    end,56)
    makeMiniSlider(tpS,"Stable FPS",50,300,5,Config.TpSettings.FpsStableThresh or 60,"%d",
        function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.TpSettings.FpsStableThresh = v; _G.JAF_FpsStableThresh = v end, 57)
    do
        local SPOT_OPTS = { {t="FRONT",v="front"}, {t="SIDE",v="side"}, {t="AUTO",v="auto"} }
        local f1row = _segRow(tpS, 61, "TP Spots: Floor 1", SPOT_OPTS,
            function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _G.JAF_F1Mode or Config.TpSettings.F1SpotMode or "auto" end,
            function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G.JAF_F1Mode = v; Config.TpSettings.F1SpotMode = v end)
        local f2row = _segRow(tpS, 62, "TP Spots: Floor 2", SPOT_OPTS,
            function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _G.JAF_F2Mode or Config.TpSettings.F2SpotMode or "auto" end,
            function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G.JAF_F2Mode = v; Config.TpSettings.F2SpotMode = v end)
        local f2bRow = _segRow(tpS, 63, "Steal Floor 2 From Floor 1",
            {{t="OFF",v=false},{t="ON",v=true}},
            function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.TpSettings.F2StealBelow == true end,
            function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.TpSettings.F2StealBelow = v; _G.JAF_F2StealBelow = v end)
        _collapse(tpS, 60, 26, cs.SURF, Theme.Accent1, 12, 6, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return "TP Spots" end, {f1row, f2row, f2bRow}, true)
    end end)
    _addTab("uih", function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local uhS = tScrolls2["uih"]
    makeSec(uhS,"HIDE UIs",1)
    for _, t in ipairs({
        {"Hide Admin Panel",        "HideAdminPanel",      11, "XiAdminPanel"},
        {"Hide Auto Steal",         "HideAutoSteal",       12, "AutoStealUI"},
        {"Hide Best Brainrot Bar",  "HideWalkingBest",     18, "JustAFanWalkingBestBar"},
    }) do
        local key, gui = t[2], t[4]
        makeToggle(uhS, t[1], function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config[key] end, function(v) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            Config[key]=v; SaveConfig(); local g=hubFind(gui); if g then g.Enabled=not v end
        end, t[3])
    end
    makeToggle(uhS,"Hide Admin Cooldowns",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.HideAdminCooldowns end,function(v) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        Config.HideAdminCooldowns=v; SaveConfig()
        if _G.JAF_SetAdminCooldowns then _G.JAF_SetAdminCooldowns(not v)
        else
            local g=hubFind("XiAdminPanel"); local f=g and g:FindFirstChildOfClass("Frame")
            local s=f and f:FindFirstChild("CooldownStrip"); if s then s.Visible=not v end
        end
    end,19)
    makeToggle(uhS,"Show Mini UI",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.ShowMiniActions end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.ShowMiniActions=v; SaveConfig(); local g=hubFind("JustAFanMiniActions"); if g then local mp=g:FindFirstChild("MiniPanel"); if mp then mp.Visible=v end end end,21)
    _cfgTog(uhS,"Lock all GUI","UILocked",22)
    makeSec(uhS,"MOBILE",90)
    _segRow(uhS, 91, "Mobile UI",
        {{t="AUTO",v="auto"},{t="ON",v="on"},{t="OFF",v="off"}},
        function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.MobileMode or "auto" end,
        function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.MobileMode = v; task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(_G.JAF_MobileRefresh) end) end)
    _segRow(uhS, 92, "Hub Button",
        {{t="AUTO",v="auto"},{t="ON",v="on"},{t="OFF",v="off"}},
        function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.HubButtonMode or "auto" end,
        function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.HubButtonMode = v; if buildMobileBarUI then pcall(buildMobileBarUI) end end)
    makeToggle(uhS,"Mobile Full-Screen Sheet",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.MobileSheet ~= false end,function(v) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        Config.MobileSheet=v; SaveConfig(); task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(_G.JAF_MobileRefresh) end)
    end,93,true)
    makeSec(uhS,"PANEL SIZE",25)
    _sliderRow(uhS, 26, "UI Scale", 0.5, 1.5, 0.05, false, Config.UIScale or 1,
        function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.UIScale = v; if _G.JAF_ApplyUIScale then _G.JAF_ApplyUIScale(v) end end,
        function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return string.format("%.2fx", v) end)
    SaveConfig()
    end)
    _addTab("cfgio", function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local cioS = tScrolls2["cfgio"]
        local cs = C()
        makeSec(cioS, "SHARE CONFIG", 1)
        local info = JUI.new("Frame", cioS, "Size", UDim2.new(1, 0, 0, 54), "BackgroundColor3", cs.SURF, "BorderSizePixel", 0, "LayoutOrder", 2)
        info:SetAttribute("JAFNoSearch", true)
        JUI.corner(info, 8)
        _P(Instance.new("UIPadding", info), "PaddingLeft", UDim.new(0, 10), "PaddingRight", UDim.new(0, 10), "PaddingTop", UDim.new(0, 6), "PaddingBottom", UDim.new(0, 6))
        JUI.new("TextLabel", info, "Size", UDim2.new(1, 0, 1, 0), "BackgroundTransparency", 1, "Font", Enum.Font.Gotham, "TextSize", 12, "TextColor3", cs.TP, "TextWrapped", true, "TextXAlignment", Enum.TextXAlignment.Left, "TextYAlignment", Enum.TextYAlignment.Center, "Text", "Export copies all your settings as a code. Share it -- others paste it below and Import to use your exact setup.")
        local function cfgBtn(label, order) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local b = JUI.new("TextButton", cioS, "Size", UDim2.new(1, 0, 0, 40), "LayoutOrder", order, "BackgroundColor3", cs.SURF, "AutoButtonColor", false, "BorderSizePixel", 0, "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", cs.TP, "Text", label)
            JUI.corner(b, 8)
            return b
        end
        local exportBtn = cfgBtn("Export My Config", 3)
        exportBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local ok, json = pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local toSave = {}
                for k, v in pairs(Config) do toSave[k] = v end
                toSave.ProximityAP = false
                toSave.StealRadius = nil
                toSave.PrivateServerCode = nil
                return HttpService:JSONEncode(toSave)
            end)
            if not ok or not json then return end
            local copied = false
            if setclipboard then pcall(setclipboard, json); copied = true end
            if writefile then pcall(writefile, "SammyDawg_config_export.json", json) end
        end)
        JUI.new("TextLabel", cioS, "Size", UDim2.new(1, 0, 0, 16), "BackgroundTransparency", 1, "LayoutOrder", 4, "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", Theme.Accent1, "TextXAlignment", Enum.TextXAlignment.Left, "Text", "PASTE A CONFIG CODE")
        local boxRow = JUI.new("Frame", cioS, "Size", UDim2.new(1, 0, 0, 92), "BackgroundColor3", cs.SH, "BorderSizePixel", 0, "LayoutOrder", 5)
        JUI.corner(boxRow, 8)
        JUI.stroke(boxRow, Theme.Accent2, 1, 0.5)
        _P(Instance.new("UIPadding", boxRow), "PaddingLeft", UDim.new(0, 8), "PaddingRight", UDim.new(0, 8), "PaddingTop", UDim.new(0, 6), "PaddingBottom", UDim.new(0, 6))
        local importBox = JUI.new("TextBox", boxRow, "Size", UDim2.new(1, 0, 1, 0), "BackgroundTransparency", 1, "Font", Enum.Font.Gotham, "TextSize", 12, "TextColor3", cs.TP, "PlaceholderText", "Paste a config code here...", "PlaceholderColor3", cs.TS, "Text", "", "ClearTextOnFocus", false, "MultiLine", true, "TextWrapped", true, "ClipsDescendants", true, "TextXAlignment", Enum.TextXAlignment.Left, "TextYAlignment", Enum.TextYAlignment.Top)
        local importBtn = cfgBtn("Import Config", 6)
        importBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local raw = importBox.Text
            if (not raw or raw == "") and getclipboard then
                local okc, c = pcall(getclipboard); if okc and type(c) == "string" then raw = c end
            end
            if not raw or raw == "" then return end
            local ok, decoded = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return HttpService:JSONDecode(raw) end)
            if not ok or type(decoded) ~= "table" then return end
            decoded.PrivateServerCode = nil
            local n = 0
            for k, v in pairs(decoded) do Config[k] = v; n = n + 1 end
            _P(Config, "StealRadius", nil, "AutoTPPriority", true, "ProximityAP", false)
            Config.OptimizeFloorSmooth = true
            Config.OptimizeStripMeshTextures = true
            SaveConfig()
            importBox.Text = ""
        end)
        JUI.new("TextLabel", cioS, "Size", UDim2.new(1, 0, 0, 16), "BackgroundTransparency", 1, "LayoutOrder", 7, "Font", Enum.Font.Gotham, "TextSize", 10, "TextColor3", cs.TP, "TextXAlignment", Enum.TextXAlignment.Left, "Text", "Rejoin after importing so every setting takes effect.")
    end)
    _addTab("autobuy", function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local abS = tScrolls2["autobuy"]
        local cs = C()
        makeSec(abS, "CARPET AUTO BUY", 8)
        makeToggle(abS, "TP To Carpet Brainrots",
            function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.CarpetAutoBuy end,
            function(v) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if _G.JAF_ToggleCarpetBuy then _G.JAF_ToggleCarpetBuy(v) end
                Config.CarpetAutoBuy = v; SaveConfig()
            end, 9, true)
        do
            local r = JUI.new("Frame", abS, "Size", UDim2.new(1, 0, 0, 36), "BackgroundColor3", cs.SURF, "BorderSizePixel", 0, "LayoutOrder", 10)
            JUI.corner(r, 7)
            JUI.new("TextLabel", r, "Size", UDim2.new(0.6, 0, 0, 16), "Position", UDim2.new(0, 10, 0, 10), "BackgroundTransparency", 1, "Text", "Carpet Min Gen", "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", cs.TP, "TextXAlignment", Enum.TextXAlignment.Left)
            local tb = JUI.new("TextBox", r, "Size", UDim2.new(0, 110, 0, 24), "Position", UDim2.new(1, -118, 0.5, -12), "BackgroundColor3", cs.SH, "Text", tostring(Config.CarpetMinGen or ""), "Font", Enum.Font.Gotham, "TextSize", 11, "TextColor3", cs.TP, "PlaceholderText", "e.g. 5k, 1m, 1b", "ClearTextOnFocus", false)
            JUI.corner(tb, 5)
            tb.FocusLost:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.CarpetMinGen = tb.Text:gsub("%s", ""); SaveConfig() end)
        end
        _sliderRow(abS, 11, "Carpet TP Speed", 30, 150, 5, false, tonumber(Config.CarpetBuySpeed) or 70,
            function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.CarpetBuySpeed = v; _G.JAF_CarpetBuySpeed = v end,
            function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return string.format("%d studs/s", v) end, nil, 120)
        if _G.JAF_FastBuyAllowed and _G.JAF_FastBuyAllowed() then
        makeToggle(abS, "Fast Auto Buy",
            function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.AutoBuyMode == "fast" end,
            function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.AutoBuyMode = v and "fast" or "normal"; SaveConfig() end, 12, true)
        do
            local fbHi = tonumber(_G.JAF_AutoBuyFastMax) or 61
            _sliderRow(abS, 13, "Fast Buy Rate", 1, fbHi, 1, false, tonumber(Config.AutoBuyFastRate) or 12,
                function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.AutoBuyFastRate = v end,
                function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return (v >= fbHi) and "MAX (per frame)" or string.format("%d /s", v) end, nil, 140, true)
        end end
        _cfgTog(abS, "Auto Grief", "AutoGrief", 14, nil, true)
        _cfgTog(abS, "Kick After Buy", "KickAfterBuy", 15, nil, true)
        makeToggle(abS, "Kick to PS",
            function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.KickAfterBuyToPS end,
            function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.KickAfterBuyToPS = v; SaveConfig(); if _G.JAF_RefreshCarpetPSHint then _G.JAF_RefreshCarpetPSHint() end end, 16, true)
        local psHint = JUI.new("TextLabel", abS, "Size", UDim2.new(1, 0, 0, 16), "BackgroundTransparency", 1, "LayoutOrder", 17, "Font", Enum.Font.Gotham, "TextSize", 10, "TextXAlignment", Enum.TextXAlignment.Left, "TextWrapped", true)
        psHint:SetAttribute("JAFNoSearch", true)
        local function updPsHint() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local linkCode = _G.JAF_ParsePSInput and _G.JAF_ParsePSInput(Config.PrivateServerCode)
            if not Config.KickAfterBuyToPS then
                psHint.Text = ""
            elseif linkCode then
                psHint.Text = "PS link saved."
                psHint.TextColor3 = Theme.Success
            else
                psHint.Text = "No PS link saved -- save one first."
                psHint.TextColor3 = Theme.Error
            end end
        _G.JAF_RefreshCarpetPSHint = updPsHint
        updPsHint()
    end)
    _addTab("inv", function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local iS = tScrolls2["inv"]
    makeSec(iS,"INVIS",0)
    makeToggle(iS,"Auto Fix Lagback",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _G.AutoRecoverLagback end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G.AutoRecoverLagback=v; Config.AutoRecoverLagback=v; SaveConfig() end,1,true)
    _sliderRow(iS, 4, "Rotation", 180, 360, 1, false, Config.InvisStealAngle or 233,
        function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.InvisStealAngle=v; _G.InvisStealAngle=v end)
    _sliderRow(iS, 5, "Depth", 0.5, 5, 0.1, true, Config.SinkSliderValue or 2.5,
        function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.SinkSliderValue=v; _G.SinkSliderValue=v end, nil, "3-4 recommended")
    makeToggle(iS,"Auto Invis on Steal",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.AutoInvisDuringSteal end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.AutoInvisDuringSteal=v; _G.AutoInvisDuringSteal=v; SaveConfig() end,10,true)
    makeToggle(iS,"Invis Delay",function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.InvisDelayEnabled == true end,function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.InvisDelayEnabled=v; _G.JAF_InvisDelayEnabled=v; SaveConfig() end,11,true)
    _sliderRow(iS, 12, "Invis Delay (sec)", 0, 1, 0.1, false, (tonumber(Config.InvisDelayMs) or 0)/1000,
        function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.InvisDelayMs=v*1000; _G.JAF_InvisDelayMs=v*1000 end)
    end)
    _addTab("adm", function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local admS = tScrolls2["adm"]
    local cs = C()
    makeSec(admS,"ADMIN PANEL",0)
    _cfgTog(admS,"Random Command","AdminRandomCmd",1,2)
    _cfgTog(admS,"Show Prox Circle","ShowProxCircle",2,2)
    _sliderRow(admS, 3, "Prox Circle Studs", 5, 50, 1, true, Config.ProximityRange or 15,
        function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.ProximityRange = v end, nil, nil, 46, true)
    do
        local CMDS = {"balloon","inverse","jail","jumpscare","morph","nightvision","ragdoll","rocket","tiny"}
        local rows = {}
        for i, cmd in ipairs(CMDS) do
            rows[#rows+1] = makeToggle(admS, cmd:sub(1,1):upper()..cmd:sub(2),
                function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return not (Config.AdminAllowedCmds and Config.AdminAllowedCmds[cmd] == false) end,
                function(v) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    if type(Config.AdminAllowedCmds) ~= "table" then Config.AdminAllowedCmds = {} end
                    Config.AdminAllowedCmds[cmd] = v; SaveConfig()
                end, 4 + i)
        end
        _collapse(admS, 4, 30, cs.SH, Theme.Accent1, 12, 7, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return "Allowed Commands" end, rows, false)
    end
    do
        local function _mkOrderUI() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            _G.JAF_BuildCmdOrderUI(admS, 14, {rowH = 28, hdrH = 30, corner = 7, textSize = 12, save = SaveConfig})
        end
        if _G.JAF_BuildCmdOrderUI then
            _mkOrderUI()
        else
            task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                for _ = 1, 100 do
                    task.wait(0.1)
                    if _G.JAF_BuildCmdOrderUI then if admS and admS.Parent then pcall(_mkOrderUI) end return end
                end end) end end
    makeSec(admS,"ANTI-AP",30)
    for _, t in ipairs({
        {"Anti-AP Atlas Users",      "AntiApAtlasUsers",      31},
        {"Anti-AP Kawaifu Users",    "AntiApKawaifuUsers",    32},
        {"Anti-AP Braintopia Users", "AntiApBraintopiaUsers", 33},
        {"Anti-AP FMLY",             "AntiApFmlyUsers",       34},
        {"Anti-AP SON",              "AntiApSonUsers",        35},
    }) do _cfgTog(admS, t[1], t[2], t[3], 1) end
    makeSec(admS,"GRIEF DETECTOR",37)
    makeToggle(admS,"Grief Detector",
        function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.GriefDetectorEnabled ~= false end,
        function(v) LPH_ATTRIBUTES(ERROR_HANDLING(false))
Config.GriefDetectorEnabled=v; SaveConfig()
            local gui = hubFind("XiGriefDetector")
            if gui then gui.Enabled = v end
            if not v then local h = hubFind("XiGriefHistory") if h then h.Enabled = false end end end, 38)
    makeSec(admS,"BLACKLIST",40)
    do
        local addRow = JUI.new("Frame", admS, "Size", UDim2.new(1,0,0,30), "BackgroundTransparency", 1, "LayoutOrder", 41)
        local input = JUI.new("TextBox", addRow, "Size", UDim2.new(1,-58,0,28), "Position", UDim2.new(0,0,0,0), "BackgroundColor3", cs.SURF, "BorderSizePixel", 0, "Text", "", "PlaceholderText", "Roblox username...", "Font", Enum.Font.Gotham, "TextSize", 11, "TextColor3", cs.TP, "PlaceholderColor3", cs.TS, "ClearTextOnFocus", false)
        JUI.corner(input, 6)
        JUI.stroke(input, cs.SH, 1, 0.3)
        JUI.new("UIPadding", input, "PaddingLeft", UDim.new(0,8))
        local addBtn = JUI.new("TextButton", addRow, "Size", UDim2.new(0,50,0,28), "Position", UDim2.new(1,-50,0,0), "BackgroundColor3", Theme.Error, "Text", "ADD", "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", _G.JAF_OnAccent(Theme.Error), "AutoButtonColor", false)
        JUI.corner(addBtn, 6)
        local function rebuildAdmBL() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            for _, c in ipairs(admS:GetChildren()) do if c.Name == "AdmBLEntry" then c:Destroy() end end
            local c2 = C()
            local rowBg = Color3.fromRGB(math.floor(Theme.Error.R*255*0.35), math.floor(Theme.Error.G*255*0.35), math.floor(Theme.Error.B*255*0.35))
            local onErr = _G.JAF_OnAccent(Theme.Error)
            for i, name in ipairs(BlacklistedPlayers) do
                local row = JUI.new("Frame", admS, "Name", "AdmBLEntry", "LayoutOrder", 100 + i, "Size", UDim2.new(1,0,0,28), "BackgroundColor3", rowBg, "BorderSizePixel", 0)
                JUI.corner(row, 6)
                JUI.stroke(row, Theme.Error, 1, 0.5)
                JUI.new("TextLabel", row, "Size", UDim2.new(1,-36,1,0), "Position", UDim2.new(0,10,0,0), "BackgroundTransparency", 1, "Text", name, "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", c2.TP, "TextXAlignment", Enum.TextXAlignment.Left)
                local rmv = JUI.new("TextButton", row, "Size", UDim2.new(0,26,0,20), "Position", UDim2.new(1,-30,0.5,-10), "BackgroundColor3", Theme.Error, "Text", "X", "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", onErr, "AutoButtonColor", false)
                JUI.corner(rmv, 4)
                local capName = name
                rmv.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    removeFromBlacklist(capName)
                    rebuildAdmBL()
                end) end end
        local function doAdd() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local name = input.Text:match("^%s*(.-)%s*$")
            if name == "" then return end
            if isBlacklisted(name) then input.Text = "" return end
            if addToBlacklist(name) then input.Text = "" rebuildAdmBL() end
        end
        addBtn.MouseButton1Click:Connect(doAdd)
        input.FocusLost:Connect(function(enter) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if enter then doAdd() end end)
        if _G.JAF_BLRefreshBase == nil then _G.JAF_BLRefreshBase = refreshBlacklistUI or false end
        local _prevRefresh = _G.JAF_BLRefreshBase
        refreshBlacklistUI = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if type(_prevRefresh) == "function" then pcall(_prevRefresh) end
            pcall(rebuildAdmBL)
        end
        rebuildAdmBL()
    end end)
    _addTab("uih", function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local rS = tScrolls2["uih"]
        local cs = C()
        makeSec(rS, "RESET", 120)
        local function resetRow(label, order, run) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local row = JUI.new("Frame", rS, "Size", UDim2.new(1, 0, 0, 40), "BackgroundColor3", cs.SURF, "BorderSizePixel", 0, "LayoutOrder", order)
            JUI.corner(row, 7)
            local lbl = JUI.new("TextLabel", row, "Size", UDim2.new(1, -130, 1, 0), "Position", UDim2.new(0, 14, 0, 0), "BackgroundTransparency", 1, "Text", label, "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", cs.TP, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
            local b = JUI.new("TextButton", row, "Size", UDim2.new(0, 100, 0, 26), "Position", UDim2.new(1, -112, 0.5, -13), "BackgroundColor3", cs.BG, "AutoButtonColor", false, "BorderSizePixel", 0, "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", cs.TP, "Text", "RESET")
            JUI.corner(b, 6)
            local bst = JUI.new("UIStroke", b, "ApplyStrokeMode", Enum.ApplyStrokeMode.Border, "Color", Theme.Accent1, "Thickness", 1.4, "Transparency", 0.15)
            local armed, token = false, 0
            local function disarm() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                armed = false
                b.Text = "RESET"; b.TextColor3 = C().TP
                bst.Color = Theme.Accent1; bst.Thickness = 1.4
            end
            _G.JAF_OnTap(b, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if not armed then
                    armed = true; token = token + 1
                    local myToken = token
                    b.Text = "SURE?"
                    bst.Color = Theme.Error; bst.Thickness = 2.2
                    task.delay(4, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if armed and token == myToken then disarm() end end)
                    return
                end
                token = token + 1
                disarm()
                local ok, res = pcall(run)
                lbl.Text = ok and (res or "Done") or "Failed"
                task.delay(2.5, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if lbl.Parent then lbl.Text = label end end)
            end)
            return row
        end
        resetRow("Reset Settings to Default", 121, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if not _G.JAF_ResetSettingsToDefault then return "Unavailable" end
            if not _G.JAF_ResetSettingsToDefault() then return "Failed" end
            return "Reset -- rejoin to apply"
        end)
        resetRow("Reset GUI Positions to Default", 122, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if not _G.JAF_ResetGuiPositions then return "Unavailable" end
            return string.format("Moved %d panels back", _G.JAF_ResetGuiPositions())
        end)
        local rHint = JUI.new("TextLabel", rS, "Size", UDim2.new(1, 0, 0, 40), "BackgroundTransparency", 1, "LayoutOrder", 123, "Font", Enum.Font.Gotham, "TextSize", 12, "TextColor3", cs.TS, "TextWrapped", true, "TextXAlignment", Enum.TextXAlignment.Left, "TextYAlignment", Enum.TextYAlignment.Top, "Text", "Settings reset also clears your blacklist, priority list and keybinds, then needs a rejoin. Positions reset just moves panels back, instantly.")
        rHint:SetAttribute("JAFNoSearch", true)
    end)
    _addTab("uih", function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local tS = tScrolls2["uih"]
    local cs = C()
    makeSec(tS,"THEME",100)
    do
        local TDEFS2 = {
            {"Cyan",     "cyan",     Color3.fromRGB(50,200,255)},
            {"Midnight", "midnight", Color3.fromRGB(125,135,255)},
            {"Ocean",    "ocean",    Color3.fromRGB(60,130,255)},
            {"Ice",      "ice",      Color3.fromRGB(150,220,255)},
            {"Rose",     "rose",     Color3.fromRGB(255,120,160)},
            {"White",    "white",    Color3.fromRGB(235,235,245)},
            {"Dawg",     "dawg",     Color3.fromRGB(196,42,38)},
        }
        for i2, td2 in ipairs(TDEFS2) do
            local r = JUI.new("Frame", tS, "Size", UDim2.new(1,0,0,36), "BackgroundColor3", cs.SURF, "BorderSizePixel", 0, "LayoutOrder", 100 + i2)
            JUI.corner(r, 7)
            local dot2 = JUI.new("Frame", r, "Size", UDim2.new(0,12,0,12), "Position", UDim2.new(0,12,0.5,-6), "BackgroundColor3", td2[3], "BorderSizePixel", 0)
            JUI.round(dot2)
            JUI.new("TextLabel", r, "Size", UDim2.new(0.5,0,1,0), "Position", UDim2.new(0,32,0,0), "BackgroundTransparency", 1, "Text", td2[1], "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", cs.TP, "TextXAlignment", Enum.TextXAlignment.Left)
            local isActive = (Config.CurrentTheme == td2[2])
            local apb = JUI.new("TextButton", r, "Size", UDim2.new(0,72,0,24), "Position", UDim2.new(1,-80,0.5,-12), "BackgroundColor3", isActive and td2[3] or cs.SH, "Text", isActive and "ACTIVE" or "APPLY", "Font", Enum.Font.GothamBold, "TextSize", 10, "TextColor3", isActive and _G.JAF_OnAccent() or cs.TP, "AutoButtonColor", false)
            JUI.corner(apb, 5)
            local tid2 = td2[2]; local tc2 = td2[3]
            apb.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                applyTheme(tid2)
                local c2 = C()
                for _, ch in ipairs(tS:GetChildren()) do
                    local b2 = ch:FindFirstChildOfClass("TextButton")
                    if b2 then _P(b2, "BackgroundColor3", c2.SH, "Text", "APPLY", "TextColor3", c2.TP) end
                end
                _P(apb, "BackgroundColor3", tc2, "Text", "ACTIVE", "TextColor3", _G.JAF_OnAccent(tc2))
            end) end
        local custRow = JUI.new("Frame", tS, "Size", UDim2.new(1,0,0,40), "BackgroundColor3", cs.SURF, "BorderSizePixel", 0, "LayoutOrder", 150)
        JUI.corner(custRow, 7)
        JUI.new("TextLabel", custRow, "Size", UDim2.new(0,46,1,0), "Position", UDim2.new(0,10,0,0), "BackgroundTransparency", 1, "Text", "Custom", "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", cs.TP, "TextXAlignment", Enum.TextXAlignment.Left)
        local custBox = JUI.new("TextBox", custRow, "Size", UDim2.new(0,90,0,24), "Position", UDim2.new(0,60,0.5,-12), "BackgroundColor3", cs.SH, "Text", tostring(Config.CustomThemeHex or "B432FF"), "PlaceholderText", "hex e.g. B432FF", "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", cs.TP, "ClearTextOnFocus", false, "BorderSizePixel", 0)
        JUI.corner(custBox, 5)
        local custApply = JUI.new("TextButton", custRow, "Size", UDim2.new(0,72,0,24), "Position", UDim2.new(1,-80,0.5,-12), "BackgroundColor3", cs.SH, "Text", "APPLY", "Font", Enum.Font.GothamBold, "TextSize", 10, "TextColor3", cs.TP, "AutoButtonColor", false, "BorderSizePixel", 0)
        JUI.corner(custApply, 5)
        local function applyCustom() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local hx = custBox.Text:gsub("#",""):gsub("%s","")
            if buildCustomThemeFromHex(hx) then
                Config.CustomThemeHex = hx; SaveConfig()
                custBox.Text = hx
                applyTheme("custom")
            else
                custBox.Text = Config.CustomThemeHex or "B432FF"
            end end
        custApply.MouseButton1Click:Connect(applyCustom)
        custBox.FocusLost:Connect(function(enter) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if enter then applyCustom() end end)
        local imgRow = JUI.new("Frame", tS, "Size", UDim2.new(1,0,0,40), "BackgroundColor3", cs.SURF, "BorderSizePixel", 0, "LayoutOrder", 152)
        JUI.corner(imgRow, 7)
        JUI.new("TextLabel", imgRow, "Size", UDim2.new(0,40,1,0), "Position", UDim2.new(0,10,0,0), "BackgroundTransparency", 1, "Text", "Image", "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", cs.TP, "TextXAlignment", Enum.TextXAlignment.Left)
        local imgBox = JUI.new("TextBox", imgRow, "Size", UDim2.new(1,-130,0,24), "Position", UDim2.new(0,54,0.5,-12), "BackgroundColor3", cs.SH, "Text", tostring(Config.ThemeImageUrl or ""), "PlaceholderText", "paste image URL...", "Font", Enum.Font.Gotham, "TextSize", 11, "TextColor3", cs.TP, "ClearTextOnFocus", false, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd, "BorderSizePixel", 0)
        JUI.corner(imgBox, 5)
        local imgApply = JUI.new("TextButton", imgRow, "Size", UDim2.new(0,64,0,24), "Position", UDim2.new(1,-70,0.5,-12), "BackgroundColor3", cs.SH, "Text", "APPLY", "Font", Enum.Font.GothamBold, "TextSize", 10, "TextColor3", cs.TP, "AutoButtonColor", false, "BorderSizePixel", 0)
        JUI.corner(imgApply, 5)
        local function applyImg() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local url = imgBox.Text:gsub("%s","")
            if url == "" then
                Config.ThemeImageUrl = ""; SaveConfig()
                _G.JAF_ThemeImageAsset = nil
                if jafApplyThemeImageToPanels then pcall(jafApplyThemeImageToPanels, nil) end
                return
            end
            imgApply.Text = "..."
            task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local ok = nil
                if jafApplyImageTheme then ok = jafApplyImageTheme(url) end
                if ok then Config.ThemeImageUrl = url; SaveConfig() end
                if imgApply and imgApply.Parent then imgApply.Text = "APPLY" end
            end) end
        imgApply.MouseButton1Click:Connect(applyImg)
        imgBox.FocusLost:Connect(function(enter) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if enter then applyImg() end end)
        makeToggle(tS, "Animate GIF",
            function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.GifAnimate ~= false end,
            function(v) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                Config.GifAnimate = v; SaveConfig()
                if not v then
                    if jafStopGifAnimation then jafStopGifAnimation() end
                elseif _G.JAF_GifUrl and _G.JAF_ThemeImageAsset and jafSetupGifAnimation then
                    task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(jafSetupGifAnimation, _G.JAF_GifUrl) end)
                end
            end, 153)
    end end)
    searchResults = JUI.new("ScrollingFrame", cArea, "Name", "FeatureSearchResults", "Size", UDim2.new(1,0,1,0), "BackgroundTransparency", 1, "BorderSizePixel", 0, "ScrollBarThickness", 0, "ScrollBarImageColor3", Theme.Accent1, "CanvasSize", UDim2.new(0,0,0,0), "AutomaticCanvasSize", Enum.AutomaticSize.Y, "Visible", false)
    do
        local sll = Instance.new("UIListLayout", searchResults)
        _P(sll, "FillDirection", Enum.FillDirection.Horizontal, "Wraps", true, "HorizontalAlignment", Enum.HorizontalAlignment.Left, "Padding", UDim.new(0,8), "SortOrder", Enum.SortOrder.LayoutOrder)
        local spp = Instance.new("UIPadding", searchResults)
        _P(spp, "PaddingTop", UDim.new(0,6), "PaddingBottom", UDim.new(0,8), "PaddingLeft", UDim.new(0,4), "PaddingRight", UDim.new(0,4))
    end
    local srNone = JUI.new("TextLabel", searchResults, "Name", "NoMatches", "Size", UDim2.new(1,0,0,36), "BackgroundTransparency", 1, "Text", "No matching features", "Font", Enum.Font.GothamMedium, "TextSize", 12, "TextColor3", CS.TP, "LayoutOrder", -1, "Visible", false)
    _indexTabRows = function(sc) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not sc then return end
        for _, ch in ipairs(sc:GetChildren()) do
            if ch:IsA("Frame") and ch.Visible and ch.Size.Y.Offset >= 32 and not ch:GetAttribute("JAFNoSearch") then
                local tl = ch:FindFirstChildOfClass("TextLabel")
                if tl and tl.Text ~= "" then
                    searchRegistry[#searchRegistry+1] = { row = ch, origParent = sc, origOrder = ch.LayoutOrder, label = string.lower(tl.Text) }
                end end end end
    for id, sc in pairs(tScrolls2) do if _tabBuilt[id] then _indexTabRows(sc) end end
    local function fuzzyMatch(hay, needle) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if needle == "" then return true end
        if string.find(hay, needle, 1, true) then return true end
        local hi, ni = 1, 1
        local hlen, nlen = #hay, #needle
        while hi <= hlen and ni <= nlen do if string.byte(hay, hi) == string.byte(needle, ni) then ni = ni + 1 end hi = hi + 1 end
        return ni > nlen
    end
    local function restoreMoved() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        for _, e in ipairs(searchMoved) do e.row.LayoutOrder = e.origOrder e.row.Parent = e.origParent end
        searchMoved = {}
    end
    local function runSearch(q) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        q = string.lower(q or "")
        if q ~= "" then _ensureAllTabs() end
        if q == "" then
            restoreMoved()
            searchResults.Visible = false
            srNone.Visible = false
            switchTab2(activTab2)
            return
        end
        restoreMoved()
        for _, sc in pairs(tScrolls2) do sc.Visible = false end
        searchResults.Visible = true
        local order = 0
        for _, e in ipairs(searchRegistry) do
            if fuzzyMatch(e.label, q) then
                order = order + 1
                e.row.LayoutOrder = order
                e.row.Parent = searchResults
                searchMoved[#searchMoved+1] = e
            end end
        srNone.Visible = (order == 0)
    end
    searchBox:GetPropertyChangedSignal("Text"):Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
runSearch(searchBox.Text) end)
    switchTab2("home")
    if _G.JAF_MobileApplySheet then pcall(_G.JAF_MobileApplySheet, panel, side, navBox, content, cArea, topbar, tScrolls2) end
    _G.JustAFanSettingsUI = {panel=panel, switchTab=switchTab2, currentTab="home"}
end
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    _G.JAF_WaitHeavy(); buildJustAFanSettingsUI()
    if _G.JAF_ThemeImageAsset and jafApplyThemeImageToPanels then
        task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(jafApplyThemeImageToPanels, _G.JAF_ThemeImageAsset) end)
    end end)
pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local old = hubFind("XiGriefDetector")
    if old then old:Destroy() end
    local oldH = hubFind("XiGriefHistory")
    if oldH then oldH:Destroy() end
end)
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if Config.GriefDetectorEnabled == false then return end
    local LPlayer = Players.LocalPlayer
        if not Config.Positions.GriefDetector then Config.Positions.GriefDetector = {X = 0.01, Y = 0.55} end
        local gdGui = JUI.new("ScreenGui", nil, "Name", "XiGriefDetector", "ResetOnSpawn", false, "ZIndexBehavior", Enum.ZIndexBehavior.Sibling, "Parent", HubGui)
        local mf = JUI.new("Frame", gdGui, "Size", UDim2.new(0, 272, 0, 24))
        _P(mf, "AutomaticSize", Enum.AutomaticSize.Y, "Position", UDim2.new(Config.Positions.GriefDetector.X, 0, Config.Positions.GriefDetector.Y, 0), "BackgroundColor3", Theme.Background, "BackgroundTransparency", 0.05, "BorderSizePixel", 0, "ClipsDescendants", false)
        Instance.new("UICorner", mf).CornerRadius = UDim.new(0, 10)
        local mfStroke = JUI.new("UIStroke", mf, "Color", Theme.Accent1, "Thickness", 1.5, "Transparency", 0.35)
        local mfLayout = JUI.new("UIListLayout", mf, "Padding", UDim.new(0, 2), "SortOrder", Enum.SortOrder.LayoutOrder)
        local mfPad = JUI.new("UIPadding", mf, "PaddingTop", UDim.new(0, 2), "PaddingLeft", UDim.new(0, 8), "PaddingRight", UDim.new(0, 8), "PaddingBottom", UDim.new(0, 8))
        local hdr = JUI.new("Frame", mf, "Size", UDim2.new(1, 0, 0, 26), "BackgroundTransparency", 1, "LayoutOrder", 1)
        MakeDraggable(hdr, mf, "GriefDetector")
        local ttl = JUI.new("TextLabel", hdr, "Size", UDim2.new(1, -4, 1, 0), "Position", UDim2.new(0, 2, 0, 0), "BackgroundTransparency", 1, "Text", "GRIEF DETECTOR ty royal crown", "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", Theme.TextSecondary, "TextXAlignment", Enum.TextXAlignment.Left)
        local content = JUI.new("Frame", mf, "Size", UDim2.new(1, 0, 0, 0), "AutomaticSize", Enum.AutomaticSize.Y, "BackgroundTransparency", 1, "LayoutOrder", 2)
        local noGriefL = JUI.new("TextLabel", content, "Name", "NoGrief", "Size", UDim2.new(1, 0, 0, 32), "BackgroundTransparency", 1, "Text", "No griefs yet.", "Font", Enum.Font.GothamMedium, "TextSize", 13, "TextColor3", Theme.TextSecondary, "TextXAlignment", Enum.TextXAlignment.Center)
        local AJ_COLORS = {
            Atlas      = Color3.fromRGB(255, 215,   0),
            Kawaifu    = Color3.fromRGB(255, 120, 200),
            Braintopia = Color3.fromRGB(170,  80, 255),
            FMLY       = Color3.fromRGB( 90, 220, 160),
            SON        = Color3.fromRGB( 90, 200, 220),
        }
        local currentCard = nil
        local griefHistory = {}
        if not Config.Positions.GriefHistory then Config.Positions.GriefHistory = {X = 0.30, Y = 0.18} end
        local histGui = JUI.new("ScreenGui", nil, "Name", "XiGriefHistory", "ResetOnSpawn", false)
        _P(histGui, "ZIndexBehavior", Enum.ZIndexBehavior.Sibling, "DisplayOrder", 60, "Enabled", false, "Parent", HubGui)
        local histPanel = JUI.new("Frame", histGui, "Size", UDim2.new(0, 360, 0, 440), "Position", UDim2.new(Config.Positions.GriefHistory.X, 0, Config.Positions.GriefHistory.Y, 0), "BackgroundColor3", Theme.Background, "BackgroundTransparency", 0.05, "BorderSizePixel", 0)
        Instance.new("UICorner", histPanel).CornerRadius = UDim.new(0, 12)
        local histStroke = JUI.new("UIStroke", histPanel, "Color", Theme.Accent1, "Thickness", 1.5, "Transparency", 0.35)
        local histHdr = JUI.new("Frame", histPanel, "Size", UDim2.new(1, 0, 0, 40), "BackgroundTransparency", 1)
        MakeDraggable(histHdr, histPanel, "GriefHistory")
        local histTtl = JUI.new("TextLabel", histHdr, "Size", UDim2.new(1, -90, 0, 20), "Position", UDim2.new(0, 12, 0, 6), "BackgroundTransparency", 1, "Text", "GRIEF HISTORY", "Font", Enum.Font.GothamBold, "TextSize", 16, "TextColor3", Theme.TextPrimary, "TextXAlignment", Enum.TextXAlignment.Left)
        local histCount = JUI.new("TextLabel", histHdr, "Size", UDim2.new(1, -90, 0, 13), "Position", UDim2.new(0, 12, 0, 24), "BackgroundTransparency", 1, "Text", "0 griefs this session", "Font", Enum.Font.GothamMedium, "TextSize", 11, "TextColor3", Theme.TextSecondary, "TextXAlignment", Enum.TextXAlignment.Left)
        local histClose = Instance.new("TextButton", histHdr)
        local _hcS = _G.JAF_MobileOn() and 36 or 28
        histClose.Size = UDim2.new(0, _hcS, 0, _hcS); histClose.Position = UDim2.new(1, -(_hcS + 8), 0, 6)
        histClose.BackgroundColor3 = Theme.Error; histClose.Text = "X"
        histClose.Font = Enum.Font.GothamBold; histClose.TextSize = 14
        histClose.TextColor3 = _G.JAF_OnAccent(Theme.Error); histClose.AutoButtonColor = false
        Instance.new("UICorner", histClose).CornerRadius = UDim.new(0, 6)
        histClose.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
histGui.Enabled = false end)
        local histScroll = JUI.new("ScrollingFrame", histPanel, "Size", UDim2.new(1, -16, 1, -50), "Position", UDim2.new(0, 8, 0, 46), "BackgroundTransparency", 1, "BorderSizePixel", 0, "ScrollBarThickness", 4, "ScrollBarImageColor3", Theme.Accent1, "CanvasSize", UDim2.new(0, 0, 0, 0), "AutomaticCanvasSize", Enum.AutomaticSize.Y)
        local histLayout = JUI.new("UIListLayout", histScroll, "Padding", UDim.new(0, 6), "SortOrder", Enum.SortOrder.LayoutOrder)
        local histEmpty = JUI.new("TextLabel", histScroll, "Name", "Empty", "LayoutOrder", -1, "Size", UDim2.new(1, 0, 0, 40), "BackgroundTransparency", 1, "Text", "No griefs yet.", "Font", Enum.Font.GothamMedium, "TextSize", 12, "TextColor3", Theme.TextSecondary)
        local _buildHistRow; _buildHistRow = function(g, order) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local row = JUI.new("Frame", histScroll, "Name", "GriefRow", "LayoutOrder", order, "Size", UDim2.new(1, 0, 0, 56), "BackgroundColor3", Theme.Surface, "BorderSizePixel", 0)
            Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8)
            local rStroke = JUI.new("UIStroke", row, "Color", g.autojoiner and (AJ_COLORS[g.autojoiner] or Theme.Error) or Theme.Error, "Thickness", 1, "Transparency", 0.5)
            local pObj = Players:FindFirstChild(g.griefer)
            local uid = pObj and pObj.UserId or 1
            local av = JUI.new("ImageLabel", row, "Size", UDim2.new(0, 40, 0, 40), "Position", UDim2.new(0, 8, 0.5, -20), "BackgroundColor3", Theme.SurfaceHighlight, "BackgroundTransparency", 0.4, "Image", "rbxthumb://type=AvatarHeadShot&id=" .. uid .. "&w=60&h=60", "BorderSizePixel", 0)
            Instance.new("UICorner", av).CornerRadius = UDim.new(0, 8)
            local nameL = JUI.new("TextLabel", row, "Size", UDim2.new(1, -170, 0, 16), "Position", UDim2.new(0, 56, 0, 6), "BackgroundTransparency", 1, "Text", g.griefer, "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", Theme.Error, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
            local timeL = JUI.new("TextLabel", row, "Size", UDim2.new(0, 64, 0, 14), "Position", UDim2.new(1, -70, 0, 6), "BackgroundTransparency", 1, "Text", g.time or "", "Font", Enum.Font.GothamMedium, "TextSize", 11, "TextColor3", Theme.TextSecondary, "TextXAlignment", Enum.TextXAlignment.Right)
            local itemL = JUI.new("TextLabel", row, "Size", UDim2.new(1, -64, 0, 13), "Position", UDim2.new(0, 56, 0, 23), "BackgroundTransparency", 1, "Text", g.item or "", "Font", Enum.Font.GothamMedium, "TextSize", 11, "TextColor3", Theme.TextPrimary, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
            local bottomTxt = ""
            local si = g.stealInfo
            if si and si.displayName then
                bottomTxt = si.displayName
                if si.mutation and si.mutation ~= "None" and si.mutation ~= "Normal" then
                    bottomTxt = bottomTxt .. " [" .. si.mutation .. "]"
                end
                if si.genText and si.genText ~= "" then bottomTxt = bottomTxt .. "  " .. si.genText end
            end
            if bottomTxt ~= "" then
                local infoL = JUI.new("TextLabel", row, "Size", UDim2.new(1, -170, 0, 12), "Position", UDim2.new(0, 56, 0, 38), "BackgroundTransparency", 1, "Text", bottomTxt, "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", Theme.Accent1, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
            end
            if g.autojoiner then
                local col = AJ_COLORS[g.autojoiner] or Color3.fromRGB(255, 200, 0)
                local pill = JUI.new("TextLabel", row, "Size", UDim2.new(0, 108, 0, 18), "Position", UDim2.new(1, -116, 1, -24), "BackgroundColor3", col, "BackgroundTransparency", 0.1, "Text", string.upper(g.autojoiner) .. " USER", "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", _G.JAF_OnAccent(col), "BorderSizePixel", 0)
                Instance.new("UICorner", pill).CornerRadius = UDim.new(1, 0)
                local pStroke = JUI.new("UIStroke", pill, "Color", col, "Thickness", 1, "Transparency", 0.3)
            end
            return row
        end
        local _refreshGriefHistory; _refreshGriefHistory = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            for _, c in ipairs(histScroll:GetChildren()) do if c.Name == "GriefRow" then c:Destroy() end end
            histEmpty.Visible = (#griefHistory == 0)
            histCount.Text = #griefHistory .. (#griefHistory == 1 and " grief this session" or " griefs this session")
            local order = 0
            for i = #griefHistory, 1, -1 do _buildHistRow(griefHistory[i], order) order = order + 1 end
        end
        local vaHolder = JUI.new("Frame", mf, "Name", "ViewAllHolder", "LayoutOrder", 3, "Size", UDim2.new(1, 0, 0, 32), "BackgroundTransparency", 1)
        local viewAllBtn = JUI.new("TextButton", vaHolder, "Size", UDim2.new(1, 0, 0, 28), "Position", UDim2.new(0, 0, 0, 2), "AutoButtonColor", false, "BackgroundColor3", Theme.Accent1, "BackgroundTransparency", 0, "Text", "VIEW ALL", "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", _G.JAF_OnAccent(), "BorderSizePixel", 0)
        Instance.new("UICorner", viewAllBtn).CornerRadius = UDim.new(0, 6)
        viewAllBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            histGui.Enabled = not histGui.Enabled
            if histGui.Enabled then _refreshGriefHistory() end
        end)
        _G.JAF_AddGriefEntry = function(griefer, item, autojoiner, stealInfo) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if currentCard then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
currentCard:Destroy() end) end
            noGriefL.Visible = false
            local hasBot = (autojoiner ~= nil) or (stealInfo and stealInfo.displayName)
            local cardH  = hasBot and 68 or 52
            local card = JUI.new("Frame", content, "Size", UDim2.new(1, 0, 0, cardH), "BackgroundColor3", Theme.Surface, "BorderSizePixel", 0)
            card.ClipsDescendants = true
            Instance.new("UICorner", card).CornerRadius = UDim.new(0, 8)
            local cStroke = JUI.new("UIStroke", card, "Color", Theme.Error, "Thickness", 1, "Transparency", 0.5)
            currentCard = card
            local pObj = Players:FindFirstChild(griefer)
            local uid  = pObj and pObj.UserId or 1
            local av = JUI.new("ImageLabel", card, "Size", UDim2.new(0, 40, 0, 40), "Position", UDim2.new(0, 8, 0.5, -20), "BackgroundColor3", Theme.SurfaceHighlight, "BackgroundTransparency", 0.4, "Image", "rbxthumb://type=AvatarHeadShot&id=" .. uid .. "&w=60&h=60", "BorderSizePixel", 0)
            Instance.new("UICorner", av).CornerRadius = UDim.new(0, 8)
            local nameL = JUI.new("TextLabel", card, "Size", UDim2.new(1, -64, 0, 18), "Position", UDim2.new(0, 56, 0, 8), "BackgroundTransparency", 1, "Text", griefer, "Font", Enum.Font.GothamBold, "TextSize", 14, "TextColor3", Theme.Error, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
            local itemL = JUI.new("TextLabel", card, "Size", UDim2.new(1, -64, 0, 15), "Position", UDim2.new(0, 56, 0, 28), "BackgroundTransparency", 1, "Text", item, "Font", Enum.Font.GothamMedium, "TextSize", 12, "TextColor3", Theme.TextPrimary, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
            if hasBot then
                local subL = JUI.new("TextLabel", card, "Size", UDim2.new(1, -64, 0, 14), "Position", UDim2.new(0, 56, 0, 46), "BackgroundTransparency", 1, "Font", Enum.Font.GothamBold, "TextSize", 12, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
                if autojoiner then
                    subL.Text = autojoiner .. " user"
                    subL.TextColor3 = AJ_COLORS[autojoiner] or Color3.fromRGB(255, 200, 0)
                else
                    local si  = stealInfo
                    local txt = si.displayName or ""
                    if si.mutation and si.mutation ~= "None" and si.mutation ~= "Normal" then txt = txt .. " [" .. si.mutation .. "]" end
                    if si.genText and si.genText ~= "" then txt = txt .. "  " .. si.genText end
                    subL.Text = txt; subL.TextColor3 = Theme.Accent1
                end end
            local stamp = ""
            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
stamp = os.date("%H:%M:%S") end)
            table.insert(griefHistory, {griefer = griefer, item = item, autojoiner = autojoiner, stealInfo = stealInfo, time = stamp})
            if histGui.Enabled then pcall(_refreshGriefHistory) end
        end
        local function getStealInfoGD() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local stealingIndex = LPlayer:GetAttribute("StealingIndex")
            if not stealingIndex then return nil end
            local Sync = _G.JAF_GetSynchronizer()
            if not Sync then return nil end
            local AnimalsData   = _G.JAF_Mod("Datas", "Animals")
            local AnimalsShared = _G.JAF_Mod("Shared", "Animals")
            local NumberUtils   = _G.JAF_Mod("Utils", "NumberUtils")
            local okAD, okAS, okNU = AnimalsData ~= nil, AnimalsShared ~= nil, NumberUtils ~= nil
            local bestMatch, highestGen = nil, -1
            for _, channel in pairs(Sync:GetAllChannels()) do
                local cok, data = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return channel:GetTable() end)
                local _al2 = _G.JAF_AnimalsOf and _G.JAF_AnimalsOf(data) or (type(data) == "table" and (data.Animals or data.AnimalList))
                if not cok or type(data) ~= "table" or type(_al2) ~= "table" then continue end
                for _, entry in pairs(_al2) do
                    if type(entry) ~= "table" or tostring(entry.Index) ~= tostring(stealingIndex) then continue end
                    local info = okAD and AnimalsData[entry.Index]
                    local genValue = 0
                    if okAS then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
genValue = _G.JAF_SafeGen(entry.Index, entry.Mutation, entry.Traits, nil) end) end
                    if genValue > highestGen then
                        highestGen = genValue
                        bestMatch = {
                            displayName = (info and info.DisplayName) or entry.Index,
                            rawName     = entry.Index,
                            mutation    = entry.Mutation or "None",
                            traits      = (entry.Traits and #entry.Traits > 0) and entry.Traits or {},
                            genValue    = genValue,
                            genText     = okNU and ("$" .. NumberUtils:ToString(genValue) .. "/s") or "",
                        } end end end
            return bestMatch
        end
        local pendingGD     = {}
        local currentSIGD   = nil
        local stealEndedGD  = 0
        local function flushGD() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if #pendingGD == 0 then return end
            for _, e in ipairs(pendingGD) do pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G.JAF_AddGriefEntry(e.griefer, e.item, e.autojoiner, currentSIGD) end) end
            pendingGD = {}
            currentSIGD = nil
        end
        local function queueGD(griefer, item) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if Config.GriefDetectorEnabled == false then return end
            if typeof(item) == "string" and (item == "" or item == "#92FF67" or item:sub(1,1) == "#") then return end
            local isStealing = LPlayer:GetAttribute("Stealing") == true
            local inGrace    = (tick() - stealEndedGD) < 0.5
            if not isStealing and not inGrace then return end
            if isStealing and not currentSIGD then currentSIGD = getStealInfoGD() end
            for _, e in ipairs(pendingGD) do if e.griefer == griefer and e.item == item then return end end
            local aj = getAutojoinerName(griefer)
            table.insert(pendingGD, {griefer = griefer, item = item, autojoiner = aj})
        end
        LPlayer:GetAttributeChangedSignal("Stealing"):Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local val = LPlayer:GetAttribute("Stealing")
            if val == true then
                currentSIGD = getStealInfoGD()
            elseif val == nil or val == false then
                stealEndedGD = tick()
                task.delay(0.5, flushGD)
            end end)
        local getHRPgd; getHRPgd = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local char = LPlayer.Character
            return char and char:FindFirstChild("HumanoidRootPart")
        end
        local _gearHolderCacheGD = {}
        local _GRIEF_GEARS = { "Boogie Bomb", "Laser Cape", "Paintball Gun", "Medusa's Head", "Body Swap Potion", "Web Slinger" }
        task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            while true do
                if Config.GriefDetectorEnabled ~= false then
                    local plrs = Players:GetPlayers()
                    local seen = nil
                    for _, p in ipairs(plrs) do
                        if p ~= LPlayer then
                            local ch = p.Character
                            if ch then
                                for _, gear in ipairs(_GRIEF_GEARS) do
                                    if not (seen and seen[gear]) and ch:FindFirstChild(gear) then
                                        seen = seen or {}
                                        seen[gear] = true
                                        _gearHolderCacheGD[gear] = { name = p.Name, t = tick() }
                                    end end end end end end
                task.wait(_G.JAF_GriefHolderScan or 0.2)
            end end)
        local getHolder; getHolder = function(gearName) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            for _, p in ipairs(Players:GetPlayers()) do
                if p ~= LPlayer then
                    local ch = p.Character
                    if ch and ch:FindFirstChild(gearName) then _gearHolderCacheGD[gearName] = { name = p.Name, t = tick() } return p end
                end end
            local rc = _gearHolderCacheGD[gearName]
            if rc and (tick() - rc.t) < (_G.JAF_GriefHolderTTL or 5) then
                local p = Players:FindFirstChild(rc.name)
                if p then return p end
            end
            return nil
        end
        local function watchPartGD(part, onHit) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            part.Touched:Connect((function(hit) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local char = LPlayer.Character
                if char and hit:IsDescendantOf(char) then onHit() end
            end))
            local hcGD
            hcGD = RunService.Heartbeat:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if not part.Parent then hcGD:Disconnect() return end
                local hrp = getHRPgd()
                if hrp and (part.Position - hrp.Position).Magnitude <= 5 then onHit() end
            end) end
        local hitBulletsGD  = {}
        local sentryOwnersGD = {}
        local trackBulletGD; trackBulletGD = function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if not obj.Name:find("SentryBullet") or hitBulletsGD[obj] then return end
            local part = obj:IsA("BasePart") and obj or obj:FindFirstChildWhichIsA("BasePart")
            if not part then return end
            watchPartGD(part, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if hitBulletsGD[obj] then return end
                hitBulletsGD[obj] = true
                local owner = next(sentryOwnersGD) or "Unknown"
                queueGD(owner, "All Seeing Sentry")
                obj.AncestryChanged:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if not obj.Parent then hitBulletsGD[obj] = nil end end)
            end) end
        local hitMissilesGD = {}
        local trackMissileGD; trackMissileGD = function(child) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local username = child.Name:match("Missle_(.+)")
            if not username or username == LPlayer.Name then return end
            if not child:IsA("BasePart") then return end
            local function onHit() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if hitMissilesGD[child] then return end
                hitMissilesGD[child] = true
                queueGD(username, "Heatseeker")
                child.AncestryChanged:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if not child.Parent then hitMissilesGD[child] = nil end end)
            end
            child.Touched:Connect((function(hit) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local char = LPlayer.Character
                if char and hit:IsDescendantOf(char) then onHit() end
            end))
            local _mhConn
            _mhConn = RunService.Heartbeat:Connect((function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if not child.Parent then if _mhConn then _mhConn:Disconnect(); _mhConn = nil end return end
                if hitMissilesGD[child] then if _mhConn then _mhConn:Disconnect(); _mhConn = nil end return end
                local hrp = getHRPgd()
                if hrp and (child.Position - hrp.Position).Magnitude <= 15 then onHit() end
            end)) end
        local dogeLastHitGD  = 0
        local watchedDogesGD = setmetatable({}, {__mode = "k"})
        local watchDogeGD; watchDogeGD = function(child) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local username = child.Name:match("^PlayerName_(.+)_Doge$")
            if not username or username == LPlayer.Name or watchedDogesGD[child] then return end
            watchedDogesGD[child] = true
            local parts = child:IsA("BasePart") and {child} or child:GetDescendants()
            for _, part in ipairs(parts) do
                if not part:IsA("BasePart") then continue end
                part.Touched:Connect(function(hit) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    local char = LPlayer.Character
                    if not char or not hit:IsDescendantOf(char) then return end
                    if tick() - dogeLastHitGD < 2 then return end
                    dogeLastHitGD = tick()
                    queueGD(username, "Attack Doge")
                end) end end
        local laserDetectedGD = setmetatable({}, {__mode = "k"})
        local laserLastHitGD  = 0
        local function onLaserHitGD() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if tick() - laserLastHitGD < 2 then return end
            local holder = getHolder("Laser Cape")
            if not holder then return end
            laserLastHitGD = tick()
            queueGD(holder.Name, "Laser Cape")
        end
        local medusaLastHitGD = 0
        local medusaRadiusGD  = (33.986812591552734 + 33.986873626708984) / 2 / 2 * math.pi
        local bodyPartSetGD   = {
            LeftFoot=true,RightFoot=true,LeftLowerLeg=true,RightLowerLeg=true,
            LeftUpperLeg=true,RightUpperLeg=true,LeftHand=true,RightHand=true,
            LeftLowerArm=true,RightLowerArm=true,LeftUpperArm=true,RightUpperArm=true,
            UpperTorso=true,LowerTorso=true,Head=true
        }
        local bsHitCooldowns  = {}
        local bsLocalJustFired = false
        local bsPrevHolder, bsPrevLocal = nil, nil
        local function hookLocalPotionGD() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local char = LPlayer.Character
            if not char then return end
            local function hookPotion(pot) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                pot.Activated:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    bsLocalJustFired = true
                    task.delay(1, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
bsLocalJustFired = false end)
                end) end
            local pot = char:FindFirstChild("Body Swap Potion")
            if pot then hookPotion(pot) end
            char.ChildAdded:Connect(function(child) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if child.Name == "Body Swap Potion" then hookPotion(child) end
            end) end
        LPlayer.CharacterAdded:Connect(hookLocalPotionGD)
        hookLocalPotionGD()
        local paintballLastHitGD = 0
        workspace.ChildAdded:Connect(function(child) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if Config.GriefDetectorEnabled == false then return end
            local _cn = child.Name
            local sentryId = (_cn:sub(1, 7) == "Sentry_") and _cn:match("Sentry_(%d+)") or nil
            if sentryId then
                local ok2, name2 = pcall(Players.GetNameFromUserIdAsync, Players, tonumber(sentryId))
                sentryOwnersGD[ok2 and name2 or "Unknown"] = true
            end
            trackBulletGD(child)
            if child:IsA("Model") or child:IsA("Folder") then child.DescendantAdded:Connect(trackBulletGD) end
            trackMissileGD(child)
            watchDogeGD(child)
            if child:IsA("BasePart") and math.abs(child.Size.X-1)<=0.1 and math.abs(child.Size.Y-1)<=0.1 and math.abs(child.Size.Z-0.1)<=0.1 then
                local holder = getHolder("Paintball Gun")
                if holder then
                    local tConn, hConn
                    local function onPBHit() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                        if tick() - paintballLastHitGD < 2 then return end
                        paintballLastHitGD = tick()
                        queueGD(holder.Name, "Paintball Gun")
                        if tConn then tConn:Disconnect() end
                        if hConn then hConn:Disconnect() end
                    end
                    tConn = child.Touched:Connect(function(hit) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                        local char = LPlayer.Character
                        if char and hit:IsDescendantOf(char) then onPBHit() end
                    end)
                    hConn = RunService.Heartbeat:Connect((function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                        if not child.Parent then if tConn then tConn:Disconnect() end if hConn then hConn:Disconnect() end return end
                        local hrp = getHRPgd()
                        if hrp and (child.Position - hrp.Position).Magnitude <= 3 then onPBHit() end
                    end)) end end
            if child:IsA("BasePart") and child:FindFirstChildWhichIsA("Attachment")
                and math.abs(child.Size.X-0.2)<=0.01 and math.abs(child.Size.Y-0.2)<=0.01
                and math.abs(child.Size.Z-0.2)<=0.01 and not laserDetectedGD[child] then
                local holder = getHolder("Laser Cape")
                if holder then
                    local char = LPlayer.Character
                    local hrp = getHRPgd()
                    if hrp and (child.Position - hrp.Position).Magnitude <= 8 then
                        local holderHead = holder.Character and holder.Character:FindFirstChild("Head")
                        if holderHead and holderHead:FindFirstChildWhichIsA("Attachment") then
                            local myHead = char and char:FindFirstChild("Head")
                            if myHead and myHead:FindFirstChildWhichIsA("Attachment") then
                                local ha = myHead:FindFirstChildWhichIsA("Attachment")
                                local pa = child:FindFirstChildWhichIsA("Attachment")
                                if ha.WorldPosition == pa.WorldPosition then return end
                            end
                            laserDetectedGD[child] = true
                            onLaserHitGD()
                        end end end end
            if child:IsA("MeshPart") and bodyPartSetGD[child.Name] and tick() - medusaLastHitGD >= 3 then
                local holder = getHolder("Medusa's Head")
                if holder then
                    local holderHRP = holder.Character and holder.Character:FindFirstChild("HumanoidRootPart")
                    if holderHRP then
                        local startT = tick()
                        local mConn
                        mConn = RunService.Heartbeat:Connect((function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                            if tick() - medusaLastHitGD < 3 or tick() - startT > 1 then mConn:Disconnect() return end
                            local char = LPlayer.Character
                            local hrp = getHRPgd()
                            if not hrp then return end
                            local d = holderHRP.Position - hrp.Position
                            if math.sqrt(d.X^2 + d.Z^2) > medusaRadiusGD then return end
                            local localPart = char:FindFirstChild(child.Name)
                            if localPart and (child.Position - localPart.Position).Magnitude <= 2 then
                                medusaLastHitGD = tick()
                                mConn:Disconnect()
                                queueGD(holder.Name, "Medusa's Head")
                            end end)) end end end
            if child.Name == "WebRope" and child:IsA("RopeConstraint") then
                local holder = getHolder("Web Slinger")
                if holder then
                    local wConn
                    wConn = RunService.Heartbeat:Connect((function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                        if not child.Parent then wConn:Disconnect() return end
                        local char = LPlayer.Character
                        if not char then return end
                        if (child.Attachment0 and child.Attachment0:IsDescendantOf(char))
                            or (child.Attachment1 and child.Attachment1:IsDescendantOf(char)) then
                            queueGD(holder.Name, "Web Slinger")
                            wConn:Disconnect()
                        end end)) end end end )
        RunService.Heartbeat:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if Config.GriefDetectorEnabled == false then bsPrevHolder = nil; bsPrevLocal = nil; return end
            local hrp = getHRPgd()
            if hrp then
                for child in pairs(watchedDogesGD) do
                    if child.Parent then
                        local uname = child.Name:match("^PlayerName_(.+)_Doge$")
                        if uname and uname ~= LPlayer.Name then
                            local part = child:IsA("BasePart") and child or child:FindFirstChildWhichIsA("BasePart", true)
                            if part and (part.Position - hrp.Position).Magnitude <= 5 and tick() - dogeLastHitGD >= 2 then
                                dogeLastHitGD = tick()
                                queueGD(uname, "Attack Doge")
                            end end end end end
            local holder = getHolder("Body Swap Potion")
            if not holder then bsPrevHolder = nil; bsPrevLocal = nil; return end
            local cd = bsHitCooldowns[holder.Name]
            if cd and tick() - cd < 30 then return end
            local holderHRP = holder.Character and holder.Character:FindFirstChild("HumanoidRootPart")
            local localHRP  = getHRPgd()
            if not holderHRP or not localHRP then return end
            local cH, cL = holderHRP.Position, localHRP.Position
            if bsPrevHolder and bsPrevLocal then
                if (cH - bsPrevLocal).Magnitude <= 12 and (cL - bsPrevHolder).Magnitude <= 12 and not bsLocalJustFired then
                    bsHitCooldowns[holder.Name] = tick()
                    queueGD(holder.Name, "Body Swap Potion")
                end end
            bsPrevHolder = cH; bsPrevLocal = cL
        end)
        task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local ok2, notifFolder = pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                return LPlayer.PlayerGui:WaitForChild("Notification", 30):WaitForChild("Notification", 10)
            end)
            if not ok2 or not notifFolder then return end
            notifFolder.ChildAdded:Connect(function(child) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if not child:IsA("TextLabel") then return end
                local text = child.Text
                if not (text:find("ragdoll") or text:find("balloon") or text:find("rocket")) then return end
                for _, player in ipairs(Players:GetPlayers()) do
                    if player == LPlayer then continue end
                    if text:find(player.Name, 1, true) then
                        local cmd = text:match('"([^"]+)"')
                        if cmd then local cmdName = cmd:sub(1,1):upper() .. cmd:sub(2) queueGD(player.Name, cmdName) end
                        return
                    end end end) end)
        task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local ok2, Net = pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                return ReplicatedStorage:WaitForChild("Packages", 10):WaitForChild("Net", 10)
            end)
            if not ok2 or not Net then return end
            for _, v in ipairs(Net:GetChildren()) do
                if v.Name == "Ragdoll" and v:IsA("RemoteEvent") then v.OnClientEvent:Connect(onLaserHitGD) end
                if v.Name:sub(1,3) ~= "RE/" then continue end
                local children = Net:GetChildren()
                local remote = children[table.find(children, v) + 1]
                if not remote or not remote:IsA("RemoteEvent") then continue end
                remote.OnClientEvent:Connect(function(...) LPH_ATTRIBUTES(PRESET(FAST), ERROR_HANDLING(false))
                    local n = select("#", ...)
                    local hit, rawGear = ...
                    if n < 1 or typeof(hit) ~= "Instance" or hit.Name ~= "Hit" then return end
                    local gear   = (typeof(rawGear) == "string" and rawGear ~= "" and not rawGear:match("^#%x+$") and not tonumber(rawGear))
                                   and rawGear or (hit.Parent and hit.Parent.Name or "Unknown")
                    local char   = hit.Parent and hit.Parent.Parent
                    if not char then return end
                    local plr    = Players:GetPlayerFromCharacter(char)
                    local attacker = plr and plr.Name or char.Name
                    if attacker ~= "Unknown" and attacker ~= LPlayer.Name then queueGD(attacker, gear) end
                end) end end)
        do
            local _LightingGD = game:GetService("Lighting")
            local _LAST = Enum.RenderPriority.Last.Value
            local _cleanCam, _shakeDeadline, _shakeBound = nil, 0, false
            local function _ensureAntiShake() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if _G.JAF_BlockBoogieShake == false then return end
                _shakeDeadline = tick() + (tonumber(_G.JAF_BoogieShakeTime) or 11.5)
                if _shakeBound then return end
                _shakeBound = true
                RunService:BindToRenderStep("JAF_BoogieCapClean", _LAST - 1, (function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    local cam = workspace.CurrentCamera
                    if cam then _cleanCam = cam.CFrame end
                end))
                RunService:BindToRenderStep("JAF_BoogieRestore", _LAST + 1, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    if tick() >= _shakeDeadline then
                        _shakeBound = false
                        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
RunService:UnbindFromRenderStep("JAF_BoogieCapClean") end)
                        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
RunService:UnbindFromRenderStep("JAF_BoogieRestore") end)
                        return
                    end
                    local cam = workspace.CurrentCamera
                    if cam and _cleanCam then cam.CFrame = _cleanCam end
                end) end
            local _boogieFxAt = 0
            local function _onBoogieFx(inst) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if not inst:IsA("ColorCorrectionEffect") or inst.Name ~= "DiscoEffect" then return end
                _ensureAntiShake()
                if tick() - _boogieFxAt < 2 then return end
                local holder = getHolder("Boogie Bomb")
                if not holder then return end
                _boogieFxAt = tick()
                queueGD(holder.Name, "Boogie Bomb")
            end
            _LightingGD.ChildAdded:Connect(_onBoogieFx)
            for _, c in ipairs(_LightingGD:GetChildren()) do pcall(_onBoogieFx, c) end
        end
        for _, c in ipairs(workspace:GetChildren()) do
            local sentryId = c.Name:match("Sentry_(%d+)")
            if sentryId then
                local ok2, name2 = pcall(Players.GetNameFromUserIdAsync, Players, tonumber(sentryId))
                sentryOwnersGD[ok2 and name2 or "Unknown"] = true
            end
            trackBulletGD(c)
            watchDogeGD(c)
            trackMissileGD(c)
        end end)
if not _G.JAF_MenuKeyBound then
    _G.JAF_MenuKeyBound = true
    local function _menuKeyCode() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local k
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if type(Config.MenuKey) == "string" and Config.MenuKey ~= "" then k = Enum.KeyCode[Config.MenuKey] end end)
        return k or Enum.KeyCode.LeftControl
    end
    function _G.JAF_ToggleMenu(force) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local ui = _G.JustAFanSettingsUI
        if not (ui and ui.panel) then return false end
        local v = (force ~= nil) and force or (not ui.panel.Visible)
        ui.panel.Visible = v
        if v and _G.JAF_MobileFitPanel then pcall(_G.JAF_MobileFitPanel) end
        return v
    end
    UserInputService.InputBegan:Connect(function(input, gp) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if gp then return end
        if input.UserInputType ~= Enum.UserInputType.Keyboard then return end
        if input.KeyCode == _menuKeyCode() then _G.JAF_ToggleMenu() end
    end) end
_G.JAF_TapSlop     = 8
_G.JAF_HoldSeconds = 0.4
function _G.JAF_HubButtonWanted() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local m = Config.HubButtonMode
    if m == "on"  then return true  end
    if m == "off" then return false end
    return _G.JAF_MobileOn() and Config.MobileHubButton ~= false
end
buildMobileBarUI = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local old = hubFind("JustAFanMobileBar")
    if old then old:Destroy() end
    _G.JAF_MobileBarUI = nil
    if not HubGui then return end
    if not _G.JAF_HubButtonWanted() then return end
    local _lvp = _G.JAF_Viewport()
    local SZ = math.floor(math.clamp(math.min(_lvp.X, _lvp.Y) * 0.06, 46, 62) + 0.5)
    local sg = JUI.new("ScreenGui", nil, "Name", "JustAFanMobileBar", "ResetOnSpawn", false, "DisplayOrder", 1600000001, "ZIndexBehavior", Enum.ZIndexBehavior.Sibling, "IgnoreGuiInset", true)
    _G.JAF_SafeInsets(sg)
    sg.Parent = HubGui
    local btn = JUI.new("TextButton", nil, "Name", "Launcher", "Size", UDim2.new(0, SZ, 0, SZ))
    do
        local sp = Config.MobileBtnPos
        if type(sp) == "table" and tonumber(sp.X) and tonumber(sp.Y) then
            btn.Position = UDim2.new(math.clamp(sp.X, 0, 0.93), 0, math.clamp(sp.Y, 0, 0.9), 0)
        else
            btn.Position = UDim2.new(1, -(SZ + 14), 0, _G.JAF_TopInset() + 10)
        end end
    _P(btn, "BackgroundColor3", Theme.Background, "AutoButtonColor", false, "BorderSizePixel", 0, "Text", "", "Active", true, "ZIndex", 10, "Parent", sg)
    Instance.new("UICorner", btn).CornerRadius = UDim.new(1, 0)
    local bStroke = JUI.new("UIStroke", btn, "Color", Theme.TextPrimary, "Thickness", 2, "Transparency", 0.25)
    local mark = JUI.new("ImageLabel", btn, "Size", UDim2.new(1, 0, 1, 0), "Position", UDim2.new(0.5, 0, 0.5, 0), "AnchorPoint", Vector2.new(0.5, 0.5), "BackgroundTransparency", 1, "ZIndex", 11, "ScaleType", Enum.ScaleType.Crop, "Image", _G.JAF_DawgResolved or "")
    Instance.new("UICorner", mark).CornerRadius = UDim.new(1, 0)
    if not (_G.JAF_DawgResolved and _G.JAF_DawgResolved ~= "") then
        local fb = JUI.new("TextLabel", btn, "Size", UDim2.new(1, 0, 1, 0), "BackgroundTransparency", 1, "ZIndex", 11, "Text", "SD", "Font", Enum.Font.GothamBlack, "TextSize", 20, "TextColor3", Theme.TextPrimary)
        task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local ok, a = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return resolveDawgImage() end)
            if ok and type(a) == "string" and a ~= "" and mark and mark.Parent then
                mark.Image = a; if fb and fb.Parent then fb:Destroy() end
            end end) end
    local TRAY_W = 208
    local tray = JUI.new("Frame", sg, "Name", "Tray", "Size", UDim2.new(0, TRAY_W, 0, 260), "BackgroundColor3", Theme.Background, "BackgroundTransparency", 0.06, "BorderSizePixel", 0, "Visible", false, "ZIndex", 8)
    Instance.new("UICorner", tray).CornerRadius = UDim.new(0, 14)
    local tStroke = JUI.new("UIStroke", tray, "Color", Theme.Accent1, "Thickness", 1.5, "Transparency", 0.3)
    local tTitle = JUI.new("TextLabel", tray, "Size", UDim2.new(1, -16, 0, 24), "Position", UDim2.new(0, 12, 0, 6), "BackgroundTransparency", 1, "Text", "QUICK ACTIONS", "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", Theme.TextSecondary, "TextXAlignment", Enum.TextXAlignment.Left, "ZIndex", 9)
    local tScroll = JUI.new("ScrollingFrame", tray, "Name", "Actions", "Size", UDim2.new(1, -12, 1, -38), "Position", UDim2.new(0, 6, 0, 32), "BackgroundTransparency", 1, "BorderSizePixel", 0, "ScrollBarThickness", 3, "ScrollBarImageColor3", Theme.Accent1)
    _P(tScroll, "CanvasSize", UDim2.new(0, 0, 0, 0), "AutomaticCanvasSize", Enum.AutomaticSize.Y, "ScrollingDirection", Enum.ScrollingDirection.Y, "ZIndex", 9)
    local tGrid = JUI.new("UIGridLayout", tScroll, "CellSize", UDim2.new(0, 92, 0, 44), "CellPadding", UDim2.new(0, 6, 0, 6), "SortOrder", Enum.SortOrder.LayoutOrder)
    local ACTIONS = {
        {"HUB",       function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_G.JAF_ToggleMenu() end},
        {"TP",        function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.tpToBestBrainrot then _G.tpToBestBrainrot() end end},
        {"CLONE",     function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.instantClone then _G.instantClone() end end},
        {"CARPET",    function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_ToggleCarpetSpeed then _G.JAF_ToggleCarpetSpeed() end end},
        {"STEAL SPD", function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if SharedState and SharedState.StealSpeedToggleFunc then SharedState.StealSpeedToggleFunc() end end},
        {"RESET",     function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_DoReset then _G.JAF_DoReset() end end},
        {"RAGDOLL",   function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_RagdollSelf then _G.JAF_RagdollSelf() end end},
        {"DROP",      function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_DropBrainrot then pcall(_G.JAF_DropBrainrot) end end},
        {"FLOAT",     function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_ToggleFloat then _G.JAF_ToggleFloat() end end},
        {"AUTO BUY",  function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_ToggleAutoBuy then _G.JAF_ToggleAutoBuy() end end},
        {"KICK",      function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_KickPlayer then _G.JAF_KickPlayer() end end},
        {"CLICK AP",  function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_ToggleClickToAP then _G.JAF_ToggleClickToAP() end end},
        {"PROX AP",   function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_ToggleProximityAP then _G.JAF_ToggleProximityAP() end end},
        {"INVIS",     function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            pcall(_G.toggleInvisibleSteal)
            if _G.updateMovementPanelInvisVisual then pcall(_G.updateMovementPanelInvisVisual, _G.invisibleStealEnabled or false) end
        end},
        {"MINI UI",   function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local g = hubFind("JustAFanMiniActions")
            local mp = g and g:FindFirstChild("MiniPanel")
            if mp then mp.Visible = not mp.Visible end
        end},
        {"AUTOSTEAL", function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local g = hubFind("AutoStealUI"); if g then g.Enabled = not g.Enabled end
        end},
    }
    for idx, a in ipairs(ACTIONS) do
        local b = JUI.new("TextButton", tScroll, "LayoutOrder", idx, "BackgroundColor3", (idx == 1) and Theme.Accent1 or Theme.Surface, "TextColor3", (idx == 1) and _G.JAF_OnAccent() or Theme.TextPrimary)
        _P(b, "Font", Enum.Font.GothamBold, "TextSize", 12, "Text", a[1], "AutoButtonColor", false, "BorderSizePixel", 0, "ZIndex", 10, "TextTruncate", Enum.TextTruncate.AtEnd)
        Instance.new("UICorner", b).CornerRadius = UDim.new(0, 9)
        local st = JUI.new("UIStroke", b, "Color", Theme.SurfaceHighlight, "Thickness", 1, "Transparency", 0.4)
        _G.JAF_OnTap(b, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local was = b.BackgroundColor3
            b.BackgroundColor3 = Theme.Success
            task.delay(0.14, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if b and b.Parent then b.BackgroundColor3 = was end end)
            pcall(a[2])
        end) end
    local function guiSize() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local s = sg.AbsoluteSize
        if s.X < 10 then return _G.JAF_Viewport() end
        return s
    end
    local function btnPx() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local s = guiSize()
        return btn.Position.X.Scale * s.X + btn.Position.X.Offset, btn.Position.Y.Scale * s.Y + btn.Position.Y.Offset
    end
    local function layoutTray() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local s = guiSize()
        local bx, by = btnPx()
        local h = math.clamp(s.Y - by - 16, 140, 300)
        tray.Size = UDim2.new(0, TRAY_W, 0, h)
        local x = (bx + SZ + 8 + TRAY_W <= s.X) and (bx + SZ + 8) or math.max(4, bx - TRAY_W - 8)
        local y = math.clamp(by, 4, math.max(4, s.Y - h - 4))
        tray.Position = UDim2.new(0, math.floor(x), 0, math.floor(y))
    end
    local function setTray(v) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false)) if v then layoutTray() end tray.Visible = v end
    local activeIn, startPos, startX, startY, moved, consumed, holdToken
    btn.InputBegan:Connect(function(i) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if activeIn then return end
        if i.UserInputType ~= Enum.UserInputType.Touch and i.UserInputType ~= Enum.UserInputType.MouseButton1 then return end
        activeIn, startPos, moved, consumed = i, i.Position, false, false
        startX, startY = btnPx()
        holdToken = {}
        local myToken = holdToken
        task.delay(_G.JAF_HoldSeconds, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if holdToken == myToken and activeIn == i and not moved and not consumed then
                consumed = true
                setTray(not tray.Visible)
                _P(bStroke, "Color", Theme.Success, "Thickness", 3, "Transparency", 0)
                task.delay(0.25, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    if bStroke and bStroke.Parent then _P(bStroke, "Color", Theme.TextPrimary, "Thickness", 2, "Transparency", 0.25) end
                end) end end)
        _G.JAF_OnInputEnd(i, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if activeIn ~= i then return end
            activeIn, holdToken = nil, nil
            if moved then
                local s = guiSize()
                local px, py = btnPx()
                Config.MobileBtnPos = { X = px / math.max(s.X, 1), Y = py / math.max(s.Y, 1) }
                _G.JAF_SaveConfigSoon()
                if tray.Visible then layoutTray() end
            elseif not consumed then
                _G.JAF_ToggleMenu()
            end end) end)
    local mvConn
    mvConn = UserInputService.InputChanged:Connect((function(i) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not btn.Parent then if mvConn then mvConn:Disconnect(); mvConn = nil end return end
        if not activeIn then return end
        if activeIn.UserInputType == Enum.UserInputType.Touch then
            if i ~= activeIn then return end
        elseif i.UserInputType ~= Enum.UserInputType.MouseMovement then
            return
        end
        local d = i.Position - startPos
        if not moved and (math.abs(d.X) > _G.JAF_TapSlop or math.abs(d.Y) > _G.JAF_TapSlop) then moved = true end
        if not moved then return end
        local s = guiSize()
        btn.Position = UDim2.new(0, math.clamp(startX + d.X, 0, math.max(0, s.X - SZ)),
                                 0, math.clamp(startY + d.Y, 0, math.max(0, s.Y - SZ)))
        if tray.Visible then layoutTray() end
    end))
    if not Config.MobileHintShown then
        local hint = JUI.new("TextLabel", sg, "Size", UDim2.new(0, 190, 0, 34))
        local hx, hy = btnPx()
        _P(hint, "Position", UDim2.new(0, math.max(4, math.floor(hx)), 0, math.floor(hy) + SZ + 8), "BackgroundColor3", Theme.Surface, "BackgroundTransparency", 0.1, "Text", "Tap = hub   \u{2022}   Hold = actions", "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", Theme.TextPrimary, "BorderSizePixel", 0, "ZIndex", 9)
        Instance.new("UICorner", hint).CornerRadius = UDim.new(0, 9)
        Config.MobileHintShown = true
        pcall(SaveConfig)
        task.delay(7, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if hint and hint.Parent then hint:Destroy() end end)
    end
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        while btn.Parent do
            local ui = _G.JustAFanSettingsUI
            local p  = ui and ui.panel
            local covered = p and p.Parent and p.Visible and p.Parent.Enabled and p:GetAttribute("JAFSheetPinned") == true
            if btn.Visible == (covered and true or false) then btn.Visible = not covered end
            if covered and tray.Visible then setTray(false) end
            task.wait(1)
        end end)
    _G.JAF_MobileBarUI = { gui = sg, button = btn, tray = tray, setTray = setTray }
end
_G.JAF_SheetMaxW = 760
function _G.JAF_SheetPin(p) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not (p and p.Parent) then return false end
    local ps = p.Parent.AbsoluteSize
    if ps.X < 10 then return false end
    local M = 8
    local k = _G.JAF_PanelScale(p)
    local availW, availH = ps.X - M * 2, ps.Y - M * 2
    local wantW = math.min(availW, _G.JAF_SheetMaxW)
    p.Size = UDim2.new(0, math.max(280, wantW / k), 0, math.max(220, availH / k))
    p.Position = UDim2.new(0, math.floor(M + (availW - wantW) / 2), 0, M)
    p:SetAttribute("JAFSheetPinned", true)
    return true
end
_G.JAF_MobileApplySheet = function(panel, side, navBox, content, cArea, topbar, tScrolls) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if not panel or not panel.Parent then return end
    if not (_G.JAF_MobileOn() and Config.MobileSheet ~= false) then return end
    local ps = panel.Parent.AbsoluteSize
    if ps.X < 10 then return end
    local STRIP = 46
    if not _G.JAF_SheetPin(panel) then return end
    if side then
        side.Size = UDim2.new(1, 0, 0, STRIP)
        side.Position = UDim2.new(0, 0, 0, 0)
        for _, ch in ipairs(side:GetChildren()) do if ch:IsA("GuiObject") and ch ~= navBox then ch.Visible = false end end
        if navBox then
            local wrap = side:FindFirstChild("JAFMobileNavWrap")
            if not wrap then
                wrap = Instance.new("ScrollingFrame")
                _P(wrap, "Name", "JAFMobileNavWrap", "BackgroundTransparency", 1, "BorderSizePixel", 0, "ScrollBarThickness", 0, "ScrollingDirection", Enum.ScrollingDirection.X, "AutomaticCanvasSize", Enum.AutomaticSize.X, "CanvasSize", UDim2.new(0, 0, 0, 0), "Parent", side)
            end
            _P(wrap, "Visible", true, "Size", UDim2.new(1, -10, 1, -8), "Position", UDim2.new(0, 5, 0, 4))
            local ll = navBox:FindFirstChildOfClass("UIListLayout")
            if ll then ll.FillDirection = Enum.FillDirection.Horizontal ll.Padding = UDim.new(0, 5) end
            _P(navBox, "Parent", wrap, "Position", UDim2.new(0, 0, 0, 0), "Size", UDim2.new(0, 0, 1, 0), "AutomaticSize", Enum.AutomaticSize.X, "Visible", true)
            for _, b in ipairs(navBox:GetChildren()) do if b:IsA("GuiObject") then b.Size = UDim2.new(0, 92, 1, 0) end end
        end end
    if content then content.Size = UDim2.new(1, 0, 1, -STRIP) content.Position = UDim2.new(0, 0, 0, STRIP) end
    local ROW_MIN = 40
    local function bumpText(o) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not (o:IsA("TextLabel") or o:IsA("TextButton") or o:IsA("TextBox")) then return end
        if o.TextScaled or o:GetAttribute("JAFTextBumped") then return end
        o:SetAttribute("JAFTextBumped", true)
        o.TextSize = math.min(o.TextSize + 2, 15)
    end
    _G.JAF_MobileApplyRows = function(sf) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if typeof(sf) ~= "Instance" then return end
        for _, row in ipairs(sf:GetChildren()) do
            if row:IsA("GuiObject") and row.Size.Y.Scale == 0 and row.Size.Y.Offset > 0 then
                if row.Size.X.Scale == 0.5 then
                    row.Size = UDim2.new(1, 0, 0, math.max(row.Size.Y.Offset, ROW_MIN))
                elseif row.Size.X.Scale == 1 and row.Size.Y.Offset >= 30 and row.Size.Y.Offset < ROW_MIN then
                    row.Size = UDim2.new(1, 0, 0, ROW_MIN)
                end end
            bumpText(row)
            for _, d in ipairs(row:GetDescendants()) do bumpText(d) end
        end end
    if tScrolls then for _, sf in pairs(tScrolls) do _G.JAF_MobileApplyRows(sf) end end
    if navBox then
        for _, d in ipairs(navBox:GetDescendants()) do bumpText(d) end
        local okTS, TS = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return game:GetService("TextService") end)
        for _, b in ipairs(navBox:GetChildren()) do
            if b:IsA("GuiObject") then
                local lb = b:FindFirstChild("TabLbl")
                local w = 92
                if okTS and TS and lb and lb:IsA("TextLabel") and lb.Text ~= "" then
                    local okM, sz = pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                        return TS:GetTextSize(lb.Text, lb.TextSize, lb.Font, Vector2.new(1000, 100))
                    end)
                    if okM and sz then w = math.clamp(math.ceil(sz.X) + 42 + 14, 84, 190) end
                end
                b.Size = UDim2.new(0, w, 1, 0)
            end end end
    pcall(_G.JAF_MobileSliderPass, panel)
end
function _G.JAF_MobileFitPanel() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local ui = _G.JustAFanSettingsUI
    local p = ui and ui.panel
    if not (p and p.Parent and p.Parent.Parent) then return end
    local ps = p.Parent.AbsoluteSize
    if ps.X < 10 then return end
    if _G.JAF_MobileOn() and Config.MobileSheet ~= false then _G.JAF_SheetPin(p) return end
    if p:GetAttribute("JAFSheetPinned") then p:SetAttribute("JAFSheetPinned", nil) end
    local w, h = p.AbsoluteSize.X, p.AbsoluteSize.Y
    local curX = p.Position.X.Scale * ps.X + p.Position.X.Offset
    local curY = p.Position.Y.Scale * ps.Y + p.Position.Y.Offset
    local x = math.clamp(curX, 0, math.max(0, ps.X - w))
    local y = math.clamp(curY, 0, math.max(0, ps.Y - h))
    if math.abs(x - curX) > 0.5 or math.abs(y - curY) > 0.5 then p.Position = UDim2.new(0, math.floor(x), 0, math.floor(y)) end
end
function _G.JAF_MobileSweepAll() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    for _, n in ipairs({"JustAFanSettings", "JustAFanMiniActions",
                        "AutoStealUI", "JAFAdvTpSpeed", "XiAdminPanel", "JustAFanGrabKick"}) do
        local g = hubFind(n)
        if g then pcall(_G.JAF_MobileSliderPass, g) end
    end end
_G.JAF_OnMobileChange(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    pcall(buildMobileBarUI)
    if buildJustAFanSettingsUI then pcall(buildJustAFanSettingsUI) end
    pcall(_G.JAF_MobileSweepAll)
    pcall(_G.JAF_MobileFitPanel)
end)
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    pcall(buildMobileBarUI)
    _G.JAF_WaitHeavy()
    for _, dt in ipairs({0.5, 2, 3, 5}) do task.wait(dt) pcall(_G.JAF_MobileSweepAll) end
end)
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local lastK
    while true do
        task.wait(1)
        if _G.JAF_MobileOn() and Config.MobileSheet ~= false then
            local ui = _G.JustAFanSettingsUI
            local p = ui and ui.panel
            if p and p.Parent then local k = _G.JAF_PanelScale(p) if k ~= lastK then lastK = k; pcall(_G.JAF_MobileFitPanel) end end
        end end end)
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local cam = Workspace.CurrentCamera
    if not cam then return end
    cam:GetPropertyChangedSignal("ViewportSize"):Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        task.wait(0.15)
        pcall(_G.JAF_MobileFitPanel)
        if _G.JAF_HubButtonWanted() then pcall(buildMobileBarUI) end
    end) end)
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    task.wait(1.5)
    if Config.ThemeImageUrl and Config.ThemeImageUrl ~= "" and jafApplyImageTheme then
        pcall(jafApplyImageTheme, Config.ThemeImageUrl)
    elseif Config.CurrentTheme == "dawg" and THEMES and THEMES["dawg"] then
        applyTheme("dawg")
    end end)
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    for _, dt in ipairs({2, 2, 3, 4}) do task.wait(dt) if jafUnifyPanelStyle then pcall(jafUnifyPanelStyle) end end
end)
if not _SABCAP.JAF_DropBrainrot then
    local _dropActive  = false
    local _DROP_MIN    = 0.15
    local _DROP_MAX    = 6.0
    local _INVIS_WAIT  = 2.0
    local _DROP_BASE_MAX = 200
    local _dropRestore, _dropRestoreRoot
    local _dropRoot; _dropRoot = function(char) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not char then return nil end
        local hum = char:FindFirstChildOfClass("Humanoid")
        local r = hum and hum.RootPart
        if r and r.Parent then return r end
        r = char:FindFirstChild("HumanoidRootPart")
        return (r and r.Parent) and r or nil
    end
    local function _invisOrphan() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local cam = Workspace.CurrentCamera
        if not cam then return false end
        for _, c in ipairs(cam:GetChildren()) do if c:IsA("BasePart") and c.Name == "HumanoidRootPart" then return true end end
        return false
    end
    local _stealing; _stealing = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        return LocalPlayer:GetAttribute("Stealing") == true
    end
    local _flingLoop; _flingLoop = (function(char0) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local t0, movel = os.clock(), 0.1
        while _dropActive do
            RunService.Heartbeat:Wait()
            if not SAB_LIVE() then break end
            local el = os.clock() - t0
            if el >= _DROP_MIN and not _stealing() then break end
            if el > _DROP_MAX then _G.JAF_DropRecToken = nil; break end
            if LocalPlayer.Character ~= char0 then _G.JAF_DropRecToken = nil; break end
            if _G.JAF_DropSuppressOnce == false then
                _G.JAF_HitRecCooldownUntil = tick() + 3
            else
                _G.JAF_DropRecToken = tick() + (tonumber(_G.JAF_DropRecTokenTail) or 2)
            end
            local root = _dropRoot(char0)
            if not (char0.Parent and root) then break end
            local vel = root.AssemblyLinearVelocity
            if vel.Magnitude > _DROP_BASE_MAX then vel = Vector3.zero end
            _dropRestore, _dropRestoreRoot = vel, root
            root.AssemblyLinearVelocity = vel * 10000 + Vector3.new(0, 10000, 0)
            RunService.RenderStepped:Wait()
            if char0.Parent and root.Parent then root.AssemblyLinearVelocity = vel end
            RunService.Stepped:Wait()
            if char0.Parent and root.Parent then root.AssemblyLinearVelocity = vel + Vector3.new(0, movel, 0) movel = -movel end
        end
        _dropRestoreRoot = nil
    end)
    _SABCAP.JAF_DropBrainrot = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if _dropActive then return false end
        if _G.JAF_DropBrainrotEnabled == false then return false end
        if not SAB_LIVE() then return false end
        if _G.JAF_TpActive or _G.JAF_ResetStowActive then return false end
        _dropActive = true
        task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local diedConn, safetyConn
            pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if _G.invisibleStealEnabled == true and _G.toggleInvisibleSteal then
                    pcall(_G.toggleInvisibleSteal)
                    _G.invisAutoActive = false
                    local _iw = os.clock()
                    while os.clock() - _iw < _INVIS_WAIT do
                        if _G.invisibleStealEnabled ~= true and not _invisOrphan() and _dropRoot(LocalPlayer.Character) then break end
                        RunService.Heartbeat:Wait()
                    end end
                if _G.invisibleStealEnabled == true or _invisOrphan() then return end
                local char0 = LocalPlayer.Character
                local hum0  = char0 and char0:FindFirstChildOfClass("Humanoid")
                if hum0 then diedConn = hum0.Died:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_dropActive = false end) end
                if _G.JAF_DropSuppressOnce == false then
                    _G.JAF_HitRecCooldownUntil = tick() + 3
                else
                    _G.JAF_DropRecToken = tick() + (tonumber(_G.JAF_DropRecTokenTail) or 2)
                end
                _G.JAF_DropFlingActive = true
                safetyConn = RunService.PreSimulation:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    local _r = _dropRestoreRoot
                    if not _r or not _r.Parent then return end
                    local v = _r.AssemblyLinearVelocity
                    local cap = tonumber(_G.JAF_DropFlingVelCap) or 500
                    if v.Magnitude > cap then _r.AssemblyLinearVelocity = _dropRestore or Vector3.zero end
                end)
                _flingLoop(char0)
            end)
            _G.JAF_DropFlingActive = false
            _dropActive = false
            if diedConn then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
diedConn:Disconnect() end) end
            if safetyConn then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
safetyConn:Disconnect() end) end
            _dropRestore, _dropRestoreRoot = nil, nil
            local _r = _dropRoot(LocalPlayer.Character)
            if _r then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_r.AssemblyAngularVelocity = Vector3.zero end) end
        end)
        return true
    end
    _SABCAP.JAF_DropBrainrotActive = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _dropActive end
end
if not _SABCAP.JAF_InstantReset then
    local _resetRemote   = nil
    local _rawFS         = nil
    local _resetFlooding = false
    local _armCapture
    do
        local _arming = false
        _armCapture = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if _resetRemote or _arming then return end
        if not hookfunction or _G.JAF_ResetCapture == false then return end
        _arming = true
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local newcc = newcclosure or function(f) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return f end
            local _t0, _restored = os.clock(), false
            local function _restore(why) LPH_ATTRIBUTES(ERROR_HANDLING(false))
                if _restored then return end
                _restored = true
                pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hookfunction(Instance.new("RemoteEvent").FireServer, _rawFS) end)
                _G.JAF_ResetCaptureMs = (os.clock() - _t0) * 1000
                _G.JAF_ResetCaptureWhy = why
                _arming = false
            end
            local CALLER = tostring(_G.JAF_ResetCaller or "ToolActivationController")
            local _gcs = getcallingscript
            local _fireCount = {}
            local _capture; _capture = (function(self, a1) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local nm = self.Name
                if nm:sub(1, 3) ~= "RE/" then return end
                if not nm:match("^RE/%x%x%x%x%x%x%x%x") then return end
                local s = _gcs()
                if not s or s.Name ~= CALLER then return end
                local c = (_fireCount[self] or 0) + 1
                _fireCount[self] = c
                if c >= (tonumber(_G.JAF_ResetMinFires) or 2) then
                    _resetRemote = self
                    _G.JAF_ResetRemote = self
                    _G.JAF_ResetRemoteName = nm
                    task.defer(_restore, "captured")
                end end)
            _rawFS = hookfunction(Instance.new("RemoteEvent").FireServer, newcc((function(self, ...) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if not _resetRemote and type((...)) == "string" then pcall(_capture, self, (...)) end
                return _rawFS(self, ...)
            end)))
            task.delay(tonumber(_G.JAF_ResetCaptureMax) or 8, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_restore("timeout") end)
        end) end end
    _G.JAF_ResetArm = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_armCapture() end
    if _G.JAF_ResetCaptureAtLoad == true then task.delay(4, _armCapture) end
    _SABCAP.JAF_InstantReset = function(times) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local function _stowOnce() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local held = false
            pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local ch = LocalPlayer.Character
                if not ch then return end
                for _, t in ipairs(ch:GetChildren()) do if t:IsA("Tool") then held = true; break end end
                if not held then return end
                local hum = ch:FindFirstChildOfClass("Humanoid")
                if hum then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hum:UnequipTools() end) end
                for _, t in ipairs(ch:GetChildren()) do if t:IsA("Tool") then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
t.Parent = LocalPlayer.Backpack end) end end
            end)
            return held
        end
        if _G.JAF_ResetJunkFire ~= true then
            if _resetFlooding then return true end
            _resetFlooding = true
            task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if _G.JAF_ResetStowTools ~= false then _G.JAF_ResetStowActive = true _stowOnce() RunService.Heartbeat:Wait() end
                local _prevShield = _G.JAF_HpShield
                _G.JAF_HpShield = false
                if _G.JAF_HpShieldOff then pcall(_G.JAF_HpShieldOff) end
                local _done = false
                local _finish = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    if _done then return end
                    _done = true
                    _G.JAF_HpShield = _prevShield
                    _G.JAF_ResetStowActive = false
                    _resetFlooding = false
                end
                local _rc; _rc = LocalPlayer.CharacterAdded:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    if _rc then _rc:Disconnect(); _rc = nil end
                    _finish()
                end)
                task.delay(tonumber(_G.JAF_ResetRestoreMax) or 8, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    if _rc then _rc:Disconnect(); _rc = nil end
                    _finish()
                end)
                pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    local ch = LocalPlayer.Character
                    local hum = ch and ch:FindFirstChildOfClass("Humanoid")
                    if hum then
                        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hum:SetStateEnabled(Enum.HumanoidStateType.Dead, true) end)
                        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hum.BreakJointsOnDeath = true end)
                        hum:ChangeState(Enum.HumanoidStateType.Dead)
                    end end) end)
            return true
        end
        local r = _resetRemote or _G.JAF_ResetRemote
        if not r then
            _armCapture()
            local _tw = os.clock()
            local _lim = tonumber(_G.JAF_ResetArmWait) or 3.5
            while not _resetRemote and (os.clock() - _tw) < _lim do task.wait(0.1) end
            r = _resetRemote or _G.JAF_ResetRemote
        end
        if not r then return false end
        if not r.Parent then _G.JAF_ResetRemoteDead = true return false end
        _G.JAF_ResetRemoteDead = false
        if _resetFlooding then return true end
        _resetFlooding = true
        local _stowed = false
        if _G.JAF_ResetStowTools ~= false then _G.JAF_ResetStowActive = true _stowed = _stowOnce() end
        local fs = (_G.JAF_ResetUseRawFS == true and _rawFS) or function(rr, a) LPH_ATTRIBUTES(ERROR_HANDLING(false))
return rr:FireServer(a) end
        local count = math.clamp(tonumber(times) or tonumber(_G.JAF_ResetBurst) or 50, 1, 50)
        task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local stop, fired = false, 0
            local conn
            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
conn = LocalPlayer.CharacterRemoving:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
stop = true end) end)
            if _G.JAF_ResetStowTools ~= false then
                local wait0  = math.clamp(tonumber(_G.JAF_ResetStowWait) or 0.25, 0, 1)
                local cap0   = math.clamp(tonumber(_G.JAF_ResetStowCap) or 1, wait0, 3)
                local tEnf   = os.clock()
                local tClean = _stowed and tEnf or (tEnf - wait0)
                while not stop and (os.clock() - tEnf) < cap0 do
                    if _stowOnce() then tClean = os.clock() end
                    if (os.clock() - tClean) >= wait0 then break end
                    RunService.Heartbeat:Wait()
                end end
            local _doFire; _doFire = (function(rr) LPH_ATTRIBUTES(ERROR_HANDLING(false))
fs(rr, "randomstring") end)
            local fire; fire = (function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(_doFire, r); fired = fired + 1 end)
            local _oldChar = LocalPlayer.Character
            local t0 = os.clock()
            local lim = tonumber(_G.JAF_ResetMaxTime) or 3
            for _ = 1, count do
                if stop or LocalPlayer.Character ~= _oldChar then break end
                if (os.clock() - t0) >= lim then break end
                fire()
                RunService.Heartbeat:Wait()
            end
            _P(_G, "JAF_ResetLastFires", fired, "JAF_ResetLastMs", (os.clock() - t0) * 1000, "JAF_ResetLastOk", (stop or LocalPlayer.Character ~= _oldChar))
            if conn then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
conn:Disconnect() end) end
            _G.JAF_ResetStowActive = false
            _resetFlooding = false
        end)
        return true
    end end
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local armed = true
    local lastAutoReset = 0
    while true do
        task.wait(_G.JAF_BalloonScanInterval or 0.35)
        if Config.AutoResetOnBalloon then
            local pg = PlayerGui
            local hit = false
            if pg then
                local _scope = pg
                local _nf = pg:FindFirstChild("Notification")
                if _nf then _scope = _nf:FindFirstChild("Notification") or _nf end
                for _, gui in ipairs(_scope:GetDescendants()) do
                    local cn = gui.ClassName
                    if cn == "TextLabel" or cn == "TextButton" then
                        local txt = gui.ContentText
                        if txt == nil or txt == "" then txt = gui.Text end
                        if type(txt) == "string" and #txt >= 12 then
                            local low = txt:lower()
                            local _s, _e = low:find("balloon[^%a]-on you")
                            if _s then
                                local _nxt = low:sub(_e + 1, _e + 1)
                                if _nxt == "" or not _nxt:match("%w") then hit = true break end
                            end end end end end
            if hit then
                if armed and (tick() - lastAutoReset) > (tonumber(_G.JAF_AutoResetCooldown) or 4) then
                    armed = false
                    lastAutoReset = tick()
                    if _G.JAF_ExecuteReset then pcall(_G.JAF_ExecuteReset) end
                end
            else
                armed = true
            end
        else
            armed = true
        end end end)
do
    local SCALED_GUIS = {
        "XiAdminPanel", "XiAdminToolsPanel", "XiStealingHUD", "AutoStealUI", "JustAFanSettings",
        "JustAFanMiniActions",
        "JustAFanStatusHUD", "JustAFanWalkingBestBar",
        "JustAFanGrabKick", "PriorityListGUI", "AutoStealPriorityEditor", "JAFAdvTpSpeed",
        "XiGriefDetector", "XiGriefHistory",
    }
    if not Config.PanelScalesMigrated then
        Config.PanelScalesMigrated = true
        if type(Config.PanelScales) == "table" then
            for _, n in ipairs({ "XiAdminToolsPanel", "JustAFanWalkingBestBar", "PriorityListGUI",
                                 "AutoStealPriorityEditor", "JAFAdvTpSpeed", "XiGriefDetector",
                                 "XiGriefHistory" }) do
                Config.PanelScales[n] = nil
            end end
        pcall(SaveConfig)
    end
    local FIT_PAD = 14
    local FIT_MIN = 0.35
    local function fitScale(want, w, h, vp) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if w <= 0 or h <= 0 then return want end
        local maxK = math.min((vp.X - FIT_PAD) / w, (vp.Y - FIT_PAD) / h)
        if maxK < want then return math.max(maxK, FIT_MIN) end
        return want
    end
    local MIN_VISIBLE = 0.6
    local function clampOnScreen(ch, vp) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local w, h = ch.AbsoluteSize.X, ch.AbsoluteSize.Y
        if w < 8 or h < 8 then return end
        if w > vp.X or h > vp.Y then return end
        local px, py = ch.Position.X, ch.Position.Y
        local ax, ay = ch.AnchorPoint.X * w, ch.AnchorPoint.Y * h
        local curX = px.Scale * vp.X + px.Offset - ax
        local curY = py.Scale * vp.Y + py.Offset - ay
        local visW = math.max(0, math.min(curX + w, vp.X) - math.max(curX, 0))
        local visH = math.max(0, math.min(curY + h, vp.Y) - math.max(curY, 0))
        if visW >= w * MIN_VISIBLE and visH >= h * MIN_VISIBLE then return end
        local newX = math.clamp(curX, 0, vp.X - w)
        local newY = math.clamp(curY, 0, vp.Y - h)
        if math.abs(newX - curX) < 2 and math.abs(newY - curY) < 2 then return end
        local function put(u, want, span) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if u.Scale ~= 0 and span > 0 then return UDim.new(want / span, 0) end
            return UDim.new(0, math.floor(want))
        end
        ch.Position = UDim2.new(put(px, newX + ax, vp.X), put(py, newY + ay, vp.Y))
    end
    local lastSize = setmetatable({}, { __mode = "k" })
    local function sizeSettled(ch) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local now = ch.AbsoluteSize
        local was = lastSize[ch]
        lastSize[ch] = now
        return was ~= nil and math.abs(was.X - now.X) < 1 and math.abs(was.Y - now.Y) < 1
    end
    _G.JAF_ApplyUIScale = function(scale) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        scale = math.clamp(tonumber(scale) or 1, 0.5, 1.5)
        Config.UIScale = scale
        local panelScales = Config.PanelScales or {}
        local dragging = (tick() - (_G.JAF_DragBusy or 0)) < 0.6
        for _, name in ipairs(SCALED_GUIS) do
            local sg = hubFind(name)
            if sg then
                local per = math.clamp(tonumber(panelScales[name]) or 1, 0.5, 1.5)
                local vp = sg.AbsoluteSize
                if vp.X < 10 then vp = _G.JAF_Viewport() end
                for _, ch in ipairs(sg:GetChildren()) do
                    if ch:IsA("GuiObject") then
                        local fullBleed = ch.Size.X.Scale >= 0.99 and ch.Size.Y.Scale >= 0.99
                        local base = tonumber(ch:GetAttribute("JAFBaseScale")) or 1
                        if base <= 0.05 or base > 1 then base = 1 end
                        local eff = scale * per * base
                        if fullBleed then
                            eff = 1
                        elseif not ch:GetAttribute("JAFSheetPinned") then
                            eff = fitScale(eff, ch.Size.X.Offset, ch.Size.Y.Offset, vp)
                        end
                        local us = ch:FindFirstChild("JAFUIScale")
                        if not us then us = Instance.new("UIScale"); us.Name = "JAFUIScale"; us.Parent = ch end
                        for _, o in ipairs(ch:GetChildren()) do if o ~= us and o:IsA("UIScale") then o:Destroy() end end
                        if math.abs(us.Scale - eff) > 0.0005 then us.Scale = eff end
                        local settled = sizeSettled(ch)
                        if not fullBleed and not dragging and settled
                           and not ch:GetAttribute("JAFSheetPinned") then
                            clampOnScreen(ch, vp)
                        end end end end end
        if _G.JAF_MobileFitPanel then pcall(_G.JAF_MobileFitPanel) end
    end
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local t0 = tick()
        while true do pcall(_G.JAF_ApplyUIScale, Config.UIScale or 1) task.wait((tick() - t0 < 15) and 0.2 or 2) end
    end)
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local ok, cg = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return (gethui and gethui()) or game:GetService("CoreGui") end)
        if not (ok and cg) then return end
        cg.ChildAdded:Connect(function(c) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if not c:IsA("ScreenGui") then return end
            task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(_G.JAF_ApplyUIScale, Config.UIScale or 1) end)
            task.delay(0.15, function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(_G.JAF_ApplyUIScale, Config.UIScale or 1) end)
        end) end)
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local cam = Workspace.CurrentCamera
        if not cam then return end
        cam:GetPropertyChangedSignal("ViewportSize"):Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
task.wait(0.12) pcall(_G.JAF_ApplyUIScale, Config.UIScale or 1) end)
    end) end
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local HUB_GUIS = {
        "AutoStealUI", "JustAFanSettings", "JustAFanMiniActions", "XiAdminPanel",
        "XiAdminToolsPanel", "JustAFanStatusHUD",
        "JustAFanWalkingBestBar", }
    local hubSet = {}
    for _, n in ipairs(HUB_GUIS) do hubSet[n] = true end
    local function activate(d) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if d:IsA("Frame") and not d.Active and d.BackgroundTransparency < 1 then d.Active = true end
    end
    local function hookGui(g) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if g:GetAttribute("JAFClickThruHooked") then return end
        g:SetAttribute("JAFClickThruHooked", true)
        for _, d in ipairs(g:GetDescendants()) do activate(d) end
        g.DescendantAdded:Connect(activate)
    end
    local pg = PlayerGui
    if pg then
        for _, d in ipairs({1, 2, 3}) do task.wait(d) for _, n in ipairs(HUB_GUIS) do local g = hubFind(n) if g then hookGui(g) end end end
        local function onChild(c) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false)) if hubSet[c.Name] then task.defer(hookGui, c) end end
        pg.ChildAdded:Connect(onChild)
        if HubGui ~= pg then HubGui.ChildAdded:Connect(onChild) end
    end end)
buildJustAFanMiniActionsUI = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local pg = HubGui
    if not pg then return end
    local oldG = hubFind("JustAFanMiniActions")
    if oldG then oldG:Destroy() end
    _G.JAF_MiniUIConns = _G.JAF_MiniUIConns or {}
    for _, _c in ipairs(_G.JAF_MiniUIConns) do pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_c:Disconnect() end) end
    table.clear(_G.JAF_MiniUIConns)
    local function _miniUISConn(sig, fn) LPH_ATTRIBUTES(ERROR_HANDLING(false))
local c = sig:Connect(fn); table.insert(_G.JAF_MiniUIConns, c); return c end
    local maGui = Instance.new("ScreenGui")
    _P(maGui, "Name", "JustAFanMiniActions", "ResetOnSpawn", false, "DisplayOrder", 25, "Parent", pg)
    local W = 230
    local BTN_H = 28
    local PANEL_H = 314
    local ROW  = Theme.Background:Lerp(Color3.fromRGB(255, 255, 255), 0.07)
    local ROWH = Theme.Background:Lerp(Color3.fromRGB(255, 255, 255), 0.12)
    local EDGE = Color3.fromRGB(255, 255, 255)
    local panel = JUI.new("Frame", maGui, "Name", "MiniPanel", "Size", UDim2.new(0, W, 0, PANEL_H))
    local savedPos = Config.MiniUIPos or {X=0.01, Y=0.35}
    _P(panel, "Position", UDim2.new(savedPos.X, 0, savedPos.Y, 0), "BackgroundColor3", Theme.Background, "BackgroundTransparency", 0.02, "BorderSizePixel", 0, "Visible", (Config.ShowMiniActions ~= false))
    Instance.new("UICorner", panel).CornerRadius = UDim.new(0, 10)
    local pStroke = Instance.new("UIStroke", panel)
    _P(pStroke, "Color", Theme.Accent1, "Thickness", 1.2, "Transparency", 0.55)
    attachResizeGrip(panel, "JustAFanMiniActions", 0.85)
    local header = JUI.new("Frame", panel, "Size", UDim2.new(1, 0, 0, 32), "BackgroundTransparency", 1)
    local hdrTick = JUI.new("Frame", header, "Size", UDim2.new(0, 3, 0, 14), "Position", UDim2.new(0, 10, 0.5, -7), "BackgroundColor3", Theme.Accent1, "BorderSizePixel", 0)
    JUI.round(hdrTick)
    local titleLbl = JUI.new("TextLabel", header, "Size", UDim2.new(1, -70, 1, 0), "Position", UDim2.new(0, 20, 0, 0), "BackgroundTransparency", 1, "Text", "Mini UI", "Font", Enum.Font.GothamBold, "TextSize", 16, "TextColor3", Color3.fromRGB(255, 255, 255), "TextXAlignment", Enum.TextXAlignment.Left)
    do
        local dragging, dragStart, startPos
        local function dragMove(inp) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if not (dragging and dragStart) then return end
            if inp.UserInputType ~= Enum.UserInputType.MouseMovement and inp.UserInputType ~= Enum.UserInputType.Touch then return end
            local delta = inp.Position - dragStart
            panel.Position = UDim2.new( startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y )
        end
        header.InputBegan:Connect(function(inp) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if Config.UILocked then return end
            if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then
                dragging = true
                dragStart = inp.Position
                startPos = panel.Position
                _G.JAF_OnInputEnd(inp, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    dragging = false
                    if panel and panel.Parent then
                        local ps = panel.Parent.AbsoluteSize
                        Config.MiniUIPos = { X = panel.AbsolutePosition.X / ps.X, Y = panel.AbsolutePosition.Y / ps.Y, }
                        _G.JAF_SaveConfigSoon()
                    end end) end end)
        header.InputChanged:Connect(dragMove)
        _miniUISConn(UserInputService.InputChanged, dragMove)
    end
    local div = JUI.new("Frame", panel, "Size", UDim2.new(1, -16, 0, 1), "Position", UDim2.new(0, 8, 0, 32), "BackgroundColor3", Theme.SurfaceHighlight, "BorderSizePixel", 0)
    local cont = JUI.new("Frame", panel, "Name", "MiniContent", "Size", UDim2.new(1, -12, 1, -60), "Position", UDim2.new(0, 6, 0, 36), "BackgroundTransparency", 1, "BorderSizePixel", 0)
    local ll = JUI.new("UIListLayout", cont, "Padding", UDim.new(0, 4), "SortOrder", Enum.SortOrder.LayoutOrder)
    local function miniSec(lbl, ord) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local r = JUI.new("Frame", cont, "Size", UDim2.new(1, 0, 0, 14), "BackgroundTransparency", 1, "LayoutOrder", ord)
        local t = JUI.new("TextLabel", r, "Size", UDim2.new(1, -6, 1, 0), "Position", UDim2.new(0, 2, 0, 0), "BackgroundTransparency", 1, "Text", lbl, "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", Theme.TextSecondary, "TextXAlignment", Enum.TextXAlignment.Left)
    end
    local function mToggle(offText, onText, order, isOn, onClick) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local base = tostring(offText):gsub("%s*:%s*OFF%s*$", "")
        local b = JUI.new("TextButton", cont, "Size", UDim2.new(1, 0, 0, BTN_H), "BackgroundColor3", ROW, "BackgroundTransparency", 0, "Text", "", "BorderSizePixel", 0, "AutoButtonColor", false, "LayoutOrder", order)
        Instance.new("UICorner", b).CornerRadius = UDim.new(0, 7)
        local bs = Instance.new("UIStroke", b)
        _P(bs, "Color", EDGE, "Thickness", 1, "Transparency", 0.9)
        local lb = JUI.new("TextLabel", b, "Size", UDim2.new(1, -64, 1, 0), "Position", UDim2.new(0, 10, 0, 0), "BackgroundTransparency", 1, "Text", base, "Font", Enum.Font.GothamBold, "TextSize", 13, "TextColor3", Theme.TextPrimary, "TextXAlignment", Enum.TextXAlignment.Left, "TextTruncate", Enum.TextTruncate.AtEnd)
        local chip = JUI.new("TextLabel", b, "Size", UDim2.new(0, 42, 0, 18), "Position", UDim2.new(1, -50, 0.5, -9), "BackgroundColor3", Theme.Background, "Text", "OFF", "Font", Enum.Font.GothamBold, "TextSize", 11, "TextColor3", Theme.TextSecondary, "BorderSizePixel", 0)
        Instance.new("UICorner", chip).CornerRadius = UDim.new(1, 0)
        local cs2 = Instance.new("UIStroke", chip)
        _P(cs2, "Color", EDGE, "Thickness", 1, "Transparency", 0.85)
        b.MouseEnter:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
b.BackgroundColor3 = ROWH; bs.Transparency = 0.75 end)
        b.MouseLeave:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
b.BackgroundColor3 = ROW;  bs.Transparency = 0.9  end)
        local function refresh() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local on = isOn() and true or false
            _P(chip, "Text", on and "ON" or "OFF", "BackgroundColor3", on and Theme.Accent1 or Theme.Background, "TextColor3", on and _G.JAF_OnAccent() or Theme.TextSecondary)
            cs2.Transparency = on and 1 or 0.85
        end
        refresh()
        b.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
onClick(refresh) end)
        return b, refresh
    end
    local function halfBtn(parent, right, text, bg, fg, strokeCol, strokeTr) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local b = JUI.new("TextButton", parent, "Size", UDim2.new(0.5, -3, 1, 0), "Position", right and UDim2.new(0.5, 3, 0, 0) or UDim2.new(0, 0, 0, 0), "BackgroundColor3", bg, "BackgroundTransparency", 0, "Text", text, "Font", Enum.Font.GothamBold, "TextSize", 12, "TextColor3", fg, "AutoButtonColor", false, "BorderSizePixel", 0)
        Instance.new("UICorner", b).CornerRadius = UDim.new(0, 7)
        _P(Instance.new("UIStroke", b), "Color", strokeCol, "Thickness", 1, "Transparency", strokeTr)
        return b
    end
    miniSec("AUTO", 30)
    do
        local _, updateAKBtn = mToggle("Auto Kick: OFF", "Auto Kick: ON", 32,
            function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.AutoKickOnSteal end,
            function(refresh) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                Config.AutoKickOnSteal = not Config.AutoKickOnSteal
                SaveConfig()
                refresh()
                if _G.setAutoKickFromSettings then _G.setAutoKickFromSettings(Config.AutoKickOnSteal) end
            end)
        _G.setAutoKickFromMiniUI = updateAKBtn
    end
    do
        local _, updKPBtn = mToggle("Kick to PS: OFF", "Kick to PS: ON", 33,
            function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Config.KickToPrivateOnSteal end,
            function(refresh) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                Config.KickToPrivateOnSteal = not Config.KickToPrivateOnSteal
                SaveConfig()
                refresh()
                if _G.setKickToPrivateFromSettings then _G.setKickToPrivateFromSettings(Config.KickToPrivateOnSteal) end
            end)
        _G.setKickToPrivateFromMiniUI = updKPBtn
    end
    do
        local _, updAB = mToggle("Auto Buy: OFF", "Auto Buy: ON", 34,
            function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return (_G.JAF_AutoBuyActive and _G.JAF_AutoBuyActive()) or false end,
            function(refresh) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if _G.JAF_ToggleAutoBuy then _G.JAF_ToggleAutoBuy() end
                task.defer(refresh)
            end)
        _G.JAF_RefreshMiniAutoBuy = updAB
    end
    _G.setAutoKickFromSettings = function(val) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        Config.AutoKickOnSteal = val
        SaveConfig()
        if _G.setAutoKickFromMiniUI then _G.setAutoKickFromMiniUI() end
    end
    _G.setKickToPrivateFromSettings = function(val) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        Config.KickToPrivateOnSteal = val
        SaveConfig()
        if _G.setKickToPrivateFromMiniUI then _G.setKickToPrivateFromMiniUI() end
    end
    miniSec("SESSION", 40)
    do
        local kickKey = Config.KickKey ~= "" and Config.KickKey or "NONE"
        local row = JUI.new("Frame", cont, "Size", UDim2.new(1, 0, 0, BTN_H), "BackgroundTransparency", 1, "LayoutOrder", 41)
        local rej = halfBtn(row, false, "Rejoin", ROW, Theme.TextPrimary, EDGE, 0.9)
        rej.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            task.delay(0.5, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local ok = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, LocalPlayer) end)
                if not ok then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
TeleportService:Teleport(game.PlaceId, LocalPlayer) end) end
            end) end)
        local kb = halfBtn(row, true, "Kick (" .. kickKey .. ")", Color3.fromRGB(180, 40, 60), Color3.new(1, 1, 1), Color3.fromRGB(200, 60, 80), 0.4)
        kb.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false)) kickPlayer() end)
    end
    miniSec("QUICK ACTIONS", 45)
    do
        local row = JUI.new("Frame", cont, "Size", UDim2.new(1, 0, 0, BTN_H), "BackgroundTransparency", 1, "LayoutOrder", 46)
        local dbBtn = halfBtn(row, false, "Drop Brainrot", ROW, Theme.TextPrimary, EDGE, 0.9)
        local dbWatching = false
        dbBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if not _G.JAF_DropBrainrot then return end
            local started = false
            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
started = _G.JAF_DropBrainrot() end)
            if not started or dbWatching then return end
            dbWatching = true
            _P(dbBtn, "Text", "Dropping...", "BackgroundColor3", Color3.fromRGB(180, 40, 60))
            task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local t0 = os.clock()
                while os.clock() - t0 < 12 do
                    task.wait(0.05)
                    local act = _G.JAF_DropBrainrotActive
                    if not (act and act()) then break end
                end
                if dbBtn and dbBtn.Parent then _P(dbBtn, "Text", "Drop Brainrot", "BackgroundColor3", Theme.Surface) end
                dbWatching = false
            end) end)
        local irBtn = halfBtn(row, true, "Instant Reset", ROW, Theme.TextPrimary, EDGE, 0.9)
        local irCooling = false
        irBtn.MouseButton1Click:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if irCooling then return end
            irCooling = true
            if _G.JAF_ExecuteReset then pcall(_G.JAF_ExecuteReset) end
            _P(irBtn, "Text", "Resetting...", "BackgroundColor3", Color3.fromRGB(180, 40, 60))
            task.delay(0.5, function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if irBtn and irBtn.Parent then _P(irBtn, "Text", "Instant Reset", "BackgroundColor3", Theme.Surface) end
                irCooling = false
            end) end) end
    do
        JUI.new("Frame", cont, "Size", UDim2.new(1, 0, 0, 10), "BackgroundTransparency", 1, "LayoutOrder", 47)
        mToggle("Auto Destroy Turrets: OFF", "Auto Destroy Turrets: ON", 48,
            function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _G.JAF_AutoTurretActive and _G.JAF_AutoTurretActive() end,
            function(refresh) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if _G.JAF_ToggleAutoTurret then pcall(_G.JAF_ToggleAutoTurret) end
                task.defer(refresh)
            end) end
    do
        local rows, totalH = {}, 0
        for _, ch in ipairs(cont:GetChildren()) do
            if ch:IsA("GuiObject") then
                local h = ch.Size.Y.Offset
                if h > 0 then rows[#rows + 1] = { r = ch, h = h }; totalH = totalH + h end
            end end
        if totalH > 0 and #rows > 1 then
            local padTotal = 4 * (#rows - 1)
            for _, e in ipairs(rows) do local frac = e.h / totalH e.r.Size = UDim2.new(1, 0, frac, -math.floor(padTotal * frac + 0.5)) end
        end end
    _G.JustAFanMiniActionsUI = {panel = panel, gui = maGui}
    return maGui
end
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    _G.JAF_WaitHeavy()
    buildJustAFanMiniActionsUI()
    if _G.JAF_ThemeImageAsset and jafApplyThemeImageToPanels then
        task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(jafApplyThemeImageToPanels, _G.JAF_ThemeImageAsset) end)
    end end)
do
    if Config.AutoBuyRange == nil then Config.AutoBuyRange = 17 end
    local autoBuyActive = false
    local abOn; abOn = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return autoBuyActive and SAB_LIVE() end
    local BL={STOLEN=true,STEAL=true,PURCHASE=true,COMPRAR=true,BUY=true,COLLECT=true,COLETAR=true,CASH=true,VALUE=true,BASE=true,EMPTY=true,GENERATION=true,COMMON=true,UNCOMMON=true,RARE=true,EPIC=true,LEGENDARY=true,DIVINE=true,RAINBOW=true,CURSED=true,GOLD=true,DIAMOND=true,CANDY=true,MUTATION=true}
    local cvOk; cvOk = (function(t) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if not t or t=="" then return false end; local c=(t:gsub("<[^>]+>","")):match("^%s*(.-)%s*$") or ""; if #c<=1 then return false end; local u=c:upper(); if u:find("^%$") or u:find("/S$") or u:find("^[%d%.]+") then return false end; return not BL[u] end)
    local pgv; pgv = (function(t) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if type(t)~="string" then return nil end; local u=t:gsub("<[^>]+>",""):upper(); if not u:find("%$") or not u:find("/S") then return nil end; local c=u:gsub("%$",""):gsub("/S",""):gsub("%s+",""); local n=tonumber(c:match("[%d%.]+")); if not n then return nil end; if c:find("B") then return n*1e9 elseif c:find("M") then return n*1e6 elseif c:find("K") then return n*1e3 else return n end end)
    local exM; exM = (function(m) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if not m then return nil,nil,0 end; local bN,bG,bV=nil,nil,0; for _,bb in ipairs(m:GetDescendants()) do if bb:IsA("BillboardGui") or bb:IsA("SurfaceGui") then for _,d in ipairs(bb:GetDescendants()) do if d:IsA("TextLabel") and d.Text then local v=pgv(d.Text); if v and v>bV then bV=v;bG=d.Text:gsub("<[^>]+>",""); local co=d.Parent; if co then local f=nil; for _,s in ipairs(co:GetChildren()) do if s:IsA("TextLabel") and s.Name=="DisplayName" then local c2=(s.Text or ""):gsub("<[^>]+>",""):match("^%s*(.-)%s*$"); if cvOk(c2) then f=c2;break end end end; if not f then local bt,bl=nil,0; for _,s in ipairs(co:GetChildren()) do if s:IsA("TextLabel") then local c2=(s.Text or ""):gsub("<[^>]+>",""):match("^%s*(.-)%s*$") or ""; if cvOk(c2) and #c2>bl then bt,bl=c2,#c2 end end end; if bt then f=bt end end; if f then bN=f end end end end end end end; return bN,bG,bV end)
    local scan; scan = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local res,vis={},{}; local deb=Workspace:FindFirstChild("Debris") or Workspace; for _,c in ipairs(deb:GetChildren()) do if c:IsA("Model") or c:IsA("BasePart") then local n,g,gv=exM(c); if gv and gv>0 then local p=c:IsA("BasePart") and c or (c:IsA("Model") and c.PrimaryPart); if not p then for _,ch in ipairs(c:GetChildren()) do if ch:IsA("BasePart") then p=ch;break end end end; if p then table.insert(vis,{name=n,gen=g,gv=gv,part=p,model=c}) end end end end; for _,obj in ipairs(game:GetService("CollectionService"):GetTagged("AnimalPurchasePrompt")) do if obj:IsA("ProximityPrompt") and obj.Enabled then local tx=(obj.ActionText or ""):lower(); if tx:find("purchase") or tx:find("comprar") or tx:find("buy") then local pp=obj.Parent; if not pp then continue end; local rp=pp:IsA("Attachment") and pp.Parent or pp; if not(rp and rp:IsA("BasePart")) then continue end; local fN,fG,fGV,fM="Brainrot","",0,nil; local md,mt=15,nil; for _,v in ipairs(vis) do local d=(v.part.Position-rp.Position).Magnitude; if d<md then md=d;mt=v end end; if mt then fN=mt.name or "Brainrot";fG=mt.gen or "";fGV=mt.gv or 0;fM=mt.model else local sr=rp;local cu=rp; while cu and cu.Parent and cu.Parent~=Workspace do sr=cu;cu=cu.Parent end; local n,g,gv=exM(sr); if n then fN=n end; if g then fG=g end; if gv and gv>0 then fGV=gv end; fM=sr end; table.insert(res,{name=fN,gen=fG,gv=fGV,prompt=obj,part=rp,model=fM,uid="conv_"..tostring(obj)}) end end end; return res end
    local _abDirty = true
    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
game:GetService("CollectionService"):GetInstanceAddedSignal("AnimalPurchasePrompt"):Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_abDirty = true end) end)
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not game:IsLoaded() then game.Loaded:Wait() end
        local _abLast = 0
        while true do
            if autoBuyActive and (_abDirty or (tick() - _abLast) >= (tonumber(_G.JAF_AutoBuyScanRate) or 0.25)) then
                _abDirty = false; _abLast = tick()
                local ok2, found = pcall(scan); if ok2 and found then SharedState.ConveyorAnimals = found end
            end
            if autoBuyActive then RunService.Heartbeat:Wait() else task.wait(0.5) end
        end end)
    local _fbCS, AUTOBUY_FAST_MAX = game:GetService("CollectionService"), 61
    _G.JAF_AutoBuyFastMax = AUTOBUY_FAST_MAX
    local _fbRawFire, _fbRE, _fbTokA, _fbTokB, _fbOff
    local _fbResolve; _fbResolve = (function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local getup, getco = debug and debug.getupvalues, debug and debug.getconstants
        if not (getup and getconnections) then return false end
        for _, pr in ipairs(_fbCS:GetTagged("AnimalPurchasePrompt")) do
            local ok, conns = pcall(getconnections, pr.Triggered); if not (ok and conns) then continue end
            for _, c in ipairs(conns) do
                local fn = c.Function; if typeof(fn) ~= "function" then continue end
                local ok2, ups = pcall(getup, fn); if not (ok2 and ups) then continue end
                local f, re; for _, v in pairs(ups) do if typeof(v) == "function" then f = f or v elseif typeof(v) == "Instance" and v:IsA("RemoteEvent") then re = re or v end end
                if not (f and re and re.Parent) then continue end
                _fbRawFire, _fbRE = f, re
                local ok3, consts = pcall(getco, fn); local strs, nums = {}, {}
                if ok3 and consts then
                    for _, v in pairs(consts) do if type(v) == "string" and #v == 36 and v:find("^%x+%-%x+%-%x+%-%x+%-%x+$") then strs[#strs + 1] = v elseif type(v) == "number" then nums[#nums + 1] = v end end
                    _fbTokA, _fbTokB, _fbOff = strs[1], strs[2], nums[1]
                end
                _fbTokA = _fbTokA or "3d3ab689-343b-4632-8337-3ee5eb6e505e"; _fbTokB = _fbTokB or "3ce04c41-b21f-4b2a-b93a-037427eac16d"; _fbOff = _fbOff or 43
                return true
            end end
        return false
    end)
    _G.JAF_FastBuyReady = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _fbRE ~= nil and _fbRE.Parent ~= nil end
    local firePurchaseFast; firePurchaseFast = (function(prompt) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local uid = prompt and prompt:GetAttribute("UID"); if not uid then return false end
        if (not _fbRE or _fbRE.Parent == nil) and not _fbResolve() then return false end
        local t, u = Workspace:GetServerTimeNow() + _fbOff, tostring(uid)
        _fbRawFire(_fbRE, t, _fbTokA, u); _fbRE:FireServer(t, _fbTokB, u)
        _G.JAF_LastAutoBuyFire = tick(); return true
    end)
    local function buyIndex(p) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local m = p:FindFirstAncestorWhichIsA("Model")
        while m and m:GetAttribute("Index") == nil and m.Parent do m = m.Parent:FindFirstAncestorWhichIsA("Model") end
        return m and m:GetAttribute("Index") end
    local function firePurchaseNatural(prompt) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not prompt or not prompt.Parent or not prompt.Enabled then return end
        _G.JAF_LastAutoBuyFire = tick()
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
local uid, ix = prompt:GetAttribute("UID"), buyIndex(prompt); _G.JAF_LastBuyTarget = { uid = uid and tostring(uid) or nil, index = ix and tostring(ix) or nil, at = tick() } end)
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if fireproximityprompt then fireproximityprompt(prompt) end end)
    end
    local carpetLockConn = nil
    local _carpetLockEquipStep = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local char = LocalPlayer.Character
        local hum  = char and char:FindFirstChildOfClass("Humanoid"); if not hum then return end
        local toolName = (Config.TpSettings and Config.TpSettings.Tool) or "Flying Carpet"; if char:FindFirstChild(toolName) then return end
        local tool = LocalPlayer.Backpack:FindFirstChild(toolName); if tool then hum:EquipTool(tool) end
    end
    local ensureCarpet; ensureCarpet = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _G.JAF_SuspendCarpetLock or _G.JAF_ResetStowActive then return end
        pcall(_carpetLockEquipStep)
    end
    local function stopCarpetLock() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if carpetLockConn then carpetLockConn:Disconnect(); carpetLockConn = nil end end
    local function startCarpetLock() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        stopCarpetLock(); carpetLockConn = RunService.Heartbeat:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if not abOn() then return end
            ensureCarpet()
        end) end
    local HOVER_HEIGHT, BUY_INTERVAL, DETECT_RADIUS = 5, 0.08, 17
    local lockedTarget, lockedPart, lockedModel = nil, nil, nil
    local function clearLock() LPH_ATTRIBUTES(ERROR_HANDLING(false))
lockedTarget, lockedPart, lockedModel = nil, nil, nil end
    local partAlive; partAlive = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            return lockedPart and lockedPart.Parent and lockedModel and lockedModel.Parent end
    local function promptAlive() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return lockedTarget and lockedTarget.prompt and lockedTarget.prompt.Parent and lockedTarget.prompt.Enabled end
    local function markClose(e, hrp) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not (e and not e.close and hrp and lockedPart and lockedPart.Parent) then return end
        local dx, dz = hrp.Position.X - lockedPart.Position.X, hrp.Position.Z - lockedPart.Position.Z
        if math.sqrt(dx * dx + dz * dz) <= (tonumber(_G.JAF_QualifyingBuyDist) or 7) then e.close = true end end
    local bodyPos = nil
    local ensureBodyPos; ensureBodyPos = function(hrp) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if bodyPos and bodyPos.Parent == hrp then return bodyPos end
        if bodyPos then bodyPos:Destroy() end; bodyPos = Instance.new("BodyPosition", hrp)
        bodyPos.MaxForce = Vector3.new(math.huge, math.huge, math.huge); bodyPos.P = 20000; bodyPos.D = 1000; bodyPos.Position = hrp.Position
        return bodyPos
    end
    local destroyBodyPos; destroyBodyPos = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if bodyPos then bodyPos:Destroy(); bodyPos = nil end end
    RunService.Heartbeat:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not abOn() or _G.JAF_CarpetBuyFlying or not partAlive() then destroyBodyPos(); return end
        local char = LocalPlayer.Character; local hrp = char and char:FindFirstChild("HumanoidRootPart")
        if not hrp then destroyBodyPos(); return end
        ensureBodyPos(hrp).Position = lockedPart.Position + Vector3.new(0, HOVER_HEIGHT, 0)
        if lockedTarget and lockedTarget.prompt then local uid = lockedTarget.prompt:GetAttribute("UID"); markClose(uid and _G.JAF_RecentBuys and _G.JAF_RecentBuys[tostring(uid)], hrp) end
    end)
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        while true do
            local _fastNow = (Config.AutoBuyMode == "fast") and (_G.JAF_FastBuyAllowed == nil or _G.JAF_FastBuyAllowed())
            if not _fastNow then task.wait(BUY_INTERVAL) else local r = tonumber(Config.AutoBuyFastRate) or 12; if r >= AUTOBUY_FAST_MAX then RunService.Heartbeat:Wait() else task.wait(1 / math.max(1, r)) end end
            if abOn() and partAlive() and promptAlive() then
                local prompt = lockedTarget.prompt
                if not (_fastNow and firePurchaseFast(prompt)) then firePurchaseNatural(prompt) end
                pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    local uid = prompt:GetAttribute("UID"); if not uid then return end; uid = tostring(uid)
                    local rb = _G.JAF_RecentBuys or {}; _G.JAF_RecentBuys = rb; local e = rb[uid]
                    if not e then
                        e = { close = false }; rb[uid] = e; _G.JAF_RBSeq = (_G.JAF_RBSeq or 0) + 1; e.seq = _G.JAF_RBSeq
                        local cnt = 0; for _ in pairs(rb) do cnt = cnt + 1 end; local cap = tonumber(_G.JAF_RecentBuyCap) or 300
                        if cnt > cap then local minSeq = _G.JAF_RBSeq - cap; for k, v in pairs(rb) do if (v.seq or 0) <= minSeq then rb[k] = nil end end end
                    end
                    local ix = buyIndex(prompt); if ix ~= nil then e.index = tostring(ix) end; e.at = tick()
                    markClose(e, LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart"))
                end) end end end)
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        while true do
            task.wait(autoBuyActive and (tonumber(_G.JAF_AutoBuyLockRate) or 0) or 0.25)
            if not autoBuyActive or _G.JAF_CarpetBuyFlying then clearLock(); continue end
            if lockedPart or lockedModel then if not partAlive() then clearLock() end continue end
            local char = LocalPlayer.Character; local hrp = char and char:FindFirstChild("HumanoidRootPart")
            if not hrp then continue end
            local radius, best, bestDist = Config.AutoBuyRange or DETECT_RADIUS, nil, math.huge
            for _, entry in ipairs(SharedState.ConveyorAnimals or {}) do
                if entry.prompt and entry.prompt.Parent and entry.prompt.Enabled and entry.part and entry.part.Parent then
                    local d = (hrp.Position - entry.part.Position).Magnitude; if d <= radius and d < bestDist then bestDist = d; best = entry end
                end end
            if best then lockedTarget = best; lockedPart = best.part; lockedModel = best.model or best.part.Parent end
        end end)
    _G.JAF_AutoBuyActive = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return autoBuyActive end
    _G.JAF_ToggleAutoBuy = function(on, noPersist) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if on == nil then on = not autoBuyActive end; autoBuyActive = on and true or false
        if not noPersist then Config.AutoBuyEnabled = autoBuyActive; if _G.JAF_SaveConfig then _G.JAF_SaveConfig() end end
        if autoBuyActive then startCarpetLock() else stopCarpetLock(); clearLock(); destroyBodyPos() end
        if _G.JAF_RefreshMiniAutoBuy then pcall(_G.JAF_RefreshMiniAutoBuy) end
        return autoBuyActive end
    LocalPlayer.CharacterAdded:Connect(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
task.wait(0.5); if autoBuyActive then startCarpetLock() end end)
    if Config.AutoBuyEnabled then Config.AutoBuyEnabled = false; if _G.JAF_SaveConfig then pcall(_G.JAF_SaveConfig) end end
end
if _G.JAF_NoCarpetScan == true then
    _P(_G, "JAF_ToggleCarpetBuy", function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return false end, "JAF_CancelCarpetBuy", function() end, "JAF_CarpetBuyActive", function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return false end, "JAF_CarpetBuyBusy", function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return false end)
else
do
    if Config.CarpetAutoBuy    == nil then Config.CarpetAutoBuy    = false end
    if Config.AutoGrief        == nil then Config.AutoGrief        = false end
    if Config.KickAfterBuy     == nil then Config.KickAfterBuy     = false end
    if Config.KickAfterBuyToPS == nil then Config.KickAfterBuyToPS = false end
    if Config.CarpetMinGen     == nil then Config.CarpetMinGen     = "" end
    if Config.CarpetBuySpeed   == nil then Config.CarpetBuySpeed   = 70 end
    local Players = game:GetService("Players")
    local RunService = game:GetService("RunService")
    local LocalPlayer = Players.LocalPlayer
    local myChar; myChar = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local c = LocalPlayer.Character
        return c, c and c:FindFirstChild("HumanoidRootPart"), c and c:FindFirstChildOfClass("Humanoid")
    end
    local myEquipCarpet; myEquipCarpet = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _G.JAF_ResetStowActive then return end
        local c = LocalPlayer.Character
        local hum = c and c:FindFirstChildOfClass("Humanoid")
        if not c or not hum then return end
        local toolName = (Config.TpSettings and Config.TpSettings.Tool) or "Flying Carpet"
        if not c:FindFirstChild(toolName) then
            local t = LocalPlayer.Backpack and LocalPlayer.Backpack:FindFirstChild(toolName)
            if t then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hum:EquipTool(t) end) end
        end end
    local function rankOf(nm) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not nm or Config.AutoTPPriority == false then return math.huge end
        local plist = (type(Config.PriorityList) == "table" and #Config.PriorityList > 0) and Config.PriorityList
            or (type(PRIORITY_LIST) == "table" and PRIORITY_LIST) or nil
        if not plist then return math.huge end
        local low = tostring(nm):lower()
        for i = 1, #plist do if tostring(plist[i]):lower() == low then return i end end
        return math.huge
    end
    local targetPos; targetPos = function(entry) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local m = entry and entry.model
        if not m or not m.Parent then return nil end
        local p = m.PrimaryPart or m:FindFirstChild("HumanoidRootPart") or m:FindFirstChildWhichIsA("BasePart", true)
        return p and p.Position or nil
    end
    local function carpetMinGen() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local s = Config.CarpetMinGen
        if type(s) ~= "string" or s == "" then return 0 end
        s = s:lower():gsub("%s", "")
        local num = tonumber(s:match("[%d%.]+")); if not num then return 0 end
        if s:find("b") then return num * 1e9 elseif s:find("m") then return num * 1e6 elseif s:find("k") then return num * 1e3 end
        return num
    end
    local function pickBestCarpetTarget() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local minG = carpetMinGen()
        local bw, bwr, bwg = nil, math.huge, -math.huge
        for _, w in ipairs(SharedState.WalkingCache or {}) do
            if w and w.model and w.model.Parent and w.genValue and (w.genValue or 0) >= minG then
                local r = rankOf(w.name)
                if r < bwr or (r == bwr and w.genValue > bwg) then bwr, bwg, bw = r, w.genValue, w end
            end end
        if not bw then return nil end
        if _G.JAF_CarpetBuyBeatBase ~= false then
            local total = tonumber(_G.JAF_PlotsTotal) or 0
            if total <= 0 or (tonumber(_G.JAF_PlotsScanned) or 0) < total then return nil end
            local br, brg, haveBase = math.huge, -math.huge, false
            for _, a in ipairs(SharedState.AllAnimalsCache or {}) do
                if a and a.genValue and a.owner ~= LocalPlayer.Name
                    and (_G.JAF_PassesMinGen == nil or _G.JAF_PassesMinGen(a.genValue)) then
                    haveBase = true
                    local r = rankOf(a.name)
                    if r < br or (r == br and a.genValue > brg) then br, brg = r, a.genValue end
                end end
            local carpetWins = (not haveBase) or (bwr < br) or (bwr == br and bwg > brg)
            if not carpetWins then return nil end
        end
        return bw
    end
    local PathfindingService = game:GetService("PathfindingService")
    local mover = nil
    local moving = false
    local carrot = nil
    local lastStep = 0
    local pfWaypoints = nil
    local pfComputing = false
    local pfLastCompute, pfLastDst = 0, nil
    local lastPos, lastMoveAt = nil, 0
    local stopMover; stopMover = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        moving = false
        _G.JAF_CarpetBuyFlying = false
        if mover then mover:Destroy(); mover = nil end
        carrot, pfWaypoints, lastPos = nil, nil, nil
    end
    local function computePathAsync(fromPos, toPos) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if pfComputing then return end
        pfComputing = true
        pfLastCompute = tick(); pfLastDst = toPos
        task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local path = PathfindingService:CreatePath({
                    AgentRadius  = tonumber(_G.JAF_CarpetPathRadius) or 3,
                    AgentHeight  = tonumber(_G.JAF_CarpetPathHeight) or 5,
                    AgentCanJump = false, AgentCanClimb = false,
                })
                path:ComputeAsync(fromPos, toPos)
                if path.Status == Enum.PathStatus.Success then
                    local wps = path:GetWaypoints()
                    local pts = {}
                    for _, w in ipairs(wps) do pts[#pts + 1] = w.Position end
                    if #pts > 0 then pfWaypoints = pts end
                end end)
            pfComputing = false
        end) end
    local driveTo; driveTo = function(hrp, to) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local now = tick()
        if (not pfWaypoints)
            or (not pfLastDst) or (to - pfLastDst).Magnitude > (tonumber(_G.JAF_CarpetBuyReplan) or 10)
            or (now - pfLastCompute) > (tonumber(_G.JAF_CarpetPathInterval) or 0.4) then
            computePathAsync(hrp.Position, to)
        end
        if not _G.JAF_SuspendCarpetLock then myEquipCarpet() end
        if not carrot then carrot = hrp.Position; lastStep = now end
        local goal
        local wps = pfWaypoints
        if wps and #wps > 0 then
            local nearI, nearD = 1, math.huge
            local cx, cz = carrot.X, carrot.Z
            for i = 1, #wps do
                local w = wps[i]
                local dx, dz = w.X - cx, w.Z - cz
                local d = math.sqrt(dx * dx + dz * dz)
                if d < nearD then nearD = d; nearI = i end
            end
            goal = wps[math.min(#wps, nearI + 1)]
        else
            goal = to
        end
        goal = Vector3.new(goal.X, goal.Y + (tonumber(_G.JAF_CarpetBuyHoverY) or 5), goal.Z)
        local dt = math.clamp(now - (lastStep > 0 and lastStep or now), 0, 0.1); lastStep = now
        local cruise = math.clamp(tonumber(_G.JAF_CarpetBuySpeed) or tonumber(Config.CarpetBuySpeed) or 70, 20, tonumber(_G.JAF_CarpetBuySpeedCap) or 150)
        local lead = tonumber(_G.JAF_CarpetBuyLead) or 12
        if (carrot - hrp.Position).Magnitude < lead then
            local d = goal - carrot
            if d.Magnitude > 0.1 then carrot = carrot + d.Unit * math.min(cruise * dt, d.Magnitude) end
        end
        if not lastPos or (hrp.Position - lastPos).Magnitude > 1.5 then lastPos = hrp.Position; lastMoveAt = now end
        if (now - lastMoveAt) > 1.2 then pfWaypoints = nil; lastMoveAt = now end
        if not mover or mover.Parent ~= hrp then
            if mover then mover:Destroy() end
            mover = Instance.new("BodyPosition")
            mover.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
            mover.P = 20000; mover.D = 1000
            mover.Position = hrp.Position
            mover.Parent = hrp
        end
        mover.Position = carrot
        moving = true
        _G.JAF_CarpetBuyFlying = true
    end
    local carpetBuyOn = false
    local didFirstTp = false
    local carpetEnabledBuy = false
    local arrivedAt, flightStartAt = 0, 0
    local carpetArmAt = tick()
    local flightStartedThisArm = false
    local function setArrivedBuy(want) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local isOn = (_G.JAF_AutoBuyActive and _G.JAF_AutoBuyActive()) or false
        if want and not isOn then
            carpetEnabledBuy = true
            if _G.JAF_ToggleAutoBuy then pcall(_G.JAF_ToggleAutoBuy, true, true) end
        elseif not want then
            if carpetEnabledBuy and isOn and _G.JAF_ToggleAutoBuy then pcall(_G.JAF_ToggleAutoBuy, false, true) end
            carpetEnabledBuy = false
        end end
    _G.JAF_CarpetBuyActive = function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return carpetBuyOn end
    _G.JAF_CarpetBuyBusy = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        return (_G.JAF_CarpetBuyFlying == true) or (carpetEnabledBuy and _G.JAF_AutoBuyActive and _G.JAF_AutoBuyActive() and true or false)
    end
    _G.JAF_CancelCarpetBuy = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        didFirstTp = true
        stopMover()
        _G.JAF_SuspendCarpetLock = false
        setArrivedBuy(false)
        arrivedAt, flightStartAt = 0, 0
        _G.JAF_CarpetBuyOwnsTp = nil
    end
    _G.JAF_ToggleCarpetBuy = function(on) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if on == nil then on = not carpetBuyOn end
        carpetBuyOn = on and true or false
        Config.CarpetAutoBuy = carpetBuyOn
        if _G.JAF_SaveConfig then _G.JAF_SaveConfig() end
        arrivedAt, flightStartAt = 0, 0
        if carpetBuyOn then
            didFirstTp = false
            carpetArmAt = tick(); flightStartedThisArm = false
        else
            stopMover()
            _G.JAF_SuspendCarpetLock = false
            setArrivedBuy(false)
        end
        return carpetBuyOn
    end
    local isStealing; isStealing = function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            return LocalPlayer:GetAttribute("Stealing") == true end
    local curTarget = nil
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        while true do
            task.wait(0.15)
            if carpetBuyOn and (not didFirstTp or _G.JAF_CarpetBuyRepeat == true) and not _G.JAF_TpActive and not isStealing() then
                local ok, t = pcall(pickBestCarpetTarget)
                curTarget = ok and t or nil
            else
                curTarget = nil
            end end end)
    RunService.Heartbeat:Connect(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not SAB_LIVE() then if moving then stopMover(); flightStartAt = 0 end return end
        do
            local buyTO = tonumber(_G.JAF_CarpetBuyBuyTimeout)
            if buyTO and arrivedAt > 0 and carpetEnabledBuy and (tick() - arrivedAt) > buyTO then setArrivedBuy(false); arrivedAt = 0 end
        end
        if flightStartAt > 0 and _G.JAF_CarpetBuyFlying == true and (tick() - flightStartAt) > (tonumber(_G.JAF_CarpetBuyFlightTimeout) or 30) then
            didFirstTp = true; stopMover(); flightStartAt = 0
        end
        _G.JAF_CarpetBuyOwnsTp = (carpetBuyOn and (
            _G.JAF_CarpetBuyFlying == true
            or (carpetEnabledBuy and _G.JAF_AutoBuyActive and _G.JAF_AutoBuyActive())
            or ((not didFirstTp) and curTarget ~= nil)
        )) or nil
        if carpetBuyOn and not didFirstTp and not flightStartedThisArm
           and (tick() - carpetArmAt) > (tonumber(_G.JAF_CarpetTpStartWindow) or 5) then
            didFirstTp = true
            if moving then stopMover(); flightStartAt = 0 end
        end
        if not carpetBuyOn or (didFirstTp and _G.JAF_CarpetBuyRepeat ~= true) or _G.JAF_TpActive or isStealing() then
            if moving then stopMover(); flightStartAt = 0 end
            return
        end
        local c, hrp = myChar()
        if not hrp then if moving then stopMover() end return end
        local t = curTarget
        if not t then if moving then stopMover(); flightStartAt = 0 end return end
        local tp = targetPos(t)
        if not tp then if moving then stopMover() end return end
        local range = tonumber(Config.AutoBuyRange) or 17
        if (hrp.Position - tp).Magnitude <= range then
            didFirstTp = true
            if moving then stopMover() end
            flightStartAt = 0
            arrivedAt = tick()
            setArrivedBuy(true)
        else
            if flightStartAt == 0 then flightStartAt = tick() end
            flightStartedThisArm = true
            local ok = pcall(driveTo, hrp, tp)
            if not ok and moving then stopMover() end
        end end)
    local MEDUSA_CD   = tonumber(_G.JAF_MedusaCooldown) or 25
    local GUMMY_CD    = tonumber(_G.JAF_GummyCooldown)  or 0.7
    local GRIEF_RANGE = tonumber(_G.JAF_GriefRange)     or 15
    local lastMedusa, lastGummy = -1e9, 0
    local function findTool(name) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local c = LocalPlayer.Character
        return (c and c:FindFirstChild(name)) or (LocalPlayer.Backpack and LocalPlayer.Backpack:FindFirstChild(name))
    end
    local _medRange
    local function medusaRange() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _medRange then return _medRange end
        local ok, r = pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local ring = game:GetService("ReplicatedStorage"):WaitForChild("Models"):WaitForChild("ToolsExtras"):FindFirstChild("Ring")
            if ring then local s = ring:GetExtentsSize(); return math.max(s.X, s.Z) / 2 * 0.6 end
        end)
        _medRange = tonumber(_G.JAF_MedusaRange) or (ok and type(r) == "number" and r) or 17
        return _medRange
    end
    local function nearestEnemyDist(hrp) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local best = math.huge
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character then
                local h = p.Character:FindFirstChild("HumanoidRootPart")
                if h then
                    local d = (h.Position - hrp.Position).Magnitude
                    if d < best then
                        local skip = false
                        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if (isBlacklisted and isBlacklisted(p.Name)) or (getAutojoinerName and getAutojoinerName(p.Name)) then skip = true end end)
                        if not skip then best = d end
                    end end end end
        return best
    end
    local function medusaOnCd(med) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local ct = med and med:GetAttribute("CooldownTime")
        return type(ct) == "number" and ct > 0.25
    end
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        while true do
            task.wait(0.1)
            pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
                local autobuyOn = (_G.JAF_AutoBuyActive and _G.JAF_AutoBuyActive() and SAB_LIVE()) or false
                if not (Config.AutoGrief and autobuyOn and _G.JAF_CarpetBuyFlying ~= true
                        and not _G.JAF_TpActive and not isStealing() and not _G.JAF_ResetStowActive) then
                    _G.JAF_SuspendCarpetLock = false; return
                end
                local c, hrp, hum = myChar()
                if not hrp or not hum then _G.JAF_SuspendCarpetLock = false; return end
                local med, gum = findTool("Medusa's Head"), findTool("Gummy Bear")
                if not med and not gum then _G.JAF_SuspendCarpetLock = false; return end
                local medR = medusaRange()
                local dist = nearestEnemyDist(hrp)
                local engageRange = math.max((med and medR) or 0, (gum and GRIEF_RANGE) or 0)
                if dist > engageRange then _G.JAF_SuspendCarpetLock = false; return end
                _G.JAF_SuspendCarpetLock = true
                local now = tick()
                local medReady = med and dist <= medR and (now - lastMedusa >= MEDUSA_CD) and not medusaOnCd(med)
                if medReady then
                    if med.Parent == c then
                        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
med:Activate() end); lastMedusa = now
                    elseif med.Parent ~= c then
                        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hum:EquipTool(med) end)
                    end
                elseif gum and dist <= GRIEF_RANGE then
                    if gum.Parent == c then
                        if now - lastGummy >= GUMMY_CD then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
gum:Activate() end); lastGummy = now end
                    else
                        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hum:EquipTool(gum) end)
                    end
                else
                    _G.JAF_SuspendCarpetLock = false
                    local toolName = (Config.TpSettings and Config.TpSettings.Tool) or "Flying Carpet"
                    local carp = findTool(toolName)
                    if carp and carp.Parent ~= c then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
hum:EquipTool(carp) end) end
                end end) end end)
    local kickFired = false
    local RSvc = game:GetService("ReplicatedStorage")
    local function doKickAfterBuy() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if kickFired then return end
        kickFired = true
        task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if Config.KickAfterBuyToPS then
                local okc = false
                if _G.JAF_KickToPrivateServer then okc = _G.JAF_KickToPrivateServer() end
                if not okc then if _G.JAF_KickPlayer then _G.JAF_KickPlayer() end return end
                task.wait(tonumber(_G.JAF_KickAfterBuyPSGrace) or 5)
                if LocalPlayer and LocalPlayer.Parent and _G.JAF_KickPlayer then _G.JAF_KickPlayer() end
            elseif _G.JAF_KickPlayer then
                _G.JAF_KickPlayer()
            end end) end
    local _sync
    local getSync; getSync = (function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if _G.JAF_NoSynchronizer == true then return nil end
        if _sync then return _sync end
        _sync = _G.JAF_GetSynchronizer()
        return _sync
    end)
    local function tryKickAfterBuy(entry, baseIndex) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if kickFired then return end
        if not (Config.KickAfterBuy or Config.KickAfterBuyToPS) then _G.JAF_KickDbg = "no-kick-toggle"; return end
        if not (_G.JAF_AutoBuyActive and _G.JAF_AutoBuyActive()) then _G.JAF_KickDbg = "autobuy-off"; return end
        if not entry then _G.JAF_KickDbg = "uuid-not-one-we-bought"; return end
        if baseIndex and entry.index and tostring(entry.index) ~= tostring(baseIndex) then _G.JAF_KickDbg = "name-mismatch"; return end
        if not entry.close then _G.JAF_KickDbg = "not-within-7-of-brainrot"; return end
        _G.JAF_KickDbg = "KICK"
        if carpetBuyOn and didFirstTp then setArrivedBuy(false) end
        doKickAfterBuy()
    end
    if _G.JAF_NoSynchronizer ~= true then task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local chFolder = RSvc:WaitForChild("Packages"):WaitForChild("Synchronizer"):WaitForChild("Channel")
        local cachedName
        local function _chIsOurs(data) LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local ok, own = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return data:Get("Owner") end)
            return ok and own ~= nil and tostring(own) == LocalPlayer.Name
        end
        local function readOurAnimalList() LPH_ATTRIBUTES(ERROR_HANDLING(false))
            local Sync = getSync(); if not Sync then return nil end
            if cachedName then
                local ok, data = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Sync:Get(cachedName) end)
                if ok and data and _chIsOurs(data) then
                    local ok2, al = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return data:Get("AnimalList") end)
                    if ok2 and type(al) == "table" then return al end
                end
                cachedName = nil
            end
            for _, ch in ipairs(chFolder:GetChildren()) do
                if ch:IsA("RemoteEvent") and #ch.Name == 36 and ch.Name:find("%-") then
                    local ok, data = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return Sync:Get(ch.Name) end)
                    if ok and data and _chIsOurs(data) then
                        local ok2, al = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return data:Get("AnimalList") end)
                        if ok2 and type(al) == "table" then cachedName = ch.Name; return al end
                    end end end
            return nil
        end
        local seen
        while true do
            RunService.Heartbeat:Wait()
            if kickFired then break end
            local al = readOurAnimalList()
            if type(al) == "table" then
                local now = {}
                local freshUuid, freshIndex, freshEntry
                for _, e in pairs(al) do
                    if type(e) == "table" and e.UUID then
                        now[e.UUID] = true
                        if seen and not seen[e.UUID] then
                            freshUuid = tostring(e.UUID)
                            freshIndex = e.Index ~= nil and tostring(e.Index) or nil
                            freshEntry = e
                        end end end
                if freshUuid then
                    local rb = _G.JAF_RecentBuys
                    local boughtEntry = rb and rb[freshUuid] or nil
                    tryKickAfterBuy(boughtEntry, freshIndex)
                end
                seen = now
            end end
    end) end
    if Config.CarpetAutoBuy then task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(_G.JAF_ToggleCarpetBuy, true) end) end
end end
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    if _G.JAF_NoCarpetScan == true then return end
    _G.JAF_WaitHeavy()
    local MUT_COL = {
        gold = Color3.fromRGB(255,215,0),     diamond = Color3.fromRGB(0,225,255),
        rainbow = Color3.fromRGB(255,120,220), candy = Color3.fromRGB(255,105,180),
        cursed = Color3.fromRGB(210,45,45),    bloodrot = Color3.fromRGB(150,0,0),
        lava = Color3.fromRGB(255,110,20),     radioactive = Color3.fromRGB(120,255,0),
        divine = Color3.fromRGB(255,250,205),  yinyang = Color3.fromRGB(235,235,235),
        galaxy = Color3.fromRGB(170,85,255),
    }
    local MUT_KW = {"Galaxy","Bloodrot","Radioactive","Rainbow","Diamond","Divine","YinYang","Cursed","Candy","Lava","Gold"}
    local function detectMutation(model) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not model then return nil end
        local hit = nil
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            for _, d in ipairs(model:GetDescendants()) do
                if d:IsA("TextLabel") and type(d.Text) == "string" and d.Text ~= "" then
                    local low = d.Text:lower()
                    for _, kw in ipairs(MUT_KW) do if low:find(kw:lower(), 1, true) then hit = kw; return end end
                end end end)
        return hit
    end
    local function partOf(entry) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local m = entry and entry.model
        if not m or not m.Parent then return nil end
        if m:IsA("BasePart") then return m end
        return m.PrimaryPart or m:FindFirstChildWhichIsA("BasePart", true)
    end
    local espFolder, bill
    local _mutCache = setmetatable({}, { __mode = "k" })
    local function cachedMutation(model) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not model then return nil end
        local c = _mutCache[model]
        if c ~= nil then return c or nil end
        local m = detectMutation(model)
        _mutCache[model] = m or false
        return m
    end
    local function clear() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if bill then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
bill:Destroy() end); bill = nil end end
    while true do
        task.wait(0.4)
        if not Config.ConveyorESP then
            clear()
        else
            local best, bestGv, bestPart = nil, -math.huge, nil
            for _, w in ipairs(SharedState.WalkingCache or {}) do
                if w and w.genValue and w.genValue > bestGv then
                    local pp = partOf(w)
                    if pp then best, bestGv, bestPart = w, w.genValue, pp end
                end end
            local bestBaseGen = -math.huge
            for _, a in ipairs(SharedState.AllAnimalsCache or {}) do
                if a and a.genValue and a.owner ~= LocalPlayer.Name then if a.genValue > bestBaseGen then bestBaseGen = a.genValue end end
            end
            if not best or not bestPart or bestGv <= bestBaseGen then
                clear()
            else
                if not espFolder or not espFolder.Parent then
                    espFolder = Instance.new("Folder", Workspace); espFolder.Name = "JustAFanConveyorESP"
                end
                if not bill or not bill.Parent then
                    bill = Instance.new("BillboardGui")
                    bill.Name = "CarpetBestESP"; bill.Size = UDim2.new(0, 196, 0, 56)
                    bill.StudsOffset = Vector3.new(0, 3.5, 0); bill.AlwaysOnTop = true
                    bill.LightInfluence = 0; bill.MaxDistance = 100000; bill.Parent = espFolder
                    local root = Instance.new("Frame", bill); root.Name = "Root"; root.Size = UDim2.new(1,0,1,0)
                    root.BackgroundColor3 = Theme.Background; root.BackgroundTransparency = 0.25; root.BorderSizePixel = 0
                    Instance.new("UICorner", root).CornerRadius = UDim.new(0, 9)
                    local st = JUI.new("UIStroke", root, "Thickness", 1.6, "Color", Color3.fromRGB(255,215,0), "Transparency", 0.15); st.Name = "St"
                    local badge = Instance.new("TextLabel", root); badge.Name = "Badge"
                    badge.Size = UDim2.new(1,-10,0,12); badge.Position = UDim2.new(0,6,0,4); badge.BackgroundTransparency = 1
                    badge.Font = Enum.Font.GothamBlack; badge.TextSize = 9; badge.TextColor3 = Color3.fromRGB(255,215,0)
                    badge.TextXAlignment = Enum.TextXAlignment.Left; badge.Text = "BEST"
                    local nm = Instance.new("TextLabel", root); nm.Name = "Nm"
                    nm.Size = UDim2.new(1,-12,0,18); nm.Position = UDim2.new(0,6,0,16); nm.BackgroundTransparency = 1
                    nm.Font = Enum.Font.GothamBold; nm.TextSize = 14; nm.TextColor3 = Theme.TextPrimary
                    nm.TextXAlignment = Enum.TextXAlignment.Left; nm.TextTruncate = Enum.TextTruncate.AtEnd
                    local gn = Instance.new("TextLabel", root); gn.Name = "Gn"
                    gn.Size = UDim2.new(1,-12,0,14); gn.Position = UDim2.new(0,6,0,36); gn.BackgroundTransparency = 1
                    gn.Font = Enum.Font.GothamBlack; gn.TextSize = 13; gn.TextColor3 = Color3.fromRGB(90,225,140)
                    gn.TextXAlignment = Enum.TextXAlignment.Left
                end
                bill.Adornee = bestPart
                local root = bill:FindFirstChild("Root")
                if root then
                    local mut = cachedMutation(best.model)
                    local mcol = (mut and MUT_COL[mut:lower()]) or Theme.TextPrimary
                    local nm = root:FindFirstChild("Nm")
                    if nm then
                        nm.TextColor3 = mcol
                        nm.Text = mut and (mut .. " " .. tostring(best.name or "Brainrot")) or tostring(best.name or "Brainrot")
                    end
                    local gn = root:FindFirstChild("Gn"); if gn then gn.Text = tostring(best.genText or "") end
                    local st = root:FindFirstChild("St"); if st then st.Color = (mut and mcol) or Color3.fromRGB(255,215,0) end
                end end end end end)
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local ASC_Players = game:GetService("Players")
    local ASC_LP      = ASC_Players.LocalPlayer
    local BIG = 10
    local CLOTHING = { TShirt = true, Shirt = true, Sweater = true, Jacket = true, DressSkirt = true, Pants = true, Shorts = true, }
    local isBig; isBig = (function(p) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not p:IsA("BasePart") then return false end
        local s = p.Size
        return s.X > BIG or s.Y > BIG or s.Z > BIG
    end)
    local function hideBig(p) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            _P(p, "LocalTransparencyModifier", 1, "Transparency", 1, "CanCollide", false, "CanQuery", false, "CanTouch", false, "CastShadow", false)
        end) end
    local accCovers; accCovers = (function(acc) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local tn
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
tn = acc.AccessoryType and acc.AccessoryType.Name end)
        if tn and CLOTHING[tn] then return true end
        for _, p in ipairs(acc:GetDescendants()) do if isBig(p) then return true end end
        return false
    end)
    local function nuke(obj) LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
obj:Destroy() end) end
    local function isOtherChar(char) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local plr = ASC_Players:GetPlayerFromCharacter(char)
        return plr ~= nil and plr ~= ASC_LP
    end
    local scrub; scrub = (function(char) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not char or not isOtherChar(char) then return end
        for _, obj in ipairs(char:GetDescendants()) do
            if obj:IsA("Accessory") then
                if accCovers(obj) then nuke(obj) end
            elseif obj:IsA("BasePart") and obj.Name ~= "HumanoidRootPart"
                   and obj ~= char.PrimaryPart and isBig(obj) then
                hideBig(obj)
            end end end)
    local function watch(char) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not isOtherChar(char) then return end
        scrub(char)
        char.ChildAdded:Connect(function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            task.defer(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if not obj.Parent then return end
                if obj:IsA("Accessory") then
                    if accCovers(obj) then nuke(obj) end
                elseif obj:IsA("BasePart") and obj.Name ~= "HumanoidRootPart" and isBig(obj) then
                    hideBig(obj)
                end end) end) end
    local function hookPlayer(plr) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if plr == ASC_LP then return end
        if plr.Character then watch(plr.Character) end
        plr.CharacterAdded:Connect(watch)
    end
    for _, plr in ipairs(ASC_Players:GetPlayers()) do hookPlayer(plr) end
    ASC_Players.PlayerAdded:Connect(hookPlayer)
    while true do
        for _, plr in ipairs(ASC_Players:GetPlayers()) do if plr ~= ASC_LP and plr.Character then pcall(scrub, plr.Character) end end
        task.wait(1)
    end end)
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local OServices = {
        OPlayers         = game:GetService("Players"),
        OLighting        = game:GetService("Lighting"),
        OMaterialService = game:GetService("MaterialService"),
    }
    local function OOn() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return _G.JAF_FpsOptimizer ~= false end
    local OXMin, OXMax = -560, -240
    local OClothingClasses = {
        "Shirt","Pants","ShirtGraphic","Accessory","Hat","HairAccessory",
        "FaceAccessory","NeckAccessory","ShoulderAccessory","FrontAccessory",
        "BackAccessory","WaistAccessory",
    }
    local OSafeDestroy; OSafeDestroy = (function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if obj.Name == "Overhead" then return end
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
obj:Destroy() end)
    end)
    local OIsClothing; OIsClothing = (function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        for _, c in ipairs(OClothingClasses) do if obj:IsA(c) then return true end end
    end)
    local OIsCharacterPart; OIsCharacterPart = (function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local m = obj:FindFirstAncestorOfClass("Model")
        while m do if OServices.OPlayers:GetPlayerFromCharacter(m) then return true end m = m:FindFirstAncestorOfClass("Model") end
        return false
    end)
    local OPROTECT = _G.JAF_OptimizeProtectNames or { CraftingMachine = true, Forge = true, Merchant = true }
    _G.JAF_OptimizeProtectNames = OPROTECT
    local OIsProtected; OIsProtected = (function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local m = obj.Parent
        while m and m ~= workspace do if OPROTECT[m.Name] then return true end m = m.Parent end
        return false
    end)
    local OIsOutOfRange; OIsOutOfRange = (function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if obj:IsA("BasePart") then if OIsProtected(obj) then return false end local x = obj.Position.X return x < OXMin or x > OXMax end
    end)
    local BASE_NAMES_O = { "baseplate","spawnlocation","spawn location","spawn" }
    local OIsBase; OIsBase = (function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not obj:IsA("BasePart") or not obj.Anchored then return false end
        local n = obj.Name:lower()
        for _, b in ipairs(BASE_NAMES_O) do if n:find(b,1,true) then return true end end
        return false
    end)
    local OIsInBase; OIsInBase = (function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local p = obj.Parent
        while p and p ~= workspace do if OIsBase(p) then return true end p = p.Parent end
        return false
    end)
    local OMakeTransparent; OMakeTransparent = (function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if OIsBase(obj) and not OIsCharacterPart(obj) then obj.Transparency = 1; obj.CastShadow = false end
        end) end)
    local OCleanObject; OCleanObject = (function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if obj:IsA("SurfaceAppearance") then
                OSafeDestroy(obj)
            elseif obj:IsA("Decal") or obj:IsA("Texture") then
                if not (obj.Name=="face" and obj.Parent and obj.Parent.Name=="Head") then OSafeDestroy(obj) end
            elseif obj:IsA("SpecialMesh") then
                obj.TextureId = ""
            elseif obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam")
                or obj:IsA("PointLight") or obj:IsA("SpotLight") or obj:IsA("SurfaceLight")
                or obj:IsA("Fire") or obj:IsA("Smoke") or obj:IsA("Sparkles") or obj:IsA("Explosion")
                or obj:IsA("Animation") or obj:IsA("AnimationController") then
                OSafeDestroy(obj)
            elseif obj:IsA("BasePart") then
                obj.CastShadow = false; obj.Material = Enum.Material.Plastic
                obj.MaterialVariant = ""; obj.Reflectance = 0
                if _G.JAF_OptimizeStripMeshTextures and obj:IsA("MeshPart") then obj.TextureID = "" end
            end end) end)
    local OStopAnimations; OStopAnimations = (function(animator) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local isChar = false
            for _, plr in ipairs(OServices.OPlayers:GetPlayers()) do
                if plr.Character and animator:IsDescendantOf(plr.Character) then isChar = true; break end
            end
            if isChar then return end
            for _, track in ipairs(animator:GetPlayingAnimationTracks()) do track:Stop() end
        end) end)
    local _animHooked = setmetatable({}, {__mode = "k"})
    local _animConns  = {}
    local function OAnimIsTool(inst) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local cur = inst.Parent
        while cur and cur ~= game do if cur:IsA("Tool") then return true end cur = cur.Parent end
        return false
    end
    local function OAnimIsPlayerRig(inst) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        for _, p in ipairs(OServices.OPlayers:GetPlayers()) do if p.Character and inst:IsDescendantOf(p.Character) then return true end end
        return false
    end
    local function OClearRigPose(model) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not model then return end
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local hum = model:FindFirstChildOfClass("Humanoid")
            local animr = hum and hum:FindFirstChildOfClass("Animator")
            if animr and #animr:GetPlayingAnimationTracks() > 0 then return end
            for _, d in ipairs(model:GetDescendants()) do
                if d:IsA("AnimationConstraint") or d:IsA("Motor6D") then d.Transform = CFrame.new() end
            end end) end
    _G.JAF_ClearRigPose = OClearRigPose
    local function OAnimSkip(animator) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _G.JAF_RemoveAnims == false or _G.JAF_FpsOptCharAnim == false then return true end
        if OAnimIsTool(animator) then return true end
        if _G.JAF_AnimKillPlayers == false and OAnimIsPlayerRig(animator) then return true end
        return false
    end
    local function OKillAnimsOn(animator) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not animator or not animator.Parent then return end
        if _animHooked[animator] then return end
        if OAnimSkip(animator) then return end
        _animHooked[animator] = true
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            for _, tr in ipairs(animator:GetPlayingAnimationTracks()) do pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
tr:Stop(0) end) end
            local _owner = animator:FindFirstAncestorOfClass("Model")
            task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
OClearRigPose(_owner) end)
            local c = animator.AnimationPlayed:Connect(function(tr) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if OAnimSkip(animator) then return end
                pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
tr:Stop(0) end)
                task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
OClearRigPose(_owner) end)
            end)
            _animConns[#_animConns + 1] = c
        end) end
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        while true do
            task.wait(tonumber(_G.JAF_AnimPoseFixHz) or 0.5)
            if _G.JAF_RemoveAnims ~= false and _G.JAF_FpsOptCharAnim ~= false
               and _G.JAF_AnimKillPlayers ~= false and _G.JAF_AnimPoseFix ~= false then
                for _, plr in ipairs(OServices.OPlayers:GetPlayers()) do if plr.Character then OClearRigPose(plr.Character) end end
            end end end)
    local function OResetCharAnims(char, force) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not char then return end
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local an = char:FindFirstChild("Animate")
            local wasDisabled = (an ~= nil) and an.Disabled
            if wasDisabled then an.Disabled = false end
            if not (force or wasDisabled) then return end
            local hum = char:FindFirstChildOfClass("Humanoid")
            if not hum then return end
            local old = hum:FindFirstChildOfClass("Animator")
            if old then old:Destroy() end
            if not hum:FindFirstChildOfClass("Animator") then local fresh = Instance.new("Animator"); fresh.Parent = hum end
        end) end
    _G.JAF_ResetCharAnims = OResetCharAnims
    _G.JAF_KillAnimsOn    = OKillAnimsOn
    local function OOptimizeCharacter(char) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not char or not OOn() then return end
        task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            task.wait(0.3)
            if not OOn() then return end
            for _, obj in ipairs(char:GetDescendants()) do if OIsClothing(obj) then OSafeDestroy(obj) else OCleanObject(obj) end end
        end) end
    local function OIsJunkLighting(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        return (obj:IsA("Atmosphere") or obj:IsA("Clouds") or obj:IsA("PostEffect")) and obj.Name ~= "JustAFanDarkModeCC"
    end
    local function OApplyGreySky() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            for _, obj in ipairs(OServices.OLighting:GetChildren()) do if obj:IsA("Sky") then obj:Destroy() end end
            local sky = Instance.new("Sky")
            _P(sky, "SkyboxBk", "", "SkyboxDn", "", "SkyboxFt", "", "SkyboxLf", "", "SkyboxRt", "", "SkyboxUp", "", "CelestialBodiesShown", false, "Parent", OServices.OLighting)
        end) end
    local function OOptimizeLighting() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local L = OServices.OLighting
        _P(L, "GlobalShadows", false, "FogEnd", 9e9, "FogStart", 9e9, "EnvironmentDiffuseScale", 0, "EnvironmentSpecularScale", 0, "Brightness", 1.5, "Ambient", Color3.fromRGB(60,60,60))
        for _, v in ipairs(L:GetChildren()) do if OIsJunkLighting(v) then v:Destroy() end end
        OApplyGreySky()
        if _G.JAF_ReassertDark then task.defer(_G.JAF_ReassertDark) end
    end
    local function OApplyTerrain() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            local T = workspace.Terrain
            _P(T, "Decoration", false, "WaterWaveSize", 0, "WaterWaveSpeed", 0, "WaterReflectance", 0, "WaterTransparency", 1)
        end) end
    if _G.JAF_OptimizeFloorSmooth == nil then _G.JAF_OptimizeFloorSmooth = Config.OptimizeFloorSmooth ~= false end
    local function OPurgeMaterialVariants() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _G.JAF_OptimizeFloorSmooth == false then return end
        local MS = OServices.OMaterialService
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            for _, m in ipairs(Enum.Material:GetEnumItems()) do
                pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                    if MS:GetBaseMaterialOverride(m) ~= "" then MS:SetBaseMaterialOverride(m, "") end
                end) end end)
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            for _, v in ipairs(MS:GetDescendants()) do if v:IsA("MaterialVariant") then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
v:Destroy() end) end end
        end) end
    _G.JAF_PurgeMaterialVariants = OPurgeMaterialVariants
    if OOn() then pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        settings().Rendering.QualityLevel        = Enum.QualityLevel.Level01
        settings().Rendering.MeshPartDetailLevel = Enum.MeshPartDetailLevel.Level01
        settings().Physics.AllowSleep = true
        settings().Physics.PhysicsEnvironmentalThrottle = Enum.EnviromentalPhysicsThrottle.Skip
    end) end
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not game:IsLoaded() then game.Loaded:Wait() end
        task.wait(_G.JAF_OptimizeDelay or 4)
        while not OOn() do task.wait(1) end
        OOptimizeLighting()
        OApplyTerrain()
        OPurgeMaterialVariants()
        local _on = 0
        for _, obj in ipairs(workspace:GetDescendants()) do
            if OIsBase(obj) then
                OMakeTransparent(obj)
            elseif OIsClothing(obj) then
                OSafeDestroy(obj)
            elseif OIsInBase(obj) then
            elseif OIsCharacterPart(obj) then
            elseif OIsOutOfRange(obj) then
                OSafeDestroy(obj)
            else
                OCleanObject(obj)
                if obj:IsA("Animator") then OStopAnimations(obj) end
            end
            _on = _on + 1
            if _on % (_G.JAF_OptimizeBatch or 50) == 0 then RunService.Heartbeat:Wait() end
        end end)
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        while true do
            task.wait(2)
            if not OOn() then continue end
            pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local L = OServices.OLighting
                for _, obj in ipairs(L:GetChildren()) do if OIsJunkLighting(obj) then OSafeDestroy(obj) end end
            end) end end)
    local ODeferredClean = (function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not OOn() then return end
        if OIsBase(obj) then OMakeTransparent(obj); return end
        if OIsClothing(obj) then OSafeDestroy(obj)
        elseif OIsInBase(obj) then
        elseif OIsOutOfRange(obj) then OSafeDestroy(obj)
        else
            OCleanObject(obj)
            if obj:IsA("Animator") then OStopAnimations(obj) end
        end end)
    local _ocPending, _ocScheduled = {}, false
    local _ocDrain; _ocDrain = (function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        _ocScheduled = false
        local _batch = _ocPending
        _ocPending = {}
        local _cap = _G.JAF_OptDrainCap or 300
        local _budgetMs = (_G.JAF_OptDrainBudgetMs or 4) / 1000
        local _n, _t0 = 0, os.clock()
        for _i = 1, #_batch do
            local obj = _batch[_i]
            if obj.Parent then ODeferredClean(obj) end
            _n = _n + 1
            if _n >= _cap or (os.clock() - _t0) >= _budgetMs then RunService.Heartbeat:Wait() _n, _t0 = 0, os.clock() end
        end end)
    workspace.DescendantAdded:Connect((function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if OIsCharacterPart(obj) then return end
        _ocPending[#_ocPending + 1] = obj
        if not _ocScheduled then _ocScheduled = true; task.defer(_ocDrain) end
    end))
    OServices.OLighting.DescendantAdded:Connect((function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not OOn() then return end
        if OIsJunkLighting(obj) then OSafeDestroy(obj) end
    end))
    OServices.OMaterialService.DescendantAdded:Connect((function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not OOn() then return end
        OSafeDestroy(obj)
    end))
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        while true do
            if _G.JAF_RemoveAnims ~= false and _G.JAF_FpsOptCharAnim ~= false then
                local _an = 0
                for _, obj in ipairs(Workspace:GetDescendants()) do
                    if obj:IsA("Animator") then pcall(OKillAnimsOn, obj) end
                    _an = _an + 1
                    if _an % (_G.JAF_OptimizeBatch or 50) == 0 then
                        if _G.JAF_AnimSweepNodeYield ~= false then RunService.Heartbeat:Wait() end
                    end end end
            task.wait(tonumber(_G.JAF_FpsOptAnimPeriod) or 60)
        end end)
    Workspace.DescendantAdded:Connect(function(obj) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _G.JAF_RemoveAnims == false or _G.JAF_FpsOptCharAnim == false then return end
        if obj:IsA("Animator") then task.defer(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
pcall(OKillAnimsOn, obj) end) end
    end)
    local function OOnCharAdded(ch) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        OOptimizeCharacter(ch)
        local _olp = OServices.OPlayers.LocalPlayer
        if _olp and ch == _olp.Character then task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
task.wait(0.3); OResetCharAnims(ch) end) end
    end
    for _, plr in ipairs(OServices.OPlayers:GetPlayers()) do OOnCharAdded(plr.Character) plr.CharacterAdded:Connect(OOnCharAdded) end
    OServices.OPlayers.PlayerAdded:Connect(function(plr) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        plr.CharacterAdded:Connect(OOnCharAdded)
    end)
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        while true do
            task.wait(15)
            pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                local kb = collectgarbage("count")
                if kb and kb > (_G.JAF_GcFullThresholdKb or 600000) then
                    collectgarbage("collect")
                else
                    for _ = 1, 6 do collectgarbage("step", 0) end
                end end) end end) end)
;(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
    local autoTurretOn = false
    local function adt_getChar() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local c = LocalPlayer.Character
        return c, c and c:FindFirstChild("HumanoidRootPart"), c and c:FindFirstChildOfClass("Humanoid")
    end
    local adt_pin = setmetatable({}, { __mode = "k" })
    local function adt_isDeploying(target) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local strict = _G.JAF_TurretDeployStrict ~= false
        local pinned, why = false, nil
        local fp = {}
        for _, d in ipairs(target:GetDescendants()) do
            if d:IsA("TextLabel") then
                local t = d.Text
                if t ~= "" then
                    local ev = false
                    if t:lower():find("ready", 1, true) then
                        ev = true
                        if not pinned then pinned, why = true, "ready-label" end
                    elseif not strict then
                        if t:find("!", 1, true) or t:match("^%s*%d+%.?%d*%s*[sS]?%s*$") then
                            ev = true
                            if not pinned then pinned, why = true, "loose" end
                        end
                    else
                        local num = t:match("^%s*(%d+%.?%d*)%s*[sS]?%s*!*%s*$")
                        if num and (tonumber(num) or 99) <= 10 then
                            ev = true
                            if not pinned then pinned, why = true, "countdown=" .. num end
                        end end
                    if ev then fp[#fp + 1] = t end
                end end end
        if not pinned then adt_pin[target] = nil return false, nil end
        local key = table.concat(fp, "\1")
        local now = tick()
        local p = adt_pin[target]
        if not p or p.key ~= key then adt_pin[target] = { key = key, at = now, why = why } return true, why end
        p.why = why
        if _G.JAF_TurretStaticRelease ~= false
          and now - p.at >= (tonumber(_G.JAF_TurretStaticPin) or 1.2) then
            return false, "static:" .. why
        end
        return true, why
    end
    local adt_seen = setmetatable({}, { __mode = "k" })
    local adt_armed = setmetatable({}, { __mode = "k" })
    local adt_tr   = setmetatable({}, { __mode = "k" })
    local adt_gate = { tp = 0, speed = 0, buykick = 0, steal = 0, join = 0, noweapon = 0 }
    local function adt_gateDump() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local parts = {}
        for _, k in ipairs({ "tp", "speed", "buykick", "steal", "join", "noweapon" }) do
            if adt_gate[k] > 0.05 then parts[#parts + 1] = string.format("%s %.2fs", k, adt_gate[k]) end
        end
        return #parts > 0 and table.concat(parts, ", ") or "none"
    end
    local function adt_isPlayerRig(inst) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _G.JAF_TurretSkipPlayers == false then return false end
        local par = inst
        while par and par ~= workspace do
            if Players:GetPlayerFromCharacter(par)
            or (par.ClassName == "Model" and par:FindFirstChildOfClass("Humanoid")) then return true end
            par = par.Parent
        end
        return false
    end
    local function adt_isTargetable(inst) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if adt_isPlayerRig(inst) then return false end
        if _G.JAF_TurretWaitReady == false then return true end
        if adt_armed[inst] then return true end
        local now = tick()
        local seen = adt_seen[inst]
        if not seen then seen = now; adt_seen[inst] = now end
        local tr = adt_tr[inst]
        if not tr then tr = { seen = now }; adt_tr[inst] = tr end
        if now - seen >= (tonumber(_G.JAF_TurretDeploySpan) or 12) then
            adt_armed[inst] = true
            if tr.pinwhy and not tr.pinrel then tr.pinrel = "age-cap" end
            return true
        end
        local dep, why = adt_isDeploying(inst)
        if why and dep then tr.pinwhy = why end
        if dep then return false end
        if tr.pinwhy and not tr.pinrel then tr.pinrel = why and "static" or "deploy-done" end
        if _G.JAF_TurretArmLatch ~= false then adt_armed[inst] = true end
        return true
    end
    local adt_cool = setmetatable({}, { __mode = "k" })
    local function adt_applyVisuals(target) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        for _, d in ipairs(target:GetDescendants()) do
            if d:IsA("BasePart") and d ~= target then
                d.Transparency = 0.5; d.CanCollide = false
                d.CanTouch = false; d.CanQuery = false
            elseif d:IsA("BillboardGui") and d.Name == "SentryLabel" then
                d:Destroy()
            elseif d:IsA("Decal") or d:IsA("Texture") then d.Transparency = 0.5 end
        end
        if target:IsA("BasePart") and target.Name == "ProxyVisual" then target.Transparency = 1; target.CanCollide = false end
    end
    local function adt_getWeapon() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local c = LocalPlayer.Character
        if not c then return nil end
        local bp = LocalPlayer.Backpack
        return (bp and bp:FindFirstChild("Bat")) or c:FindFirstChild("Bat")
            or (bp and bp:FindFirstChild("Gummy Bear")) or c:FindFirstChild("Gummy Bear")
    end
    local function adt_equipBat() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _G.JAF_ResetStowActive then return end
        local c, _, hum = adt_getChar()
        if not c or not hum then return end
        local w = adt_getWeapon()
        if w and w.Parent ~= c then hum:EquipTool(w) end
    end
    local adt_lastDeep = 0
    local adt_getClosestSentry; adt_getClosestSentry = function(includeCooled) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local _, hrp = adt_getChar()
        if not hrp then return nil end
        local closest, shortest = nil, math.huge
        local sfind = string.find
        local now = tick()
        local hrpPos = hrp.Position
        local function _scan(list) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            for _i = 1, #list do
                local inst = list[_i]
                local nm = inst.Name
                if sfind(nm, "Sentry", 1, true) and nm ~= "SentryBullet"
                  and (inst.ClassName == "Model" or inst:IsA("BasePart"))
                  and (includeCooled or not (adt_cool[inst] and now < adt_cool[inst]))
                  and adt_isTargetable(inst) then
                    local root = inst:IsA("BasePart") and inst or inst:FindFirstChildWhichIsA("BasePart", true)
                    if root then
                        local dist = (hrpPos - root.Position).Magnitude
                        if dist < shortest then shortest = dist; closest = inst end
                    end end end end
        _scan(workspace:GetChildren())
        if not closest and (now - adt_lastDeep) > (tonumber(_G.JAF_TurretDeepEvery) or 3) then
            adt_lastDeep = now
            _scan(workspace:GetDescendants())
        end
        return closest
    end
    function _G.JAF_AutoTurretActive() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return autoTurretOn == true end
    function _G.JAF_ToggleAutoTurret() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        autoTurretOn = not autoTurretOn
        if autoTurretOn then adt_lastDeep = 0 end
        return autoTurretOn
    end
    local adt_cycleActive = false
    local function adt_destroyCycle(targetSentry) LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if adt_cycleActive then return end
        adt_cycleActive = true
        local _trc = adt_tr[targetSentry]
        if _trc and not _trc.engage then _trc.engage = tick() end
        local _tdSurf = _G.invisibleStealEnabled == true
        if _tdSurf then _G.JAF_InvisSurface = true end
        local _t0 = tick()
        while targetSentry and targetSentry.Parent
          and LocalPlayer:GetAttribute("Stealing") ~= true
          and tick() - _t0 < (tonumber(_G.JAF_TurretCycleCap) or 6) do
            local c, hrp, hum = adt_getChar()
            if not c or not hrp or not hum then break end
            if not SAB_LIVE() then break end
            if _G.JAF_TpActive then break end
            if _G.JAF_TurretYieldToSpeed ~= false and _G.JAF_CarpetSpeedActive then break end
            if _G.JAF_TurretYieldToBuyKick ~= false then if _G.JAF_AutoBuyActive and _G.JAF_AutoBuyActive() then break end end
            local w = adt_getWeapon()
            adt_applyVisuals(targetSentry)
            local offset   = hrp.CFrame.LookVector * 4
            local targetCF = CFrame.new(hrp.Position + offset, hrp.Position)
            if targetSentry:IsA("Model") then
                targetSentry:PivotTo(targetCF)
            elseif targetSentry:IsA("BasePart") then targetSentry.CFrame = targetCF end
            if w then
                if w.Parent ~= c then hum:EquipTool(w) end
                w:Activate()
            else
                break
            end
            task.wait(0.1)
        end
        if targetSentry and targetSentry.Parent and tick() - _t0 >= (tonumber(_G.JAF_TurretCycleCap) or 6) then
            adt_cool[targetSentry] = tick() + (tonumber(_G.JAF_TurretCooldown) or 2.5)
        end
        if _tdSurf then _G.JAF_InvisSurface = false end
        if _trc then
            local t1   = tick()
            local seen = _trc.seen or t1
            local arm  = _trc.armed or seen
            local eng  = _trc.engage or t1
            local dead = not (targetSentry and targetSentry.Parent)
            local pin = _trc.pinwhy and string.format(": %s -> %s", _trc.pinwhy, _trc.pinrel or "?") or ""
            local rep = string.format(
                "[Turret] %s | total %.2fs | seen->armed %.2fs (ready-wait%s) | armed->engage %.2fs (ours) | swinging %.2fs | blocked by: %s",
                dead and "DESTROYED" or "SURVIVED", t1 - seen, arm - seen, pin, eng - arm, t1 - eng, adt_gateDump())
            _G.JAF_TurretLastReport = rep
        end
        for k in pairs(adt_gate) do adt_gate[k] = 0 end
        adt_cycleActive = false
    end
    function _G.JAF_DestroyTurretIfThreat() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if not (autoTurretOn and SAB_LIVE()) then return false end
        if _G.JAF_TurretYieldToBuyKick ~= false then if _G.JAF_AutoBuyActive and _G.JAF_AutoBuyActive() then return false end end
        local _, hrp = adt_getChar()
        if not hrp then return false end
        local s = adt_getClosestSentry(true)
        if not s then return false end
        local root = s:IsA("BasePart") and s or s:FindFirstChildWhichIsA("BasePart", true)
        if not root then return false end
        if (hrp.Position - root.Position).Magnitude > (tonumber(_G.JAF_TurretHitRange) or 45) then return false end
        if not adt_getWeapon() then return false end
        adt_equipBat()
        adt_destroyCycle(s)
        return true
    end
    function _G.JAF_TurretThreatNearby() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if not (autoTurretOn and SAB_LIVE()) then return false end
        local _, hrp = adt_getChar()
        if not hrp then return false end
        local s = adt_getClosestSentry(true)
        if not s then return false end
        local root = s:IsA("BasePart") and s or s:FindFirstChildWhichIsA("BasePart", true)
        if not root then return false end
        return (hrp.Position - root.Position).Magnitude <= (tonumber(_G.JAF_TurretHitRange) or 45)
    end
    local adt_boot = tick()
    local adt_sweepIdx, adt_lastSweep = 0, 0
    local adt_lastScan, adt_hadTarget = 0, false
    local adt_sweeping = false
    local adt_known = 0
    local adt_whyLog = {}
    local function _why(s) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _G.JAF_TurretWhy ~= s then
            _G.JAF_TurretWhy = s
            local L = adt_whyLog
            L[#L + 1] = string.format("%.1f %s", tick() - adt_boot, s)
            if #L > 24 then table.remove(L, 1) end
            _G.JAF_TurretWhyLog = table.concat(L, " | ")
        end end
    task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        local adt_lastTick, adt_dt = tick(), 0
        local function _gate(name) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if adt_known > 0 then adt_gate[name] = (adt_gate[name] or 0) + adt_dt end end
        while true do
            task.wait(0.1)
            local now = tick()
            adt_dt = now - adt_lastTick; adt_lastTick = now
            if not SAB_LIVE() then _why("not-in-game") continue end
            if _G.JAF_TurretObserve ~= false then
                local n, kids = 0, workspace:GetChildren()
                for i = 1, #kids do
                    local inst = kids[i]
                    local nm = inst.Name
                    if string.find(nm, "Sentry", 1, true) and nm ~= "SentryBullet"
                      and (inst.ClassName == "Model" or inst:IsA("BasePart"))
                      and not adt_isPlayerRig(inst) then
                        n = n + 1
                        local tr = adt_tr[inst]
                        if not tr then tr = { seen = now }; adt_tr[inst] = tr end
                        if autoTurretOn and adt_isTargetable(inst) and not tr.armed then tr.armed = now end
                    end end
                adt_known = n
            else
                adt_known = 0
            end
            if not autoTurretOn then _why("off") continue end
            if now - adt_boot < (tonumber(_G.JAF_TurretJoinGrace) or 8) then _why("join-grace") _gate("join") continue end
            if _G.JAF_TpActive then _why("gate:tp") _gate("tp") continue end
            if _G.JAF_TurretYieldToSpeed ~= false and _G.JAF_CarpetSpeedActive then _why("gate:speed") _gate("speed") continue end
            if _G.JAF_TurretYieldToBuyKick ~= false then
                if _G.JAF_AutoBuyActive and _G.JAF_AutoBuyActive() then _why("gate:buykick") _gate("buykick") continue end
            end
            if LocalPlayer:GetAttribute("Stealing") == true then _why("gate:steal") _gate("steal") continue end
            if adt_known == 0 and not adt_hadTarget and (now - adt_lastScan) < (tonumber(_G.JAF_TurretIdleScanDt) or 0.3) then _why("idle:no-sentry") continue end
            adt_lastScan = now
            local targetSentry = adt_getClosestSentry()
            adt_hadTarget = targetSentry ~= nil
            if not targetSentry then
                _why(adt_known > 0 and "sentry-known-not-ready" or "idle:no-sentry")
                if _G.JAF_TurretStreamSweep ~= false and not adt_sweeping
                  and now - adt_lastSweep > (tonumber(_G.JAF_TurretSweepEvery) or 1) then
                    adt_lastSweep = now
                    local plots = workspace:FindFirstChild("Plots")
                    if plots then
                        local kids = plots:GetChildren()
                        if #kids > 0 then
                            adt_sweepIdx = adt_sweepIdx % #kids + 1
                            local mr = kids[adt_sweepIdx]:FindFirstChild("MainRoot")
                            if mr and workspace.StreamingEnabled then
                                adt_sweeping = true
                                local _sweepPos = mr.Position
                                task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                                    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
workspace:RequestStreamAroundAsync(_sweepPos, 1) end)
                                    adt_sweeping = false
                                end) end end end end
                continue
            end
            if not adt_getWeapon() then _why("no-weapon") _gate("noweapon") continue end
            _why("engaging")
            adt_equipBat()
            adt_destroyCycle(targetSentry)
        end end) end)()
task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local OWN_CLONE_NAME = tostring(LocalPlayer.UserId) .. "_Clone"
    local isOtherClone; isOtherClone = function(inst) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not inst:IsA("Model") then return false end
        local n = inst.Name
        if not (type(n) == "string" and string.find(n, "_Clone", 1, true)) then return false end
        return n ~= OWN_CLONE_NAME
    end
    local disableCollide; disableCollide = function(root) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not root or not root.Parent then return end
        for _, d in ipairs(root:GetDescendants()) do
            if d:IsA("BasePart") and d.CanCollide then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
d.CanCollide = false end) end
        end end
    while true do
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character then disableCollide(p.Character) RunService.Heartbeat:Wait() end
        end
        for _, child in ipairs(Workspace:GetChildren()) do
            if isOtherClone(child) then disableCollide(child) RunService.Heartbeat:Wait() end
        end
        task.wait(_G.JAF_NoclipInterval or 0.4)
    end end)
do
    local function silenceFriendPanel() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        if _G.JAF_SilenceFriendPanel == false or typeof(getconnections) ~= "function" then return 0 end
        local CS = game:GetService("CollectionService")
        local killed = 0
        for _, sig in ipairs({ CS:GetInstanceAddedSignal("PlotFriendPanel"), CS:GetInstanceRemovedSignal("PlotFriendPanel") }) do
            local ok, conns = pcall(getconnections, sig)
            if ok and conns then
                for _, c in ipairs(conns) do
                    local on = true
                    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
on = c.Enabled end)
                    if on ~= false then
                        if not pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
c:Disable() end) then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
c:Disconnect() end) end
                        killed = killed + 1
                    end end end end
        return killed
    end
    _G.JAF_SilenceFriendPanel_Run = silenceFriendPanel
    task.spawn(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        task.wait(1)
        silenceFriendPanel()
        local _last = 0
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            game:GetService("ScriptContext").Error:Connect(function(msg) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
                if tostring(msg):find("FriendPanel", 1, true) and tick() - _last > 1 then _last = tick(); silenceFriendPanel() end
            end) end)
        while true do task.wait(_G.JAF_FriendPanelSweep or 10); pcall(silenceFriendPanel) end
    end) end
if _G.JAF_NoclipMachines == nil then _G.JAF_NoclipMachines = true end
task.spawn((function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
    local NOCLIP_KW = _G.JAF_NoclipKW or { "truck", "fuse", "craft", "incubator", "cupid", "soccerball" }
    local AVOID_KW  = _G.JAF_NoclipAvoidKW or { "trade","portal","wheel","cyber","spin","roulette","summer","admin","plot","shop","board","sentry","laser","base" }
    local FORCE_KW  = _G.JAF_NoclipForceKW or { "summer base", "summerfuse" }
    local _clsMemo, _clsMemoN = {}, 0
    local classify; classify = function(name) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local raw = name or ""
        local hit = _clsMemo[raw]
        if hit ~= nil then if hit == false then return nil end return hit end
        local n = raw:lower()
        local res = nil
        for _, k in ipairs(FORCE_KW)  do if n:find(k, 1, true) then res = "noclip" break end end
        if res == nil then for _, k in ipairs(AVOID_KW)  do if n:find(k, 1, true) then res = "avoid"  break end end end
        if res == nil then for _, k in ipairs(NOCLIP_KW) do if n:find(k, 1, true) then res = "noclip" break end end end
        if _clsMemoN >= (_G.JAF_ClsMemoMax or 8000) then _clsMemo, _clsMemoN = {}, 0 end
        _clsMemo[raw] = (res == nil) and false or res
        _clsMemoN = _clsMemoN + 1
        return res
    end
    local _targets, _lastDiscover = {}, 0
    local _chainClassify; _chainClassify = function(p) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local chain, a = {}, p
        while a and a ~= Workspace do table.insert(chain, 1, a); a = a.Parent end
        if not a then return nil end
        for _, node in ipairs(chain) do
            if node:FindFirstChildOfClass("Humanoid") then return nil end
            local cls = classify(node.Name)
            if cls then return cls end
        end
        return nil
    end
    local _ncPending, _ncScheduled = {}, false
    local function _ncDrain() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        _ncScheduled = false
        local batch = _ncPending
        _ncPending = {}
        local n = 0
        for p in pairs(batch) do
            if _G.JAF_NoclipMachines and SAB_LIVE() and p.Parent and p.Anchored and _chainClassify(p) == "noclip" then
                pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
p.CanCollide = false; p.CanQuery = false end)
            end
            n = n + 1
            if n % (_G.JAF_NoclipDrainBatch or 150) == 0 then RunService.Heartbeat:Wait() end
        end end
    Workspace.DescendantAdded:Connect(function(p) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not p:IsA("BasePart") then return end
        _ncPending[p] = true
        if not _ncScheduled then _ncScheduled = true; task.delay(0.15, _ncDrain) end
    end)
    local function discover() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local out = {}
        local _wn = 0
        local function walk(container, depth) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            for _, c in ipairs(container:GetChildren()) do
                if c:IsA("Model") or c:IsA("Folder") or c:IsA("BasePart") then
                    if c:FindFirstChildOfClass("Humanoid") then
                    else
                        local cls = classify(c.Name)
                        if cls == "avoid" then
                        elseif cls == "noclip" then
                            out[#out + 1] = c
                        elseif depth < (_G.JAF_NoclipDepth or 5) then
                            walk(c, depth + 1)
                        end end end
                _wn = _wn + 1
                if _wn % (_G.JAF_NoclipWalkBatch or 200) == 0 then RunService.Heartbeat:Wait() end
            end end
        pcall(walk, Workspace, 0)
        _targets = out
    end
    local _noclipBoot = tick()
    while true do
        if _G.JAF_NoclipMachines and SAB_LIVE() then
            local _every = (tick() - _noclipBoot < 12) and 1 or (_G.JAF_NoclipDiscoverEvery or 3)
            if tick() - _lastDiscover > _every then _lastDiscover = tick(); discover() end
            for _, m in ipairs(_targets) do
                if m.Parent then
                    if m:IsA("BasePart") then
                        if m.CanCollide then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
m.CanCollide = false end) end
                        if m.CanQuery  then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
m.CanQuery  = false end) end
                    else
                        for _, p in ipairs(m:GetDescendants()) do
                            if p:IsA("BasePart") then
                                if p.CanCollide then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
p.CanCollide = false end) end
                                if p.CanQuery  then pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
p.CanQuery  = false end) end
                            end end end end end end
        task.wait(_G.JAF_NoclipApplyEvery or 0.3)
    end end))
do
    local HUB_TOP = 1500000000
    local function _isHubGui(n) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        return n:find("^JustAFan") or n:find("^JAF") or n:find("^Xi")
            or n:find("AutoSteal") or n == "PriorityListGUI"
            or n:find("Sammy") or n:find("[Dd]awg")
    end
    local function _raise(container) LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not container then return end
        for _, g in ipairs(container:GetChildren()) do
            if g:IsA("ScreenGui") and _isHubGui(g.Name) and g.DisplayOrder < HUB_TOP then g.DisplayOrder = HUB_TOP end
        end end
    local function _coreGui() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        local ok, cg = pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
return (gethui and gethui()) or game:GetService("CoreGui") end)
        return ok and cg or nil
    end
    local function _sweep() LPH_ATTRIBUTES(ERROR_HANDLING(false))
_raise(PlayerGui); _raise(_coreGui()) end
    _sweep()
    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
PlayerGui.ChildAdded:Connect(function(c) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if c:IsA("ScreenGui") then task.wait(); _raise(PlayerGui) end end) end)
    pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
local cg = _coreGui(); if cg then cg.ChildAdded:Connect(function(c) LPH_ATTRIBUTES(ERROR_HANDLING(false))
if c:IsA("ScreenGui") then task.wait(); _raise(cg) end end) end end)
    task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
while true do task.wait(2); _sweep() end end)
end
do
    local _capsUp = false
    local function _publish() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if _capsUp then return end
        for k, v in pairs(_SABCAP) do _G[k] = v end
        _capsUp = true
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if Config.AntiRagdollV2 ~= false and not antiRagV2On then startAntiRagdollV2(true) end
        end)
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            if Config.TpSettings.InfiniteJump and not Connections.infiniteJumpConnection then setInfiniteJump(true) end
        end)
        _G.JAF_HpShield = nil
    end
    local function _teardown() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
        if not _capsUp then return end
        for k, v in pairs(_SABCAP) do if rawequal(_G[k], v) then _G[k] = nil end end
        _capsUp = false
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            State.carpetSpeedEnabled = false; carpetSpeedEnabled = false; _G.JAF_CarpetSpeedActive = false
            if Connections.carpetSpeedConnection then Connections.carpetSpeedConnection:Disconnect(); Connections.carpetSpeedConnection = nil end
        end)
        pcall(function() LPH_ATTRIBUTES(VM(NONE), ERROR_HANDLING(false))
            State.infiniteJumpEnabled = false; infiniteJumpEnabled = false
            if Connections.infiniteJumpConnection then Connections.infiniteJumpConnection:Disconnect(); Connections.infiniteJumpConnection = nil end
            if Connections._ijInputBegan then Connections._ijInputBegan:Disconnect(); Connections._ijInputBegan = nil end
            if Connections._ijInputEnded then Connections._ijInputEnded:Disconnect(); Connections._ijInputEnded = nil end
            if Connections._ijYRemove then pcall(Connections._ijYRemove); Connections._ijYRemove = nil end
        end)
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_FloatTeardown then _G.JAF_FloatTeardown() end end)
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if SharedState and SharedState.DisableStealSpeed then SharedState.DisableStealSpeed() end end)
        _G.JAF_DropFlingActive = false
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if stopAntiRagdollV2 then stopAntiRagdollV2() end end)
        _G.JAF_HpShield = false
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.invisibleStealEnabled then _G.toggleInvisibleSteal() end end)
        pcall(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
if _G.JAF_ToggleAutoBuy then _G.JAF_ToggleAutoBuy(false, true) end end)
    end
    if SAB_LIVE() then _publish() end
    task.spawn(function() LPH_ATTRIBUTES(ERROR_HANDLING(false))
        while true do local ok, live = pcall(SAB_LIVE) if ok and live == true then _publish() else _teardown() end task.wait(0.25) end
    end) end